# readonly_observer_pipeline

Canonical home for the Bybit readonly observer pipeline scripts.

Policy:
- Real implementation files live here.
- Legacy entrypoints under `program_code/exchange_connectors/bybit_connector/scripts/`
  are compatibility wrappers only.
- Runtime paths and external operator habits remain backward-compatible during migration.
