"""REF-20 replay/ subpackage tests.

REF-20 replay/ 子套件測試。

This package houses pytest modules for the REF-20 Paper Replay Lab
Python sibling helpers (manifest signer, embargo validator,
calibration gate, ...). Tests are pure-Python unit tests with no
real PostgreSQL or HTTP server dependency — they execute identically
on Mac dev and Linux CI.

本 package 收 REF-20 Paper Replay Lab 各 Python 鏡像 helper（manifest
signer、embargo validator、calibration gate 等）的 pytest 模組。測試
為純 Python unit test，不依賴真 PostgreSQL 或 HTTP 伺服器，Mac dev 與
Linux CI 同行為。
"""
