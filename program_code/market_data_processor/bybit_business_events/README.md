# bybit_business_events

This directory stores the real business-events implementation files migrated from the
legacy flat script layout.

During transition, legacy flat entry files under:

program_code/exchange_connectors/bybit_connector/scripts/

are kept as compatibility wrappers so old paths and old shell calls continue to work.
