---
date: 2026-04-08
type: session-progress
scope: ARCH-RC1 1C-3-D — Python RiskManager 空殼化主體
---

# 1C-3-D Main Body — Python RiskManager 收編完成

## 完成項

**`risk_manager.py` 1633 → 53 行**（-1580 / -97%）
- 只保留兩個對外符號：
  - `REGIME_TIME_MULTIPLIERS` 常量（bridge_stats / test_winrate_param_fixes 仍消費）
  - `RiskManager(RiskViewClient)` 薄子類，建構不需 ipc_client，所有 deprecated 行為走 RiskViewClient 內建 no-op stub
- 7 套並行風控系統 → 1 套（Rust ConfigStore 唯一權威，Python 只剩此 53 行 shim）

**`paper_trading_wiring.py` 收編**
- 移除 `RISK_MANAGER.set_portfolio_risk_control(PORTFOLIO_RISK_CONTROL)`
- 移除 `RISK_MANAGER.set_governance_hub(GOV_HUB)`
- 移除 `RISK_MANAGER.set_change_audit_log(CHANGE_AUDIT_LOG)`
- `PORTFOLIO_RISK_CONTROL` 模組級 singleton 仍保留供其他 consumer

**測試清理（approach A: aggressive cull）**
- 刪除 9 檔總計 ~6900 行純 Python 風控/H0/Engine 測試（邏輯已 100% 在 Rust 748 tests 覆蓋）：
  - `test_risk_manager.py` (1494)
  - `test_risk_manager_edge.py` (174)
  - `test_h0_gate.py` (1276)
  - `test_h0_gate_cooldown_integration.py` (268)
  - `test_paper_trading_engine.py` (989)
  - `test_paper_trading_engine_inverse.py` (678)
  - `test_trailing_stop_cost_constraint.py` (301)
  - `test_integration_phase5.py` (648)
  - `test_integration_phase8.py` (378)
- `conftest.py` 移除 `paper_engine_with_risk` / `risk_manager` / `global_risk_config` / `category_risk_config` 4 個 fixtures
- `test_integration_phase2.py::test_portfolio_risk_control_injected` 重寫為 `test_portfolio_risk_control_present`（驗證 wiring singleton 仍存在，不再驗證 RiskManager 注入）

## 規模

13 files changed · **+46 / -7882**（淨 -7836）

## E4 Regression

**Python tests**: `2944 passed / 22 failed / 1 skipped`
- 22 failures **與 baseline 完全一致**（已 stash + re-run baseline 對照驗證）：
  - 19× `test_grafana_data_writer.py` — 既有 mock spec/_write_pnl 環境問題
  - `test_paper_trading.py::test_session_start_via_api` — 既有 401 auth 問題
  - `test_symbol_category_registry.py::test_is_stale_initially` — 既有
- **0 regressions caused by 1C-3-D**

**Rust**: 未動 Rust 檔案；上次 1C-3-D 開頭兩個 micro-commit (f8772c0 / a1cf772) 後 engine lib **748** 持續綠燈。

## Smoke check

```python
from app import paper_trading_wiring
type(paper_trading_wiring.RISK_MANAGER).__name__       # "RiskManager"
type(paper_trading_wiring.RISK_MANAGER).__mro__[1].__name__  # "RiskViewClient"
```

## 留待 1C-3-E / 1C-4

- `paper_trading_engine.py` 內 ~15 個 `engine.risk_manager.X` 死路徑（`ENGINE = None` since RC-10）— 屬於 disabled engine 自身的清理，不在 1C-3-D scope
- `bridge_core.py:294` `self._engine.risk_manager._price_tracker` 死引用（同上）
- 6 個 1C-3-C skipped `TestRiskRoutes` 測試重寫
- `PAPER_STORE.mutate` 拆分（session_halted 從 Rust snapshot 派生）
- Governor tier override cooldown PG 持久化（live 前必須）

## 風控收編軌跡（最終）

```
1A 前：     Python RiskManager (1633) + 6 套並行 = 7 套
1A 後：     Python RiskManager (1633) + 4 套（刪 3 套確認死碼）
1C-1 後：   Python RiskManager (1633) + 1 套權威（RiskConfig 13 sub-struct）
1C-3-A/B/C：RiskViewClient (~300) 取代 RiskManager + 路由切換
1C-3-D：    RiskManager → 53 行 shim 子類（本 commit）→ Config 一統
```
