# docs/audits

> **DATED AUDIT REPORTS / REFERENCE**
>
> This directory contains general dated audit and verdict reports. It is an
> evidence library, not the active queue.

Use root `TODO.md` for current blockers, owners, gates, runtime evidence, and
next actions. Use `docs/audit/` for the legacy 62-finding audit bundle. Use
`docs/governance_dev/audits/` for governance-development audit evidence.

