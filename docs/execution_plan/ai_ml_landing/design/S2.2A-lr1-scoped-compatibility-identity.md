# S2.2A — LR1 Scoped Compatibility Identity (Design)

**Status**: SOURCE-LANDED (source-only, `NONE`-effect). Session S2.2A / LR1.
Branch `agent/aiml-s2-2a-lr1-compat`. No production PG / runtime / engine /
broker / order / network / systemd effect. No `TODO.md` / `PROGRESS.md` /
`memory/` / `requirements-ml.txt` mutation. Git owned by PM.

This design replaces whole-repo-HEAD liveness for the ALR scanner-snapshot
event consumer with a positive `learning_runtime_digest` computed over only the
learning-relevant source surface, and threads that identity through three bind
points (preflight, spawn, finalize).

---

## 1. The defect

`alr_event_consumer.py::run_event_consumer` used to call
`verify_runtime_source_head(source_head, ...)` before any DB use. That helper
runs `git rev-parse HEAD` and does an **exact string compare** against the
pinned `source_head`, raising `source_head_mismatch` and refusing the **entire**
event consumer on any difference.

Because the consumer drives all Scanner-snapshot ingestion, **any docs-only
commit moves HEAD and stops ingestion.** Whole-repo HEAD is far too broad a
liveness key: it couples raw evidence capture to every unrelated change
(README, unrelated modules, other lanes).

## 2. The manifest — `learning_runtime_manifest_v1`

New SSOT module `program_code/ml_training/learning_runtime_manifest.py` builds a
positive, frozen-allowlist manifest. Identity helpers are **reused** from
`aiml_gate_receipt_validator.py` (`canonical_digest`, `artifact_self_digest`);
no new hashing abstraction is introduced.

```
schema_version = "learning_runtime_manifest_v1"
generated_at_utc                      # wall clock; NOT hashed into identity
repo_source_head    (40-hex)          # TELEMETRY ONLY; never in any component digest
capture_contract  { digest, inputs:{<root-rel path>:<sha256hex>},
                    snapshot_feature_schema_version }
training_contract { digest, components:{
                      learning_code_digest, migration_fingerprints{V151..V160},
                      feature_contract_digest, label_contract_digest,
                      action_policy_digest, dependency_lock_digest,
                      runtime_config_digest } }
boundary = "source_only_none_effect"
self_digest                           # == learning_runtime_digest
```

Per-file content hash = `sha256(path.read_bytes())`, paths root-relative and
sorted. Component/identity digests use `canonical_digest` (`sha256:` + canonical
JSON). **`self_digest` (the `learning_runtime_digest`) is computed over only
`{schema_version, boundary, capture_contract.digest, training_contract.digest}`**
— deliberately excluding `generated_at_utc` and `repo_source_head`, so it is
stable across docs-only commits and across regenerations. This is the key that
makes "docs-only never stops ingestion": docs-only moves only `repo_source_head`
(telemetry), never the component digests, so `self_digest` is unchanged.

### Frozen input allowlist (settled)

- **CAPTURE** (minimal, so ingestion keeps running):
  `alr_consumer_repository.py`, `alr_freshness_runtime.py`,
  `alr_persistence_repository.py`, `alr_scanner_snapshot_adapter.py`, plus the
  snapshot feature-schema id `alr_scanner_novelty_features_v1`.
  *Deviation vs prompt*: the raw scanner-cycle persistence lives in
  `alr_persistence_repository.py` + `alr_freshness_runtime.py` (real code), not
  in `alr_operational_repository.py`. Only `alr_event_consumer.py` (multi-role
  orchestrator, edited this Session) is **intentionally excluded** from every
  allowlist to avoid self-invalidation and role bleed;
  `alr_operational_repository.py` is fit-side projection persistence and is
  placed in **TRAINING** (its drift quarantines fit).
- **TRAINING** (drift ⇒ quarantine): the 4 `_REQUIRED_MODULE_HASHES`
  (`pit_dataset_manifest.py`, `quantile_trainer.py`,
  `run_training_pipeline.py`, `model_registry.py`);
  `alr_challenger_training_contract.py`,
  `alr_challenger_training_result_contract.py`,
  `alr_challenger_fit_capture_attestation.py`, `alr_candidate_policy.py`,
  `alr_candidate_learning_arbiter.py`, `alr_candidate_learning_projection.py`,
  `alr_operational_repository.py` (fit-side projection/statistical-run
  persistence); the `program_code/learning_engine/` gates `dsr_gate.py`,
  `pbo_gate.py`, `promotion_gate.py`, `residual_alpha_gate.py`.
  - `migration_fingerprints` = per-file sha256 of `sql/migrations/V151..V160`
    (exactly those 10; the build asserts the span has no gaps).
  - `feature_contract_digest` = `canonical_digest` over
    `compute_feature_schema_hash(EDGE_P3_FEATURE_NAMES)` +
    `EDGE_P3_FEATURE_SCHEMA_VERSION` (imported from `parquet_etl.py`).
    *Accepted limitation*: this binds the feature **name set + schema version**,
    not the `parquet_etl` feature-computation logic (window/formula bodies). A
    change to how a feature is computed without a name/version bump would not
    move `feature_contract_digest`; `parquet_etl.py` itself is not in the
    learning-code allowlist. Deeper feature-compute binding is deferred (LR2/
    S2.3 scope).
  - `label_contract_digest` = `canonical_digest` over the pit label-lineage
    required fields (`label_schema_hash`, `label_config_hash`,
    `outcome_cutoff_ts`; mirrors `pit_dataset_manifest.py:316-330`) + the whole
    `regime_oos_label_contract.py` file sha256. *Deviation*: whole-file hash of
    the regime contract (robustly covers its `SCHEMA_VERSION`) is used instead
    of importing/executing that research module at runtime preflight.
  - `action_policy_digest` = `canonical_digest` over the policy template
    sha256 + the frozen policy-config key surface (mirrors
    `alr_candidate_policy._POLICY_KEYS`).
  - `dependency_lock_digest` = `sha256:` + sha256(`requirements-ml.txt`). This
    is the **spec text** — acceptable for LR1 identity. The true sealed lock is
    LR2/S2.3 and is **not** built here.
  - `runtime_config_digest` = `canonical_digest` over the policy template
    sha256 + the arbiter runtime constants read from the template
    (`algorithm_version`, `tie_break_version`, `q18_scale`, `thresholds`,
    `cooldown_seconds`, `unknown_portfolio_penalty`). systemd unit/env is **not
    in-repo** (an S2.4 effect) and is deliberately not covered.

### Real digests on the final S2.2A worktree state

Regenerated by `build_source_compatibility_receipt()` against the final
post-review-fix worktree (the emitted receipt reproduces from this state; a
drift-guard test enforces the match). `repo_source_head` is telemetry only
(pre-fix parent commit `1bfb7920` at regeneration time; the three digests below
are HEAD-independent).

| Field | Value |
|---|---|
| `learning_runtime_digest` (`self_digest`) | `sha256:6cf76b60a763035d26d0d4e9e0e6aa0aa8877d99966367c778420e5f63a79595` |
| `capture_contract_digest` | `sha256:1e406a6e4d84e5cecebe565dc1314122f16fae1b13bd1bcf0b4c5cc4a8f397e7` |
| `training_contract_digest` | `sha256:d99484878264ec545577136fe49f02cf073df9ed86f7c56f7879b3b0be440d2d` |
| migration fingerprints | 10 (V151..V160, no gaps) |

## 3. The three bind points

All three bind the **same** immutable identity (identical `self_digest`).

1. **Preflight** — `alr_event_consumer.py::run_event_consumer` (via
   `_preflight_source_compatibility`). This is the **single authoritative
   evaluator path**: it builds the actual checkout manifest, loads the reviewed
   *expected* manifest from the in-repo pinned `source_compatibility_receipt_v1`
   (`_load_expected_compatibility_manifest`, default path overridable via
   `--expected-compatibility-receipt` / `ALR_EXPECTED_COMPATIBILITY_RECEIPT`;
   the receipt is fail-closed re-validated through `validate_aiml_artifact` and,
   when `--expected-learning-runtime-digest` /
   `ALR_EXPECTED_LEARNING_RUNTIME_DIGEST` is supplied, authenticated against it),
   then calls the one authoritative `evaluate_compatibility(expected, actual)`.
   **Capture stops iff `capture_status != COMPATIBLE`** — so a genuine
   capture-surface incompatibility (or an unavailable/forged expected receipt,
   or a build failure ⇒ `INDETERMINATE`) fail-closed stops ingestion, while a
   training-surface drift yields `fit_status != COMPATIBLE` ⇒
   `fit_quarantined=True` with capture still running (matches §4 exactly). The
   scalar `evaluate_runtime_digest_pin` helper was **retired** (no divergent
   second evaluator remains). `verify_runtime_source_head` is **kept but
   demoted** to a non-fatal telemetry helper (`_telemetry_source_head_match`);
   its exact contract and its direct test stay green.
2. **Spawn** — `alr_challenger_training_contract.py::build_alr_challenger_training_contract`.
   `learning_runtime_digest` is a required `code_manifest` field; when the new
   `expected_learning_runtime_digest` pin is supplied it must equal it, else
   `learning_runtime_digest_mismatch` refuses spawn.
3. **Finalize** — `alr_challenger_fit_capture_attestation.py`
   (`_normalize_actual_training_inputs`). The runner's **attested**
   `code_manifest_material` is JSON-parsed and its `learning_runtime_digest` is
   asserted equal to the spawn admission's `code_manifest.learning_runtime_digest`
   **before** the generic material byte-check, so an attested-material drift of
   that field raises the specific `actual_learning_runtime_digest_mismatch`
   (full byte-equality still backstops via `actual_code_manifest_material_mismatch`).
   `learning_runtime_digest` also travels through `expected_training_inputs`
   (result contract, mirroring `source_head`), where the trusted-fit **handshake**
   independently validates its digest format (`learning_runtime_digest_invalid`).

## 4. Quarantine semantics

- **Capture** continues unless the capture surface is itself incompatible
  (`INCOMPATIBLE`) or the manifest cannot be built (`INDETERMINATE`,
  fail-closed). Docs-only ⇒ capture keeps running.
- **Fit / promotion** is quarantined (`QUARANTINE`) whenever the training
  contract digest drifts from the reviewed pin, or the manifest is
  indeterminate. Quarantine fences the fit-triggering candidate-projection
  transition in the consumer loop, and refuses spawn / finalize.
- **Deny-by-default**: any build error — missing allowlisted input, symlink,
  non-regular file, unreadable — ⇒ `INDETERMINATE` on **both** capture and fit.

## 5. Receipt

`source_compatibility_receipt_v1` (schema
`program_code/ml_training/schemas/aiml_gate_receipts/source_compatibility_receipt_v1.schema.json`,
registered in `aiml_gate_receipt_validator.SCHEMA_FILES`). The validator adds
identity cross-checks: receipt `self_digest`, `learning_runtime_digest ==
manifest.self_digest`, and `capture/training_contract_digest` +
`migration_fingerprints` all bind the embedded manifest. The emitted
`SOURCE_READY` instance is
`docs/execution_plan/ai_ml_landing/receipts/S2.2A-source-compatibility-receipt-v1.json`,
generated by running `build_source_compatibility_receipt()` against this
checkout (every digest is real) and passing `validate_aiml_artifact`.

## 6. Test plan

- `program_code/ml_training/tests/test_learning_runtime_manifest.py` (tmp_path
  fake repo): docs-only no-stop; learning-code flip ⇒ fit QUARANTINE / capture
  COMPATIBLE; capture-code flip ⇒ capture INCOMPATIBLE; feature/label/action-
  policy flip ⇒ QUARANTINE; preflight==spawn==finalize identical `self_digest`
  + tamper detection; V151-V160 fingerprint byte-flip; fail-closed
  missing/symlink ⇒ INDETERMINATE on both; receipt round-trip + tamper; **inner
  input/component forgery reject** (corrupt inner `capture_contract.inputs` /
  `training_contract.components`, reseal only outer `self_digest` ⇒ validator
  recompute rejects); **committed-receipt drift guard** (rebuild the manifest
  from the real checkout and assert the committed receipt's three digests
  match).
- `tests/structure/test_learning_runtime_manifest_source_static.py`: no order /
  PG-write / Bybit / fetch / git-mutation tokens; frozen allowlist projection
  sha256; migration span V151..V160; allowlist references real files.
- `program_code/ml_training/tests/test_alr_event_consumer.py`: docs-only
  HEAD-differs does **not** stop capture; **capture-surface incompatibility
  stops capture**; **training drift quarantines fit while capture continues**;
  build failure and missing/forged expected receipt stop capture (fail-closed);
  the demoted `verify_runtime_source_head` telemetry helper stays green.
- Firing negative tests for the three bind edges: spawn
  `learning_runtime_digest_mismatch`, finalize
  `actual_learning_runtime_digest_mismatch` (attested code-manifest material
  drift), handshake `learning_runtime_digest_invalid`.

## 7. Scope guards

- **No lockfile here.** `dependency_lock_digest` hashes the `requirements-ml.txt`
  spec text; the sealed lock is LR2/S2.3.
- **runtime_config = in-repo only.** systemd unit/env is an S2.4 effect and is
  not covered.
- **V151..V160 exact** (span asserted; no gaps).
- **Source-only, `NONE`-effect.** S2.2B issues the runtime DONE (operational
  wiring: unit env `ALR_EXPECTED_LEARNING_RUNTIME_DIGEST`, real ingestion
  observation). This Session lands source + tests + receipt only.
