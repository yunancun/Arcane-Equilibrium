# AI/ML Landing Progress Ledger

**Program**: `AIML-LONG-LIVED-LANDING-V2`
**Ledger version**: 17
**Updated**: 2026-07-30
**Overall state**: `PROGRAM_ADOPTED` · **`S1_CLOSED`** · every S2 effect-session
source-seam predicate is narrowly `SOURCE_READY` (S2.0 + S2.1 + S2.2A + S2.3 +
S2.4 + S2.5 + S2.2B), while Sprint 2 remains
**`S2_EFFECT_EXECUTION_READINESS_ACTIVE`**. The nine effect-readiness split
packages are **5/9 source-landed**: `S2E.0`, `S2E.1`, `S2E.2a`,
`S2E.2b-1`, and `S2E.3`. The **only current entry is `S2E.2b-2`**; the
remaining order is `S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5`.
`S2-operator-action-packet-v1.md` remains `DRAFT_NOT_EXECUTABLE`: the real
S2.5 host/recovery lane, S2.4 row drivers and kernel amendments, authenticated
runtime capture/closure/attestor lane, and disposable full-DAG rehearsal remain
source work. All landed readiness packages were `NONE`-effect: no
runtime/PG/broker/order effect occurred and nine authorities stay false.
**S2 is NOT `S2_CLOSED`**: production effect progress is **0/6**. Linux
observation at
2026-07-28T15:55:23+02:00 found both candidate learning units
`not-found/inactive/dead`, and both canonical AIML install/state roots absent.
Nine authorities remain false; no production effect may start before the S2E
source waves close and a newly emitted packet receives fresh exact authority.
The 2026-07-30 calibration checkpoint closes only the launch-gate review debt:
signed external single-use bootstrap authority now survives local
state/anchor/lock reset without permitting an unsigned or cross-candidate
successor; governed pytest is reconstructed from exact reviewed-head,
hash-pinned Git wheel blobs; the closure schema requires its provider identity;
the review manifest includes transitive repo-local Python dependencies and every
tracked package-parent `__init__.py` pytest can execute; and the W0 capture
default is 600 seconds after the 210-test profile left two tests incomplete at
the old 300-second bound. This checkpoint issues no W0/LW1 receipt,
does not satisfy `S2E_2B_2A_SECURITY_RECOVERY_READY`, does not unlock LW2, and
does not change the 5/9 package projection or production effect 0/6.
A fresh Linux run at H_effect
`6e1ea957af35544a844f704978366d11aa6c2364` passed all eight target-host seams
with `binding=BINDING` and zero residue; the complete `closure_packet_v1`
passed trusted finalization under the operator SSHSIG for fingerprint
`SHA256:uGJ9veN7PoE6BBgfsSP2aiMndrwgbt7o/7/YfdzNzCQ`. Direct Codex exact-head
closeout on `da8e54148a60fc7be38fe5844cf85b28b293a044` found
P0/P1/P2=`0/0/0`; all exact-head CI, CodeQL, schema-consumer, governance and
IBKR checks passed; unresolved review threads and open code-scanning alerts
were both zero. PR #115 then merged exactly as
`22876b16d3b00fcaafa4f2f46ae02b1c08c60b3b`.
The external S3 Object-Lock **effect** is S8.6, not an S1 blocker.
The exact-head adversarial pass repaired five more P1s before this emission:
the child now requires an operator SSHSIG over the exact intent/source head
instead of accepting a self-created checksum capsule; both preflight and
postcheck require the exact signed governed observer command and complete
semantic stdout rather than a generic capture or same-process claim; closure
freshness uses the signed bundle `issued_at`; and every mandatory reviewer must
cite the authenticated wave that owns its exact fragment. Independent
`ssh-keygen -Y verify`, immediate finalization and historical replay at the
signed instant all pass with zero errors. The required governance CI selection
now includes the two-phase run-driver suite. The final Linux gate exposed and
closed one additional publication blocker: the intentionally large inline
Context test harness exceeded Linux `ARG_MAX` through `node -e`; commit
`6e1ea957a` feeds it through stdin and raises the complete governance job
ceiling from 10 to 20 minutes without changing production behavior or
authority.
**Adopted source generation**: reviewed head
`1a933fcc28e9f7341e023b5d401c479957c14c5f`, merged as
`fed223bebd278c50b0ab3330980e66441a30c9ed`
**Program-adoption receipt**:
`docs/execution_plan/ai_ml_landing/receipts/S0.3-program-adoption-receipt-v1.json`
(`sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b`)
**Attested finalization evidence**:
`docs/execution_plan/ai_ml_landing/receipts/S0.3-program-adoption-finalization-attestation-v1.json`
plus `.sig`, and `S0.3-trusted-execution-bundle-v1.json` plus `.sig`; both
signatures verify against the adopted source trust root.
**S1 source provenance**: branch `agent/aiml-s1-landing` atop main `5362fdd4b`;
six source-complete session commits — S1.1 `3c0b7fb2f`, S1.2 `d12f84632`,
S1.4 `7d0befd86`, S1.3 `08789982e`, S1.5 `0b2805a4d`, S1.6 `0e8e9fd9d` — each
passing its per-session role chain (E2/E4/CC/E3/OPS/QA + fix rounds) plus a
cross-session review round (PA integration / CC whole-body / FA spec-compliance
all PASS). Those commits are ancestors of PR #115's H_effect.
**Next gate**: execute only the remaining source-readiness sequence
`S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5`; do not reopen the five completed
packages. Only after all nine split packages close may PM regenerate a
current-head `BLOCKED_OPERATOR_ACTION_PACKET_READY`.
The production effect DAG remains
`S2.0→S2.4→S2.5A→S2.1→S2.5B→S2.2B` and then requires fresh exact Operator
authority per step.
**Canonical boundary**: S0 and S1 are closed. S1 has committed real disposable
S1.5 evidence, a platform-attested Linux S1.6 effect, a complete validated
closure packet, and a domain-separated operator SSHSIG. These prove the
Sprint-1 disposable effect seams and selected runtime mechanism; they do not
grant production runtime/build/PG/migration/deploy/ML5/ML6/broker/order
authority. All nine grants remain false and source adoption is not production
runtime readiness.

## Ledger Contract

`TODO.md` owns active priority. Immutable `session_attempt_v1` and
`closure_packet_v1`/Report Sink records own authoritative Session outcomes; this
file is their repo-resident projection and resume index. Update a row only with
the exact branch/PR/head and completion receipt. Preserve prior outcomes in the
row notes or append a dated ledger event; do not silently rewrite failed
evidence. A later Session consumes only valid, scope-compatible, hash-bound
dependency generations.

Allowed status:

```text
PLANNED | READY | IN_PROGRESS | SOURCE_READY | RECOVERY_REQUIRED | BLOCKED | BLOCKED_STALE_EVIDENCE | NOT_APPLICABLE_NO_CANDIDATE |
DONE | DONE_WITH_EXTERNAL_EVIDENCE_PENDING | SUPERSEDED
```

Required closure fields for a `DONE` row:

- `(session_id, landing_scope_id, cohort_epoch, attempt)` or `PROGRAM` template;
- `platform_scope`, `policy_surface_id`, exact covered decision cells and
  evidence-environment promotion edges;
- exact branch, reviewed head, PR/merge head and owned path manifest;
- route/DAG digest, explicit node IDs/classes/permissions/path ownership/
  predecessors and builder -> E2 -> E4 plus post-build semantic review;
- focused/local validation and CI classifier digest, selected workflows, exact
  head, invocation count and failure fingerprints;
- classifier-derived `required_effects`, Adapter ID, actor node, rollback and
  distinct postcheck node, including a typed `NONE` when no effect is possible;
- completion receipt hash, validity class, causal-time edges and next dependency.

Before work, W0 CAS-creates the attempt with owner lease epoch/expiry/heartbeat,
branch/worktree, baseline/checkpoint, path manifest and dependency generations.
Lease expiry moves to `RECOVERY_REQUIRED`; only CAS adoption/finalization can
resume or close it.

Class-specific invalidity recursively demotes all dependent attempts.
`CURRENT_STATE_TTL` expiry and explicit retroactive compromise/revocation do;
natural expiry of already consumed `EFFECT_TIME_AUTHORITY` does not invalidate
its `IMMUTABLE_CONSUMED_EFFECT`. `IMMUTABLE_LINEAGE` fails only on hash/causality
break. No generic scope template can be `DONE`; it must first be instantiated.

## Amendment A2 — Sprint 2 effect predicate split (source vs effect)

Per delivery-protocol Amendment A2, each S2 effect Session and the
runtime-`DONE` session S2.2B carries two mechanical predicates — `SOURCE_READY`
(all seam source built/reviewed/tested/disposable-PG-exercised, fail-closed
until an exact operator authorization; ledger status `SOURCE_READY`, nine
authorities false, no fixture may impersonate a platform-attested runtime PASS)
and `EFFECT_DONE` (the real operator-authenticated production apply; ledger
status `DONE`). Dependency qualifiers apply **per predicate**: a session's
`SOURCE_READY` predicate consumes an upstream `@SOURCE_READY`; its `EFFECT_DONE`
predicate — including S2.2B's runtime-`DONE` — consumes only an upstream
`@EFFECT_DONE` (so S2.2B's source work legitimately consumes
`S2.5@SOURCE_READY`, while its runtime-`DONE` consumes `S2.5@EFFECT_DONE`). The
effect **SOURCE** predicates are buildable now **in source-dependency order**
(the `source_deps` chain `S2.0→S2.1→S2.4→S2.5→S2.2B` is *sequentially eligible* —
a session's source predicate is eligible only once its `source_deps` upstreams
are `@SOURCE_READY` — not freely parallel), each routed and closed by a bound
work package under `P0-S2-PRODUCTION-EFFECT-SEAMS-SOURCE` (see the binding table
below). Only the serial operator-gated **apply** chain
`S2.0→S2.4→S2.5A→S2.1→S2.5B→S2.2B` (the §1.2-corrected effect DAG in the S2.4
install design — `S2.1`'s quiesce/restore drill runs *after* the S2.4 install
and the S2.5A initial start because a real drill needs the installed+started
runtime, so `S2.1@EFFECT_DONE` is **not** an `S2.4` predecessor; the
`source_deps` build order above is unchanged) stays blocked. A minimal
`BLOCKED_OPERATOR_ACTION_PACKET_READY` may be emitted only after the additional
S2E source-readiness waves below close; source-seam predicates alone are
insufficient.

| Session | source_deps (buildable now) | effect_deps (operator-gated apply — §1.2-corrected DAG) |
|---|---|---|
| S2.0 | `PROGRAM_ADOPTED` (+ landed S1 seams) | root — operator external-admin (DB-superuser-class) authorization |
| S2.1 | `S2.0@SOURCE_READY`, `S1.6` | `S2.4@EFFECT_DONE_INACTIVE`, `S2.5A@running_attested` (the quiesce/restore drill needs the installed+started system runtime) |
| S2.4 | `S2.0@SOURCE_READY`, `S2.1@SOURCE_READY`, `S2.2A@SOURCE_READY`, `S2.3@SOURCE_READY` | `S2.0@EFFECT_DONE` (+ intermediate three-side exact-head sync + landed program-DAG amendment; `S2.1@EFFECT_DONE` is **not** a predecessor) |
| S2.5 | `S2.4@SOURCE_READY` | two effect phases per §1.2: **S2.5A** initial start + running attestation consumes `S2.4@EFFECT_DONE_INACTIVE`; **S2.5B** final post-drill attestation consumes `S2.1@EFFECT_DONE` |
| S2.2B | `S2.2A@SOURCE_READY`, `S2.5@SOURCE_READY` | `S2.5B@EFFECT_DONE` (runtime-`DONE` only) |

**Work-package → session-source binding.** `P0-S2-PRODUCTION-EFFECT-SEAMS-SOURCE`
builds each effect session's `SOURCE_READY` seam through the standard
source-implementation route `PM → PA → E1 → E2 → E4 → CC/E3/OPS → QA → PM`
(isolated worktree/writer lease, exact-head PR/merge — identical governance to
the S2.2A/S2.3 source sessions). This binding makes the source work routable and
closable from the board:

| Work package | Builds the `SOURCE_READY` seam of | Status |
|---|---|---|
| WP1 | S2.2A/S2.3 source-debt closure (central registration + `_v2` identity) | DONE — PR #125 merge `6f2481c9` |
| WP2 | `S2.0` observer-bootstrap source Adapter | DONE — reviewed head `b831f3ff0`, PR #127 merge `e86e945bf`; `S2.0` exits `SOURCE_READY` |
| WP3 | `S2.1` quiesce/evidence/static-guard seam | DONE — reviewed head `5fe1fc11a`, final `c8e88c652` (adds `@file` CI fix + main merge), PR #129 merge `31ef0b4bc`; `S2.1` exits `SOURCE_READY` |
| WP4 | `S2.4` per-component install seams | DONE — W0–W5 six adversarial rounds; final head `029d2adfc`, PR #145 exact-head merge `e1b14b7d5` (tree byte-identical to `029d2adfc`); `S2.4` exits `SOURCE_READY` (PM declaration 2026-07-28; effect-side preconditions S2.4-AMEND-1/2 gate `S2.4@EFFECT_DONE`) |
| WP5 | `S2.5` running-attestation (one source package; two effect phases **S2.5A** initial start+attest / **S2.5B** post-drill attest per §1.2) + `S2.2B` `ingestion_compatibility_receipt_v1` | DONE at the narrow source-seam predicate — tranche 1/1b/2 across PR #148 merge `dab875882` and PR #150 merge `6be29043c`; this does not provide effect routing, host execution, runtime-attestor verification or an executable Operator packet |

**Effect-execution readiness work packages (current active sequence).**

Current projection: **5/9 complete** (`S2E.0`, `S2E.1`, `S2E.2a`,
`S2E.2b-1`, `S2E.3`); **only `S2E.2b-2` is ACTIVE**. Production effect
remains **0/6** and S2 is not closed.

| Work package | Scope | Status / exit |
|---|---|---|
| S2E.0 | S2.4-AMEND-1/2: dependency-refresh ingress/receipt/profile binding and plan-derived expected topology | SOURCE_LANDED (2026-07-28) — commit chain ending at `f30ede361` (AMEND commits + E2 P2 fixes + PR#153 Codex fixes) + round-9 receipt re-emission (carrier `3df9d2f45`): §9.2 refresh ingress wired into APPLY step (2b), terminal-receipt `dependency_refresh_digests`, §9.1 profile discriminators, plan-derived `expected_topology`; obligations 16/23/25/26 flipped to `CLOSED_BY_S2_4_AMEND_[12]_SOURCE`; PA design + E1 build + E2 (0 P0/0 P1, 2 P2 fixed, 7 mutations killed) + E4/CC/E3 PASS; full suite at the final head `f30ede361` measured 6341 passed/46 skipped/0 failed (at `cc6a8b97a` it was 6337/46 with exactly the two PM-owned projection discriminators red, closed by the round-8 emission + projection commit; the PR#153 Codex review then returned 3 findings fixed in `f30ede361` with a fourth adjudicated into S2E.3) |
| S2E.1 | Claim-gated routing, adapter selection, distinct OPS postcheck and closure binding for all six effect steps | SOURCE_LANDED (2026-07-29) — ten commits ending at `d6f7b585d`: nine per-phase claim-gated EFFECT routes (selector `s2_effect_adapter_selection_v1` + per-step exact claim inventory + class interlock + mandatory `authority` surface so the constitutional gate always joins the DAG), S2.2B identity-only registry adapter, and `agent_governance_s2_effect_binding.py` closure binding that delegates to the four pre-existing per-step hard gates. **Honest boundary:** transitive blocking leaves all nine steps unable to closure-PASS today. Three review rounds: CC FAIL → FAIL → **PASS**, E2 PASS_WITH_CONCERNS → 退回 → ACCEPTED, E4 PASS, E3 PASS. **Residual ownership is explicit:** `S2E.4` implements authenticated runtime command-capture candidates, signed-bundle kind + typed-observation support, `scope=runtime` closure representation, exact bytes/digest and signature coverage, verifier↔receipt/postcheck identity cross-binding, apply actor identity and central applier≠verifier enforcement for S2.4/S2.5/S2.2B, trusted-attestor SSHSIG identity/namespace/key/freshness/trusted-clock verification, and the missing S2.4 semantic branches. `S2E.5` must prove these end to end and reject caller/self-claimed capture, nonce, attestor, or schema substitution. |
| S2E.2a | Trusted-host kernel/observer + S2.0/S2.1 runners + disposable rehearsal | SOURCE_LANDED (2026-07-29) — PR#156 merge `5486a6183`. **Pure-addition wave: the four adapter modules have zero edits**, verified three ways (byte-identity, suite equality, and behavioural equality of the emitted receipt digests). PA showed the full S2E.2 scope was too large and that the S2.5 runner was the only real conflict with S2E.3, so PM narrowed it: S2.5 has no real-systemd disposable target and cannot have one (`unit_name` is a module constant and the driver deliberately has no caller unit parameter), S2.4's paths are likewise constants so its rehearsal can only reach `SOURCE_SIMULATION_PASS`, and only S2.0/S2.1 have a genuine throwaway-`initdb` target |
| S2E.2b-1 | S2.4 recovery core: POSIX file/lock drivers, reverse-order compensator, startup-reconcile injection | SOURCE_LANDED (2026-07-29) — PR#158 merge `521c813c5`, eleven commits. PA re-split S2E.2b on evidence that three of the five S2.4 row drivers each need a kernel hard-boundary change and T2 needs two more, so this wave is **recovery core with zero new host capability** |
| S2E.2b-2 | Real S2.5 host runner + authorized startup recovery | **ACTIVE — the only current entry** (unlocked by S2E.2b-1). Exit requires all of: (1) close the private-penetration guard's `ImportFrom` hole; (2) reject de-listing penetration through `runpy`/`imp`/`code`/`pdb`/`timeit`/`concurrent.futures`; (3) prevent nonce/self-claimed verifiers and discarded `verifier_node` identity from obtaining `COMPLETED_EXACT`; (4) formally adjudicate the 2004-line `aiml_gate_receipt_validator.py` under the 2000-line exception registry; (5) add an S2.5 state-root directory anchor/manifest so deleting the journal is detectable; (6) provide a lawful, authorized, replayable startup recovery/repair path — never manual file deletion; and (7) deliver the real S2.5 host runner, five-dimensional independent observer, S2.4 install + S2.5 lifecycle dual locks, closed kernel session, and CLI. Production authority absent still fails closed. |
| S2E.2b-3 | S2.4 five row drivers + the kernel capability amendments they each require + T2 tier | WAITING_S2E_2b_2. Each item is a hard-boundary amendment deserving its own adversarial round: `daemon-reload` is in the global `FORBIDDEN_ARGV_TOKENS`; `systemd-creds` needs an FD path and binary stdout that the kernel structurally cannot carry; `useradd` needs variable argv that a closed literal allowlist cannot express; T2 needs `--user` (also globally forbidden) plus env additions |
| S2E.3 | S2.5 F2/F3/F4/note-1: reconcile-under-hold, split lock API, typed release, journal integrity chain | SOURCE_LANDED (2026-07-29) — PR#155 merge `94a9f514b`. All four defects plus the deferred PR#153 Codex-4 finding closed. **F3 exposed something worse than the audit had found**: `S2_5FlockProbe` defaulted to `S2_5_LOCK_PATH`, so the `install_lock_free` flag had *never* probed the S2.4 install lock, and the resulting misattribution ("the S2.4 install lock is held" when a second S2.5 applier held the lifecycle lock) was fossilised in an existing test, which this wave inverted |
| S2E.4 | S2.2B remote-readonly observer; runtime capture/closure identity; runtime-attestor SSHSIG producer/verifier and executable route | WAITING_S2E_2b_3. Owns the S2E.1 residual source repairs enumerated above. Intake must also issue an explicit keep/split/exception size adjudication for the 2231-line `tests/structure/test_agent_governance_s2_effect_binding.py`; this W0 records but does not repair it. |
| S2E.5 | Disposable full-DAG positive + forgery-negative rehearsal; close/adjudicate all 28 ABI obligations; terminal packet gate | WAITING_S2E_4. Requires distinct applier/verifier proof; rejects caller/self-claimed capture, nonce, attestor and schema substitution; refuses packet regeneration while any S2E.4 capture/schema/identity/attestor condition is unresolved. GitHub CodeQL alert #95 must be truly fixed or evidence-adjudicated before the terminal packet. Only then regenerate a current-head Operator packet. |

The `Dependencies` column of the Current Sessions table below remains the
original edge list; this table is its source/effect projection. Until a
session's bound WP merges, its Current Sessions row keeps its original effect
route/status; on the WP's exact-head merge the row flips to `SOURCE_READY`, and
to `DONE` only at `EFFECT_DONE`. The effect (`EFFECT_DONE`) route in each row is
unchanged and stays operator-gated.

## Current Sessions

| Sprint | Session | Work package | Scope template | Dependencies | Required role route template | Status | Completion receipt | Required effect | Sync / CI policy |
|---:|---|---|---|---|---|---|---|---|---|
| 0 | S0.1 | Integrate current origin/main, preserve TODO union, publish V2 documents | PROGRAM | none | PM -> PA -> TW -> R4 -> QA -> PM | DONE | `docs/execution_plan/ai_ml_landing/receipts/S0.1-planning-documents-published-v1.json`<br>`sha256:8fc9417f984025deabdc1b83ace95921ccfff1acb26a1b29243fc0a0a5ba79ad` | `NONE` | PR #100 lineage: base `96d26245068cbfbc8d60e73fb8eb82c4109b0d40`, head `35b4d1e4091b7dc34af248f51f512f2d8d51e9b0`, merge `cfb3a4040ffb2974192c53609b72e7afba4a845d`; at S0.1 closure the reconciled Mac/GitHub/Linux source head was `c2f5a2e26e422d56b8ec9b540d7f36bea9a0be54`; merge remains an ancestor of the adopted generation |
| 0 | S0.2 | Accept ADR-0049/AMD serving/retraining/rollback/no-broker authority | PROGRAM | S0.1 | PM -> PA -> CC/E3 -> R4 -> QA -> PM | DONE | `docs/execution_plan/ai_ml_landing/receipts/S0.2-serving-authority-receipt-v1.json`<br>`sha256:0115dbd3dc62d84e183aae5a28cbfd252eb45ecee51a652d8a4a155f14dfb41a` | `NONE` | Accepted source-policy commit `f325b4dfdafd1979197c8a9e6450efeaf85e091c`; its immutable receipt was consumed by the completed S0.3 finalizer |
| 0 | S0.3 | Scope/attempt/effect governance, terminal-sink contract and GitHub admin attestation | PROGRAM | S0.1, S0.2 | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/MIT/R4 -> QA -> PM | DONE | `docs/execution_plan/ai_ml_landing/receipts/S0.3-program-adoption-receipt-v1.json`<br>`sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b`<br>producer-signed finalization attestation + trusted execution bundle in the same directory | `EXTERNAL_READONLY_ATTESTATION` | PR #104 source landing; PR #106 forge-resistance hardening merge `afa7eb2e97d6ab975709d4472b12f7397ee03bfb`; PR #107 live PR projection merge `0cdd3537ead94675a4d0033df5bbcbf5d33b1b16`; PR #108 Linux fixture repair reviewed head `1a933fcc28e9f7341e023b5d401c479957c14c5f`, merge `fed223bebd278c50b0ab3330980e66441a30c9ed`. Seven authenticated review fragments, git ancestry/blob verification, live GitHub policy attestation and Linux governed E4 `275/275` passed before trusted-host issuance; repo-resident SSHSIG evidence independently binds the PASS result to this receipt and lineage. |
| 1 | S1.1 | LR0A PG read-only identity Adapter | PROGRAM | PROGRAM_ADOPTED | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | DONE | source/disposable `pg_readonly_identity_receipt_v1`<br>branch `agent/aiml-s1-landing` @ `3c0b7fb2f`; evidence class `LOCAL_REPRODUCIBLE` (real 42501/28P01); closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`) | `DISPOSABLE_ONLY` | Migration/ACL CI if touched; no production PG |
| 1 | S1.2 | LR0B typed effects, governance wiring and terminal WORM sink Adapter | PROGRAM | PROGRAM_ADOPTED | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | DONE | effect-contract/governance/sink receipt (terminal WORM sink CONTRACT_ONLY -> implemented flip)<br>branch `agent/aiml-s1-landing` @ `d12f84632`; evidence class `LOCAL_REPRODUCIBLE`; closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`) | `DISPOSABLE_ONLY` | Protected/deploy workflow CI if touched |
| 1 | S1.3 | Host UID/PG role/auth/socket ACL/secret lifecycle contracts | PROGRAM | S1.1, S1.2 | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | DONE | `identity_acl_contract_receipt_v1`<br>branch `agent/aiml-s1-landing` @ `08789982e`; evidence class `LOCAL_REPRODUCIBLE`; closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`) | `DISPOSABLE_ONLY` | Disposable PG + migration/ACL CI |
| 1 | S1.4 | LR0C offline OCI vs fixed-path candidate proof | PROGRAM | PROGRAM_ADOPTED | PM -> PA -> E1 -> E2 -> E4 -> CC/OPS -> QA -> PM | DONE | runtime-candidate receipts: `runtime_candidate_receipt_v1` ×2 + `runtime_candidate_comparison_v1` (final_choice const null)<br>branch `agent/aiml-s1-landing` @ `7d0befd86`; evidence class `LOCAL_REPRODUCIBLE` (real python3 -I); closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`) | `DISPOSABLE_ONLY` | Runtime/build CI on stable head |
| 1 | S1.5 | Per-component Adapter plus remote/platform observation seam | PROGRAM | S1.1-S1.4 | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | DONE | `effect_seams_ready_receipt_v1` (`sprint_gate_scope` const `S1.5_CONTRIBUTION`; matrix `adapter_binding_status` flip)<br>branch `agent/aiml-s1-landing` @ `0b2805a4d`; evidence class `LOCAL_REPRODUCIBLE` (real os.replace/chmod apply/rollback/postcheck); closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`); final receipt `sha256:19498ba4…a37` | `DISPOSABLE_APPLY_ROLLBACK_POSTCHECK` | Stable-head deploy CI; no production apply |
| 1 | S1.6 | Target-host isolated disposable runtime probes and final choice | PROGRAM | S1.4, S1.5 | PM -> Adapter actor -> distinct OPS/CC/E3 -> QA -> PM | DONE | `learning_runtime_choice_receipt_v1` (final_choice `content_addressed_fixed_path`, BINDING; oci_selectable false; production_running_attested false)<br>branch `agent/aiml-s1-landing` @ `0e8e9fd9d`; evidence class `LOCAL_REPRODUCIBLE` (real python3 -I disposable probes); closed by authenticated S1 finalization + PR #115 exact-head merge (H_effect `6e1ea957a`, merge `22876b16d`); final effect `sha256:0a0d050b…a89b`, `binding=BINDING`, zero residue | `TARGET_HOST_DISPOSABLE_RUNTIME_PROBE` | Cleanup postcheck; no production running claim |
| 2 | S2.0 | Bootstrap production PG observer role/auth/ACL only | PROGRAM | EFFECT_SEAMS_READY | PM -> external admin Adapter -> distinct OPS/E3 -> QA -> PM | SOURCE_READY | **`SOURCE_READY`** (WP2): `pg_observer_bootstrap_adapter_v1` (registry `declared_production_apply_disabled_until_operator_sshsig`) — typed intent/result/postcheck/rollback schemas + structured-SQL-allowlist read-only observer role (role-level `default_transaction_read_only`/`search_path=pg_catalog`) + disposable-PG apply/rollback/postcheck proofs (real 42501/28P01); branch `agent/aiml-s2-wp2-s20-observer`, reviewed head `b831f3ff0`, PR #127 merge `e86e945bf`; 5-reviewer wave (E2/E4/CC/E3/OPS) + 4 adversarial delta/closure gates + 4 Codex P1/P2 all resolved. **Hash-bound proof**: the adapter IS the source deliverable, hash-bound by merge commit `e86e945bf`; the disposable apply/rollback/postcheck proof is `tests/structure/test_agent_governance_pg_observer_bootstrap_disposable.py` (real 42501/28P01 on a throwaway cluster), reproducible from that checkout — so `S2.0@SOURCE_READY` is validatable/invalidatable by checkout+rerun, with no standalone receipt artifact (unlike the NONE-effect S2.2A/S2.3, whose deliverable IS a receipt file). Evidence `LOCAL_REPRODUCIBLE` (59 offline+disposable; frozen classifier `sha256:1cf8c021…` unchanged, `PROGRAM_SCHEMA_PATHS` untouched); apply fail-closed `EXTERNAL_VERIFICATION_PENDING` until operator SSHSIG. **`EFFECT_DONE` pending** operator SSHSIG at the S2.0 EFFECT session | `PG_OBSERVER_BOOTSTRAP` | Exact intent; no migration/writer |
| 2 | S2.1 | LR0 evidence/quiescence/static guards | PROGRAM | S2.0, S1.6 | PM -> OPS/E3 -> E1 -> E2 -> E4 -> QA -> PM | SOURCE_READY | **`SOURCE_READY`** (WP3): `alr_quiesce_fence_adapter_v1` (registry `declared_production_fence_disabled_until_s20_effect_and_operator_sshsig`) — typed `quiesce_{intent,observation,result,rollback}_v1` schemas (centrally registered in `SCHEMA_FILES`) + owner/process/unit/cgroup/env/restart/watchdog/queue/credential inventory (`agent_governance_alr_quiesce_inventory.py`) that fences **only** a confirmed single ALR owner (C1 exact 10-token content-addressed invocation + `posixpath.normpath` anti-traversal guard, C2/C3 `candidate_pids==[main_pid]`) via the unit's own **system-level** `systemctl stop` (never pid/name/pattern) + `apply_quiesce_fence` with try/finally un-fence guarantee (typed `FAILED` on exception) + C4 TTL cross-field bound (`duration+sample_interval ≤ min(ttl, MAX_AUTHORIZATION_TTL=900)`); route CLASS with no effect-node injection; SSHSIG namespace `arcane-equilibrium-aiml-s2-quiesce-fence`; production fence fail-closed `PENDING`/`EXTERNAL_VERIFICATION_PENDING` until `S2.0@EFFECT_DONE` + operator SSHSIG. Branch `agent/aiml-s2-wp3-s21-quiesce`, reviewed head `5fe1fc11a`, final `c8e88c652`, PR #129 merge `31ef0b4bc`; 5-reviewer wave (E2/E4/CC/E3/OPS) + 4 Codex P1 + C4-TTL all resolved. **System-unit amendment (Blocker-1, PR #134 merge `e514f1e76`)**: per the operator's corrected S2.4 §8 this seam was realigned from the user-level `openclaw-alr-shadow.service`/`systemctl --user`/`alr_shadow` to the **system-level** `arcane-equilibrium-aiml-engine-scanner.service` (`User=aiml-engine-scanner`, content-addressed ExecStart, DB `trading_ai`, PG role `aiml_engine_scanner`, `LoadCredentialEncrypted` DSN, hardened) — the byte-for-byte unit identity WP4 §8 installs; C1 is now a 10-token content-addressed fingerprint with a `posixpath.normpath` anti-traversal guard. This satisfies the WP4-`SOURCE_READY` admission requirement "WP3 landed and aligned to the same system unit" (§11.2); together with the landed program-DAG amendment (Blocker-2, PR #132), **both WP4 admission blockers are cleared**. Evidence `LOCAL_REPRODUCIBLE` (86 offline+disposable, disposable ran on a throwaway cluster; frozen classifier `sha256:1cf8c021…` unchanged, `PROGRAM_SCHEMA_PATHS` untouched). **`EFFECT_DONE` pending** `S2.0@EFFECT_DONE` + per-adapter operator SSHSIG at the S2.1 EFFECT session | `QUIESCE_FENCE` | Typed intent; no general CI |
| 2 | S2.2A | LR1 scoped compatibility source implementation | PROGRAM | S1.6 | PM -> PA -> E1 -> E2 -> E4 -> QA -> PM | SOURCE_READY | `source_compatibility_receipt_v1` (`receipts/S2.2A-source-compatibility-receipt-v1.json`) self `sha256:a8fba423…`, `learning_runtime_digest sha256:6cf76b60…`, 10 V151-V160 fingerprints; branch `agent/aiml-s2-2a-lr1-compat`, reviewed `7054a3b0`, PR #121 merge `87a3a2503`; E2/E3 PASS + E4 PASS_WITH_CONCERNS; evidence `LOCAL_REPRODUCIBLE` (415/1 + 2259/36); receipt reproduces from checkout | `NONE` | Narrow Python local-first; exited SOURCE_READY. Runtime `DONE` is S2.2B |
| 2 | S2.2B | LR1 runtime revalidation of exact S2.2A manifest | PROGRAM | S2.5, S2.2A@SOURCE_READY | PM -> independent OPS/E4 -> QA -> PM | SOURCE_READY | **`SOURCE_READY`** (WP5 tranche 2, PM declaration 2026-07-28, PR #150 merge `6be29043c`): `ingestion_compatibility_receipt_v1` closed schema + central leaf + builder/CLI + fixtures anchored on the real persisted S2.2A receipt and a fully re-validated `s2_5_final_attestation_v1`; runtime-`DONE` still consumes `S2.5B@EFFECT_DONE` and remains the only row that may issue the LR1 runtime DONE | `REMOTE_READONLY` | Only row that issues LR1 runtime DONE |
| 2 | S2.3 | LR2 sealed immutable runtime build/trust chain | PROGRAM | S1.3, S1.6 | PM -> PA -> E1 -> E2 -> E4 -> CC/OPS -> QA -> PM | SOURCE_READY | `sealed_build_receipt_v1` self `sha256:169d2e6c…` (runtime_content `sha256:8b2092e8…`, closure `sha256:26307134…`, target `x86_64-unknown-linux-gnu`) + `expected_identity_receipt_v1` self `sha256:a08c6965…`; real `requirements-ml.lock` (38 pinned/0 unpinned); branch `agent/aiml-s2-3-lr2-sealed`, reviewed `73b083e9`, PR #122 merge `051df8262`; E2/E3/E4/CC/OPS + 2 Codex P2 + FA/CC final audit PASS; heavy `learning-runtime-sealed-build` CI job green (real offline install+import on Linux) | `NONE` | Runtime/build CI green; NOT a running attestation (S2.5/LR6). Central-validator registration = serialized follow-up |
| 2 | S2.4 | Credential/PG/unit/install effects and component restore | PROGRAM | S2.0, S2.1, S2.2A@SOURCE_READY, S2.3 | PM -> OPS preflight -> E3 -> Adapter -> distinct OPS -> QA -> PM | SOURCE_READY | **`SOURCE_READY`** (WP4/W5, PM declaration 2026-07-28, PR #145 merge `e1b14b7d5`): W0–W5 chain + eight persisted receipts bound `f30ede36134f1464cef4bc025f808b931aebe180` (carrier `3df9d2f451ceb7ab66117de6e33fd882d59a9531`, round 9, ledger digest `sha256:90f9261845b4782e577ef34e40369b3bcaf30381ce46d62d7246c087b3ed208e`) + ABI-pinned obligation ledger (28 rows) + discriminator projection regression; effect route unchanged and still gated — the S2.4-AMEND-1/2 **source** preconditions landed in S2E.0 (final head `f30ede361`, 2026-07-28: §9.2 refresh ingress in APPLY, terminal-receipt refresh digests, §9.1 profile discriminators, plan-derived `expected_topology`); `S2.4@EFFECT_DONE` still requires the remaining `S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5` chain and fresh operator authorization | `CREDENTIAL_PG_UNIT_INSTALL` | Intermediate exact-head sync first |
| 2 | S2.5 | Running attestation, watchdog-last recovery, observer/dead-man and rollback | PROGRAM | S2.4 | PM -> OPS/E3 -> E4 -> QA -> PM | SOURCE_READY | **`SOURCE_READY`** (WP5 tranche 1/1b/2, PM declaration 2026-07-28, PR #148 merge `dab875882` + PR #150 merge `6be29043c`): five closed s2_5 schemas + the replay-ledger schema, additive v3 classifier (frozen digests unchanged), lifecycle/attestation/driver leaves + CLI, three-state lifecycle fixtures, hold-style lock across the consume→persist window, §6 closure binding; effect route unchanged and still gated — `S2.5A/S2.5B@EFFECT_DONE` need fresh operator permits per the S2 operator action packet | `WATCHDOG_ROLLBACK_TEST` | Platform-attested; no CI without source change |
| 3 | S3.1A | LR3 durable queue/controller/worker source implementation | PROGRAM | S2.3 | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | PLANNED | queue source receipt | classifier-derived PG/service source | Migration/ACL/runtime CI; exits SOURCE_READY |
| 3 | S3.1B | LR3 runtime queue/controller/worker verification | PROGRAM | S2.5, S3.1A@SOURCE_READY | PM -> independent OPS/E4 -> QA -> PM | PLANNED | `queue_recovery_receipt_v1` | `REMOTE_READONLY` | Only row that issues LR3 runtime DONE |
| 3 | S3.2 | LR4 loss-aware Scanner gap/drop-SLO handoff | landing_scope_id instance | S2.5, S3.1B | PM -> PA -> E1 -> E2 -> E4 -> QC/OPS -> QA -> PM | PLANNED | loss-aware Scanner receipt | classifier-derived engine deploy | Rust CI if Scanner changes |
| 3 | S3.2A | Pre-filter universe/PIT/reason/choice/policy/RNG/propensity persistence | landing_scope_id instance | S3.2 | PM -> QC/MIT -> E1 -> E2 -> E4 -> QA -> PM | PLANNED | universe/selection receipt | classifier-derived PG/engine effect | Rust/migration CI as classified |
| 3 | S3.3 | LR5 physical retention/backpressure/deleter/restore | PROGRAM | S2.5, S3.1B | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | PLANNED | retention apply/per-object receipts | `RETENTION_APPLY_RESTORE` | Destructive fixtures before apply |
| 3 | S3.4 | LR6 faults, independent observer and 72h/two-cycle soak | landing_scope_id instance | S3.1B, S3.2, S3.2A, S3.3 | PM -> distinct OPS observer -> E4 -> QA -> PM | PLANNED | `foundation_ready_receipt_v1` | `FAULT_INJECTION_OBSERVE` | Platform-attested; no CI without change |
| 4 | S4.1 | ML0 scope/policy surface/cell coverage/environment/cohort baseline | landing_scope_id instance | FOUNDATION_READY | PM -> QC/MIT -> OPS read-only -> QA -> PM | PLANNED | `scope_cohort_receipt_v1` | `PG_READONLY` | No CI; LR0A only |
| 4 | S4.2 | ML1 full universe/propensity/exploration portfolio | landing_scope_id instance | S3.2A, S4.1 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E -> QA -> PM | PLANNED | `target_portfolio_receipt_v1` | `NONE` | Narrow Python local-first |
| 4 | S4.3 | ML2 bitemporal labels/proof/reward revisions | landing_scope_id instance | S4.1, S4.2 | PM -> E1 -> E2 -> E4 -> QC/MIT -> QA -> PM | PLANNED | `label_revision_receipt_v1` | classifier-derived PG effect | Migration CI if schema changes |
| 4 | S4.4 | ML2A loaded-row PIT matrix, revision IDs, feature vectors and parity | landing_scope_id instance | S4.3 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E -> QA -> PM | PLANNED | `pit_dataset_receipt_v1` | `NONE` | Rust CI if parity code changes |
| 5 | S5.1 | ML3 trusted host and qualified-only reproducible fit | landing_scope_id instance | S4.4 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E/CC/OPS -> QA -> PM | PLANNED | `trusted_fit_result_receipt_v1` | `FIT_ARTIFACT_REGISTRY_WRITE` | Runtime/build/serving CI |
| 5 | S5.2 | ML4 action authority lattice, holdout ledger and adversarial OOS | landing_scope_id instance | S5.1 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E -> QA -> PM | PLANNED | `oos_action_policy_receipt_v1` | `NONE` | Statistical fixtures; Rust CI if contract changes |
| 5 | S5.3 | ML5 qualified registry writer, legacy fail-close and shadow server | landing_scope_id instance | S5.2 + serving authority | PM -> E1 -> E2 -> E4 -> PA/QC/CC/OPS -> QA -> PM | PLANNED | registry/shadow receipt | `REGISTRY_SHADOW_ACTIVATION` | Migration/serving CI |
| 6 | S6.1 | Rust IntentProcessor + EdgePredictorStore consumer and ORT/runtime proof | landing_scope_id instance | S5.3 + serving authority | PM -> E1 -> E2 -> E4 -> PA/QC/CC/OPS -> QA -> PM | PLANNED | `real_consumer_receipt_v1` | `CONSUMER_CANARY_ROLLBACK` | Rust/serving CI |
| 6 | S6.2 | Consumer-ACK activation and legacy writer retirement | landing_scope_id instance | S6.1 | PM -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | PLANNED | single-path activation receipt | `MODEL_ACTIVATION_SWAP` | Migration/serving CI |
| 6 | S6.3 | ML6 mechanical harness plus optional economic causal design | landing_scope_id instance | S5.2, S6.2 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E/E3/BB -> QA -> PM | PLANNED | causal-design readiness receipt | `NONE` | Rust CI if policy code changes; no broker effect |
| 6 | S6.4 | Bounded Demo mechanical integration and optional economics | Demo promotion edge instance | S6.3 + fresh authority | PM -> E3 -> BB -> Adapter/runtime -> distinct OPS/QC -> QA -> PM | PLANNED | mechanical + optional economic receipts | `BYBIT_DEMO_POLICY_EFFECT` | Intermediate sync/runtime attestation first; no live |
| 7 | S7.1 | Natural controller G2 evaluation and ModelOps SLO/actions | landing_scope_id instance | S6.4 | PM -> E1 -> E2 -> E4 -> QC/MIT/AI-E/OPS -> QA -> PM | PLANNED | `generation_2_receipt_v1` | `NONE` or fresh gated model activation | New forward data; serving CI if changed |
| 7 | S7.2 | Encrypted off-host DR, clean restore, key/corruption/RPO/RTO | landing_scope_id instance | FOUNDATION_READY + platform components; trading branch also S7.1 | PM -> E1 -> E2 -> E4 -> CC/E3/OPS -> QA -> PM | PLANNED | `dr_restore_receipt_v1` | `ISOLATED_DR_RESTORE` | No production overwrite |
| 7 | S7.3 | Seven unattended days, independent observer and natural workload | landing_scope_id instance | S7.2 + platform components | PM -> distinct OPS observer -> QA -> PM | PLANNED | `continuous_operation_receipt_v1` | `OBSERVE_ALERT_TEST` | No CI unless source changes |
| 7 | S7.4 | ML9 causal-time landing candidate manifest | landing_scope_id instance | S7.1-S7.3 | PM -> QC/MIT/AI-E/CC/E3/OPS -> QA -> PM | PLANNED | `aiml_landing_manifest_v1` / candidate only | `NONE` | Current runtime receipts only; no terminal state |
| 7 | S7.NC | No-eligible-candidate platform candidate validator | landing_scope_id instance | FOUNDATION_READY, S3.2A, S4.2 no-eligible, S7.2, S7.3 | PM -> independent QC/OPS -> QA -> validator | PLANNED | `aiml_platform_no_candidate_candidate_v1` | `REMOTE_READONLY` | Candidate-only Sessions become N/A, never DONE; P1 remains open |
| 8 | S8.1 | Stabilize and freeze release-candidate head | PROGRAM + instantiated scopes | S7.4 or S7.NC | PM -> builders/reviewers as needed -> QA -> PM | PLANNED | release-candidate freeze receipt | classifier-derived | No publication until all fixes integrated |
| 8 | S8.2 | Exact-head full audit/review/required CI/admin-policy attestation | frozen candidate | S8.1 | PM -> PA/QC/MIT/AI-E/CC/E3/OPS/R4 -> QA -> PM | PLANNED | exact-head final audit receipt | `EXTERNAL_READONLY_ATTESTATION` | Any head change returns S8.1 |
| 8 | S8.3 | Exact-head merge | PROGRAM | S8.2 accepted | PM publication lane -> QA | PLANNED | merge receipt | `GIT_PUBLICATION` | Match reviewed head; no closure write pending |
| 8 | S8.4 | Final Mac/GitHub/Linux ff-only sync, four-head, deploy if digest changed | PROGRAM | S8.3 | PM -> OPS -> Adapter if required -> distinct OPS/QA | PLANNED | final source/deploy receipt | `SOURCE_SYNC` plus classified deploy | No force/reset/stash/clean |
| 8 | S8.5T | Trading AIML runtime attestation including registry ACK/loaded model | landing_scope_id instance | S8.4, S7.4 | PM -> independent OPS/E3/QC -> QA -> PM | PLANNED | `aiml_final_runtime_attestation_v1` profile TRADING | `REMOTE_READONLY` | Source sync is not runtime proof |
| 8 | S8.5NC | No-candidate platform attestation proving no model/promotion/consumer/order authority | landing_scope_id instance | S8.4, S7.NC | PM -> independent OPS/E3/QC -> QA -> PM | PLANNED | `aiml_final_runtime_attestation_v1` profile NO_CANDIDATE | `REMOTE_READONLY` | Platform/runtime health without trading evidence |
| 8 | S8.6 | Trading final reconcile, WORM append and readback validator | landing_scope_id instance | S7.4, S8.5T | PM -> independent QA/OPS/QC -> sink actor -> distinct readback verifier | PLANNED | immutable trading terminal receipt + ACK | `TERMINAL_RECEIPT_APPEND` | No repo write after; only source of trading landing state |
| 8 | S8.NC | No-candidate final reconcile, WORM append and readback validator | landing_scope_id instance | S7.NC, S8.5NC | PM -> independent QA/OPS/QC -> sink actor -> distinct readback verifier | PLANNED | platform terminal receipt + ACK | `TERMINAL_RECEIPT_APPEND` | Platform state only; P1/ML5-ML10 remain incomplete |

## Ledger Events

| Time | Session | Event | Evidence |
|---|---|---|---|
| 2026-07-29 | S2E W0 handoff | **Current projection frozen before W1:** nine readiness packages are 5/9 source-landed (`S2E.0`, `S2E.1`, `S2E.2a`, `S2E.2b-1`, `S2E.3`); the only ACTIVE entry is `S2E.2b-2`, followed strictly by `S2E.2b-3 → S2E.4 → S2E.5`. Production effect stays 0/6; S2 remains open and the packet remains `DRAFT_NOT_EXECUTABLE`. `S2E.2b-2` now owns the full S2.5 runner/recovery/anchor/penetration/nonce/2004-line exit; `S2E.4` owns runtime capture, closure schema, actor/verifier identity and trusted-attestor source repair plus explicit adjudication of the 2231-line effect-binding test; `S2E.5` owns the forgery-negative full-DAG proof, 28-obligation closure, and CodeQL #95 fix/evidence adjudication before any terminal packet. S2.0 EFFECT operator prerequisites are pinned in the packet: lazy GRANT strictly between adapter steps 7/8, dedicated login `NOSUPERUSER`, post-window REVOKE, and out-of-band `credential_escalation_connect`. This event changes governance projection only: no source contract/schema/test/runtime/receipt, no W5 re-emission, no deploy/restart/PG/broker/order mutation. | W0 intake baseline `83dc0ec3017e541e156e557c2e2d16f1704682a3`; TODO v861 / ledger v16 / packet handoff calibration |
| 2026-07-29 | S2E.2b-1 | **S2.4 recovery core landed as source.** PR#158 merge `521c813c5`, eleven commits on `agent/aiml-s2e2b-runners`. PA re-split S2E.2b after showing the full scope still could not be done without loosening hard boundaries: three of the five S2.4 row drivers each require a kernel change (`daemon-reload` is in the global `FORBIDDEN_ARGV_TOKENS`; `systemd-creds` needs an FD path and binary stdout the kernel structurally cannot carry; `useradd` needs variable argv a closed literal allowlist cannot express) and T2 needs `--user` plus env additions. **PA also found the工單 itself was built on a contradiction**: `install_driver.py:948-951` said the W6 runner "compensates **and re-runs**", but `:977-990` and `:1048-1052` prove that after a successful compensation the same plan can structurally never re-run — building "compensate then re-run" would have hit a door it could not open. This wave therefore delivers **recovery core with zero new host capability**: the POSIX `DurableFileDriver`/`InstallLockDriver` implementations (the repo had only test doubles), the **startup reverse-order compensator** (which existed in no file), the journal-routed driver injection for the probe/PREPARE entry points, the corrected comment plus a regression pinning the real contract, and two hardenings of the acceptance tooling itself (the `RUNNER_FAMILY` tuple was hand-maintained, so a new runner file that forgot to join it would not be AST-scanned at all; the import allowlist was family-wide, so widening it for a runner would also have let the observer and kernel import applier modules). **The single most important safety property** is that the compensator refuses to touch the host unless the replay ledger carries a consumption record for *this* plan — without it the tool would be an unauthenticated destructive utility that acts on any journal it finds; the permit id is re-derived from the frozen profile table rather than the permit's self-reported fields, so a legitimate aggregate permit placed in the pg_migration slot is caught. SSHSIG freshness is deliberately not re-checked (on restart the permit is necessarily expired, so checking it would make the compensator unavailable exactly when it is needed; the anchor is the ledger's `authorization_digest`, which covers the signature bytes) — E2 ruled the trade-off acceptable and bounded it: what an attacker buys is accelerating an undo that was due anyway, not a new destructive capability. **Adversarial cost:** E2 failed the first round on two P1s. The load-bearing one: `pre_compensation_observed_digest` used the WAL's **applier-space** digest while its only consumer compares it for inequality against a **verifier-space** value — across digest spaces the two are never equal, so the check that was supposed to exclude a constant-returning verifier was vacuous, and E2 demonstrated a constant verifier reaching `COMPLETED_EXACT`. E2 also showed the stronger anchor E1 had described as non-existent (`capture_pre_compensation_observation`) was already in the repo, read-only, and in use on the mid-effect path. E1 fixed it and **correctly rebutted one of E2's own recommendations** — the suggested WAL fallback would have re-vacuated the check on the branch where the verifier escapes before compensation but works after; E2 implemented the fallback, reproduced `COMPLETED_EXACT`, and withdrew the recommendation. CodeQL raised two highs whose taint source proved to be the *name* of a frozen status constant (`COMPONENT_STATUS_SECRET_LEAK_BLOCKED`, whose value is the fixed string `SECRET_MATERIAL_LEAK_BLOCKED`); renaming was refused on two grounds (the name is accurate, and it is shared frozen egress vocabulary already on main across five modules), both sinks were nonetheless guarded by the repo's central secret scanner, and the alerts were dismissed as false positives with the full argument recorded on the PR. Tests use a two-part split because E2 proved this environment cannot collect the whole `tests` tree in one pytest process (`tests/ml_training/__init__.py` claims the top-level `ml_training` name): `tests/structure` **4406 passed, 13 skipped**; the other segment's 10 reds are pre-existing with base and head failing identically. The crash matrix was replayed exhaustively across 15 fault windows and lands only on `COMPLETED_EXACT` or `RECOVERY_REQUIRED`. **Honest boundaries:** nine authorities stay false; zero runtime reach; the CLI's two s2_4 modes have a permanently `None` driver until S2E.2b-3 supplies the aggregate host driver; **obligation 7's substance is now in place but the ledger is deliberately not flipped** (flipping costs a full round-10 re-emission of all eight W5 receipts for what would still only be a PARTIAL, so S2E.5 will do one batch); and E2 recorded four residuals that open S2E.2b-2. | PR#158 merge `521c813c5`; this projection commit |
| 2026-07-29 | S2E.2a + S2E.3 | **Two writers ran in parallel on disjoint owned paths; both landed as source.** S2E.3 (PR#155 merge `94a9f514b`, nine commits) closed the four S2.5 durability defects plus the deferred PR#153 Codex-4 finding: reconcile moved inside the lifecycle hold as the sole authority (with the S2.4 install lock re-probed inside the window), the two locks split into `S2_4InstallLockFreeProbe` / `S2_5LifecycleLockHold` with type-exclusion and resource-separation guards, release became a typed outcome carried into verdict/receipt/schema and enforced on the central gate's success arm, and the journal became a hash chain with a self-digest that never silently resets. **F3 turned out to expose something worse than the audit had found**: `S2_5FlockProbe` defaulted to the S2.5 lifecycle path, so `install_lock_free` had never probed the S2.4 install lock at all, and the resulting misattribution was fossilised in an existing test — this wave inverted that test and added the contrast case. S2E.2a (PR#156 merge `5486a6183`, ten commits) built the trusted-host kernel (sole exec point, argv allowlists derived from their existing owners rather than copied, host-fact-derived target class with zero caller parameters), the read-only observer (object-capability + real process separation; **OS-uid separation explicitly not provided** — that is operator/W6B), and the S2.0/S2.1 runners with disposable rehearsal that runs `apply → independent postcheck FAIL → compensate → byte-for-byte catalog restoration` against a real throwaway PostgreSQL cluster. **It is a pure-addition wave: the four adapter modules have zero edits**, verified by byte-identity, suite equality, and behavioural equality of emitted receipt digests. PM narrowed the original S2E.2 scope on PA's evidence (S2.5 has no real-systemd disposable target and cannot have one without falsifying a registry invariant; S2.4's paths are module constants so its rehearsal can only reach `SOURCE_SIMULATION_PASS`) and moved the S2.4/S2.5 runners, the reverse-order compensator and startup-reconcile injection into a new **S2E.2b** row. **Adversarial cost was high and is worth recording**: E2 failed S2E.2a on four P1s including a caller `target_view` that reproduced the very L1 hole the runner exists to close, and a **SQL identifier injection proven on a real PostgreSQL 16.14 cluster**; E1 then **could not self-certify** that its verifier's `GRANT`/`SET ROLE`/`REVOKE` path satisfied the adapter invariant "no role membership" and switched to a design that needs no membership DDL from the runner — which surfaced a genuinely new operator prerequisite (see below). Codex added two further P1s: `disposable_candidate` could be derived for the real runtime during provisioning or recovery and thereby skip all three production acknowledgements, and the intent's `target_class` was never bound to the derived host, so a validly signed `disposable_local` intent could drive real system-level `stop`/`start` through real kernel capabilities while reporting rehearsal-class evidence. Both are fixed; the target-class priority order is now platform → hostname → provisioning signals, with `disposable_candidate` demoted to rehearsal-only and never derivable. CodeQL raised a high on the observer's stdout; the taint source proved to be the identifier `trusted_host_time` classified by a name heuristic, which was renamed to `host_wall_clock_time` (a real fix — the function's own docstring says it is not an attestation), the egress was additionally guarded by the repo's central secret scanner, and the guard's own diagnostic was narrowed so no derived string leaves any fd. **New operator prerequisite for the S2.0 EFFECT session** (proven on real PG 16.14, to be added to the operator action packet): peer/ident authentication does not solve *membership*; the observer role is created at adapter step 7 and step 6 hard-refuses any pre-existing observer role, so the `GRANT <observer> TO <login>` **cannot be pre-created** and must be granted lazily between steps 7 and 8; the dedicated login role must be `NOSUPERUSER` or the 42501 denial proof is fake; the post-window `REVOKE` is likewise the operator's job; and `credential_escalation_connect` must be provisioned separately. Full suite `6794 passed, 46 skipped, 0 failed` at the S2E.2a head. Source only — no runtime/PG/deploy/broker effect; nine authorities stay false; every rehearsal vertex in both waves is already closure-PASS-blocked by S2E.1's transitive blocking, so **none of this green may be projected as EFFECT progress**. | PR#155 merge `94a9f514b`; PR#156 merge `5486a6183`; this projection commit |
| 2026-07-29 | S2E.1 | **Claim-gated effect routing + closure binding landed as source (S2E.1 closed).** Ten commits on `agent/aiml-s2e1-effect-routing` ending at `d6f7b585d` (base `3c4b6058c`): S2.2B identity-only registry adapter (`c23619905`); nine per-phase claim-gated EFFECT routes (`b2b974b49`) — selector digest `s2_effect_adapter_selection_v1` over {adapter_id, schema_version, side_effect_class, step}, per-step exact claim inventory encoding the §1.2-corrected DAG order, selector↔`side_effect_class` interlock, and a mandatory `authority` surface so `constitutional_gate` always joins the effect DAG; closure binding module `agent_governance_s2_effect_binding.py` (`26a731de6` … `d6f7b585d`). **Three adversarial rounds were needed and the first two were blocked by CC.** Round 1 (CC FAIL): the binding module had short-circuited the two pre-existing per-step closure hard gates and admitted production `APPLIED`. Round 2 (CC FAIL again): the S2.0 narrowing reproduced the same defect one step over, S2.5A/S2.5B still bypassed `validate_s2_5_attestation_binding`, and the honesty fix had become a reverse false-underclaim; E2 separately proved the S2 `ops_postcheck` evidence shape was unrepresentable in `closure_packet_v1` (a functional regression). Round 3: CC **PASS**, E2 delta ACCEPTED after one further blocker (an independent `ops_postcheck` artifact reporting its own `FAIL` still endorsed a closure PASS — the S1.6B precedent had been adopted incompletely). Final state: the four pre-existing hard gates (`validate_pg_observer_bootstrap_binding`, `validate_quiesce_fence_binding`, and `validate_s2_5_attestation_binding` for both S2.5 phases) are delegated to, not replaced; S2.0 and S2.1 carry typed `closure_pass_blocked_reason` and are unreachable for closure PASS this wave; the §6 freshness clock reads `packet["adjudicated_at"]` rather than the receipt's own timestamp; postcheck artifacts enforce `status == "PASS"` plus an exact field contract. Full suite `6496 passed, 46 skipped, 0 failed` at `5fd0e180d` (base was 6341/46); the Codex round's transitive-blocking and attestation fixes were mutation-verified 7/7. **Honest boundary — after the PR#154 Codex round, zero of nine steps can reach a closure PASS today.** Codex returned two P1s, both upheld: (1) an authorized W6A probe could closure-PASS without any validated S2.0 predecessor because `s2_0_effect_receipt` was declaration-only — fixed by **transitive blocking**, where `S2_EFFECT_DAG_UPSTREAM_STEPS` is derived (not hand-copied) from the existing claim inventory and any step whose transitive upstream is blocked is itself typed-refused with the upstream reason quoted; since S2.0 is the root of the whole chain, all nine steps are now unreachable, which is the correct posture while the root predecessor has no verifiable success status. (2) the custom `s2_effect_ops_postcheck_v1` was accepted on a caller-controlled self-digest and a self-reported `PASS` and never reached the host verifier, so a packet producer holding a genuine effect receipt could swap in a freshly self-digested PASS postcheck after W6B installation and PG migration — fixed by feeding its exact bytes into the execution-attestation candidates with an identity of (kind, digest, effect_step, verifier_node) so it cannot collapse onto an effect receipt. This supersedes the earlier "7/9 shape-reachable" reading and thereby settles CC dissent D-3. No real receipt exists, runtime stays inactive, and the nine authorities stay false. **Three further boundaries recorded rather than silently fixed:** the S0.3 signed-bundle verifier's `ALLOWED_EXECUTION_KINDS` does not admit the new postcheck kind (widening a signed-bundle surface is an S0.3-sealed path needing its own reviewed wave); the same evidence additionally trips the closure typed-observation branch as an unsupported `schema_version` (adding it to that exclusion set would be a *widening*, deliberately left for PM); and `s2_effect_verifier_command_capture_v2` bytes are likewise caller-controlled and not yet an attestation candidate (it normalizes on `record_digest`, not `self_digest`). S2.5A/S2.5B/S2.2B additionally require a real trusted-host attestor SSHSIG; the three s2_4 probe/prepare steps have no central-gate semantic branch, so their closure PASS is achievable from packet-local artifacts alone and their real semantic gates (permit consumption, replay ledger, trusted-host time) live entirely in the adapter layer. **CC immutable dissents (5), carried forward:** (D-1) the gap-list drift test enumerates delegated leaves rather than deriving them from the gate source, so a future third leaf could resurrect the same false-underclaim silently; (D-2) the S2.5 verifier capture uses a custom kind outside the registry `commandCaptureV2` contract and binds more weakly than the S1.6B precedent it cites, and `verifier_capture_digest` is not covered by the attestor signature; (D-3) this delta flipped the S2 closure lane from globally unreachable to 7/9 shape-reachable, and the composite statement of that fact belongs in the ledger (this row); (D-4) minor wording/mechanical-reason gaps in the blocked reasons; (D-5) both CC rounds were source-only reads — the green test runs are `LOCAL_REPRODUCIBLE`, not platform-attested. **Also carried:** `command_capture_v2` + `scope=runtime` remains unrepresentable in `closure_packet_v1` (schema pins `scope: const "test"`), which today constrains only the two already-blocked steps; applier≠verifier is not enforced at closure for the s2_4 family and S2.2B (their receipt schemas carry no apply-actor-node field) and remains closed only at the adapter layer. Registry S2 adapter invariants were **not** edited: PM adjudicated that "no route_task effect node injected before the EFFECT session" stays true because selector claim admission *is* the machine definition of the EFFECT session; the equation is now written into the two tests and both modules so the pair of green-but-apparently-contradictory tests cannot be misread. **E2 final-round residuals (5, all P3, none blocking merge, all carried to the S2E backlog):** (RES-1) `packet["adjudicated_at"]` has no monotonicity check inside the S2.5 adapter — a backdated value makes the freshness face vacuous again; the real fix is threading the already-trusted-clock-bound `evaluation_timestamp` through the `validate_deploy_effect_binding` signature, which is out of this wave's scope (the outer gate does bind adjudicated_at to the trusted host clock, which is why this is P3). (RES-2) S2.5A/B postcheck `verifier_node` is not cross-bound to the receipt's `independent_verifier_node` even though those two schemas do carry the field — applier≠verifier is still enforced receipt-side by the delegated §6 gate, so the artifact label is merely unbound. (RES-3, pre-existing) the four s2_4 steps and S2.2B have no source-provable postcheck independence at all: `verifier_node` is a free string and the whole artifact can be minted by the applier, W6B APPLY included — this is the strongest remaining closure-face gap and belongs in the S2E backlog. (RES-4) the NEW-P2-F equation is settled by comment only, with no machine check, and the comment cites line numbers that will rot. (RES-5) the `6490 passed, 46 skipped` figure is Mac-local `LOCAL_REPRODUCIBLE`; E2's own environment could not reproduce the absolute number (python 3.10.1, no `tomllib`), though it independently confirmed the +30 delta and proved 28 base-only failures matching the three new guards exactly. Source only — no runtime/PG/deploy/broker effect; S2E.2 and S2E.3 unlocked. | branch `agent/aiml-s2e1-effect-routing` head `d6f7b585d`; this projection commit |
| 2026-07-28 | S2E.0 | **S2.4-AMEND-1/2 landed as source (S2E.0 closed).** Four-commit chain on `agent/aiml-s2e0-amend12` at head `cc6a8b97a` (base `1255275dd`): (1) §9.1 profile discriminators — code-owned `S2_4_EVIDENCE_CLASS_DISCRIMINATORS` derived from the frozen `S2_4_AUTHORIZATION_PROFILES`, threaded into `_dependency_evidence_receipt_errors` so a genuine artifact of the wrong profile/scope is refused (obligation 26); (2) plan-derived `expected_topology` — `expected_topology` removed from the frozen row-payload allowlist, replaced by the signed `s2_4_pg_hba_delta_v1` bound to the plan core via a no-cycle binding digest, with the five baseline keys made fail-closed in `derive_pg_topology_status` (obligation 25); (3) atomic AMEND-1 — `derive_apply_source_dependency_admissions` §9.2 ingress wired into APPLY step (2b) before permit verification on every lane, terminal `s2_4_install_effect_receipt_v1` gains required closed `dependency_refresh_digests`, obligations 16/23/26 flipped with the honest-surface latch re-keyed to typed status; (4) E2-returned P2 fixes (builder fail-closed contract gate; attestation self-digest recompute). Review: E2 adversarial (0 P0/0 P1, 5+2 mutations killed), E2 delta ACCEPTED, E4 PASS (independent rerun `2 failed, 6337 passed, 46 skipped`; the exactly-two reds are the PM-owned projection discriminators closed by this round-8 emission + projection commit), CC PASS (all changes tighten fail-closed surfaces; nine authorities stay false), E3 PASS (0 confirmed; OBS-1 LOW recorded as follow-up: (2b) binds live HEAD not plan-core source_head). Round-8 W5 receipt re-emission at `cc6a8b97a` (carrier `c911ea9c1`, new ledger digest `sha256:90f9…ed208e`, count=28). Recorded residuals: refresh digest not folded into the signed plan core (defense-in-depth, W6B may revisit), S1.3 not gated (typed `s1_3_not_gated_reason`, owner W6), E2-delta P3 nit (bracket KeyError on doubly-absent digest keys in a directly-called leaf). Source only — no runtime/PG/deploy/broker effect; S2E.1 unlocked. | branch `agent/aiml-s2e0-amend12` head `cc6a8b97a`, receipts carrier `c911ea9c1`; this projection commit |
| 2026-07-28 | S2 | **Independent effect-readiness audit supersedes the earlier packet-ready projection.** The seven narrow source-seam predicates and their landed PRs remain valid, but `BLOCKED_OPERATOR_ACTION_PACKET_READY` was disproved: AMEND-1/2 are unimplemented source work; hypothetical authorized S2.4/S2.5 routes still inject zero effect adapters; S2.5 has reconcile/lock/release/journal durability gaps; S2.2B `apply` is always pending and has no trusted observer/runtime-attestor SSHSIG execution path; 28 W5 obligations include unresolved W6/W6B source requirements. Linux read-only observation at 2026-07-28T15:55:23+02:00 found the candidate learning units `not-found/inactive/dead` and both canonical AIML roots absent. Current state is `S2_EFFECT_EXECUTION_READINESS_ACTIVE`; execute S2E.0-S2E.5 before regenerating an Operator packet. | `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-28--aiml_s2_source_and_effect_readiness_audit.md`; corrected `TODO.md`; packet v1 marked `DRAFT_NOT_EXECUTABLE` |
| 2026-07-28 | S2 | **`S2.5@SOURCE_READY` + `S2.2B@SOURCE_READY` declared; Sprint 2 source chain converges on one `BLOCKED_OPERATOR_ACTION_PACKET_READY`** — WP5 tranche 2 (PR #150 merge `6be29043c`) closed the three carried P2s from the tranche-1b review (mutation-survivor test, hold-style lock across the whole anchor→consume→persist→journal window, no self-reported `record_digest` in the closure-binding verifier) plus notes 2/3, and built the `S2.2B` `ingestion_compatibility_receipt_v1` seam anchored on the real persisted S2.2A receipt and a fully re-validated `s2_5_final_attestation_v1` (a simulated-lane anchor never unlocks the runtime lane; one failing V151-V160 item fails the whole receipt). Independent E2 delta re-review PASS (5/5 further mutations killed; its F1 doc-residue finding fixed in the same branch; F2 reconcile-outside-the-lock-window, F3 lock-semantics conflation and F4 release observability carried to the effect lane). Round-7 W5 receipt re-emission at `5be472193` (carrier `0eb90e40c`) because the tranche necessarily touched the four W5-owned central-registration files. Measured on the committed clone: structure 3735/6 + ml_training 2571/40 = 6306/46/0; `agent_governance.py validate` PASS/roles=20. With S2.0/S2.1/S2.2A/S2.3/S2.4 already landed, **no executable S2 source work remains**: the only blocker is a fresh, exact, typed operator authorization, and it is now a single packet — `docs/execution_plan/ai_ml_landing/S2-operator-action-packet-v1.md` (serial effect DAG `S2.0→S2.4→S2.5A→S2.1→S2.5B→S2.2B`, per-step authorization/rollback/postcheck, the two prerequisite PM amendments S2.4-AMEND-1/2, and the `S2_CLOSED` criteria). Nine authorities stay false; the S1 disposable authority is not reusable; runtime remains inactive/not-found per the 2026-07-27 read-only observation. | PR #148 `dab875882`, PR #150 `6be29043c`; this commit |
| 2026-07-28 | S2.5 | **WP5 tranche 1+1b: the S2.5 running-attestation source seam built, adversarially reviewed, remediated, and re-reviewed (source-only, zero runtime reach)** — per the PA design (`design/S2.5-running-attestation-source-seam.md`) and six PM rulings (O-1..O-6, §5.4 option b): five closed schemas + `s2_5_authorization_replay_ledger_v1`, additive v3 classifier (frozen v1/v2/S0.3 digests byte-unchanged), lifecycle/attestation/driver leaves + CLI, three-state lifecycle fixtures on an injected harness, typed-status-keyed two-way obligation latch closing `S2_5_LIFECYCLE_FIXTURES_DO_NOT_EXIST` (W5 receipts re-emitted at `0faa6499d`, carrier `0b06982e0`, round 6). Round-1 adversarial review: E2 FAIL (F1 cross-lane pre-drill anchor reached production `FINAL_ATTESTED`) and E3 FAIL (post-effect exception escape with no rollback; upstream receipts trusted by self-reported `self_digest` string; replay ledger undetectable tail-truncation and zero persistence) + P2 batch. Tranche 1b remediated all five P1 with pristine-clone red→green evidence and closed thirteen P2 (kind binding, PLATFORM⇒attestor leg, redaction, journal hardening + derived paths, non-finite rejection, secret scan, §5.3 ruling, verifier-arm negatives, status-set split, TTL budget gate, owner-fingerprint recompute, CLI docstring, forward skew cap). E2 delta re-review **PASS**: 11/12 mutations killed, all round-1 P1s confirmed closed; measured 6281/46/0 on the committed clone (structure 3712/6 + ml 2569/40). **`S2.5@SOURCE_READY` is NOT declared**: carried P2-1 (one mutation survivor test gap), P2-2 (probe-only flock probe leaves the consume→persist window without mutual exclusion; design §5.7 named the hold-style s2_4 lock reuse), P2-3 (binding verifier accepts a self-reported `record_digest`) plus three notes gate the declaration; they close in tranche 2 together with the `S2.2B` seam. Nine authorities stay false; no runtime/PG/broker/order effect exists or is granted. | branch `agent/aiml-s2-wp5-s25-seam` heads `9decbac71..dd6c10936`; E1/E2/E3/E4 reports in-session |
| 2026-07-28 | S2.4 | **`S2.4@SOURCE_READY` declared by PM (post-merge projection; source-only, zero runtime reach)** — all six exit conditions MET at the merged generation: ① focused/adjacent suites re-run at the committed round-6 head `029d2adfc` (structure 3628/6 + ml_training 2547/40, independently reproduced by E2 on a committed clone and E4 on the clean committed worktree); ② the six PM-owned obligations adjudicated (0 source-closure blockers; S2.4-AMEND-1/2 gate `S2.4@EFFECT_DONE`); ③ the ABI-generated obligation projection live and pinned; ④ five independent exact-generation reviews (E2/E4/CC/OPS/QA) at `029d2adfc` all PASS with 0 P0/0 P1 (carried P2s closed by this projection commit: duplicate-row guard + fullwidth-equals claim variant + round-field assertion in the discriminator test, and the gate-① evidence-attribution wording); ⑤ required classified CI green at `029d2adfc`, the Codex P1 thread answered and resolved, and PR #145 merged with `--match-head-commit 029d2adfc` as `e1b14b7d5` whose tree is byte-identical to `029d2adfc`; ⑥ the eight artifacts bound to `aaee7f1a2` with carrier `456bd0c20` and no re-emission owed. Mac/GitHub/Linux `trade-core` synced ff-only to `e1b14b7d5` (sync is not deploy: learning service remains inactive/not-found per the 2026-07-27 read-only observation). The declaration licenses source structure only — nine authorities stay false; no runtime, PG, unit/service, deploy, restart, broker, order, or live effect exists or is granted. Next source dispatch: WP5 = `S2.5`, then `S2.2B`. | this commit; PR #145 `e1b14b7d5`; gate row in `TODO.md` |
| 2026-07-28 | S2-WP4-W5 | **Round-6 PM-audit closure (projection + adjudication; source-only, zero runtime reach)** — the PM work order on PR #145 reproduced one P1 at `456bd0c20`: the active-state projection in `TODO.md`/this file still described the `c2a7263ce` generation ("round 5 uncommitted", "re-emission still owed") while all eight persisted artifacts were already bound to `aaee7f1a2` with carrier `456bd0c20`. Closed three ways. (a) The active-state projection is reconciled to the actual generation (round-5 remediation `aaee7f1a2` committed; receipts re-emitted at `aaee7f1a2`, committed as `456bd0c20`; stale `c2a7263ce`/uncommitted-round-5 claims removed). (b) A discriminator regression, `tests/structure/test_aiml_w5_receipt_binding_projection.py`, reads all eight committed artifacts, requires exactly one unique `source_head`, requires the `W5-RECEIPT-BINDING` projection markers in `TODO.md` and this file to equal the artifacts' actual value, requires every `source_head = <hex>` claim in both files to match it, and requires the PM-owned obligation projection in both files to be set-equal to the live ABI's `owner_wave=PM` rows — a stale `c2a7263ce` projection or a resurrected "uncommitted round 5" claim is red. (c) The PM-owner projection is corrected from the live ABI (28 rows: W6B=15, PM=6, W6=4, W5=1, S2.5A=1, S1.3=1 — the old gate text said "four PM rows" while omitting three PM-owned rows and counting the W6-owned `DEPENDENCY_REFRESH_REPRODUCER_NODE_IS_DECLARATIVE` as PM), and all six PM-owned rows are explicitly adjudicated in **§ W5 PM-owned obligations adjudication** below: 0 source-closure blockers, 4 separately scheduled into two named PM amendments gating `S2.4@EFFECT_DONE` (S2.4-AMEND-1, S2.4-AMEND-2), 2 accepted carry-forwards. `S2.4@SOURCE_READY` remains `NOT_MET` in this commit; the declaration is a post-merge PM projection after gate ①④⑤ are verified at the merged head. | branch `agent/aiml-s2-wp4-w5`, PR #145; the adjudication section and the discriminator test in this commit |
| 2026-07-27 | S2-WP4-W5 | **WP4 W5 (source closure) built and adversarially reviewed five times; source-only, zero runtime reach; merged to main but not `SOURCE_READY`** — four deliverables. (a) The **§10.5 acceptance sweep**: every one of the 39 items was mapped to the test that fails when the *source* is wrong, treating "a test is named after it" as no evidence; six items W5 found unproven or half-proven are folded as **live** re-derivations into the W5 exported ABI, so deleting a source predicate breaks the wave exit rather than only a test. Each newly-closed item carries the mutation showing the pre-existing suite was green under the exact weakening (#12 the observer role — the only prior assertion was vacuous because the fixture manifest never mentioned `aiml_observer_ro`; #16 no controller/worker/serving/deleter activation — zero tests existed, an added `SERVING_INSTALL` applier node left 329 green; #15 the four encoded secret forms; #11 the retired `alr_shadow` identity and a mutable-checkout `WorkingDirectory`; #29's S2.4 half; #1 central schema registration, where an orphan schema nobody validates left 255 green). (b) The **W5 wave projection and the W0→W5 chain**, following the W4 shape. (c) The **§9.2 dependency-refresh gate** — `s2_4_dependency_refresh_attestation_v1` was named in §10.1's owned-path allowlist and was **the one §10.1 path that did not exist** (no schema, no registration, no validator branch, no builder, no test; the substring `dependency_refresh` appeared nowhere in the tree), while §9.2 makes it the sole mechanism by which an expired S2.2A/S2.3/S1.3 identity may still be admitted. Built rather than waived, because the alternatives were to amend the design so the gate passes or to declare §11.2's "complete source inventory" complete when it was literally false. Independence is structural: the gate recomputes the semantic digests itself at the current head from the reviewed producer SSOT instead of trusting `reproduced_semantic_digests`, the positive path replays the repository's own genuinely-expired S2.2A v1/v2 and S2.3 receipts, nine never-refreshable rows are a closed table, and the deliberate absence of an SSHSIG is pinned by a test that fails if any signature/namespace/signer property ever appears. (d) **The W5 emitter** (`agent_governance_s2_4_w5_emit.py` + CLI `w5-emit`): §10.3:1275 says *every* source wave emits `s2_4_wave_exit_receipt_v1` and W0–W4 each have a receipts directory, but `build_w5_wave_exit_receipt` returned an in-memory object and wrote nothing, so W5 was the only source wave with no persisted receipt. It mirrors `_w4_emit` (historical-W4 lineage read with a hard refusal on self-digest tamper → in-memory regeneration of the current-generation W0…W4 chain → central derivation of the W5 receipt bound `predecessor_wave_receipt=<W4>` + `predecessor_wave_chain=(W0,W1,W2,W3)` → central gate → only then a single atomic write; any non-`ADMITTED`/`PASS` is a typed refusal with zero files written) and adds two things W0–W4's records do not carry: the derivation record **serialises the whole unclosed-obligation ledger, enumerated from `_W5_EXPORTED_ABI["remaining_owned_obligations"]` rather than hand-copied** and pinned by `obligation_ledger_digest`, so at W6 incident time the operator's precondition list is readable from an artifact instead of by importing a module from an unmerged branch; and it records what the declaration rests on (`source_head`, per-wave status and self-digests, `replay_note`, `review_provenance`, `test_evidence`). **Acceptance headline: 36 proven / 3 partial / 0 unproven** — E4's independently rebuilt map, **re-scored at the reviewed head `756a59ef7`** and superseding the 35/4/0 E4 scored one head earlier. It arrives at the same numbers the wave first self-assessed, but from the opposite direction and on different grounds; what is adopted is E4's reasoning, not the wave's. **#24 moves to PROVEN** because its discriminator is now mutation-proven: `agent_governance_s2_4_host_identity`'s refusal of a supplied manifest whose canonical digest is not the one in the signed intent used to be only *named* by a test that supplied `uid=1/gid=1` — rejected first by the §8 identity contract, so the guard was never reached and `if False:` left 6111 passed/46 skipped unchanged. The **three held PARTIALs are #26, #28a and #29**, held not because a discriminating test is missing but because the obligation ledger shows they cannot close inside WP4 at all: #26 needs `prctl(PR_GET_DUMPABLE)` observed on a real applier, i.e. production implementation on a host (`PR_SET_DUMPABLE_IS_DECLARED_NOT_ENFORCED`, owner W6B); #28a's plan-expiry field sits outside the signed core (`PLAN_EXPIRY_OUTSIDE_SIGNED_CORE`, owner W6B); and **#29 is terminally PARTIAL for S2.4** — no S2.5 source or fixture exists in this repository, so its second clause has no owner in this work package (`S2_5_LIFECYCLE_FIXTURES_DO_NOT_EXIST`, owner S2.5A). **Label correction (round 4):** the S2.5-lifecycle-source fold was described here and in `TODO.md` as "#28a"; it is §10.5 **#29**'s second clause, which is what `tests/structure/test_agent_governance_s2_4_acceptance_matrix.py` has said all along. **Four adversarial rounds, and each round found defects in the previous round's fix.** Round 1 (two reviewers at `3412281ac`): a BLOCK on the §9.2 gate plus a DONE+FAIL carrying three P1s the first fix did not touch, and two more found PM-side. The load-bearing one: for three of the four refreshable classes `semantic_digest_fields` resolved to the producer module's own file hash, a duplicate of `producer_module_digest`, so "independently replays its producer/semantic checks" reduced to "two files are unchanged" — against the real shipped S2.3 receipt, drifting `sealed_build_digest`, `runtime_content_digest` or `status` still yielded `DEPENDENCY_REFRESH_ADMITTED`. Also: `owned_scope_worktree_delta` was folded into the projection and **no reason read it**, so W5's own new acceptance-matrix test file could be emptied in the working tree and the whole W0–W5 chain still derived PASS; and the carry-over obligation tuple was hard-coded at twelve rows while W4 exported fifteen, silently dropping `ATTESTED_EVIDENCE_CLASS_VERIFIER` and softening `ATTESTATION_EXPIRY_AND_HOST_TIME_ARE_NOT_CROSS_CHECKED` from `NOT_` to `PARTIALLY_` against its own statement — the carry-over now enumerates from the W4 ABI. Round 2 (five reviewers at `b5d546e3a`; E3 lifted its BLOCK, E2 confirmed all four prior P1s closed with zero survivors across eleven mutations, E4 confirmed every measured number exactly) found that **the fix itself was the gate**: three reviewers converged on the owned-scope delta comparing the worktree against HEAD via `git status` rather than against the bound commit by content — OPS demonstrated end-to-end that committing a tamper to a producer module and asking for a reproduction bound to the pristine commit reported CLEAN and returned the tampered value, and both git index bits hide a modified file from that oracle; E2 and E3 converged separately on the §9.2 expiry decision being read off two caller-authored fields whose `self_digest` the supplier reseals, so editing `observation_time`/`expires_at` and resealing moved all three real receipts from `EXPIRED_NO_REFRESH` to `FRESH` — the entire gate this wave was built for, skipped by editing two fields; E4 and E3 converged on the live input-scope check omitting the one path (`edge_feature_schema_contract.py`) that materialisation cannot protect, and deleting that line left the entire 6111-test tree green; OPS found the fail-closed owned-scope consumption was W5-only while W2 owns the scripts a W6 operator actually executes, so a W2 receipt binding `source_head` could derive PASS from a tree where four of them were locally modified (every wave now consumes its own delta, at the cost of re-deriving twenty wave-exit assertions across five W0–W4-owned test files onto an explicit clean-scope fixture); and **both honesty latches were syntactic proxies whose false positives landed on the "close the obligation instead" arm** — a bare comment mentioning the entry point defeated the call-site scan and adding a `description` to a schema `$defs` flipped the evidence latch, so call sites are now resolved by AST across 717 modules and an unrecognised keyword yields *undecidable* rather than *false*. Round 3 also produced the emitter, the §10.1.2 owned-path amendment and the ledger/TODO projection — **none of which any reviewer had seen**. Round 4 (two reviewers at the merged head `756a59ef7`) was their first review and returned **3 P1**, with §11.2 predicate 4 (independent review at the current head) **NOT MET** until they closed. Every one was the same shape the wave keeps producing — a guard correct in source whose deletion leaves the tree green, or a claim asserted rather than measured. **P1-1**: the emitter's `_validate_emit_evidence` call had no test; deleting it left `tests/structure` at 3601 passed / 6 skipped, byte-identical to pristine, and the one test named `test_emitters_refuse_secret_like_evidence` (plural) exercised exactly one of the six emitters — it is now parametrised over `emit_w0/w3/w4/w5_receipts` × {secret-bearing, empty `test_evidence`, empty `review_provenance`} and each of the four goes red under deletion of its own call. **P1-2**: the `regenerated_w0_admission` refusal stage had no test at all, because every negative test in the W5 emit suite forces `derive_source_admission_status` to `ADMITTED` and the chain-link parametrisation covers only the six wave-exit stages; now covered without that fixture. **P1-3**: round 3's correction wrote a **measurably false capability claim into the exported ABI** — the row `PROGRAM_CODE_IS_ON_THE_SCANNER_PATH_VIA_W3_AND_W4` stated that all four occurrences were closed and "the process-level property IS achieved for the facade's import closure", while at that head importing the facade still left `program_code` on `sys.path` and `broker_connectors`/`exchange_connectors`/`dashboard`/`ai_agents` all `find_spec`-resolvable. There was a **fifth occurrence the sweep never enumerated**, `aiml_gate_receipt_wave_w2.py`, which the facade imports at module top level and the runtime closure declares; round 3's regression looped leaf-by-leaf over two of the named leaves and never imported the facade, which is exactly how it hid. PM ruled fix the code, not the sentence: the insertion is removed the way W3's was, under an explicit extension of the round-3 path-scope ruling to that W2-owned path, and the regression now imports the facade itself and asserts both that `program_code` is absent from `sys.path` and that none of the four packages resolves. Measured before/after at this head: `True` / all four resolvable → `False` / none resolvable. Removing it broke nothing (`agent_governance_s2_4_render.py`, the only consumer wave_w2 reaches that needs the package form, puts the path up itself at its own import). **P1-4**: this section's own preamble named `a757c006f` as "W5's reviewed head" when nobody had reviewed it — corrected above. Six P2 were taken with it. **The latch-D false positive had survived through a new vector**: the closing arm was gated on `emit_sink_accepts_fabricated_evidence`, a vacuity control, so a benign shape check in `agent_governance_s2_4_emit_sink.validate_emit_evidence` produced "the evidence digests are no longer shape-only; close the obligation instead" while `only_shape_is_constrained` was still `True` in the same projection — a false message whose prescription deletes a still-true honesty row, the same failure P1-E fixed once from a different input. The arm now reads the subject and the sink probe joins the empty/secret controls; recorded alongside it, `emit_sink.py` is in `_W2_OWNED_PATHS` only, so W5's wave exit reads a file it neither owns nor watches for drift, and adding it is an owned-path decision §10.4 places outside a worker's choice. **§10.1.2 clause (d) did not stop a worker narrowing its own scope**: the membership test pinned 5 of W5's 14 paths, so dropping `agent_governance_s2_4_install.py` — a §10.1 literal path a W6 operator executes — left the whole W5 lane green while re-opening the owned-scope bypass for it; the pin is now the full set. `_design_fenced_paths` took the *first* heading match, so a decoy `#### 10.1.2 ` section carrying a copy of the real list shadowed a gutted authoritative block with all three (d) tests green; the heading must now be unique. `_WAVE_OWNED_PATHS` was unpinned — the wiring test monkeypatches the delta function, so pointing `"W2"` at `_W0_OWNED_PATHS` stayed green; each wave is now asserted to be its own leaf's projection object and the six pairwise distinct. `persisted_dir` in the W5 derivation record is now repo-relative, so all eight artifacts re-derive byte-identically from another checkout rather than seven of eight. **Not closed, recorded precisely:** `_measured_out_of_list_union` still covers W3∪W4∪W5 only, because (d) is forward-looking and W0/W1/W2's out-of-list paths rest on §10.1.1's retroactive prose; measured, 29 W0–W2 paths are outside §10.1's literal list and unnamed in the (b) block, and §10.1.1's prose ratifies roughly 21 of them, so closing it properly needs a PM amendment naming the rest, which §10.4 puts outside a worker's choice. What the wave *did* do is pin that gap so it cannot grow: a new out-of-list path in W0/W1/W2 is now red. The W3/W4 emitters still record an absolute `persisted_dir`; those are W3/W4-owned paths and were not touched. **Load-bearing honesty (CC ruling): a green W0→W5 chain evidences source structure at a head only.** `test_digests`, `capture_digests` and `review_fragment_digests` are shape-only — the chain derives PASS on a literal `sha256:111…1` — so the chain **never** evidences that tests ran or that a review happened; raising that bar in the source lane was refused rather than faked, because the only thing a source-lane verifier can independently recompute is repository content, which the receipt already binds through `owned_path_diff_digest`. Every test number in the Evidence column is a measured suite run, not a chain derivation. Recorded-not-absorbed obligations are the 28 rows in **§ S2.4 (WP4) unclosed owned obligations** below, regenerated from the live ABI. | branch `agent/aiml-s2-wp4-w5`, **merged to GitHub main as `427bd0dd7` (same tree hash as local `756a59ef7`) and synced to Linux**; heads `3412281ac` (sweep + projection + chain), `b78185b00` (§9.2 gate), `b5d546e3a` (round-2 remediation), `a757c006f` (five-leg round-3 remediation), `fcc44eca7` (emitter + §10.1.2 amendment + ledger rows), `756a59ef7` (the eight persisted W5 receipts). The round-4 remediation landed as `c2a7263ce`, the round-4 re-emitted receipts as `25d501366`, the round-5 remediation as `aaee7f1a2`, and the round-5 re-emitted receipts as `456bd0c20` (the current carrier; PR #145). Measured suite runs on a **committed clone** — `3412281ac`: structure 3500 passed / 6 skipped, ml_training 2547 / 40; `b78185b00`: structure 3515 / 6, ml_training 2547 / 40; `b5d546e3a`: structure 3564 / 6, ml_training 2547 / 40 (6111/46); `a757c006f`: structure 3581 / 6, ml_training 2547 / 40 (6128/46 against 6111/46, zero regressions, zero new skips). The emitter/ledger generation landed in `fcc44eca7` and the eight artifacts in `756a59ef7`; measured on the merged head as a committed clone: structure **3601 passed / 6 skipped**, ml_training **2547 passed / 40 skipped** (6148/46). The **round-4 remediation** (`c2a7263ce`) measured: structure **3620 passed / 6 skipped**, ml_training **2547 passed / 40 skipped** (6167/46 against 6148/46 = **+19 tests, zero regressions, zero new skips**), reproduced byte-for-byte in the dirty worktree (3620/6 and 2547/40). Round-4 mutation matrix, every one committed in the clone so the content-addressed owned-scope gate reports a clean scope: deleting each emitter's own `_validate_emit_evidence` call is **red** in the fixed clone for all four (`emit_w0/w3/w4/w5`, 3 failed / 9 passed each) and **green** on pristine for w3/w4/w5; deleting the `regenerated_w0_admission` stage is green on pristine (17 passed) and red in the fixed clone; re-introducing the `program_code` insertion in `aiml_gate_receipt_wave_w2.py` is red on the facade parametrisation (2 failed / 4 passed) where the round-3 leaf loop passed; the benign emit-sink shape check yields the **false** reason `"…no longer shape-only; close the obligation instead"` on pristine and the correct fail-closed `"…not a real predicate"` after the fix, with `only_shape_is_constrained` measured `True` in both; dropping `agent_governance_s2_4_install.py` from `_W5_OWNED_PATHS` leaves the 182-test W5 lane green on pristine and is red after the fix; pointing `_WAVE_OWNED_PATHS["W2"]` at `_W0_OWNED_PATHS` is green on pristine and red after; a decoy `#### 10.1.2 ` section carrying the real 45-path list while the authoritative block is gutted to one path is green on pristine (3 passed) and red after (4 failed); adding a new out-of-list path to `_W2_OWNED_PATHS` is green on pristine and red after; restoring the absolute `persisted_dir` is red after. On the merged head the emitter runs to `W5_RECEIPTS_EMITTED` writing 8 artifacts (7 receipts + derivation record) with `remaining_owned_obligation_count=28`, historical W4 lineage `VERIFIED` at `f66fe0790`; a second emission into the same directory is `W5_EMIT_REFUSED / output_collision` with the bytes unchanged. In a **dirty** worktree the same emitter is `W5_EMIT_REFUSED / regenerated_w1_wave_exit` because `agent_governance_s2_4_install.py` differs from the bound commit — the committed-blob contract working, not a defect. Six frozen digests byte-identical between the merged head and the round-4 tree (`1cf8c021…d0ddbc`, `22d78882…86c445`, `01d3062c…143c64`, `1bd86de7…6f92f6`, `79b27b70…4be2dc`, `0ce1503f…98640b`); both CLIs PASS with zero exclusions (`agent_governance.py validate` → `{"status":"PASS","roles":20}`; `agent_governance_s2_4_install.py engine-scanner-split` and `application-closure` → `status=PASS`, `reasons=[]`, and their `manifest_digest`/`sql_inventory_digest`/`closure_digest` are byte-identical before and after the round-4 change); three adapters exactly `AUTHORITY_LOCKED_PRODUCTION_CAPABLE`; nine authorities false on every artifact of the W0→W5 chain; W0→W5 PASS on a clean checkout with **each** of W0/W1/W2/W3/W4 tampered in turn yielding NOT_PASS. Disposable PG lanes executed for real at `3412281ac` (40 passed, 0 skipped); the six target-host probe tests skip because they need Linux + `systemd-run` and are **not** counted as coverage. The receipts were re-emitted at `c2a7263ce` and committed as `25d501366`; round 5 added the decoy-fence and heading-uniqueness discriminators and corrected this row, and the receipts were then re-emitted once more at `aaee7f1a2`, the head carrying it, and committed as `456bd0c20`. No runtime was contacted and no effect produced in any round. |
| 2026-07-26 | S2-WP4-W2 | **WP4 W2 (runnable application) landed** — PR #140 merged 12 commits. (a) Engine-scanner privilege split §2.1: the consumer has zero import path to the retention repository (AST closure BFS plus a synthetic-reintroduction refusal), retention moved to its own runner, and a closed `pg_acl_manifest_v1` derived from the real post-split SQL surface is enforced **both ways** — an unlisted statement and a never-exercised grant each fail. (b) Application closure §8.1 #3: a checked-in reviewable allowlist plus a committed-blob manifest builder (dirty tree, non-HEAD, symlink and deny-listed content each a typed refusal), and the consumer's §8.3 production ABI hard-compares a whole-tree digest with zero git/module-relative/ambient fallback. (c) §8.3 renderers and builders: the rendered unit is byte-identical to the design's ini block (now bound by a test that extracts the block from the design doc), every hardening setting is re-parsed and a weakened one is a typed rejection, and the exported invocation contract discharges the argv half of `WP3_INVOCATION_FINGERPRINT_ALIGNMENT_REQUIRED`. Five-leg review (E2 PASS→RECHECK_PASS, E3 PASS_WITH_NOTES, E4 EVIDENCE_CONFIRMED, CC PASS_WITH_NOTES grade A, OPS PASS_WITH_CONCERNS) plus 11 Codex findings — every P1 closed in-branch with a regression that fails against its pristine parent. The load-bearing ones: the §8.3 resident reconnect was missing entirely so any outage over 90s exhausted the systemd start limit into a permanently failed unit; a data-modifying CTE bypassed the no-DELETE gate; `parquet_etl` really reaches PostgreSQL with an env-DSN fallback yet sat in the runtime closure behind a scan exclusion; psycopg2 supplies no SQLSTATE for connection-time failures so the auth/HBA exit-78 path never fired (the fixture had encoded the false assumption as truth); the launch identity was unbound to application bytes; PUBLIC-inherited TEMPORARY made `database_temp:false` unprovable and the disposable fixture was masking it; owned-path projections hashed working-tree bytes while certifying an unchanged HEAD; and the three bundle builders were named as strings but never executed. CC's §10.1 path-scope amendment (§10.1.1) landed as a publication precondition. | branch `agent/aiml-s2-wp4-w2`; final head `98a7f0d4eb47338161a13cf6ce22dffdcf227c4f`, PR #140 merge `c0f89159c550c2de939adfbd99078317911ad890`; receipts `S2.4-WP4-W2/` bound to `a3ddecbf4`, W2 wave-exit `sha256:9bb1dc5e…2862`; whole tree 5235 passed/46 skipped; GitHub main == Linux `trade-core` @ `c0f89159c` (ff-only, clean) |
| 2026-07-26 | S2-WP4-W3 | **WP4 W3 (typed host drivers) built and reviewed; source-only, zero runtime reach** — W3a: the capability probe (host actions behind an injected driver protocol; `driver=None` returns typed `EXTERNAL_VERIFICATION_PENDING` with zero mutation), two non-interchangeable attestation scopes whose substitution is blocked at core derivation because the property digest is computed from the scope, the PG topology observer (six drift classes each typed `PG_TOPOLOGY_UNPROVEN`), the §10.5 #39 fault-injection matrix and the W3 wave derivation. W3b: the PREPARE driver and five APPLY component drivers plus the credential broker, with a disposable PG apply→postcheck→rollback cycle on a real throwaway cluster. Two findings surfaced during the build: the forward grant sequence revokes TEMP/CREATE from PUBLIC, so a compensation that only revoked what the plan granted to the role would leave PUBLIC permanently stripped (closed by capturing the PUBLIC pre-state and restoring only surfaces it actually held); and exact restoration is provable at the effective-privilege level rather than the ACL-text level, because PostgreSQL never returns a materialized ACL to NULL — so the proof digests role attributes plus a `has_*_privilege` matrix and asserts the representation change separately instead of hiding it. Five-leg review: E4 EVIDENCE_CONFIRMED, but **E2 DONE+FAIL (six P1)** and **E3 PASS_WITH_NOTES naming five P1 arming blockers**; deduplicated, eight W3-owned defects, all closed with regressions that fail against pristine HEAD. Highest severity: uid/gid were unconstrained and pre-state was classified by name alone, so a manifest with uid=gid=26 would place the scanner on postgres's UID — postgres to the kernel, bypassing the entire PG role/ACL boundary. Also closed: driver exception text (systemd-creds argv, libpq connection options) reaching serializable `reasons` while the module's own secret scanner had no production caller; `ownership_evidence` accepting a bare `{}` so unowned policy/guard/evidence were overwritten then deleted by compensation; an incomplete PUBLIC capture silently under-restoring while reporting exactness; `exact_pre_state_restored` derived from "nothing raised"; task-owned re-apply stripping or removing existing resources; textual containment letting `..` escape the staging root; and every in-memory fixture minting `PLATFORM_ATTESTED`. Three W4-owned gaps are **recorded, not absorbed**, folded into the W3 exported ABI so removing one breaks the wave-exit digest: `PERMIT_PLAN_BINDING` (the authorization artifact carries only an id and field names, so no gate can bind a permit to one exact plan and §10.5 #36 cannot be satisfied by construction today), `REPLAY_LEDGER_APPEND` (the ledger head is read but never appended, so one permit drives unbounded operations), and `ENCRYPTED_BLOB_DIGEST_ORDERING`. | branch `agent/aiml-s2-wp4-w3`; W3a `676189236`, merge reconcile `408ba4de2`, W3b `9c22f770d`, review fixes `2445299c8`; tests/structure 3015 passed/6 skipped, ml_training 2547 passed/40 skipped; three disposable lanes ran for real (36 passed, zero skips); six frozen digests byte-identical; W3 receipts and publication pending the E2 recheck |
| 2026-07-26 | S2-WP4-W4 | **WP4 W4 (permits, durability, recovery, aggregate transaction) built, adversarially reviewed twice and remediated; source-only, zero runtime reach** — W4a closed three of the four obligations W3 recorded: `PERMIT_PLAN_BINDING` (`authorization_id` becomes a domain-separated derivation over the exact payload VALUES in a new required `payload_binding`, re-derived at the gate and independently per consumption site — against pristine main a permit signed for intent A drove intent B to `TERMINAL_CLEAN` with the driver engaged; after the fix the same substitution is `AUTHORIZATION_REJECTED` with zero mutation), `REPLAY_LEDGER_APPEND` and `INDEPENDENT_POST_COMPENSATION_POSTCHECK`, plus the §5.2 install lock, three WAL journals and the §9 TTL budget. W4b added the §10.2 aggregate entry point `apply_s2_4_install_plan`, startup reconciliation, the §5.3 matrix row by row and the per-effect-edge crash matrix. **The five-leg round against the W4b checkpoint found 3 P0 and 16 P1 — in the durability and authorization layer, not the permit layer, which E2 re-verified as sound.** The load-bearing defects: the WAL temp-file name came from a per-`JournalStore` attempt counter, so a crash in the one window the WAL exists for left a `.tmp.0` that wedged every later process **after both signed permits had already been durably consumed**; an unobservable installed unit was treated as observed-active, so one `systemctl` read timeout destructively rolled back a correct five-row install; `driver.row_driver()` during compensation sat outside any `try/except`, so an untyped `RuntimeError` escaped the frozen §10.2 ABI exactly when the host was partially applied; the install-lock proof was an unauthenticated caller dict and a released verdict passed forever; startup reconcile inspected only caller-named lanes **and** its install lane was plan-scoped, so a prior plan's stranded transaction was structurally invisible (plan A crashed with two rows applied, plan B ran to a terminal successful receipt); a terminal `FAILED` journal — §5.4's known residue — reconciled to nothing-to-reconcile; compensation authority was a shape check on two caller strings, so two literal `sha256:000…0` bought destructive reverse compensation; evidence freshness was caller-self-reported and a year-stale probe receipt passed; and the §9.1 trust-root pin was a self-consistency check inside a WP4-editable file. **Two reviewers then attacked the remediation and found a further 2 P0 and 9 P1 that the fixes had introduced or left** — the most consequential being that the SSHSIG verification which is the only key to `APPLIED_INACTIVE` had **zero** test coverage (a one-line regression minted it from the literal bytes `AAAAdeadbeef` with all 624 tests green, because every negative had mutated attestation fields and re-signed them, so each was caught by a field comparison and never by the signature); that `COMPENSATED_EXACT` could still be bought by a constant-returning verifier on the crash matrix's own second failure mode, because a mid-effect row failure produced no postcheck and the byte-identity check read `None` as *no constraint* rather than *not proved*; that `ALREADY_APPLIED_IDEMPOTENT` — introduced while fixing a false `RECOVERY_REQUIRED` — converted an unknown or partially-applied host into "no operator action is required"; and that the trust-root fix had pinned a copy no verifier reads, since every verifier resolves the facade and the facade module is itself inside `_W4_OWNED_PATHS` (confirmed dynamically). Mutation testing across both rounds ran 54 targeted mutations; the 13 survivors each now have a regression proven red under that exact mutation. **PM ruling:** `ATTESTED_EVIDENCE_CLASS_VERIFIER` moved from W6B to the source lane on the S2.0 precedent (`validate_apply_attestation`) — assigning it to an effect wave made it unsatisfiable and would have shipped §10.2's success identity into `SOURCE_READY` never once exercised. `APPLIED_INACTIVE` is now reachable **only** through a valid SSHSIG `s2_4_install_apply_attestation_v1`. That changes what keeps production out of reach from structural unreachability to key custody, so the Registry invariant for `s2_4_install_adapter_v1`, which still asserted the old property, was restated rather than left to read false. Recorded-not-absorbed: `ENCRYPTED_BLOB_DIGEST_ORDERING`, `OBSERVER_SPACE_PRE_STATE_DIGEST`, `PRIOR_LINEAGE_ENTRY_IDENTITY`, `PLAN_EXPIRY_OUTSIDE_SIGNED_CORE`, `INSTALLED_UNIT_PROBE_CORE_BINDING`, `EFFECT_RECEIPT_RECONCILE_BINDING`, `STARTUP_RECONCILE_SURFACE_ABSENT`, `STARTUP_JOURNAL_PARENTS_MUST_PREEXIST`, `RECEIPT_EMISSION_PENDING_IS_NOT_A_RECEIPT_RETRY`, `STRANDED_WAL_TEMP_FILES_ARE_REPORT_ONLY`, `ATTESTATION_EXPIRY_AND_HOST_TIME_ARE_NOT_CROSS_CHECKED`, `ATTESTOR_KEY_IS_NOT_SEPARATE_FROM_THE_PERMIT_KEY`, and the honest boundary that the replay ledger's consume-once is a filesystem-ACL property rather than a cryptographic one. | branch `agent/aiml-s2-wp4-w4`; W4a `55c99bb7c`, reviewed checkpoint `677a12df9`, remediation `f66fe0790`; whole tree 5965 passed / 46 skipped (5717 at the checkpoint, +248 tests, zero regressions, zero new skips, independently re-measured by PM); six frozen digests byte-identical incl. §9.1 profiles; both CLIs PASS with zero exclusions; W0→W1→W2→W3→W4 chain PASS with a tampered W3 yielding NOT_PASS; nine authorities false on every terminal including `APPLIED_INACTIVE`; receipts `S2.4-WP4-W4/` bound to `f66fe0790`, W4 wave-exit `sha256:79db18bb…7bad`; no runtime contacted, no effect produced |
| 2026-07-25 | S2-WP4-W0W1 | **WP4 W0 formal exit + full W1 (contracts/routing) landed** — PR #139 merged 17 commits: (a) formal, persisted, centrally-derived W0 receipts (`receipts/S2.4-WP4-W0/`, bound head `37cb9094`; production emitter `agent_governance_s2_4_install.py` — evidence-only receipts, ADMITTED/PASS derived exclusively by the central validator, typed fail-closed refusals, secret-scan on persisted evidence, hard-refuse on tampered historical lineage); (b) 2000-line governance split of `aiml_gate_receipt_validator.py` (3664→facade 1818 + 4 leaves) and its main test (3974→3 files + testkit) with 132 facade names, four frozen value digests and 123 test functions byte/count-identical, plus E2's two latent P1s closed (`resolve_facade()` sys.modules-aware routing kills the package-form dual-facade patch bypass; `_W0_OWNED_PATHS` extended to the full split family); (c) W1 remainder — three Registry adapters (`s2_4_capability_probe/prepare/install_adapter_v1`, exactly `AUTHORITY_LOCKED_PRODUCTION_CAPABLE`, effect-DAG `S2.0@EFFECT_DONE→W6A PREPARE→W6B APPLY`, driver deferred to W3), three CLASS routes with zero source-lane effect-node injection, per-profile SSHSIG/freshness/TTL verification + hash-chained replay consumption binding (wall-clock fail-closed default), central W1 wave derivation with live-registry ABI folding (adapter substitution/downgrade breaks the receipt), forged negatives per §10.5 #3/#9/#27/#36; (d) formal W1 receipts (`receipts/S2.4-WP4-W1/`, bound head `58d62a0e1`) carrying the full five-leg provenance — E2 PASS+RECHECK_PASS, E3 PASS_WITH_NOTES, E4 EVIDENCE_CONFIRMED, CC PASS_WITH_NOTES (A), OPS PASS_WITH_CONCERNS — with every P1 and consensus P2 fixed in-branch (incl. CC/OPS registry denial-string drift). CI-environment fix: governance gate gains `fetch-depth: 0` (shallow checkout made the honest ancestry re-derivation fail-closed on CI) and `test_s2_4_w0_admission.py` is now explicitly in the gate (was silently outside the glob since PR #137). Final head `1cb1acdf2`: tests/structure 2547/6, ml_training 2406/39, validate PASS, CodeQL green. Nine authorities false; source waves only; open items carried: `WP3_INVOCATION_FINGERPRINT_ALIGNMENT_REQUIRED` (W2 renderer + WP3-side amendment), W6 builder obligations (replay-ledger head-anchoring, serial OPS-chain injection), CC/OPS P3 queue (repo-relative persisted_dir, refuse-if-exists, typed CLI errors). Current dispatch = **WP4 W2** (retention/scanner split, closed PG ACL, application closure/ABI, policy/unit renderer, base/app/launch builders). | branch `agent/aiml-s2-wp4-w1`; reviewed/final head `1cb1acdf2349718035506466bf0588f7902d8fe9`, PR #139 merge `66d64da114751b1d7bced85b4b65ab151ade5b61`; W0 admission `sha256:d40bed03…baf12`, W0 wave-exit `sha256:6c86008f…8a16`, W1 wave-exit `sha256:3b462d91…001e`; GitHub main == Linux `trade-core` @ `66d64da11` (ff-only, clean) |
| 2026-07-25 | S2-WP4-UNIT-CONTRACT | **S2.4 unit-identity contract reconciled to `arcane-equilibrium-aiml-engine-scanner.service` (W0-deferred fork #4 + registry DRIFT #5 closed)** — PM contract amendment resolving the cross-WP unit-name fork logged in `S2.4-W0-admission-alignment.md` §7 #4/#5. The corrected S2.4 install design still carried six draft references to `openclaw-alr-shadow.service` (boundary banner, §5.1 PREPARE denial, §8 unit-name sentence, §8.2 FragmentPath postcheck, §8.2 lifecycle-denial sentence, §10.1 template path), contradicting both the landed WP3 amendment (PR #134, whose merged inventory/fence/schema constants bind the engine-scanner unit) and §8.3's own no-`alr_shadow`-identity rule. All six now name the canonical system unit, with a dated reconciliation note in §8; the §10.1 owned template path becomes `helper_scripts/deploy/arcane-equilibrium-aiml-engine-scanner.service.template` (the legacy `openclaw-alr-shadow.service.template` stays owned by the frozen user-level P0-B ALR lane and is no longer an S2.4 surface). Fork #5: the stale S2.1 registry prose (`alr_quiesce_fence_adapter_v1.authority`/`.invariant` still describing `systemctl --user` on the old unit) is updated to the system-level engine-scanner identity; the WP3 executable constants were already aligned and `agent_governance.py validate` stays PASS. Docs + registry-prose only; no schema/code/runtime/effect change; frozen S0.3 classifier untouched; nine authorities false. W2's unit renderer now has a single unambiguous unit identity — workers cannot silently choose. **Normative sync (Codex P1)**: the same amendment projects the operator-ratified system-level fence into the accepted-ADR layer (ADR-0050 dated 2026-07-25 addendum) and adds the missing `alr_quiesce_fence_adapter_v1` entry to `docs/agents/development-agent-governance.md` §4 (the AGENTS.md Registry-change sync rule; PR #129/#134 had left both unsynced), and the W0 fork log §7 #4/#5 receives dated supersession notes. **Scope disclosure (Codex P1)**: this reconciliation unifies the unit *name* only — `WP3_INVOCATION_FINGERPRINT_ALIGNMENT_REQUIRED` stays OPEN (WP3's C1 10-token `runtimes/…` fingerprint + 3-key env-hash vs §8.3's rendered `launches/… -I -B` full argv/ten-env contract), owed by the W2 unit renderer + a WP3-side fingerprint amendment, blocking the S2.1 EFFECT drill (not W0/W1 source admission; W0 binds the system-level property only). | branch `agent/aiml-s2-wp4-unit-contract-fix` atop main `9572a2c06`; files: `docs/execution_plan/ai_ml_landing/design/S2.4-install-source-seams.md`, `docs/execution_plan/ai_ml_landing/design/S2.4-W0-admission-alignment.md` (§7 supersession notes), `.codex/agent_registry_v1.json` (prose fields only), `docs/adr/0050-development-agent-governance.md` (dated addendum), `docs/agents/development-agent-governance.md` (§4 adapter entry), this ledger; authority = operator-merged corrected S2.4 design (PR #130 §1/§8) + PR #134 `S2.1-system-unit-amendment.md` (canonical unit pin) + `S2.4-W0-admission-alignment.md` §7 #4/#5 (PM-deferred fork log) |
| 2026-07-25 | S2-WP3-UNIT | **S2.1 quiesce realigned to the system-level engine-scanner unit (Blocker-1 done)** — the second admission blocker of the operator's corrected S2.4 install design (PR #130 §1/§8). The merged S2.1 seam fenced a user-level `openclaw-alr-shadow.service` / `systemctl --user` / `alr_shadow` unit; §8 requires a system-level unit with a dedicated UID (per-component UIDs cannot be enforced by one user's systemd manager). This amendment retargets the inventory/fence/schemas/tests to `arcane-equilibrium-aiml-engine-scanner.service` (`User=aiml-engine-scanner`, content-addressed `ExecStart /opt/arcane-equilibrium/aiml/runtimes/<digest>/bin/python3 -I -m ml_training.alr_event_consumer --dsn-file %d/pg-dsn …`, `LoadCredentialEncrypted`, DB `trading_ai`, PG role `aiml_engine_scanner`, Unix socket, hardened) — the byte-for-byte unit identity WP4 §8 installs. The C1 invocation fingerprint became a 10-token content-addressed form (rejects `/usr/bin/python3`), plus a `posixpath.normpath` anti-traversal guard closing a `..`/`.` segment gap surfaced by the E2/E3/E4 review (E3: not exploitable — the confirm path already binds an operator-SSHSIG-signed cmdline fingerprint). Four in-place schema `const` retargets (§0-additive); frozen S0.3 classifier `sha256:1cf8c021…` byte-unchanged (CC + E4 independently recomputed); `PROGRAM_SCHEMA_PATHS` untouched; nine authorities false; source lane fail-closed `EXTERNAL_VERIFICATION_PENDING`. Reviews: PM-conducted 5-reviewer wave (E2 PASS / E3 PASS-LOW / E4 CLEARED / CC CLEAN / OPS PASS_WITH_CONCERNS) + E2/E4 re-verify of the fix. 94 tests passed; disposable lane ran on a real throwaway cluster; Linux governance-gate attestation green. OPS EFFECT-contract notes recorded for the S2.1 EFFECT operator packet (system-level fence needs root/polkit `manage-units`; corrected break-glass runbook). With the program-DAG amendment (PR #132), **both WP4 `SOURCE_READY` admission blockers (§11.2) are now cleared**. | branch `agent/aiml-s2-wp3-s21-unit-amend`; reviewed/build head `5103d9ee`, fix `a0daf7d9`, final up-to-date head `de7e97bea29ae1f27f10bddc37175322dcfeba0f`, PR #134 merge `e514f1e761ab9c1965a133f1f113e2e7ccd854df`; canonical unit `arcane-equilibrium-aiml-engine-scanner.service`; authority = operator-merged corrected `S2.4-install-source-seams.md` §1/§8 |
| 2026-07-25 | S2-DAG | **Canonical program effect-DAG amended to the corrected S2.4 install design (§1.2)** — governance-only projection change (Blocker-2 of the operator's PR #130 "PM decision"). The operator-merged corrected S2.4 install design removed the old `S2.1@EFFECT_DONE`-before-`S2.4` cycle (a real quiesce/restore drill needs the runtime that S2.4 installs and S2.5 starts). This amendment updates the operator-gated **apply** order in `PROGRESS.md` + `TODO.md` + `docs/agents/ai-ml-landing-delivery-protocol.md` from `S2.0→S2.1→S2.4→S2.5→S2.2B` to `S2.0→S2.4→S2.5A→S2.1→S2.5B→S2.2B`: S2.4 install (`EFFECT_DONE_INACTIVE`) → S2.5A bounded initial start + attest → **S2.1 quiesce/restore drill** → S2.5B final post-drill attest → S2.2B ingestion/LR1. `S2.1@EFFECT_DONE` is no longer an `S2.4` predecessor; S2.4's effect deps are `S2.0@EFFECT_DONE` (+ three-side sync + this landed amendment). The **`source_deps` build order `S2.0→S2.1→S2.4→S2.5→S2.2B` is unchanged** (S2.1 source still precedes S2.4 source — S2.4 installs the same system unit S2.1 fences). Until this landed, S2.4 production admission fails closed `PROGRAM_DAG_AMENDMENT_REQUIRED`; this is a prerequisite for a later WP4 `SOURCE_READY` (§11.2). No code/runtime/effect/schema change; nine authorities false; frozen classifier untouched. | docs-only: `PROGRESS.md`, `TODO.md`, `docs/agents/ai-ml-landing-delivery-protocol.md`; authority = operator-merged corrected `docs/execution_plan/ai_ml_landing/design/S2.4-install-source-seams.md` (PR #130 merge `331d33bdd`, §1.2/§11.2) |
| 2026-07-24 | S2-WP3 | **S2.1 quiesce/evidence/static-guard `SOURCE_READY` landed** (`P0-S2-PRODUCTION-EFFECT-SEAMS-SOURCE` step 3 under Amendment A2; `source_deps` `S2.0→S2.1` satisfied). New `alr_quiesce_fence_adapter_v1` (registry `declared_production_fence_disabled_until_s20_effect_and_operator_sshsig`): typed `quiesce_{intent,observation,result,rollback}_v1` schemas (centrally registered in `SCHEMA_FILES`; frozen S0.3 classifier digest `sha256:1cf8c021…d0ddbc` byte-unchanged, `PROGRAM_SCHEMA_PATHS` untouched); an owner/process/unit/cgroup/env/restart/watchdog/queue/credential inventory (`agent_governance_alr_quiesce_inventory.py`, split from the applier to hold both modules under the 2000-line cap) that confirms a **single** ALR owner (C1 exact 9-token `--max-batch`-bounded invocation with `basename==python3`, C2/C3 `candidate_pids==[main_pid]` ∧ `main_pid_invocation_ok`; AMBIGUOUS/STALE/DOWN all fail-closed) before `apply_quiesce_fence` fences it via the unit's **own** `systemctl stop` (never pid/name/pattern), with a try/finally un-fence guarantee that emits a typed `FAILED` (never a bare raise) if the observation window or stop path throws, and a C4 TTL cross-field bound (`duration_seconds + sample_interval_seconds ≤ min(ttl_seconds, MAX_AUTHORIZATION_TTL=900)`) checked at build and validate. Route CLASS injects no effect node; SSHSIG namespace `arcane-equilibrium-aiml-s2-quiesce-fence` (domain-separated from S2.0/S1); `operator_signature_pem` strict-base64 armor enforced unconditionally. Production fence is fail-closed `PENDING`/`EXTERNAL_VERIFICATION_PENDING` in the source lane even with a valid SSHSIG (arming needs `S2.0@EFFECT_DONE` + real allowlisted host-probe/fence-ops + operator SSHSIG at the S2.1 EFFECT session); nine authorities false; no production socket; no secret serialized. Reviews: 5-reviewer wave (E2/E4/CC/E3/OPS PASS/PWC, no blocking P0/P1) + 4 Codex P1 + a C4-TTL cross-field addition, all fixed + threads resolved. **CI-defect disclosure**: the required-glob governance job first failed on a Linux `execve` `E2BIG` (`Argument list too long`) in the unrelated `test_agent_governance_command_capture_v2.py` — root cause was that one outlier test passed the compiled context artifact as a single `--context-artifact` argv element, and this WP's registry/route/schema additions grew that artifact from 129826 to 133040 bytes, crossing the 128 KiB `MAX_ARG_STRLEN` per-argument limit (measured in isolated worktrees; deterministic, not environmental). Fixed in `64eba19c1` by aligning the test with the canonical `@<context.json>` file convention already used by every real caller (`AGENTS.md`, all role cards, `test_development_agent_governance.py`); no production path was affected. Evidence `LOCAL_REPRODUCIBLE` (86 offline + disposable, disposable ran on a throwaway PG cluster). All 8 required checks green on the up-to-date head. | branch `agent/aiml-s2-wp3-s21-quiesce`; reviewed head `5fe1fc11a9b502483899d5b8f187a84f183423d3`, `@file` CI fix `64eba19c1fc49daf101f55e20d5c34dc4479c9a8`, final up-to-date head `c8e88c65244b7fd2d64ac8adf83132d889bc4396`, PR #129 merge `31ef0b4bc1ad74500b7c2439efe03e4cb146ffc2` |
| 2026-07-24 | S2-WP2 | **S2.0 observer-bootstrap `SOURCE_READY` landed** (`P0-S2-PRODUCTION-EFFECT-SEAMS-SOURCE` step 2 — first effect-seam SOURCE predicate under Amendment A2). New `pg_observer_bootstrap_adapter_v1` (registry `declared_production_apply_disabled_until_operator_sshsig`): typed `pg_observer_bootstrap_{intent,result,postcheck,rollback}_v1` schemas (centrally registered in `SCHEMA_FILES`; frozen S0.3 classifier digest `sha256:1cf8c021…d0ddbc` byte-unchanged, `PROGRAM_SCHEMA_PATHS` untouched), a structured-SQL-allowlist minimal read-only observer role (`NOLOGIN` + all `NO*`, `GRANT USAGE`/`SELECT` only, role-level `default_transaction_read_only=on`/`search_path=pg_catalog`, peer/ident auth), a route CLASS with no effect-node injection, and a SOURCE-only (unwired) closure binding. `operator_signature_pem` locked to a strict-base64 SSHSIG armor, enforced unconditionally at build+validate across all result statuses (secret-hygiene iterated to root cause). Production apply is fail-closed `EXTERNAL_VERIFICATION_PENDING` even with a valid SSHSIG in the source lane; nine authorities false; no production socket; no secret serialized. Reviews: 5-reviewer wave (E2/E4/CC/E3/OPS PASS/PWC, no blocking P0/P1) + 4 adversarial delta/closure gates (E2 surfaced two real scan-excluded-field secret-leak gaps → root-cause unconditional closure) + 4 Codex P1/P2 (postcheck full-identity binding, non-dict verifier-capture reject, `ALTER ROLE` actually applied, canonical governance-doc record) all fixed + E2 re-review PASS + threads resolved. Evidence `LOCAL_REPRODUCIBLE` (59 offline + disposable real-PG 42501/28P01). `EFFECT_DONE` deferred to the S2.0 EFFECT session (operator SSHSIG + real production socket + platform attestation + route/closure wiring). | branch `agent/aiml-s2-wp2-s20-observer`; reviewed head `b831f3ff0cd86386374b06e3ebbeff2ed4d7a5a9`, PR #127 merge `e86e945bf749d64220fbedd21318f5968c92736d` |
| 2026-07-24 | S2-WP1 | **WP1 source-debt closure landed** (`P0-S2-PRODUCTION-EFFECT-SEAMS-SOURCE` step 1, under Amendment A2). Central-validator registration of `sealed_build_receipt_v1` + `expected_identity_receipt_v1` + additive `source_compatibility_receipt_v2` (offline-structure; the frozen S0.3 classifier digest `sha256:1cf8c021…d0ddbc` is byte-unchanged — `SCHEMA_FILES` ∉ digest input; `PROGRAM_SCHEMA_PATHS` untouched). Additive `learning_runtime_manifest_v2` binds `dependency_lock = {spec_digest, lock_digest}` (both `requirements-ml.txt` and the real `requirements-ml.lock`, via `verify_lock_closure`) and folds `parquet_etl.py` compute into the learning-code identity; v1 is byte-frozen (`learning_runtime_digest sha256:6cf76b60…` reproduces). A launcher value-guard (`ALR_EXPECTED_LEARNING_RUNTIME_DIGEST_V2`) fails closed before any DB unless the committed v2 identity reproduces from the checkout and matches the operator pin (inert source; the S2.4 unit sets it). Reviews CC CLEAN / E2 PASS / E3 PASS / E4 PASS_WITH_CONCERNS (no P0/P1) + a consolidated hardening pass. Amendment A2 (effect predicate split) adopted. Source-only `NONE` effect; nine authorities false. | branch `agent/aiml-s2-wp1-debt`; reviewed `0e1a5a3750f2b089bf3e6c8eb8f87dd30a685820`, PR #125 merge `6f2481c9332be89179602dc986e205998d233e81`; v2 `learning_runtime_digest sha256:58bb9cc3a827872284196f57811227d367e4ff4aed5f3b22a031df1b39904c62` |
| 2026-07-24 | S2.3 | **`SOURCE_READY`** (LR2 sealed immutable runtime build + expected-identity). A real `requirements-ml.lock` (`uv pip compile --generate-hashes`, 38 pinned / 0 unpinned, target `x86_64-unknown-linux-gnu`) seals the S1.6 `content_addressed_fixed_path` runtime; `sealed_build_receipt_v1` + `expected_identity_receipt_v1` bind the S1.3 identity matrix, S1.4-B candidate digest and the S1.6 schema-level choice, with all `production_*` / `running_attested.*` / `load_verified_on_target` const false, `observation_owner` const `S2.5_LR6`, nine authorities false. Self-validating (central-validator registration deferred as a serialized follow-up, keeping it disjoint from S2.2A). Reviews E2/E3/E4/CC/OPS PASS_WITH_CONCERNS → consolidated fix → E2 recheck PASS; two Codex P2 (target_platform pinned to the lock, S1.6 schema-digest now offline-verified) fixed + threads resolved; FA/CC final cross-cutting audit CLEAN (no P0/P1). Full CI green including the heavy `learning-runtime-sealed-build` job (real fetch-once/clean-offline `--require-hashes` install + `-I` import on the Linux target). NOT a running attestation. | branch `agent/aiml-s2-3-lr2-sealed`; reviewed `73b083e9b76b6f0a6c7971cc9f06d1c8c0651f66`, PR #122 merge `051df8262da85123213bd0937ad03c206152f5a3`; sealed_build self `sha256:169d2e6c9a8b90d4adc4e06897f854943acf31f9ed0e0771850e5c5007b4c852`, runtime_content `sha256:8b2092e8f55ac0484ed597fe906fecb083fdbbf106367728be71decd911d7fea`, closure `sha256:26307134daaa63d0ddd6a5fee0abf363fe243e45add49052d6ef0ae53caeaeb1`; expected_identity self `sha256:a08c69657d80995793855e817c69c505c4b0e2142f0aa53231136acba7bf8baf` |
| 2026-07-24 | S2.2A | **`SOURCE_READY`** (LR1 scoped-compatibility identity). New `learning_runtime_manifest.py` computes a `learning_runtime_digest` over learning code + V151-V160 fingerprints + feature/label/action-policy contracts + dependency spec + runtime config, bound at preflight/spawn/finalize; a docs-only HEAD advance no longer stops ingestion; an incompatible training contract quarantines fit while compatible Scanner capture continues; `source_compatibility_receipt_v1` is emitted and the validator recomputes inner capture/training digests (anti-forgery). `NONE` effect; the LR1 runtime `DONE` is S2.2B. Reviews E2/E3 PASS + E4 PASS_WITH_CONCERNS after a fix round that closed the receipt-drift P1 + validator inner-forgery + evaluator-coherence + three firing bind-edge negatives; FA/CC final audit CLEAN (no P0/P1). | branch `agent/aiml-s2-2a-lr1-compat`; reviewed `7054a3b075171d9374fea935802d99740c0ef5da`, PR #121 merge `87a3a2503f7ef6e47cffdac3db80bbd3b1b1762b`; learning_runtime_digest `sha256:6cf76b60a763035d26d0d4e9e0e6aa0aa8877d99966367c778420e5f63a79595`, receipt self `sha256:a8fba423ebbd697d6bf2119a04affb710920c3421e1f14dfee9e2d3d4df64376`; evidence `LOCAL_REPRODUCIBLE` 415/1 core + 2259/36 tree |
| 2026-07-24 | S1 Sprint closure | **`S1_CLOSED`**. Operator SSHSIG, independent signature verification, signed-time closure replay, Linux S1.5/S1.6 effects, zero-residue postcheck and nine-authority false boundary were already authenticated at H_effect `6e1ea957a`. Direct Codex reviewed final PR head `da8e54148` with P0/P1/P2=`0/0/0`; all exact-head CI/CodeQL jobs passed (development-agent governance 8m27s, schema-consumer 6m30s, IBKR lane 6m9s), open code-scanning alerts=0 and unresolved threads=0. PR #115 merged with `--match-head-commit` as `22876b16d`. The immutable finalization artifact remains truthfully named `S1_CLOSURE_AUTHENTICATED_PENDING_MERGE`; this later merge evidence satisfies its declared publication predicate and the ledger projects the composite terminal state, rather than rewriting signed history. S2 READY pool opens as `S2.0 ∥ S2.2A ∥ S2.3`; no S2 effect is executed by this projection. | PR #115 reviewed `da8e54148a60fc7be38fe5844cf85b28b293a044`, merged `22876b16d3b00fcaafa4f2f46ae02b1c08c60b3b`; H_effect `6e1ea957af35544a844f704978366d11aa6c2364`; S1.5 `sha256:19498ba4303df77eb102e259526ec04a19c665673716280818ec5d0103b60a37`; S1.6 effect `sha256:0a0d050b8b555b1f8d627937c52a91a7bb0c132364fa8f78b0ccd640b64a89bb`; closure `sha256:e110598b83123f60881e982156913944de37bdf1bab1fdaabdc31c2b567e3dbc`; finalization `sha256:68bbced3a100c9e52e9f0845e600cce0552b1b67cf3a11d925f4b537dee86d6c`; exact-head review comment `https://github.com/yunancun/Arcane-Equilibrium/pull/115#issuecomment-5065722362`. |
| 2026-07-24 | S1-formal-closure-exact-head | `S1_CLOSURE_AUTHENTICATED_PENDING_MERGE` re-issued at `6e1ea957a` after the five forge-resistance P1s were repaired in `f6e009952` and the Linux gate's `ARG_MAX` publication blocker was repaired by moving the large inline-Context harness from argv to stdin and raising the governance ceiling to 20 minutes. The target-host child now authenticates a source-pinned operator SSHSIG over the exact intent/source head and rejects self-resealed root/launcher substitutions. The applier and distinct verifier captures must be the exact signed `ops_preflight`/`ops_postcheck` observer commands with complete semantic stdout; a structurally valid generic command or same-process residue assertion cannot close. S1 evaluates freshness at the SSHSIG-bound bundle `issued_at`, and each mandatory non-OPS reviewer fragment cites the authenticated workflow wave that owns its digest. Linux `trade-core` freshly reran all six S1.5 disposable classes and all eight S1.6 target-host seams at exact clean H_effect; result `BINDING`, zero residue, observed==expected host, production PG `:5432` untouched. The authorized key signed both the short-lived apply permit and final closure bundle. Independent `ssh-keygen -Y verify`, trusted finalization and signed-time historical replay all PASS; errors empty; nine authorities false. `S1_CLOSED` remains withheld only for the artifact/docs exact-head review, required CI, PR #115 merge, final ledger projection and three-way sync. | H_effect `6e1ea957af35544a844f704978366d11aa6c2364`; intent `sha256:0da89b2ff22d2fc2ebe01feafb664dab8984369d39dd1974b743b02aa6106da9`; apply permit `sha256:fb7aaf2fe34268d6b811781816968cd6ae8a784f71f21cacb0a0561dcdc0232a`; S1.5 `sha256:19498ba4303df77eb102e259526ec04a19c665673716280818ec5d0103b60a37`; S1.6 effect `sha256:0a0d050b8b555b1f8d627937c52a91a7bb0c132364fa8f78b0ccd640b64a89bb`; closure `sha256:e110598b83123f60881e982156913944de37bdf1bab1fdaabdc31c2b567e3dbc`; trusted bundle `sha256:cdbed2fcacfa26f93d5c6a0a8e36f604df8b6fb28bbf5793d9a0baceea9bd0b7`; signature `sha256:bd84abf880954177c6d23367ac2c6ca907003eaad22e044ad0bb16193727635e`; landing attempt `sha256:b572eb279056b4647c93cdad5333de9ff9ecaaeb559e8b181e6e73538a429e1e`; finalization `sha256:68bbced3a100c9e52e9f0845e600cce0552b1b67cf3a11d925f4b537dee86d6c`; fingerprint `SHA256:uGJ9veN7PoE6BBgfsSP2aiMndrwgbt7o/7/YfdzNzCQ`; durable causal record under `receipts/S1-closure-fix-2026-07-24/`. |
| 2026-07-24 | S1-formal-closure-fix | `S1_CLOSURE_AUTHENTICATED_PENDING_MERGE` — the operator signing blocker is resolved and the final adversarial regression is closed. The first artifact-generation attempt exposed that one generated JSON line could bypass the count-only caller-preview bound and exhaust every standard Context budget; commit `43735ff3d` byte-bounds each preview while retaining a digest over the complete original match set. A deliberately mistyped full SHA then exposed that the target-host driver trusted caller `--source-head` without checking its actual worktree; commit `102b1bb85` now requires exact lowercase 40-hex equality with a completely clean target-host `HEAD`, with load-bearing mismatch/dirty negatives. Historical replay then exposed that the finalizer persisted the run-start time as `evaluated_at`, which could precede later Context source `valid_from`; commit `e6572b96e` captures the replay timestamp only after trusted finalization and adds a load-bearing ordering regression. Exact-head CI then exposed that the new finalizer regression did not match the required governance glob and that the complete gate was cancelled by its five-minute job ceiling; commit `45a854fa6` moves the regression under the required glob and statically locks a 10-minute ceiling. At exact final H_effect `45a854fa6638aa0be677a2b705f42fe8f417ac95`, Linux `trade-core` revalidated the still-fresh, source/schema-identical S1.5 six-class `effect_seams_ready_receipt_v1` (`PASS`) and produced a new S1.6 target-host effect (8/8 seams, `binding=BINDING`, zero residue, production PG `:5432` untouched). The finalizer bound real PA/CC/E3 provenance, separate governed OPS preflight/postcheck captures, workflow DAG/wave, complete closure packet and both effect receipts into one trusted-execution bundle. The operator-authorized SSH agent key `SHA256:uGJ9veN7PoE6BBgfsSP2aiMndrwgbt7o/7/YfdzNzCQ` signed it; the domain-separated SSHSIG independently verifies for identity `aiml-s1-target-host-operator-v1` and namespace `arcane-equilibrium-aiml-s1-target-host`. Immediate trusted-host validation and receipt-time historical replay both PASS. Finalization status is `S1_CLOSURE_AUTHENTICATED_PENDING_MERGE`, errors empty, nine authorities false. `S1_CLOSED` remains withheld until exact-head Codex review, required CI, PR #115 merge, final ledger projection, and three-way sync complete. | H_effect `45a854fa6638aa0be677a2b705f42fe8f417ac95`; S1.5 `sha256:ab63d9db3682e94be195446e4e4d9a586d1ef327427547d88347d934914b140f`; S1.6 effect `sha256:9f8f40b15598822544f0dd8618429ae3c6c2ac2b153d8b3acd70094b73fffd99`; closure `sha256:eeef47cca1bcbfd44fb917759539b6afd06610669ec65a3be9e30b27a1f46de1`; trusted bundle `sha256:c9c7756940fb2493c14a6e45261b236fcb40c1e72697696e1172cb4d8cefe359`; signature `sha256:3ab69e6211127937441ab8d574bf78142ac6c551b6968020b5dc1210d0cf0c19`; landing attempt `sha256:e2522fe13952fa4203fa3ecc9609cbce3db04bb1e01a16daa9753c0e3ed0243a`; finalization `sha256:ed6635c44cae7e7e758e4dfa419e505ebfe111158abed48280ae7450b80b58a0`; durable artifacts and causal record in `receipts/S1-closure-fix-2026-07-24/`. |
| 2026-07-24 | S1-formal-closure-fix | `S1_EFFECT_RERUN_BINDING_BLOCKED_OPERATOR_SIGNING` — after the source fixes + independent E2/E3/E4/CC review, the real bounded target-host effect was rerun on `trade-core` against the **committed final head** `5705a480a` (fresh throwaway `git worktree`; runtime `main` untouched). The rerun surfaced and fixed a **real regression** the fix introduced: the P1 #2 child launched the probe under `python3 -I`, whose `-s` excludes user-site packages, so the pg_identity seam's user-site `psycopg2` could not load → the seam falsely DEFERRED and the S1.6 runtime choice downgraded BINDING→PROVISIONAL. Switched the child to `python3 -E` (still ignores PYTHON*/PYTHONPATH ⇒ injection-safe; security boundary unchanged). Codex re-review of the pushed head then raised **two further P1** — the verifier capture was bound as ≠applier but not to its **declared verifier node** (an unrelated read-only capture could back a fabricated postcheck), and the child authorization capsule could **outlive its intent** (fresh 120s TTL, unbounded `ttl_seconds`); both fixed (closure now requires `capture.node_id == ops_postcheck` verifier node; capsule `expires_at = min(now+TTL, intent_expiry)`, `ttl_seconds ∈ [1,120]`, and the child rejects a capsule outliving its intent) and the on-host effect was re-run against the final head. Re-run result: **all 8 fixed-path seams `PASSED_TARGET_HOST`, `final_choice=content_addressed_fixed_path`, `binding=BINDING`, `effect_status=PASS`, observed==expected==trade-core, ZERO residue (prod PG :5432 untouched)**. The distinct-verifier governed `command_capture_v2` passes the full `validate_governed_command_capture`; offline Mac re-verification of the persisted artifacts: `validate_target_host_effect_result(require_success=True)` / `validate_governed_command_capture` / `validate_target_host_effect_binding` **all PASS**. The only remaining `S1_CLOSED` gate is **one operator out-of-band SSHSIG** over the trusted execution bundle (existing S0.3 key; private key on neither Mac nor trade-core). Honest terminal: **`BLOCKED_OPERATOR_SIGNING_ACTION`** (all internal work + effect + artifacts complete; only signing remains). Nine authorities false; source_adoption_only. | H_effect/H_final source `5705a480a` (PR #115); driver `helper_scripts/maintenance_scripts/aiml_s1_closure_target_host_run.py`; 12 persisted artifacts + `RUN_RECORD.md` + offline `closure_verification.json` (all PASS) in `docs/execution_plan/ai_ml_landing/receipts/S1-closure-fix-2026-07-24/`; effect `receipt_digest sha256:6a605616…bd40`. |
| 2026-07-23 | S1-formal-closure-fix | `S1_PR114_FINDINGS_FIXED_SOURCE_COMPLETE` — closes the three Codex findings that PR #114 merged before review completed (2×P1 + 1×P2), plus the S1 signing decision and the CI-selection gap. **P1 postcheck binding**: `validate_target_host_effect_binding` no longer accepts a bare `source==ops_postcheck` label — a PASS closure now requires the distinct-verifier UPGRADED effect result (structured `verifier_capture_digest` non-null; applier self-run alone cannot close), an ops_postcheck bound to the verifier's own governed `command_capture_v2` (structural+self-digest integrity, distinct role/node/process/capture from the applier), a clean residue observation (nonzero fails closed), `source_head`/`host`/`observed_at` bound to the receipt, a three-way digest cross-check, and acceptance binding all three evidence ids. **P1 process-global auth**: applier no longer mutates parent `os.environ[AIML_TARGET_HOST_PROBE]` — the real runner passes an intent-derived authorization capsule over a one-time stdin pipe to an isolated `python3 -I` child (new `agent_governance_target_host_child_apply.py`) that re-validates and opens the gate only in its own env; direct/expired/tampered/replayed/host-mismatch all fail closed. **P2 WORM**: readback + dedup now require observed `retain_until` ≥ approved (tz-normalized). **Signing (Amendment A1 §6)**: S1 profile reuses the S0.3 trust root under the domain-separated S1 identity+namespace (placeholders removed; no second key). S1.2A external-CAPABLE source adapter is complete; the external S3 Object-Lock **effect** (real bind/append/readback) is **S8.6**, NOT an S1 blocker; an S3 config is no longer required to close S1. `S1_ENGINEERING_CLOSED_EXTERNAL_WORM_BINDING_PENDING` is superseded — the only remaining S1-closure gate is the out-of-band operator SSHSIG over the target-host execution bundle (its private key is on neither Mac nor trade-core). Nine authorities false; source_adoption_only; no runtime/broker/order. | Branch `agent/aiml-s1-closure-p1p2-fixes`; source commit `af2491300` (base `origin/main` `7d78765a2`). +38 focused tests across `test_target_host_effect_adapter.py` / `test_target_host_apply_orchestrator.py` / `test_terminal_receipt_external_sink.py` / `test_agent_governance_aiml_trusted_host.py` + new `test_agent_governance_target_host_child_apply.py`; full local `tests/structure/` green (2138 passed, 6 skipped). Design: `docs/execution_plan/ai_ml_landing/design/S1.6B-real-target-host-probe.md` §11. Independent E2/E3/E4/CC review + Linux bounded-probe rerun + `closure_packet_v1` follow this event. |
| 2026-07-23 | S1-formal-closure | `S1_FORMAL_GOVERNANCE_CLOSURE_ENGINEERING_COMPLETE` (Waves A-C). The S1.6B target-host probe is now a fully closure-admissible effect seam: central-validator registration of `learning_runtime_choice_receipt_target_host_v1` (structure-only offline; STRICT attested at the effect/closure lane), additive `aiml_landing_session_attempt_v1` (own validator; rejects the S0.* family), a `target_host_probe` route class + `target_host_disposable_runtime_probe_adapter_v1` + typed intent + dedicated `target_host_effect_result_v1`, an intent-derived apply orchestrator (probe auth from a validated intent, NOT a user env var), a distinct (role+process+capture) verifier, an SSHSIG S1 signer profile (reuse `_verify_ssh_signature`; S0.3 byte-identical; real key operator-gated), and the S1.2A S3-Object-Lock external-CAPABLE WORM source adapter (fail-closed w/o creds; AWS-secret smuggling closed; non-destructive readback). A REAL bounded non-root disposable probe ran on `trade-core` through the orchestrator → binding=**BINDING** (8/8 fixed-path seams real; OCI NON_SATISFIABLE), evidence_class=PLATFORM_OR_EXTERNAL_ATTESTED, observed_host==expected==trade-core, **zero residue** (prod PG :5432 untouched). The real effect result + choice receipt + `aiml_landing_session_attempt_v1` are producer-generated + central-validator PASS. Frozen S0.3 classifier digest byte-identical (`sha256:1cf8c021…d0ddbc`); nine authorities false; source_adoption_only. Terminal: **`S1_ENGINEERING_CLOSED_EXTERNAL_WORM_BINDING_PENDING`** — a fully-attested `S1_CLOSED` is gated on TWO operator inputs (the S1 target-host SSHSIG signer key + signing, and the external S3 Object-Lock config), emitted as one `external_verification_pending` request. | Branch `agent/aiml-s1-formal-closure`: Wave A `d942d6fc3`, Wave B `9ea6be9b9`; design `docs/execution_plan/ai_ml_landing/design/S1-formal-closure-wave-a.md`; run record `docs/execution_plan/ai_ml_landing/receipts/S1-formal-closure-target-host-run-record.md` (effect receipt `sha256:e2b838e8…`, choice `sha256:447ab474…`, attempt `sha256:5f174e05…`). PA design → CC/E2 architecture gate → E1 builds → E2/E3/E4 hard-edge reviews → fix rounds; repair plan Amendment A1 + delivery-protocol S1.2A/S8.6 WORM split. |
| 2026-07-23 | S1.6B | `S1.6_TARGET_HOST_ATTESTATION_REAL_ON_TRADE_CORE`. Closes the "Linux attestation OWED" on the S1.6 runtime choice (Codex PR review: S1.6 had made a BINDING choice from a Mac-local disposable stand-in without the real target-host probe). The real non-root probe ran on `trade-core`: **7/8 fixed-path seams `PASSED_TARGET_HOST` on genuine kernel evidence** (`start_stop`, `cgroup_resource_isolation`, `network_denial`, `native_lib_loading`, `immutable_closure_persistence`, `failure_rollback_cleanup`, `pg_identity`=real disposable initdb cluster → 42501); `independent_postcheck` is DEFERRED by design in the applier receipt and earns PASS via a distinct verifier's clean residue sweep → `content_addressed_fixed_path` is **BINDING** on real target-host evidence; OCI stays `NON_SATISFIABLE_NON_ROOT`. Executing the probe for real surfaced that three as-designed primitives are blocked by the host policy `apparmor_restrict_unprivileged_userns=1` (Ubuntu 24.04; no `newuidmap`) — each was swapped to a real non-root primitive that preserves the seam's essential property (no forced PASS): network_denial `bwrap --unshare-net`→**seccomp** `SCMP_ACT_ERRNO(ENETUNREACH)` differential; cgroup OOM read-race fixed (`OOMPolicy=continue` + child-hog OOM-killed while main survives → real `oom_kill`); native_lib copy-a-system-lib→**compile a unique-soname `.so`** + maps-origin + callable symbol(=42), with a direct-load fallback since bwrap mount-isolation is AppArmor-blocked. Independent **E2** adversarial review (reproduced on trade-core) confirms all three are HONEST real-effect proofs — every fabrication/absent-mechanism/no-baseline path honestly DEFERs; prod PG :5432 untouched; zero residue after every run. Two honest closure obstacles recorded for follow-up: (a) `session_attempt_v1` is schema-pinned to S0.3 (const `AIML-S0.3-GOVERNANCE-ADOPTION`) so the durable-attempt record for S1.6B is `task_execution_admissions_v1`, not a literal `session_attempt_v1`; (b) governed `capture-command` env-strips `AIML_TARGET_HOST_PROBE`/`XDG_RUNTIME_DIR`, so it attests the source/structural layer (`LOCAL_REPRODUCIBLE`) — the real seams are `PLATFORM_OR_EXTERNAL_ATTESTED` via on-target run + distinct OPS on-host observation (design §3), a different assurance class. Nine authority grants remain false; no production PG/deploy/runtime/broker/order occurred; `source_adoption_only` unchanged. | Branch `agent/aiml-s1.6b-real-probe` — harness+3-seam-fix `e98d743fb`, E2-P3 cleanup `17f259dd8`; design `docs/execution_plan/ai_ml_landing/design/S1.6B-real-target-host-probe.md` §10; on-target pytest `tests/structure/test_agent_governance_target_host_probe_on_target.py` 5 passed (incl. explicit cgroup + native-lib PASS), Mac logic suite 64 passed; E2 verdict: HONEST real-effect proofs, no P0/P1, only P3 cosmetic (all fixed). Pending: exact-head PR → Linux CI → merge. |
| 2026-07-23 | S1.1-S1.6 | `S1_SOURCE_COMPLETE_ON_REVIEW_BRANCH`. All six Sprint-1 sessions are source-complete and committed on branch `agent/aiml-s1-landing` (atop main `5362fdd4b`). Each session passed its full per-session role chain (E2/E4/CC/E3/OPS/QA) with fix rounds, plus one cross-session adversarial review round — PA integration PASS, CC whole-body constitutional PASS, FA spec-compliance PASS. The digest-binding dependency chain is coherent and the frozen S0.3 classifier digest is byte-unchanged (`PROGRAM_ADOPTED` intact); the only frozen-surface edits were S1.2's legitimate terminal-sink CONTRACT_ONLY -> implemented flip and S1.5's matrix `adapter_binding_status` flip, both reality-reflecting with forge-resistance unchanged. Every S1 effect is disposable (DISPOSABLE_ONLY / DISPOSABLE_APPLY_ROLLBACK_POSTCHECK / TARGET_HOST_DISPOSABLE_RUNTIME_PROBE) and evidence class is `LOCAL_REPRODUCIBLE` (real 42501/28P01, os.replace/chmod, python3 -I on Mac dev); the platform-attested Linux trusted-host / CI test attestation is OWED and the S1 receipts are emitted disposably (not committed JSON like S0.3). The Sprint-1 exit `EFFECT_SEAMS_READY` is substantively composed by S1.5 (`effect_seams_ready_receipt` scope `S1.5_CONTRIBUTION`) + S1.6 (`learning_runtime_choice_receipt_v1`, final_choice `content_addressed_fixed_path`), but it is NOT yet a PM-declared ledger gate — it will be declared only after the exact-head merge and a green Linux CI/trusted-host attestation. Nine authority grants remain false; no production PG/deploy/runtime/broker/order occurred; `source_adoption_only` is unchanged. | Branch `agent/aiml-s1-landing` commits — S1.1 `3c0b7fb2f`, S1.2 `d12f84632`, S1.4 `7d0befd86`, S1.3 `08789982e`, S1.5 `0b2805a4d`, S1.6 `0e8e9fd9d`; per-session E2/E4/CC/E3/OPS/QA PASS + cross-session PA/CC/FA PASS; frozen S0.3 receipt digest `sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b` byte-unchanged; branch not yet merged/pushed — next step is the three-way sync (push -> exact-head PR -> Linux CI attestation -> exact-head merge -> ff-only), where the merge head and platform attestation land. |
| 2026-07-22 | S0.3 | `PROGRAM_ADOPTED` issued on Linux `trade-core` after the final P1 repairs and exact-generation adversarial closeout. A PR #110 P1 then correctly required independently verifiable durable finalization evidence; the producer-signed finalization attestation, trusted execution bundle and both SSHSIG sidecars were persisted and bound to the receipt/closure/source/GitHub digests. This closes S0 and opens `S1.1 ∥ S1.2 ∥ S1.4`; it does not claim runtime readiness or grant any runtime/effect/trading authority. | PR #106 merge `afa7eb2e97d6ab975709d4472b12f7397ee03bfb`; PR #107 merge `0cdd3537ead94675a4d0033df5bbcbf5d33b1b16`; PR #108 reviewed head `1a933fcc28e9f7341e023b5d401c479957c14c5f`, merge `fed223bebd278c50b0ab3330980e66441a30c9ed`; E2/E4/CC/E3/MIT/R4/QA PASS with no P0/P1/P2; Linux governed E4 `275/275`; finalizer closure `sha256:27f7b0041a418298ef49943f6f37283b603fce38f48f67f9a825f249f2615c63`; receipt `sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b`; persisted-signature regression `tests/structure/test_agent_governance_aiml_trusted_host.py` |
| 2026-07-21 | S0.3 | Source implemented + merged: scope/attempt/receipt/effect schemas (7), fail-closed `aiml_gate_receipt_validator.py`, Registry/router/closure integration, `terminal_receipt_sink_v1` contract-only (owner S1.2), and a strict GitHub repo-policy attestation contract. `SOURCE_READY`, not `PROGRAM_ADOPTED` — the receipt emission is a separate trusted-host step. | PR #104 merge `b945fe0f8db6bdf5f93657b3c404025ade4f2de4`, reviewed head `d6dd1f98470ddea7c1941fe572aa6f89071cf09d`; 8/8 required GitHub checks green; 7-role review (E2/E4/CC/E3/MIT/R4/QA) all PASS after E2-P1 (bind changed governance file) + E4-P1x3 (lock crown-jewel invariants) fixes were re-verified |
| 2026-07-21 | S0.3 | Independent Codex PR review found 3 verified P1 forge-resistance gaps on the adoption gate (reviewer bindings self-supplied not tied to authenticated fragments; no reviewed_head->merge_head ancestry proof; writer lease forced on the read-only finalization). All 3 fixed per PA minimum-coherent design and re-verified; the gate is now genuinely forge-resistant and (correctly) requires trusted-host attestation to mint `PROGRAM_ADOPTED`. | Hardening commit `d6dd1f98470ddea7c1941fe572aa6f89071cf09d`; re-review CC/E3/E2 PASS, E4 code-PASS-quality (CONCERNS only on Mac governed-attestation provenance -> Linux follow-up). See coverage debt below |
| 2026-07-21 | S0.2 | Accepted the advisory-serving source-policy authority after repairing and exactly rechecking three P1 blockers, source-policy QA acceptance, and final R4 cold review. Historical AMD bytes are unchanged. Effect is `NONE`; no ML5/ML6 implementation, runtime or Program adoption is claimed. | `docs/execution_plan/ai_ml_landing/receipts/S0.2-serving-authority-receipt-v1.json` (`sha256:0115dbd3dc62d84e183aae5a28cbfd252eb45ecee51a652d8a4a155f14dfb41a`); accepted head `f325b4dfdafd1979197c8a9e6450efeaf85e091c` |
| 2026-07-21 | S0.1 | Exact PR #100 publication and current three-source lineage reconciled; S0.1 is `DONE`. This immutable-lineage receipt grants no runtime, build or Program-adoption authority; S0.2 is next. | `docs/execution_plan/ai_ml_landing/receipts/S0.1-planning-documents-published-v1.json` (`sha256:8fc9417f984025deabdc1b83ace95921ccfff1acb26a1b29243fc0a0a5ba79ad`); current source `c2f5a2e26e422d56b8ec9b540d7f36bea9a0be54` |
| 2026-07-20 | PROGRAM | Four targeted correction-verification lenses accepted V2 after the no-candidate final-attestation branch was separated from trading attestation. Planning coverage is accepted; no implementation state advanced. | `ACCEPT_V2_FOR_PROGRAM_ADOPTION` |
| 2026-07-20 | S0.1 | Live read-only remote recheck found planning base `b486c071...` behind GitHub/local `origin/main=96d262450...` by seven commits; `TODO.md` overlaps. Integration is deferred to S0.1 and must preserve the unrelated IBKR delta. | `SOURCE_DRIFT_REQUIRES_CURRENT_HEAD_INTEGRATION` |
| 2026-07-20 | PROGRAM | Post-draft cold review rejected premature adoption/landing, coarse scope, impossible TTL overlap, effect-classification bypass and final-head drift; templates were reworked. No implementation state advanced. | `REWORKED_AWAITING_FINAL_ADVERSARIAL_ACCEPTANCE` |
| 2026-07-20 | PROGRAM | Initial V2 ledger designed in isolated planning worktree. No implementation, publication, runtime or effect occurred. | `PROGRAM_ADOPTION_REQUIRED` |

## S2.4 (WP4) unclosed owned obligations

**Generated, not maintained by hand.** The single source of truth is
`aiml_gate_receipt_wave_w5._W5_EXPORTED_ABI["remaining_owned_obligations"]`
(pure data in `program_code/ml_training/aiml_gate_receipt_w5_obligations.py`), and
`tests/structure/test_agent_governance_s2_4_install_w5.py` fails if this table and that
list disagree by a single `obligation_id`. Regenerate rather than edit:

```text
python3 -c "import sys;sys.path[:0]=['program_code/ml_training','program_code'];\
import aiml_gate_receipt_validator as v;\
[print(r['obligation_id'], r['typed_status'], r['owner_wave']) \
 for r in v._W5_EXPORTED_ABI['remaining_owned_obligations']]"
```

At the current W5 generation (source head
`f30ede36134f1464cef4bc025f808b931aebe180`, round-9 receipt carrier
`3df9d2f451ceb7ab66117de6e33fd882d59a9531`, ledger digest
`sha256:90f9261845b4782e577ef34e40369b3bcaf30381ce46d62d7246c087b3ed208e`)
every row below remains on the 28-row ledger; five rows now carry a
`CLOSED_BY_*` typed status (row 22 by S2.5 source under the PM O-1 ruling, and
rows 16/23/26 by `S2.4-AMEND-1` plus row 25 by `S2.4-AMEND-2`, both landed as
source in S2E.0), and the remaining rows are unclosed with their named owners.
Review history per generation: what was independently reviewed at the merged head `756a59ef7` (GitHub main
`427bd0dd7`, same tree), by two reviewers, is the round-3 remediation, the W5 emitter
(`agent_governance_s2_4_w5_emit.py` + the `w5-emit` CLI verb), the §10.1.2 owned-path
amendment, and the eight then-persisted W5 artifacts — none of which had been reviewed
before. Round 3's own head `a757c006f` was **never** reviewed by anyone; an earlier
version of this sentence named it as the reviewed head, which is the claim class §11.2
predicate 4 exists to check. That round returned 3 P1 plus six P2 (remediated as
`c2a7263ce`). Round 5 (reviewing `25d501366`) returned 1 P1 + 2 P2, remediated as
`aaee7f1a2` with the receipts re-emitted at that head and committed as `456bd0c20`.
The round-6 PM audit (at `456bd0c20`) reproduced one remaining P1 — the active-state
projection in `TODO.md` and this file still described the `c2a7263ce` generation and a
stale PM-owner projection — closed by the commit carrying this sentence together with
the discriminator regression
`tests/structure/test_aiml_w5_receipt_binding_projection.py`.

`owner_wave` names who must close it; `PM` rows are design decisions §10.4 forbids a
worker from taking. Full statements are in the W5 derivation record
(`receipts/S2.4-WP4-W5/S2.4-WP4-W5-derivation-record.json`, emitted by PM) under
`remaining_owned_obligations`, pinned by `obligation_ledger_digest`. An earlier
hand-maintained projection carried 13 of these rows; that is the drift this generated
section exists to prevent.

**The persisted receipt set in this tree is bound to `f30ede361` — the S2E.0 final
head (S2.4-AMEND-1/2 chain `223a479d7`→`e9b26e895`→`a48355a29`→`cc6a8b97a`, round-8
receipts `c911ea9c1`, projection `23037eb0b`, then the PR#153 Codex-review fixes
`f30ede361`) — and was committed one commit later, as `3df9d2f45` (round 9).** *Bound to*
and *committed at* are different facts and the artifacts state the first: each of the
eight carries `source_head = f30ede36134f1464cef4bc025f808b931aebe180`. Read the
binding out of the artifact field, never out of this paragraph; the discriminator regression
`tests/structure/test_aiml_w5_receipt_binding_projection.py` mechanically requires one
unique `source_head` across all eight artifacts and requires every claim of that form in
`TODO.md` and this file to equal the artifacts' actual value. Superseded generations,
each replaced rather than edited: the set bound to `cc6a8b97a` (S2E.0 AMEND head,
committed as `c911ea9c1`, round 8, same ledger digest), before it the set bound to
`5be472193` (WP5-tranche-2 head,
committed as `0eb90e40c`, round 7, ledger digest `sha256:57696d69…53ea`), before it
the set bound to `0faa6499d` (WP5 tranche-1
ledger-closure head, committed as `0b06982e0`), before it the set bound to `aaee7f1a2`
(round-5 remediation head, committed as `456bd0c20`, ledger digest
`sha256:fe3558a3…ca97`), before it the
set bound to `c2a7263ce` (round-4 remediation head, committed as `25d501366`), and
before it the set bound to `fcc44eca7` (committed as `756a59ef7`, ledger digest
`sha256:4548d526…e5af` — what stood here once called that set "bound to `756a59ef7`",
which was only the commit it landed in). The current `obligation_ledger_digest` is
`sha256:90f9261845b4782e577ef34e40369b3bcaf30381ce46d62d7246c087b3ed208e`, equal to the
live ABI digest at this head after the S2E.0 ledger change (rows 16/23/26 →
`CLOSED_BY_S2_4_AMEND_1_SOURCE`, row 25 → `CLOSED_BY_S2_4_AMEND_2_SOURCE`; rows kept,
owner unchanged, statements appended not rewritten, mirroring the O-1 precedent).
`persisted_dir` is repo-relative and `remaining_owned_obligation_count=28`.
The standing rule: a receipt is re-emitted after the last owned-path change of a round,
at the head that carries it. Round 5 edited a W5-owned test path, so PM re-emitted at
`aaee7f1a2`; WP5 tranche 1 edited `aiml_gate_receipt_w5_obligations.py` (W5-owned, at
`7e2c2c490`), so PM re-emitted at the tranche head `0faa6499d`; WP5 tranche 2 edited
the four W5-owned central-registration files (SCHEMA_FILES 78→79, the facade delegate
branch, the runtime-closure reseal and the count pin), so PM re-emitted at `5be472193`
(round 7); S2E.0 edited `aiml_gate_receipt_w5_obligations.py` and `…wave_w5.py`
(W5-owned) inside its atomic commit 3, so PM re-emitted at `cc6a8b97a` (round 8), and
after the PR#153 Codex-review fixes touched further owned paths PM re-emitted at the
round's final head `f30ede361` (round 9) — the emitter enforces the rule mechanically rather than in prose
(`W5_EMIT_REFUSED` on a dirty owned scope with zero files written;
`W5_RECEIPTS_EMITTED` on the clean committed head). Round 8 changed the ledger digest
(four typed-status flips); round 9 re-binds owned-path blobs only (ledger unchanged).

W5-RECEIPT-BINDING: source_head=f30ede36134f1464cef4bc025f808b931aebe180 carrier_commit=3df9d2f451ceb7ab66117de6e33fd882d59a9531 artifacts=8 round=9 status=COMMITTED

| # | `obligation_id` | `typed_status` | `owner_wave` | Spec refs |
|---|---|---|---|---|
| 1 | `ENCRYPTED_BLOB_DIGEST_ORDERING` | `OPEN_DESIGN_QUESTION` | `W6` | `§5.1`, `§7` |
| 2 | `OBSERVER_SPACE_PRE_STATE_DIGEST` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§5.2`, `§5.4`, `§6` |
| 3 | `PRIOR_LINEAGE_ENTRY_IDENTITY` | `NOT_PROVIDED_BY_W5` | `W6B` | `§5.1`, `§9.1` |
| 4 | `PLAN_EXPIRY_OUTSIDE_SIGNED_CORE` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§9`, `§9.2`, `§10.4`, `§10.5 #28` |
| 5 | `INSTALLED_UNIT_PROBE_CORE_BINDING` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§6`, `§10.4`, `§10.5 #36` |
| 6 | `EFFECT_RECEIPT_RECONCILE_BINDING` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§5.2`, `§10.4` |
| 7 | `STARTUP_RECONCILE_SURFACE_ABSENT` | `OPEN_BY_DESIGN_W6_RUNNER_PRECONDITION` | `W6` | `§5.2`, `§10.5 #39` |
| 8 | `STARTUP_JOURNAL_PARENTS_MUST_PREEXIST` | `NOT_PROVIDED_BY_W5` | `W6B` | `§5.2` |
| 9 | `RECEIPT_EMISSION_PENDING_IS_NOT_A_RECEIPT_RETRY` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§10.5 #8`, `§10.5 #14` |
| 10 | `STRANDED_WAL_TEMP_FILES_ARE_REPORT_ONLY` | `PARTIALLY_PROVIDED_BY_W4B` | `W6B` | `§5.2` |
| 11 | `ATTESTATION_EXPIRY_AND_HOST_TIME_ARE_NOT_CROSS_CHECKED` | `NOT_PROVIDED_BY_W5` | `W6B` | `§9.1`, `§10.2` |
| 12 | `ATTESTED_EVIDENCE_CLASS_VERIFIER` | `VERIFIER_PROVIDED_BY_W4B_ATTESTATION_PENDING` | `W6B` | `§6`, `§9.1`, `§10.2`, `§10.5 #13`, `§10.5 #14` |
| 13 | `ATTESTOR_KEY_IS_NOT_SEPARATE_FROM_THE_PERMIT_KEY` | `NOT_PROVIDED_BY_W5` | `W6B` | `§9.1`, `§10.2` |
| 14 | `REPLAY_LEDGER_CONSUME_ONCE_IS_A_FILESYSTEM_PROPERTY` | `OPEN_HONEST_BOUNDARY` | `W6B` | `§9.1`, `§10.5 #8` |
| 15 | `STARTUP_RECONCILE_LANE_PATHS` | `NOT_PROVIDED_BY_W5` | `W6B` | `§5.2` |
| 16 | `DEPENDENCY_REFRESH_RECEIPT_BINDING_ABSENT` | `CLOSED_BY_S2_4_AMEND_1_SOURCE` | `PM` | `§3`, `§10.4`, `§11.3` |
| 17 | `DEPENDENCY_REFRESH_REPRODUCER_NODE_IS_DECLARATIVE` | `PARTIALLY_PROVIDED_BY_W5` | `W6` | `§9.1`, `§9.2` |
| 18 | `DEPENDENCY_OBSERVATION_WINDOW_IS_CALLER_AUTHORED` | `PARTIALLY_PROVIDED_BY_W5` | `W6` | `§9.2`, `§10.5 #28` |
| 19 | `ENCODED_SECRET_SCAN_MISSES_COMPOSITE_PAYLOADS` | `RECORDED_NOT_CLOSED` | `W6B` | `§10.5 #15` |
| 20 | `PROGRAM_CODE_IS_ON_THE_SCANNER_PATH_VIA_W2_W3_AND_W4` | `PROVIDED_BY_W5_UNDER_PM_RULING` | `W5` | `§10.1.1` |
| 21 | `PR_SET_DUMPABLE_IS_DECLARED_NOT_ENFORCED` | `NOT_PROVIDED_BY_W5` | `W6B` | `§7`, `§10.5 #26` |
| 22 | `S2_5_LIFECYCLE_FIXTURES_DO_NOT_EXIST` | `CLOSED_BY_S2_5_SOURCE` | `S2.5A` | `§10.5 #29`, `§11.3` |
| 23 | `SOURCE_IDENTITY_FRESHNESS_HAS_NO_PRODUCTION_CALL_SITE` | `CLOSED_BY_S2_4_AMEND_1_SOURCE` | `PM` | `§8`, `§9.2`, `§10.1.1`, `§10.4`, `§10.5 #28` |
| 24 | `EMITTED_EVIDENCE_DIGESTS_ARE_UNAUTHENTICATED` | `RECORDED_NOT_CLOSED` | `PM` | `§10.3`, `§10.5 #27`, `§11.3` |
| 25 | `EXPECTED_TOPOLOGY_IS_CALLER_SUPPLIED_AND_UNSIGNED` | `CLOSED_BY_S2_4_AMEND_2_SOURCE` | `PM` | `§8.2`, `§10.1.1`, `§10.2`, `§10.4`, `§10.5 #25` |
| 26 | `EVIDENCE_CLASS_TO_SCHEMA_MAP_IS_MANY_TO_ONE` | `CLOSED_BY_S2_4_AMEND_1_SOURCE` | `PM` | `§9.1`, `§9.2`, `§10.5 #28` |
| 27 | `S1_3_ASSURANCE_CLASS_IS_INFLATABLE_WITHOUT_CHANGING_THE_PROJECTION` | `RECORDED_NOT_CLOSED` | `S1.3` | `§9.2`, `§10.1.1` |
| 28 | `OWNED_PATH_PROJECTION_RULER_IS_NOT_UNIFORM` | `RECORDED_NOT_ABSORBED` | `PM` | `§10.3` |

## W5 PM-owned obligations adjudication (2026-07-28)

PM adjudication of every `owner_wave=PM` row in the live ABI (six rows; the set below
is pinned set-equal to the ABI by
`tests/structure/test_aiml_w5_receipt_binding_projection.py`, so a PM row added to or
removed from the ABI without a matching adjudication row is red). Each row is classified
`source_closure_blocker` (blocks `S2.4@SOURCE_READY`), `accepted_carry_forward`
(accepted with its condition, closure owned by a named later lane), or
`separately_scheduled` (closed by a named PM amendment that gates `S2.4@EFFECT_DONE`,
not `SOURCE_READY`). W6/W6B/W5/S2.5A/S1.3-owned rows are deliberately **not**
adjudicated here — recording them for their owner is not closure, and W5 is not asked to
solve rows outside its ownership.

<!-- W5-PM-OWNED-ADJUDICATION-BEGIN generated-from=_W5_EXPORTED_ABI -->
- `DEPENDENCY_REFRESH_RECEIPT_BINDING_ABSENT` → classification=separately_scheduled —
  folded into **S2.4-AMEND-1** (one PM decision: where the dependency refresh enters
  APPLY, plus the terminal `s2_4_install_effect_receipt_v1` fields for the §3 refresh
  digests, plus the §9.1 profile threading below). It gates `S2.4@EFFECT_DONE`: the
  shipped S2.2A/S2.3 dependency identities are genuinely expired, so the W6 apply run
  must admit them via §9.2 refresh, and without the receipt field a §3-conformant
  terminal receipt cannot be produced. It does not gate `SOURCE_READY`: the §9.2 gate
  source is complete and the missing field is an exported-schema amendment §10.4
  reserves to PM.
- `SOURCE_IDENTITY_FRESHNESS_HAS_NO_PRODUCTION_CALL_SITE` → classification=separately_scheduled —
  the same S2.4-AMEND-1 decision (the refresh ingress point *is* the missing production
  call site). Wiring it today would permanently break the W3 wave exit (the shipped
  receipts are expired and nothing can carry a refresh yet), so a half-wiring is
  refused; the two-way latch already fails the wave exit if the row is deleted while
  the zero-call-site fact holds or kept once a call site appears. Gates
  `S2.4@EFFECT_DONE` via S2.4-AMEND-1.
- `EMITTED_EVIDENCE_DIGESTS_ARE_UNAUTHENTICATED` → classification=accepted_carry_forward —
  structurally unclosable in the source lane (a packet-local artifact cannot
  authenticate its own execution; CLAUDE.md's three evidence tiers). Acceptance
  condition, already load-bearing in the gate text and `replay_note`: any
  `SOURCE_READY` declared from the chain licenses source structure at a head only, and
  every test number must cite a measured suite run. Closure belongs to the W6
  trusted-host lane where `ORCHESTRATOR_BOUND`/`PLATFORM_OR_EXTERNAL_ATTESTED`
  evidence exists.
- `EXPECTED_TOPOLOGY_IS_CALLER_SUPPLIED_AND_UNSIGNED` → classification=separately_scheduled —
  **S2.4-AMEND-2**: make `expected_topology` plan-derived (thread the signed
  `s2_4_pg_hba_delta_v1` and the plan core's `topology_pre_digest`/`hba_delta_digest`
  into the row, as the W5 prescription already names) instead of caller-supplied. It
  gates the `PG_ROLE_ACL_MIGRATION` step of `S2.4@EFFECT_DONE` — a different-cluster
  substitution via omitted baseline keys is a substitution of the subject and must not
  reach an effect run. It does not gate `SOURCE_READY`: omission of the whole object is
  already fail-closed (`PG_TOPOLOGY_UNPROVEN`) and the only weakening path is key
  omission by the caller, which in the source lane is a governed repo fixture.
- `EVIDENCE_CLASS_TO_SCHEMA_MAP_IS_MANY_TO_ONE` → classification=separately_scheduled —
  folded into **S2.4-AMEND-1** (threading the §9.1 profile/scope check into the
  class→artifact resolution is part of deciding where the permit set enters §9.2).
  Measured severity bound: all nine never-refreshable classes yield refusals under
  §9.2, the verdict has zero production consumers today, and the substitution can at
  most buy `SOURCE_DEPENDENCY_FRESH` for a wrong-profile permit — it authorizes
  nothing. Closes with S2.4-AMEND-1 before `S2.4@EFFECT_DONE`.
- `OWNED_PATH_PROJECTION_RULER_IS_NOT_UNIFORM` → classification=accepted_carry_forward —
  bounded residual: the content-addressed `owned_scope_worktree_delta` is already
  consumed fail-closed for W0..W5, so no receipt can be derived from a tree whose owned
  scope drifts from its bound commit; the remaining divergence is that W3/W4's own
  projections hash working-tree bytes. Unifying them edits W3/W4-owned paths — a PM
  path-scope call scheduled for the next wave that legitimately touches those files
  (W6 intake), not a W5 obligation and not a `SOURCE_READY` blocker.
<!-- W5-PM-OWNED-ADJUDICATION-END -->

**Closure update (2026-07-28, S2E.0):** the two `separately_scheduled` amendments have
landed as source at head `cc6a8b97a` — `S2.4-AMEND-1` closed rows 16/23/26 and
`S2.4-AMEND-2` closed row 25 (typed statuses `CLOSED_BY_S2_4_AMEND_1_SOURCE` /
`CLOSED_BY_S2_4_AMEND_2_SOURCE` in the live ABI; rows kept with owner `PM` and
appended closure notes, mirroring the O-1 precedent). The classifications recorded in
the generated block above are preserved as the adjudication history; the two
`accepted_carry_forward` rows (24/28) remain open with their named owners. The closure
licenses source structure only; `S2.4@EFFECT_DONE` still requires the S2E.1-S2E.5
chain and fresh operator authorization.

Net result: **0 source-closure blockers**. `S2.4-AMEND-1` (refresh ingress + terminal
receipt refresh-digest binding + §9.1 profile threading; closes rows 16/23/26) and
`S2.4-AMEND-2` (plan-derived `expected_topology`; closes row 25) are PM amendments that
must land before `S2.4@EFFECT_DONE` and are named preconditions of the operator action
packet; rows 24/28 are accepted carry-forwards with named owners. Gate ② of the
`S2.4@SOURCE_READY` exit conditions is therefore **ADJUDICATED**; the `SOURCE_READY`
declaration itself remains a post-merge PM projection after gates ①④⑤ are verified at
the merged head, and it licenses source structure only — nine authorities stay false and
no runtime/PG/unit/service/deploy/broker/order/live effect is granted by it.

## S0.3 Trusted-Host Finalization (completed)

Linux `trade-core` completed the production finalization on 2026-07-22 against
reviewed head `1a933fcc28e9f7341e023b5d401c479957c14c5f` and merge head
`fed223bebd278c50b0ab3330980e66441a30c9ed`:

1. All seven mandatory roles (E2/E4/CC/E3/MIT/R4/QA) supplied authenticated
   PASS fragments for the same final generation; the final reviews had no
   P0/P1/P2 finding.
2. The trusted source verifier proved reviewed-head ancestry and exact blobs;
   the external verifier observed the live GitHub ruleset. Linux governed E4
   passed `275/275` after the two-line portable fixture-mode repair in PR #108.
3. The production finalizer returned `PASS` with no errors, closure digest
   `sha256:27f7b0041a418298ef49943f6f37283b603fce38f48f67f9a825f249f2615c63`,
   and issued `program_adoption_receipt_v1` digest
   `sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b`.
4. Independent verification is durable in repo: the exact producer-signed
   finalization statement is
   `S0.3-program-adoption-finalization-attestation-v1.json` + `.sig`, and its
   signed input index is `S0.3-trusted-execution-bundle-v1.json` + `.sig`.
   `test_persisted_s03_finalization_evidence_is_independently_verifiable`
   rechecks both SSHSIGs against the public key/fingerprint/identity/namespace
   pinned in the adopted `agent_governance_aiml_trusted_host.py`, and binds the
   receipt bytes/self-digest, closure digest, source heads, review generation,
   GitHub attestation and all-false authority limits.
5. The receipt has `source_adoption_only=true` and all nine authority grants
   false. The present ledger/docs commit is a post-emission projection and a
   descendant of the receipt's merge generation; it is deliberately not a
   recursive precondition of the already-issued receipt.

## Accepted Coverage Debt (S0.3 review, non-blocking)

Deferred P2 hardening from the 7-role review (fail-closed or forward-looking;
none affect the merged gate's forge-resistance):

- E2 P2: `validate_*` raise instead of return on malformed classification / deep
  graph (fail-closed in the wired path); `agent_governance_schema.py` not bound
  in the adoption governance manifest (host under same repo/CI protection).
- E3 P2: `GITHUB_SECRET_LIKE_RE` prefix-only + attestation-only scan; shared
  manifest path pattern weaker than `_s0_3_owned_path` (both non-exploitable).
- MIT P2: `landing_scope_v1` should bind `intent_position_effect_class` before
  the ML5 serving schema (not in the S0.3 bundle; owner S4/S5).
- QA P2: keep the receipt review-binding cross-check (now implemented via the
  enforced closure path in the hardening).
- R4 P3: the dependency-receipt schema's generic `uniqueItems` does not itself
  enforce unique `session_id`; the S0.3 validator/finalizer still requires the
  exact S0.1 and S0.2 dependency bindings, so this is not an adoption bypass.
