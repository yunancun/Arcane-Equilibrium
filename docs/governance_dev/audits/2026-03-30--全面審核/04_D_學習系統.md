# Batch D：學習系統與觀察記錄
# Batch D: Learning System & Observation Verification

**審查時間：** 2026-03-30
**狀態：** ✅ 完成
**結論：** E1 路徑代碼完整，但因 P0-A1/A2 導致 0 fills → observations = 0，路徑從未被實際執行過

---

## D1：E1 觀察觸發路徑

✅ **代碼確認正確**

```python
# pipeline_bridge.py:1192-1204 _emit_round_trip()
if self._observation_writer:
    try:
        self._observation_writer(
            symbol=symbol,
            strategy_name=strategy_name,
            close_pnl=close_pnl,
            hold_ms=hold_ms,
            regime=regime,
        )
    except Exception:
        logger.debug("Observation writer error (non-fatal)")

# phase2_strategy_routes.py:698 初始化時連接
PIPELINE_BRIDGE.set_observation_writer(_write_auto_observation)
```

- `_emit_round_trip()` 在每次平倉後調用 `_observation_writer`
- Writer 已在服務器啟動時綁定
- **無法驗證運行效果：** 新 session 0 fills，此路徑從未執行

---

## D2：Observation 持久化

✅ **代碼確認正確**

```python
# phase2_strategy_routes.py:658-695 _write_auto_observation()
record = {
    "observation_id": f"auto:{uuid.uuid4().hex[:12]}",
    "observation_ts_ms": ts,
    "observation_type": "trade_outcome",
    "confidence_level": "fact",
    "title": f"Trade: {strategy_name}/{symbol} → {outcome}",
    "detail": obs_text,
    "related_hypothesis_id": None,
    "tags": ["auto", "trade", strategy_name, symbol, outcome, regime],
}
ls.setdefault("records", {}).setdefault("observations", []).append(record)
```

- 觀察寫入 `openclaw_bybit_control_state.json` 的 `learning_state.records.observations`
- 非 `paper_trading_state.json`，因此 session restart 不影響觀察計數

---

## D3：Session 重啟後 observations 清零

✅ **設計如此（非 bug）**

- `learning_state` 存儲在 `openclaw_bybit_control_state.json`
- `paper_trading_state.json` 的 session restart 不影響 `learning_state`
- 當前 observations = 0 的原因：P0-A1/A2 → 0 fills → 0 round_trips → E1 從未觸發

**當前狀態（runtime 文件確認）：**
```json
"observation_summary": {
    "last_observation_ts_ms": null,
    "recent_lessons_count": 0,
    "recent_hypothesis_count": 0,
    "recent_experiment_proposal_count": 0
}
```

---

## D4：tick 路徑平倉觀察記錄（E1b 修復）

✅ **代碼確認正確**

```python
# pipeline_bridge.py:1317-1378 on_tick_result()
# 通過 fill 方向與 _open_positions 對比檢測平倉
if fill_side == "Sell" and pos["side"] == "long":
    # long 倉位被平，計算 close_pnl
    ...
    self._emit_round_trip(...)  # line 1375
```

- tick 路徑（risk_auto_close/時間止損/軟止損）現在也觸發 E1 觀察記錄
- 與 intent 路徑共用 `_emit_round_trip()` 邏輯

---

## D5：Learning API 端點數據

✅ **路由存在，數據源確認**

| 路由 | 數據源 |
|------|--------|
| `GET /learning/feed` | `learning_state.records.observations`（最近 20 條） |
| `GET /learning/overview` | `learning_state.observation_summary`（匯總計數） |
| `GET /learning/experiments` | `learning_state.records.experiments` |
| `GET /learning/net-pnl` | paper_trading_state 的 PnL 計算 |
| `GET /learning/review-queue` | `learning_state.review_queue` |

GUI tab-learning 使用 `/learning/feed → totals`（Session 12 G6 修復後）。

---

## D6：Auto-scan hypotheses/lessons

⚠️ **路由存在，實際邏輯未確認**

- 路由：`POST /learning/auto/scan-observations`、`/scan-lessons`、`/scan-hypotheses`
- 從審計數據無法確認 scan 的具體邏輯是否有實際 AI 調用或只是佔位路由
- 因系統處於 read_only 狀態且 win_rate < 20%，AI 咨詢未被調用，此路徑影響不大

---

## 結論

學習系統路徑**代碼設計完整**（E1/E1a/E1b/G6 修復均到位），但因上游 P0-A1/A2 bug 導致 0 round_trips，整個學習系統處於"代碼就緒但從未運行"的狀態。修復 P0 後，E1 路徑應自動開始積累觀察數據。
