# Batch B：交易核心路徑
# Batch B: Trading Core Path Verification

**審查時間：** 2026-03-30
**狀態：** ✅ 完成
**結論：** 核心路徑邏輯完整，但被 Batch A 的 P0 bug 攔截在入口

---

## B1：Tick Fan-Out 路徑

✅ **確認正確**
```
WS → bybit_public_ws_listener (913,066 條消息)
   → MarketDataDispatcher (throttle: 30,821 ticks 觸發)
   → on_tick(event) → [KlineManager, StrategyOrchestrator.dispatch_tick, _process_pending_intents]
```
- MarketDataDispatcher 運行中，attention_level=low，throttle 10s
- PipelineBridge 已激活（auto-start at server boot）

---

## B2：Intent 生命週期

⚠️ **未驗證（被 A1/A2 阻斷）**

Intent 路徑：
```
Strategy._emit_intent() → _pending_intents
→ orchestrator.collect_pending_intents()  ← intents_collected=0（P0-A1 阻斷）
→ PipelineBridge._process_pending_intents()
→ Guardian review → engine.submit_order()
→ Fill → _emit_round_trip() → E1
```

**無法驗證的原因：** P0-A1（regime falsy bug）導致策略從不調用 `_emit_intent()`，
因此整個 intent 以後的路徑在當前 server process 中從未執行過。

---

## B3：PaperTradingEngine 訂單狀態機（7 狀態）

✅ **代碼確認正確**

```
paper_order_created
    ↓ submit_order()
paper_order_submitted
    ↓ _transition_order()
paper_order_working
    ↓ partial fills
paper_order_partially_filled
    ↓ fully filled
paper_order_filled

    或
paper_order_canceled / paper_order_rejected
```
- ACTIVE_STATES = {working, partially_filled}（GUI 過濾器 G1 修復正確對應）
- `_transition_order()` 記錄 state_history（審計完整）

---

## B4：F1 修復（尾量 1% 合並）

✅ **代碼確認正確**

```python
# paper_trading_engine.py:459-462
# Dust check: if remaining < 1% of original qty, fill it all at once.
if remaining <= order["qty"] * 0.01:
    return remaining  # fill remainder in one shot
```

預期效果：每單 fills 從 25-30 次降至 ≤10 次。**無法從運行數據驗證（0 fills 的新 session）**

---

## B5：net_realized_pnl 計算（B2 修復）

✅ **代碼確認正確**

```python
# paper_trading_engine.py:1687
pnl["net_realized_pnl"] = realized - total_fees
```

- Session status 也返回 `net_realized_pnl` 字段
- 從舊 session：net_paper_pnl = -$63.78 = realized(-$51.81) - fees(-$11.97) ✅

---

## B6：_emit_round_trip 雙路徑

✅ **代碼確認正確**

- Intent 路徑（`_on_position_close`）：line 1315 → `_emit_round_trip()`
- Tick 路徑（`on_tick_result`）：line 1375 → `_emit_round_trip()`
- 都能觸發 E1 觀察記錄（前提是有 round trip 發生）

---

## B7：Paper + Demo 雙重執行

✅ **Demo 路徑獨立運作**

- Demo 通過 `bybit_demo_sync.py` 和 Bybit Demo API 獨立運作
- 目前 13 個持倉、totalEquity $99,968，UPL +$45.00
- Demo 路徑不依賴 PipelineBridge/PaperTradingEngine

**⚠️ 注意：** 新 paper session（$100k）與 Demo（$100k 初始）之間**無同步關係**
Demo 的倉位是舊 server 在舊 session 期間通過 Demo 橋接建立的，現在仍存在

---

## 結論

交易核心路徑邏輯本身**設計完整**。所有 Session 8-12 修復（F1/F2/B2/S1）代碼均已到位。
問題完全在 **Batch A 的兩個 P0 bug**——策略從不產生 intent，後面的路徑永遠不執行。
