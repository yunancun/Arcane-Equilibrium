# CC — Deep-Dive: Authority Chain Integrity (#2), Phase 5 "深挖"

**Auditor**: CC (Compliance Checker) — cold READ-ONLY adversarial deep-dive of audit direction #2.
**Baseline**: frozen `187704f6`. **Source delta = 0** (re-confirmed: `git diff --stat 187704f6 HEAD -- '*.rs' '*.py' '*.sql' '*.toml'` → **empty**; HEAD `3f805a61` differs only by docs/TODO commits). Audited tree == baseline.
**Run**: 2026-05-30/31.
**First-pass ref**: `docs/CCAgentWorkSpace/CC/workspace/reports/2026-05-30--CC--root_principle_compliance_audit.md`.
**Tooling caveat (honesty, principle #10)**: intermittent stdout flakiness this run (some `grep|wc` pipes returned spurious 0; `--include=*.rs` failed under zsh until quoted). I worked around it by writing greps to scratch files and reading those back. Every datum below is from output that **genuinely returned and was re-read**. The one item I could not pin to a specific consume-site line is the **per-decision Decision-Lease TTL-on-consume** (distinct from the Authorization TTL, which IS confirmed) — tagged NEEDS-MORE, not fabricated.

---

## DEEPER VERDICT: **CONFIRMED-CLEAN (high confidence)** on the structural authority model, D2 audit lineage, and Authorization-TTL — **NEEDS-MORE on 1 narrow leaf only** (per-decision Decision-Lease TTL-on-consume)

No bypass, no fake lineage, no un-gated order path found. The deep-dive (a) surfaced a **material architectural fact** — the prompt-named 5-hop chain is the `agent_spine` **SHADOW** lineage, structurally incapable of placing an order (below); (b) **FACT-confirmed D2 `ConvergeExchangeZero` emits a proper, deliberately-honest audit row** (sub-q #5 resolved); (c) **FACT-confirmed Authorization TTL is enforced boundary-inclusive + tested** (`live_authorization.rs:356`). The single residual is whether the **per-decision Decision Lease** (separate from Authorization) re-checks its own TTL at the consume site — I confirmed lease IDs are threaded + released on every branch but did not pin the lease-expiry comparison line. No sign of weakness; simply unread.

---

## ★ KEY STRUCTURAL CLARIFICATION (FACT — read in full) ★

**The `StrategySignal → StrategistDecision → GuardianVerdict → ExecutionPlan → ExecutionReport` chain named in the prompt is the `agent_spine` SHADOW/ADVISORY lineage — it is NOT the live order path.** This reframes #2 correctly.

- **FACT** `agent_spine/runtime_shadow/mod.rs:1-14` + module contract: emits the 5-hop lineage **in shadow**, persisted to the agent_spine JSONL store, "**without touching live order flow or the OMS execution path … it never places an order, never acquires a Decision Lease, and never mutates live state.** The live execution path remains the canonical **`tick_pipeline → IntentProcessor → OMS`** flow." `grep -c "never acquires…|never places…|side-effect-free"` = 3.
- **FACT** `contracts.rs:68-71`: `StrategySignal` is "**advisory lineage only** … not an order, permission, Guardian verdict, ExecutionPlan, or Decision Lease."
- **FACT** the ONLY non-test `GuardianVerdict {` / `ExecutionReport {` constructors are inside `runtime_shadow/mod.rs` (verdict :144; report :208,:470). The 5 typed structs are built **only** in the shadow loop (+ tests).
- **FACT** the live entry is a **different** type system: real order POST = `order_manager.rs:354 place_order → POST /v5/order/create` (`:422 post_checked`), driven from `event_consumer/dispatch.rs:789 om.place_order(...)`, fed by `tick_pipeline/on_tick/step_4_5_dispatch.rs` Step 4+5 + `intent_processor` (`OrderIntent`/`VerdictInfo`).

**Compliance read**: This *is* root principle #3 (AI output ≠ command; must become a Decision Lease + local checks) and #7 (learning ≠ live mutation) realized in code — the spine is an observed-only validation lane behind a hard MODULE_NOTE boundary ("must never gain an order-submit/lease-acquire/live-mutation call; any such addition is a hard-boundary violation"). **Answer to prompt sub-q #2 for the spine chain: the spine CANNOT emit an order-effect by construction.** The live-money gating that matters lives in step_4_5_dispatch + intent_processor + OMS.

---

## LIVE-PATH gating — VERIFIED (FACT, from returned greps of step_4_5_dispatch.rs, 1803 lines)

- **Decision Lease threaded through the live dispatch**: `lease_id` (10 refs); `gate.lease_id` carried into dispatch input (:804), captured `decision_lease_id` (:826) and propagated into the persisted record (:872, `spine_verdict_id` :901). Lease is **released on every terminal branch**: `release_decision_lease_for_governance(...)` at :937, :951, :1108, :1159, :1186 (6 release sites incl. helper import :39; `pipeline_helpers.rs:41/:80` define it; also released in `event_consumer/loop_handlers.rs:385`). → lease lifecycle is structurally wired around execution. **PASS**.
- **Guardian verdict gates execution, rejections are persisted (no silent drop)**: `verdict`/`.allow` (30 refs); non-approved verdicts persisted immediately (`persist_verdict` :638,:705,:1016; comment S-01 ":630 persist non-approved verdicts immediately"); pre-risk + exchange-gate + paper-gate rejects each call `record_pre_risk_rejection` / `strategy.on_rejection` + `emit_decision_feature_intent_rejected` (:536,:566,:674,:687,:959,:973,:1401,:1416). A historical bug comment (:726-735) documents that the exchange branch was fixed to persist the **intent**, not just the verdict — i.e. the team actively hardened lineage completeness here. **PASS**.
- **Close/reduce path documented as intentionally lighter**: `:1352 decision_lease_id: None` corresponds to the close branch — `:1598 "Close reduces risk (not increases it), so Guardian/cost_gate/Kelly/P1 are skipped."` This is **principle #5 (survival>profit) compliant** (reduce-only closes must not be blocked by risk gates) and consistent with first-pass + CLAUDE §四 ("cancel/close 不需 Decision Lease"). Not a bypass: it is reduce-only, risk-decreasing. **PASS (by design)**.
- **Authority surface present**: `scanner_authority_mode` / `authority_mode` logged through dispatch (:418,:431,:445,:512,:587); `LiveActive`/`execution_authority` refs = 5. Consistent with first-pass "denylist-not-auth; real gate = LiveActive + signed authorization."

---

## Lineage-completeness map

| Hop | Type / entry (FACT) | Lane | Persistence | Status |
|---|---|---|---|---|
| StrategySignal→…→ExecutionReport | contracts.rs:73/104/165/183/234; ctors only @ runtime_shadow | **SHADOW** | agent_spine JSONL store (`store.rs`) + `agent.decision_state_changes` (SpineStateTransition ×5) + `shadow_filled` ExecutionReport on real fill (runtime_shadow:9-13) | end-to-end persisted, advisory-only (FACT) |
| **LIVE order** | `step_4_5_dispatch` Step4+5 → `intent_processor` → `event_consumer/dispatch.rs:789 om.place_order` → `order_manager.rs:354 POST /v5/order/create` | **LIVE** | verdicts persisted (persist_verdict ×3 sites incl. rejects); intents persisted; fills → trading.fills V094 (carry-fwd) | Guardian(30)+lease(10 refs/6 releases)+authority — **PASS** |

The shadow chain has **complete** persistence (its purpose). The live chain persists verdicts (incl. rejections) + intents + fills; lease is threaded and released on all branches.

---

## DEEPER sub-questions — status

1. **Every hop persisted/auditable?** — SHADOW: **YES** (JSONL store + decision_state_changes). LIVE: **YES for verdict/intent/fill** (persist_verdict incl. non-approved, persist_intent, trading.fills V094). FACT.
2. **Any order-effect WITHOUT GuardianVerdict + valid Lease?** — Spine: **NO by construction** (no submit/lease call; hard MODULE_NOTE). Live: the only non-test order POST is `order_manager.rs:354 place_order` → `POST /v5/order/create` (:422), reached via `dispatch.rs:789` under the Guardian-dense (30 refs), lease-threaded (10 refs / 6 releases) step_4_5 path; reduce-only **close** is intentionally lease-exempt (risk-decreasing, principle #5, `step_4_5_dispatch.rs:1598`). **No un-gated open-order path found.** The live submit is **centralized** in `order_manager.place_order` — the grep for order-submit verbs surfaced only this one production POST (plus the IPC `cancel_all_orders` close path). A hidden ML/Dream/Scout/Earn/Canary bypass would require a *second* POST /v5/order/create site, and none surfaced (positive single-entry signal, principle #1). Confidence high; not exhaustively per-agent enumerated.
3. **Python "success" without Rust ExecutionReport (fake lineage)?** — first-pass found none on live cancel path (classifyLiveMutation, rust_synced/partial_failure); the spine-vs-live split *reduces* this risk (Python cannot mint spine reports; live status comes from OMS). No new fake-success found. **PASS (carry-forward).**
4. **Lease/Authorization TTL/expiry enforced on consume?** — **Two distinct TTLs; one CONFIRMED, one NEEDS-MORE.**
   - **(a) Authorization TTL — CONFIRMED enforced + tested (FACT).** `live_authorization.rs:348-363`: the one-pass verifier (HMAC-SHA256 + expiry + env binding + system mode, "pure verification, no side effects") rejects with `LiveAuthStatus::Expired` when `auth.expires_at_ms <= now_ms` (:356, **boundary-inclusive**, comment `FIX-LIVE-AUTH-VERIFY-1`). Tests assert it: `expiry_at_now_is_rejected_not_accepted` (:558, "expires_at_ms == now_ms must reject (strict <=)") and `tampered_expiry_detected` (:588). This is gate-5 of the 5-gate live boundary → an expired Authorization **cannot** execute. **PASS.**
   - **(b) Per-decision Decision-Lease TTL — NEEDS-MORE.** `ExecutionPlan.lease_ttl_ms/lease_id/lease_scope` are **plumbed** (contracts.rs:211-213) and `lease_id` is threaded through dispatch + released on all 6 terminal branches (FACT). But the lease-acquire/TTL comparison site is **NOT in `governance/`** (that grep returned empty); decision_lease symbols live in `event_consumer/{types,dispatch,loop_handlers}.rs`. I did not pin a `lease.expires_at <= now` check at the lease consume site. So "an *expired Decision Lease* (as opposed to expired Authorization) cannot execute" is **not yet proven** — though Authorization-TTL already provides a hard outer time-bound on any live execution. *Single highest-value open leaf.*
5. **D2 `ConvergeExchangeZero` audit-lineage?** — **CONFIRMED: emits a proper, deliberately-honest audit row — NOT an un-lineaged position mutation (FACT, source read at orphan_handler.rs:482-528).** Path: reconciler `dispatch_ghost_converge` (`orphan_handler.rs:451`) sends IPC `PipelineCommand::ConvergeExchangeZero` → `handle_converge_exchange_zero` (`handlers/lifecycle.rs:321`) → `pipeline.converge_exchange_zero_close(symbol,is_long,ts_ms)` (`lifecycle.rs:328` / `loop_handlers.rs:504`). The reconciler emits a fire-and-forget audit `spawn_ghost_converge_audit` (`orphan_handler.rs:488`) that does a **real `INSERT INTO observability.engine_events (ts_ms,event_type,source,config_name,…,payload)`** (:509-519) with `event_type="reconcile_ghost_converge"`, `source="position_reconciler"`, and payload `{symbol, side, baseline_qty, engine, removed_position}` (:499-505). **Principle #10/#8 done *right* (the load-bearing nuance):** the doc (:484-487) states it **deliberately writes to `observability.engine_events`, NOT `trading.order_state_changes`**, "**because D2 is a pure position convergence with no real close order_id**" — i.e. it refuses to fabricate an order-lineage row for a non-order, while still leaving a full audit trail. That is the *opposite* of fake lineage. **Anti-reentry + double-guard**: deliberately does NOT route through `dispatch_orphan_close`/CloseSymbol (avoids re-hitting 110017 reentry loop, RCA §3a, :444-447); only fires after Bybit confirmed `size==0` (:446-447); `converge_exchange_zero_close` has an `is_exchange()` "二重保護回 noop" guard (`mod.rs:801`) + `MANDATORY re-review` annotation (`mod.rs:804`). Semantics: `positions_remove + mirror sync, no PnL, no new order` (`lifecycle.rs:308-328`). **Auditable per principle #8 → PASS.** (Confirms + sharpens SHARED FACT "D2 live + guard-correct + E4-tested" with the lineage-row evidence the prompt explicitly requested.)

---

## D2 ConvergeExchangeZero — verdict (sub-q #5)
**CONFIRMED-CLEAN (FACT, source-read).** A lineage-tracked, double-guarded, anti-reentry local-drift convergence — not an un-lineaged mutation, and explicitly *not* a faked order row: `spawn_ghost_converge_audit` does a real INSERT into `observability.engine_events` (`event_type="reconcile_ghost_converge"`, full payload) and **deliberately avoids** `trading.order_state_changes` because there is no real close order_id (orphan_handler.rs:484-519). `is_exchange()` double-guard + MANDATORY-re-review (`mod.rs:801/804`); only after Bybit `size==0`; `positions_remove + mirror sync, no PnL, no new order`. Auditable per principle #8; honest per #10.

## NEW finding

**NONE asserted.** Strongest result is positive and now multiply-confirmed: (1) shadow/live separation makes the prompt-named chain incapable of an order-effect; (2) centralized `order_manager.place_order` single live-submit entry (principle #1); (3) lease-threaded + Guardian-dense live dispatch with **rejections persisted** (no silent drop); (4) D2 convergence emits an **honest** audit row that deliberately refuses to fake an order-lineage entry (principle #10 *positively* demonstrated); (5) Authorization-TTL enforced boundary-inclusive + tested. No bypass / no fake lineage / no un-gated order path. The sole open leaf — per-decision Decision-Lease TTL-on-consume (#4b) — is **unverified, not failed**, and is backstopped by Authorization-TTL; I will not assert a finding without the read. Carry-forward governance P1-CC-A (ADR-0046 phantom funding_arb revive-gate) is unrelated to authority-chain code and remains open from first-pass.

---

## Honesty / fail-loud note & turnkey close
I resolved #1 (persistence), #2 (live submit centralized in `order_manager.place_order` POST /v5/order/create), #3 (no Python fake-success), #5 (D2 honest audit), and #4(a) Authorization-TTL — all from greps/source that **returned and were re-read**. The **only** residual is #4(b): the **per-decision Decision-Lease** TTL-on-consume comparison line (the lease lives in `event_consumer/{types,dispatch,loop_handlers}.rs`, NOT `governance/`); I confirmed lease IDs threaded + released on all branches but did not pin the lease-expiry compare. I downgraded only that one sub-item rather than over-claim. Note: Authorization-TTL (#4a) already imposes a hard outer time-bound on ALL live execution, so even if the per-decision lease lacked its own TTL re-check, no execution can outlive the signed Authorization window. No `ssh trade-core` mutation; READ-ONLY honored; no secrets echoed. **Scratch files removed.**

**To close #4(b) (healthy session, ~3 calls)**: `grep -rIn 'lease' rust/openclaw_engine/src/event_consumer/types.rs rust/openclaw_engine/src/event_consumer/dispatch.rs` → find the DecisionLease struct + its TTL field, then read the dispatch consume site (around `dispatch.rs:789 om.place_order`) and confirm an expired-lease check (or document that lease validity is bounded solely by Authorization-TTL + the per-tick acquire/release lifecycle). Expected outcome: CONFIRMED-CLEAN-with-evidence.
