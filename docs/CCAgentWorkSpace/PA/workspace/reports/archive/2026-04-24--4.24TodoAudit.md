# PA 審計報告：TODO.md 全面架構檢視 — 2026-04-24

**日期**：2026-04-24 02:48 CEST  
**前置文件**：memory.md (§架構狀態快照) + profile.md + CLAUDE.md (§三狀態摘要 + §四硬邊界 + §九代碼結構)  
**基準狀態**：engine PID 884467 · binary mtime 02:06 · engine lib 1980 / 0 failed

---

## 一、架構完整性總覽

### 1.1 關鍵模塊現狀表

| 模塊 | 現狀 | 技術債 | 依賴關係 |
|------|------|--------|---------|
| **Rust ConfigStore (RiskConfig + ExitConfig)** | ✅ ArcSwap 無鎖讀 + Mutex 序列化寫 + TOML 回寫 | 無 | Tick 熱路徑讀 (~5ns) |
| **IPC 寫入面 (7 exit.* fields)** | ✅ EDGE-DIAG-1-FUP-IPC commit `1a53400` | FUP-SHADOW-ENABLED-IPC (P3) | Python IPC handler 全 28 字段支持 |
| **Python ExecutorAgent shadow→live** | ⚠️ `_shadow_mode=True` 硬編碼 (line 482) | **需要 toggle 機制**；目前無 GUI/IPC path flip | Path A/B 衝突避免機制 |
| **Path A (Python intent→IPC→Rust)** | ✅ 代碼完整 (lines 484-577) | 無運行時驗證 | ExecutorAgent 中 ∉ Path B |
| **Path B (legacy pipeline_bridge)** | ✅ 仍存活 (CLAUDE.md §三) | 缺 acquire_lease (P1 aff/mem) | 與 Path A 互斥機制鬆散 |
| **ML 三層 (L0/L1/L2)** | ✅ h0_gate.py (185 L) / local_llm_factory.py / layer2_*.py | local_llm_factory ABC 界面乾淨 | _LMStudioShimClient 鏡像 OllamaClient surface |
| **5-Agent 框架** | ✅ multi_agent_framework.py (1137 L) + 5 agents (4552 L 合計) | Conductor stub · 消息路由邏輯輕量 | Scout/Strategist/Guardian/Analyst/Executor |
| **Combine Layer 骨架** | ✅ INFRA-PREBUILD-1 Part A 落地 (commit `74b678a`) | Phase 1a dormant · `shadow_enabled=False` | ML inference shadow (Phase 2 待啟) |
| **Model Registry 骨架** | ✅ INFRA-PREBUILD-1 Part B 落地 (commit `01085a6`) | Phase 4 auto-promote cron 待建 | canary_status state machine (V023 guard) |
| **V023/V021 Schema Guard** | ✅ RAISE EXCEPTION 已加 (line 86-92 & 70-75) | N/A | CLAUDE.md §七新規則: 2026-04-24 強制 |
| **主 Python 層拆分** | ✅ main_legacy 468 L + 5 sibling routes 1558 L 合 2026 L | Tier B 實質閉環 | 28 下游檔 `_base.xxx()` 動態尋址 |

### 1.2 架構依賴健康度評分

| 維度 | 評分 | 註記 |
|------|------|------|
| **確定性路徑完整** (Rust engine + governance) | 8.5/10 | SM-01/04/02/EX-04 全接線 · H0 Gate blocking mode ✅ |
| **AI 治理層接線** (H1-H5 + 5-Agent) | 7.5/10 | H1-H5 fully wired · ExecutorAgent shadow→live flip 缺 GUI |
| **IPC 跨語言邊界** | 8.0/10 | 28 字段 (legacy 21 + exit 7) · EDGE-DIAG-1-FUP-IPC 完 · FUP-SHADOW 延後 |
| **交易鏈路一致性** | 6.5/10 | **Path A/B 無強互斥** · ExecutorAgent shadow 預設 ON (設計安全) · lease workflow 標示 P1 debt |
| **技術債堆積** | 6.0/10 | P1-6/7/10/11/19 · P2/P3 backlog · 無架構級阻塞 |
| **整體 PA 評估** | **7.2/10** | 與 2026-03-31 同級 · 新增 2 個 migration guard · Combine/Registry 骨架減風險 |

---

## 二、Path A vs Path B 設計現狀

### 2.1 路徑定義與實現

**路徑 A（推薦，Principle 3 完整實施）**
```
StrategistAgent → MessageBus.send(APPROVED_INTENT)
  ↓
ExecutorAgent.subscribe(APPROVED_INTENT)
  ↓
ExecutorAgent._execute_via_ipc(intent_id, symbol, side, qty, ...)
  [shadow_mode=True: log only]
  [shadow_mode=False: → IPC SubmitOrder → Rust paper_state]
  ↓
ExecutionReport (success / error / shadow)
```

**路徑 B（遺留，Principle 3 未完整）**
```
pipeline_bridge.on_tick()
  → _process_pending_intents()
  → Guardian.verdicts[intent] = APPROVED
  → ⚠️ 缺 acquire_lease() ← P1 debt TD-1
  → submit_order() 直呼 Rust IPC 或 paper_trading_engine
```

### 2.2 架構缺口與衝突風險

| 缺口 | 位置 | 風險等級 | 狀態 |
|------|------|---------|------|
| **ExecutorAgent.shadow_mode toggle 無 GUI path** | executor_agent.py:482 | 中 | ⚠️ 手改代碼或添加 API endpoint |
| **Path A/B 無 mutual exclusion** | 設計層面 | 中 | ✅ ExecutorAgent shadow=true default 降低事件發生機率 |
| **Path B 缺 acquire_lease** | pipeline_bridge.py:701 (ref: memory.md) | 高 (Principle 3) | 🟡 P1 記錄；live gate 先驗法則降級（demo_only PaperTrading) |
| **IPC SubmitOrder 結果解析** | executor_agent.py:558-577 | 中 | ✅ 實裝完整；fail-closed |
| **Rust IPC 無對稱 ack 機制** | paper_trading_routes.py 端 | 低 | 異步 fire-and-forget 接受度高 |

### 2.3 真正的架構 Gap：ExecutorAgent shadow→live 切換

**當前機制**：
- 硬編碼 `_shadow_mode: bool = True` (line 482)
- 無 IPC / GUI 動態切換路徑
- 只能手改程式碼或重啟

**需要的設計**：
1. 新增 IPC endpoint `patch_executor_config` (JSON `{ shadow_mode: false }`)
2. ExecutorAgent 內加 ConfigStore-like `_executor_config_store` (lock-free 讀)
3. GUI 在 "Live Control" 頁加 toggle （Operator 權限門控）
4. 切換時自動記 audit log + 事件通知 Conductor

**優先級建議**：P1 前期（不阻 live 但過渡期必做）

---

## 三、Leverage Points TOP 3

### Leverage Point #1：Rust ConfigStore + IPC hot-reload 完整化 ⭐⭐⭐

**現狀**：
- ConfigStore<RiskConfig> + ConfigStore<ExitConfig> 基礎設施完整 (ArcSwap + Mutex + persist)
- IPC handler 已支持 28 字段 (legacy 21 + exit 7) — EDGE-DIAG-1-FUP-IPC ✅
- 但 `ExitConfig.shadow_enabled` **仍需補 IPC 路徑** (FUP-SHADOW-ENABLED-IPC, P3)

**槓桿效應**：
- 補 1 字段 (~5 LoC) → Phase 2 Combine Layer shadow flip 無需 rebuild (~3 min)
- 降低「試驗→回滾」週期從 3 min → <60s
- **解鎖**：EDGE-DIAG-1 Phase 3 auto-gate 可信度提升 + 實驗敏捷性

**工作量**：1d (FUP-SHADOW-ENABLED-IPC + 1 test)

### Leverage Point #2：ExecutorAgent shadow→live toggle 基礎設施 ⭐⭐⭐

**現狀**：
- Path A 代碼結構完整，只是 shadow=true 硬編碼
- IPC handler 結構已驗證可行 (risk config handler 示範)
- Rust 端無需新增邏輯（純 Python Executor 狀態）

**槓桿效應**：
- 新增 ConfigStore<ExecutorConfig> (trivial clone from RiskConfig)
- Executor.subscribe(PipelineCommand::UpdateExecutorConfig) + apply patch
- GUI toggle → IPC → Python ConfigStore → ExecutorAgent reads lock-free
- **解鎖**：Path A 轉 live 時無需停服、支持 A/B 比較測試、Principle 3 完整度從 65% → 95%

**工作量**：3-4d (E1 + E2 + E4 含完整測試)

### Leverage Point #3：dual-layer exit decision shadow 監控自動化 ⭐⭐⭐

**現狀**：
- INFRA-PREBUILD-1 Part A 骨架完成 (`shadow_exit_writer.rs` + `combine_layer.rs`)
- `learning.decision_shadow_exits` 表空（Phase 1a dormant）
- Manual counterfactual audit 通過 (`helper_scripts/db/counterfactual_exit_replay.py`)

**槓桿效應**：
- Phase 2 啟動後 (`shadow_enabled=true` flip via Leverage Point #1)，自動填 shadow 表
- Operator 每日執行 `passive_wait_healthcheck.py` [8] + cron `counterfactual_daily_cron.sh`
- **解鎖**：(1) Track P vs Track L 一致性可量化(目標 ≥60%) (2) EDGE-DIAG-1 Phase 3 auto-gate 前置條件 (3) Phase 2→3 晉升信度

**工作量**：2d (健康檢查補充 + cron driver 稍許擴展，多數已有)

---

## 四、架構債 vs 參數債分類

### 4.1 架構債（需設計變更）

| 債項 | 根本原因 | 解決路徑 | 優先級 |
|------|---------|---------|--------|
| **Path A/B 互斥機制鬆散** | 設計時假定 Path A 為未來、Path B 短期並存 | Leverage #1 + explicit routing rule | P1 |
| **ExecutorAgent shadow toggle 無 GUI** | 原型階段；shadow=true 為 demo safe default | Leverage #2 (4d) | P1 |
| **Combine Layer Phase 2 shadow 啟動成本** | TOML 改動需 rebuild + IPC 欄位缺失 | Leverage #1 (1d FUP-SHADOW) | P2 |
| **P1-7 learner 下游消費缺失** | experiment_ledger_snapshot 結構異常、21 schema 表無 consumer | 結構審計 + API 層重建 | P2 (架構級) |
| **Model Registry canary auto-promote** | Phase 4 設計完成但無執行驅動 | cron job + state machine validator | P3 |

### 4.2 參數債（調閾值可解）

| 債項 | 現狀 | 調整範圍 | 預期效果 |
|------|------|---------|---------|
| **ExitConfig.missing_edge_fallback_bps** | -10.0 (守舊) → 10.0 (Option A live) | 0～20 bps 逐步調 | Gate 1 通過率 0% → 3-5% |
| **ExitConfig.exit_giveback_{base,slope,floor}** | v2 default (hardcoded) | 資料驅動校準 (Phase 1b counterfactual) | phys_lock fire 率 0% → 5-15% (預期) |
| **H0 Gate hardness** | blocking mode ✅ | warn-only ↔ blocking toggle | 已最優 |
| **cost_gate threshold** | 寫死常數 | hot-reload IPC (已有) | 動態自適應 (Phase 5) |

---

## 五、跨模塊依賴圖（文字樹）

```
【Rust Engine 層】
├─ ConfigStore<RiskConfig>
│  ├─ on_tick 無鎖讀 (~5ns)
│  ├─ IPC handler UpdateRiskConfig 寫入
│  │  ├─ event_consumer/handlers/risk.rs (apply_patch 驗證)
│  │  └─ TOML persist on success (non-Startup/Migration)
│  └─ 28 字段 (legacy 21 + EDGE-DIAG-1-FUP-IPC 7)
├─ ConfigStore<ExitConfig>
│  ├─ tick_pipeline/helpers.rs build_ml_inference_shadow 讀
│  ├─ Priority 6 physical_micro_profit_lock_v2 lookup
│  └─ 9 字段 (missing_edge_fallback + 3×giveback + 5×shadow)
├─ Combine Layer (骨架 ✅，Phase 1a dormant)
│  ├─ shadow_exit_writer (8-tuple via events::emit_shadow_exit)
│  ├─ TradingMsg::Fill.exit_source (4 enum: Physical/Hybrid/ML/Disabled)
│  └─ learning.decision_shadow_exits INSERT (hypertable + 4 indexes)
└─ Model Registry (骨架 ✅，Phase 4 待 auto-promote)
   ├─ learning.model_registry (V023 guard ✅)
   └─ /api/v1/ml/model_promote (Operator-gate)

【Python 層 × Rust IPC 邊界】
├─ ExecutorAgent (shadow_mode=true default ⚠️)
│  ├─ _execute_via_ipc(...)
│  │  ├─ Shadow: log only → ExecutionReport(success=true, error="shadow_mode")
│  │  └─ Live (future): IPC SubmitOrder → Rust paper_state → order tracking
│  ├─ MessageBus.send(EXECUTION_REPORT) ← no current subscribe
│  └─ _ipc_command("submit_order", params) ← paper_trading_routes._ipc_command
├─ StrategistAgent
│  ├─ shadow=False (live intent emission ✅)
│  ├─ MessageBus.send(APPROVED_INTENT)
│  └─ Guardian verdicts already appended
└─ GuardianAgent
   ├─ subscribe(INTENT)
   ├─ evaluate_risk (RiskConfig + multi-rule)
   └─ emit verdicts → MessageBus

【Learning 層】
├─ decision_features (entry-time supervised data)
│  ├─ 1.65M rows (as of 2026-04-24)
│  └─ 17 features (no symbol_embedding)
├─ learning_labels (exit-time outcomes)
│  ├─ backfill_labels.py edge_label_backfill (FILL-CONTEXT-LINKAGE-1 ✅)
│  ├─ context_id tracking (signal ts → order dispatch → confirmed fill)
│  └─ 130 labels demo / 8 labels live_demo (2026-04-19 start)
├─ exit_features (Phase 1b accumulation)
│  ├─ 7 dimensions (ATR norm, hold seconds, net bps, etc.)
│  └─ INSERT on every exit (exit_feature_writer.rs + channel @ tasks.rs:446)
└─ model_registry (canary + artifact metadata)
   ├─ V023 migration guard ✅
   └─ rows=0 until run_training_pipeline.py writes (P1-7 C pending)

【AI 治理 H1-H5】
├─ H0 Gate (blocking mode ✅)
│  └─ oracle freshness + health + eligibility + risk envelope
├─ H1 thought_gate.py (185 L advisory-only mock)
│  └─ scout_worker edge case filter
├─ H2 layer2_cost_tracker.check_daily_budget
├─ H3 strategist_agent._ai_evaluate + layer2_engine._l1_triage
├─ H4 Ollama timeout + max_retries=0 (fail-closed)
└─ H5 cost_logging (record claude_cost)

【模型層】
├─ L0 確定性層 (h0_gate.py 邏輯)
├─ L1 本地 Ollama / LM Studio (local_llm_factory ABC ✅)
│  └─ LMStudioShimClient 鏡像 OllamaClient.surface
└─ L2 Claude API (layer2_*.py 1000+ lines)
   ├─ cost_gate bind (JS config feedback)
   └─ autonomous loop (新聞搜索 / 推理鏈 / 工具 — 骨架存檔)
```

---

## 六、PA 建議：架構視角的 TODO 重組方案

### 6.1 當週優先 (W17/18 - 4/24～4/30)

**Tier A（blocking Live Gate）**
- ✅ EDGE-DIAG-1 Phase 1+2+4 已完（counterfactual auditing ✅）
- 🔴 EDGE-DIAG-1 Phase 3 等資料（clean window ≥200，ETA ~5/1）
- 🟡 **FUP-SHADOW-ENABLED-IPC** (1d) — 補 `exit_shadow_enabled` IPC 欄位 → Phase 2 phase-in 敏捷

**Tier B（Path A→Live 過渡，非 blocking）**
- 🟡 **ExecutorAgent.shadow→live toggle 基礎** (3-4d) — 新增 ConfigStore<ExecutorConfig> + GUI endpoint

**Tier C（觀測/驗收）**
- 🟡 P1-10 STRATEGY-ASYMMETRY-1 PostOnly 1w 觀察（下 4/28 判決）
- 🟡 P1-7 C ONNX 訓練延後至 P1-10 結果（等入場量回升）

### 6.2 未來重構建議

**架構層需求**（不在本週 TODO，但 PA 建議超前規劃）

1. **Combine Layer Phase 2 自動化**
   - 2026-05-01 後（clean rows ≥200）自動啟動？或 operator 手動 flip？
   - 建議：operator 手動 flip + cron 持續監控 (healthcheck [8])

2. **5-Agent 訊息層升級**
   - 當前 MessageBus broadcast + subscribe；Conductor 仍 stub
   - Phase 5 建議：Conductor 狀態機 (route scout → strategist → guardian → executor)

3. **Learning Pipeline 下游重建**
   - experiment_ledger_snapshot 結構異常、21 schema 無 consumer
   - 建議 Phase 4 同步建：(1) ledger view generator (2) API 層 + 對應的 frontend cockpit

4. **模型層 Phase 4 auto-promote**
   - Model Registry 骨架備好，cron driver + state machine validator 待實施
   - 建議：先手動測試晉升流程（/api/v1/ml/model_promote），再自動化

---

## 七、技術建議：架構問題疏通

### 7.1 短期（<2 週）

| 項目 | 建議 | 理由 |
|------|------|------|
| **EDGE-DIAG-1-FUP-SHADOW** | E1 週內落地 | 開啟 Phase 2 shadow 無需 rebuild |
| **ExecutorAgent toggle 設計** | PA 先出設計文檔 (RFC) | 決定誰持 shadow_mode state (Executor / ConfigStore / GUI) |
| **Combine Layer Phase 2 啟動決策** | operator 評估 counterfactual 結果 | 若 clean 信號 <60%，考慮 skip Phase 2 或重設目標 |

### 7.2 中期（2-6 週）

| 項目 | 建議 | 理由 |
|------|------|------|
| **Path A/B 互斥加強** | 在 H0 Gate 層加 routing hint | demo/paper/live 不同路徑選擇 |
| **五層 AI 完整度審計** | sub-agent explore code + layer2_* coverage | H1-H5 真實訪問率、L0/L1/L2 自動切換邏輯 |
| **Learning pipeline consumer** | 新建 experiment_routes + UI cockpit | P1-7 下游 21 schema 表利用 |

### 7.3 長期（6+ 週）

| 項目 | 建議 | 理由 |
|------|------|------|
| **Model Registry canary 自動化** | Phase 4 第二階段 | 每週 retraining + auto-promote decision |
| **Conductor 狀態機實裝** | 5-Agent 編排升級 | 依賴管理、故障傳播、優先級控制 |

---

## 八、CLAUDE.md 一致性檢查 ✓

### 查證項目表

| 項目 | CLAUDE.md 敘述 | 代碼現狀 | 一致性 | 備註 |
|------|---|---|---|---|
| Rust ConfigStore hot-reload | §三「4 IPC 寫入面 → tick-level hot-reload」| ✅ ArcSwap + Mutex + TOML persist | ✓ | EDGE-DIAG-1-FUP-IPC ✅；FUP-SHADOW-ENABLED-IPC 待補 |
| ExecutorAgent shadow→live | §五「Path A / Path B」 | `_shadow_mode=true` 硬編碼 | ⚠️ 過期 | 說法「shadow→live 切換」需補 GUI path，當前無 |
| Model Registry 部署 | §三「Phase 4 auto-promote cron 延後」 | V023 guard ✅；cron 待建 | ✓ | 骨架完成，實施延後 |
| V023/V021 Schema Guard | §七「新規則 2026-04-24」 | ✅ RAISE EXCEPTION 已加 | ✓ | 符合強制規則 |
| ML 三層乾淨 | §七「LocalLLMClient 抽象乾淨」 | ✅ local_llm_factory ABC + LMStudioShimClient | ✓ | LLM-ABC-MIGRATION-1 ✅ |
| Python layer2 行數 | 「layer2_tools.py 906 行超過 800 警告線」| ✅ 906 L 符合敘述 | ✓ | 監控中，未超 1200 硬限 |
| Rust engine lib & tests | §三「engine lib 1980 / 0 failed」 | ✅ 實測數字一致 | ✓ | 2026-04-24 rebuild |
| tick_pipeline/mod.rs 行數 | §九「1012 行進 1200 硬上限」 | ✅ 1012 L | ✓ | TICK-PIPELINE-MOD-SPLIT-1 ✅ |
| Python main_legacy 瘦身 | §三「468 行（原 ~5265 行瘦身 91.1%）」 | ✅ 468 L 一致 | ✓ | DEDUP-PY-RUST Tier B 確認 |
| 5-Agent 訊息通信 | §五「正式對象通信」 | ✅ MessageBus + 5 agents subscribe | ✓ | Conductor stub；訊息路由可用 |

**結論**：CLAUDE.md 敘述與代碼一致性 95%（2 個預期 gap：FUP-SHADOW-ENABLED-IPC + ExecutorAgent toggle GUI）

---

## 九、跨模塊風險熱點

### 風險矩陣

| 熱點 | 觸發條件 | 影響範圍 | 降低策略 |
|------|---------|---------|---------|
| **IPC 反序列化失敗** | Python 字段名/型別不同步 | Executor intent lost | E2 check: ipc_params vs IpcRiskUpdate struct (weekly) |
| **Combine Layer shadow_enabled flip** | operator 手動切（無 IPC path） | 需要 rebuild (~3 min downtime) | Leverage #1 (FUP-SHADOW) 解決 |
| **ExecutorAgent intent duplicate** | shadow=true AND PATH_B 同時發 intent | 倉位重複開，爆 risk gate | ExecutorAgent shadow default 降機率；設計上監視 intent_id |
| **Model Registry 表結構不全** | V021/V023 migration 靜默 no-op (歷史遺留) | Rust 讀 canary_status NULL，功能退化 | Guard A/B ✅ (RAISE EXCEPTION)；operator 執行 audit_migrations.py 定期檢查 |
| **Learning label backfill stall** | strategy net edge 收縮 (P1-10 symptom) | ONNX 訓練延遲 | 已知 H3 認定；P1-19 結案 (duplicate of P1-10) |

---

## 十、PA 最終判決

### 架構層總體評估

**當前架構是否適配 Live Gate？**

✅ **是**，條件：
1. Path A 暫時 shadow=true（預設安全）
2. Path B lease workflow 暫時不檢查（demo_only 降級接受）
3. Combine Layer Phase 1a dormant（不啟 ML override）
4. Model Registry Phase 4 延後（無新模型產生）

**需要立即修複的缺口？**

❌ **無致命架構缺陷**。P0/P1 均為「完成度提升」非「功能缺失」。

**高優先級架構改進？**

1. **Leverage #1 FUP-SHADOW-ENABLED-IPC** (1d) — 降低 Phase 2 成本
2. **Leverage #2 ExecutorAgent toggle** (3-4d) — Path A→Live 過渡安心
3. **Combine Layer Phase 2 啟動決策** (需 operator 評估) — 基於 counterfactual 結果

### 對下一輪 todo 分派的建議

**當週 (W17/18)：**
- 👤 E1a：FUP-SHADOW-ENABLED-IPC (1d)
- 👤 E1b：ExecutorAgent ConfigStore 設計 RFC (0.5d)
- 👤 PA/FM：EDGE-DIAG-1 Phase 3 auto-gate 條件評估 (1d)

**下週 (W19)：**
- 👤 E1/E2/E4：ExecutorAgent ConfigStore + GUI 實施 (3-4d 平行)
- 👤 FA：Path A/B 互斥加強提案 (1d)

---

## 附錄：檔案位置速查

| 文件 | 路徑 | 行數 | 備註 |
|------|------|------|------|
| Rust ConfigStore | `rust/openclaw_engine/src/config/store.rs` | 200+ | ArcSwap 實裝 + TOML persist |
| IPC risk handler (28 字段) | `rust/openclaw_engine/src/ipc_server/handlers/risk.rs` | 150+ | EDGE-DIAG-1-FUP-IPC ✅ |
| ExecutorAgent | `program_code/.../executor_agent.py` | 630 | shadow_mode=true line 482 |
| local_llm_factory | `program_code/.../local_llm_factory.py` | 300+ | LocalLLMClient ABC ✅ |
| Combine Layer | `rust/openclaw_engine/src/combine_layer.rs` | ~200 | Phase 1a dormant |
| V023 guard | `sql/migrations/V023__model_registry.sql` | line 86-92 | Guard A RAISE ✅ |
| V021 guard | `sql/migrations/V021__fills_exit_source.sql` | line 70-75 | Guard B RAISE ✅ |
| Main legacy | `program_code/.../main_legacy.py` | 468 | 5 sibling routes 1558 L |

---

**報告編寫**：PA (OpenClaw Project Architect)  
**文件版本**：v1.0 — 2026-04-24  
**簽核路由**：PM (確認) → 下一輪 10-agent 審議 → 最終建議合成
