# W7-3 Option B Deploy SOP — Emergency 1-tick Defense

**Date**: 2026-05-10
**Owner**: PM
**Scope**: ma_crossover.on_rejection duplicate_position sync (W7-3 Option B 補丁，~20 LOC)
**Trigger**: E4 regression PASS 後

---

## §1 Pre-deploy 檢查（PM 確認）

- [ ] E1 unit test 4 個全 PASS
- [ ] E2 review approved
- [ ] E4 cargo test --lib --release -p openclaw_engine 全 PASS
- [ ] Mac 端 `cargo build --release --bin openclaw-engine` 編譯 PASS
- [ ] commit pushed origin + Linux 同步
- [ ] 當前 hot loop 仍 active（spot check 確認 reject burst pattern）

## §2 Deploy 步驟（Linux trade-core）

```bash
ssh trade-core "cd ~/BybitOpenClaw/srv && git pull --ff-only origin main && bash helper_scripts/restart_all.sh --rebuild --keep-auth 2>&1 | tail -30"
```

**預期**：~3-5 min downtime（Rust release rebuild + engine restart）

## §3 Post-deploy 驗證（30 min observation window）

### §3.1 立即驗（restart 後 5 min）

```bash
ssh trade-core "PG_PASS=\$(awk -F= '/^POSTGRES_PASSWORD=/{print \$2}' /home/ncyu/BybitOpenClaw/secrets/environment_files/basic_system_services.env) && \
  PGPASSWORD=\$PG_PASS psql -h 127.0.0.1 -U trading_admin -d trading_ai -c \
  \"SELECT date_trunc('minute', ts) min, COUNT(*) FROM trading.risk_verdicts \
   WHERE ts > NOW() - INTERVAL '5 minutes' AND reason ILIKE '%duplicate_position%' \
     AND details->>'symbol'='INXUSDT' AND details->>'strategy'='ma_crossover' \
   GROUP BY 1 ORDER BY 1 DESC;\""
```

**驗收**：reject < 5/min 即 PASS（從 baseline 666/min, ~99.2% 改善）

### §3.2 30 min 後驗（rolling burst confirmed gone）

```bash
ssh trade-core "PG_PASS=\$(awk -F= '/^POSTGRES_PASSWORD=/{print \$2}' /home/ncyu/BybitOpenClaw/secrets/environment_files/basic_system_services.env) && \
  PGPASSWORD=\$PG_PASS psql -h 127.0.0.1 -U trading_admin -d trading_ai -c \
  \"SELECT COUNT(*) reject_30min FROM trading.risk_verdicts \
   WHERE ts > NOW() - INTERVAL '30 minutes' AND reason ILIKE '%duplicate_position%' \
     AND details->>'symbol'='INXUSDT' AND details->>'strategy'='ma_crossover';\""
```

**驗收**：30min total < 30 reject 即 PASS（從 baseline ~333000/30min）

### §3.3 ma_crossover 整體行為驗（不退化）

確認 ma_crossover 在其他 symbol（ETHUSDT 等）entry path 仍正常運作 — 不被誤套用 sync 邏輯。

```bash
ssh trade-core "PG_PASS=\$(awk -F= '/^POSTGRES_PASSWORD=/{print \$2}' /home/ncyu/BybitOpenClaw/secrets/environment_files/basic_system_services.env) && \
  PGPASSWORD=\$PG_PASS psql -h 127.0.0.1 -U trading_admin -d trading_ai -c \
  \"SELECT strategy_name, symbol, COUNT(*) fills FROM trading.fills \
   WHERE ts > NOW() - INTERVAL '30 minutes' AND engine_mode IN ('demo','live_demo') \
   GROUP BY 1,2 ORDER BY 1,2;\""
```

**驗收**：ma_crossover 30min 至少 0-3 fill（依 market 自然頻率，不能 0 fill 全 strategy）

## §4 Rollback Plan（任一驗證 FAIL）

```bash
# Mac 端
cd /Users/ncyu/Projects/TradeBot/srv && \
  git revert <W7-3 commit hash> --no-edit && \
  git push origin main

# Linux 端
ssh trade-core "cd ~/BybitOpenClaw/srv && git pull --ff-only origin main && \
  bash helper_scripts/restart_all.sh --rebuild --keep-auth 2>&1 | tail -10"
```

**Rollback 觸發條件**：
- §3.1 驗：5 min 內 reject 仍 > 100 → rollback
- §3.2 驗：30 min 內 reject > 100 → rollback
- §3.3 驗：ma_crossover 0 fill 且 cross-strategy 其他策略也 0 fill → rollback
- engine 啟動失敗（pgrep openclaw-engine 0 process）→ rollback
- 任何 panic / cargo build fail → rollback

## §5 Watch metric 影響評估

restart 在 watch 期內進行（HIGH-5 09:23 UTC start，21:30 UTC sign-off）：

| Metric | restart 前 | restart 後 預期 |
|---|---|---|
| 1 chain integrity | 100% | 100%（不受影響） |
| 2 [40] avg_net | +8.75 bps（含 hot loop noise） | 預期 **改善**（少 ~666/h reject 寫的 negative label noise） |
| 3 TONUSDT cell | -31.23 bps n=10 | 不受影響（grid_trading non-INXUSDT） |

→ watch 結果重新解讀為「N+0 + W7-3 partial」baseline；對 sign-off 不破壞。

## §6 Post-deploy 動作

- [ ] commit deploy 結果 update dispatch v3.2 → v3.3 標記 W7-3 partial closed
- [ ] memory entry 加 W7-3 deploy 結果（reject before/after + watch metric impact）
- [ ] W7-1 trait skeleton + W7-4 systemic audit + W7-5 on_fill/bootstrap 仍維持 sign-off 後做（complete W7 chain）
- [ ] 通知 operator deploy 完成 + 30min observation 結果
