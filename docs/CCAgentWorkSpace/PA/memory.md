# PA Memory — 工作記憶

> 本檔=長期教訓+近期記錄；超 300 行由 R4 巡檢標記、PM 派工壓實，舊條目原文遷 memory-archive.md（append-only）；agent 完成序列照常追加於檔尾。

## 長期教訓（2026-06-10 壓實蒸餾，源自原第 1-7242 行；原文見 memory-archive.md）

- 任何 brief/spec/operator/前審 claim（「0 caller」「已刪除」「IPC seam 已 live」「writer healthy」「V### 已 land」）必親 grep/ssh 證偽後才採用——代碼註釋與 plan 文字是 aspiration 非 runtime fact；deletion claim 必附 commit SHA；phase 推進會使 greenfield/migration-number 假設 stale，必對當前 HEAD 重驗。
- Linux PG empirical 是唯一可靠 schema 防線：Mac mock pytest/cargo test/sqlx parser 全不驗 runtime semantic；migration 派發前必 ssh 查 `_sqlx_migrations` + 真實 schema + V###/ADR 編號占用（spec 浮動編號是 dispatch race source），double-apply 必測；PG 連線用 trading_admin/trading_ai（非 openclaw），timezone 以 `date -u` 對齊。
- exhaustive caller grep 必含間接鏈：helper_scripts/*.sh→*.py、`pub use` re-export、shell→python 呼叫；漏查會把生產路徑誤判為死碼。
- runtime 證據 > filesystem/git 證據：binary mtime、engine boot kind（Manual --rebuild vs watchdog Auto respawn）、「檔案 mtime 新 ≠ engine 看到新值」；deploy verify 必先 check boot kind；「engine silent」≠「engine broken」。
- fail-closed 設計鐵則：fail-OPEN guard（`if x>0 {gate} else {pass through}`）是反模式；placeholder/snapshot 必走 OK band 防 DEGRADED 染色；cold-start fail = conservative boot + alert 而非 crash；projection/IPC fail 一律 deny，永不 fabricate permissive。
- 環境事實：Mac srv 全源可讀（引擎在 `rust/openclaw_engine/src/` 488 .rs；兩度誤判 fixture stub 皆因查錯路徑），但 IPC/PG/engine runtime 必須 ssh trade-core；Mac engine not_running 是預期。
- 派發 SOP：self-contained dispatch packet（AC + 必讀 + 反模式 + Disconnect Recovery + NO-OP exit）；派發前 grep TODO active state + 既有代碼字面 + 編號占用；prompt 給的 module path/SQL schema/數字不可盲信（數字 6h 即漂移，crate ownership 用 find 5 秒可驗）。
- PA+E1 合一僅限：純 test split / isolated refactor + 規格清晰 + 既有 sibling 範本可循；高風險 IMPL 必走 A3+E2 對抗鏈；PA 任務字面誘導越界 IMPL 時主動 push back。
- §九 LOC 治理慣例：800 警告 / 2000 hard cap；sibling extract（`pub(crate)` struct+fn；persist.rs 是教科書範本）優於把工作擠回既有檔；拆分計劃必含外部 caller 全盤點 + 未來 N 次成長 headroom。
- 維度衛生：enum aggregation key 禁拼動態 trace 字串（free-text 屬 trace dim）；任何 enum column 配 cardinality healthcheck；healthcheck 必設 key-existence + value-quality 雙層；severity 排序 INSUFFICIENT_SAMPLE > PASS。
- async/sync 邊界：設計方案前先確認 caller 是 async 還是 sync——FastAPI async 路由禁 threading.Lock 阻塞操作，on_tick 同步線程可用；async fn 遞迴必 `Box::pin`。
- multi-session race 慣例：不 revert 非己改動、meta-doc 用 `git commit --only`、接手前 `git fetch` + `git log -5`；sibling 可能無害撞單（同做相同收口）；判 regression 看 `0 failed` 非 test count。
- leak/look-ahead 紀律：rolling(N) 含 current bar 必 mean-revert（必並列 shift(1) 對比）；sub-bar event timing 必做「事件落在 bar 開瞬間」boundary 思想實驗——1m bar 用 (open+close)/2 會洩漏 post-event 價格；alpha 驗證走 entry_index=i+1 + leakage_scan。
- 統計判讀：RED 先分「sample insufficient vs signal failure」（比對 baseline 採樣率，差千倍即 gate self-imposed scarcity）；trigger gate rate 是 design choice 可 sensitivity sweep；breadth ≠ n_independent（time-cluster-bound，不隨 symbol 數膨脹）。
- SM/audit 治理：append-only event log——transition 描述既有 object 狀態變化，不在新建 object 掛 from_state；audit table 是 multi-process SM 的 SoT 真值權威；AMD supersede 用 callout 保 historical lineage 而非刪檔/strikethrough 全文。
- 上游 verdict 不照單全收：operator「已確認事實」可大半錯、QC verdict inline 升級必 codebase grep 驗、audit report 的 call-site 清單必親 grep 重數；cross-finding reconcile 先對齊 definition 再對齊 verdict；兩源 push back 同事項必明標兩源合併 AC。
- spec 字面陷阱：「parallel group」標記必驗 file overlap + IPC contract/state import + lexical scope；容差/閾值一刀切誤讀是 sub-agent dispatch 最大誤讀源；「metric classify」vs「SM band dwell」類用詞不清是 IMPL drift 根因——spec 必把 named-but-underspecified gate 釘成 phase-bound 驗收。
- 工具慣例：`psql 2>/dev/null` 吞 SQL error 必交叉檢核；大檔分段 offset/limit 讀 + grep 定位勿整檔讀；ssh runtime 解讀易錯（systemd scope/transient PID），優先 sub-agent 驗證或接受 operator 更正；給 operator 的 shell 一律單行。

## 近期記錄

## 2026-06-14 — hftbacktest fill-realism harness 技術設計（Track3, design-only, E1-READY）
- 交付 `.../reports/2026-06-14--PA--hftbacktest-fill-realism-harness-design.md`（Operator/ 副本已放）。HEAD 48cdbfe2，read-only（0 業務碼/0 部署/0 DB write；dirty 164 檔只寫自己 report）。
- **data_availability 裁決=我們 PG 不能 replay（結構性缺失，非差一點）**：親 grep 證 L2/trade tape 全在引擎內聚合成 1m bar 才落盤——`market.ob_snapshots`(V002:69) 是 1m L5 summary（imbalance/weighted_mid/depth_5），`market.trade_agg_1m`(V002:94) 是 1m 聚合（buy/sell_vol·count·vwap），schema 註釋自承「replace raw trade tape/L25 per-second，減 99.7%」。WS orderbook parser(parsers.rs:115)只抽 top-5 進 transient PriceEvent，逐 tick 不落盤（aggregators.rs:263 ObAggregator flush）。全 src grep raw_trade/tick_recorder/l2_tape/book_delta=0 hits。hftbacktest 需 8-field（ev/exch_ts/local_ts/px/qty）逐筆 L2 delta+trade tape+雙時戳+queue model 三輸入（queue-ahead qty/該價成交量/depth 減量）=全是我們聚合掉的 99.7%。
- **缺口補法（已驗 $0）=Tardis 免費 first-day-of-month**：Bybit linear incremental_book_L2+trades+quotes+liquidations 自 2020-05-28（liq 自 2020-12-18），無需 API key，normalized 帶 exch+local ts=hftbacktest 原生源。**誠實限制**：只有每月 1 號單日（非連續）→對 fill-realism 校準夠，對 D2 多事件統計（n=5634 跨月）不足，只能可靠裁 NON-HARVESTABLE（fill_rate 致命低小樣本即穩健）；HARVESTABLE 需大樣本→標 INSUFFICIENT_SAMPLE。
- **architecture=獨立離線 harness** `helper_scripts/research/hftbacktest_fill_realism/`：鏡像 ADPE 進程模型（CLI/cron-only 獨立進程，control_api 0 hits，不需 restart_all/--rebuild）+polymarket_axis PIT append-only（run dir exist→raise，禁回填）+aeg_execution_realism artifact.py 範式（sha256+git_provenance+manifest）。不碰 order_manager/5-gate/IPC/live；artifact-only 落 OPENCLAW_DATA_DIR/alpha_history_runs 0 V### migration。**Rust-first 合規**：hftbacktest 有維護 Rust crate(v0.9.4,MIT,MSRV 1.91.1 同棧)→模擬器核心走 Rust binary（npz-in/json-out），Python 膠水（fetch/convert/bridge/cli，非 trading/risk/config 邏輯不增 Rust 債）；降級=Python hftbacktest 2.x。**fee 鐵則**：maker_rebate_bps=0.0 hardcode+test 斷言 rebate>0→raise（approved-MM=DECISIVE BLOCKER，OpenClaw 物理收不到）。
- **revalidate_d2_path**：D2=liquidation-cascade reframe delta-中性 LP，殘差+8.17bps t=5.4 n=5634 β_down=0.06 PASS bull/down 鏡像 PASS=6 週首個過 beta-中性鐵則，但疑 non-harvestable（半衰期<5min 活 spread/queue，taker net −21.66bps，maker 疑逆選擇吃光）。bridge_d2.py 用 market.liquidations(read-only 逐筆,已在庫)構 cascade 事件→hftbacktest ProbQueueModel 在 Tardis 真 L2 模擬反 cascade maker 掛單 fill+queue position+逆選擇→net（禁 rebate）裁 HARVESTABLE/NON-HARVESTABLE/INSUFFICIENT_SAMPLE。leak hot-spot=maker 掛單時戳嚴格落事件後第一 tick+feed latency，禁 (open+close)/2/同 bar 入場。**未成交單計入 fill_rate 分母**（D2 non-harvestable 核心=接不到=edge 蒸發）。**與 aeg_execution_realism 互補非重複**：後者從我們自己 demo/live fills 報已實現 fill 率（事後），hftbacktest 是反事實 queue 模擬（D2 從未在 demo 真掛過故 aeg 對 D2 無數據）。
- **E1 派發**：W0 E1-A(__init__+data_fetch+toml)∥E1-B(artifact reuse+converter+leak-guard)；W1 E1-C(harness+Rust binary,待 B npz schema)∥E1-D(bridge_d2+cli,待 A config+B converter)。檔零重疊。rollback=純 additive `rm -rf` dir+撤 toml/index 行，0 schema/部署副作用。E2 三審查=①maker_rebate=0 hardcode+rebate>0 raise②entry_ts>event_detect_ts 逐單斷言（leak-free）③小樣本禁吐 HARVESTABLE（fake-positive 災難源）。verdict=E1-READY conditional 2 confirm（operator confirm 接受免費單日只裁 NON-HARVESTABLE 建議 yes；Rust crate vs Python 由 E1-C 工時實測）。
- 教訓：①「夠不夠 replay」必親查落盤粒度非訂閱粒度——我們訂了 orderbook.50+publicTrade（流豐富）但落盤前聚合成 1m bar，replay 看的是 PG 不是 WS topic；schema 註釋「減 99.7%」就是 hftbacktest 缺的那 99.7%。②hftbacktest 數據源不是我們 PG 是 Tardis（外部 read-only md，ADR 允，鏡像 deribit_vol_axis），免費 tier 限制決定了「只能可靠裁負不能可靠裁正」=設計上把工具能力誠實框死，不讓小樣本吐高後果 HARVESTABLE。③D2 revalidate 的命門=「未成交單計入分母」——offline beta-fold +8bps 是「假設成交」的 gross，hftbacktest 的價值就是把「接不接得到」這個被 1m bar 隱藏的維度算進來。承 four-lens #1+feedback_active_profit_unconventional_mandate D2 突破+carry-vs-mm 禁 rebate 鐵則。

## 2026-06-14 — 四 lens 成熟方案可採用排序（fork-2 數據軸+fork-3 執行 並舉，design-only）
- 交付 `.../reports/2026-06-14--four-lens-adoptable-ranking.md`（Operator/ 副本已放）。HEAD 48cdbfe2，read-only。誠實主結論：**成熟可採用的真 edge 稀少**——前 2 名是工具/執行衛生（縮虧非創 alpha），後 4 名是待驗探針軸（多數預期扣費後歸零）。排序：#1 hftbacktest（離線 fill-realism harness，PROVEN-LIVE，$0，Rust 同棧）>#2 Bybit BBO-peg+PostOnly（執行衛生補 35% close-maker fill）>#3 Deribit DVOL（換軌合規入口，正交 OHLCV）>#4 XS/時序動量（週頻成本逃逸，daily-kline 已 backfill）>#5 cascade-fade（allLiquidation WS+266K rows 已 live）>#6 OFI/MLOFI 毒性 gate（非主信號，crowd-decay）。
- **親 grep 鐵證**：①L2 book50 已 live 已被消費（multi_interval_topics.rs:115、btc_lead_lag/snapshot.rs:30、anomaly_event_writer.rs:368 orderbook_imbalance）→OBI/OFI 原料在引擎內，新特徵不需新訂閱。②allLiquidation.{} WS 已 live（:131、main_ws.rs:73）=cascade-fade 數據源就緒。③PostOnly 機械半就緒（bybit_rest_client.rs:863 PostOnlyOnlyStage、bybit_private_ws.rs:362 EC_PostOnlyWillTakeLiquidity）但 **BBO-peg 字段 0 hits**=需 additive 新增於 order_manager.rs（中風險，走唯一寫入口）。④Hyperliquid 執行已 ADR-0040 hardcode 拒（order_router.rs:213）=只能當信號軸。⑤**beta-clean sign-fold harness 已存在=residual_alpha_producer_db.py**（RESIDUALIZATION_METHOD_V1=btc_beta_train_fit_residual_v1，leak-free FIFO net_pnl_bps，gated residual PSR/DSR/CSCV-PBO）→所有新軸 validate_path=成為此 producer 的 factor 跑 beta-fold IC，不發明新範式。⑥Deribit/Tardis/CryptoQuant/Polymarket 全 0 hits=全新軸需 point-in-time append-only 採集（禁回填）。
- **第一步建議**：採 #1 hftbacktest 離線 harness ∥ #3 Deribit 採集器（Wave 0，$0，零 live 風險）。hftbacktest 把「微結構 edge 是否 net-positive / 該不該建 BBO-peg」從猜測變 data-backed；★ 致命經濟學發現貫穿全評：所有 net 計算**禁 maker rebate**（OpenClaw 收不到，approved-MM=BLOCKER，承 carry-vs-mm map）。
- 教訓：①「借用評估」要把每條釘死成「工具/方法論/探針軸」而非含糊「策略」——沒有一條是現成 alpha，混淆會誘導寫執行碼=用錢自動化負期望。②先建離線驗工具（hftbacktest）再談寫策略碼=依賴拓撲鐵律（沒 net 證明不寫 BBO-peg/cascade）。③新軸 validate 全接既有 residual producer beta-fold，0 新 harness=最低風險。承 [[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]] 桶 C+本日 carry-vs-mm map+D2。

## 2026-06-14 — MIGRATION-TREE-1 virgin replay 前向相容修（scratch-DB 彩排 GREEN，design-only）
- 親查根因＋scratch 彩排：runner=Rust sqlx `Migrator`（per-file tx、checksum=Sha384(raw bytes) 無 normalization、`ignore_missing:false`）。prod `trading_ai` `_sqlx_migrations`=122/1/139；V004/V005/V023 DB checksum **逐字節等於現檔**→改這三檔任一即 checksum drift。但 prod active env `OPENCLAW_AUTO_MIGRATE=0`（非-bak 唯一活檔）→migrator 啟動不跑→改檔**今天不炸 live**，僅留 latent landmine（未來翻 =1 才浮現，可用 `repair_migration_checksum --apply` 修，operator-gated）。
- **brief 修正（重大）**：真 virgin psql replay（timescaledb 已建）不止 ordinal-23 一處 RED，有**兩處**真缺陷：①V005 ord=5 `public.account_snapshots_legacy does not exist`（PART4 Grafana bridge view 是 brownfield-only，virgin 無 legacy 表）；②V023 ord=22（版本 23）schema_guard A RAISE（V004 legacy stub vs V023 新 shape）。其餘 18 處 FAIL 全是我 scratch DB 缺 timescaledb 的環境噪音（prod 有 2.26.1，加 `CREATE EXTENSION` 後全消）。要「過 V023」必先修 V005（V023 在其後）。
- **設計（scratch 彩排全綠）**：V005=PART4 5 個 `FROM *_legacy` view 包 `DO $WRAP$ IF EXISTS(legacy) THEN EXECUTE ... END IF`（virgin skip / brownfield 照建）；V023=Guard A 前插 self-heal：偵測 legacy 形狀（有 model_name 無 canary_status）**且 0 row**（虛擬產物）才 DROP，**有資料則不動**仍由 Guard A RAISE（真 drift 保護不削弱）。**彩排證據**：full virgin V001→V139 全 121 檔 ON_ERROR_STOP GREEN＋final shape=新 21 欄；V005/V023 各 in-place 雙跑 rc=0 冪等；注資料模擬真 drift→V023 仍 RAISE rc=3 且資料 1 row 未丟；brownfield-positive→view 照建。**caller proof**：model_registry 生產消費者（Rust combine_layer/ml/registry + py model_registry）全讀新 shape canary_status，0 處讀 legacy is_active/model_name/version→legacy stub 是死碼，drop 空表零下游影響。
- **權衡誠實標**：三選項（改 V004/V005、改 V023 guard、新 V14x）中，**新 V14x 對 ordinal-23 無效**（V023 已 RAISE 在前，後置 repair 救不了 virgin）；任何能讓 virgin 過 V023 的修**必動 ≤V023 檔**→必改 checksum。選 V005+V023 是因：(a)動 2 檔最貼合 2 個真缺陷；(b)V023 self-heal 保留 Guard A 真 drift 保護；(c)prod auto_migrate=0 使 checksum drift 今天 0 runtime 影響。**Rollback**=git revert 兩檔（純 additive，無 schema 副作用）。**owed（operator-gated）**：(1)若未來開 auto_migrate=1 前需先 `repair_migration_checksum --verify` 看 V005/V023 drift 再 `--apply`；(2)真 deploy 走 PM→E1（改 2 SQL 檔）→E2（逐字驗 self-heal predicate + EXECUTE wrap）→Linux double-apply gate→QA。
- 教訓：①「ordinal-N RED」brief 可能只報最顯眼那處——continue-on-error 全枚舉才見真 scope（這裡 V005 brownfield 比 V023 更前）；scratch DB 缺 timescaledb 會噴 18 個假 FAIL，必先對齊 extension 才能隔離真缺陷。②checksum=raw-bytes-Sha384＋migrator 只在 auto_migrate=1 跑→「改舊 migration 會否炸 live」答案取決於 runtime flag 非檔案本身，必 ssh 查 active env（非 .bak）實值。③self-heal 安全鐵則=只 heal「空且死碼」，有資料一律退回原 guard RAISE，且必 scratch 實證「注資料後仍 RAISE」才算保護未削弱。

## 2026-06-14 — P1 live RiskConfig 繞 5-gate 對齊修（confirmed，design-only）
- 親驗 P1 屬實：`update_per_engine_global_config`(risk_routes.py:688,engine path param 可="live")只過 `_require_risk_write`(=operator+risk:write scope，:111/115)即 IPC `patch_risk_config engine=live`(:714)，**無** all_five_live_gates_ok；對比 `post_live_session_start`(live_session_endpoints.py:168) 先過 `all_five_live_gates_ok(actor,require_authz=True)`(live_preflight.py:247-327=operator+live_reserved 精確+OPENCLAW_ALLOW_MAINNET(Mainnet)+secret slot+簽名 authz)。即 live 訂單要全 5 gate，但改 live 風控上限只要 2。
- **既有正解先例**=`executor_routes._verify_live_gate`(:182-450)：engine=="live" flip shadow→submit 走完整 5-gate（inline 重實作）。risk-config 路徑沒鏡像它=漏洞。**最小修=Python 主修**：route 內 `if engine=="live": ok,rc=all_five_live_gates_ok(actor,require_authz=True); if not ok: raise 409{live_gate_failed,...}`，在 `_require_risk_write` 後、IPC 前；demo/paper 不變。用 live_preflight 既有 primitive（單一真源，勝過抄 executor inline 版）。
- **caller_check（全盤點）**：①demo/paper 三 route(/config/global·/config/category·/agent-adjust)走 risk_view_client._patch **無 engine 參數**→Rust default "paper"，不受影響。②`mlde_demo_applier.py:1564` AI sink **demo-pinned**(engine_mode default "demo":154/191，註明「Live rows never applied here，改 emit governed live candidate」)→送 engine=demo 不被擋。③`executor_routes` 已自有 5-gate。④**無任何自動 demo→live promotion 經此 route 寫 live RiskConfig**(grep 0 hits)→閘不誤擋合法流。⑤`g2_03_bind_helper.py` operator canary CLI **直連 IPC socket(HMAC)繞 FastAPI**，engine 可="live"(:109)→Python 修不覆蓋它。
- **Rust defense-in-depth=可選但建議只 audit-log 不硬擋**：dispatch.rs:311-316 既定架構=「Python route MUST front operator auth」，Rust IPC 今天對所有 method 0 per-method authz（socket HMAC=engine-access 信任層）。在 handle_patch_config 對 config_name.starts_with("risk/live") 加**硬**擋會：(a)Rust 無 secret-slot/authz 上下文無法驗 5-gate；(b)連帶擋掉合法 g2 CLI 直連路徑。故 Rust 端只應 warn!-log「live risk patch via IPC」供 V014 審計可見，不改授權語意=不破既有信任模型。真防線在 Python route。
- 教訓：①「對稱性漏洞」找正解先 grep 同模組的姊妹 route——executor 的 5-gate 是現成範本+live_preflight primitive 是現成單一真源，0 新授權邏輯。②caller_check 必查「直連 IPC 繞 route」的 CLI/sink——socket HMAC 是 engine-access 信任層非 operator 信任層，Python route 修不覆蓋它，得在 risks 標清楚而非假裝全閉。③Rust 端硬擋 live 會破現有「Python front authz」信任模型+擋合法 CLER，defense-in-depth 該 audit-only。

## 2026-06-14 — 統一 build 計劃：explore-gate(Track1)+new-arms(Track2) 合併 [design-only]
- 交付 `.../reports/2026-06-14--unified-explore-gate-plus-new-arms-build-plan.md`(Operator/ 副本已放)。HEAD e454078d，dirty 85 檔只寫本 report。親驗修正 brief 路徑漂移：`edge_estimates.rs` 在 `rust/openclaw_engine/src/`(非 intent_processor 子目錄,CellEstimate :20-51 七欄無 explore)；allocator 在 `program_code/ml_training/regime_bandit_allocator.py`(explore_budget_remaining:467/explore_budget=30:137/positive_prob_gate=0.55:124/forgetting_gamma=0.99:141 全對)；ADPE 引擎在 `program_code/ml_training/adaptive_demo_profit_engine/`(sink 未存=design-only 證實)。live gate :255-322 親讀 0 explore 引用=隔離單點守住。
- **統一序鐵律**：Track1=Track2 前置基礎——新 arm 都 cold-start 低樣本,沒 explore-gate 一進 branch A/B 就永久判負(first-detection deadlock cost-gate 版)。Phase0(explore-gate E1-A∥E1-B 並行)→Phase1(candidate A listing entry,gated on ≥1 真實 capture)→Phase2(candidate B illiquid MR,gated on spread 覆蓋)→Phase3 PARK(事件軸)。Phase1/2 串行(各自數據阻塞)。
- **arm 排序**：A(listing entry,護城河最強=thin-book $10-100k 硬約束機構吃不下)>B(illiquid MR,護城河邊際 C 可能≥M_inst)>C(事件軸 PARK 需3-6月)>D(cascade+cross-CEX 已證偽/out-of-scope RETRACT)。新 arm 6 步插入鏡像 liquidation_cascade_fade dormant(registry.rs:320 set_active=false);**歸因自動接通命門**=reward_source.py:50-53 用 allocator make_arm_id 重建 arm_id→新策略只要 strategy_name 進 fills+decision_context_snapshots 即自動歸因,無需改 mlde view/無需進 linucb v1_15(後者 arm_not_found fail-soft Shadow)。
- **honest EV**：explore-gate 找 edge≈0(設計上不創 alpha=cheap-info);新 arm 樂觀<20%(A~10-15%/B~5%);最可能=系統學會誠實歸零非硬湊正。小帳戶 capacity 假設=可證偽有條件(三證偽路徑:容量曲線平坦/non-transferable 撮合/live_demo 執行差吃光,三者同時成立才真,當前 5 軸無一通過)。真翻正仍在換軌 Rank7。
- **biggest risk**=fake-positive edge 來自不可轉移 demo 撮合被誤當真 edge 推 live。結構已緩釋(cost gate 最後一道+live gate 0 explore 引用+positive_prob_gate 歸零+三層降級);殘留人為防線 E2/QC 不可省:①逐字驗 live gate 0 explore 引用②QC 每候選報殘差 IC+regime slice+leak-vs-clean③sink 嚴禁寫死 explore_eligible=true 全 arm④demo→live 升級前過 fill_realism_tier 檢查。
- **教訓**：合併兩條 track 不是並列功能清單——先問依賴拓撲(explore-gate 是 new-arm 累積樣本的物理前置),序由依賴決定非由 EV 決定;new-arm「可建性」最 load-bearing 事實是歸因軌(reward_source make_arm_id 重建)而非 strategy trait 插入(後者 6 步現成),新策略零阻力接通歸因=Track2 真低風險所在。承本日 Track1 design + carry-vs-mm map + ADPE 部署序。

## 2026-06-14 — Track1 demo explore-gate 技術設計（餵 realized PnL 給 allocator 學）[design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--track1-demo-explore-gate-design.md`（Operator/ 副本已放）。HEAD e454078d。read-only（0 業務碼/0 部署；dirty 83 檔只寫自己 report）。
- **最 load-bearing 安全事實（親讀 router.rs 全鏈）**：cost gate 是**最後一道** gate。Guardian(Gate2 :293/336)/Kelly+P1 2%cap(Gate2.6 :410-421)/准入曝險日損全局名目(Gate2.7 :450-503) 全在 cost gate(Gate3 :982/gates.rs:125)**之前**執行→explore-gate 把 cost-reject 翻 pass **物理上不可能繞 Guardian/P1cap/准入**（根原則1/4 結構保證非 emergent）。
- **demo 真 entry 證實**：exchange mode(step_4_5_dispatch.rs:606) → process_gates_only_with_features(:633) → Validation profile(mode_state.rs:89) → `cost_gate_moderate_with_slippage`(gates.rs:125)。**不是 cost_gate_paper**（那是非交易所 paper-fill 分支 :1018）。brief 點名函數正確。
- **99.97% reject 母體=branch A(:144 low-sample deep-neg<-15)+branch B(:216 robust-neg n≥min_n)**；C/D/E 已放行。explore 介入=A+B 插 `if explore_eligible && explore_remaining>0 { return None }`。**B 也要開**（死 flat 主體；regime 轉折後 forgetting_gamma 衰減把 n_trials 縮回探索期，否則一次判負永久死=first-detection deadlock）。
- **Python→Rust seam 全已存在 0 新 IPC**：settings/edge_estimates.json ← load_for_mode(demo) ← reloader daemon(OPENCLAW_EDGE_RELOAD=1, main_boot_tasks.rs:472) → PipelineCommand::ReloadEdgeEstimates(mod.rs:656) → set_edge_estimates(mod.rs:790)。explore 信號=每 cell 加 2 additive 欄(explore_eligible/explore_remaining)，缺欄=fail-closed no-explore。demo+live 共用檔但 **live gate(gates.rs:255-320) 0 explore 引用=唯一隔離守門點(CC/E2 逐字驗)**。
- **allocator 已有 explore 信號**：regime_bandit_allocator.py `explore_budget_remaining(arm_id)`(:467)+`explore_budget=30`(:137)，arm=regime__strategy。**維度 mismatch 裁決**：regime 在 Python sink collapse(讀當前 PIT regime label)、symbol fan-out(allocator 不分 symbol=既有 arm-space)→gate 保持純確定性無 regime 依賴。新 sink=explore_quota_sink.py(additive merge 不覆蓋 JS writer，dry_run 默認 true)接 ADPE runner。
- **誠實鐵則**：explore-gate **不創造 alpha**=cheap-information 論點(小 size 收樣本邊際成本≪價值)非 capacity-alpha(後者=hype)；最可能結果=探索滿額後多數 arm 仍誠實歸 flat(positive_prob_gate 守)，把「search-space 有無 edge」從 unknown 變 data-backed NO=進展非賺錢。size 折扣列 P2(首版用 P1-cap 後 final_qty+explore_budget 筆數上限)。三層降級(欄位/sink/gate)全 fail-closed default=no-explore；runtime kill=OPENCLAW_EDGE_RELOAD=0。
- **E1 派發**：E1-A(rust edge_estimates.rs 2欄+gates.rs branch A/B，live 函數 0 改)∥E1-B(python explore_quota_sink.py+runner 接線+regime collapse)，檔零重疊。E2 重點=①live gate 0 explore 引用(demo↔live 隔離單點)②JSON 雙 writer(james_stein+sink)additive 不互踩③explore 信號 fail-closed(嚴禁 sink 寫死 explore_eligible=true 全 arm)。verdict=E1-READY conditional 2 confirm(operator 改 gate 已授權=已 confirm；QC/MIT confirm regime collapse 語意建議 yes)。
- 教訓：①「翻 reject 為 pass 會否破風控」必先親讀 gate 鏈**順序**——cost gate 在最後=所有硬風控已在前執行，explore 翻最後一道不繞任何前道(結構保證)，不能只看單函數。②Python→Rust「信號傳遞」先 grep 既有 seam——edge_estimates.json+reloader+ReloadEdgeEstimates 整鏈已 live，0 新 IPC，只 additive JSON 欄。③demo+live 共用 state 檔時隔離靠「消費端不讀」非「生產端分流」——live gate 0 explore 引用是唯一守門點必逐字驗。④allocator arm(regime__strategy) vs gate arm(strategy::symbol)維度不對齊→在有 regime 上下文的 Python 端 collapse，不在純確定性 gate 塞 regime。承 [[project_first_detection_deadlock_pattern]]+2026-06-14 ADPE 條+[[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]]桶 B。

## 2026-06-14 — ADPE MVP runner demo 部署序（dirty-tree-safe，0 改既有檔）
- 交付部署步給主會話：deployable unit = 恰 9 untracked 檔（`regime_bandit_allocator.py` + `adaptive_demo_profit_engine/{__init__,reward_source,ipc_lever,demo_maker_arm,runner}.py` + `settings/adaptive_demo_profit.toml` + 2 test）。`git commit --only <9 檔>` 安全：~25 個 `M`/`??` 屬其他 session（closed_pnl/tab-demo/m4/memory），完全不碰。
- **親驗鐵證**：①9 檔全 untracked 且無 gitignore 攔截（git add OK）；②allocator 唯一 program_code import=`thompson_sampling`（tracked clean），reward_source import `linucb_trainer`(tracked)+`regime_bandit_allocator`(在本批)→ 提交本 9 檔即閉包，Linux import 不斷；③runner 是**獨立進程**（grep control_api app 0 hits ADPE）→ **不需 restart_all.sh / 不需 --rebuild**，下次 cron/CLI 調用即取新碼；lever lazy-import 既有 IPC client（已在跑）。
- **CLI 真形態（grep runner.py 確認）**：默認 dry-run；`--apply` 真發 set_strategy_active；`--snapshot-out FILE`（apply 前存 kill-switch 快照）；`--kill-switch`（讀現態+還原，配 `--apply` 才真寫）；`--dsn`(default `OPENCLAW_DATABASE_URL`)；`--config`/`--seed`。**`--reset-all-active` 設計提過但 E1 未實作 → 不可引用**；真 rollback = kill-switch snapshot+restore。
- **誠實標**：current arms 多負 → 初期多 flat 預期（allocator `positive_prob_gate=0.55` + `flat_arm_cost_floor_bps=5`，全負 EV → all_regimes_flat=True 歸零）；profit 待 demo-maker arm（artifact tier，走 all_fills 軌、transferable_only 不吸收 → 不自動驅動配置）+ 後續正 EV arm 證實。Linux-verify owed（apply 前）= mlde view demo rows 非空 + regime 值域 + 真 snapshot 鍵形。
- 教訓：dirty multi-session tree 下「部署序」第一要務=釘死 deployable closure（哪些檔 + 哪些 import 必同批）並證無 gitignore/untracked-import 斷裂；標準 cron wrapper（`ml_training_maintenance_cron.sh`）給 env/PG/PYTHONPATH/venv/lock 範本，新 runner 直接套不重造。

## 2026-06-14 — carry vs MM edge 決策圖：real_buildable_edge=FALSE（兩條都不該建）
- 交付 `.../reports/2026-06-14--PA--carry-vs-mm-edge-decision-map.md`（複製 Operator/）。綜合 MIT carry net-APR + QC 對抗複核 + BB MM-tier per-RT 表 + read-only PG fee 事實。
- **carry=KILL**（QC 複核 survives）：net@coc5%=−4.81%、樂觀上限 coc5%僅 breakeven+0.09%、last-90d 正在付錢持有(−3.56%/yr)。機制=gross funding(majors 5-6%)≈spot coc(5-10%)無 buffer；9 季僅 2024Q4 過關=regime-bet。不建 spot 子系統。
- **MM=REJECT-as-currently-feasible**：唯一穩健翻正 cell=mid-cap×approved-MM rebate(−0.5~−1.0bps)=+0.98~+1.98bps/RT，但前提2(approved-MM 資格)=DECISIVE BLOCKER 非工程可解。當前 Standard 2.0bps 所有非-thin pair per-RT 負；thin 正號=AS/inventory 低估假象(stress −3.02bps)。
- **fee 事實**：帳戶零 mainnet volume（demo$406k+live_demo$24.9k 不計入 tier）；距 VIP1 23.2x；唯一 fee lever=執行衛生(close-maker 35%→↑)+rpiTaker，NOT tier upgrade。VIP≠MM（VIP 最高 Supreme 0% 非 rebate）。
- **PA build-cost 親 grep**：雙邊報價循環/inventory SM/OBI reprice=0 hits(需建)；但 L2 book50 訂閱已 live(`orderbook.50.{symbol}`)=MM 前提1 半就緒。spot 執行路徑=0 hits，但 spot config schema 已在(`risk_config.rs:537,727`,placeholder)。兩條工程都可建但都不該建——瓶頸非工程(carry=coc/regime；MM=資格)，放大 size 不改 sign。
- 教訓：「edge 評估」結論要釘死瓶頸性質(經濟結構 vs 帳戶資格 vs 工程)；瓶頸與 size 無關時，寫代碼=用錢自動化負期望。承 5 次 down-beta NO-GO 同根因，主路徑回真換軸(另類數據軸 Rank7,$0 離線驗)。

## 2026-06-07 — 幻影倉位記帳 bug 定位 + 修復設計（TONUSDT demo phantom LONG）

**RCA 鐵證鏈（已驗，非假設）**：幻影 LONG 不是 `apply_fill` 算錯，是 **WS `PositionUpdate` 與 `Fill`
兩條訊息對同一份 `PaperState.positions` map 亂序雙寫**：
- 兩回調共用同一 unbounded channel（`startup/private_ws.rs:95/143-162/167-179`），相對順序=Bybit 推送
  順序，引擎不保序不關聯。
- Bybit 平倉先推 `position(side=None,size=0)` → `loop_exchange.rs:550` `upsert_position_from_exchange(0)`
  → `fill_engine.rs:98` `positions_remove` 把 short 移除（flat）→ 隨後 close `Buy` `Fill` 才到 →
  `apply_fill`（`fill_engine.rs:295`）`if let Some(pos)` 落空 → 落到 `:364` **開新倉分支** → 開出
  `is_long=true,qty=437.3,entry=1.5744` LONG（指紋=平倉價+平倉量，完全吻合）。
- `commands.rs:686` `was_open` 落空 + `:710` 把這筆當新倉寫 `entry_context_id` → 幻影被賦正規血緣。
- GAP-7 `on_tick_helpers.rs:700` 每 1000 ticks 遍歷 `paper_state.positions()` 寫 `position_snapshots`
  （`trading_writer.rs:645`，engine_mode 來自 `effective_engine_mode`）→ 28548 假快照來源。

**關鍵架構事實（更正 PM 推測）**：①所有 PipelineKind（Paper/Demo/Live）共用同一 `PaperState`
（`pipeline_ctor.rs:52`），snapshot 對**所有模式**都來自 `PaperState` 非 Bybit 拉取 → **bug 非 demo 專屬，
live_demo/真 live 同樣脆弱 = 極高風險**。②`position_manager.rs`（Bybit 拉取）只給 reconciler 對賬，
非 snapshot 源。③Python `control_api` **無倉位帳本**（grep apply_fill/upsert/position_snapshots 全空，
不讀 snapshots）→ 倉位帳 Rust 單一權威，**無跨語言 parity**。

**reconciler-miss 結論（call-path grep proof）**：reconciler 在跑、覆蓋 demo，但**對賬軸接錯**——
`reconcile_once`/cycle 的 baseline 與 current **都來自 `pos_mgr.get_positions(Bybit)`**
（`mod.rs:296/438/470`），比「Bybit 上輪 vs Bybit 這輪」。TON 17:03 後 Bybit 一直 flat → baseline 每輪
seed flat → `classify(None,None)=Match` 永不 drift。reconciler **有讀** `engine_positions_mirror`
（paper_state 鏡像，`mod.rs:826`）但只是 Ghost verdict **已成立後**取方向派平倉（`process_ghosts:838`
第一行 `if !matches!(verdict,Ghost) continue`），**mirror 是「方向來源」非「偵測來源」**。即「偵測 Bybit
自我跨輪變化」與「偵測本地帳 vs Bybit 背離」是兩條軸，reconciler 只做前者 → 本地幻影結構性盲視 7.4h。

**修復設計**：①根因修 Option A（推薦）=`PositionUpdate` 降為 advisory 只讀比對，不再 add/flip/remove
本地帳，size=0 走既有 `converge_exchange_zero_close`（`commands.rs:1267` 已測），倉位唯一 mutating 源
變回 `apply_fill` → 競態消失。②`apply_fill` 補翻倉餘量（`:299` `qty>pos.qty` 時餘量被丟、目前**完全沒
實作翻倉**=附帶 bug）+ reduce-only fill 本地無倉時 no-op 不開倉（fail-closed 縱深）。③告警新軸=
reconcile cycle 加 mirror-vs-Bybit-current phantom 偵測（2-cycle streak+點查 gate），復用
`spawn_reconcile_audit`→`observability.engine_events` + canary_events.jsonl（事故②宕機 20h 無人知教訓）。
首版只告警不自動收斂。④需求 #2（錯過+5%）是 #1 下游：幻影佔位→引擎以為持倉→不再入場；#1 修好自動解。

**E1 拆解**：T1 fill_engine（根因+翻倉，linchpin）/T2 loop_exchange（PositionUpdate advisory+傳 is_close）
/T3 commands（透傳 is_close）/T4 paper_state mirror 升 struct 帶 qty/T5 reconciler phantom 軸。
Wave1 並行 T1/T3/T4，Wave2 serial T2(待T1簽名)/T5(待T4)。檔互不重疊。E4 核心=**17:03 亂序回歸 golden
test**（先 PositionUpdate(size=0) 後 Fill(Buy,is_close) → 斷言 flat 非 LONG、不寫幻影血緣）。

**教訓**：①「記帳 bug」先別怪算術——`apply_fill` 邏輯本身對，真因是**兩個無序寫入源競爭同一 state map**
（B-1 Phase 2 把 WS position 也接進本來純 fill 累積的 PaperState）。查 bug 要查「誰還會寫這份 state」。
②reconciler「沒抓到」≠「沒在跑」——**對賬軸接的對象**才是關鍵（baseline=Bybit 自我快照 vs =本地帳，
天差地別）。mirror 被讀≠被當偵測軸。③同一 channel 串行處理保留 venue 推送順序=venue 不保序時的隱藏
競態源。④共用 PaperState 跨三模式 → demo bug 自動是 live 風險，評級必須拉到極高。報告路徑
`docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-07--phantom-position-fill-bug-design.md`。

## 2026-06-08 — L2 D3 Phase-1 (provenance & audit) tech design (design-only, E1-READY)
- 交付: `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-08--l2-d3-phase1-tech-design.md`。承 v4-final design draft + execution plan Phase 1。
- **設計修正(ground 在 file:line)**: ① hypotheses 真表 = `learning.hypotheses`(PK `hypothesis_id BIGSERIAL`, V100:273-274), **非** design 寫的 `research.hypotheses`/`hid`。② `replay.experiments`(V041:81) 是 plain TEXT-PK 表, **非** hash-chained(V131/V132 才是 residual/hidden-OOS registry)。③ strategy-variant **無物理表**(grep CREATE TABLE 0 命中; Track-B 是 `trading.fills.track` enum, V101:79-82)→P1 該 hop N/A/DEFER。④ demo fills = `trading.fills`(engine_mode='demo', V003+V015:15), 是 columnstore hypertable → ADD COLUMN 必 nullable/無 DEFAULT/無 SET NOT NULL(V077/V101:170-181 feature_not_supported 陷阱)。
- **append-only 真相**: repo 兩模式。D3 用 DB-level REVOKE(強): `REVOKE UPDATE,DELETE FROM PUBLIC` + DO-block `REVOKE FROM trading_ai`(V099:298-307 最貼近語意; app role=`trading_ai`, 訂正 role=`trading_admin`)。application-discipline-only(V133/V064/V035)較弱不用於 ledger。
- **consequential 矛盾解法**: 推薦 Option(b) V114 narrow-column UPDATE 例外(`GRANT UPDATE(consequential) TO trading_admin` only, V114:204-265), **P1 不開 compression** 避 compressed-twin column-grant abort(V114:208-216 = feedback_v_migration_pg_dry_run V114 教訓本身)。writer INSERT-only; consequential promote 走獨立 trading_admin path。
- **redactor reuse-or-build**: hybrid。reuse `error_sanitize.py`(reason_code→safe msg) 解 str(e) 半; **新建** `l2_secret_redactor`(secret-pattern 大文本掃描, repo 無)。sanitize 在 D3 writer INSERT 前; sha256 over SANITIZED text(design:698-699)。
- **migration 號**: V134=ledger(+§C additive ALTER), V135=gate-seam(跳 V128 軟保留 reserved-if-needed breadth, V127:125)。
- **hypertable 強制 composite PK** `(l2_reply_id, created_at)`(design "text PRIMARY KEY" 是陷阱; V035:135/V064:163/V114:178 先例)。`agent.ai_invocations`(ledger:222-227) 只存 prompt_hash+response_summary 非 full → D3 確為 NEW table reuse helpers(_sha256_text:49, deterministic_event_ts:55)。
- 教訓: design draft 是 heads/grep 讀, in-full 親查推翻 3 個表名/結構斷言(research.hypotheses / replay hash-chained / strategy-variant 表存在)。**call-path grep + 親讀 migration 全文 > 採信 design 文字**。R2-5 live hop 按 operator 拍板 EXPLICITLY DEFERRED, 只 shape-note 不設計。

## 2026-06-08 (later) — L2 D3 Phase-1 design LOCKED to operator final decisions (overrode 2 PA recs)
- 同檔 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-08--l2-d3-phase1-tech-design.md` 由 "recommendation" 鎖成 LOCKED design。operator 拍板覆蓋我原 rec 兩處：
- **① consequential → Option (c) side-table（非我推的 (b)）**。理由=長期可擴展：ledger 純 append-only、**零 column-level UPDATE grant** → 未來可自由開 TimescaleDB compression，**V114 compressed-twin column-grant abort 根本不發生**（這是 (c) 勝 (b) 的長期 key；我親驗 `V114:208-216` twin-abort + `:249-257` nested-exception kludge 確為 column-level grant 專屬陷阱）。新 append-only side-table `agent.l2_consequential_marks`(mark_id/l2_reply_id/marked_at/reason/lane/marked_by/details, 建在 V134, 純 REVOKE, 同樣零 column-grant)。precedent cite=`learning.lease_transitions`(`V054:225`, PK `(transition_id,created_at):240`, event-sourced 每筆 append 非 mutate)。
- **at-creation 子變體選 (i)**（非 ii）：ledger 留**不可變** `consequential_at_creation BOOLEAN`（INSERT 設一次永不 UPDATE→零 grant），marks 表給 later-discovered。理由：known-at-creation 是多數→cheap 單欄掃描免 join；(i) INSERT-only 不重新引入 V114 trap。predicate=`consequential_at_creation=true OR EXISTS(marks)`。
- **② provenance §C ALTER → 獨立 V136**（非我推的 fold 進 V134）。理由：ledger 全可現 designable→V134 早定稿乾淨；provenance 有 Linux-verify 未決(`trading.fills` columnstore 形 / `decision_outcomes` table-vs-jsonb)→拆 V136 讓 V134 不等、rollback/dry-run blast radius 更清。P1=**V134**(ledger+marks)·**V135**(gate-seam)·**V136**(provenance ALTER)。三號全 free, 不撞 V128 軟保留（親驗 ls=V133 head, V128 absent）。
- **retention/compression**: P1 只鎖 shape, **不含 drop 邏輯、不開 compression**。post-P1 drop=anti-join marks 表 + at-creation flag。partial index 隨 retention deferred（舊 `WHERE consequential=false` 單欄 index 移除：(c) 下 false-at-creation 仍可能因 later mark 而 retention-worthy，單 predicate 錯）。
- **新發現(ground)**: `trading.decision_outcomes` = **plain table**(`V075:8` "2 plain tables")非 columnstore→plain ADD COLUMN 可；但 decision_outcomes provenance 屬 deferred R2-5 live hop **不在 P1 V136**。`agent` schema 已存(`V001:35`/`V064:18`/`V133:38`)→V134 mirror `CREATE SCHEMA IF NOT EXISTS agent` 即可不假設。
- **§H Linux dry-run checklist** 改覆蓋全三 migration：V134 兩表雙 apply 冪等 + 證 `trading_ai` 無 UPDATE/DELETE on 兩表 + 證 V134 **零 `GRANT UPDATE(...)`**(grep 0 hits)；V135；V136 columnstore-safe ALTER(nullable/無 DEFAULT/無 SET NOT NULL)。
- **內部一致性驗**: grep 確認 0 殘留 (b)/narrow-column/fold 肯定句(只剩標明「rejected」「overrides earlier」的 LOCKED 文字)、24-col 不變(rename 非加減)、marks 命名一致、V134/V135/V136 全 free。design-only 本 pass 0 碼/0 apply/0 DB write。E1-READY LOCKED。
- 教訓：operator 用「長期可維護/可擴展」軸推翻「reuse vetted pattern」軸——(b) 雖是 repo 已 dry-run 的 V114 pattern，但它**永久把 ledger 綁死在 V114 ordering+nested-exception**且擋掉 compression；(c) 多一張表卻換來 ledger 純淨可壓縮。短期 reuse-vetted ≠ 長期最優；column-level UPDATE grant on hypertable 是「會傳染 compressed twin」的長期負債，不只是「需 idempotency kludge」。

---

## 2026-06-08 — Gap-A PBO peer-validity ruling (residual gap-closure follow-up; PM methodology challenge)

**任務**：PM 質疑 residual gap-closure design 的 Gap A「A1-lite peer generation」是否 PBO theater。要求 code-grounded 裁決 + 修正 P2 scope。

**Code-ground 結論（5 問）**：
1. **A1-lite 機制**：PM 描述「re-bucketing candidate's own demo round-trips under param-perturbed exit/entry rules computed offline from trading.fills」= **re-grouping 同一條 fills**，不是 per-variant re-simulation。對照 `residual_alpha_gate.py:791-800` `_probability_of_backtest_overfit`（degenerate proxy：只比 peer **means** vs observed mean）+ line 728 註解自承「最小近似…正式 CPCV/peer beta 可在後續版本取代」。
2. **A1-lite PBO 無效**：PBO/CSCV（`pbo_gate.py:200-205`/`_cscv_pbo`）按定義需 **N 條 genuinely different config 的 OOS series** 做 IS-rank→OOS-rank。Re-group 同一序列 ≠ 不同 config → PBO invalid theater。**A1-lite 不得 fabricate peers**。
3. **DSR 不需 peers（確認）**：`dsr_gate.py:381` `compute_dsr(observed_sharpe, n_trials:int, ...)`，`n_trials` 是 **count**，`_compute_expected_max_sharpe(K)` 走理論 Gaussian E[max]；`trial_sharpes` optional。residual gate 內 `_deflated_psr(psr, n_trials)` 同理（count-based）。**DSR/PSR/beta-residual/permutation 全 peer-independent，single-config 可跑**。
4. **Promotion 對 PBO fail-closed（確認）**：`promotion_gate.py:106,125-131` — `candidate_oos_returns is None or <2` → `pbo_verdict=missing_cpcv_returns` → 整體 `verdict=defer_data`（除非已 block），`passes = verdict=='promote'`。即 **PBO defer → whole candidate defer，即使 DSR pass**。Rust 側 `canary_promotion.rs:376-388` 同構：`pbo=None`→Pending（不 fail 不 promote），Stage3→4 需 `DSR PASS + PBO≤0.5`。**結論：fake 2 peers 與 pass None 的 defer outcome 相同——除非 fake peers 真把 PBO 翻成 promote（正是要避免的 theater）。所以建 real peers 必須 A-full（Rust replay 真 variant series）**。
5. **修正建議（確認 PM 的）**：P2 = orchestrator wiring A1(residual/beta)+A2(DSR by n_trials count)+A3(permutation) on real candidates（關 defer-by-absence，gate 在 beta-masquerade P0 維度真正 active），**誠實 defer PBO 維度**（single-config，標 `pbo_not_applicable`/沿用 `missing_cpcv_returns` defer path）直到 A-full（P3）Rust replay 供 genuine variant series。

**關鍵架構事實**：
- 現役 production peer 語意（`promotion_evidence.py:184-236` `build_strategy_promotion_evidence`）：peer = **同一 config 跨 symbol 的 per-cell return series**（`candidate_key=symbol`），K=symbol-cell 數。是 cross-sectional 軸（不同 instrument=不同 sub-portfolio，legitimate-ish CSCV），**從來不是 param-variant，也從來不是 re-group 單序列**。A1-lite 的 re-group 比這還弱。
- **無既有 A-full variant-series producer**：`replay_runner.rs:378` 註解「T2 will land formal comparison route + DSR/PBO metrics」=Rust replay 尚未產 per-variant 比較序列。genuine PBO peers 須新建（=P3/A-full）。
- residual_alpha_gate 已內建誠實 defer：`_is_defer_only_reason`（line 892）把 `pbo_missing_candidate_returns` 當 defer-only（非 hard fail）→ `defer_data`；docstring（line 97-98）明寫 `allow_missing_pbo_for_core_tests` 只給 unit diagnostic「不得作 promotion evidence」。**設計已誠實，A1-lite 是多此一舉且有害的 fabrication**。

**教訓**：「真實有效」的 gate 設計，缺維度要 **honest defer 而非 synthesize peers**。PBO 的 input 契約（N 條獨立 config series）本身就否決了任何「從單 config 變出 peers」的捷徑——無論 re-group 還是 Python mini-backtest（後者還有 Rust↔Python 行為發散風險）。defer outcome 在 fail-closed policy 下與 fake-peers 相同，故 fabrication 零收益純風險。承 [[project_2026_06_05_residual_producer_build]]（當時已因「讓閘可信會誠實 defer」對單配置一律 defer，本裁決與之收斂）。

---

## 2026-06-08 — L2 Advisory Mesh Phase 2 (Orchestrator+registry+contracts+guard+admission+adjudication) 技術設計 [CC linchpin]

**任務**：產 P2 設計（design-only，no feature code / no migration apply / no deploy）。接 P1 D3（已落 `f1c3c1ca`：V134/V135/V136 + `l2_call_ledger_writer.py` + `l2_secret_redactor.py` + 78 tests）。報告 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-08--l2-p2-orchestrator-tech-design.md`。

**關鍵 grounding 修正（execution-plan 文字已 stale）**：plan §0「V134 next free / V13x after V134」是舊的——**P1 已吃掉 V134/V135/V136**（在 disk @ f1c3c1ca）。P2 真正 next-free = **V137**（驗 `V13[7-9]/V14*` = 0 hits）。但 **P2 結論=不需 migration**（registry=TOML SSOT；admission/adjudication=in-memory+log 進既有 V135 gate-seam；guard verdict 用既有 V134 `guard_verdict` 欄；C1 promote-inbox `learning.l2_promote_candidates` 是 **P5** 非 P2）。V137 reserved-not-used，只在 operator 重開「DB-backed runtime registry override」才取。

**carbon-layer invariants 全 ground 在真碼（CC 會逐條 grep）**：
- `promote_tier` auto-raise L1→L4 with `approved_by=None`：`learning_tier_gate.py:520`(def)/`:525`(approved_by None)/`:550-553`(只 L5 要)/`:570-574`(AUTO_PROMOTE L2/L3/L4) — **C1 hazard 真實**。
- `can_auto_deploy_to_paper=True@all-tiers`：`:185/196/205/218/231` + property `:660-662` — **C2 no-signal 真實**。
- `can_modify_live_config` 硬回 `False`：`:178`(field)/`:664-671`(property literal False, "EX-05 §8.2") — live hard line 已在碼。
- operator-scope WRITE 雙 pattern：`governance_routes.py:129-152` `_require_operator_auth`（docstring 自稱 "standard Depends() target for all write/state-change endpoints"）；TOTP-gated switch `governance_autonomy_service.py:598-601`（C1 promote-confirm 重用此）。`_AUTONOMY_PATH_MATRIX` `:80,116-130`（asymmetry 半成品：(e)/(j) auto-trigger=contract、(a)/(c)/(d) operator manual=expand）。
- `/cost/reset`(`:354`)+`/cost/pricing`(`:389`) POST **已** operator-scope（`require_scope_and_operator(...,"ai_budget:write")` `layer2_routes.py:247`）→ E3-E1 對這兩個是「確認非新增」，只有新 Orchestrator/registry route 要新硬化。

**D3 wiring 接點（P2 wire 進 P1 writer）**：`l2_call_ledger_writer.py:113 record_l2_call / :266 record_consequential_mark / :314 record_gate_seam / :367 get_l2_call_ledger_writer`（全 INSERT-only，已登記 singleton-registry §2.6.1 line 351）。engine 今天已在 `layer2_engine.py:352` 呼 `record_l2_call`，但傳 **hardcoded** `L2_DEFAULT_CAPABILITY_ID`/`L2_PROMPT_CONTRACT_VER`/`L2_OUTPUT_SCHEMA_VER`（`:89-92`/`:355/359/360`）→ **P2 wiring delta = 改成 registry/contract-driven**，既有 manual path 留作 seed `l2.manual_reasoning` capability（無 regression）。

**CC-grep-verifiable 的設計手法（每個 invariant 設成單一 construct 非 emergent）**：LANE_DIRECTION = 一個 loader-owned table（無 `live` key）+ 一個 STEP-1 `if expand: return MANUAL`（function 頂、不可被 tier/posture 覆寫）；C1 = Orchestrator module「0 個 promote_tier ref」；C2 = 「0 個 can_auto_deploy_to_paper posture branch」+ loader-reject；F.2 = table-driven adjudicator「內部 0 model call」；fail-safe iron rule = 「每個 state 都是 L2 capability 的減法、0 個 live-enabling write，worst case=NO_ADVICE=今日 baseline」。

**reuse-vs-new ground 確認**：~70% wiring（LearningTier `:165-234` + path matrix `:80` + P1 writer + operator-auth `:129` + budget gate `layer2_cost_tracker.py:286` + `check_daily_budget` + DOC-08 cap `budget_config.toml:9 daily_usd_max=2.0` + `ConfigDict(extra="forbid")` `agent_contracts.py:109` + RiskConfig TOML SSOT）；~30% net-new（Orchestrator/registry/LANE_DIRECTION/contract registry/guard registry/admission/adjudication 全 greenfield 0 hits）。

**verdict**：E1-ready，**conditional on 1 operator confirm**——鎖「P2 ships TOML-SSOT registry, no DB table, no V137」（§N.1）。其餘 design-decided。CC 擁 load-bearing audit（stress-test 5/6/10/15/16/18）；E3 擁 write-auth + fail-safe-under-fault。殘留 Linux-verify=無（P2 無 migration/無 PG semantic/無 Rust IPC）；E4 欠標準 Mac+Linux test regression。

**教訓**：execution-plan 的 migration-number 文字會被 phase 推進 stale（P1 吃掉 V134-136 但 plan 還寫「V134 next free」）——PA 設計時必 `ls sql/migrations/` + git log 對當前 HEAD 重新確認 next-free，不可信 plan 文字。承 [[project_2026_06_08_l2_d3_phase1]]（同 session P1 設計）。

---

## 2026-06-09 — L2 Advisory Mesh Phase 3 `ml_advisory.v1`（FIRST L2 capability）技術設計 [接 ML 管線]

**任務**：產 P3 設計（design-only，no feature code / no migration / no deploy）。接 P2 Orchestrator（已落 `6a9dd0f1`：**整套 mesh scaffolding 已 commit**——`l2_advisory_orchestrator.py`/`l2_capability_registry.py`+`settings/l2_capability_registry.toml`(skeleton 0 stanza)/`l2_prompt_contract_registry.py`/`l2_out_of_bound_guard.py`/`l2_conflict_adjudicator.py`，全 capability enabled=false）。報告 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-09--l2-p3-ml-advisory-tech-design.md`。

**關鍵框架修正（execution-plan/design 文字假設 P3 較 greenfield）**：P2 已 commit 整套 orchestration plumbing。**P3 不是 greenfield orchestration**——是 (a) 3 個 TOML capability stanza、(b) `ml_advisory.*.v1` PromptContract+OutputSchema 入 contract registry、(c) `ml_advisory.guard.v1` clause 入 guard registry（P2 `get_guard` 是 placeholder echo `l2_out_of_bound_guard.py:154-159`，P3 改 callable registry）、(d) **cascade executor** 接入 P2 dispatch seam（`l2_advisory_orchestrator.py:300-301` 明寫「P3 接各 capability executor + parsed_output」）、(e) **deterministic math gate 含 B1 beta_neutral_check（NEW，QC-gated）**。真問題是 data+math-gate，不是 plumbing。

**ML 管線 seam（全 ground:file:line，execution-plan §0 命令 read in full）**：
- `run_training_pipeline.py:474` `run_pipeline` → `PipelineResult{verdict, metrics{pinball_skill/crossing_rate/decile_lift/feature_schema_hash}, acceptance_report_path, onnx_artifacts}`；sink=fs acceptance-report JSON `:299-312` + `learning.model_registry`(V023) `:374`（gate verdict≠no_ship）。
- `mlde_shadow_advisor.py:578` → `list[ShadowRecommendation]` → **`learning.mlde_shadow_recommendations`** via `verify_replay_evidence_and_insert` `:469-489`，**`p_applied=false`:480 / `p_requires_governance=true`:481 hardcoded**。
- `leakage_check.py:41` `check_feature_leakage(names, strict)` → `(passed, violations)`，**僅 name-pattern**（FORBIDDEN_PATTERNS `:20-30` + ALLOWED_PREFIXES `:33-38`），78 行確認。pure fn 無 sink。
- **advisory sink CONFIRMED 雙層**：schema-enforced（V031 `applied DEFAULT FALSE`:432 / `requires_governance DEFAULT TRUE`:433 / live-gate CHECK 需 decision_lease_id `:444-449` / COMMENT「Not an execution queue」`:466`）+ producer-enforced（advisor hardcode applied=false）。→ ml_advisory feed 此 sink = **0 new exec authority**。

**beta_neutral_check 判定 = MUST BE BUILT（但有可重用 machinery）**：
- grep `beta_neutral` = **0 hits**。必建。
- **BUT `residual_alpha_gate.py` 已存在**（2026-06-05 residual-producer build）：`DEFAULT_REQUIRED_FACTORS=("btc","market")` `:23`，train-window beta fit→OOS residual `:4-5`，`ResidualEdgeReport{residual_mean_bps, beta_loadings, beta_edge_share, r_beta_retention, dsr_residual...}` `:101-127`，`evaluate(candidate_returns, factor_panel, protocol)` `:147`。**這是 B1 重用的 OLS/factor-fit machinery**，但非 B1 本身（residual gate 用 beta_edge_share<0.5；B1 要 `|β_btc|<0.15 AND |β_alt|<0.15 AND |β_down|<0.15` 確定性閾值 + down-market 子樣本 + altcap factor；且 factor_panel 是 caller-supplied，不產 BTC/altcap 序列）。memory「grep beta=0 殺你5次的維度沒接進 gate」=此 machinery 存在但未綁進 production gate。

**B1 data availability 判定（最重要交付）**：
- **BTC return series = READY**（V125 `alpha_history_storage` + daily-kline backfill 14505 日線，承 2026-06-02 aeg infra）。
- **down-market regime label = schema+writer READY, population OWED Linux-verify**：**V127 `research.aeg_regime_labels`** 有 `ret_30d`/`ret_90d`/`main_regime`/`market_anchor_regime`(BTC-anchored)，versioned+leak-free PIT daily（`V127:5-21,68-71`），writer `aeg_regime_runner/db_writer.py:28-79`。down-market mask(30d dd>8% OR 7d<-5%)可從 ret_30d/ret_90d 衍生。**BUT V127 只經 runner `--write-db` 填**（`db_writer.py:6`「默認只產 artifact」；migration `:17-21` 只建表不含 runner data）→ **Linux `SELECT count(*)` owed**。
- **★ PIT cap-weighted altcap basket return series = DOES NOT EXIST，必建（最大 B1 gap）**：grep `altcap/cap_weight/basket/market_cap/weighted-return` over research+ml_training = **0 producer**。FND-2 `fnd2_pit_universe` 只產 **symbol-list artifact**（included/cohort_ids/alive_from，`aeg_breadth_ladder/universe_artifact.py:3-10`；builder.py 0 return/price/cap-weight），**非 return series**。cap-weighted return basket 還需 per-symbol PIT cap × daily return。→ **QC/MIT-data construction item**。

**shift1_compliance / is_oos_gap（M3 leak-typing）判定**：
- **`shift1_compliance` = 0 hits（必建）**。
- **`is_oos_gap` = 只有 namesake-different metric**：`sample_weight_sensitivity.py:329-334` 是 train-vs-OOS **RMSE gap-ratio overfitting** detector，**非** M3 的「真 in-sample→out-of-sample 時序 gap」source-class。leak-typing producer 必建。
- 只 `name_pattern_check` 存在（leakage_check.py）。M3：ml_advisory 不得宣稱 leakage_check 輸出=leak-free PIT；每 leak claim 帶 `source_class∈{name_pattern_check,shift1_compliance,is_oos_gap}`；name_pattern_check 不滿足 math-gate leak 前提。**P3a 只需 source_class typing 強制；producer 是 MIT-owned，gate P3b**。

**3 modes + cascade**：全 `direction=neutral`（`LANE_DIRECTION["ml_backlog"]="neutral"` `l2_capability_registry.py:70`），0 new exec，feed 既有 advisory sink。diagnose_leak/interpret_result（L1，assert no alpha）+ hypothesize（**L3** because `can_generate_hypotheses` first True @L3 `learning_tier_gate.py:203`，bind `tier_capability_flag`）。cascade=Ollama screen(recall≥0.85，reuse layer2_critic)→**deterministic math gate（唯一 alpha validator）**→cloud-L2 interpret only survivors。**LLM 永不驗 alpha**（math gate 唯一，design §G.2:1224）。接 P2 orchestrator `:300-301` executor seam。dsr_gate.compute_dsr(`:381` count-based n_trials，single-config 可跑) reuse；pbo honest-defer single-config（承 2026-06-08 Gap-A PBO ruling）。

**★ P3a/P3b split（最重要建議，execution-plan §1:79-82/§2:197-199 明示）**：
- **P3a**（diagnose_leak+interpret_result，assert no alpha）：gate=**E2+MIT(M3 typing+M4 recall)**，**NOT B1**。**可現在 ship**——證 cascade+D3+M3+M4 於 zero-alpha 面，不等 B1 final / altcap data。
- **P3b**（hypothesize，promotion-relevant verdict）：**HARD-BLOCKED** on **QC(B1 final 4 numbers + altcap construction + leak-free PIT) + (L3 tier) + shift1/is_oos producers**。理由：B1 是 unattended-gate command-line（design §N.1:1864）；P3b 提前 ship 會讓 down-beta masquerade 過無人閘（殺 5 候選的失敗）。tier 對齊強化（L3 + enabled=false 雙閘）。

**Migration 判定 = P3 NO migration**：advisory sink V031 / D3 V134-135 / novelty agent.lessons V133 / regime V127+V125 全既有；registry=TOML。V137 reserved-not-used——只在 QC 要 altcap persisted table 才取（**PA rec：altcap on-the-fly/artifact 避 migration**，mirror FND-2 CSV-artifact）。shift1/is_oos 是 compute 非 schema（讀既有 data）無 migration。

**verdict**：**P3a E1-ready now**（design-decided+grounded，gate E2+MIT 非 B1）。**P3b design-ready 但 EXECUTION-BLOCKED**（QC B1 final + altcap basket 不存在 + shift1/is_oos producers）。rec：先派 P3a 證 cascade，平行開 QC B1-finalize + altcap-construction track。需 operator 鎖「P3 no migration；altcap on-the-fly」或重開 V137。

**教訓**：(1) phase 推進使「greenfield 假設」stale——P2 已 commit 整套 plumbing，PA 必先 `find l2_*.py` + 讀 P2 已 shipped 碼確認 seam（`:300-301` executor hook），不可信 design/plan 文字當 P3 greenfield。(2) 「data 存在」要分三層查：schema(migration) vs writer(producer code) vs **population(Linux runtime)**——V127 schema+writer 在但 population owed；altcap 連 producer 都無。(3) namesake collision 陷阱：`is_oos_gap` 在 sample_weight_sensitivity 是 RMSE-gap 非 M3 leak-typing，grep 命中≠語義命中，須讀 context。承 [[project_2026_06_08_l2_d3_phase1_green]]（同 session P1/P2 設計）+ [[project_2026_06_05_residual_producer_build]]（residual_alpha_gate machinery 來源）+ [[project_2026_06_03_v58_archive_audit_s2_design]]（V127 aeg_regime_labels 來源）。

## 2026-06-09 — L2 Phase 3b 實作設計（hypothesize alpha-bearing + B1 beta_neutral_check + altcap + leak producers）[design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-09--l2-p3b-implementation-design.md`。整合 QC B1 spec（4 numbers FINALIZED, altcap=EQUAL-WEIGHT operator-鎖）+ MIT spec（shift1 reuse / is_oos build / M4 / V127 0-rows）。承 P3a `aeae4da4`（cascade executor 已 ship diagnose/interpret 2 mode）。
- **最 load-bearing 架構真相**：hypothesize alpha-bearing 但 lane 仍 `ml_backlog=neutral`（`l2_capability_registry.py:70`）。verdict 是 promotion-relevant，但**promotion 動作**是另一條 `demo_stage1=expand=MANUAL`（`effective_autonomy` STEP-1 `:119-120`）。B1 gate 的是 verdict 不是 lane → hypothesize 可 neutral 又 B1-gated。alpha 驗證在 cascade 內**確定性 math gate**（唯一 validator，LLM 永不驗，iron-rule grep target `l2_ml_advisory_executor.py:27-35` 已驗 comment-only 0 calls）。
- **B1 插 math gate 序（§A.5）**：STEP0 Q1(N_trades_oos≥50→DEFER via `dsr_gate.compute_dsr(min_observations=50)`→insufficient_observations→defer_data `:465-507`)→STEP1 DSR(K)→STEP2 PBO honest-defer(single-config，承 2026-06-08 Gap-A 不 fabricate peers)→**STEP3 beta_neutral_check(pooled β_btc/β_alt on≥90d + down-leg β_down on≥180d-span)**→STEP4 leak precondition(shift1/is_oos leak_free=True 否則 DEFER)。overall=strictest-wins(fail>DEFER>pass，mirror `residual_alpha_gate._verdict_from_blocking_reasons:882-889`)。B1 是與 DSR 同 tier 的 hard precondition（design §N.1(5)）。
- **SE 加法點（§A.2）**：reuse `residual_alpha_gate._fit_factor_beta:619-624`(np.linalg.lstsq)**不 fork**，wrap 加 SE=sqrt(σ²_resid·diag((X'X)⁻¹))（QC formula）；DW<1.5→HAC Newey-West（Bartlett kernel，hand-roll 無 statsmodels，仿 dsr_gate `:161-164`）。β_upper=β+1.96·SE<0.20 殺 noisy-small-β（5 候選失敗模式）。down-mask=klines-direct lagged-PIT（MIT Path B，不硬依 V127，V127 0-rows runtime-verified）。
- **altcap producer（§B）**：`program_code/research/altcap_basket.py` 新建，equal-weight ex-BTC CORE25 24 檔（`cohorts.py:27-33` minus BTCUSDT）daily-rebal on-the-fly。**PIT walk-forward**用 FND-2 `alive_from`/`alive_to`（`builder.py:235-236` clip-to-window；`SymbolLifecycle.listed_at/delisted_at:79-80` 是 lifetime 權威，first/last_seen_ts:83-84 診斷-only snapshot 27d 陷阱）。bar t 用 t-時 alive set 非今日 survivors，no zombie forward-fill = **唯一 M3 leak hot-spot**。producer 無 → altcap_returns=None → B1 DEFER（BTC-only）。**NO V137**。
- **shift1_compliance（§C）**=thin adapter reuse `feature_engineering_validator.py`（`is_leaky_sql:43`/`is_leakfree_sql:51`/`validate_shift1_pattern:72-113`）emit source_class；any DEFER→leak_free=False fail-closed。**is_oos_gap（§D）**=新建 `check_oos_gap`（distinct 名避 `sample_weight_sensitivity.py:329` RMSE-gap namesake，MIT option(a) 保 source_class string `:157` PA concur），temporal-sep+embargo+purge+no-shuffle 4 檢查。兩 producer 缺→math gate leak precondition unmet→hypothesize DEFER。
- **hypothesize mode（§E）**：TOML stanza `min_tier=L3`(`learning_tier_gate.py:203` can_generate_hypotheses first True@L3)+`tier_capability_flag=can_generate_hypotheses`+`enabled=false` 雙閘。executor 擴 `_P3A_MODES:563` 加 hypothesize + 插 math-gate STAGE(guard 後 sink 前，只 hypothesize)。guard 擴：reuse clause D(axes `:277-287`)+clause C(regime/bull-only `:257-275`)+NEW empty-mechanism curve-fit clause；**novelty dedupe 不在 guard（guard 0-DB 不變式 `:24`）改在 executor 跑 `retrieve_lessons(lesson_type=dead_mode)` DB 讀**（修正 naive「guard 做 novelty」誤讀）。promotion routing=demo_stage1 expand MANUAL，auto 須 B2 forward-OOS≥21d demo-only。**0 promote_tier/order/lease import；can_modify_live_config=False 不碰；C1 AUTO_PROMOTE_L3_TO_L4 `:119` 不觸**。
- **Migration 判定=P3b ZERO-migration，V137 reserved-not-used**（全 compute/JSON/TOML；altcap on-the-fly；V127 schema 已 applied sqlx_max=133 只欠 population）。
- **2 個 E1 implementation decisions（非 blocker）**：(i) cascade order reconcile——P3a executor cloud-first(`:591` before guard `:611`)，design §G.2 要 Ollama-generate→math-gate→cloud-interpret-survivors（cost 紀律 root principle 13），E1 implement §G.2 序；(ii) M4 artifact path 對齊 `..._screen_benchmark.json`(MIT) vs `..._screen_calibration.json`(P3a loader `:153`)。
- **owed-runtime（非 design blocker，blocks verdict-going-live）**：V127 population / agent.lessons seed 5-10 dead-modes / down-bars≥30 確認(full-span=309,last-90d=23) / altcap Linux smoke。

## 2026-06-14 — WS1 Polymarket 事件軸啟動累積 file-level 變更規格（design-only, E1-READY）
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--ws1-polymarket-event-axis-activation-design.md`。read-only 設計（0 業務碼 / 0 部署 / 0 runtime；只讀碼 + dirty 樹只碰自己 report，未動任何 agent memory/髒檔）。HEAD `976d420e`。
- **最 load-bearing 架構真相（決定整個設計形狀）**：collector 有 R-0 硬不變量「採集端零 relevance 截斷、零丟 row」（`__init__.py:18-22` + `collector.py:20-24` MODULE_NOTE + 負面測試 `test_collector_code_has_no_relevance_truncation_tokens:321`）。故「收窄到事件/監管市場、排除價格目標市場」**絕不可**做成 collector 端 market-type filter（會違反 R-0 + 撞負面測試 + 不可逆毀 raw）。唯一合規路徑 = **改 query set（發哪些 query）非 filter（丟哪些 row）**。
- **★ 關鍵發現：price-target vs event/regulatory 分類已可由既有 artifact 100% 下游重建，0 collector 改動**——flattened row 已存 `event_tags`(`collector.py:408`)+`question`(`:420`)+`event_title`(`:407`)，研究端可純離線 regex 分桶（"BTC>$X by date" 價格目標 vs ETF/SEC/FOMC 事件）。所以「收窄」嚴格而言是 query-efficiency 優化（少打無關 query、hourly top-N 名額不被價格目標市場吃光），**非數據可得性 blocker**。
- **WS1 設計三件（全 query-set 軸，沿用既有 pattern）**：(1) **QUERY_SET v2**=`__init__.py` 加 `QUERY_SET_V2_*` 常量（**保留 v1 不改**——`QUERY_SET_VERSION="v1"` 進 manifest/row 是 append-only 版本契約，改集合=升版鐵則 `__init__.py:52`），keyword 集移除純價格目標詞（"bitcoin price"/"all time high"/"btc"/單純 ticker），保留/強化事件監管詞（etf/blackrock/grayscale/sec/cftc/regulation/fed/fomc/rate cut/cpi/recession + 新增如 "approval"/"lawsuit"/"shutdown"）；tag 仍 crypto（無乾淨 event-only tag，web 證 Polymarket tags array 無 price-target/regulation 分離 slug）。(2) cli `--query-set` 選 v1|v2（默認 v1 不破既有 cron），`QUERY_SET_VERSION` 跟著選擇寫進 manifest（v2 run 標 v2，研究端可分版 join）。(3) hourly cron 啟用準備=**僅文檔/installer 已就緒，啟用本身=gated_activation 不做**（`OPENCLAW_POLYMARKET_CRON_HOURLY=1` 重裝既存 `install_...:101`）。
- **副作用清單（全部已 grep 證偽 = 零外溢）**：package 零生產 import（R-0），外部 0 importer（grep 確認唯一 consumer=自己+tests+SCRIPT_INDEX）；`QUERY_SET_VERSION` 三處讀（artifact:191 manifest / collector:394 row / cli:172 default）——若 v2 要寫進 manifest 須讓此常量隨 `--query-set` 動態化（非 import-time 常量），這是唯一接線改點。
- **test 影響**：既有 `test_polymarket_axis.py` 48+1 全綠不破（v1 常量未動、`test_row_carries_query_set_version_and_lane:314` 斷言 v1 仍成立）；新增 = v2 常量存在性 + cli `--query-set v2` 走 v2 keyword + manifest query_set_version=v2 三條。cron static test 不破（hourly 仍 commented）。
- **降級/rollback**：v2 是純加法（v1 路徑零改）→ rollback = 不傳 `--query-set v2`（默認回 v1）；cron daily 不受影響。hourly 啟用 rollback = `install_...--remove`（既存）。
- **教訓**：「收窄採集範圍」直覺會想加 filter，但本軸的 R-0 鐵則把 filter 一律推到研究端——PA 必先讀 MODULE_NOTE 硬邊界 + 負面測試，才不會設計出撞既有不變量的方案。且「分類已可下游重建」這點使 WS1 從「數據 blocker」降級為「query 效率優化」，scope 大幅縮小。承 [[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]]（Rank7 另類數據軸）+ QC discipline memo `2026-06-11--polymarket_axis_discipline.md` §0 定位鐵則 + MIT H4 calibration（liquid 市場 Brier 0.052 well-calibrated）+ MIT leadlag preliminary（forward IC 結構性不可算，需 hourly ≥20-30 時間點）。
- **verdict=E1-READY** conditional on 4 cross-team sign-off（operator altcap=equal-weight+zero-migration 已 prompt CONFIRMED；QC B1 final；MIT M3/M4 + is_oos option(a)）。其餘 design-decided ground file:line。
- **教訓**：①hypothesize「alpha-bearing」但 lane 仍 neutral——promotion-relevant 是 verdict 屬性非 lane 屬性，promotion 動作走獨立 expand/MANUAL。設計時別把「會 promote」誤當「lane=expand」。②novelty dedupe 需 DB 讀 → 不能塞進 0-DB 的 guard，要放 executor（已有 DB I/O）。guard 純確定性無 DB 是 load-bearing 不變式，別為 novelty 破它。③SE 是 residual_alpha_gate 唯一沒做的——OLS 全可 reuse(`_fit_factor_beta`)，wrap 加 SE+DW+HAC 即可，不 fork lstsq。④P3a executor cloud-first 與 design §G.2 math-gate-first 衝突——phase 推進使 executor 既有路徑與後階設計不一致，E1 須 reconcile，PA 設計時要抓出這種「既有實作 vs 後階 spec」的序差。承 [[project_2026_06_09_l2_p3_ml_advisory]]（同 session P3 設計）+ [[project_2026_06_08_l2_d3_phase1_green]]。

## 2026-06-10 — L2 P3b owed-before-enable：conductor wiring（owed ①③）+ dead-modes seed（owed ②）+ deployed-E2E 入口設計 [design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-10--l2-p3b-owed-conductor-wiring-design.md`（worktree `/tmp/wt-l2-owed` branch `fix/l2-owed` @ `0ce45a09`）。main 已 deploy P1-P3b（sqlx=136，3 表 dormant，registry 全 enabled=false）；`dispatch_and_execute`（orchestrator :379）零 production caller = 本輪要補的洞。
- **三個 load-bearing 偵察事實**：① math gate 真契約比任務描述多 2 鍵——leak stage 讀 `shift1_compliance_leak_free`/`is_oos_gap_leak_free`（executor :1139-1140），映射表必含。② **AEG-S3 候選來源無 daily return series**——`aeg_candidate_metrics/builder.py`（main `f3d4a29e`）輸出 per-regime 標量 rows（n_independent/oos_sharpe/k_trials...），harness 內部 series 不進 report JSON → candidate_returns 缺=B1 DEFER 誠實；**嚴禁標量合成常數序列（OLS β≈0 → B1 偽 pass，比 DEFER 危險——直接放行 down-beta 偽裝）**。③ `retrieve_lessons` filter=`WHERE symbol=%s`（必）+lesson_type+trgm；**source 完全不參與 filter**（layer2_critic :326-340）→ seed source 用第 4 namespace `dead_mode_seed` 純 provenance 區分；**symbol 才是命中關鍵**：seed 全用 placeholder `ml_advisory`（=_SINK_SYMBOL_PLACEHOLDER :444）+ 配套 `_check_novelty` 6 行 union 修補（先具體 symbol 後 placeholder），否則帶 symbol 的 dispatch miss 全部 seed=死資料。
- **設計決策**：觸發入口=(a) 新 operator-scope route `POST /api/v1/paper/layer2/ml-advisory/dispatch`（reuse require_scope_and_operator :254 pattern；inline-only evidence 零 path traversal；cron 拒=P2 open question+manual 哲學；CLI=curl 薄殼）。deployed-E2E 兩段：E2E-0 zero-enable（disabled dispatch → `_record_admission_seam` :330 真 seam row=鏈路證明，零 model call）+ E2E-1 operator-gated（enable diagnose_leak L1 一次→真 l2_calls→disable；不用 hypothesize 做 E2E）。owed ③=新 `app/l2_candidate_evidence_adapter.py` 兩層（純函數 build_math_gate_context + DB 層 load_factor_bundle reuse altcap producer/compute_down_market_mask）；regime 多行無顯式選擇→DEFER（防 cherry-pick selection bias）。owed ①=新 `learning_engine/bar_index_reindex.py` 純函數；**push back 任務的「0..N-1」**：`_span_days` 對 int key 用 max−min 當天數（beta_neutral_check :569-592），down-leg ≥180d 是 calendar span 語意→dense 0..N-1 在缺 bar 時低估 span，採 ordinal-day offset（無缺 bar 時恰=0..N-1；雙規則實作 index_rule 參數可切）；4h fail-loud 不支持（toordinal 撞 key）。owed ②=6 條英文主幹 seed（funding_arb_v2/funding_short_v2/cascade_fade/funding_tilt/grid_short_downtrend/textbook_scalping；**不 seed listing fade**=active 主路徑且在 M4 good-set 側）；工具=helper_scripts/m4/seed_dead_mode_lessons.py 默認 --dry-run 顯式 --write+--dsn、context_id=seed:<slug> 冪等錨點。
- **E1 拆分**：E1-A（learning_engine+helper_scripts）∥ E1-B（control_api app/）檔案零重疊；全部新測試強制 autouse `_no_real_db`（承 `0ce45a09` prod 污染 RCA：mock 不掩蓋邏輯的對偶=連線層必隔離；Mac fail-soft 吞錯假綠、連得上 prod 的環境就真寫）。layer2_routes 760→~835 超 800 review 線（標註）。
- **教訓**：①「producer 接口存在」≠「序列存在」——aeg_candidate_metrics 名字像 series producer 實為標量 normalizer，owed ③ 設計前親讀 builder 全文推翻「轉換層只是欄位改名」的隱含假設；缺的維度（daily series）誠實 DEFER + 契約預留，而非在 adapter 層補洞。② pg_trgm 檢索的三重對齊（symbol 相等 + lesson_type 相等 + content 語言同 hint）任一錯=seed 永 miss；中文 content vs 英文 statement 相似度≈0 是最隱蔽的死資料模式——seed 落庫前必須用 retrieve_lessons 真查驗收，不能只驗 INSERT 成功。③ re-index 規則不是純編號問題：int key 的數值差會被 `_span_days` 當天數消費，「交集後怎麼編號」直接改變 QC 檢查的語意，設計 re-index 必先 grep 誰消費 key 的數值（非只有序）。承 [[project_2026_06_08_l2_d3_phase1_green]]（P3b 設計同鏈）。

## 2026-06-10 — L2 P2p incident_sentinel（本地哨兵，alert-only never remediate）tech design [design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-10--l2-p2p-incident-sentinel-design.md`（worktree `/tmp/wt-l2-p2p` @ `13ae589f6`，docs-only 未 commit；Operator/ 已放副本）。6 軸最小集：A1 engine 心跳(pipeline_snapshot*.json mtime>900s=watchdog 45s 的後盾)/A1b watchdog 活性(pgrep,WARN)/A2 canary_events.jsonl 消費(MEDIUM-2 收口；alertable={RESTART_FAILED,NETWORK_OUTAGE,TRADING_INERT_PROLONGED,RESTART_SKIPPED}，排除 watchdog 已自 alert 的 circuit-broken 族)/A3 healthz(`/api/v1/healthz` 無 auth 監控端點 system_legacy_routes:326)/A4 seam reject>10/h(baseline≈0)/A5 lessons 雙層(rate>6/h+source 白名單 {'l2_session','ml_advisory','dead_mode_seed'})/A6 _sqlx_migrations drift(DB max vs repo max+success=false)。
- **load-bearing 偵察**：①06-05 watchdog alert wiring **已全量落地**（`92cdcc41`：`_send_alert_best_effort`:698 file-first alert_config.json+env fallback）→ P2p 通知路徑=sibling-import 該函數（`test_watchdog_alert.py:26-40` 既有 sys.path-insert 慣例），0 watchdog 邏輯改動，REJECT 抽共用模組(動穩定件)/app import/API 轉發(A3 在監測它)/第三拷貝。②**06-10 污染 rows source='ml_advisory' 是合法值** → 純白名單抓不到，A5 必須 rate 軸打主力。③canary 9 事件中 4 種只寫 jsonl 無 alert=A2 軸的精確目標集。④helper_scripts/canary 不在 conftest guard v2 進程級封鎖範圍 → 測試隔離鐵則必須自帶（conn/opener 注入+0 真 DSN+默認 dry-run）。
- 形態=cron */5（mirror halt_audit 1min wrapper+install_pg_dump installer 慣例；daemon 重現 who-watches-the-watcher 遞迴故拒）；dedup=key+4h re-alert 窗 mirror `emit_engine_down_alert_if_new` 語義但 **state 檔獨立**（watchdog_state.json 跨進程共寫=race）；canary 游標用 ts 非 offset（rotate-safe）。短命進程 daemon-thread alert 須 main 退出前 drain（watchdog 長駐沒這問題，sentinel 有=最易照抄漏掉的差異點）。真 alert 演練=--probe-alert INFO 探針+合成證據 dry-run+真 prod 兩輪 no-op 觀察，**禁製造真事故**。單 E1 四件套(~900 行)，零 migration 零 app 改動，rollback=移 cron 行。
- 教訓：①sibling 設計可能已被實作——設計前先 grep 設計文件裡的函數名於當前 HEAD（06-05 design 的 T1-T4 已全 land 且升級成 GUI-configurable file-first，直接改變本件 reuse 結論）。②「污染偵測」軸設計前必先查事故 rows 的實際特徵值（source 合法 → 白名單單軸是假防線，rate 才咬得住）。③alert 消費缺口要按「事件×已接線」矩陣盤點（9 事件只 4 種接 alert），不能籠統說「canary 無消費者」。承 [[project_2026_06_05_engine_selfheal_bindhost_incident]]+[[project_2026_06_08_l2_d3_phase1_green]]。

## 2026-06-11 — bb_reversion regime observability（hurst label 進 intents.details）tech design [design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-11--bb_reversion_regime_observability_design.md`。方案=dispatch 層 `persist_intent`（on_tick_helpers.rs:406）details 加 `hurst_label`/`hurst_value` 兩鍵，值從 step_4_5_dispatch 既有 `indicators` 參數（:148，與 gate 讀的 ctx.indicators :287 同一引用）搬運——0 OrderIntent/0 IPC/0 migration/0 策略碼；全策略統一加（分支反更貴）；commands.rs `source:"command"` 路徑不加且驗收 SQL 沿 opportunity_tracker 同口徑排除。1 個 E1（兩檔互鎖 ~25 行碼+~100 行測試）。
- **brief 證偽 4 件**：①`HurstHysteresis` 不存在（真名 HysteresisDetector）且三環境 TOML `[hurst] enabled=false` dormant → gate 消費 openclaw_core **瞬時** label（`hurst(close,10,50,0.60,0.40)`，閾值非 HurstConfig 0.55/0.45）；②`market.regime_snapshots` 0 rows 根因=enum+writer 在但 **0 producer**（死管道，不復活）；③`AlphaSurface.regime`（RegimeTag Tier4）是無關軸；④`openclaw_types::OrderIntent`=agent-spine 同名異物（namesake 陷阱再現）。
- 教訓：「label 要穿層傳遞」的假設先驗 caller 簽名——dispatch 函數參數早就持有 gate 同 tick 同 snapshot 引用，FUP-8 註釋寫的「等接進 OrderIntent」路線實測不必要；觀測欄第三鍵（自報 gate flag）不如 label 分佈本身的對抗證據力（反例直接證偽 gate，自報 flag 可與 bug 同謀）。

## 2026-06-13 — 綜合盈利機會地圖 ROI 排序（整合 QC/BB/MIT/AI-E 守攻研判）[design-only]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-13--PA--profit_opportunity_map_roi.md`（Operator/ 副本已放）。四域收斂同一根因（FACT 四源親證）：全系統負 edge=搜索空間問題（OHLCV+技術指標 1m demo 級 4-14bps 成本牆下無 net alpha，正 PnL=DOWN regime down-beta 副產品）非執行問題。
- ROI 鐵律：「攻」才有翻正可能但全 ASSUMPTION 需先 leak-free 驗；「守」只縮虧不翻正但成本側縮虧（rpiTaker/close-maker）是近零成本高證據0風險無悔動作；最高 ROI=最快驗證路徑（flag/近零成本 > 開發）。
- 排序：R1 rpiTakerAccess（守/成本，FACT，半天 IMPL，無悔）→ R2 L2 recall shadow（守/研究複利，$0+1 flag）→ R3 residual 多配置（攻/換軌，med）→ R4 close-maker fill（守/成本）→ R5 L2 hypothesize（攻，2 硬前置）→ R6 cost_edge_advisor（守/治理，gated on R5）→ R7 另類數據軸（攻/範式，全 ASSUMPTION 需離線 leak-free 驗）。
- **PA 親 grep 復驗（修正研判 2 處）**：① rpiTaker VERIFIED `order_manager.rs:357-419` 平鋪 optional-field（reduceOnly 同款）+0 hits=BB 半天估屬實。② residual claim 過樂觀——`residual_alpha_cycle.py:232 required_factors=("btc",)` 預設 **BTC-only 單因子**，多因子 beta-neutral 須 Stage-0R orchestrator 經 cell_kwargs 餵 factor panel=真 wiring 非 flag flip。③ L2 hypothesize 兩硬前置：MIT 親證 producer→context seam 無 live trigger + pre-call cumulative-cap DENY 未修（僅 post-call accounting，違原則 13）。
- 硬邊界：守動作全 0 觸碰三硬邊界/5-gate/can_modify_live_config；唯一鄰接=R5 L2 付費的 pre-call cap（開付費前硬前置）。E1 派發=Wave-A(rust/order_manager α+β 同 commit)∥Wave-B(operator flag + python/ml_training γ)∥Wave-C(序列 PA 先設計)；E2 重點=fee 語義一致性/factor-panel PIT-leak/pre-call cap fail-closed。
- 教訓：研判 claim「現成基建破陷阱」「operator 一行 flag」必親讀 producer 預設參數——residual 預設 BTC-only 單因子（多因子要 wiring），「現成」是 scaffold 在不是端到端通；PA 排序前必把「flag flip」與「真 wiring」分開，否則 ROI 成本估算失真。承 [[project_2026_06_05_residual_producer_build]]+[[project_2026_06_01_fail_closed_gate_stack_root_cause]]。

## 2026-06-14 — attacker 復攻 ship-now（推翻 DEFER）→ VERDICT: DEFER-HOLDS（cost-gate 因果鏈親 grep 證偽）[read-only]
- 任務=以 attacker mindset 盡力推翻「桶 A 縮虧過早應 DEFER」結論，為 ship-now 找最強因果。交付同檔 `2026-06-13--PA--profit_opportunity_map_roi.md` 追加 §7 attacker addendum（Operator/ 同步）。
- **最強 ship-now 假說（cost_gate-unlock）親 grep 證偽**：`intent_processor/gates.rs` demo 主路 `cost_gate_moderate_with_slippage` 的 fee 敏感分支**只在 `shrunk_bps>0 AND fresh AND runtime-derived AND validated` 時觸發**（:177-214）；99.97% reject 的主體是 `Some(cell)=>rejected`（:216-225，負 edge）+ deep-negative low-sample（:144），**fee_bps/threshold 從不參與**→降 fee 對主體 reject 0 影響。量化：demo threshold `fee_bps/wr*1.3`，taker entry @wr0.5=**54.6bps**、@wr0.3 floor=**91bps**；rpiTaker(maker-fee+taker-slip) 只降到 36.4bps，full PostOnly maker 降到 10.4bps。系統 realized edge≈0~負（MIT 全 cell 負+0 validation_passed），**離任何門檻數十 bps**→降 fee 解鎖 0 marginal 單。reject-with-positive-estimate=0（QC 反事實）坐實。
- **第二刀**：rpiTaker 是 taker-**close** 側價改，但 close **完全繞 cost_gate**（`strategies/mod.rs:69-71`+`step_4_5_dispatch.rs:1614` 親證）→close 側 fee 改善物理上不可能回饋 entry gate throughput。且 gate fee 是 `fee_rate_for_tif`（mod.rs:1956-1960）config/API-seeded 估計（taker 5.5bps/maker 2bps 常量），非 per-order 實際成交費→實際 close 費降也不改 gate 輸入。**cost-gate-unlock 因果鏈三重斷裂**。
- **被低估快贏查證=同樣 DEFER**：DYNAMIC STOP exit-drag（MIT −2~−6.3bps/fill）per-fill 真實，但 frequency 微小（05-10 close-tag audit `risk_close_dynamic_stop`=6 fills/0.06%）+ gate 擋 99.97% entries→總 fill 極少→aggregate $≈0；exit-policy 是「在近零成交流上的二階優化」與桶 A 同性質，非更近的真錢。
- **同意 DEFER 的部分**：桶 A 是 cheap insurance 不是 speculative（additive optional+1 行 rollback，CLAUDE Simplicity-first 不否決），但它**不解鎖任何當前被擋的 marginal 正 edge**——「無悔」成立、「現在就有真 $」不成立。主會話桶 A「立即打包但標執行衛生非進展」的定性**正確**；唯一修正=任務 brief 的「DEFER 到有正 edge flow」比 PM adjudication 嚴（PM 是 ship-now-as-hygiene），我裁 ship-as-hygiene 可、勿期待 $。
- 教訓：「降成本→gate 放行率變」要先 grep gate 的**比較分支觸發條件**——demo/live 主路對負 edge 是 `negative=>reject` 直接拒，fee/threshold 只在正 edge 子集算，所以 cost-side 縮虧對 fee-insensitive 的 reject 主體零槓桿；ship-now 的「解鎖 marginal 單」敘事必被「reject 母體的 edge 分布離門檻多遠」量化證偽。承本檔 2026-06-13 條 + [[project_2026_06_01_fail_closed_gate_stack_root_cause]]。

## 2026-06-11 — L2 結構化記憶層完整 spec（V139/V140+蒸餾管線+召回接縫+seed CLI）[design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-11--l2_memory_layer_design.md`（Operator/ 副本已放）。借鑒 TencentDB-Agent-Memory（MIT）l1_records/l1-extraction/l1-dedup 移植 PG+交易語義三類（system_trait/incident/rule）。V139/V140 對 fetch 後 origin/main 重驗 free（head=V138；**prod sqlx head=137，V138 apply 仍 owed**——V139 deploy 連帶）；healthcheck canonical=`passive_wait_healthcheck/runner.py:589-590`，佔至 [87]，**預留 [88][89]**（本批僅 1 行 reserved 註釋防撞，不實作 dormant check）。
- 關鍵裁決：①`agent.agent_memory`=plain table 非 hypertable（mutable 記憶庫，dedup 需 UPDATE；DELETE REVOKE+supersede 軟刪鏈保原則 8 lineage，content 不可變=merge 寫新 row）；②FTS 級=tsvector('simple') 拍板軸+pg_trgm 雙路 UNION（'simple' 中文弱，trgm 是 V133 已驗軸，0.1 門檻教訓）；③V140 pgvector 緩裝雙路徑：CREATE EXTENSION 權限 dry-run 失敗則檔不入 migrations/（sqlx fail-stop 卡鏈風險）改 helper_scripts/db/ 手動 apply；④fail-open-to-store **只在 dedup 段**，extraction parse fail=skip+cursor 不推進（反向=偽造記憶觸硬邊界）；⑤embedding 新 OllamaEmbeddingClient 不擴 LocalLLMClient ABC，body 絕不帶 dimensions（bge-m3 matryoshka 400 坑，README:336）；⑥B3 zero engine diff，影子期方案=bundle 走 engine 既有 input_context 落 l2_calls 零新表；⑦seed 源 B 用 `srv/memory/MEMORY.md` 索引行（現成人寫蒸餾，CLI 零 LLM、repo 內跨平台路徑）。
- 偵察推翻 brief 假設 2 件：(a) signal_postmortem **零 caller 零持久化產物**（純函數）——「讀 postmortem 產物」改為 distiller 內聯呼 classify_signal_failure(drar.report_jsonb)，本設計給它第一個消費者；(b) bge-m3 現機未 pull（僅 qwen3.5 9b/27b）——FTS-only 是部署日事實預設非理論降級。教訓：「產物」一詞必查三層（純函數→caller→sink），無 sink 的分類器=無產物可讀，設計要把缺的 wiring 內聯補上而非假設存在。

## 2026-06-14 — WS2 Rank3 residual 多配置 wiring file-level 規格 [design-only, read-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--WS2-rank3-residual-multiconfig-wiring-design.md`（Operator/ 副本已放）。**親查推翻 brief greenfield 假設：WS2 字面範圍 ~85% 已 shipped + flag-OFF**——cell_kwargs factor-panel wiring（btc+market+funding）整條已建並接線：`residual_alpha_cycle.evaluate_cell:119-157`/`bucketed_multi_factor:430-526`/`bucketed_funding_factor:363-422`/`residual_stage0r_preflight._load_multi_factor_inputs:335-406` 經 bridge 餵 evaluate_cell；多配置 enumeration（數值預閘+缺 lineage）`:243-286`；三重 flag-OFF（cfg.enabled∧stage0r∧producer + cron 不在 DEFAULT_JOBS）`:708-717`；net_side 真實推導禁 +1。
- **3 個真 delta**：①「market」≠「altcap」——路徑 A（Stage-0R）的 `bucketed_multi_factor` market 臂=全 PIT-active 寬基，`altcap_basket.py`（ex-BTC CORE25）是路徑 B（beta_neutral_check L2 ml-advisory）的 producer，**從未接進路徑 A**；Task-A 加 altcap 為第 4 因子=共線冗餘（altcap PC1~69% / equal-cap-vol ~0.95 corr）**建議 DEFER**。②bull/non-bull regime slice over residual 路徑=0 hits 真缺口（Task-B 新檔 `residual_regime_slice.py` 純函數 + Stage-0R `_load_regime_mask` 讀 V127 aeg_regime_labels；bull-slice 強制 promotion_eligible=False 落實 CLAUDE bull-only=learning-only 鐵則；保守版只進 summary 不碰 stamp/registry/drar hash byte-identity §5.6）。③regime label PIT leak-free（asof≤bucket，禁 forward-look）標 E2。
- 髒樹（HEAD 976d420e cold-audit WIP：TODO/8 agent memory/fills_loader/closed_pnl/static）與 WS2 目標檔 0 重疊；E1 用 `git commit --only`。gated_activation（本階段不做）：雙 flag-on + cron --jobs + V127 population（owed Linux count）+ Task-A operator sign-off。無硬邊界觸碰（demo evidence lane）。教訓：brief 寫「待 wiring」必先 grep caller 全鏈——多因子 panel 早已 shipped flag-OFF，PA 不重做已建路徑，只補 regime slice 缺口。

## 2026-06-14 — WS3「桶A 執行衛生」file-level 變更規格（rpiTaker + close-maker fill-quality）[design-only, E1-READY]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--ws3-bucket-a-execution-hygiene-design.md`（Operator/ 副本已放）。read-only 設計（0 業務碼/0 部署/0 runtime；只碰自己 report，未動 8 agent memory/fills_loader.py 等髒檔）。HEAD `976d420e`。
- **★ 最 load-bearing 發現（修正 brief 隱含假設）**：close-maker fill-quality 的 schema 基建 **V094（2026-05-15）已完整 live**——`trading.fills.details` JSONB + `close_maker_attempt` bool + `close_maker_fallback_reason`（10 值 CHECK 已含 `timeout_taker`/`postonly_reject`/`cancel_grace_expired`/`ack_lost`）。故 change 2 **不需 migration、不新建欄位**，「成交/timeout/reject 分佈」=既有 `close_maker_fallback_reason` 維度 aggregate 即得；change 2 只在 `commands.rs:807-815`（`apply_confirmed_fill_with_close_maker_audit` 唯一 `json!` 組裝點）additive 衍生一個 `close_maker_outcome` key 進 free-form `details`（不觸 CHECK 字典），衍生源=既有 `audit.fallback_reason`（不加上游採集=零維度漂移）。
- **change 1（rpiTaker）file:line ground**：`CreateOrderRequest`(`order_manager.rs:124-159`)末欄加 `Option<bool>` additive；body 組裝(`order_manager.rs:380-412` 平鋪 `if let Some` 模式)接 sl_trigger_by 後加 4 行 block。**fail-closed=None 不傳鍵=baseline taker**；rollback=註釋 body block（schema field 留著無害）。
- **2 blockers gated on BB**：①Bybit V5 RPI body **鍵名/型別待 BB 裁決**（API ref `2026-04-04--bybit_api_reference.md` grep rpi=0 hits→淨新欄位，E1 不得猜鍵名，掛 TODO(BB)）；②若 BB 裁定 RPI 走不同 fee 分類，`fee_rate_for_tif`(`intent_processor/mod.rs:1951-1961`)cost_gate 口徑須同步——**本階段不改**（標 gated，BB 裁後是獨立中風險 ticket，禁 change 1 順手改 fee）。
- **execution_path_touch=true → CC review**：兩 change 觸根原則 1 單一寫入口，但**控制流零改動**（change 1=additive capability opt-in body field；change 2=additive observation 記帳）。change 1 須 exhaustive 補全 `CreateOrderRequest {` 全構造點 `rpi_taker_access: None`（漏=不編譯=fail-loud 非靜默）+ 驗 PyO3 binding 0 hits（不跨 IPC）；change 2 唯一 caller `loop_exchange.rs:266` 無 signature 改。
- **E1 派發**：T1(change1 order_manager+caller 補None，**gated on BB 鍵名**)∥T2(change2 commands.rs 單點，**無 blocker 可即派**)，檔零重疊。建議 T2 先行證 observation 鏈。E2 重點=①caller 補全完整性(防 Default::default() 遮蔽)②outcome 衍生與 V094 字典語義一致(禁第三套 vocab)③執行路徑控制流不變。
- **教訓**：①「要加觀測欄」直覺會想加 migration/新欄——先 grep schema 既有 column 常發現基建已 live（V094 close-maker 三欄 2026-05-15 已落，brief 不知），additive 進既有 free-form JSONB 即可。②fill-quality「分佈」維度若已有受控字典（close_maker_fallback_reason CHECK），新觀測 key 必衍生自同源、語義對齊字典，禁並行 vocabulary。③淨新 venue body field（RPI）的鍵名/型別/fee 分類屬 BB 領域，PA 留 seam + 標 gated，不猜不寫死。承 [[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]] + ROI 地圖 R1/R4。

## 2026-06-14 — from-zero crypto-native anti-crowd 盈利論 + cost_gate verdict + top 3 方向 [design-only, read-only]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--PA--from-zero-crypto-native-anti-crowd-profit-thesis.md`（Operator/ 副本已放）。HEAD `48cdbfe2`。綜合三 lens（costgate-crowd/crypto-native/microstructure），三者收斂同一根因。
- **thesis**：之前虧=三層設計缺陷非市場無 edge：①我們是 crowd（5 active 全教科書 retail bot 配置→被當 exit liquidity，正 PnL=DOWN down-beta 副產品）②cost_gate 是方向-taker per-cell 守門員，結構盲視非方向/組合/做市 edge ③真 crypto-native 數據（liquidations/trade_agg_1m/LSR/OI/funding/spread）全在 production PG，$0 離線可驗，之前框架只測一個假設族。
- **cost_gate_verdict（親讀 gates.rs 全 370 行）**：是問題一部分但別拆=「對的 gate 用在錯範圍」。FACT：`fee_bps=2*(fee+slip)*10000` 雙邊 **taker 寫死**（DEFAULT_TAKER_FEE=5.5bps/leg、MAKER=2bps/leg，account_manager.rs:206-208）；比較的是 `get_cell.shrunk_bps`（方向標量，無 portfolio-net/non-directional 軸）；edge 估計來自自己策略 demo fills=**循環自證**。對方向策略它沒誤殺（06-14 attacker 已證 fee 不參與 reject 主體分支）。三錯：①成本腳對非方向用錯（該 maker 不該 taker）②per-trade 擋組合/非方向（要加軸或開獨立閘）③循環。修正=**按策略類型路由**：方向走現行 cost_gate、非方向走新 Gate3'（在 Guardian/Kelly/P1cap/准入之後=router 親 grep cost_gate 最後一道→換它不繞風控）。
- **top 3（全 $0 離線先驗，可能再 NO-GO 誠實標）**：D1 LSR-extreme+OI-crowding+funding 同號反轉（fade crowd 槓桿耗竭，當非方向定位收斂測非方向賭，crowded_risk 中）；D2 cascade-fade 重構為 delta-中性 LP 行為（策略已在 Rust registry.rs:300 含時間出場+反向 fade，預設 active=false，走方向 cost_gate 被當方向賭判+03-03 NO-GO，reframe=LP convergence+maker 成本腳，crowded_risk 中-高）；D3 non-directional viability evaluator（基建非 edge，為 D1/D2/未來做市開繞 cost_gate 的 Gate3'，crowded_risk 低，最大風險 fake-positive）。MM/carry 不入 top=瓶頸是帳戶資格(MM approved-rebate)/regime+無 spot 基建(carry)非設計（06-14 decision map 親證）。
- **對抗誠實**：LSR/cascade/OI/funding 之前**當方向賭測過 NO-GO**（03-03 cascade 280 事件|t|<1.3+down-beta 偽裝），本輪真新意=當非方向 LP/對手盤定位測；reframe 真假由 $0 beta-neutral 離線驗證裁決，t-stat 仍|t|<1.3/DSR≈0/全 down-beta 就再 NO-GO，嚴禁用「非方向」標籤掩蓋 down-beta 偽裝。E2 重點：①PIT leak-free（funding 8h 結算+OI 快照+cascade sub-bar 三隱蔽 leak）②down-beta negative control 不可省③離線 edge≠demo maker-queue 可捕捉（D3 fake-positive 防線）。
- 教訓：①cost_gate「99.97%真負」是定義域指紋非市場宣判——親讀全文確認 per-cell 方向-taker 框架+循環自證，修正是按策略類型路由非調參。②站對手盤數據全在庫，之前 NO-GO 是當方向賭測，reframe=非方向定位收斂測，真假由 $0 驗裁決不硬湊。③Gate3' 結構安全=cost_gate router 最後一道換它不繞上游風控（同 explore-gate 保證）。承 [[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]]+[[project_2026_06_03_blocked_signal_and_cascade_fade_nogo]]+本檔 06-14 carry-vs-mm/unconventional-edge/attacker 三條。

## 2026-06-14 — 四 lens 非常規盈利探索對抗複核排序 + top edge 設計骨架 [design-only, read-only]
- 交付 `docs/CCAgentWorkSpace/PA/workspace/reports/2026-06-14--PA--unconventional-edge-ranking-and-top-design.md`（Operator/ 副本已放）。HEAD `976d420e`。四 lens（stat_arb/hawkes_flow/info_theory/delta_neutral_carry）全自報 KILLED，**存活 edge=無**。
- 排序（軸權重：對抗存活>profit>fit-bybit>可實作）：①delta_neutral_carry（唯一機制不被摧毀=最接近翻案，死於「無 spot 腿 + 當前負 funding regime」工程/時機類，可恢復條件性）②stat_arb（4/300 真協整但 DSR≈0/N_eff≈2.5/UP>DOWN 不對稱=down-beta 偽裝，機制不成立）③hawkes_flow（η>0 真防禦資訊但宿主策略不存在 + 500ms feed 污染 sub-second 信號物理接不住）④info_theory（ΔOI 殘餘僅主軸 3.0% 無方向，淨 −14~−18bps cost_ratio≈45×）。
- **PA 親 grep 修正「現成」假設**：cash-carry ≠ 復活 funding_arb——`funding_arb.rs:461` 是單腿 perp（非 spot+perp 雙腿），dormant active=false（`:66,223`），G-2 已結案 NEGATIVE（−36.76bps/0勝率 n=13）。`OrderCategory::Spot` enum 存在（`order_manager_tests.rs:71`）+ per-cat P0 spot 槽（`risk_config.rs:722,727`）但**僅 scaffold**；`account_manager`/`position_manager` grep spot balance/position=**0 hits**=無 spot 庫存 ingest=cash-carry 連庫存都追不了=major build。
- **top 設計（delta_neutral_carry 構造 A，條件性非 ship-now）**：構造 B(calendar)=套利均衡永久 dead；構造 A(cash-carry spot long+perp short 收 funding)=delta 中性無隱藏 beta（與 stat_arb 不同=機制乾淨）但當前負 funding regime（BTC −1.73%/SOL −14.85% APR）下 short perp 付錢=現在賠。翻案需三條件同時：(A)spot 子系統 build (B)正funding+高premium PIT regime gate（現有 funding history 衍生非新數據軸）(C)size+cost-of-capital 模型（報告樂觀偏差源）。
- **設計鐵則**：Wave-0 先做 $0 離線 leak-free 驗證（新 `cash_carry_basis.py`+`cash_carry_regime_gate.py` 純函數 reuse altcap_basket PIT pattern + candidate_hidden_oos_sealer/residual_stage0r_preflight/cpcv_validator harness）——**這步就可能再 NO-GO**（扣 cost-of-capital 後 ≈0 則不啟動 spot build，省 major build）。spot 下單強制走 IntentProcessor+Guardian+cost_gate（根原則 1/4 不繞）；cost_gate 須算雙腿合計+spot fee 分類（`mod.rs:1951` fee_rate_for_tif 現只認 linear perp）；三重 flag-OFF+regime auto-dormant；裸腿 fail-closed。E2 重點：funding window asof≤bar 防 look-ahead（8h funding 結算是最隱蔽 leak）/delta 不變式+雙腿原子對賬/cost_gate 雙腿合計+cost-of-capital。
- **教訓**：①「最接近翻案」≠「存活」——四條全 false，但 killer 性質分兩類：統計幻覺類(stat_arb/info_theory)=結構性死，工程/時機類(carry「無基建+regime 不利」)=可恢復條件性，排序首位給後者。②「cash-carry 復活 funding_arb」是陷阱——funding_arb 是單腿 perp 占位（cost 常量假設 spot 20bps 但策略不開 spot 倉），cash-carry 是新雙腿子系統，只借 funding-direction 邏輯不借執行路徑（它沒 spot 執行路徑）。③enum/risk 槽存在 ≠ 下單路徑存在——OrderCategory::Spot scaffold 在但 IntentProcessor wire + 庫存 ingest 全缺，「現成」要分 scaffold/reserved vs live-path 三層查。承 [[project_2026_06_13_profit_diagnosis_searchspace_reconfirm]]+[[project_g2_funding_arb_monitor]]+[[project_2026_06_03_blocked_signal_and_cascade_fade_nogo]]。
