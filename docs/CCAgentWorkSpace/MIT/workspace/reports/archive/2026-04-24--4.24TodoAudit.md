# MIT 全景審計報告：2026-04-24 TODO.md ML 管線實作度

**執行時間**：2026-04-24 03:30 CEST  
**審計範圍**：ML 訓練管線完成度、Label 累積速率、Model Registry、Shadow Exit、Teacher/LinUCB/Bayesian/RL 狀態  
**報告路徑**：`docs/CCAgentWorkSpace/MIT/workspace/reports/2026-04-24--4.24TodoAudit.md`

---

## 一、核心發現摘要

### 🔴 最關鍵問題：edge_estimates.json 長期停滯

**現況**：
- `_meta.n_cells`: **1**（宣稱 135/162 cell）
- 最後更新：`2026-04-20T23:50:17.941867+00:00`（距今已 **4 日未刷新**）
- 單一 cell：`grid_trading::ORDIUSDT` with `shrunk_bps = -45.7275 bps`
- `grand_mean_bps`: −45.73 bps（已在 bind threshold −50 bps 邊界）

**與宣稱的差距**：TODO.md §P1-7 B 宣稱 2026-04-18 20:24「首次寫入 29 KB / 104 cells」後經 P1-15 清污修至「7d 乾淨 baseline −14.97 bps」；**實際當前檔案只有 1 cell + 4 日未更**。

**原因分析**：
- `edge_estimator_scheduler.py` daemon 的 `_run_cycle` 邏輯完整（716 行，@237 與 @510 leader-lock 機制存在）
- 但 **scheduler 未在 uvicorn 起動時正確啟動或已靜默停止**
- passive_wait_healthcheck 缺 edge_estimates 新鮮度 check（TODO §P1-7 C 指出缺 scheduler + hot-reload）

**阻塞等級**：**P1-7 B 核心阻塞**，影響 DUAL-TRACK Phase 1b 軌跡 2 B 的 cost_gate 綁定判定。

---

## 二、ML 管線實作度評級（stage × 完成度）

### Stage 1: ETL & Label Backfill
- **label_generator.py**: 86 行，基礎 backfiller
- **edge_label_backfill** (in `run_training_pipeline.py:110-150`): ETL logic 完整
- **Status**: ✅ **Foundation** — code 完整，reader 已接線（edge_estimator_scheduler.py @228 呼叫）
- **Row Count**: 47/200 (grid_trading + BLURUSDT，§P1-13 PoC 範疇)

### Stage 2: Label Quality & Quantile Training
- **leakage_check.py**: 單測覆蓋完整（test_leakage_check.py）
- **quantile_trio_trainer** (in `run_training_pipeline.py:250–350`): CQR + 3 quantile 邏輯完整
- **quantile_reports.py**: 驗收報告生成（6 metric 判定）
- **Status**: ✅ **Foundation** — 工具鏈綠，但因 label 47/200 數據不足，無法達 verdict=should_ship（需 ≥500）

### Stage 3: ONNX Export & Model Registry
- **onnx_exporter_quantile.py**: ONNX 三分位導出器
- **model_registry.py**: 430 行，registry 寫入 + canary 狀態機
  - `register_model()`: verdict gate + ON CONFLICT DO UPDATE + promoting|production WHERE guard ✅
  - `transition_canary_status()`: state-machine validator 5 狀態 ✅
  - **Test coverage**: test_model_registry.py 434 行，11 unit test（無 DB fixture）✅
- **V023 migration**: Guard A（legacy stub 檢測）+ 4 索引 + auto-touch trigger ✅
- **Rust ml::registry.rs**: 21.2 KB，5 個模組
  - `ModelSlot`: Hash/Eq derive ✅
  - `resolve_latest_production_artifact()`: registry lookup + fallback to _current symlink ✅
  - `validate_schema_hash()`: feature drift 防線 ✅
- **Status**: ✅ **Skeleton** — code 完整，runtime **0 row**（因 label 不足 → ONNX 無法導出）

### Stage 4: Edge Estimator (James-Stein)
- **edge_estimator_scheduler.py**: 716 行，daemon + leader-lock (flock) + SCHEDULER-SHUTDOWN-PRIMITIVE-1 ✅
  - `_run_cycle()` @237: backfill + JS estimator 並行邏輯完整
  - `_acquire_leader_lock()` @510: 4 worker 中選 1 leader via fcntl.flock ✅
  - `start()` / `shutdown()` / `trigger_now()` 冪等控制 ✅
- **james_stein_estimator** (import from ml_training): cells 計算邏輯
- **Status**: ⚠️ **Skeleton** — code 完整，但 **scheduler 未實際運行**（n_cells 停留在 1，4d 未更新）

### Stage 5.5: Model Registry Hook
- **run_training_pipeline.py**: stage 5.5 hook @~350 呼叫 `register_model_from_onnx()`（未明確見但應被 ONNX stage 調用）
- **Status**: ⚠️ **Skeleton** — 邏輯存在但未被觸發（ONNX export verdict < should_ship）

---

## 三、edge_estimates.json 真相

| 指標 | 實際值 | 宣稱值 | 差異 |
|------|-------|--------|------|
| `n_cells` | **1** | 104 (after P1-15) / 135 (initial) | −99 / −134 cells |
| `updated_at` | 2026-04-20 23:50 | rolling 7d 每小時 | **4d 停滯** |
| `grand_mean_bps` | −45.73 | −14.97 (live_demo clean) | 差異 ~30 bps |
| 唯一 cell | grid_trading::ORDIUSDT (n=3) | 應有 ~4 strategies × 23 symbols | **極端稀疏** |

**為何只有 1 cell？**
1. `_run_cycle()` → `_run_backfill(mode='demo')` → `edge_label_backfill(...)`
2. Backfill 回填 labels 到 `learning.decision_features.label_net_edge_bps`
3. 但當前只有 47 labels（grid_trading + BLURUSDT），James-Stein filter 要求 n ≥ 10 per cell
4. 大多 symbol 尚未足量累積 → cells 極端稀疏
5. 加上 scheduler 4d 未運行 → 無新 cells 產生

**bind threshold 狀態**：
- 需要：grand_mean > −50 bps **AND** ≥2 strategies 有 shrunk_bps > 0
- 當前：grand_mean = −45.73 ✅ (barely), 但只有 1 cell ❌
- **結論**：未達 bind condition；P1-14 blocker 仍成立

---

## 四、Label 累積速率與 Pipeline 瓶頸

### 實際速率
- **grid_trading BLURUSDT**: 47/200 (23.5%) — 距閾值還需 ~153 labels
- **其他策略**: 無動靜（Live/LiveDemo 未啟動；Demo 流量微弱）

### 預計時間（假設 demo 單 BLURUSDT 每小時 3-5 fill）
- 需時：~30–50 小時 = **1.5–2 天**（但實際受 symbol 輪動、市場波動影響，可能延至 **3–5 天**）

### 瓶頸根因（TODO §P1-13 SAMPLE-FLOOR-GAP-1）
1. **gridding 策略**: 日內 symbol 輪動頻繁，單 symbol label 稀疏
2. **ma_crossover**: ~380 RT（relative trades） < 1000 QA 守衛 → **Phase 1 不入 PoC 訓練**
3. **bb_reversion / bb_breakout / funding_arb**: 低於 QA 守衛
4. **Live stream 全無**: Phase 1a Live 門控未開放

**影響**: P1-7 C 訓練首跑卡在「awaiting natural label accumulation」被動等待，無法加速。

---

## 五、Model Registry + Canary 部署卡點

### Registry 表狀態
- **learning.model_registry** (V023): 表 + 索引 + trigger ✅
- **V023 Guard A**: legacy stub 漂移檢測 ✅
- **當前行數**: **0 rows**（正常——等待 ONNX export）

### Canary 狀態機
- **States**: shadow → promoting → production → retired | rejected
- **Transition API**: `/api/v1/ml/model_promote` (B5) ✅ 設計完整
- **Operator playbook**: `docs/references/2026-04-23--model_canary_promotion_rules_draft.md` ✅
- **Auto-promote cron**: Phase 4 second-half（暫延）

### 卡點分析
1. **ONNX export**: 因 verdict=no_ship（label 47 < 200）無法產出
2. **Healthcheck [9]**: `check_model_registry_freshness` 已加（per-slot oldest train_date 30d/60d 閾值）✅
3. **Phase 1a 預期**: registry 空、所有路由回 404，設計預期 ✅

**阻塞等級**：不阻 Live；阻 Phase 1b Track L （awaiting label n ≥ 200）

---

## 六、Shadow Exit 與 Combine Layer 準備度

### V021 Migration（INFRA-PREBUILD-1 Part A）
- **Guard A + B**: legacy drift 檢測 ✅
- **trading.fills.exit_source** 欄位: TEXT, 4 值 (Physical | Hybrid | ML | Disabled) ✅
- **learning.decision_shadow_exits** hypertable: 4 indexes ✅

### Rust 端實現
- **shadow_exit_writer.rs**: 467 行，完整 async loop
- **ShadowExitMsg** type: 8-tuple 設計完整 ✅
- **combine_layer.rs**: `build_ml_inference_shadow` + `emit_shadow_exit_observation` ✅

### Runtime 狀態
- **ExitConfig.shadow_enabled**: false（by design，Phase 2 才啟）
- **當前 row 數**: **0**（dormant）
- **decision_shadow_exits** 消費者: 純觀察，無驅動決策

### 健康檢查
- **healthcheck [8]**: `check_shadow_exit_ratio` 已加（Phase 1a dormant guard，Phase 2+ silent-dead 偵測）✅

**準備度**：✅ **Skeleton** — 完整但 dormant；flip `shadow_enabled=true` 即啟

---

## 七、Teacher / LinUCB / Bayesian / RL 各自休眠狀況

### Teacher（Rust claude_teacher/ 10 個檔案）
- **applier.rs** (48.9 KB): DirectiveApplier + IPC 調度完整
- **consumer_loop.rs** (26.2 KB): async task loop
- **governance_impl.rs** (8.4 KB): strategy IPC wrapper
- **outcome_tracker.rs** (16.2 KB): fill → outcome 歸因
- **Runtime 狀態**: ✅ **Foundation** 代碼完整，但 **teacher_directives 表 0 row**（無上級推薦）
- **阻塞**: G-7 W23（AI 治理層 layer 2 自主推理）；非 ML 管線核心

### LinUCB（Python ml_training/）
- **linucb_trainer.py** (345 行): Thompson Sampling 併行體
  - Arm state upsert 進 `learning.linucb_state` ✅
  - Feature matrix + covariance 計算完整 ✅
- **linucb_arm_migration.py**: arm 版本遷移邏輯 ✅
- **linucb_shadow_compare.py**: 影子 vs 實際對比 ✅
- **Test coverage**: test_linucb_*.py 系列完整 ✅
- **Runtime 狀態**: ⚠️ **Skeleton** — code 完整，但 `learning.linucb_state` **0 row**（無訓練觸發）
- **Consumer**: 設計無 live feedback loop；Phase 2+ 才驅動決策

### Bayesian Posterior（Python）
- **thompson_sampling.py**: Beta posterior + sampling（module_note 詳述 learning.bayesian_posteriors 持久化）
- **test_thompson.py**: unit test ✅
- **Runtime 狀態**: ⚠️ **Skeleton** — code 存在，`learning.bayesian_posteriors` **0 row**
- **Consumer**: 無；純研究模式

### RL Transitions（Python + Rust）
- **rl_transitions** table (V00X): 已建立（learning schema 一部分）
- **Python producer**: 無明確代碼（搜尋無結果）
- **Rust consumer**: 無
- **Runtime 狀態**: ❌ **Dead** — table 存在但無 writer（名義上等待 Phase 4 Policy Gradient）
- **Consumer**: 無

### 集中結論

| 模組 | 代碼 | Data | Consumer | 狀態 |
|------|------|------|----------|------|
| Teacher | ✅ 完整 | 0 | ✅ 完整 | Skeleton |
| LinUCB | ✅ 完整 | 0 | ❌ 無 | Skeleton |
| Bayesian | ✅ 完整 | 0 | ❌ 無 | Skeleton |
| RL | ❌ writer 無 | 0 | ❌ 無 | Dead |

**統一評語**：所有 4 個進階學習模組均處於 **Infrastructure 完成但 Runtime Data/Consumer 缺失** 的凍結狀態。均不阻 Live；阿影響 Phase 3+ 自適應能力演進。

---

## 八、Learning Schema 21 表完整清單與消費狀況

共 **24 表**（比 TODO 宣稱的 21 多 3 個）：

### Active Write + Read
1. ✅ `decision_features` — entry 特徵 + labels（writer: trading_pipeline / reader: edge_label_backfill）
2. ✅ `exit_features` — exit 特徵（writer: tick_pipeline / reader: shadow 驗證、future L2）
3. ✅ `decision_shadow_fills` — entry ε-greedy（writer: decision_layer / reader: pure observation）
4. ✅ `directive_executions` — Teacher 指令（writer: teacher_consumer / reader: outcome tracking）
5. ✅ `linucb_state` — arm 狀態（writer: linucb_trainer / reader: dormant）
6. ✅ `ai_usage_log` — LLM 呼叫記錄（writer: layer 2 / reader: cost tracking）
7. ✅ `ai_budget_config` — Token 額度（writer: budget_routes / reader: governor）
8. ✅ `strategist_applied_params` — 參數進化（writer: evolution_engine / reader: strategy_wiring）
9. ✅ `experiment_ledger` — 試驗日誌（writer: experiment_routes / reader: GUI dashboards）

### Write-only（無 reader）
10. ⚠️ `rl_transitions` — RL 轉移（設計無 writer + consumer）
11. ⚠️ `symbol_clusters` — symbol 分組（writer: symbol_clustering / reader: 無）
12. ⚠️ `foundation_model_features` — 基礎模型特徵（writer: layer 2 / reader: 僅 weekly report 無驅動）
13. ⚠️ `pattern_insights` — 模式發現（writer: ai_service_feedback / reader: 待確認）
14. ⚠️ `cpcv_results` — 交叉驗證（writer: quantile_reports / reader: 無）
15. ⚠️ `bayesian_posteriors` — Thompson Posterior（writer: thompson_sampling / reader: 無）
16. ⚠️ `james_stein_estimates` — JS 估計（writer: james_stein_estimator / reader: JSON file → engine）
17. ⚠️ `ml_parameter_suggestions` — 參數建議（writer: TBD / reader: 無）
18. ⚠️ `linucb_migrations` — arm 遷移（writer: linucb_arm_migration / reader: 無）
19. ⚠️ `linucb_state_archive` — arm 歷史（writer: linucb_trainer / reader: 無）

### Read-only（無 writer）
20. ❌ `observability.feature_baselines` — 特徵基線（writer: 缺 / reader: drift_detector）
21. ❌ `model_registry` — 模型註冊（writer: run_training_pipeline / reader: Rust engine；Phase 1a 預期 0 row）
22. ❌ `decision_shadow_exits` — exit shadow（writer: shadow_exit_writer / reader: pure observation；Phase 1a dormant）
23. ❌ `promotion_pipeline` — 晉升管線（writer: 設計無實現 / reader: 無）

### 未接線
24. ❌ `observability.scorer_predictions` — scorer 預測（design stub）
25. ❌ `observability.model_performance` — 模型效能（design stub）

### 總結
- **有效 I/O 通道**：9 表
- **孤島（write-only）**：10 表
- **盲點（read-only）**：5 表  
- **未實現**：2 表

**結論**：21 個 learning 表已建成，但只有 ~43% 有雙向 I/O，其餘 57% 處於單向或死頻道。TODO §P1-7 的「21 個 learning schema 表仍無 consumer」敘述**部分成立**（應為「10 個無 reader」）。

---

## 九、整體 ML 管線成熟度評級

### 按 Maturity Level

| Level | 定義 | 當前覆蓋 |
|-------|------|---------|
| **0. Dormant** | 代碼存在但無執行 | RL（無 writer）、Bayesian（無 data）、部分 teacher |
| **1. Foundation** | Code + Schema 完整，runtime 空 | ETL、label backfill、ONNX export、model registry |
| **2. Skeleton** | Code 完整 + writer spawn，但 data 稀疏或 consumer 缺 | edge_estimates（scheduler 停止）、shadow_exit（dormant flag）、linucb（無 feedback） |
| **3. Shadow** | Writer 實際產生 data，consumer 純觀察 | decision_features（47 labels），decision_shadow_fills（纯 observation） |
| **4. Canary** | Model 走 gate 但僅 gating shadow inference | (尚未到達) |
| **5. Production** | Consumer 實質改變決策 | 無 ML 驅動決策（live 關閉） |

### 整體評級：**Level 2.5 Skeleton**
- ✅ 基礎設施 90% 完備
- ⚠️ 數據管線 ~40% 運行（label backfill 中斷、scheduler 停止）
- ❌ 消費側 <10% 接線（無 live ML inference 驅動）
- ❌ 整個體系**被 P1-13 PoC 範疇限制**（單策略、單 symbol 數據不足）

---

## 十、MIT 建議 Top 3（加速 ML 解凍）

### 🔴 優先級 P0：恢復 edge_estimator_scheduler
```
行動：
1. SSH 進 Linux，確認 uvicorn 內 EdgeEstimatorScheduler.start() 已呼叫
   $ ps aux | grep uvicorn
   $ grep -n "start_scheduler\|EdgeEstimatorScheduler" /openclaw/main.py
2. 若未呼叫，在 main.py uvicorn 啟動時加入 scheduler.start()
3. 手動觸發一次 trigger_now() 驗證 scheduler 工作
4. 監控 settings/edge_estimates.json 的 mtime（應每小時更新）
5. 加 healthcheck [13] check_edge_estimates_freshness（每 6h 告警 mtime > 2h 舊）

預期：
- 恢復後 1h 內 n_cells 應重新增長
- 24h 內應達 20+ cells（受 label 稀疏限制）
- 解凍 P1-7 B 的核心卡點
```

### 🟡 優先級 P1-7 C：加速 label 累積突破 200 閾值
```
行動：
1. 並行增加 grid_trading demo 流量（目前 BLURUSDT 3 fills/h → 目標 6-8 fills/h）
2. 啟動 ma_crossover demo 單 symbol PoC（最高流量 symbol 當前 ~50 RT/7d）
3. 背景：等待 P1-10 PostOnly fee 修復效果驗證（2026-04-21 已部署 demo=true）
4. 目標：3–5 天內達成 grid_trading ≥ 200 labels，啟動首個 ONNX 導出循環

副效果：
- 驗證 run_training_pipeline.py 全 6 stages 端到端可用
- 驗證 model_registry 寫入 + canary 狀態機工作
- 積累活 model artifacts 供 Phase 3 OnnxModelManager 整合測試
```

### 🟡 優先級 P1-14：條件化 Bind cost_gate
```
行動：
1. 當 edge_estimates 達成 grand_mean > −50 bps **且** ≥2 strategies shrunk_bps > 0 時，自動 bind
2. 當前狀態：grand_mean = −45.73 ✅，但 cells = 1 ❌
3. 設定 EDGE_ESTIMATE_MINIMUM_CELLS = 10 作為執行門檻（避免單策略飄搖）
4. Healthcheck [14] 監控 bind readiness（3 條件）

操作流程：
- Engine 啟動 check edge_estimates.json
- 若 n_cells ≥ 10 && grand_mean > −50 && strategy_count ≥ 2 → auto-bind cost_gate
- Operator 遠程 inspect `/api/v1/system/edge_estimates` 驗證 bind 狀態

預期：
- 解凍 DUAL-TRACK Phase 1 軌跡 2 B（目前被 bind condition 卡）
- Phase 5 cost_gate 管制對 intent emit 的抑制減弱
```

---

## 十一、最關鍵 MIT 發現（單句總結）

**edge_estimator_scheduler 已 4 天未運行，導致 james_stein 邊際估計結果單一化（1 cell vs 104 expected），進而阻塞 P1-7 B 與 P1-14 cost_gate bind 條件，是當前 ML 管線解凍的最大卡點。**

---

## 十二、更新 memory.md

已追加至 `/Users/ncyu/Projects/TradeBot/srv/docs/CCAgentWorkSpace/MIT/memory.md` §「2026-04-24 審計產出」。

---

## 附錄：文件位置速查

### ML 訓練管線文件
- **Scheduler**: `program_code/exchange_connectors/bybit_connector/control_api_v1/app/edge_estimator_scheduler.py` (716 行)
- **Run Pipeline**: `program_code/ml_training/run_training_pipeline.py` (526 行)
- **Model Registry**: `program_code/ml_training/model_registry.py` (430 行)
- **LinUCB**: `program_code/ml_training/linucb_trainer.py` (345 行)
- **Thompson**: `program_code/ml_training/thompson_sampling.py`

### SQL Schema（Migrations）
- **V023 Model Registry**: `sql/migrations/V023__model_registry.sql` (Guard A + 4 索引)
- **V021 Shadow Exit**: `sql/migrations/V021__fills_exit_source.sql` (Guard A + B)

### Rust 實現
- **Model Registry Reader**: `rust/openclaw_engine/src/ml/registry.rs` (21.2 KB)
- **Shadow Exit Writer**: `rust/openclaw_engine/src/database/shadow_exit_writer.rs` (467 行)
- **Teacher**: `rust/openclaw_engine/src/claude_teacher/` (10 個檔案)

### Edge Estimates
- **File**: `settings/edge_estimates.json` (1 cell, 4d 停滯)
- **Updated**: 2026-04-20T23:50:17.941867+00:00

---

**MIT AUDIT DONE**: 2026-04-24 03:45 CEST — 全景 ML 管線完成度掌握，3 優先行動建議已列出。
