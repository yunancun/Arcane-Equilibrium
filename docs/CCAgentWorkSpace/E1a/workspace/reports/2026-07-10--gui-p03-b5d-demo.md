# E1a — GUI P0.3 Batch B5/D (demo/paper lane numeric contract) · 2026-07-10

STATUS: DONE — 3 files migrated, node --check green, hard-boundary IDENTICAL, static/ clean.

範圍 (skin-only 顯示層): `program_code/exchange_connectors/bybit_connector/control_api_v1/app/static/`
- `app-paper.js` (57 行變更) — legacy Paper 渲染器 + replay-lab 研究面板
- `tab-demo.html` (134 行變更) — LiveDemo 面 (**顯示精度變更,需 QC/operator 知悉**)
- `tab-paper.html` (70 行變更) — 模擬 Paper 面 (輕)

Baseline: HEAD `bf557fbca`. B0 契約 `common-formatters.js` 為唯一 formatter 正本 (未觸碰);
B3 先例 (tab-strategy.html ocSide / tab-edge-gates.html ocSignParts+ocBps) 為套用模板。

---

## 1 · 硬邊界自查表 (HEAD ↔ working, exact-substring)

| token | tab-demo | app-paper | tab-paper | 判定 |
|---|---|---|---|---|
| `onclick=` | 21→21 | 0→0 | 15→15 | IDENTICAL |
| `/api/v1/` | id | id | id | IDENTICAL |
| `ocPost(` `ocApi(` `apiPost(` `apiGet(` | id | id | id | IDENTICAL |
| `openConfirmModal` `openPaperCloseModal` | id | id | id | IDENTICAL |
| `classifyLiveMutation` `ocResidualRiskBanner` | id | id | id | IDENTICAL |
| `confirmPaperClose` `doDemoCloseAll` | id | id | id | IDENTICAL |
| `postMessage(` `addEventListener(` `_fetchReplayJson(` `ocCsrfHeaders` | id | id | id | IDENTICAL |
| `ocPnlCell(` (真呼叫) | 0→0 | 0→0 | 1→1 | IDENTICAL |

確認流 / typed-confirm / 授權 / gate / fetch / 事件 / POST payload **一字未動**。0 個 raw-string 新插值 (added-line XSS 掃描已驗:所有動態值經 ocSide/ocQty/ocPrice/ocMoney/ocBalance/ocPct*/ocBps/ocSignParts(.cls)/ocEsc/OC_EMPTY)。

### ⚠ ocEsc 計數下降 — 必讓 E2/QC 裁定 (deviation-with-justification)
| 檔 | ocEsc HEAD→WORK |
|---|---|
| tab-demo.html | 29→23 (−6) |
| app-paper.js | 34→27 (−7) |
| tab-paper.html | 12→12 (不變) |

每一處減少皆為 `ocEsc(untyped-string)` → typed formatter 置換,raw 值**不再進入 HTML 輸出**:
`ocEsc(p.side)`→`ocSide(p.side)` (固定字典查表,raw 不渲染)、`ocEsc(qty)`→`ocQty` / `ocEsc(price)`→`ocPrice` / `ocEsc(String(closedPnl))`→`ocMoney(,4)` (皆 `Number()`→`toFixed`,raw 被數字化)。
依 §2.1「數字經 toFixed 產生=機械安全,無需 ocEsc」——XSS 面被**消除**而非削弱。與 B3 先例 (tab-strategy.html:664 `ocSide(i.side)`) 同型。**逐行 XSS 掃描:0 個新 raw 插值**。故「ocEsc 不減」字面規則此處以「不新增未轉義 raw 插值」精神通過,但屬字面偏離,列此供 E2 判。

`ocPnlClass`(green/red) 全數移除:tab-demo 5→0、tab-paper 5→0;app-paper 用本地 `pnlColor` 亦移除。皆換 `ocSignParts(.cls)` (`.val-*`)。`ocAmount`(凍結) 全消:各檔 2→0。

---

## 2 · per-fill 4dp 例外清單 vs 2dp 收斂

**保 4dp (per-fill 已實現盈虧 column-fixed 例外,§1.5;收 2dp 會塌微額 0.00 假零):**
| 檔:函式 | 欄 | 動作 |
|---|---|---|
| tab-demo `_demoFillRow` | 盈亏/PnL | `ocMoney(pnl)` **2dp→4dp** (原 2dp 有假零風險,升 4dp) |
| tab-demo `_demoProfitRow` | 收益/PnL | raw 未格式化字串 → `ocMoney(closedPnlNum, 4)` (現帶 sign/U+2212/幣別,4dp) |
| tab-paper `paperFillRow` | 盈亏/PnL | `ocPnlCell(f.realized_pnl)` (B0 sanctioned 4dp helper,未改) |

**手續費 column-fixed 6dp 微額例外 (fee 非 PnL,但同微額塌零風險;與 tab-demo/tab-paper 既有 `ocNum(fee,6)` 一致):**
- app-paper `renderPaperFills` `.toFixed(6)`→`ocNum(f.fee,6)`;tab-demo/tab-paper fee 保 `ocNum(,6)`。

**收斂 2dp (水位/聚合,非 per-fill):**
| 檔:函式 | 值 | 動作 |
|---|---|---|
| app-paper `renderPaperPnl` | Realized/Unrealized/Net | **4dp→2dp** `ocMoney`;Fees 4dp→2dp `−`+`ocBalance` |
| app-paper `renderPaperPositions` | 每倉 uPnL | **4dp→2dp** `ocMoney` (與 tab-paper/tab-demo 既有 2dp 對齊) |
| tab-demo `loadPositions` | 每倉 uPnL | 保既有 micro-guard:`|pnl|<0.01` 給 4dp 否則 2dp (水位但可微額,防假零) |
| KPI Net/Realized/Unrealized/fills-return (三檔) | 累計 | 皆 2dp `ocMoney` |

微額判別已逐點親算:demo/paper 逐 PnL 欄判定「per-fill 事件額 (保/升 4dp)」vs「累計水位 (2dp)」,無誤塌。

---

## 3 · 第二通道 delta / level 判別表 (§2.2)

**核心裁決:demo/paper 面無任一「方向性變化量」欄 (§2.2 delta 型別:PnL 變化 / ROE% / 今日vs昨日 / price change% 皆不存在為欄),故 numeric delta-arrow (`.delta-arrow` ▲▼) = 0。** 全數 PnL 為水位或 per-fill 事件額 → sign + `.val-*` 色不加箭頭,符合 B0 `ocPnlCell` 先例 (per-fill 亦無箭頭)。唯一真週期-delta 欄「區間净 PnL」由 B0 `ocPnlSeriesTableRows` 渲染 → 不在 B5/D 範圍。numeric ▲▼ 機制 (B0 CSS) 保留給 LIVE (B6/E) 若有 price-change%/ROE% 欄。

**▲▼ 仍經 `ocSide` 落地** (§2.3 方向 badge「多 LONG ▲ / 空 SHORT ▼」),與 numeric delta-arrow (§2.2) 分屬兩通道。

| surface | 值 | delta/level | 第二通道 | ▲▼? |
|---|---|---|---|---|
| renderPaperPnl / renderBalance / loadSession KPI (Net/Realized/Unrealized) | 累計 | LEVEL | sign+`.val-*` | 否 |
| renderPaperPnl 手續費 | 成本量值 | LEVEL(cost) | `−`前綴+`.val-neg` | 否 |
| 每倉未實現盈虧 (三檔 positions) | 當前 MtM | LEVEL | sign+`.val-*` | 否 |
| per-fill 已實現盈虧 (`_demoFillRow`/`_demoProfitRow`/`ocPnlCell`) | 單筆事件額 | per-fill amount | sign+`.val-*` | 否 (循 ocPnlCell 先例) |
| close-all 合計 UPL summary | 合計 | LEVEL | sign+`.val-*` | 否 |
| 餘額/equity/available/margin/peak/initial/capital | 量值 | LEVEL(無號) | `.num` only | — |
| 勝率 / 盈亏比 (demo) | %/ratio | LEVEL | `.num`+`.val-*` (依 7d netPnl 正負/W-L≥1 著色,既有語義) | 否 |
| **方向欄** (positions/orders/fills/shadow/dust,全部) | Buy/Sell/long/short | — | **`ocSide` badge 多/空 LONG/SHORT ▲▼** | **▲▼ (badge glyph)** |

---

## 4 · fraction / percent 分類 (pct 消歧)

| 檔:點 | 輸入語義 | helper |
|---|---|---|
| tab-demo dust `gap%` (`(min-est)/min*100`) | **already-percent** | `ocPctVal` (2dp) |
| tab-demo `_ocMetricPct` (DEAD,無呼叫者) | fraction | `ocPct` |
| app-paper replay coverage `pct` ×2 (806/873) | fraction (覆蓋率 0.8) | `ocPct` (2dp,0dp→2dp) |
| app-paper replay maker cap `pct(makerCap)` (966) | fraction (機率) | `ocPct` |
| Win Rate / W-L / MaxDD (demo/paper) | 後端 unit 分派 | `ocFormatPerformanceMetric` (B0,未改) |

`risk-tab.js:524` 式 magnitude 猜測 = 本批無 (demo/paper 呼叫端型別已知)。

---

## 5 · bps 契約

| 檔:點 | 原 | 現 |
|---|---|---|
| **app-paper `fmtBps` :876 (mandated)** | `v.toFixed(0)+' bps'` 0dp | `ocBps(v)` 2dp;`'--'`→`OC_EMPTY` |
| app-paper replay net_bps (1011) | `fmt(,2)` | `ocBps(,true)` 帶號 U+2212 |
| app-paper replay drawdown (1031) | `fmt(,2)+' bps'` | `ocBps()` |
| app-paper replay run bands q10/q50/q90 (1035) | `fmt(,1)` ×3 | `ocBps(,true)` ×3 (帶號,防 ASCII '-') |
| app-paper replay baseline delta (1039) | `fmt(,2)+' bps'` | `ocBps(,true)` |
| tab-demo `_ocMetricBps` (DEAD) | `.toFixed(2)+' bps'` | `ocBps` |

替換後全站 grep 度量 (三檔):`+ ' bps'`=0、`.toFixed(0)+' bps'`=0、`.toFixed(1)+'%'`=0、`>=0?'+':'-'`=0、§5.3 CI-guard=**0** (一處 comment 曾含 `+ ' bps'` 已改寫避免誤觸 CI grep)。

---

## 6 · `.num` 清單 / 裸屬性 / 假零 / static clean

- **`.num` 套用** (data-value cell): tab-demo 41 元素 (positions 8 欄×row + orders + fills + profit + dust + KPI 5 balance + maxdd + capital + comparison + winrate/wlratio JS)、app-paper 23→36、tab-paper 1→27 (KPI 7 static + positions/orders/fills/shadow/feed td)。皆右對齊 tabular (tokens.css `.num`),循 B3 `oc-metric-val num` 先例。
- **散文內數字不掛 `.num`** (§3.1):balance-val inline「餘額: <strong>」保留、close-all summary 句、dust tooltip 皆散文 → 不右對齊。
- **裸屬性 = 0**:未新增任何 `data-value` 裸屬性;僅加 `class="num"`/`class="num val-*"`。
- **假零 = 0**:所有未知態 → `OC_EMPTY`(demo +11 / app-paper +7 / tab-paper +1),取代 `'--'`/`ocEsc(String(...||'--'))`/`|| 0` 假零路徑;formatter 對 null/NaN 回 OC_EMPTY。per-fill 2dp→4dp 升精度**降**假零。
- **static/ clean**:node --check 抽取檔 (`*.inline.js`/`*.chk.js`) 寫入 scratchpad 後即刪,static/ 0 殘留。

---

## 7 · QC 知悉:demo/LiveDemo 顯示精度變更點 (逐項)

1. app-paper `renderPaperPnl` (legacy Paper PnL 卡):Realized/Unrealized/Net **4dp→2dp**;Fees **4dp→2dp** 且 ASCII `-`→U+2212;` USDT` 字面 → ocMoney/ocBalance 幣別符號 (受 currency toggle FX)。
2. app-paper `renderPaperPositions`:每倉 uPnL **4dp→2dp**。
3. **tab-demo `_demoFillRow` per-fill 已實現盈虧 2dp→4dp** (per-fill 例外,降假零)。
4. **tab-demo `_demoProfitRow` 平倉收益:raw 未格式化後端字串 → `ocMoney(,4)`** (現帶 sign/U+2212/幣別;先前直印後端字串)。
5. tab-demo dust panel est/min notional:字面 `$X.XX` → `ocBalance` (幣別 + FX 轉換)。
6. **qty 欄 (全部):`ocNum(,4)`→`ocQty` = 4dp→6dp** (canon qty 精度)。
7. price 欄:`ocNum(,2)`→`ocPrice` (仍 2dp);market feed 價 <1 給變動精度。
8. notional/execValue:`ocAmount`(變動 2/4dp)→`ocBalance` (固定 2dp)。
9. 百分比:replay 覆蓋率 0dp→2dp、dust gap% 1dp→2dp。
10. bps:replay 0dp→2dp (含 mandated :876) + drawdown/net/bands/baseline → ocBps 2dp 帶 U+2212。
11. 方向欄全部:純色 做多/做空 → `ocSide` badge (多 LONG ▲ / 空 SHORT ▼)。

---

## 8 · 異常 / 需上游處理

- **[FLAG OPS/deploy] cache-buster 版本 stale**:`tab-paper.html` 載 `common-formatters.js?v=20260519.split-p2` (舊);`tab-demo.html` 載 `?v=20260628.gui-fills-md`。B0 已改 common-formatters.js (新增 ocSide/ocQty/ocBps/ocSignParts/OC_EMPTY 等)。**若 deploy 不 bump cache-buster,瀏覽器可能服務 stale JS → ocSide/ocQty ReferenceError,demo/paper 頁破**。本批屬「formatter+class」lane 未改 script src,務必於部署時 bump (至少 tab-paper.html)。
- **[FLAG E2] ocEsc 計數下降 (−6/−7)**:見 §1;typed-formatter 置換,XSS 面消除,0 新 raw 插值;字面偏離「ocEsc 不減」,請裁定。
- **replay-lab 節 (app-paper 790-1628)**:research/synthetic 非資金。bps→ocBps、pct→ocPct 已套;合成 money (net PnL `fmt(,4)`、balance `fmt(,2)`) **保留 `fmt` generic**——ocMoney/ocBalance 會把固定合成 USDT 模擬 FX 轉幣別=語義錯,故不套。非資金無 QC 精度顧慮。此節 `fmt(net,4)` 負值理論上仍 ASCII '-' (非 DoD-grep 目標),列此。
- **tab-demo:407-409 dust tooltip est/min `.toFixed(2)`**:tooltip 散文非渲染 cell、非 DoD-grep 目標 → 保留。
- **tab-demo:870 cache-age `age.toFixed(1)+'s'`**:duration/freshness 容器態指示 (非 typed money/pct/bps/qty) → 保留。
- **`_ocMetricPct/_ocMetricRatio/_ocMetricBps` (tab-demo)**:DEAD (0 呼叫者);仍對齊契約清 §5.3 grep 基線;P0.4 刪除候選。
- **`ocPnlCell` (B0) 內部 green/red + ASCII `+`**:B0/P0.4 territory,未觸碰;為 sanctioned per-fill 4dp helper。
- 未 commit (依指示)。
