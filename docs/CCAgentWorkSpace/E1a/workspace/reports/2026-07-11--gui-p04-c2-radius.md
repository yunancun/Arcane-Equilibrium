# E1a GUI 改動 — P0.4 C2 半徑收斂 · 2026-07-11

範圍：`program_code/exchange_connectors/bybit_connector/control_api_v1/app/static/`（除 `login.html` 兄弟髒改動）
spec-of-record：`docs/execution_plan/gui_redesign/design/07_consolidation.md` §3.1/§3.2
baseline HEAD：`4e89300ce`

## STATUS：DONE

全站 border-radius literal（3-18px）→ `var(--r-*)` token 化完成。180 個 literal 轉換，
0 殘留（除 login.html）。純機械確定映射，零裁量，零非-半徑改動。

## 方法

確定性腳本（`scratchpad/radius_convert.py`，非 static/，用畢即棄）：只匹配 `border-radius:VALUE`
宣告的 value 段，逐 token 按 §3.1 映射；表外值/2px/999px/50%/0/inherit/`var(...)` 一律原樣保留；
多值 border-radius 逐非零 literal 分別映射。**border 寬度等同行其他 px 不觸碰**（regex 只吃
border-radius value，如 tab-agents.html:256 `border-left:2px` 保持不動）。

映射（§3.1）：3/4/5/6px→`var(--r-1)`；7/8/9/10px→`var(--r-2)`；12/14/18px→`var(--r-3)`。

## 每檔半徑前後計數

| 檔 | before literal | converted | after literal | 保留(未動) |
|---|---|---|---|---|
| app-paper.js ⚠交易 | 9 | 9 | 0 | 999px×1, var(--card-radius)×1 |
| common-modals.js | 2 | 2 | 0 | 999px×1, var(--card-radius)×1 |
| common.js | 21 | 21 | 0 | 999px×3, var(--card-radius)×4, 多值零角×1 |
| console.html | 11 | 11 | 0 | 50%×2, 999px×2, var(--r-1)×1 |
| handoff_helper.js | 13 | 13 | 0 | 999px×1, **2px×1(seal 預設保留)**, var(--card-radius)×1 |
| risk-tab.js ⚠交易 | 1 | 1 | 0 | — |
| styles.css | 38 | 38 | 0 | 999px×1, var(--r-1)×2, var(--r-2)×1, 多值零角×3 |
| tab-agents.html | 6 | 6 | 0 | var(--r-*)×多, 多值零角 |
| tab-ai.html | 5 | 5 | 0 | 999px×1, 50%×1 |
| tab-demo.html ⚠交易 | 1 | 1 | 0 | var(--r-1), var(--r-2) |
| tab-development.html | 5 | 5 | 0 | — |
| tab-earn.html | 8 | 8 | 0 | 999px×1, 50%×1, var(--card-radius)×1 |
| tab-edge-gates.html | 2 | 2 | 0 | — |
| tab-governance.html ⚠交易 | 10 | 10 | 0 | 999px×1, var(--r-*)×多 |
| tab-learning.html | 1 | 1 | 0 | var(--card-radius)×1 |
| tab-live.html ⚠交易 | 13 | 13 | 0 | var(--r-*)×3 |
| tab-paper.html ⚠交易 | 4 | 4 | 0 | var(--card-radius)×2, var(--r-3), var(--r-2) |
| tab-phase4.html | 4 | 4 | 0 | 50%×1, var(--r-1)×2 |
| tab-replay.html | 2 | 2 | 0 | — |
| tab-settings.html | 10 | 10 | 0 | 50%×1, var(--r-2)×2 |
| tab-stock-etf.html | 2 | 2 | 0 | — |
| tab-system.html | 7 | 7 | 0 | 50%×1, 999px×1, inherit×1, var(--r-1)×1 |
| trading.html | 5 | 5 | 0 | 50%×2, 999px×1 |
| **合計** | **180** | **180** | **0** | |

**0 convertible（未觸碰）**：oc-utilities.css、tab-monitoring.html、tab-risk.html、
tab-strategy.html、tokens.css（皆只含 `var(--r-*)`/`var(--card-radius)`/50%，無 literal）。
**excluded**：login.html（兄弟 auth 髒改動，未觸碰）。

> `var(--card-radius)` 是 tokens-compat 舊名 token（非 literal），依 §1.2 屬 C3/C4 遷移範疇，
> 本 C2 批不動（規則「已是 var(--...) 不動」）。

## 驗證（§10 DoD）

1. **半徑收斂**：AFTER 全站 border-radius value 內 3-18px literal = **0**（value-isolated grep，
   除 login.html）。180/180 轉換，per-file converted == per-file before，逐檔一致。
2. **表外值**：轉換器 FLAGGED = 空（無 11/13/15/16/20px 等表外值）。
3. **保留值未動**：2px×1(seal)、999px×15、50%×10、inherit×1，前後計數一致；0/0px 零角保留
   （多值如 `0 0 var(--r-3) var(--r-3)` 逐一驗）。
4. **node --check**：common.js / handoff_helper.js / app-paper.js / common-modals.js / risk-tab.js
   全 OK。HTML inline `<script>` **未觸碰**（所有 HTML 改動皆在 `<style>` 內 CSS，無抽取檔）。
5. **guard 續綠**：`pytest tests/structure/test_gui_numeric_formatter_contract_static.py
   tests/structure/test_gui_utilities_spec_drift_static.py -q` → **3 passed**。
6. **交易檔零非-半徑改動自查**：tab-live.html / tab-demo.html / tab-governance.html / risk-tab.js /
   app-paper.js / tab-paper.html 的 `git diff -U0` 每條 ±行皆含 `border-radius`，無 color/background/
   `--live`/邏輯/onclick/endpoint 改動（canon 6 熱紅 verbatim）。全站 diff 亦零非-半徑行。
7. **static/ clean**：無 untracked/stray 檔（轉換器在 scratchpad）。login.html 僅前存 `M`（未由本批動）。

## 三原則檢查
- 交易環境優先：✅ 交易檔零非-半徑改動，canon 6 熱紅/`--live` 一字不動。
- 可審計：✅ 逐檔前後計數 + git diff 全綠自查。
- 簡單優於華麗：✅ 純 token 化，無新增屬性/邏輯。

## 視覺
半徑收斂微變（6→5 / 10→8 / 14→12 / 18→12 等）為 canon 5 意圖（數據控件永不 pill）。
token 化後半徑與主題無關 → 雙主題半徑一致。零新增裸 hex。交易頁 E2 硬邊界親算 + 雙主題目視待接。

## 異常
無。
