# E1a — P0.3 B6/E live 數字契約 (gated · REAL FUNDS 顯示精度變更) · 2026-07-10

STATUS: DONE — skin-only, hard boundary IDENTICAL, DoD greps 清零, node --check + guard + spec-drift 綠。

範圍：`tab-live.js`(1882→1908 行,<2000 硬頂,+26 淨)、`tab-live.html`(583 行)。
規格正本 `design/06_numerics.md`;承 B0(1f85b382d)/B3(f066da5b1 metricValue 先例)/B5-D(d766df666 per-fill 4dp/ocSide 先例)。
未動 `common-formatters.js`/`oc-utilities.css`/任何 Python/Rust。

## 硬邊界自查表 (HEAD ↔ working IDENTICAL)
| token | js H/W | html H/W |
|---|---|---|
| onclick | 11/11 | 21/21 |
| ocPost( | 6/6 | 0/0 |
| ocApi( | 11/11 | 0/0 |
| openTypedConfirmModal | 6/6 | 1/1 |
| openConfirmModal | 2/2 | 0/0 |
| classifyLiveMutation | 7/7 | 0/0 |
| doEmergencyStop | 1/1 | 1/1 |
| doLiveStop | 1/1 | 1/1 |
| doLiveCloseAll | 3/3 | 1/1 |
| closeLivePosition | 12/12 | 0/0 |
| liveStart | 1/1 | 2/2 |
| ocResidualRiskBanner | 4/4 | 0/0 |
| phrase: | 4/4 | 0/0 |
| /api/v1/live/ | 22/22 | 1/1 |
| authorization.json | 2/2 | 0/0 |
| execution_authority | 8/8 | 0/0 |
| live_reserved | 1/1 | 0/0 |

- **typed-confirm phrase + 全 modal 文案 (title/body/impact/rollback/confirmLabel/actor) BYTE-IDENTICAL** (diff 去行號後空)。
- diff hunk headers 確認：零 action-handler 改動 (liveStart/doLiveStop/doEmergencyStop/doLiveCloseAll/closeLivePosition/classifyLiveMutation 皆無 hunk)。全改動限 render/formatter 函式。
- 只改 顯示 formatter + class;fetch/授權/gate/confirm/emergency/flatten/POST payload/事件文案 一字未動。

## canon 6 熱紅逐點 (HEAD ↔ working 原樣)
- tab-live.html `rgba(239,68,68` 7 行 (btn-emergency ×3 / live-mode-mainnet / real-funds-badge / trust-bar.crit)：BYTE-IDENTICAL,全在 `<style>` 塊,未觸碰。
- tab-live.html `REAL FUNDS` (banner 文案+說明)：BYTE-IDENTICAL。
- tab-live.js `REAL FUNDS` 3 (banner innerHTML+comment) / `oc-chip-live` (`--live` active badge)：BYTE-IDENTICAL。
- 虧損紅一律走 `--neg`(ocSignParts.val-neg);`--live`/熱紅 未稀釋/未轉 --neg/未改色值。**--live 與 --neg 未互換。**

## per-fill 4dp 例外清單 (§1.5 防塌零)
| 點 | 處理 | dp |
|---|---|---|
| 主 fills 表 PnL cell (`_liveFillRow`) | `ocPnlCell(_liveFillPnl(f))` (B0 shared,未動) | 4dp |
| 主 fills 表 Fee cell | `ocNum(parseFloat(fee),6)`;sentinel→OC_EMPTY | 6dp |
| 收益 tab per-trade PnL (`_liveProfitRow`) | `ocMoney(r.pnl, 4)` + val-* | 4dp |
| closed-pnl per-trade PnL (`_liveClosedPnlRow`) | `ocMoney(closedPnlNum, 4)` + val-* | 4dp |

聚合/水位 → 2dp (下節)。node 實證：`ocMoney(0.0034,4)="+₮0.0034"`(微額不塌),`ocMoney(0.0034)="+₮0.00"`(聚合收斂,預期)。

## LIVE 顯示精度變更點 (QC + operator 必知悉)
skin-only,底層真值/計算/gate 不動;僅渲染 dp/字形/class 變。
1. **今日已實現 / 淨 PnL KPI**：4dp→**2dp** (`ocMoney(gross)`/`ocMoney(net)`,舊 `_formatSignedMoneyValue` 4dp 已刪)。
2. **未實現 PnL KPI + 每倉未實現 PnL cell**：4dp→**2dp** (`ocMoney(upnl)`/`ocMoney(pnl)`)。
3. **餘額 KPI (equity/available/wallet/margin)**：`'₮'+toFixed(2)` → `ocBalance`(2dp,**FX 轉換後**,隨幣別 toggle)。
4. **價格 cell 全面 column-fixed dp**：positions entry/mark/liq、orders price、fills price、closed-pnl entry/exit、profit open/close → `_livePriceCell`（≥1→2dp;<1→4/6/8dp 防微幣塌零）。原顯交易所 raw 字串,現規格化;node 實證 0.00002100→"0.00002100"(8dp 全精度)、0.0000004→"0.00000040"(**無假零**)。
5. **今日淨零/缺值語義**：舊 `_formatSignedMoneyValue` 把「真 0」與「無資料」都顯 '--'(混淆);今真 0→`+₮0.00`(誠實零)、缺值→`—`(OC_EMPTY)。canon 7 改善。
6. **子標費用/資金費**：4dp→2dp (`ocBalance`/`ocMoney`)。
7. **drawdown 徽章 %**：1dp→2dp (`ocPctVal`)。

## 第二通道 delta / level 判別表 (§2.2)
tab-live.js 經全檔搜索 (`grep roe|_change|change_pct|delta|yesterday|vs_|prev_day`) **無任何 period-over-period delta 欄位**。所有 PnL = 水位或 per-fill 已實現 → 與 demo(B5-D)同:**本批 0 個 ▲▼ 數值箭頭**;方向 ▲▼ 全由 `ocSide` badge 承載。
| 值 | 類別 | 處理 |
|---|---|---|
| 今日已實現/淨 PnL、未實現 PnL (KPI) | 帳戶級水位 | sign + `.val-*`,無箭頭 |
| 每倉未實現 PnL | 倉位水位 | sign + `.val-*`,無箭頭 |
| avg_net_bps (edge gate) | 帶號水位 | `ocSigned(v,ocBps)` = sign+val-*,無箭頭 (mirror B3) |
| fills/profit/closed 已實現 PnL | per-fill 已實現 | sign + `.val-*` 色,無箭頭 (mirror demo) |
| position/order/fill side | 方向 | `ocSide` badge (多 LONG ▲ / 空 SHORT ▼) — 唯一 ▲▼ 載體 |

## ocEsc 移除安全 (逐點)
| 點 | 舊 | 新 | 安全依據 |
|---|---|---|---|
| fills Fee cell | `ocEsc(feeStr)` | `feeStr`(裸) | feeStr 現全 formatter 產(ocNum 6dp / OC_EMPTY),機械數字無 HTML 特殊字元 |
| positions side | `ocEsc(p.side)` | `ocSide(p.side)` | ocSide 固定字典 badge,未知→OC_EMPTY,不回顯輸入 |
| positions size/entry/mark/liq/entryValue | `ocEsc(String(...))`/`ocEsc(ocAmount(...))` | `ocQty`/`_livePriceCell`/`ocBalance` | 值走 typed formatter,raw 不進 HTML |
| orders side/qty/price | `ocEsc(...)` | `ocSide`/`ocQty`/`_livePriceCell` | 同上 |
| fills side/qty/price/execValue | `ocEsc(...)` | `ocSide`/`ocQty`/`_livePriceCell`/`ocBalance` | 同上 |
| closed-pnl entry/exit/qty/side/PnL | `ocNum`/`ocEsc(String(...))` | `_livePriceCell`/`ocQty`/`ocSide`/`ocMoney` | 同上 |
| dust panel side/qty/mark/est/min | `ocEsc(String(...))` | `ocSide`/`ocQty`/`_livePriceCell`/`ocBalance` | 同上 |
外來字串保 ocEsc：symbol / strategy(orders strat) / orderType / status / orderId / owner_strategy(`_ocRenderOwnerStrategy`) / drift note / closeTime。**XSS 面消除非削弱**(raw 值不再進 HTML)。

## fraction / percent 分類 (6 pct)
| 點 | 語義 | 處理 |
|---|---|---|
| dust gap % (`(min-est)/min`) | fraction | `ocPct(fraction)` 內部 ×100 → 2dp |
| available-sub (`avail/equity`) | fraction | `ocPct(avail/equity)` → "X.XX% of equity free" |
| drawdown 徽章 (halt/warn) | already-% | `ocPctVal(drawdownPct)` (1dp→2dp) |
| edge fee_drop_pct / maker_like_pct / win_rate_pct | already-% (producer `*100.0`) | `ocPctVal` (mirror B3 逐點親證) |
| signed-auth snapshot DD / limit (:82/84) | already-% (prose) | **保留** `toFixed(2)+'%'` — 安全-status 散文,已 2dp 合規,§3.1 散文豁免;與 `ocPctVal` 輸出 byte-identical,不觸碰授權文案 |

## _edgeMetricValue 分解 (消 bps helper #2 · mirror B3 metricValue)
- helper body：`'--'`→`OC_EMPTY`,僅服務非契約型別 (count/ratio/'x'/'m')。
- `avg_net_bps ' bps'` → gate cell `ocSigned(c.avg_net_bps, ocBps)`(sign+val-*,無箭頭);readiness `ocBps(item.value, true)`(帶號,純字串)。
- `fee_drop_pct / maker_like_pct / win_rate_pct '%'` → **already-%** → `ocPctVal`。
- `lifetime_ratio 'x'` / `live_demo_p50_min 'm'` / `entry_fills`·`rows` count / `reentry_rate` → 保留 `_edgeMetricValue`(非契約型別)。
- bps/% 已全數離開 `_edgeMetricValue` → helper #2 的 bps 角色消除。

## fee 輸出鏈修 (B0 deferred)
`:1489` `fee !== '--' ? parseFloat(fee).toFixed(6) : '--'` → `!ocIsBlank(fee) ? ocNum(parseFloat(fee),6) : OC_EMPTY`(sentinel-agnostic;fee 保 6dp column 例外)。

## 每檔度量 (DoD grep 清零 · per-file)
tab-live.js：`.toFixed(1)+'%'` = **0**;raw `' bps'`/`'bps'` 拼接 = **0**;ASCII `>= 0 ? '+' : '-'` = **0**;`_edgeMetricValue` bps/% 殘留 = **0**;`ocAmount` = **0**(→ocBalance);`_formatSignedMoneyValue` = **0**(刪)。
殘留 `toFixed` 7 處皆刻意保留散文/泛型:snapshot DD/limit(:82/84 安全 status,已 2dp)、clean_days(:163 天數 prose)、dust tooltip est/min(:532-534 hardcoded $ 提示文)、hold 分鐘(:1578 泛型 duration,'m' 非 DoD 型別)。

## `.num` 清單
- JS 表格數值 cell：**23**(positions size/entry/entryValue/mark/pnl/liq;orders qty/price;fills qty/price/execValue/fee;profit qty/pnl;closed entry/exit/qty/pnl;dust qty/mark/est/min/gap)。
- HTML KPI tile：**9**(equity/available/wallet/margin/unrealized/realized/net/position-count/order-count)。**視覺 QC 注意**:`.num`=mono+tabular+**text-align:right**,KPI hero 數由左對齊 sans → 右對齊 mono(§3.1/§3.2 意圖,demo 先例)。
- 裸屬性 0(class 全在引號內);loading/empty/N-A 容器態未掛 .num(非數值)。

## 驗證
- `node --check tab-live.js` PASS;tab-live.html inline `<script>`(僅 `ocAuthCheck();ocInjectBaseCSS();`)抽 scratchpad → node --check PASS → 即刪,static/ clean。
- guard `test_gui_numeric_formatter_contract_static.py` + spec-drift `test_gui_utilities_spec_drift_static.py`：**3 passed**(未動 common-formatters.js/oc-utilities.css → 契約鎖不變)。
- node harness(真載 common-formatters.js + FX stub)實證 _livePriceCell 跨量級無假零 + per-fill/聚合 dp + U+2212 + ocSide badge + null→OC_EMPTY。
- cache-buster bump：`tab-live.js?v=20260628.gui-fills-time` → `?v=20260711.p03-b6e-live`(deploy hygiene;E2 或 site-level 再確認)。
- 兄弟髒改動：兩檔 HEAD 皆 clean(無 sibling dirty),未觸碰他檔。未 commit。

## 異常 / 交接
- **NEEDS-DEPLOY**:cache-buster 已 bump;engine restart 非必要(純 static)。
- avg_net_bps gate cell 用 `ocSigned(...,ocBps)` 產 `<span class="num val-*">` 嵌 `.value` div——與 B3 tab-edge-gates 同制;E2 可對照兩檔一致性。
