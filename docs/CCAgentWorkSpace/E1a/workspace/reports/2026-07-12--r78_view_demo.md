# R78 · view-demo 原生遷移(Phase 2 交易關鍵 view)— E1a · 2026-07-12

STATUS: DONE

## 交付檔(2 新檔 + 3 接線點,tab-demo.html 零修改)
- `static/view-demo.js`(294 行)— thin wrapper 類比 view-settings.js,強化 discover 參 view-governance.js;
  register `OC_NATIVE_VIEWS['demo']` render/resume/pause + 具名導出。
- `static/view-demo.css`(201 行)— `.demo-view` scope 兩層(① 頁內樣式塊 port ② legacy 短名 shim)。
- 接線:`shell.html:65` link(view-risk.css 鄰近)+ `shell.html:260` script(shell.js 前)+
  `shell.js:82-89` VIEWS demo entry 加 `iframe: false` 保留 `flag: true` + 遷移註釋。
- `tab-demo.html` byte-untouched(mtime Jul 11 06:15,唯一 reuse 正本 + rollback 錨)。

## 如何滿足契約
- **IIFE 隔離**:唯一大內聯 script(html:263-1443)IIFE-wrap → 頂層 loadAll/_demo*/DEMO_* 全 IIFE-local,
  不進 global lexical,過 R71 guard 貢獻空集;不撞 paper/settings/overview/risk/governance 頂層。
- **loadAll→__ocDemoLoadAll**:re-export 生成器 `if (nm === 'loadAll') return`(view-demo.js:175)顯式排除;
  只曝 `window.__ocDemoLoadAll = loadAll`;grep 證**零 `window.loadAll =` 賦值**(僅 MODULE_NOTE 提及)。
- **12 handler re-export 零交集**:強化 discover(掃 html∪combined、抓非首 token、交集頂層函式、濾保留字)
  經真實純函式 harness 實測 → discovered=12(closeDemoDialog/closeDemoPosition/demoFillNext/demoFillPrev/
  demoSessionAction/doDemoCloseAll/openDemoCloseAllDialog/openDemoRisk/refreshDemoFillsNow/setDemoFillView/
  setDemoPnlRange + **onDemoFillsToggle**),includes onDemoFillsToggle=true、excludes loadAll=true。
  逐一 grep 對 tab-paper/app-paper/tab-settings/tab-system/tab-risk/risk-tab/tab-governance/governance-tab/
  autonomy-posture/canary-tab **全零交集**,各在 tab-demo.html 定義恰 1 次。
- **4 寫 byte-parity**:demoSessionAction(:1290)/doDemoCloseAll(:1389,前置 #dlg-demo-close-all)/
  closeDemoPosition ×2(:1419 dust、:1435 一般,各前置 openConfirmModal + classifyLiveMutation·
  ocResidualRiskBanner)全在未改 script 內經 Rust IPC;view 只 fetch+clone+rerun,零自建寫路徑。
- **canon-6 琥珀不升 live**:demo-cta-* 全平確認框標題/確認文字 → `--warn`(琥珀警示語義),tint 走 rgba;
  view-demo.css 零 `--live` 類、零熱紅。confirm gate 逐字保留不短路。
- **canon-7 三態復用**:demo-badge 三態、stale-banner、空表「暫無/無法載入」皆由 reused script 原樣渲染,零 fake。
- **CSS shim 依實測**:全域殼 CSS 僅供 `.oc-btn*`;其餘 oc-* 全來自殼刻意不呼的 ocInjectBaseCSS →
  類比 view-paper.css 補 `.demo-view` scoped shim,另補 paper 未用的成交子標籤(oc-fill-tabs/tab/table/time)+
  oc-not-configured + oc-pre;色助手 green/red/yellow。

## 驗證(全綠)
- `node --check view-demo.js` OK;`node --check shell.js` OK。
- view-demo.css:零裸十六進位色值(規則+註釋)、零 `<style>` 字面、所有頂層規則 scoped `.demo-view`
  (僅 @keyframes/@media 豁免)。
- view-demo.js:零 inline style 屬性 / 零 `.style.` 寫 / 零裸 hex。
- shell 接線 v tag `?v=20260712.p2-demo` 就位;demo entry `flag: true, iframe: false`。

## 與事實的出入(以源碼為準,誠實指出)
1. brief「11 onclick handler」→ 實測 on* handler 集 = **12**(第 12 = `ontoggle="onDemoFillsToggle(this)"`
   html:190,onDemoFillsToggle 頂層 function html:972)。穩健 discover 掃 on* 全類自動涵蓋。
2. 自初始化尾巴 brief 未列 **`ocInitFx()`**(html:1352,FX 匯率初始化);IIFE 重跑當下同步跑,byte-parity 保留無害。
   occurrencychange(html:1353)為拼錯 dead 事件,無害保留(brief 已述)。
3. demo loadAll **確認不在** onclick 集(零 `onclick="loadAll()"`)→ **不需** governance 的 clone-rebind。
4. `oc-btn-warn`(closeDemoPosition dust 確認 confirmClass)全域未定義(ocInjectBaseCSS 僅 `oc-btn-warning`),
   該按鈕由 common-modals.js 掛於 `.demo-view` 外 → **既有源碼債**(pre-existing),byte-parity 不動、不在
   scoped shim 內、退回 `.oc-btn` 基樣式,功能不受影響。

## 新發現 / 限制
- **visibility 行為守恆限制**:內聯自帶 `document.addEventListener('visibilitychange',…)`(html:1346)為 IIFE-local,
  pause 無法外清 → 瀏覽器分頁重獲焦點時它會再呼 loadAll(受 `_demoRefreshInFlight` 重入閘 +
  `_isDemoTabRefreshVisible()` 節流,且全唯讀 GET)。同 risk setInterval 先例;built guard 防 resume 重跑內聯。
  依 brief 採 pause=ocStopRefresh(未 postMessage `_demoTabVisible`),為安全側(只多不少 GET,永不誤阻)。
- 未動:tab-demo.html 及其他 view/shell 面(並發安全:僅新建 2 檔 + 3 接線點)。

## NEEDS-LINUX + operator(靜態不可 attest)
真渲染 / 4 寫真行為 / confirm 真閘 / 三態徽色 / 15s 輪詢 / 琥珀確認框可讀性且不升 live = runtime + 視覺走查。

不 commit(PM 統一提交)。
