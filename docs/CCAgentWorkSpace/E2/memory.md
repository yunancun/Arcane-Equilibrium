# E2 Memory — 工作記憶

> 本檔=長期教訓+近期記錄；超 300 行由 R4 巡檢標記、PM 派工壓實，舊條目原文遷 memory-archive.md（append-only）；agent 完成序列照常追加於檔尾。

## 長期教訓

1. 對抗驗證三層缺一不可：跑綠（必要不充分）→ mutation 注入「最像 E1 會犯的錯」證每條 load-bearing claim 有 test-bite → 真資料/真 artifact 撈 bug-signature 計數證非 vacuous；mutant 改 /tmp copy 或改後還原，結束 `git status --porcelain` 驗空。
2. 永不採信自報：E1/MIT 的測試數、coverage、「byte-identical」「我都 re-export 了」全部親跑/親 grep 重驗；re-review 抽驗優先找算術 invariant（test 總數守恆、`git diff | grep -cE '^\+\s*(async )?def test_'` 計數）；per-file 拆分宣稱要抽查。
3. behavior-preserving 拆分/搬移黃金證法：AST 逐 symbol body byte-compare（比肉眼讀長 diff 可靠）+ 全套 test count 不變 + caller namespace/re-export 同級；「某檔 0 改動」宣稱用 `git diff -- <path>` 空輸出一條命令證實。
4. 拆分/DI/rename/tuple 擴張後必 grep 全 repo 窮舉 caller（含跨模塊 importer、test mock return_value、multi-value unpack）；symbol importable ≠ 簽名兼容；helper 回 N 個值要反問「沒串接的是 by design 還是漏」。
5. 註釋/spec/docstring 每句當 claim 對源碼逐路徑證偽——comment-accuracy fix 自己最易寫進新錯；「宣稱的保證」（per-cap 上限、fail-loud panic、依賴聲明）要追 data-flow 上游實證可達，「mirror 既有 pattern」可能 mirror 到不可達性；spec 內部矛盾應 escalate 仲裁而非自行 invent。
6. silent-fail 同族一律標 issue：Python `except: pass`、SQL `EXCEPTION WHEN OTHERS`、bash `2>/dev/null`/`|| echo 0`、cron wrapper「non-fatal」echo、healthcheck 預設 PASS；cron review 必先 grep non-fatal/set -e/env export（cron cwd=$HOME，env 必顯式 export 不靠繼承）。
7. 「代碼存在 ≠ 接線可達」：新 check/producer/handler 必驗接進真會 fire 的中央 runner/conductor（`git grep <module> -- '**/runner.py'` 0 hit = silent-dead）；dormant/deferred code 的 latent bug 仍要退，dormant≠免審。
8. 監測儀器自身要驗有效：shadow comparator「0 divergence/0 mismatch」可能是 gate-placement artifact 非一致性證據；必畫「能 fire 的分歧子空間」+ 雙向 mutation（短路 record→測試紅、echo 全 match→測試紅）證 instrument 真活；fail-closed 正確 ≠ soak 證據有效。
9. verdict 命脈與 fail 方向必查：先查 verdict emit 在哪個函數、被哪個 test 直接斷言（CLI-only override 是假安心）；手刻統計必測退化輸入（constant/zero-dispersion），fail-open（false-GO）比 false-NOGO 危險；小樣本終局 kill verdict 用 power 模擬破「低 power 假陰性當鐵證」。
10. 測試 bite 隱形殺手：同向 soft scorer 掩蓋 gate 測試（必中性化隔離使 gate 成唯一決定因素）、unit 注入參數遮上游解析錯（provenance/版本欄需 caller→registry→落庫 e2e 斷言）、test 手放被測欄位遮 absence-regression、邊界 test 對 spec 核非對 code 核。
11. collateral 抓漏靠全套 sweep 不靠點名：受影響檔案清單由 grep 推導必漏 test fixture 隱性依賴；base-vs-HEAD 全套失敗清單 sorted diff 是決定性手法（既有紅會淹沒肉眼）。
12. migration/PG 審查：V### 必獨立 Linux sandbox 親跑 double-apply + 經驗 schema 反射 + 對抗 INSERT 探針；Mac mock-PG 抓不到 NOT NULL/grant/jsonb 數值正規化（此類必標 Linux-owed 不默認 PASS）；`CREATE OR REPLACE VIEW` 只能 APPEND 不能 DROP column；跨表同義異名欄（actor_id vs created_by）逐表 grep 不信 mental model。
13. §5 multi-session race 紀律：review 前 fetch 比 `origin/main...HEAD`；髒樹他人改動/外來 stash 一律不碰不 revert；review 期間 sibling push 用檔案集 comm/diff 證 0 overlap，照 SOP 記錄不忽略；meta-doc narrow-stage `git commit --only`。
14. 角色與報告慣例：E2 發現 issue 退回 E1 不代寫；re-review/second-pass 限 fix delta 不 scope creep；report 默認 inline 回 parent 不寫檔，長報告才放 workspace/reports/；over-redaction（過遮毀用途）與漏遮同為 finding。
15. 審查強制清單（原文已歸檔，要點不變）：中文優先註釋/MODULE_NOTE、innerHTML 必 ocEsc()、SQL 參數化、`except HTTPException: raise` 穿透、governance route 必 `_require_operator_role()`、`submit_order()` 前 `acquire_lease()` fail-closed、治理不可被 env-var 禁用、測試數 ≥ 任務前基準、§九 800 行警告/2000 hard cap（exact-touch 默認補 split follow-up）。
16. 資料怪象先分視角再判 bug：aggregate（max(delisted_at)）vs latest（is_delisted_at_asof）不一致可能是「已公告未來下市」等真實資料形狀非 corruption；判 source-data 特性 vs 實作缺陷看 window-clip/statuses_seen 是否正確處理。

## 近期記錄

## 2026-07-07 — WP3.1 training registry contract emission E2 review → PASS_TO_E4
- 審 `registry_serving_contract.py` + `run_training_pipeline.py` + focused tests；builder 只讀 acceptance report / onnx_out / artifact bytes，無 DB/runtime/env/exchange/secret；contract-bound path 在 ONNX 後、DB precheck 前 build+persist+pass contract，non-contract-bound 不合成 contract。
- 親跑 py_compile PASS、focused pytest 74 passed、adjacency 106 passed/1 skipped、diff-check PASS。0 blocking finding；測試可後續補 binding schema/status、artifact written=false、manifest schema_version 異常等負向分支。`run_training_pipeline.py` 1046 行為 review-attention，非本 patch blocker。

## 2026-06-08 — L2 Advisory Mesh Phase 2（Orchestrator+registry+admission+adjudication+fail-safe+guard）E2 對抗審 → RETURN E1（1 HIGH + 2 MED + 2 LOW；全 dormant，0 P2-runtime-blocker，但需修正 spec-claim 才 PASS）
- **範圍**：branch `feature/l2-critic-lessons-tools`，P2 疊 P1 `f1c3c1ca` 未 commit。5 新模塊（l2_capability_registry/l2_prompt_contract_registry/l2_out_of_bound_guard/l2_conflict_adjudicator/l2_advisory_orchestrator，共 1275 行全 <800）+ wiring delta（layer2_engine +20/layer2_routes +95）+ TOML(48) + 60 test + singleton §2.6.2-2.6.4。全用 `venvs/mac_dev/bin/python`（3.12 tomllib）。
- **裁決 = RETURN E1**。linchpin 全綠（mutation 親證有 bite），但 **HIGH-1 per-capability daily ceiling 是 no-op**——code/comment 宣稱 PA §F.1 stage4「per-capability 硬日上限→NO_ADVICE」但實際只 re-read 全域 `remaining`，零 per-cap spend accounting。實證：cap_daily=0.01 + 全域 remaining=1.5 → 30/30 distinct-subject trigger 全 admitted，per-cap ceiling 從不 fire。**load-bearing 全域 $2/day storm 閘正確**（test_storm 綠），HIGH-1 只是被宣稱卻未交付的「額外」per-cap 細化。dormant（dispatch 0 production caller）但 spec-claim 不實必修。
- **★ 關鍵 framing（嚴控 severity）：dispatch() + report_call_outcome() 在 P2 皆 0 production caller**（grep `app/` 證；`.dispatch(` 命中是 ai_service_dispatch/listener 別物件；routes 只呼 status/reload/reset 不呼 dispatch）。整個 admission loop + fail-safe SM 是 **dormant skeleton**（PA §A.2「P3 才接 executor」一致）。唯一 P2-live 路徑 = layer2_engine wiring delta（contract_ver/schema_ver 改 registry-resolved，fail-soft fallback 既有常數）。故所有 SM/admission finding 都是 **latent**（P3 wire trigger surface 才 bite），非 P2 runtime hazard。
- **MED-1 fail-safe SM ollama_available 分支序 bug**：`report_call_outcome` :388-397，`elif ollama_available: DEGRADE_OLLAMA` 在 `<5 NO_ADVICE`/`<10 TRIPPED`/`else GLOBAL_CONSERVATIVE` **之前**→ 實證：ollama 持續 available 時 SM 永卡 DEGRADE_OLLAMA，50 連敗仍到不了 TRIPPED/GLOBAL_CONSERVATIVE（unique states 只 {RETRY,DEGRADE_OLLAMA}）。PA §H「repeated failure/guard storm→TRIPPED→systemic→GLOBAL_CONSERVATIVE」escalation 在 ollama-up 場景不可達。測試只測 ollama_available=False 路徑（漏 ollama-up persistent-fail）。方向（不代寫）：escalation 須與 ollama-availability 解耦——consecutive_failures 累積到閾值該 TRIP（即便 ollama 還活），或 DEGRADE_OLLAMA 本身要計次升級；+補 ollama-up persistent-fail 測試。
- **MED-2 admission state race（dedup/debounce 無鎖）**：`dispatch`→`_admit` 改 `self._admission.last_served_ts[k]`(:322)/`debounce_pending`(:275/287) **不持 self._lock**；而 report_call_outcome/reload_registry/reset_fail_safe 都持鎖但碰的是別 state(_fail_safe/_registry_cache)。鎖保護不對稱：admission 窗口裸奔。dispatch 是 **sync 無 await**（驗 :163 無 async/await）→ 單 event-loop 內不交錯（asyncio 邊界 #7 PASS），唯多 OS thread 並發呼 dispatch 才 race（兩同 dedup_key trigger 各讀 last_served=None 各放行=破 dedup storm-control identity）。P2 0 caller 故 dormant；P3 多執行緒 trigger surface 會 bite。方向：admission 窗口 mutation 納入同一 self._lock（或獨立 admission lock）。
- **LOW-1 cold-cache 壞 TOML 讀路徑 500**：boot 後 operator 手改 TOML 成 malformed（繞過 validated /registry/reload）+ 冷快取 → 下次 `_registry_obj()`（status() 讀路徑亦呼）raise L2RegistryLoadError **uncaught** → GET /orchestrator/status 回 500 非結構化 degraded envelope。實證 status()/dispatch() 皆裸 raise。安全意義 fail-closed（不採壞 config），唯 graceful 不如 /registry/capabilities route（該 route 有 catch）。方向：_registry_obj 或 status 對 L2RegistryLoadError fail-soft 回上次good/empty+degraded flag。
- **LOW-2 adjudicate_vs_gate 未知 verdict fail-open（gate-recognition 軸）**：:89 只 reject/fail/block→a_wins，其餘（含 'warn'/''/None/未知）→b_wins。「gate reject 永勝」不變式成立（reject→gate 勝），且 b_wins=「L2 走自身下游 gate 非直接 apply」（docstring 明載），故非 safety bypass。唯未知 gate verdict 默認「放 L2 續走」是 gate-辨識軸的溫和 fail-open。as-designed 可接受，P3 真 gate verdict 接線時建議未知→fail-closed。
- **全綠（實證非讀碼）**：① LANE_DIRECTION linchpin：無 'live' key（test 驗 key+value 皆無）；STEP-1 expand→MANUAL 在函數頂（inspect 剝 docstring 驗 idx_step1<idx_tier<idx_posture）；mutation 改 STEP-1 return AUTO_VIA_GATE → **4 test 紅**（含 dispatch :239 防禦性 fail-closed 補抓 leak 的 expand）；model_construct 繞 validator 給 lane='live' → STEP-1 subscript **KeyError fail-loud**（可接受）；validated 構造 reject live(ValidationError)。② 三 reject 分支全綠（autonomy_level 宣告/can_auto_deploy_to_paper-as-posture/lane∉表 + min_tier 非法 + unknown-field extra=forbid + unknown-top-key + 重複 capability_id）；enabled 預設 false；TOML 不存在→空 registry fail-closed；解析錯→L2RegistryLoadError。③ adjudication：mutation flip PRECEDENCE(contract=0/expand=2) → 2 test 紅；adjudicate_vs_gate 9 verdict 變體（含 case/None/空）全對；stateless 純函數。④ guard：inf→clamp10、-5→clamp0、neg cost→reject、NaN→reject、empty available_axes+referenced→reject(fail-closed)、==cap 邊界 pass；無 model。⑤ **carbon-grep tokenizer-strip 真有 bite**：注入 promote_tier 為**真碼**→C1 test 紅；既有 promote_tier 在**註解/docstring**→baseline C1 綠（strip 區分碼 vs 散文，非空測）。⑥ wiring delta：reachable（run_session:671 呼 _record_l2_call_to_ledger）；非死碼；**fail-soft 親證**——強制 resolve_contract_versions raise → fallback l2_contract.v1/l2_schema.v1 且 **D3 row 仍寫出**（零回歸）；lazy import 破 cycle（engine 端 lazy，registry 端 module-level import 常數）。⑦ 2 write route（/registry/reload、/orchestrator/fail-safe/reset）首句 require_scope_and_operator("ai_budget:write")（同 /cost/reset 範式，raise fail-closed）；reload 先驗載入成功才換 cache（壞 config 不換，HTTPException 400）；2 GET 唯讀。⑧ C1/C2/order-lease grep 跨**全 advisory-loop 模塊集**：每 hit 全在 comment/docstring/MODULE_NOTE 或 _FORBIDDEN_POSTURE_GATE_TOKEN 字面（C2 reject-target 非 flag-branch）。
- **標準項全綠**：60 test pass（0.17s）；layer2/l2 family 358 pass+4 xfailed（wiring delta 零回歸，含 test_layer2_critic 走 engine 路徑）；py_compile 7 檔 OK；0 hardcoded path（2 hit 全註解「禁硬編」policy ref）；0 f-string log；0 bare except；0 detail=str(e)；0 私有屬性穿透；0 stray print；threading.Lock 無跨 await（dispatch sync）；5 MODULE_NOTE 全在；comment 中文優先（0 大段英文）；singleton §2.6.2-2.6.4 class/binding/getter 名對；**0 production-code scope creep**（diff 只動 L2 P2 + 已知 wiring 檔）。
- **out-of-scope 確認**：test_layer2_critic.py(+26) 0 ref P2 模塊=他 session WIP（D3 l2_reply_id context_id，brief 指明勿審）；aeg builder/test_aeg ` M`=他 session；4 replay collection error=pre-existing import-path harness。
- **§5 race 5/5 PASS**：5a/5e `git fetch` 後 origin/main=`b6f918d9`（residual PART4 track，非 L2），`git log origin/main -- <P2 3 檔>` **0 commit**（greenfield uncommitted local，0 file-scope overlap）；HEAD=`f1c3c1ca` 落後 origin/main 因 P2 疊 P1 未 rebase（PM merge 前 rebase，非 P2 blocker）；5b unstaged 全 L2 P2 scope+agent memory；5c 3 stash 全 pre-existing/別 session（標「not mine」）未動；5d N/A（未 commit）；review 中無新 sibling push 進 P2 scope。mutation 全 cp backup + 還原（非 git destructive），每次還原 git diff --stat empty 親驗。
- **教訓**：(1) **「宣稱的保證」要實證它真交付**——HIGH-1 comment 寫「per-capability 硬日上限」但 code 只讀全域 remaining，跑 cap_daily=0.01+remaining=1.5 才抓到 30/30 全放行；不能信 comment+變數名（cap_daily 讀了但沒比 per-cap spend）。(2) **dormant skeleton 的 latent bug 仍要退**——P2 dispatch/SM 0 caller，但 PA §M row 明列 SM 為 E1 交付物且要求轉移正確；fail-safe ollama-up 卡 DEGRADE_OLLAMA 是真 SM bug，P3 wire 就 bite，不能因「現在沒跑」放行（對抗審查 = 找 root cause 非症狀，dormant≠免審）。(3) **鎖保護不對稱是隱性 race**——3 處 with self._lock 給人「有併發保護」錯覺，但 admission 窗口（storm-control 命脈）裸奔；要逐一對照「哪些 mutable state 被哪把鎖蓋」，sync-no-await 只擋 event-loop 內交錯非多執行緒。(4) **fail-soft 要親跑**——強制 resolver raise 證 D3 row 仍寫得出 + fallback 值對，比讀 try/except 可靠。(5) carbon-grep 這種「剝註解驗真碼」測試必親自 mutate（注入真碼 vs 註解兩路）證 strip 有 bite，否則可能是空測。

## 2026-06-09 — E2 residual PART 4 Gap A market-basket selection fix `2fca92fe` — PASS to E4

審 `feature/residual-activation` 上 `2fca92fe`（parent `67730b7b`）：`_load_multi_factor_inputs` basket 選取由字母序改流動性排序。**verdict PASS-to-E4（0 finding）**。

- **★ survivorship/PIT 全綠（key risk）**：候選域**仍是** `pit_active_symbols(lifecycles, s_epoch, e_epoch)`（line 378，與舊碼 byte-identical），新 `load_liquid_basket_symbols` 只用 `symbol = ANY(active)` 在該 PIT 集**內**按 `count(*)` 排序+過濾無資料者，**從不擴/縮候選域**。關鍵推理：`pit_active_symbols` 成員條件 `delisted_at > exit_ts`（=活過整窗）→ 任何 PIT-active symbol 必在 `[since,oos_start]` 有 bar → count 查詢必留它；mid-window 下市者本就被 `pit_active_symbols` 排除（舊新碼皆不選）。故**修復不可能引入新 survivorship bias**——只重排既有集。窗 = `start=cfg.since`/`end=cfg.oos_start`（line 349-351），與 `pit_active_symbols` 及 `load_klines_by_symbols` 三者**同一 PIT 窗**，非「recent」。
- **real-seam test 真關盲點 + mutation 親跑有 bite**：`test_load_multi_factor_inputs_selects_data_bearing_not_alphabetical`（:1117）**不** patch `load_liquid_basket_symbols`（:1148 明示），走真選取 seam，lifecycles 讓字母序前綴 symbol 全 PIT-active（舊碼必選）。**親手 mutation**：把 orchestrator 還原成 `sorted(set(active))[:N]` → 該 test **紅**（`AssertionError: Extra items: 0GUSDT/1000000BABYDOGEUSDT/1000000CHEEMSUSDT`，精準重現真 bug）；`git checkout` 還原後 4 新 test 全綠。E1 claim 誠實。
- **query 安全（read-only/參數化/no-crash）全綠**：`_LIQUID_BASKET_QUERY` 靜態常數 + `%(...)s` bind；唯一 `execute(` 傳 dict params（無 f-string/format 拼 SQL）；新碼 0 INSERT/UPDATE/DELETE/commit；`if not syms: return []` 在開 cursor 前→空候選 0 查詢（test 驗 `conn.cursors==[]`）；`RealDictCursor` lazy import 同模塊既有 8 處精確範式。
- **behavior-neutral 全綠**：唯一 caller = flag-gated orchestrator :475；diff 4 處 `min_basket_symbols` **全是註解**（下游 guard 0 邏輯改）；`_patch_db` stub 簽名與真 fn **完全一致**（kwarg-only `start_ts/end_ts/timeframe/limit`）→ 26 既有 test 為對的理由攔截非簽名漂移；`_process_candidate` 在 `oos_start/data_end is None` 時 :432-433 fail-closed `_skip` **早於** :475，故 `end_dt else start_dt` 退化窗在生產不可達（防禦性）；新 basket-window 表達式與相鄰 `load_klines_by_symbols` byte-identical（選到的 symbol 必同窗載到 bar，無 window mismatch）。
- **§3 OpenClaw 全綠**：0 hardcoded `/home/ncyu`/`/Users`；comment 中文優先（英文僅 `market.klines`/`pit_active_symbols`/symbol 名等識別符）；註解解釋 why（survivorship rationale + Gap A 根因）；`residual_alpha_producer_db.py` 943→1012（+69，未到 800 review-attention 增量門檻外、距 2000 hard cap 遠）。
- **全綠（實證）**：full `ml_training+learning_engine` **859 passed/31 skipped**（baseline 855/31，+4 新 test，0 regression）；mutation probe 親跑紅+還原綠。
- **§5 race 5/5 PASS**：5a/5e `git fetch` 後 origin/main=`b6f918d9`（residual PART4 track 的**前身** lineage `7b5d92e9`=Gap A orchestrator 已 rebase 上 main，與本 fix 同一 workstream 非競爭 sibling；本 fix `2fca92fe` 尚未上任何 remote）；overlap 屬同 track 預期非 5e RETURN trigger；5b unstaged 僅 meta（TODO v120→v121 / E2 memory 本人 / PA report `??`）**無 impl leak**；5c 3 舊 stash 未動；5d N/A（未 commit）。
- **教訓**：(1) 「選取/排序 fix」的 survivorship 審查 = 證**候選域未變**（domain 同一函數同一窗）比逐行讀排序邏輯更關鍵——bias 只能從 domain 擴縮來，重排既有集不引入新 bias。(2) 「修 synthetic-test 漏掉的 DB-seam bug」的 regression test 必須**不 patch 被修的 seam**（否則就是當初漏 bug 的同一盲點）；mutation = 還原舊實作看真 test 紅，是唯一可信證明。(3) stub 簽名 vs 真 fn 簽名要逐字對——kwarg-only 不一致會讓既有 test 為錯的理由 pass（本次一致，PASS）。

## 2026-06-09 L2 P2 fix round-2 delta re-review → PASS to E4
narrow re-review（上輪 RETURN 1 MED+1 LOW，E1 修 + 折入 /cost/reset+/cost/pricing operator-scope）。branch feature/l2-critic-lessons-tools，P2 未 commit 疊 P1 f1c3c1ca。改 4 檔：l2_advisory_orchestrator.py(untracked 新檔)、layer2_routes.py(M)、test_l2_p2_orchestrator.py(+14)、test_layer2.py(2 collateral)。**verdict PASS，0 BLOCKER/HIGH/MED/LOW**。

關鍵判定（實證非讀碼）：
- **MED-1 prune 純回收無語意變**：`_prune_stale_spend(today)` list-comp 收 `k[1]!=today` 後 del；wired 進 record_capability_spend lock 內 **prune-then-accumulate-today**。今日 key（k[1]==day）不被 prune→accumulate 仍對；`_cap_spend_today` 只讀今日 day_key→prune 只刪非今日→今日恆存。**per-cap 獨立性未退化**：同日 20 cap 共存（prune 只跨日 fire），test_spend_dict_bounded_multi_cap_single_day 證 20 key 全今日。跨日刪昨日桶=純回收（閘永不再讀昨日，daily-reset 語義本就存在於「新 day_key 無紀錄」）。
- **★ mutation 真 bite（親跑非信 docstring）**：把 prune 改 no-op → TestPerCapSpendBounded **3 紅**（across_many_days 10≠1 / prune_drops_only_stale / mutation_bite_without_prune）；2 同日 test 仍綠（prune 對同日本就 no-op）=正確。revert 已驗 clean 無殘留。
- **★ collateral test = 正當更新非弱化遮測（關鍵，親跑 auth 證）**：test_layer2.py 的 test_update_pricing_empty/valid 原用 **bare MagicMock()** actor（pre-gate update_pricing 只有 Depends(current_actor)無 scope 閘故穿透）；fold-in 後 update_pricing 首句 require_scope_and_operator。**親跑證 bare MagicMock → 403 operator_role_required**（MagicMock.__contains__ default False → 'operator' not in mock.roles → True → 403）。即舊 test gate 落地後**必 403**→collateral 更新**必要**。改後加 roles={"operator"}/scopes={"ai_budget:write"} 真 set（非 MagicMock auto-viv）→親跑證**genuinely 通過 gate**（非繞過）。仍驗原意圖（empty→no-updates / valid→success）只補 auth。grep reset_today_costs/update_pricing tests/ 確認**無遺漏 caller**：645/328 是 tracker.update_pricing(method 非 route 無閘)；1139/1153 已改；test_l2_p2 的 1125-1175 是新 +14 affirmative auth test。
- **/cost/reset 三態 auth 正確**：親跑 require_scope_and_operator：viewer→403 operator_role_required / operator+scope→PASS / operator-missing-scope→403 forbidden_scope。gate 在 state 變更**前**（reset :373 gate < :375 mutation；pricing :413 gate < :430 mutation；empty-update early-return 在 gate 後）。鏡像 :731/:757 同 first-statement 範式。test_handlers_source_calls_scope_gate_before_mutation 用 inspect.getsource 斷言 idx_gate<idx_tracker=結構序 bite。:675-680 訂正註釋誠實（明寫「先前誤稱已 scope 化」）與實況一致。
- **docstring LOW-1 準確**：cold-cache→空 fail-closed（無 cap=無 advisory=baseline） vs warm→last-good 已驗證 config（subtraction-only 保守降級非放未驗壞 cap）—與碼 _registry_obj :205-223 一致。
- **無回歸（親跑 mac_dev py3.12）**：test_l2_p2_orchestrator 88 passed / test_layer2 94 / 7 L2 檔 386 passed+4 xfailed（critic linchpin 綠）。full-suite 66 failed/4492 passed=**pre-existing PG/engine.sock IPC**（grep 證 0 FAILED 在 l2_advisory/layer2_routes/test_l2_p2/test_layer2 scope；4 collection-error 是 replay ModuleNotFoundError program_code，不 import 本 2 模塊）。
- OpenClaw 特殊：cross-platform grep 命中只在 leak-prevention test（斷言 /home /Users 不洩，§3.1 政策反例不在限）；無 except:pass；logger 用 %s 無 f-string；新註釋中文優先；檔 582/760 行 < 800。
- race check：5a HEAD 18ahead/32behind(已知 P2 疊 P1)，sibling push 全 residual PART4/phantom-fill = file scope disjoint(helper_scripts/research + rust，非 L2 app)；5b/5d 我 working tree 僅 task-scope 2 檔；5c stash 3 個 pre-existing 不碰；mutation-probe 已 revert clean。

教訓：collateral test「弱化 vs 正當」判定硬方法=**親跑 gate on 舊 actor 看是否 403**。bare MagicMock 在 require_operator_role 下 403（__contains__ default False）→舊 test 必破→更新必要且正當。對照 5/31 memory line 4150 ai_budget caller-guard 判斷亦同模式（grep caller + 親驗臨界區）。

## 2026-06-09 · L2 P3a ml_advisory cascade 對抗審查 → RETURN E1（1 CRITICAL sink-DB）

審查面：`l2_ml_advisory_executor.py`（新698）+ orchestrator dispatch_and_execute 活化 + guard ml_advisory.guard.v1 + 2 contract + TOML 2 stanza。branch `feature/l2-critic-lessons-tools` P3a 未 commit。

**verdict = RETURN E1（1 CRITICAL）**。其餘 7 軸全綠（cost/cascade/dispatch/coarse-subject/sink-0exec/no-regression/comment）。

- **★ CRITICAL（decision #1 sink）**：`write_ml_advisory_advisory_sink` 直接 `INSERT INTO learning.mlde_shadow_recommendations` **省略 `evidence_source_tier`**。V040 已把該欄 `SET NOT NULL`（無 column DEFAULT；`DEFAULT 'real_outcome'` 只是 V036/V055 **SQL 函數參數**預設，非欄位預設）→ 真 Linux DB INSERT **必 NOT NULL violation = sink 永遠寫不進**。三重問題：(a) V040 NOT NULL；(b) V051 paired CHECK lineage（real_outcome→replay_experiment_id+manifest_hash 皆 NULL，否則 FK 必填）→ 唯一可用值 'real_outcome' 對「斷言無 alpha 診斷」語意錯；(c) V037 REVOKE PUBLIC INSERT，唯一合規寫路是 `verify_replay_evidence_and_insert()`（V036/V055），全庫**唯一**直接 INSERT 就是 E1 新碼 line 430（grep 證），canonical producer mlde_shadow_advisor.py:470 走 SQL 函數。**為何 44 綠測抓不到**：test `_CapturingConn.execute` 只 append SQL+params list，**不打真 PG**=不驗 NOT NULL/CHECK/grant → 經典「mock 遮真 DB fail」。建議轉 operator：方向 A 換 sink 走既有 `verify_replay_evidence_and_insert`（過 V037 grant，但函數帶 replay-evidence 語意 mismatch）｜B 開 V137 新 advisory 表（乾淨但 migration+Linux dry-run）｜C 既有表加 `evidence_source_tier='real_outcome'`+確認 db_pool role 屬 replay_writer_role（最小改但語意臟）。E2 傾向 B 或 A，operator 拍。

- **decision #2 cost = CONFIRMED 正確**：`_record_call_cost`→`engine._cost_tracker.record_claude_cost(tmp_session,...)`→delegate `layer2_cost_recording.record_claude_cost`→`_add_daily_claude_cost` 更新 `daily_spend.<day>.total_usd`（line 194）。admission stage-4 `_check_budget`→`_get_cost_tracker().check_daily_budget()`→讀**同一** total_usd。instance 同一：layer2_routes:101 engine 用 `cost_tracker=_get_cost_tracker()` singleton。**ml_advisory cloud 花費真進 $2/day counter，無 bypass**。tmp Layer2Session 是既有範式（engine 自身 triage_session line440 同樣）；daily rollup 是 tracker-level persist 非 session-scoped → cost 非永 0。local: tier→cost 0 但仍記（Ollama 免費正確）。

- **dispatch 活化 = 真非死碼**：dispatch_and_execute 先 sync dispatch（admission/RLock 不變）再對 `admitted+routed_to==neutral_sink+capability_id.startswith("ml_advisory")` 才接 cascade；disabled/deduped/tier_locked/非-ml_advisory/engine-None 全短路（test 8 條驗 eng.calls==[]）。executor_ok 把 screen_rejected/guard_rejected/unknown_mode 算 healthy（確定性閘正確擋下≠故障），只 cloud_unavailable/sink_fail 推 fail-safe = 對。

- engine interface 全對：`_provider_complete`(provider_name/tier/system_prompt/messages/tools/max_tokens/timeout) + `_resolve_effective_provider`(base_provider/base_tier/role) + PROVIDER_ANTHROPIC/TIER_HAIKU/TIER_SONNET + L2Response.input_tokens/output_tokens/text 全存在 → cascade 對真 engine 結構通（非 mock-only）。
- coarse_subject DoS（E3 域，碼正確）：_evict_admission_windows 在臨界區 top 持鎖呼，只清 >TTL(24h)≫dedup窗(≤900s)→不破 dedup 語意（test_dedup_still_works 證）。_derive_coarse_subject 折白名單+upper 降基數。
- M3 typing guard（clause B）+ regime_caveat（clause C）+ axes-subset（clause D）+ per-mode 必填（clause A）確定性無 model；run_guard API 對；_guard_ml_advisory_v1 呼 guard_output（非 run_guard）無遞迴。
- TOML 2 stanza 全 enabled=false，hypothesize（P3b）正確不含；P2 test 從 empty-skeleton→2-stanza 是**正當**更新（enabled_capabilities()==[] 仍成立 + 加 hypothesize-not-in 強斷言），非遮測。
- no-regression：P3a 44 + L2 全樹 430 passed（親跑驗）；replay 4 collection error 是 pre-existing 無關。
- 教訓：**advisory sink 落 schema CHECK 重表（V031→V038/V040/V051/V055 疊 5 層 retrofit）時，必查「V031 後加的 NOT NULL/CHECK/REVOKE 欄」+「唯一合規寫路是否 SQL 函數」**。code-review grep `INSERT INTO <table>` 比讀 V031 base schema 更快定位「全庫唯一直接 INSERT = 繞合規路」。Mac mock-PG 永遠抓不到 NOT NULL/grant → 此類 finding 必標「需 Linux PG dry-run 證」。

## 2026-06-09 — L2 P3a sink delta re-review + LOW-2 adjudication（narrow re-review；RETURN to E1）

承上輪（2026-06-08 CRITICAL #1 sink）。E1 已把 sink 從 `mlde_shadow_recommendations` 改寫成 `agent.lessons`（operator 拍板 = 上輪 decision-B「開乾淨 advisory 表」的等價：複用既有 inert lessons store，免 V137 migration）。本輪只審 sink delta + 評 E1 flag 的 LOW-2。**測試用 `venvs/mac_dev/bin/python`（3.12）**。

### sink delta = CONFIRMED 正確 + inert 真閉（S-2 結構性成立）
- **schema 對 V133**：`sql/migrations/V133__agent_lessons.sql` 8 欄 INSERT 全對齊（symbol/lesson_type/content NOT NULL 由 placeholder+mode+redacted-content 滿足；context_id=l2_reply_id；outcome_net_bps/session_cost_usd 內聯 NULL literal；source 覆寫 default 'l2_session' 為 'ml_advisory'）。param tuple index 與 INSERT 順位**逐位對**（test params[0..5] 全核）。type 對（TEXT←str / REAL←NULL）。
- **content 過 redactor = 真**：line 441 `content = _redactor.redact(content).text[:4000]`。**親跑 mutation-kill**：strip 掉 redactor → `test_sink_content_passes_through_redactor` FAIL（`KeyError: 'called_with'` spy 未被呼）→ 還原 → 綠。**bite 確認有牙**。
- **inert 安全閉合（獨立驗，非採信註解）**：grep 全庫 `agent.lessons` 的 prod consumer 只有 `layer2_critic.retrieve_lessons`（line 329/366 SELECT，唯讀 trigram+recency 回 rows 進 LLM 推理）+ critic persist（line 486 寫）。**對比驗 applier**：`program_code/ml_training/mlde_demo_applier.py` line 649 `FROM learning.mlde_shadow_recommendations` + line 451 `recommendation_type != "regret_summary"` + line 353/445 `_derived_strategy_targets`/`_bounded_risk_patch`→IPC mutate RiskConfig；該檔 **0 個 agent.lessons 引用**（grep EXIT=2）。⇒ P3a sink 寫 agent.lessons ⇒ **0 applier 撿 ⇒ 0 新執行權結構性成立**（非旗標約束）。上輪 CRITICAL（寫 mlde_shadow_recommendations 會被 applier line 649 撿去改配置）**因此被正確閉合**。
- **0 mlde_shadow_recommendations INSERT 殘留**：executor+test 的命中全在「解釋為何棄用」的註解 + test 反向斷言（`assert "INSERT INTO learning.mlde_shadow_recommendations" not in raw`）。0 真 INSERT。
- scope 整潔：跨平台 0 硬編（line 148 命中是「禁硬編」註解）；0 新 mutable singleton；0 except:pass / detail=str(e) / f-string log。cost/dispatch/cascade/M3/M4/coarse_subject guard 碼**未擾動**（上輪已 PASS，本輪確認 sink-only delta）。

### ★ LOW-2 = CONFIRMED 真 D3 provenance bug（退 E1；修在 orchestrator 非 sink）
E1 flag 對。`l2_advisory_orchestrator.py:429-432` `dispatch_and_execute` 傳 `resolve_contract_versions(contract_ref=None, schema_ref=None)` → 這個 contract_ver/schema_ver 是**寫進每條 D3 ledger row**（traverse run_ml_advisory_cascade → `_ledger` → record_l2_call）。但 `contract_ref=None` 使 `resolve_contract_versions`（registry line 271）跳過 branch1（顯式 ref）、跳過 branch2（`_CAPABILITY_SEED_CONTRACT` line 256 **只含 manual cap**，ml_advisory 不在）→ 落 **branch3 fallback** = `L2_PROMPT_CONTRACT_VER='l2_contract.v1'` / `L2_OUTPUT_SCHEMA_VER='l2_schema.v1'`（通用引擎常數）。
**親跑兩 branch 實測**（mac_dev py）：
- diagnose_leak：D3 記 `l2_contract.v1`/`l2_schema.v1`；**executor 實際用** `ml_advisory_diagnose.v1`/`ml_advisory_schema.v1`（registry line 173/137）→ **match=False/False**
- interpret_result：D3 記 `l2_contract.v1`/`l2_schema.v1`；實際 `ml_advisory_interpret.v1`/`ml_advisory_schema.v1`（line 212）→ **match=False/False**
⇒ D3 ledger 的 contract_ver/schema_ver **記成通用 fallback，與 cascade 實際送 cloud 的 per-mode 契約模板不符** → 違 root principle 8（reconstructable）+ D3 provenance 準確性。contract drift 時 ledger 會指向錯模板。**非無害**。修法：`dispatch_and_execute` 改傳 cap 的 ref（重取 cap 或把 dispatch() 已解析的 contract_ver/schema_ver thread 下來，line 354-360 已算對值只是沒用）。
**為何測試漏**：P3a test 全部**直接把 contract_ver 當參數注入 run_ml_advisory_cascade**（line 278 傳 `ml_advisory_diagnose.v1` 並 line 284 斷言 ledger 收到）→ 驗的是「executor 忠實 thread 收到的值」（對），**0 test 跑 dispatch_and_execute 對真 registry 的解析**→ divergence 漏網。RETURN 時建議 E1 補一條 dispatch_and_execute→真 registry→D3 contract_ver 斷言（鎖 regression）。

### no-regression（環境噪音已隔離）
- L2 全綠：P3a 48 + P2 orchestrator + critic = **172 passed**（親跑）。redactor bite 等 16 sink/inert/alpha test 全 run（非 skip）。
- 全樹 `tests/` 67 failed/4539 passed：**0 在 L2**。失敗 5 檔（api_contract/learning_chapter/product_family_business_settings/runtime_snapshot_bridge/snapshot_stable_entrypoint）**0 import P3a/L2 碼**（grep EXIT=1）；failure = `assert 403==200` + `engine.sock 未連` + `PG port 15432 Connection refused` = Mac sandbox 無 engine/DB 的 pre-existing 環境失敗（CLAUDE §六：真 runtime 在 Linux）。replay/ 4 collection error = `No module named 'program_code'` import-path artifact，pre-existing 無關。
- 教訓：**「executor 單元測試把 X 當參數注入並斷言被 thread」≠「X 的上游解析正確」**。本次 contract_ver 在 executor 層測得完美（注入 ml_advisory_diagnose.v1）卻遮蔽 orchestrator 傳 l2_contract.v1 的真相。provenance/版本欄這類「值由 caller 算、callee 只搬運」的欄位，**必須有一條 caller→真 registry→落庫值的端到端斷言**，否則 unit 注入會說謊。

## 2026-06-09 — L2 P3a contract_ver fix narrow re-review → PASS to E4（D3 bug 真閉、mutation-bite 驗證）

上輪 RETURN 的 LOW-2（真 D3 provenance bug：`dispatch_and_execute` 傳 `resolve_contract_versions(contract_ref=None)` → branch-3 generic fallback `l2_contract.v1`，D3 記錯 per-mode 契約）E1 已修。narrow re-review **只審此 fix**。

### ★ 兩 branch 收斂實測（PASS）
- 修法 `orchestrator:435-439`：`cap = self._registry_obj().get(capability_id)` 後傳 `cap.prompt_contract_ref`/`cap.output_schema_ref`。`_registry_obj().get()` 與 `dispatch():311-312` 同路徑同物件 → **同 registry 同 ref 同版本，解析等價**（非另開 registry）。
- **per-mode 機制澄清**：per-mode contract_ver 不是「一個 cap 內 mode 分支」，而是**兩個獨立 capability**——`ml_advisory.diagnose_leak` cap.prompt_contract_ref=`ml_advisory.diagnose_leak.v1`→`ml_advisory_diagnose.v1`；`ml_advisory.interpret_result` cap→`ml_advisory_interpret.v1`（TOML 兩 stanza 各一 ref）。`capability_id`→cap.ref→registry→contract_ver 鏈成立。
- executor 用 `mode`（`_MODE_CONTRACT_REF[mode]`:318）選**實際送 cloud 的 prompt template**；D3 contract_ver 用 orchestrator 傳入值（cascade 不 re-resolve，:603/627/729 忠實 thread）。consistent-pair（diagnose cap↔diagnose mode）下 D3 == 實際 prompt 契約 → **bug 真閉**。
- 實測 D3-recorded：diagnose→`ml_advisory_diagnose.v1`/`ml_advisory_schema.v1`；interpret→`ml_advisory_interpret.v1`/`ml_advisory_schema.v1`（`TestD3ContractVerProvenance` 5/5 PASS，對真 registry 非 inject）。`record_l2_call` 簽名顯式收 contract_ver/schema_ver（無 silent drop）。
- cap=None fail-soft：`(cap.prompt_contract_ref or None) if cap is not None else None`→generic fallback，防 registry reload race（防禦性；註解註明 cap 必非 None 因 dispatch 已 admit）=對。

### ★ mutation-bite 親驗（revert 真紅）
- 暫 revert fix（contract_ref=None/schema_ref=None）→ `TestD3ContractVerProvenance` **5/5 紅**（`AssertionError: 'l2_contract.v1' == 'ml_advisory_diagnose.v1'`=正是 bug）；restore 後 5/5 綠。**非空驗非遮測**。
- E1 自糾的 `test_mutation_bite_orchestrator_never_resolves_via_none_for_ml_advisory` 推理**正確且有 bite**：斷言「無任何 contract_ref=None 解析」而非「存在真 ref」——因 dispatch() 內部本就用真 ref 解析一次（:354-359），「存在真 ref」對 buggy 版也成立（0 bite）；bug 是 dispatch_and_execute **額外**一次 None 解析。revert→出現 None 解析→紅（已親驗在 5 紅內）。上輪 memory:5245 預判的 subtlety 被正確處理。

### scoped（PASS）
- contract_ver threading 改動限 `orchestrator:435-439`（re-fetch+resolve）+ `:444`（cascade 傳 resolved 值）。registry diff **0 `resolve_contract_versions` 邏輯改**（純 ADD 新 ml_advisory 契約=prior P3a，非本輪）。executor/sink/cost/cascade/M3/M4/coarse_subject guard/linchpin **0 改**（coarse_subject DoS 上輪已 review PASS、memory:5222/5237 確認未擾動）。
- P2 test 1 處改（`test_default_checked_in_toml_loads_p3a_stanzas_all_disabled`）=TOML 換 P3a 2 stanza 的合理後果，斷言全 enabled=false（fail-closed 保留）=對。
- 跨平台 0 硬編；0 except:pass/detail=str(e)；0 f-string log（exception 用 `%s`+fail-soft 再 raise，advisory 失敗減 L2 能力不阻 baseline=對）。

### no-regression（PASS）
- P3a **53 passed**（48 prior + 5 新 D3）；P2+critic+P3a **177 passed**；全 L2 surface（8 檔）**439 passed/4 xfailed**（xfail=strict bare/short 殘留，prior 已知）。replay/ 4 collection error=`No module named 'program_code'` pre-existing import-path artifact，無關。
- 0 production caller of `dispatch_and_execute`（grep 唯一 hit 是自身註解）⇒ 路徑 wired-but-dormant；cap/mode consistency 由未來 conductor 建立，本 fix 不引入新 hazard。

### 教訓（承上輪）
- provenance/版本欄「值由 caller 算、callee 搬運」**必須**一條 caller→真 registry→落庫值端到端斷言；本輪 E1 補的 5 test 正是此鎖。mutation-bite 對「caller 內部已有正確解析、bug 是額外錯誤解析」場景，**斷言點要選 buggy 版會違反的不變式（無 None 解析）**，不是「存在正確值」（會被內部正確解析遮蔽=0 bite）。
- 親跑 revert→紅→restore→綠 是驗 mutation-bite 非空的硬手段，不採信「test 有寫斷言」。

**verdict：E2 PASS to E4**（0 finding 待修）。

## 2026-06-14 · E2 對抗審 Deribit DVOL/IV-surface 採集軸（artifact-only 離線研究）→ PASS to E4
4 新檔（__init__/collector/artifact/cli 1041 行 + test 711 行/46+1skip）。**verdict PASS, 0 finding 待修**。
- **全契約逐項綠（grep+mutation 親證）**：(1) net 計算**禁 maker rebate** DECISIVE BLOCKER **不觸**——全 4 檔 0 net/rebate/maker/fee/pnl（純 IV-surface 數據採集，無 net 經濟學計算入口，rebate 結構性無法進）；(2) read-only 公開 API 無 key 洩漏——只用 `/api/v2/public/get_volatility_index_data`+`/get_book_summary_by_currency`，0 api_key/secret/signature（"private 全程不碰" 僅 MODULE_NOTE policy ref）；(3) artifact-only 無 PG migration——0 psycopg/asyncpg/CREATE TABLE/INSERT；(4) 不碰執行/5-gate/Bybit——0 control_api/openclaw_engine/program_code import；(5) PIT append-only 真禁回填——`run_dir.mkdir(exist_ok=False)` raise + DVOL 窗口錨定 now_ts_ms 往回（`start=end-window`，不抓更早歷史）；(6) mirror polymarket 紀律正確（ThrottledJsonClient/redirect-refused/零過濾/原子寫 .tmp+os.replace/git provenance 逐項對齊）；(7) 中文註釋優先；(8) 只新檔（git ?? untracked，0 既有檔擾動）。
- **mutation bite 5+ 條親跑紅→還原 byte-identical**：append-only/redirect-refusal/zero-drop/None-vs-zero/window-anchor。負面不變量 test（tokenize 剝 COMMENT/STRING 驗禁字 relevance/ranking/top_n/private/order_id/authorization）非空——注入真碼 'ranking'→紅。
- **★ multi-session race 事故（§5e 觸發，閉合）**：review 期間隔壁 session 在 16:52-17:08 持續改寫 deribit 檔（中途整 dir 一度消失再重建）；初跑 37 test→重 fetch 後 46 test（test +211 行/+9 E4 補測：cli 端到端×4/parquet 非阻斷×3/PIT 時間戳×2，真路徑跑非 stub 受測對象）。**collector/artifact/cli/__init__ 4 實作檔重讀確認 byte-identical 於初審**（drift 僅 test）。未碰未認領改動、0 destructive git、重讀現行全文才出 verdict。final HEAD==origin/main `48cdbfe2`(0/0)，deribit scope 0 sibling commit overlap。
- **教訓**：(1) 「net 禁 rebate」對純數據採集軸=證**無 net 經濟學計算入口**（grep net/fee/pnl 全 0）比逐行讀更快閉合。(2) review 期間檔被並行重寫=evidence 失效，**必重讀現行 bytes 再簽**（37-test 證據不能簽 46-test 檔）。(3) untracked greenfield 檔被隔壁 session 刪=git 無 object 可救（fsck dangling 全無關舊文檔），唯一保全=等並行 writer 收手後重讀。

## 2026-06-14 · E2 re-review — DIRTY-FIX m4 fills/closed_pnl 3 LOW reconcile → PASS to E4
承上輪 3 LOW latent。E1 surgical 修 4 檔（dirty tree 僅動 task scope）：LOW-1 LATERAL 加 `e.fee IS NOT NULL`（entry_fill_found=TRUE ⟹ entry_fee 非 NULL）；LOW-2 net label CASE 加 `f.realized_pnl IS NOT NULL` + 移除 realized_pnl 的 COALESCE（close 行 rpnl NULL → emit NULL 不再把純費用小負值當真實 net bps）；LOW-3 closed_pnl fail-closed 對稱化（closedPnl 真缺 → authoritative/learning=None + source='bybit_pnl_missing_fail_closed'，`_attach` override 改用原始 `row["closedPnl"]` 判存在非展示欄 enriched["closed_pnl"]）。**verdict PASS, 0 finding 待修**。
- **mutation bite 三證親跑紅→還原綠**（cp backup 非 git destructive）：LOW-1 拔 `e.fee IS NOT NULL`→1 紅；LOW-2 復原 COALESCE+拔 guard→1 紅；LOW-3 `_attach` 改回 enriched["closed_pnl"]→1 紅、snake_row 改回 coalesce-0.0→2 紅。全還原後 byte-identical 親驗，/tmp temp 已清。
- **severity 上界=LOW 結構性確認**：fills net label 在 `stage1_production_runner.generate_stage1_candidates` **未被消費**（只 load+len 計數，未傳入 candidate 生成）；closed_pnl 的 learning_pnl/authoritative_pnl **0 ML/prod 消費者**（grep ml_training/learning_engine 空），唯一消費 pnl_source_drift_usd=GUI 展示（tab-demo/tab-live，非我 scope）。⇒ fail-closed 是 forward-protective 寧缺勿假，非當前 runtime hazard。MIT-MF-1 不觸（PnL 欄非 close_maker_*）。
- **canonical 對齊**：LATERAL `entry_context_id IS NULL`+`ORDER BY ts ASC LIMIT 1`+`engine_mode` 對齊 edge_label_backfill.py:188-198/434；fills_loader 排 5 前綴（risk_close/orphan_close/adopted_close/shadow_fill/unattributed）比 canonical（只排 unattributed）更保守=安全方向（誤排真 entry → entry_fill_found=FALSE → net NULL → caller dropna，fail-closed 非捏造）。
- 全綠：m4 116 + closed_pnl 43 + route 18 = 177 passed（mac_dev 3.12）；test 純加（+6/+8 func，0 刪 0 弱化 assert）；0 硬編 path；%% escape=5 不變；0 新 except/f-string-log；diff 全在 helper fn（0 route/auth）；py_compile OK。
- **E4 Linux-owed（合理 defer，Mac 不證 planner）**：(a) LATERAL EXPLAIN + fan-out 行數核（1000PEPE partial-close case）；(b) `e.fee IS NOT NULL`/`f.realized_pnl IS NOT NULL` 真 PG fee-NULL/rpnl-NULL 盛行率 + label 確 NULL 行為。
- **§5 race 5/5 PASS**：fetch 後 HEAD==origin/main=`48cdbfe2`（0/0）；dirty tree 巨大（多 sibling session），我只審 task 4 檔不碰其餘；3 stash pre-existing 不碰；我 4 檔最新 commit=`45a4720a`(已在 main)，3h 窗無新 sibling push 進 file scope；mutation 全 cp+restore byte-identical，HEAD 未變。
- 教訓：「fail-closed 硬化 fix」的 severity 由**真消費者**定界——load-but-not-consumed 的 net label + 0-ML-reader 的 learning_pnl ⇒ LOW（forward contract）非 runtime hazard；但 dormant≠免審，捏造值→None 的對稱性仍須 mutation 親證有 bite（snake_row 與 _attach override 兩處都要 mutate）。

## 2026-06-14 · E2 對抗審 MIGRATION-TREE-1 V005/V023 前向相容 → PASS to E4（0 finding；Linux 真 PG 9 scenario 全綠）
改 V005 PART4（5 個 legacy-bridge view 各包 `DO $WRAP$ ... IF EXISTS(information_schema.tables ...) THEN EXECUTE $VV$ CREATE OR REPLACE VIEW ... $VV$ END IF END $WRAP$`）+ V023 Guard A 前插 self-heal DO（`model_name 有 AND canary_status 無` → 動態 EXECUTE count → 僅 count=0 才 DROP）。**verdict PASS，0 finding**。
- **★ Linux 真 PG re-validate（非採信 E1 Mac scratch）**：prod PG = docker `trading_postgres`（timescale/timescaledb:latest-pg16, port 5432, db trading_ai）；server binaries 在 container 內非 host。建唯一命名 ephemeral DB `e2_mtree_<epoch>`（`CREATE DATABASE ... TEMPLATE template0 LC_COLLATE 'C'` 繞 container template1 collation-version 報錯）+ `CREATE EXTENSION timescaledb`，**絕不碰 trading_ai**，收尾 DROP DATABASE 親驗 trading_ai 仍在。
- **(a) virgin 全鏈 124/124 GREEN**：V001..V139 完整 replay（bootstrap 同序 `ls V*.sql|grep -v rollback|grep -v ^V999|sort`，`-v ON_ERROR_STOP=1`）0 失敗含 timescaledb hypertables。virgin end-state：V023 新 shape（canary_status=1/model_name=0）；V005 wrapped_views=0（無 *_legacy base 正確跳過）；6 non-legacy view 仍在；0 legacy table。
- **(c) V023-E 非空 legacy 不誤刪（最關鍵安全）**：注 1 真 row 進 V004 legacy stub（model_name 有/canary_status 無）→ patched V023 **exited NON-ZERO（Guard A RAISE「missing required columns... legacy V004-era stub」）**，**after: row=1 保留、仍 legacy shape** = self-heal `IF v_legacy_rows=0` 鐵則成立，非空絕不 drop，真-drift 交 operator。
- **V023-D 空 legacy drop+重建**：0 row legacy stub → self-heal DROP → CREATE 新 shape（canary_status=1/model_name=0）。**三 predicate 親證**：model_name 有(EXISTS)+canary_status 無(NOT EXISTS)+count=0 三者皆須；額外測「model_name AND canary_status 皆有」→ self-heal NOT fire（row 保留=1）；adversarial「空 legacy + V005 idx_model_registry_active 依賴索引」→ DROP TABLE 成功（索引隨表自動 drop，無需 CASCADE）。
- **(d) brownfield V005 PART4 雙跑冪等**：預建 5 legacy 表 → patched PART4 跑兩次 rc=0（CREATE OR REPLACE in DO/EXECUTE 冪等）→ 5 wrapped view 全建+可 SELECT。V023 新 shape 三跑冪等 rc=0、單表無 dup。
- **dollar-quote tag 無撞號**：`$WRAP$`/`$VV$` 全庫 grep 唯一在 V005（20 hits=10對×5view），0 外洩；不撞 PART3 裸 `$$` DO block。
- **★ 抓到 pre-existing 非-regression（嚴控 severity，不誤退）**：V005 **standalone** re-apply 在已套 V096 的 DB 上 PART1 `idx_rl_episode ON learning.rl_transitions` 報 `relation does not exist`（rl_transitions V004 建、V096 drop）。**親跑 origin/main 未改版 baseline V005 命中 identical error** → 確認是 V005 patch-independent 既有性質（patch 只動 PART4），forward 鏈順序跑無礙，非本 PR 問題。
- **§5 race 5/5**：HEAD==origin/main `64f33560`(0/0)，review 全程無新 sibling push；sibling 動 V005/V023 全是歷史 commit（22b4b372 舊 retrofit）非 2h 窗；working tree 僅 V005/V023+E1 memory（其餘 ?? 全隔壁 session leftover 報告/workflow，不碰）；我 review 0 edit patched 檔（git diff --stat 與初讀一致）；ephemeral DB+temp 全清。
- **教訓**：(1) migration re-validate 在 docker-PG 環境=用 `CREATE DATABASE TEMPLATE template0 LC_COLLATE 'C'` 建 throwaway DB 在**同 container 內**跑（取得真 timescaledb），比另起 cluster 省事且不碰 prod。(2) 「double-apply 冪等」要區分 **forward-chain 順序冪等**（真實 apply 路徑）vs **standalone re-apply**——後者會撞「本 migration 建、後續 migration drop」的 PART1 漂移（rl_transitions/V096），須跑 baseline 證 patch-independent 才不誤退。(3) 「非空 legacy 不誤刪」這種資料安全鐵則必親注真 row 跑 → 看 row count after = preserve，不能只讀 `IF count=0` 邏輯。(4) nested ssh→bash→docker 下 `$$`/`$X$` dollar-quote 會被中間 shell 展開成 PID，斷言查詢一律寫檔 docker cp 進 container 跑，避免引號地獄。

## 2026-06-14 · E2 review — AI-PRICING-FB-FD（F-B Rust stale 真名 + F-D fail-OPEN→closed + _sync 鍵空間）→ RETURN E1（1 HIGH + 1 MED + 3 LOW/INFO）
F-D fail-closed 親證有 bite（mutate silent-sonnet→test 紅，還原綠），fail-closed 對 PROVIDER_TIERS 全集 cross-check **不誤拒**（agent path line 680 無 cost>0 guard 但每個可達 eff_tier 都在 PricingTable.models）。但 RETURN：
- **HIGH-1 報告 modification list 重大不實**：報告稱「5 檔 + YAML 無改 + 值已對」，實際 diff 含 **ai_pricing.yaml 大改寫**（新增 opus-4-8/sonnet-4-6/haiku-4-5 active + 舊型號改 active:false + header 重寫）+ **pricing.rs test spot-check 改**（sonnet-4-5→4-6 + retired fail-closed assert）+ **MODEL_IDS[opus] 真 id 改**（4-7→4-8，報告只說 last_verified）。真 scope=8 處實質改，報告漏列 2 檔 + 誤述 1。違 lesson #2/#11，必更正報告才能放行（spec-claim 不實）。
- **MED-1 _sync 鍵空間修 0 test bite（vacuous）**：`model=MODEL_IDS.get(model_tier, model_tier)` 是 load-bearing claim（修 Rust 漏記）但 mutate 回 `model=model_tier` → **98 test 全綠**（親跑證）。報告「mutation bite 雙證」只蓋 fail-closed test，此 fix 無任何斷言。CLAUDE Style #9 + lesson #10。需補一條 `_sync_to_rust_budget` 收到真 model-id 的 spy 斷言。
- **LOW-1 provider_pricing_catalog.py:64 production 仍硬編 stale `claude-opus-4-7`**：報告稱「唯一 production 硬編 stale=tasks.rs:270」**不實**。official_pricing_manifest()→refresh_pricing_if_needed()（layer2_routes:416 prod route）→ refresh 觸發時 update_pricing(manifest) 會把 runtime opus model_id 寫回 4-7，撤銷 E1 對齊。billing 不受影響（Python 按 tier alias 查、Rust sync 讀 MODEL_IDS 常數=4-8），但 GUI/manifest model_id drift。建議同步改 4-8。
- **LOW-2 跨表三方 drift**：改後 MODEL_IDS[opus]=4-8 + YAML=4-8 vs catalog manifest=4-7，本來想消 drift 反留半。
- **INFO**：(a) tasks.rs `let model=model.as_str()` shadowing 可省（new() 收 impl Into<String> 可直傳 String），非 defect；(b) anthropic last_verified 只 bump 一家，is_stale() 仍 True（deepseek 49d/openai 35-37d/perplexity 63d 均>30），bump 無 freshness 效果，pre-existing 出 scope。
- 全綠實證：test_layer2 98 passed、cost/pricing 子集 44、cargo build openclaw_engine 綠（3 pre-existing dead-code warn）、ai_budget::pricing 9 passed（含 test_load_default 抽查真 YAML active>=5 + sonnet-4-6 some + sonnet-4-5 none）。tracker.rs/consumer_loop.rs/client.rs:275 的 sonnet-4-5 全 #[cfg(test)] inert=E1 NO-OP 分類正確。
- §5 race 5/5 PASS：fetch 後 HEAD==origin/main=`48cdbfe2`（0/0）；dirty tree 巨大多 sibling（ADPE/L2/perf3…）我只審 8 task 檔；test_layer2 working-tree 含 sibling perf3 `TestGetOllamaStatusPerf`(~106 行)非本任務，不碰（§5c）；3 stash pre-existing 不碰；2h 窗 sibling push 0 觸 8 scope 檔（`git log --since 6h origin/main -- <8檔>` 只回 4 天前 L2 commit）；mutation 全 cp+restore byte-identical /tmp 已清 HEAD 未動。
- 教訓：報告「修改清單」必逐檔對 `git diff --stat` 全集（本次報告漏 YAML+pricing.rs 兩實質改、誤述 MODEL_IDS）；「mutation bite 雙證」宣稱要逐 fix 親 mutate（fail-closed 有 bite≠鍵空間修有 bite，後者 0 斷言=vacuous）；「唯一 production 硬編」宣稱要 grep 全 repo 同 token（漏 catalog:64 opus-4-7）。

## 2026-06-10 · E2 review — OPS-2 Phase-2 cutover `a3d27729` (RETURN to E1)

**Verdict: RETURN**（1 HIGH + 1 MED + 1 LOW；production 代碼 0 defect，HIGH=漏掃 collateral 測試）。report: `workspace/reports/2026-06-10--ops2_phase2_cutover_review.md`。

**抓法（可複用）**：
- **base-vs-HEAD 全套失敗清單 diff** 是抓漏掃 collateral 的決定性手法：分支單 commit off base → `git checkout HEAD~1 -- <9 files>` 跑全套存 sorted FAILED 清單，還原 HEAD 再跑一次，`diff` 出唯一新增紅 = `test_strategist_promote_api.py::test_live_apply_all_gates_green_succeeds`（:574 注入 `OPENCLAW_IPC_SECRET` 當簽名 key，Phase-1 fallback 養出來的隱性依賴）。E1 只跑點名 5 檔（62 綠）就宣稱 collateral 清完 — **「受影響檔案清單」由 grep app-caller 推導必然漏「test fixture 對舊行為的隱性依賴」**，必須全套 sweep + 與 base diff（Mac 既有紅 66 條會淹沒肉眼，diff 才乾淨）。
- **雙語言 mutation 全 bite**：Rust panic gate 鎖 false → 指名測試紅；Python 重加 fallback → 3 條 cutover 定義性測試紅。新測試非遷就。
- **panic post-domination 結構分析**：spec §3.2 偽碼 gate `live_bindings.is_some()` 被 LIVE-GATE-BINDING-1 的 load_and_verify（try_spawn 內先 fail → live_bindings=None）post-dominate → 典型缺-key 啟動 panic 永不 fire，實際症狀=engine 起、live 拒 spawn、WARN kind deny-loop。實作=spec 逐字 → 定性為 PA advisory 非 E1 defect（fail-closed 完整 + §13.2 log-alert 可命中）。**教訓：fail-loud 聲明要追 gate 變數的 data flow 上游，「mirror 既有 pattern」可能 mirror 到不可達性**。
- 舊 fixture 對新代碼跑出 7 紅 = 親證 E1「collateral 紅」宣稱的最快手法（git show HEAD~1:file > 覆蓋 → 跑 → 還原）。
- E1 報告數字 2 處不符（watcher 15/15 實 12；signing 16 實 13；總數 62 對）— per-file 拆分宣稱要抽查。

**naming debt 裁決前例**：spec 兩 phase 欄都列的 rename（§4.1.1 `ipc_secret`→`live_auth_signing_key` param）+ 已有修復輪 → 裁 in-scope bundle 給 E1，不自修（PA 方案範圍內），不另開 debt ticket。

**§5 race**：origin/main 領先 3 commit 全 docs `[skip ci]` 0 overlap；3 條外來 stash 照例不動；mutation 後逐次 `git status --porcelain` 驗空還原。

## 2026-06-10 · re-E2 — OPS-2 cutover fix `cf1b9320` → ACCEPT (PASS to E4)

三項修復（HIGH env-key collateral / MED 4 處 param rename / LOW 註釋語義）全機械到位；diff 恰 3 檔 +11/−7 零漂移。親跑：promote_api 18/18 + 原紅單測綠；cargo lib live_authorization 24/24 + bin watcher 12/12 + cutover 3/3；`bash -n` 過。report 同檔追加 section（不開新檔）。

- **省 full-suite 的算術核驗法（可複用）**：E1「passed 4255→4256=淨增 1 測試」用 `git diff base..HEAD -- '*.py' | grep -cE '^\+\s*(async )?def test_'`（+6/−5=+1）零成本核實，不重跑全套；Rust flake 宣稱用 **total 不變性**核（上輪 4154+1=4155 = 本輪 4155+0，fix 0 新 test）。re-review 抽驗宣稱優先找這類 invariant。
- **rename 零 caller 影響實證**：Rust 位置參數 rename 理論不可能破 caller，但便宜實證 = 跑「唯一外部 caller 所在 test target」編譯+綠。注意 openclaw_engine **bin 名 `openclaw-engine`（連字號）**，watcher/cutover tests 都是 main.rs/src `#[cfg(test)]` mod 在 **bin target**（`--bin openclaw-engine <filter>`），不是 `--test` integration target（`--test` 會報 no target + 列可用清單）。

## 2026-06-14 · E2 — P2-DIRTY m4 fills + closed_pnl 聚合修 → RETURN E1（0 CRITICAL/HIGH/MED；3 LOW latent + 3 INFO）
- 範圍：dirty 多 session WIP 樹（14-20h stalled，HEAD=origin/main `48cdbfe2` 0/0；E3 凍 baseline `976d420e` 為 HEAD ancestor，committed delta=EMPTY → closed_pnl reconciliation 業務碼=**pre-existing WIP** 非 E1 新寫，git 證）。4 task 檔 +346/−44。E1 surgical 增補=docstring 中文化(item3)+測試(item2)+m4 LATERAL(item1)。其餘 dirty .py（layer2/risk_routes/shadow_decision_builder/audit_migrations/passive_wait…）=隔壁 session WIP，**不審不碰不 revert**（§5b/§5c）。
- 3 spec 項全到位且 mutation 親證有 bite（非信報告）：①m4 fan-out 黑名單斷言（重現裸 LEFT JOIN 字串→紅）②learning_pnl fail-closed（None→estimated_net→紅，順證 `from app import db_pool` ≡ `from . import db_pool` monkeypatch 真生效非空測）③pg_unavailable fail-loud（raise→return empty→紅）；4 probe 全 cp-backup+還原，`git diff --stat` 逐次驗 +346/−44 無殘留。test 114 m4 / 40 closed_pnl / 18 route 全綠 0 regression（親跑 mac_dev py3.12，匹配報告）。
- **m4 測試本質=FILLS_QUERY_SQL 字串 grep（white/black-list），非行為 PG 測**——bite 只證 SQL 文字含/排某子串，**不證 LATERAL 真消 fan-out**。Mac 無 PG → LATERAL 真行為（+33 fan-out 是否歸零）E4 Linux EXPLAIN owed（E3 已實證 OLD 裸 join +33 行真存在；NEW LATERAL 未驗）。`%%` 轉義對 caller `cur.execute(sql, dict(params))` psycopg 路徑正確（5 `%%`+1 named+0 stray 親驗）。fills loaded-but-unused（`generate_stage1_candidates` 不吃 fills）→ 全 latent。
- **LOW-1（E1 新契約自身瑕疵，conf med）**：`entry_fill_found=TRUE` ≠ net label 非 NULL——entry 行 `e.fee` 為 NULL 時（`trading.fills.fee REAL DEFAULT 0` 可空，V003:278）CASE 算術 `-entry_rep.entry_fee`=NULL 但 flag 仍 TRUE。docstring 稱 caller 依 entry_fill_found 旗標 dropna→imprecise。緩解=caller dropna-on-net-label + DEFAULT 0 罕 NULL + 與 canonical「NULL label 被 WHERE drop」一致。Linux 驗 fee-NULL 盛行率。
- **LOW-2（E3 LOW-2 僅部分閉，conf med）**：新 docstring 釐清 raw `realized_pnl` 欄保留 NULL，但未處理「realized_pnl=NULL 但 entry found → net label= COALESCE(NULL,0)-fee-entry_fee = 小負值非 NULL」靜默捏造。empirical 今日 vacuous（E3 close_null_rpnl=0）。
- **LOW-3（fail-closed 不對稱，conf high，latent）**：`_closed_pnl_snake_row` 主 Bybit 路徑 default `learning_pnl = closed_pnl if not None else 0.0` source=`bybit_closed_pnl`——Bybit closedPnl 真缺時捏造 0.0 標 authoritative，違 PG-fallback 路徑正確套用的「寧缺勿假」（learning_pnl=None）。learning_pnl/authoritative_pnl **0 consumer**（latent）；pre-existing WIP 非 E1 新寫，但在 task「正確聚合/fail-closed」框架內。
- **INFO**：①closed_pnl 業務碼=pre-existing WIP，E1 報告 line20 框成「docstring 中文化」**under-states** 隨附 uncommitted 業務碼（將與 commit 同進，未以業務碼受審）②報告 test count 偏差（m4「新 3」實 4 / closed_pnl「新 6」實 5 新+1 updated，lesson#2）③m4 entry LATERAL 加 4 close-shaped 前綴排除超 canonical（只 `unattributed:`）=stricter belt-and-suspenders 非 bug。
- 裁 RETURN 理由：3 LOW 全 latent 但 LOW-1/LOW-3 觸 E1 本 diff 新引入的 discriminator 契約 + fail-closed 框架不對稱，屬 E1 應 reconcile（修 or 明示 defer + QC sign-off）非 E2 代寫；core fix 本身 sound。LATERAL 行為 E4 Linux owed。
- 教訓：(1) **dirty 多 task WIP 樹審查先用 `git diff <E3-baseline> HEAD -- <file>` 證 committed delta=EMPTY** 區分「pre-existing WIP」vs「E1 新寫」——business logic 雖在 working-tree diff 但非本任務作者，severity 與裁決需據此 scope。(2) **字串-grep 測試的 mutation bite 只證文字不證行為**——SQL LATERAL 這類必標 Linux PG EXPLAIN owed，不可因 grep 測綠當行為已證。(3) fail-closed 審查必雙路徑對照（主路徑 vs fallback）：fallback 硬到位不代表主路徑亦 fail-closed，本例 PG-fallback None 對但 Bybit 主路徑 0.0 捏造=不對稱。
- **殘留字串域判定**：rename 後 grep `ipc_secret` 殘 6 處全 comment/負向測試名（IPC-transport 域概念引用）= 合法；抽 2 處讀上下文驗，不只看 grep 行。
- **5e 真 fire 案例**：review 中 L2 Mesh 7 commits 推上 origin/main（含 E2/memory.md +574 行 sibling 寫入）→ `comm -12` 比對 PR 11 檔 vs sibling 檔 = 0 overlap → verdict 不受影響、照 SOP 記錄不忽略。

## 2026-06-14 · E2 — P2-MIT model_registry [9] shadow-freshness 盲區修 → PASS to E4（0 BLOCKER/HIGH/MED；1 LOW + 1 INFO non-blocking）
surgical 修：`checks_ipc_edge.py::check_model_registry_freshness` 僅在 `slots==0` 分支新增 shadow cohort fallback（production query + `slots!=0` age 路徑與 HEAD **byte-identical** 親驗：`git show HEAD:` diff 確認）。schema 對 V023 親驗（`canary_status` CHECK 含 'shadow' DEFAULT 'shadow'；`created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`；`promoted_at` NULL until promote；index `idx_model_registry_canary_status_created` 覆蓋新查詢）。runner.py:762 wired 真 fire；WARN 經 runner :491-504 surface 進 SUMMARY（exit 0=PASS/WARN，不阻塞=設計）；0 其他 production caller（grep program_code/rust 空）。9 新 test 親跑綠；**mutation A/B 親跑**：blanket-PASS 還原 → 6 紅（含 mutation_bite WARN→PASS）/ 3 綠（production-skip/table-missing/boundary-PASS，不觸 shadow 探測正確仍綠）→ cp backup 還原 → 9 綠 + `git diff --stat` 確認唯一 task 改動無殘留。full `tests/helper_scripts/` 25 passed 零回歸。
- **LOW-1（non-blocking）**：fail-soft try/except 僅包 `cur.execute`+`fetchone`（:518-528），datetime 減法 `(now_ts - newest_shadow).days`（:543）在 guard **外**。psycopg2 對 TIMESTAMPTZ 預設回 tz-aware datetime → aware-aware 減法安全；唯若驅動回 naive datetime 會 `TypeError` 未被 fail-soft 接。實務 psycopg2 預設不發生，故 LOW 非 RETURN；docstring「any query error degrades to PASS」措辭略寬（datetime 算術不在保護內）。
- **INFO-1**：`slots==0` 且只有 `promoting`/`retired`/`rejected`（無 shadow 無 production）→ shadow query 回 0 → PASS「shadow rows=0 Phase 1a/2」訊息略誤導（其實有 promoting row）。非 safety issue，cohort 設計只覆 shadow（spec scope）。
- 邊界：`shadow_age_days > 30`（`.days` floor）→ 實際 WARN 在 age≥31d；與 production 路徑 `age_days > 30` parity 一致（30d 閾值對齊 retrain-due 語義，設計擇定）。
- 註釋：file 局部 convention=English-first docstring + 中文 `[N]` 補述 + 中文 inline；E1 嚴格 match 既有 pattern（CLAUDE §七「touched bilingual 保 Chinese」）→ 合規非違規。
- **§5 race 全 PASS**：HEAD==origin/main `48cdbfe2`（無 sibling 領先）；task scope=`checks_ipc_edge.py`(M)+新 test(??)，大髒樹（README/TODO/app static 等 40+ 檔）= 既有多 session WIP 非本 task leakage（IMPL 只觸 2 檔）；3 stash 全 pre-existing「not mine」未動；review 中無新 sibling push；mutation 全 cp backup 還原（非 git destructive），0 git 寫操作。
- 教訓：surgical 「只改 X 分支」宣稱用 `git show HEAD:<file>` 抽未改路徑 byte-compare 證 production 路徑零漂移最快；fail-soft 範圍要逐行核 try/except 邊界——「any error degrades」常漏掉 except 區塊外的算術/型別運算（本例 datetime 減法在 guard 外，naive-datetime 路徑未被接，雖實務不觸發仍標 LOW）。
- E1 本輪採上輪校正計數（watcher 12 非 15），數字 0 不符 → RETURN 輪揪 per-file 精度有效，re-review 輪信任度可升但仍抽驗。

## 2026-06-14 · E2 — PERF-3 async urlopen fix（layer2_routes.py get_ollama_status）→ RETURN E1（1 HIGH + 1 MED + 1 LOW）
被改的 model-list fetch block（531/538 兩 arm urlopen→httpx.AsyncClient）本身正確：diff 精準 scoped（無 scope creep）、httpx 是本目錄既有範式（requirements httpx>=0.28.0、layer2_tools.py:529/1068）、非阻塞親證（mock 0.3s 慢 server，並發 ticker 60/60 不凍）、cited 133 tests 親跑綠、跨平台/bare-except/f-log/py_compile 全綠、in-function import 屬本檔慣例非 finding。
- **★ HIGH — 殘留同步阻塞 IO 在同一 async route（fix 不完整）**：line 517 `client.is_available()` 是**同步** urlopen——cache-miss/過期時 `local_llm_factory.py:149` + `ollama_client.py:151` 各 `urllib.request.urlopen(req, timeout=1)` 在 event loop thread 阻塞最多 1s，GUI 每 ~60s 輪詢至少 fire 一次。PA P1-PERF-3 scope = 「async route 內同步阻塞 urlopen 凍 event loop（get_ollama_status）」——是 route-level defect class 非單 line 531。報告稱 517「60s-TTL-cached cheap check, untouched, out of PA scope」**對 cache-miss 路徑判斷錯誤**（cache-miss 不 cheap）。修法 trivial 且 drop-in 既存：`await client.is_available_async()`（兩 client 都已有 :158/:173，內含 asyncio.to_thread）。
- **★ MED — fail-soft 語意非「byte-for-byte 保留」（HTTP-error-with-JSON-body 退化）**：urllib urlopen 對 4xx/5xx **raise HTTPError**→入 except→設 model_list_error；httpx `.get()` 對 4xx/5xx **不 raise**→若 error body 是合法 JSON（過載 proxy/LM Studio 常見回 503+{"error":...}），`resp.json()` 成功、`data.get("models",[])==[]`→**不進 except、model_list_error 不設**→回 models=[]/model_count=0「像 0 模型」而非「取列表失敗」。本目錄既有範式（layer2_tools.py:545/1070）正是用 `resp.raise_for_status()` 防此。實證 mock 503+JSON 親跑確認。緩解：model_list_error **0 外部 consumer**（GUI tab-ai.html:1270 只讀 model_count），故 GUI 退化僅「0 模型」vs「?」，但報告「fail-soft 保留 byte-for-byte」claim 不實。修法=兩 arm `await http.get(url)` 後加 `resp.raise_for_status()`（對齊既有範式）。
- **MED/LOW — 0 committed 回歸測試**：「self-test 3 cases」是 throwaway，無永久 test 覆蓋 get_ollama_status；blocking 重現/fail-soft 路徑零 guard，未來編輯可靜默回退。建議 E1 補 httpx.MockTransport 回歸測（non-blocking + 503-json fail-soft 兩條）。
- 教訓：(1) **「perf fix 移除阻塞 IO」必窮舉同 route 全部同步 IO 點**，不能只改 PA 點名的那一行 line；caller chain（is_available→urlopen）要追進去——「cached cheap」的 cache-miss 路徑仍阻塞。(2) **urllib→httpx 遷移非 drop-in**：urlopen 對 HTTP-error raise、httpx .get() 不 raise，少 `raise_for_status()` = 靜默吞掉 4xx/5xx，「byte-for-byte fail-soft」是錯的；對齊同目錄既有 httpx 範式（必帶 raise_for_status）比讀 diff 像不像更可靠。(3) §5 race：HEAD==origin/main `133eaccf`，layer2_routes.py 僅本人改動 0 sibling 競爭，3 stash 非本人未碰；髒樹是別 session 跨檔 WIP。

## 2026-06-10 · E2 — fix/l2-owed P3b owed wiring 三 commit 對抗審 → RETURN to E1（2 MED + 2 LOW）
- 範圍 `a187b4e7`(adapter/reindex/route/novelty/seed)+`aae7656e`(guard v2)+`470687ac`(harness import)。核心全綠：捏造禁令 tokenizer-strip 結構成立+消費鍵 10+1 逐一對上+ordinal-offset 與 _span_days/_chrono_key 語意親證一致+mask date-key 空化 workaround 宣稱實證成立+guard finally 重封鎖；3 mutation 全 bite（union 還原 3 紅/ordinal→dense 2 紅/M3 typing 削除 1 紅）；L2 面 413p/4xf+58p 親跑。
- RETURN 因：MED-1 route async handler 直呼同步 load_factor_bundle（layer2_critic :294 同家族明文 to_thread 範式，event-loop 阻塞）；MED-2 harness fix 註釋虛假引用（docstring 無「必以 -m」慣例，grep 唯一命中=新註釋自身）+direct-file --write-db 從可用變炸（頂部 fallback 明文支援該模式；修法=鏡像同檔 dual-path try/except）+_write_db 零測試。
- §5e 真 fire ×2：review 期間 sibling 在同 branch 同 worktree 落 `58192465`+`026dd75d`（adapter 裁窗，QC 帶）——驗零 mutation 殘留吃入+新 HEAD 53p 綠；該 2 commit 不在本輪 sign-off 範圍，標給 re-review 輪一併蓋。教訓：worktree 共用時 mutation probe 必須「改完即還原」短窗操作，每輪 status 驗空——本次因此倖免污染 sibling commit。

## 2026-06-14 · E2 — ADPE 閉環 runner + demo-maker arm 對抗審 → RETURN E1（1 CRITICAL）
- 範圍：新 package adaptive_demo_profit_engine/（reward_source/ipc_lever/demo_maker_arm/runner/__init__）+ adaptive_demo_profit.toml + 28 新 test。全 untracked 新檔，0 改既有/髒檔（linucb/thompson clean，regime_bandit_allocator 本身新檔）。28 新 test + 28 allocator core 親跑全綠、compile OK、MODULE_NOTE 全在、0 forbidden token（全在「禁用」註釋）、0 硬編路徑、SQL 參數化、engine_mode_scope('demo')=('demo',) demo 硬鎖正確、kill switch snapshot/restore 對、dual-track honesty **真有 bite**（artifact 80bps×50→transferable_only n=0+flat、saw_artifact=True；mutant 改 taker_real→transferable 吸收 n=50→權重 1.0）。
- **★ CRITICAL（閉環對真資料 = no-op）**：view `linucb_arm_id`（V031:326 `regime_norm || '__' || strategy_name`）的 regime 前綴用 **view 詞彙**（trending/mean_reverting/random_walk），但 ADPE 其餘全用 **allocator 詞彙**（VALID_REGIMES=bull/bear/high-vol/chop/range/insufficient_context）。reward_source 只 remap `regime` **欄位**（map_view_regime_to_alloc_regime）卻把 `linucb_arm_id` **原樣穿過**（:149）→ 兩套詞彙在 arm_id 層對撞。後果端到端親證：真 view row（arm_id='mean_reverting__grid_trading', +45bps×60）→ runner `_discover_candidate_arms` 用 `parse_arm_id` 取前綴 'mean_reverting' 去 `by_regime` 找桶（桶 key 只 allocator 詞彙）→ **arm 被靜默丟棄**→ candidate_decisions=[]/desired_active={}/all_regimes_flat=False/0 IPC。贏家不活化、輸家不歸零，閉環對 production 資料是純 no-op。config candidate_strategies 經 make_arm_id 種 'range__grid_trading'（allocator 詞彙）也撈不到 posterior（落在 'mean_reverting__grid_trading'）= 同根因第二死叉。
- **★ 為何 28 test 全綠卻 vacuous**：runner test 用 `make_arm_id("range",...)` 手造 **allocator 詞彙** arm_id（reward_source 真路徑永不產出）；reward_source test fixture 更捏「不可能的 row」——arm_id='range__grid_trading'（allocator 詞彙前綴）配 regime='mean_reverting'（view 詞彙），真 view 必 'mean_reverting__grid_trading'。test 餵了上游解析永遠給不出的一致 arm_id → 遮死真缺陷。經典「unit 注入一致值遮上游解析錯」（承 P3a contract_ver 教訓）。

## 2026-06-14 · E2 — P1-AUTH-1 live config write gate 對齊（risk_routes + handlers_config）對抗審 → PASS to E4（0 finding，2 INFO 已 spec-acknowledge）
- 範圍：3 task 檔（risk_routes.py +23 / handlers_config.rs +13 / test_risk_routes_live_config_gate.py 新8test），從多 session 大髒樹精準隔離（`git diff -- <path>` 只取本 task scope，其餘 dirty 屬 sibling 不碰）。HEAD==origin/main==`e454078d`，review 全程未變。
- **stage0 spec 合規逐項對齊 PA cold_audit_validated_fix_plan §P1-AUTH-1**：(1) guard 置 `_require_risk_write`+engine-validity 之後、`_get_direct_ipc` 之前=spec 位置；(2) 複用 `live_preflight.all_five_live_gates_ok(actor, require_authz=True)` 唯一 primitive 非 copy inline，簽名/回型 `(bool,list[str])` 親驗對；(3) 409 detail error/gate_failed/message 鏡 `post_live_session_start`（:174-187），省略 session-only 欄 authority/session_state/rust_synced 屬正確 carve（config route≠session route）；(4) Rust 維持 audit-only warn!（config_name `risk/{engine}`→`risk/live` 前綴可達親證，dispatch.rs:433；warn! 已 import；置 store.replace 成功臂；未碰 RiskConfig::validate/無硬拒）=符合 spec「defense-in-depth + 明禁硬擋（無五門 context + 破 g2 canary）」。
- **三層對抗驗證全綠**：① 8 test 親跑綠；② mutation-bite 親跑——cp 備份→移除 guard block→3 live 測紅（live_gate_failed 200≠409 / require_authz / real-chain global_mode）+5 demo/paper/ordering/green 仍綠（by design）→cp 還原 shasum 比對 + `git diff --stat` 回 +23 + grep E2-MUTATION=0 三重驗淨；③ 真實五門鏈 test 非空轉（不 patch primitive，走真 secret slot + 簽名 authorization.json fixture，env `OPENCLAW_LIVE_AUTH_SIGNING_KEY` 親驗=verify_signed_authorization 真讀的 key 非 stale 名）。adjacent 139 passed（118+executor21）、Rust config IPC 16 passed（含 engine routing test_p2_patch_risk_config_engine_routing）零回歸。
- **pre-fix gap 真實性確認**：`_require_risk_write`=只 `require_scope_and_operator("risk:write")`（operator+scope，**無**五門）→ PA P1-AUTH-1 authority 不對稱屬實。`_ALLOWED_ENGINES={paper,demo,live}` 此 route 無 live_demo 值（LiveDemo 用 live 槽+demo 端點，Gate3 跳 mainnet env 但仍要 secret+簽名）→ guard 對 LiveDemo 不誤殺、合法路徑可過（real-chain green test 證）。
- **INFO-1（spec-acknowledge 限制 #1 同族，非本 fix defect）**：`mlde_demo_applier.py:1564` 直呼 `patch_risk_config` IPC 繞 FastAPI route + 新 guard；engine_mode 默認 'demo' 但可經 env `OPENCLAW_MLDE_DEMO_APPLIER_ENGINE` 覆寫成 live → 與 direct-socket bypass 同類未閉面（受 socket trust tier 約束，requires_governance 對 live/live_demo flip true :758）。primary fix 按 PA spec 只蓋 FastAPI route，此 caller 與 g2 直連同屬 owed「live authz 搬進 Rust」operator 決策。**INFO-2**：GUI risk-tab.js _wrapLiveSave 不渲染 409 gate_failed=minor follow-up（fail-closed 仍正確）。
- **§5 race 5/5 PASS**：5a/5e fetch 前後 origin/main 恆 `e454078d`，2h 窗內 sibling push（ADPE perf/L2 docs）file-scope 與本 task 3 檔 0 overlap；5b unstaged 大髒樹但本 task 3 檔精準隔離無外洩；5c 3 舊 stash（recovered/8c-branch/E1-rebase）一律不碰；5d N/A 未 commit；mutation cp-backup-restore 非 git destructive，每步 status 驗淨。
- **教訓**：(1) 大髒多 session 樹下 stage0 審查靠 `git diff -- <精準 path>` 隔離 task scope，不信 `git status` 全列（本次 30+ dirty 檔僅 3 屬 task）。(2) 「鏡像既有 409 契約」要逐欄對源（post_live_session_start），但要分辨「正確省略不適用欄」（session-only 欄在 config route 省=對）vs「漏欄」——不是欄位數守恆而是語意守恆。(3) 「複用 primitive 非 copy」要親驗 primitive 簽名+回型+其讀的 env key 是真權威（OPENCLAW_LIVE_AUTH_SIGNING_KEY），否則 real-chain green 可能 spurious。(4) primary-fix-at-route 型 fix，必 grep 「繞 route 的 production 直呼 caller」（mlde_demo_applier 直呼 IPC + env-overridable engine）證 fix 覆蓋面，未蓋者標 INFO 對齊 spec owed 非自行 blocker。
- 修方向（不代寫）：reward_source 把 arm_id 也經詞彙映射重建（拆 view arm_id 前綴 → map → make_arm_id(alloc_regime, strategy)），或 runner discovery/config 種子改用 view 詞彙——擇一統一；補一條「真 view-shape row（view 詞彙 arm_id）→ 贏家活化 / 輸家歸零」端到端斷言（鎖 regression）。
- §5 race 5/5 PASS：HEAD==origin/main==`976d420e`；ADPE 6 路徑全 untracked 新檔 0 file-scope overlap；tracked M 檔（closed_pnl/m4 fills/memory）皆 sibling session 不碰；review 全程唯讀 probe（/tmp 腳本，已刪）status clean；0 git 寫操作。
- 教訓：(1) **arm-space 詞彙是隱性 SSOT——跨 migration view 與 Python allocator 各自演進的 enum 在 arm_id 字串層對撞，比欄位層更難抓**；reward 來源真 schema（view CASE 的字面 enum）必親讀，不能信「regime 欄位映射了就對」。(2) 閉環 review 的決定性手法=**手造一條「reward_source 真會吐」的 row（view 詞彙 arm_id）灌進 runner 看 desired_active/IPC**，比逐行讀 allocate 更快定死 no-op；test 用 make_arm_id 自造一致 arm_id 是危險信號。

## 2026-06-10 · re-E2 — fix/l2-owed 3 commit narrow re-review → RETURN to E1（1 新 MEDIUM + 1 LOW）
- `58192465`+`026dd75d` 裁窗 PASS（mask 仍吃全 buffer closes、裁輸出不破 prior-only 回看；ws/we :377 early-guard 型別安全；INFO：`_clip_window` 可出 `{}` 不歸一 None 無 reason，但 B1 :1134 fail-soft 兜 DEFER 無 false-GO）；`2c5d6a62` (a) to_thread PASS（asyncio :34/critic 範式明文/adapter 零可變模塊態）、(b) dual-path 生產修 PASS 但新測試 0 bite（AST 證只 callable/hasattr 從不執行 _write_db → 函數體 import 永不被行使，docstring「迴歸鎖」overclaim）= LOW。
- **★ 新 MEDIUM：LOW-1 短路把 dt.date-key 因子 dict 留進 context → STAGE 2 `json.dumps(context, default=str)` 必 TypeError（default 不兜 dict key，端到端 repro 釘死：真 adapter 短路 fire + :376 逐字表達式炸）**。鏈：dispatch(:827/:835)→orchestrator(:442)→cascade :648→_run_cloud_interpret :376 無包裹→orchestrator :449 兜→`report_call_outcome(ok=False)` 推 fail-safe SM + 無 D3 row + 零 diagnosis；pre-fix 同 dispatch 因子連帶 None=JSON 安全可完整跑。教訓：**「語義等價」要分層驗——B1 層等價≠cascade/序列化層等價；保留下游零消費者的資料（B1 對 cand=None 不讀因子）只剩序列化危害**。修方向：短路改回傳全 None+新 reason，或 adapter 邊界字串化 date key；補 evidence-built context→json.dumps round-trip 測試。
- §5e 又 fire ×1：sibling 同 worktree 落 `bec21427`（docs-only QC signoff，0 code overlap）照 SOP 記錄不影響 verdict；本輪 0 mutation 全唯讀 repro，status 全程 clean。

## 2026-06-10 · E2 — half_life scipy importorskip `dc5c60d7` → APPROVE-with-nits（PASS to E4，0 需修 + 2 INFO）
- 2 檔 +7 行純插入零刪除；三模式親跑：scipy 7p / shadow 5p+2s（skip 可見誠實）/ base 版+shadow 恰 2F 精準重現 trade-core 症狀（`assert 'default_14d' == 'pnl_decay'`）= 守衛有 bite、RCA 真因非遮蓋；0 production caller（grep 證，唯一命中 embargo_validator.py:160 字串）。報告：`workspace/reports/2026-06-10--half-life-scipy-skip-review.md`。
- 可複用手法：dep-skip 守衛審查 = PYTHONPATH 注入 `raise ModuleNotFoundError` shadow 模擬缺依賴（importorskip 與模組 try/except 同被觸發），配 base 版 untracked 副本重現原始 FAIL 證 bite；「不擴守衛」可以是對的——降級路徑測試在 scipy-less 解譯器上行使的正是該環境生產路徑，加守衛反減覆蓋。

## 2026-06-14 · re-E2 — P1-SCHEMA-1 E2-RETURN 兩項修補 narrow re-review → PASS to E4（0 finding，1 INFO；第三 BLOCKER 正確升 PA）
- 範圍：3 檔（schema_contract_test.rs untracked +seed_legacy_precondition ~45 行 / ci.yml +63 新 schema-contract job / audit_migrations.py +8 註解+cwd path）。從多 session 大髒樹精準隔離。HEAD==origin/main==`48cdbfe2` review 全程未變。
- **★ bite primitive 真 PG 實證（中心問題：column rename 會不會紅）**：本機 Homebrew PG16 建 ephemeral DB e2_bite_probe，跑 test:318 的**逐字** model_registry SELECT→綠（0 row=match `assert!(row.is_none())`）；`ALTER...RENAME COLUMN artifact_path TO artifact_uri`→同 query `ERROR: column "artifact_path" does not exist`→Rust=`.expect()` panic=test RED。**rename 確 bite**。INSERT test(1-4) bite 更強（column 存在 + bind-type execute 雙驗）。消費者 registry.rs:224 SELECT 與 test:318 **byte-identical**（欄/WHERE/ORDER BY/tuple shape 全同）。
- **未 mass-convert query!**：grep 0 個 `query!/query_as!` 巨集（unchanged）、98 runtime `sqlx::query(` call site 0 改。E1 沒誤把 93 call site 轉 query!（對）。
- **audit reverse-check 正確性**：main() 所有 drift-detection 路徑 return 0；非零只在 setup error（缺 env=2/缺 psycopg2=2/find_migrations_dir 缺目錄 raise）→ 不可能產 false-positive-drift CI 紅（正是上輪 MEDIUM 訴求）。connects 127.0.0.1:5432 對齊 CI service；cwd-relative path 加在頂（legacy Mac fallback :221 保留=surgical）。
- **CI 成本紀律**：新 job `if: github.event_name == 'pull_request'`+`runs-on: ubuntu-latest`(1x)+timeout 20m+pin image tag `timescale/timescaledb:2.26.1-pg16`。零 macOS 10x、不 push-to-main、不 weekly。`--test-threads=1` 正確必要（process-global env set_var + 並發 migration race + legacy-seed canary）。timescaledb extension step 必要實證：V006:23 `add_compression_policy` 無 guard，缺 ext 必 hard-fail（V002 hypertable 有 pg_extension guard 但 V006 無）。
- **第三 BLOCKER 升 PA 屬實非 paper-over**：V004:28 `is_active`/V005:168 `idx_model_registry_active(is_active)`/V023:70-95 schema_guard A 要新欄否則 RAISE = 真三方矛盾，無單一靜態 shape 同滿足。改已套用 migration=P0 checksum-drift 類，出 E1/test scope，正確升 PA。E1 報告誠實=失敗點 ordinal 5→23（V005 確修、V023 殘留），未假稱「全樹綠」。
- **INFO（唯一）**：MODULE_NOTE:9-11「同時驗 type/nullable」對 INSERT test 成立，但 2 個 SELECT test(5,6) 對空表只 prepare-time 驗 column 存在、不驗 output-tuple decode type（無 row 可 decode）；per-test 註解(:299/:339) 已正確收窄=MODULE_NOTE 輕微措辭不精，INFO-only 不退。
- **out-of-scope 確認**：handlers_config.rs +13（risk/live warn! audit）是 sibling P1-AUTH-1 task（已另檔 sign-off），非 SCHEMA-1 leak；其餘 30+ 髒檔皆 sibling，未碰。
- **§5 race 5/5 PASS**：5a/5e fetch 前後 HEAD==origin/main==`48cdbfe2`；2h 窗 sibling push（ADPE/L2/ops）file-scope 與 3 task 檔 0 overlap；5b 大髒樹但 3 檔精準隔離無外洩；5c 3 舊 stash 不碰；5d N/A 未 commit；bite probe 用獨立 ephemeral DB（建後 drop），0 git 寫操作 status clean。
- **教訓**：(1) **runtime-checked sqlx contract test 的「rename 會紅」必親跑 PG 證**——本機建獨立 ephemeral DB + ALTER RENAME + 重跑同 query 看 ERROR，比讀「sqlx 會 prepare 失敗」推理可靠；SELECT 對空表只驗 column 存在不驗 output type（fetch_optional 無 row 可 decode），INSERT 才驗 bind-type。(2) **「informational-only 非 gate」要逐 return 路徑核**——drift 路徑 return 0、setup 路徑 return 2/raise；setup 失敗該紅（診斷沒跑）≠false-positive-drift 紅（上輪訴求）。(3) **test-layer 不該硬扛的 migration-tree 矛盾要升 PA**——V004/V005/V023 三方無共存 shape，pre-seed 反觸 canary，正確判「test-precondition 層無解」升 migration-hygiene owner。

## 2026-06-10 · E2 第三輪 narrow delta — fix/l2-owed `bc83ecc9` → RETURN to E1（MEDIUM 修 PASS；LOW 修 mutation 不殺）
- MEDIUM 修全綠：短路全 None + reason 保留；TestContextJsonSerializable 鎖 executor :376 逐字 `json.dumps(ctx, ensure_ascii=False, default=str)`（:315/:922 同式）；mutation 還原保留因子 → 短路測試紅且死法=`TypeError: keys must be str...not datetime.date`（前輪 repro 同 signature），full-path 不受擾；build_context_from_evidence 對 context 零後加料=鎖內層即鎖最終物件。
- LOW 修 FAIL：`test_write_db_import_seam_truly_executes` 兩個歷史壞形 mutation（原始絕對形 + 470687ac relative-only）**全存活**（各 1 passed）——根因=tests/conftest.py:15 插 research/ 進 sys.path + harness 以 package member 被 import，pytest in-process 結構上無法重現 package-mode/direct-file 任一 import context → docstring「revert→ModuleNotFoundError」宣稱實證不成立（同測試連續第二輪 bite overclaim）。修方向=subprocess 真 package-mode probe 或誠實改寫宣稱。
- 教訓：import-seam regression 測試的 bite 取決於 import context 可否重現——conftest sys.path 注入會讓「壞 import 形」在測試裡照樣解析；宣稱鎖「執行模式 X 下會炸」的測試必須真的在模式 X 下執行（subprocess -m），in-process pytest 永遠不是模式 X。

## 2026-06-10 · E2 終輪單點 — fix/l2-owed `209652f1` seam 測試 subprocess 版 → PASS to E4
- mutation 親重放：harness dual-path 換回 `470687ac~1` 原始裸絕對形 → 測試紅且死法=`SEAM_FAIL:ModuleNotFoundError:No module named 'aeg_regime_runner'`（V127 populate 原 bug signature，非 sentinel）；還原 → 7 passed、status 驗空。docstring 宣稱逐條對實況（conftest 僅 in-process sys.path 不洩 env、PYTHONPATH 未設、namespace-package 鏈成立→relative 唯一通路、direct-file fallback 誠實標不覆蓋=harness `pragma: no cover` 對應）無 overclaim。
- 教訓：subprocess probe 的「乾淨」要驗兩層——sys.path 注入不跨進程（conftest in-process 安全）但 os.environ 會繼承（本次 PYTHONPATH 未設故成立；更硬化可傳 scrubbed env，INFO 級不退）。

## 2026-06-10 · E2 — L2 P2p incident_sentinel `d7f5f283` 對抗審 → RETURN to E1（2 MED）
- 56 passed 親跑、watchdog 22 passed 零回歸、bash -n×2 / py_compile OK、800 行（恰 review-attention 門檻邊界檔審）。6 條 deviation 全 ACCEPT（env-file WARN/axis-error audit-only/exit FAIL>connect/--remove APPLY-gate/report 不落檔皆對位設計或慣例）。mutation 8/10 bite（should_emit 窗/SQL寫字/drain/游標/watchdog_state.json/subprocess-2nd-use/A1b-fold/A5-whitelist/exit-precedence 全紅）。
- **MED-1 read-only 結構斷言 NO-BITE（safety guard 失效）**：`test_db_session_read_only_and_timeout_params_present`(:531) `assert "default_transaction_read_only=on" in self.SRC` 對全檔文本——該字串同現於 MODULE_NOTE docstring :23。親證 mutation：只刪功能行 :467 `options=` kwarg（保 docstring）→ test 仍綠（1 passed）。即未來 refactor 拔掉真 read-only kwarg 而留 docstring，prod DB session 變 read-write 但守衛測試不紅，正反設計 §13.1「真實生效非註釋承諾」。現碼正確（:467 在），純 no-bite test-validity 缺陷。修法：mirror 同檔 watchdog_state.json 測試的 partition-at-`from __future__` 收窄，或 `inspect.getsource(_connect_readonly)` 斷言功能行。
- **MED-2 A2 游標 int() 截斷跨輪 MISS（違 §5.2 no-miss + §8.4-5 不漏新事件）**：alert_key=`a2:through_{int(max_alertable_ts)}`，cursor=max_ts(float)。同一整數秒內 burst alertable 事件被 cron read 切兩輪：首輪 alert key=through_X 且 cursor→X.a；次輪見 X.b(>cursor float)但 int(X.b)==X→同 key→should_emit False→該事件靜默吞，cursor 已過不重掃。親 repro 確認（NETWORK_OUTAGE 被吞）。crash-storm 多事件 <1s 聚簇正是 A2(MEDIUM-2 收口軸)守的形狀；首事件仍 alert 故非全盲（completeness miss 非 total-silence）。修法：key 用全精度 ts 或 batch hash，勿 int() 截斷。
- 教訓：「結構斷言查全文」的 grep-bite 殺手 = 被測字串同現於 docstring/MODULE_NOTE → 功能行 mutation 不紅。E1 自己已對 watchdog_state.json 做了 partition 收窄卻漏了 read-only 同款陷阱；驗 no-bite 必逐條只 mutate 功能行（保 docstring）看是否紅。time-keyed dedup 用 int() 截秒會把同秒不同事件併為同 key = 跨輪靜默漏。

## 2026-06-10 · re-E2 — L2 P2p incident_sentinel fix `1e2b094d` 2 MED 修復 → PASS to E4
- narrow re-review 限 fix delta：diff 恰 2 檔（key 1 行+註釋 / 測試收窄+1 新回歸）零漂移。三組 mutation 親重放全 bite（刪 options kwarg→:555 紅 / 刪 statement_timeout→:556 紅 / key 還原 int() 截秒→新回歸測試 :406 紅），各還原 status 驗空；bonus：刪 _connect_readonly docstring→:554 結構守衛 fail-loud 非 silent-pass。57+22 全綠。
- MED-2 修法對抗實證：float ts JSON round-trip 200k 樣本 0 fail（json shortest-repr 可逆，float(f"{ts}") bit-identical）、相鄰 ULP key 必異；新批 ts 嚴格 > 游標 ≥ 前批 max → 跨輪新 key 數學成立；A2 批次指紋 key 結構性退出 §5.2 re-alert 窗（安靜輪 ok→WARN key 靜默清）無歧義。NaN-ts 异型在新碼反而從 axis-error 升級為真 alert（watchdog 寫 time.time() 不可達，INFO 不退）。
- 教訓：double-partition 剝 docstring 的結構測試要正反兩 probe——功能行 mutation 紅（bite）+ docstring 刪除也要紅（fail-loud 守衛），缺後者會在重構時 silent 變形。

## 2026-06-10 · E2 — P2p sentinel A3 API base fix `bd324886` narrow re-review → PASS to E4
- 單檔 +16 wrapper Tailscale IPv4 解析；stub-harness 親跑 10 case（已設尊重/空字串視同未設/無 CLI/daemon-down/多行/trailing-space/CR/err-on-stdout/首行空/exit0空）全 rc=0；真實故障形（daemon down=非零+stderr+空 stdout）安全不設變數；髒輸出畸形 URL 僅 counterfactual CLI 行為可達且 m11:147 byte-identical 同形、下游 _safe_axis catch-all 兜成 axis_error WARN 非靜默 → INFO 不退。lib/api_bind_host.sh 親驗=server-bind 視角（吃 OPENCLAW_BIND_HOST、拒 0.0.0.0/:: return 2），E1 不 source 論證成立惟 sourcing 計數自報「三個」實為 5（漏 start_local/beta_quickstart，全 lifecycle 0 cron）。installer 不設 env 裁決=夠（cron 必經 wrapper；installer :112 --probe-alert 明示不跑監測軸；裸 python 留 loopback=by-design 可攜+互動可見）。
- 教訓：審 client 端 IP 解析 fix 用 PATH-stub harness 枚舉 CLI 異型（含 err-on-stdout）比讀碼快且決定性；「該不該 source lib」判準=lib 吃的 env 變數在目標場景有無語義（server-bind var 對 client 無義且引入 return-2 集合）。

## 2026-06-10 · E2 — P5-SM soak 監測基建（feat/p5sm-soak-observability @ 9dc533b0）→ RETURN to E1（2 HIGH + 2 LOW）
- 隔離性/零 mutation/三偏差（Production profile・V137 6 型・engine_mode）全綠全 ACCEPT（Rust `requires_lease`:131/acquire:413-421/bypass wire "bypass"/mainnet guard exit 7 全親驗）；242+2 復現（canary53/flusher25/healthcheck35+1s/bridge68/hub61+1s）；E1 兩 mutation redo 紅→綠。RETURN 因 `[82]` gate 算術兩個假綠：**HIGH-1 crash-loop（<30s epoch）重複 epoch_rollover 攜同一 V129 終值重複求和 → 成功率稀釋**（probe 親證 真值 94.8% 計得 99.21% PASS；dedupe key=detail.prev_canary_updated_at_epoch_s 已在手未用）；**HIGH-2 canary 死於 ~17h（500 probe）後收口不可見**（probe：31h 全黑 + 510 ok → PASS rate=1.0000；flusher 照 flush 保 V129 fresh=flusher-dead 軸不咬、停擺不產失敗=連段軸不咬；PA §5.3 case 3 在最關鍵區域未交付）。report: workspace/reports/2026-06-10--p5sm_soak_infra_review.md。
- 教訓：(1) 審「跨 epoch 求和」類 gate 必構造 **退化 epoch 序列**（短命 epoch 重讀未刷新快照=同值多計）——E1 的 per-支路 mutation 全 bite 但支路之間的算術組合縫隙 mutation 測不到，要用端到端合成場景 probe。(2) 「X 死 → gate FAIL」宣稱要算**偵測下限的參數區域**（floor=300s×0.5 → 死亡時點 >16.7h 即逃逸），不能停在「停擺支路有 bite」；aggregate floor ≠ 連續性證明。(3) flusher 與 canary 的活性是兩個獨立軸，共用同一 freshness 列會互相遮蔽（flusher 活著替死 canary 保鮮）。

## 2026-06-10 · re-E2 — P5-SM soak fix `0ce0874c` → ACCEPT(PASS to E4;2 LOW+3 INFO 不阻)
- 四原 finding 全閉實證:Probe A-hb 變體(加合規 heartbeat 隔離算術軸)FAIL `rate=0.9477/cum=860` 正中真值;Probe D-hb(31h 全黑+完整 flat hb 鏈=最對抗形態)由 (iii) 殺;dedup key 前提親驗=V129 UPSERT 每輪 `updated_at=now()`;修前紅親證 7+5;M1/M2/M3 mutation 全精準 bite;256+2 復現;diff 恰 7 檔零漂移。
- **M1b over-dedup mutant(key 削 updated_at)44 測試全綠存活**=test-pin 缺口(LOW):probe F 證 mutant 誤殺真實同計數雙 epoch(590→300 假 NOGO)。教訓:dedup fix 的 mutation 要雙向打——under-dedup(還原 bug)與 over-dedup(key 削弱)各一;後者測試常漏因 PASS-雙生只配 distinct-counts 場景。
- 殘餘縫隙量化(G-a:canary 死+sub-30min 重啟循環+600s 寬限內讀=逐讀 ~34% 逃逸;G-b/G-c 證 ≥600s 必殺)=LOW 非 HIGH 復發。教訓:合成事件 probe 必須遵守生產 ORDER BY 遞增——G 首版 while-loop 產未來時間戳亂序差點誤報 (iv) 不咬;構造後 assert ascending+≤NOW 當標配。

## 2026-06-11 · E2 — L2 P4 online-FDR 三線（e1a/e1b/e1c）合併前對抗審 → A PASS / B PASS-with-FIX(`3cdcc9ed` 去 or True 死斷言) / C RETURN（V137 號+[82] 號被 P5-SM 撞佔 = §5e 真 fire 抓到的 merge-blocker；+prh CHECK 頂層非 object 三值殘洞+double-seal warning 0 test bite）
- 實證：A 131p / B 家族 648p+4xf+P4 107p / C 28p+鄰接 147p 全親跑對自報；mutation 重放 2 組（debit 去 dsr 臂→2 紅、區間→點比較→恰 2 golden 紅）皆 bite；三向檔案交集=空；_FakeAwc/C fake 對 A 真簽名逐參數一致；B↔C binding 事件（operator_adjustment+demo_* 三欄 vs 不鎖 type 三欄齊備查詢）咬合。
- 教訓：(1) §5e 不是儀式——本次 review 中 P5-SM merge 進 main 佔走 V137 與 [82]，兩個「號碼資源」衝突 git merge-tree 只報 2 文本衝突（SCRIPT_INDEX/runner），**migration 版本號與 healthcheck 編號是 git 看不見的全局命名空間**，跨 branch 取號必須 merge 前夕再驗一次。(2) PG CHECK 的 `?`+`->` 對頂層 array/scalar 仍走三值放行——封 jsonb 結構洞必須先鎖 `jsonb_typeof(col)='object'` 再驗內層，「封死」宣稱要對頂層型別變體逐一推演。(3) log-only 觀測 delta（rowcount==0→warning）若無 caplog 測試=0 bite，§9.3 這類「以 log 為 gate 證據」的 AC 必須配一條 rowcount=0 測試。

## 2026-06-11 — P4 E1-C RETURN 修復 delta 複審 + f08 merge-seam 裁決 → PASS to E4（E2 seam commit `4d7a4d84`）
- 三 commit（merge `809bf568`/fix `e752e960`/micro `9f12a1d4`）全審 PASS：改號 V137→V138+[83]-[87] 殘留 0（單趟映射親 grep）；C-LOW-1 在 Linux PG 獨立 dry-run 實證（scratch db double-apply 0 ERROR；頂層 array/string/number/jsonb-null/array-ft/缺 key 六攻擊全拒+合法 object 不誤殺；N-1 三向+N-2 第二終局拒；dropdb 0 殘留）；C-LOW-2+stage0r 三向映射三 mutation 親跑全恰 1 紅（刪 warning 塊/fail 臂回退/字彙外落真值表）；A 線 demo_confirm_verdict 簽名與真值表逐臂對照（green=False 結構性不可 refund）；P5-SM 側零波及（V137 檔+模組 byte-identical，128p/1s 綠）；full 984p/16s（基線 980+4 算術核實）。
- **f08 裁決關鍵發現：紅非 C 引入**——main `7b5d92e9`（06-08 residual PART4 加 OPTIONAL_JOBS）即破 da2aba119 的精確等式，以 main 三檔隔離佈局親跑同紅實證。C 不違 F-08 意圖；按授權直修（組成封閉+DEFAULT 精確+逐 job flag-gate 釘）+ runner docstring inventory 補 [83]-[87]（LOW 直修），mutation 3/3 紅，commit `4d7a4d84` 已推 branch。
- 教訓：merge-seam 紅測先「對 base 雙親各自跑」歸因（main 版三檔搬 /tmp 同構佈局即可隔離實證），別默認是本線引入；pin-test 適配後必對自己的新斷言跑 mutation（rogue 來源/混 DEFAULT/偷加成員）證 pin 仍有牙。

## 2026-06-11 · E2 — bb regime observability `52727d82` 對抗審 → PASS to E4（0 需修 + 5 INFO）
- 小 diff 熱路徑相鄰審查全綠：同 snapshot 宣稱以全鏈引用追蹤親證（dispatch param :152→ctx :290→iter_ctx clone :374→on_tick :407→bb_reversion ind :421 vs persist 3 點同局部變數，`let indicators` grep 證無 shadowing）；HEAD lib 3791/0/1 親跑+diff test fn 計數算術閉 base 3787；雙 mutation 獨立打點（exchange site→None 結構測試紅 n=2、刪 hurst_value 鍵→恰 3 新測試紅）全 bite 全還原驗空。
- 可複用：E1「clippy 0 警告但整鏈被 pre-existing 阻斷」宣稱的驗法=複製同款臨時診斷修補（semver 1 行）→ 跑 clippy → grep `-->` 限本 diff 檔 → 還原驗空；順帶暴露 follow-up 真實 scope（修 1 行後又冒 4+1 個 deny-level pre-existing error，「1 行解鎖」單開小了要列全）。`details->>` 讀方 grep 要看 FROM 哪張表再判 PA「唯一讀方」宣稱（canary_promoter 讀的是 drift_events 非 intents，假陽性排除）。

## 2026-06-11 · E2 — subagent 四態契約生效
- 回報首行改 `STATUS: DONE|DONE_WITH_CONCERNS|NEEDS_CONTEXT|BLOCKED`+一行理由；E2.md 新增審查順序鐵律：stage 0=spec 合規（對照派工契約逐項查漏建/超建/誤解）先於代碼質量審查，不信實作報告、讀真代碼逐行對 spec。

## 2026-06-11 · E2 複審 — rtk pytest patch 修復輪(A 批 delta)→ PASS to E4
- HIGH 真閉:`is_bare_summary_line()` 整行文法錨定+last-match-wins;雙 mutation 親跑(first-match 回退→verbatim 測試紅;substring 回退→2 測試紅含原始餌 e2e)、原始餌+pre-footer 逐字餌真 pytest A/B 親跑全對;patch 套 pin SHA 後與 commit `32561a07` 樹 byte-identical;PR #2399 head SHA 對上+review-update 節誠實披露。E1 否決「Failures 段內禁捕」實證成立:`-q -rxX` RED run 0 條 short-summary header,footer 到達時 parser 必在 Failures 段。
- 新殘留(LOW,不擋):post-footer 文法精確餌(atexit 印 `99 passed in 0.01s`)可偷 headline 計數(親證 `Pytest: 99 passed, 0 failed`),但 exit code 透傳+Failures 細節仍在+全綠短形被 `failures.is_empty()` 防線擋;觸發需病態自傷代碼非意外散文,任何單趟文本啟發式無解,first-match 替代方案實證更差。教訓:審 parser 啟發式要分「意外散文觸發」vs「逐字對抗觸發」兩級定 severity;「真 footer 永遠最後」這類結構宣稱要用 atexit/post-process 探針實際打穿驗邊界。

## 2026-06-11 · E2 線 B — alert sink+BB 哨兵 / polymarket 軸 / analyze_token_usage / mnemopi 四件套對抗審 → RETURN to E1（1 MED + 3 LOW 待修；0 CRITICAL/HIGH，spec 合規全證實）
- RETURN 因：MED-1 engine_watchdog.py 觸碰前已超 2000 hard cap（2088→2150）無 documented exception/split follow-up；W-3 哨兵 format_alert 外部 title/url 不剝換行=log 注入面；P-2 state.py `entry["status"]` 直下標與 load_state fail-soft 不對稱；W-2 sink 失敗 debug 級+INFO 行說謊（sink 壞+無 creds 時稱已記錄）。全部 <20 行修。report: workspace/reports/2026-06-11--sentinel_polymarket_smallitems_review.md。
- 強項實證（可複用手法）：①「兩半測試拼接」（identity 斷言+emitter 行為測試）的 composition 縫用親跑全鏈 probe 補（run_once→真 resolve→alerts.jsonl 真落盤）；②外部腳本「會計修正」宣稱用自建合成 fixture 親證 bite（last-wins 99 非 104 / transcript 權威 300 非 toolUseResult 500 / fallback 互斥）；③smoke artifact 的 mtime 考古抓「修復前產物」——daily run 留 0-byte parquet 殘檔=清理 fix 之前跑的，宣稱的 cleanup 零測試覆蓋（manifest 記 failed 但檔在=正是註解警告的誤認場景）；④零外連聲明實物複驗 installed npm 包源碼（embed()/embedQuery() 入口短路 embedApi 不可達）。
- 教訓：多線並行批審先用 git status 分桶 ownership（本批 brief 還有路徑筆誤 helper_scripts/memory/mnemopi_seed*=E2-A 檔）；「未動 SCRIPT_INDEX/memory 紀律」這類 blanket 檢查項要對照各線各自派工契約再判（兩線契約明令登記=CONFLICT 交 PM 非 E1 違紀）。

## 2026-06-11 · E2 線 A — L2 記憶層 M1(V139+memory_distiller)+M2(cron 三件套+seed+manual V140+[88][89]) 對抗審 → RETURN to E1(M1)（1 MED+3 LOW；0 CRIT/HIGH）
- RETURN 因：MED-1 `OllamaEmbeddingClient` 全 repo 零非測試建構點（CLI 不傳 embed_client→run_backfill(conn,None) 恆 embed_unavailable）= `EMBED_BACKFILL` flag 有 gate 無 functional consumer（dead param）；**MIT 並行 ratify 線獨立收斂同 finding（F-1/C-3）=雙線交叉證實**。LOW：pipeline flag gate 無 .strip()（CLI 有）+target_date 模式 disabled 回 dict 不 raise=游標跳日邊角；event_start/end 永不被填（spec §2.1 死欄）；[88] 語義性死亡不可見（no-op/全 dropped 日恆推游標，MIT F-4 同源）。核心全綠：接縫簽名/游標紀律/fail 方向（extraction skip vs dedup fail-open-to-store）/supersede 白名單/原則 #7（寫面 grep 僅 agent_memory 兩新表）全實證；5 mutation 全 bite（dedup 方向反轉 9 紅/接縫 raise 削除 1 紅精準/merge 不 supersede 1 紅/supersede 偷改 content 2 紅/extraction 方向反轉 4 紅）；110+78+513+125 全綠親跑。report: workspace/reports/2026-06-11--l2_memory_review.md。
- 教訓：(1) **flag 審查三段式**——gate 存在→gate 位置正確→flag-ON 的 functional consumer 真可達；前兩段全對仍可能 dead param（本次 gate 完美但 client 零建構點，靠 caller-grep 抓）。(2) untracked 新檔 mutation probe 不能靠 `git status` 驗還原，必 cp+shasum 前後比對。(3) 兩線並行接縫審查=雙向親驗：M1 側 mutation 證鎖測試真 bite + M2 側確認其測試 mock 接縫（不重複鎖）=正確分工而非覆蓋缺口。(4) prompt 抄改審查對照原版逐紀律判「丟失 vs 結構性 N/A vs spec 有意替代」三類（merged_timestamps 並集被 source_refs 並集+supersede 鏈等價取代=第三類，非丟失）。

## 2026-06-12 · E2 — incident_policy dispatch trigger CORE+auth+Bybit review → PASS-with-CONDITIONS
- Reviewed existing source-live path, not remaining producers. Verdict `PASS-WITH-CONDITIONS`, 0 blocker/high/medium/low. Arm/notify split, push-secret downgrade, single armed owner, stale in-flight generation guard, current-class self-heal, and fail-soft sender/drop paths all match PA model.
- Important wording: Bybit producer is business-retCode fail-closed only; transport/parse/no-credentials are intentionally outside `RetCodeCounter`. `WatcherUnavailable` skips ledger accumulation, acceptable fail-soft because watcher is boot-wired, but not a substitute for watcher boot healthcheck.
- Ticket remains partial until `sm_halt_stuck`, `position_drift`, and external `engine_dead` producers are wired or explicitly descoped; after coverage decision it still needs E4/QA/full-chain review before runtime-complete claim.

## 2026-06-12 · E2 follow-up needed — sm_halt_stuck source slice landed
- PM/E1 source update added `event_consumer/sm_halt_incident.rs`: active HaltSession is detected from `TickPipeline.halt_kind` + `halt_set_ts_ms`, observed after tick handling and after the 60s lease/auth sweep, fed into existing `IncidentClass::SmHaltStuck`, and resolved when halt clears.
- This is not yet E2-reviewed. Review delta should check: stale `[69]` healthcheck selector is not used; operator IPC pause (`halt_kind=None`) cannot report; producer 5s cadence does not bypass incident_policy 120s sustained/throttle/cooling; self-heal cannot disarm another armed class; C4 owner-handler boundary remains unchanged.
- Remaining producers after this source slice: `position_drift` notify-only and external `engine_dead` watchdog notify-only.

## 2026-06-12 · E2 follow-up needed — position_drift notify-only source slice landed
- PM/E1 source update added `position_reconciler/incident.rs`: post-reconcile unresolved actionable drifts feed `IncidentClass::PositionDrift` only after 3 consecutive cycles; MinorDrift ignored; startup grace suppresses accumulation; clear emits class-scoped resolved.
- This is not yet E2-reviewed. Review delta should check: producer observes the post-orphan/post-ghost drift list, cannot report handled orphan/ghost entries, does not bypass `IncidentClass::PositionDrift` notify-only policy, and does not add any `PipelineCommand` / RiskGovernor / DB / auth / order side effect.
- Remaining producer after this source slice: external `engine_dead` watchdog notify-only. Ticket still needs E2 review for `sm_halt_stuck` + `position_drift`, then E4/QA/full-chain before runtime-complete.

## 2026-06-14 · E2 — ADPE reward_source arm_id vocab fix 對抗審 → PASS to E4（0 finding 待修）
- 範圍：ADPE 全 untracked greenfield（`git diff` 空，直接讀檔非依賴 diff）。delta=reward_source.py:198 reconstruct line（`alloc_arm_id = make_arm_id(alloc_regime, strategy)` 取代 raw view passthrough）+ test 5 新增/2 改 vacuous。root cause 親讀 V031:326 證實：view `linucb_arm_id = regime_norm('trending'/'mean_reverting'/'random_walk') || '__' || strategy_name_norm`，allocator VALID_REGIMES={bull,bear,high-vol,chop,range,insufficient_context} 與 view regime 詞彙不相交 → 原樣沿用會被 `_discover_candidate_arms` 的 `if regime in by_regime` 靜默丟棄 → 真路徑永遠 flat。
- **mutation-bite 親跑（強於 E1 自報）**：revert reconstruct→`str(view_arm_id)` → **4 紅**（E1 報 2）：test_e2e_real_view(精確重現 `desired_active={}, candidate_decisions=[]` flat 簽名)+test_e2e_vacuous_guard+test_reward_source_sql_is_demo_scoped+test_reward_source_skips_null。restore→22 綠，`git status` 驗 0 殘留。
- 非 vacuous 證實：test (A)(B) 走真 `fetch_demo_arm_rewards`（fake cursor 餵真 V031 schema row）→ run_cycle → discover/allocate，非手造 ArmReward。7 處 fetch call-site。
- 全綠（實證）：round-trip 對全 6 VALID_REGIMES clean（含 `high-vol` 連字號，因 `_ARM_SEP='__'≠'-'`）；`engine_mode_scope('demo')==('demo',)` 無 live/live_demo 洩漏，SQL param 驗 `['demo']`；0 hardcoded path；0 forbidden hard-boundary token（唯一命中=負向測試名 mainnet_like）；reward_source 唯一 importer=runner+test；70 passed 全 ADPE 家族（allocator/thompson/maker 0 regression）。§5 race 0/0、無 sibling 觸 ADPE scope。
- INFO（結構性不可達非 blocker）：`_strategy_from_view_arm_id` 對 flat-arm（`__flat_cash__`→parse 回 strategy=`__flat_cash__`）與 3-part arm_id（strategy 含 `__`→ValueError fallback strategy_name 欄）皆 graceful；但 view linucb_arm_id 由 5-enum strategy_name_norm 構造（V031:325，無 `__`、永非 flat），兩路 production 不可達。
- 教訓：(1) greenfield untracked fix 的「delta」靠讀檔+知道 reconstruct line 位置，`git diff` 對 untracked 是空的會誤判 0 改動。(2) mutation-bite 親跑可能比 E1 自報抓到更多紅（本次 4 vs 2），多出的紅是 reward_source 層直接斷言 reconstructed arm_id 的單元測試——驗證面更廣是好事，不是 E1 漏報缺陷。(3) vocab-consistency fix 的 round-trip 證法=遍歷全 enum × strategy 跑 make→parse 比對，特別查含特殊字元的 regime 值（high-vol 連字號）是否與 separator 衝突。

## 2026-06-14 · re-E2 — cost_gate double-deduct healthcheck（HIGH-1 per-cell fix）對抗複審 → PASS to E4（0 finding 待修；2 INFO + 報告計數訂正）
- 範圍：6 檔 cost_gate scope（standalone check + standalone test + wrapper + wrapper test + runner.py + __init__.py；SCRIPT_INDEX [90] 條目已在 HEAD）。HEAD==origin/main `48cdbfe2`，髒樹是多 session WIP（m4/closed_pnl/risk_routes/layer2_routes 等 file-scope disjoint 不碰）。
- **HIGH-1 per-cell 修真閉（核心軸全綠）**：`cell_threshold_bps(cell_win_rate)` clamp(wr,floor,1.0) 與 gates.rs L42-45/215-218/326-329 `cell.win_rate.clamp(floor,1.0)` 逐字對齊；mutation（cell_threshold 改回固定 floor）親跑→3 紅（per_cell_win_rate_weighting / win_rate_shrunk_precedence / threshold_uses_win_rate_above_floor），restore→21 綠，git diff 驗空。bps 軸關鍵推理：healthcheck `_cell_runtime_bps`（runtime_bps 優先、fallback shrunk_bps）== Rust `cell.shrunk_bps` 的 populate 來源（edge_estimates.rs L158-162 同 fallback order），且 gate 三路全比 `cell.shrunk_bps < threshold`——命名叫 runtime_bps 但讀同一值，非 divergence。win_rate clamp [0,1]→[floor,1] 與 Rust deserialize-clamp 再 gate-clamp 數學等價。
- **驗證實證**：standalone 21p + wrapper 7p = 28 全綠（親跑）；canary 全套 463p、db passive_wait 405p+1s 零回歸；py_compile 6 檔 OK + runner import smoke OK + __all__ 全 importable。真 config CLI 三 env PASS（demo fee_bps=26/floor-thr=112.67、live 24/104、paper 34/147.33），dormant 非 vacuous（真 edge_estimates.json 1 cell、0 validated-positive，scan 真迭代後過濾）。0 hardcoded path、0 bare except、0 detail=str(e)、0 f-string log、MODULE_NOTE 全在、中文優先。
- **★ 報告自述不實（非 defect，記錄供 PM）**：brief 稱「未碰 __init__.py」「runner.py 僅 L382/L1569 兩處註解訂正」——實際 __init__.py 加了 import+__all__ re-export、runner.py 加了真 wiring（import block + post-cursor [90] + call-site + docstring inventory）。這些 wiring **正確且必要**（否則 check silent-dead 不可達，承教訓#7），但 brief 嚴重低報改動範圍。E1 自報數字也錯（wrapper 8→實 7 def，brief 稱 7 對但列檔漏了 test_cost_gate_double_deduct_healthchecks.py 本身）。
- **INFO-1（不阻）**：`compute_gate_params` 只擋 win_rate_floor<=0（除零），未擋 negative safety_multiplier / negative slippage default_rate → 負門檻 → 靜默 under-report。但此類 nonsense config 會讓真 gate 同樣壞（同公式），healthcheck 忠實鏡像 gate 行為非 divergence；且 monitor 非 config-validator，超出 stated scope。**INFO-2**：runner `_emit_results` 用 `{status:4s}` 格式，INSUFFICIENT_SAMPLE（19 字）會撐欄錯位（cosmetic，min-width 不截斷）。
- **狀態不一致（記錄）**：SCRIPT_INDEX.md [90] 條目已在 HEAD committed，但 impl/runner wiring 仍 uncommitted → 索引先於實作落地（prior round artifact）；內容正確對齊 impl，非本輪 defect，PM commit 時留意。
- §5 race 5/5 PASS：5a/5e HEAD==origin/main `48cdbfe2`，2h 窗 sibling 全 ADPE/L2-docs（file-scope disjoint）；5b cost_gate 6 檔全 task scope 無外洩；5c 3 舊 stash 不碰；5d N/A 未 commit；mutation /tmp cp-backup-restore 非 git destructive，status 驗淨。
- 教訓：(1) 「per-cell vs per-env 標量」對齊審查 = 直接對 SSOT（gates.rs）逐 clamp 比公式 + mutation 改回 buggy 標量看真 test 紅，比讀 docstring 可靠。(2) 命名漂移（healthcheck 叫 runtime_bps / Rust 叫 shrunk_bps）要追 deserialize 的 populate 來源證讀同一值，別因名字不同誤判 divergence。(3) brief 低報改動範圍（漏報 __init__/runner 真 wiring）必用 `git diff <file>` 逐檔親驗，不信「未碰 X」自述——本次 wiring 反而是正確的可達性接線，漏報但無害。

## 2026-06-14 · E2 — P1-SCHEMA-1 schema-consumer contract test 對抗複審 → RETURN E1（1 CRITICAL：full-tree apply 在 virgin DB 必 RED，整個 test DOA + 1 MEDIUM audit no-op gate）
- 範圍：新 `schema_contract_test.rs`(300)+ci.yml schema-contract job(+59)+audit_migrations.py(+3 cwd candidate)。HEAD==origin/main `48cdbfe2`，§5 race 5/5 PASS（3 舊 stash 不碰、無 sibling 觸 scope）。髒樹其餘 M 檔=他 session WIP（file-scope disjoint）。
- **★ CRITICAL（親跑實證，非讀碼）**：Mac 本機 Homebrew PG16 可達（role ncyu），建 virgin DB 跑 → `MigrationRunner::run_if_enabled` 全樹 apply **在 migration ordinal 5（V005）hard-fail**：`relation "public.account_snapshots_legacy" does not exist`。根因：**無任何 migration CREATE base `public.account_snapshots`**（grep 證 0 hit），V005 PART3 RENAME `public.account_snapshots`→`_legacy` 是 `IF EXISTS` guarded（virgin DB skip），PART4:414 卻**無條件** `CREATE OR REPLACE VIEW ... FROM public.account_snapshots_legacy` → virgin DB 必崩。psql 手動 apply V001-V004 OK、V005 line414 ERROR 乾淨重現（非 timescaledb 問題，V001-V004 無 timescaledb 也過）。CI `contract_db` 是 fresh ephemeral（ci.yml grep 0 legacy-seed step）= 正是 virgin case → schema-contract job **第一次跑就 RED**，6 個 contract probe **全不執行**（皆 gate on `migrated_pool()` panic）。E1 OWED「CI 首跑 V001-V139 全樹真 apply 綠」=偽，整個 test DOA。
- **為何 migrations_test.rs test#4 fresh_db 沒抓到**：`wipe_app_schemas` 只 drop learning/trading/market 等 app schema，**不 drop `public.account_snapshots_legacy`**（prior 真 apply 殘留）→ 整合 DB 非真 virgin，V005 RENAME 找得到舊表；且 test#4 gated behind OPENCLAW_TEST_PG_DESTRUCTIVE=1（多半沒跑）+ 只斷言到 V023。新 test 對**真 fresh ephemeral** 跑全樹是首次暴露此 V005 brownfield 假設。
- **6 表 INSERT/SELECT 對 schema+real-caller 全對（這部分設計正確）**：case1 data_quality_events 與 quality_writer.rs:98 **byte-identical**；case3 exit_features 與 exit_feature_writer.rs:123 全 18 欄同序（ON CONFLICT DO NOTHING vs 真 DO UPDATE=安全簡化，key 同）；case4 decision_outcomes 與 outcome_backfiller 10 欄同；case5 model_registry SELECT 與 registry.rs:227 byte-identical；case6 klines 鏡像 backfiller LATERAL 子查詢。engine_mode='demo' 過 V077 CHECK（IN paper/demo/live/live_demo）；fills case 用 16/26 欄 subset（被省的 fee_rate/slippage_bps 等 rename 不會被抓=覆蓋面有限但非錯）。SKIP 路徑親跑 6 passed（env 未設零回歸）；--no-run 編譯 clean（僅 pre-existing warning）。
- **MEDIUM（audit no-op gate）**：`audit_migrations.py` main() 結尾**無條件 `return 0`**，gap 只 print 不影響 exit code → ci.yml「Migration-declared vs DB audit（反向檢）」step **永不 fail**，column drift 印出來但 CI 仍綠。與「補 consumer-test 另一向反向檢抓 drift」的 stated purpose 矛盾=假閘。cwd candidate +py_compile OK、YAML 合法、PR-only 成本紀律（zero macOS 10x）皆正確。
- 教訓：(1) **「對 fresh ephemeral 跑全 migration 樹必綠」是強假設，必親跑 virgin DB 證**——本地 Homebrew PG（即便無 timescaledb）能在 ordinal 5 抓到 brownfield-only 的 V005 VIEW 依賴，比信 E1 OWED「CI 首跑會綠」可靠太多；timescaledb 缺席不擋 V001-V004，依賴型 fail 與 extension 無關。(2) 整合測試的 fresh-DB case 若 wipe 不徹底（漏 public.*_legacy）+ destructive-gated 少跑，會養出「全樹可 apply」假象；新 contract test 對真 virgin 暴露真相。(3) advisory 反向檢若 return 0 恆綠=假閘，stated「抓 drift」必驗 exit code 真會 nonzero。

## 2026-06-14 — PERF-1 Phase A (5m 指標 bar-close gated 快取) E2 對抗審 → RETURN E1（1 LOW dead-import；核心正確性全綠）
- 範圍：6 檔 PERF-1（klines.rs +25 / tick_pipeline mod +26 / ctor +8 / on_tick_helpers +53 / step_4_5_dispatch +6-1 / tests/mod +2）+ 新測檔 perf1_indicators_5m_cache.rs(348L,6 test,untracked)。未 commit，HEAD=origin/main=48cdbfe2。
- 核心正確性實證：epoch key (last_closed_open_time_ms, ewma_lambda) 精準涵蓋 compute_indicators_for_timeframe 唯二輸入。追 data-flow：get_ohlcv→buffer().ohlcv_arrays() 只讀**已關閉** bars；building bar 在 Aggregator.current_bar（與 KlineBuffer.bars 分離），唯有新 close 才 buffer.append 推進 bars.back().open_time_ms→同 bar 內 cache clone 與重算 bit-identical。IndicatorSnapshot 純值 struct(Clone/Serialize,無 Rc/Arc/RefCell)→clone 深拷,serde_json 比對為有效 proxy。scope fence 成立：indicators_5m by-ref 用於 dispatch:277/295/381,bb_breakout 讀 cache Donchian 仍每 tick 比 live ctx.price,只換值來源非消費方式,無 backtest/live divergence。
- mutation bite 親跑 3/3（cp backup→mutate→test→restore，0 residue）：①drop lambda→hot_reload 測試紅 ②cache default on None→never_cached 紅 ③gate 忽略 open_time_ms→new_close 紅。E1 只報 2,我加驗 open_time_ms 第三條。
- 標準：perf1 6/6、engine lib 3823/0、core lib 426/0（對 E1 報告）。0 硬編 path、0 unwrap/panic/unsafe 在 production diff。handlers_config.rs(+13 LIVE RiskConfig audit-log)=parallel WIP,E1「0 touch」屬實。兩新 HashMap 是 struct field 非 mutable singleton(§3.7 不適用)。
- **唯一 finding LOW**：perf1_indicators_5m_cache.rs:23 `use ...{Strategy, StrategyAction, StrategyParams}` 的 StrategyParams 未用→唯一新 unused_imports warning。Strategy(trait for .on_tick/.update_params)+StrategyAction(:203 matches!) 皆用。RETURN 給 E1 刪 StrategyParams（單行）。
- §5 race 5/5：fetch 前後 origin/main=HEAD=48cdbfe2,review 期間 0 sibling push 進 PERF-1 scope;髒樹他 session 檔(ADPE/L2/AI-E reports)未碰;mutation cp-backup 非 git destructive,還原後 diff stat 不變。
- 教訓：cache-gate 類審查命脈=證「epoch key ⊇ 被快取函數的全部 varying 輸入」,須讀被快取函數 body 逐輸入對 key,且追「同 bar 內哪些輸入會變」(building bar vs closed buffer 分離是此處關鍵);clone bit-identical 須驗 struct 無內部可變性。

## 2026-06-14 GUI-FIXES (A3 三項 MEDIUM) — E2 PASS to E4
- (1) Paper 死按鈕 / (2) engine_alive 第一屏 / (3) Earn 術語白話化：純前端 Vanilla JS，3 spec 項全核對 OK，無 finding。
- 對抗核驗證據：後端 410 確認 paper_trading_routes.py:678 (_RC10_DISABLED_MSG)；engine_alive path d.data.runtime.engine_alive 解析正確 (openclaw_routes.py:230 facts→965 data.runtime)；color class .green/.red/.yellow 由 common.js:756-757 ocInjectBaseCSS 注入(非 dead class)；Stage 0R 狀態比較 _state.stage_0r_status!=='PASS' 未被顯示文字改動破壞；node --check 5 檔(含 inline HTML script)全過；earn badge flex-wrap 友好(tab-earn.html:111)1366 不裁切；submitPaperOrder defensive ocToast warn fail-closed，無 fake-success；無硬編路徑/XSS innerHTML 新增。
- 環境註記：dirty multi-session 樹(大量 ADPE/cold-audit sibling 未暫存檔)，5 GUI 檔全屬本任務 scope；HEAD==origin/main(0/0)，sibling 2h push 無觸 5 檔；未做任何 git 寫操作。

## 2026-06-14 · E2 — SEAM-GUARDS-RUST 三 seam 降級守衛 → PASS to E4（0 finding；1 commit-scope 警示）
- 範圍：4 檔（commands.rs submit_external_order early-reject+docstring / method_registry.rs module docstring / strategy.rs as_str 註釋 / submit_order_tests.rs 新 fail-closed test），diff 56+/4−，與摘要逐字相符。
- 對抗核驗（親跑非採信）：① `effective_engine_mode()=="live"` ⇔ 僅 (Live,Mainnet)（mode_state.rs:42），LiveDemo/Demo/Testnet/None 折疊 live_demo/live_testnet **不被擋**——guard 精準鎖真錢。② guard 是函數**第一句**（commands.rs:187，先於 paper_paused/qty/price/atr/process()），真早退零副作用。③ 唯 2 production caller（lifecycle.rs:187 SubmitOrder + IPC handle_submit_paper_order）皆委派此 single sink，guard 覆蓋兩路。④ **自做 mutation**：`if false &&` 短路 → 新 test `assert!(result.is_err())` 紅 → 還原綠（0 殘留）；mutant 同時證 guard 是 Live+Mainnet 唯一阻擋（價/ATR/授權齊備仍過 guard 才被擋）。⑤ no-false-positive：happy_path 走 Paper（with_balance→Paper→"paper"）過同函數**綠**=demo/paper 模擬不被誤殺。⑥ method_registry docstring HMAC SEC-08 為真（connection.rs HMAC-SHA256 verify_slice）；registry=IpcMethodSpec{name,readonly,slot} 純描述被 dispatch.rs 只讀 slot-readiness 非授權→「NOT enforcement surface」準確。⑦ strategy.rs 註釋準：find_strategy_mut→None→Err"strategy not found" fail-closed（strategy_params.rs:117/131/145 親讀證）。
- 測試：build 綠（3 pre-existing dead-code warning 無關）；submit_order 5 / method_registry 3 / strategy_params 16 全綠=與 E1 報告數字逐項相符。0 硬編路徑；註釋中文優先；fail-closed 加強。
- **commit-scope 警示（非本 4 檔 defect）**：dirty 樹混 2+ sibling workstream——PERF-1 5m-cache（mod.rs/on_tick_helpers.rs/pipeline_ctor.rs/step_4_5_dispatch.rs/tests/mod.rs+untracked perf1_indicators_5m_cache.rs）與 ai_budget/pricing.rs+handlers_config.rs+untracked schema_contract_test.rs。PM 統一 commit 時須 `git commit --only` 僅含 SEAM-GUARDS 4 檔，勿掃入。§5b/c 照例不碰不 revert sibling。
- §5 race：fetch 前後 HEAD==origin/main==`48cdbfe2`，2h 窗 0 sibling push，review 中 0 新 push；mutation cp-free 用 Edit 後逐次驗 `if false` 計數歸 0。
- 教訓：「guard 是唯一阻擋」型 fix 的 mutation-bite 兼證兩事——test 非 vacuous + 確認沒有第二道隱性防線在替它兜底（短路後流程真的走下去）；no-false-positive 靠既有 happy_path 走非 live kind 過同函數的綠燈白嫖驗證，免另寫 demo test。

## 2026-06-14 — AI-E P2 ai_pricing 雙副本不一致修復（3 檔 named scope）E2 對抗審 → PASS to E4（0 blocker；3 INFO/LOW pre-existing-or-flagged）
- 範圍：ai_pricing.yaml(+28/-6)/layer2_types.py(MODEL_IDS opus-4-7→4-8)/pricing.rs(Test8 spot-check 換現行+加 sonnet-4-5 is_none)。全 unstaged，HEAD=origin/main=`48cdbfe2`(0/0)。
- **裁決 PASS**：spec 逐項一致；YAML 合法 active=8(>=5 OK)；opus-4-8/sonnet-4-6 真名+價(5/25,3/15)與既有 Python manifest **三處(layer2_types PricingTable / provider_pricing_catalog / provider_model_catalog)** 一致 → **非 E1 本次臆造**（PricingTable opus 早已 5/25 pre-existing，E1 只把 YAML 對齊既有 manifest）；opus-4-8 真名與我自身 exact model id 一致佐證。
- **★ load-bearing 確認**：`provider_client.py:259 model=self._model_id(tier)` 真送 MODEL_IDS[tier] 給 Anthropic API → opus 真名改 4-8 是 runtime-critical（API 不認字串會 400）。MODEL_IDS[MODEL_OPUS] 自動傳遞修正 PricingTable.opus.model_id（py import 親證）。
- **★ Rust Test8 mutation-bite 親證有牙**：flip YAML sonnet-4-5 active false→true + OPENCLAW_PRICING_PATH override → Test8 `is_none()` 斷言 **panic@pricing.rs:284 紅**；還原→綠。cp backup 還原 git diff clean 親驗。
- **★ Test8 標準 cargo test 下是 no-op skip**（pre-existing 設計非本次引入）：CWD=package dir(rust/openclaw_engine) 無 settings/ai_pricing.yaml → `default_path().exists()`=false → early-return skip。要 bite 必 `OPENCLAW_PRICING_PATH=<repo yaml>` 或從 repo root 跑。INFO 標 PM/E4：CI 真要驗 YAML 需設 env 或調 CWD。
- **不破既有 lookup（中性）**：Python cost lookup key=tier 別名("opus")via `_pricing.models[model_tier]`；Rust pricing.rs lookup key=真名。`_sync_to_rust_budget(model=model_tier)` 一直傳別名"opus"給 Rust→Rust YAML 真名鍵查不到→Err"unknown model pricing"→fail-closed cost=0。**此 sync-leg pre-existing broken（F-C/F-D 範疇，E2 摘要待核#4 已標），本次既不修也不破**。
- **out-of-scope 確認(E1 已正確 flag)**：provider_pricing_catalog.py:64 `claude-opus-4-7`=live Python 唯一剩餘 stale anthropic 真名（layer2_routes:416 refresh_pricing_if_needed live caller）→ 真 drift 但超 named scope，選項1(SSOT 合一)vs 選項2 留 PM。F-B Rust 硬編(tasks/consumer_loop/client.rs)+F-D fail-OPEN 同未碰。
- **INFO pre-existing**：PricingTable.last_verified_date 仍硬編"2026-04-16"(本 diff 未碰)→Python is_stale()早回 True(自~05-16，距 30d 門檻)，但 is_stale 是 advisory(只進 to_dict 給 GUI 顯示非 hard gate)，非本次 finding。
- **測試全綠**：9 Rust pricing test + 16 Python(test_layer2/test_provider_keys_store pricing/model/catalog) pass。
- **§5 race**：5a/5e 兩次 fetch origin/main=HEAD=`48cdbfe2`(0/0) 無 sibling push 進本 3 檔 file scope；working tree 50+ dirty 檔=多 session 共享髒樹(ADPE/L2 sibling WIP，已在 origin)，§5c 不碰不 revert；本 3 named 檔 diff 逐行確認 0 夾帶外來改動。
- 教訓：(1) 「同名異義 lookup key」要分別追兩語言——Python cost 用 tier 別名、Rust 用真名，model_id 字串改名只 load-bearing 在「送 API」與「Rust YAML lookup」兩路徑，不在 Python cost lookup。(2) load_default 類「讀 CWD 相對檔」的 test 在 cargo test(CWD=package dir)下會 silent-skip；驗 collateral 必 env-override 強制讀真檔才有 bite。(3) 「價格非臆造」最快證法=grep 既有同欄 manifest 多副本看 E1 是否對齊既有值 vs 引入新數字。

## 2026-06-14 — SIZING-RIGOR（dynamic_risk_sizer UP-LCB 顯著性 gate + kelly Wilson/R-haircut shrinkage）E2 對抗審 → PASS to E4（0 blocker；2 INFO pre-existing）
- 範圍 9 檔 unstaged，HEAD=origin/main=`48cdbfe2`(0/0)；其餘 50+ 髒檔=ADPE/L2/profit sibling WIP（file-scope disjoint，§5c 不碰）。
- **裁決 PASS**：spec 逐項一致（UP 用 LCB=SR−z·SE,SE=sqrt((1+0.5SR²)/(n−1)) + 硬 floor max(min_trades,sig_min_trades)；DOWN 不 gate；compute_sharpe 數學 byte-identical 未動；last_sharpe_annualized 純遙測 map；validate 追加 sig_z>=0 finite/sig_min_trades>=min_trades；Kelly W 改 Wilson LB clamp[0,W] 只縮、R shrink-toward-1 default haircut=0 不動；FIX-27/max_fraction/vol-adjust 全保留)。
- **★ 雙 mutation-bite 親證有牙**（cp backup→mutate→紅→restore→git diff clean 親驗）：① drs UP-gate 改「忽略 LCB 直接比 point」→`sig_gate_blocks_marginal_up_but_off_fires` 紅（Some(0.035)≠None）。② kelly wilson_lower_bound 改「return w(point)」→3 紅（shrinkage_only_reduces / fix27_reject / wilson_properties）。gate/shrinkage 真 load-bearing 非 vacuous。
- **★ 只增保守數學自洽**：marginal 33W/17L → SR=0.334,SE=0.147,LCB=0.093（test 註解值親算吻合,passing test 編碼正確值）；W_lcb<=point 嚴格→FIX-27 只會更常拒；n−1 除數安全（min_trades>=2 validate 守 + sig_min_trades>=min_trades → up_floor>=2 → LCB 分支時 n>=2）。
- **★ 接線非對稱（關鍵 framing）**：Kelly 側 **live-reachable**——`risk.kelly` TOML → KellyTierConfig(serde default,3 新 key)→ `RiskConfig::validate` 親 gate(line288 self.kelly.validate())→ `KellyConfig::from_risk_config`(intent_processor:877 hot-reload 重派)→ compute_kelly_qty(router:398/895)。dynamic_sizer 側 **runtime 仍走 ::default()**（pipeline_ctor:162,**pre-existing** 無 TOML→sizer 接線,非本次引入）→ live TOML sig_min_trades=100 只是 validate-surface 契約非 runtime delta；但 default sig_gate_enabled=true+sig_min_trades=50 → runtime gate **仍以保守 default 生效**（feature 非全 moot）。
- **三環境對稱+sentinel**：paper `[dynamic_sizing]` 只 `enabled=false`，其餘全 serde default(min_trades=50/sig_min_trades=50 → validate 過)；sentinel `if cfg.dynamic_sizing.enabled` guard 正確跳過 paper UP-floor 斷言。live min_trades=100=sig_min_trades=100（100>=100 過）。sentinel 真讀三檔真 TOML validate（env!(CARGO_MANIFEST_DIR).pop().pop() 與同檔既有 4 處 dyn-stop test 同範式,portable 0 硬編）。
- **INFO-1（pre-existing,非本次）**：`cargo clippy -p openclaw_engine` 在 rust 1.95.0 不過——dep `openclaw_core/src/risk/price_tracker.rs:132 since="2026-04-22"` 觸 clippy::deprecated_semver error（該檔 worktree unmodified,committed HEAD 即有=toolchain-version 敏感,非 scope）。**新碼本身 0 clippy warning**（RUSTFLAGS=-A deprecated_semver 後 grep 三 scope 檔僅命中 kelly_sizer:9-15 pre-existing MODULE_NOTE doc_lazy_continuation,新增行 0 hit）。E4 Linux toolchain 可能不同,標待 E4 確認。
- **INFO-2（pre-existing pattern）**：runtime `KellyConfig::validate`(kelly_sizer.rs) **0 production caller**（只 test 用,G7-01 起即如此）；load-bearing gate=authoritative `KellyTierConfig::validate` via RiskConfig::validate(已 wired)。新 shrinkage validate 加進 runtime struct=defense-in-depth mirror 既有 pattern,非新洞。
- **全綠**：engine lib **3845 passed/0 failed**（含 9 drs+7 kelly+4 config-contract+1 sentinel=21 新 test）；§3.1 跨平台 0 硬編 user path；註釋中文優先（英文僅 formula/identifier）。
- 教訓：(1) **「sizer config 接線」要查 ctor 是否真灌 TOML**——dynamic_sizer pipeline_ctor 用 ::default() 忽略 TOML（pre-existing）,TOML live override 對 runtime 無效,但 conservative default 仍生效救場；判 feature 是否 moot 要分「TOML→runtime 是否接」與「default 是否已保守」。(2) clippy 0-warning 宣稱在髒/多版本 toolchain 下要分「新碼 0 warning」（grep 新增行範圍）vs「整 crate clippy 過」（會被 pre-existing dep error 擋）——後者非本任務責任,前者才是判據。(3) Kelly/dynamic 雙 validate 結構：authoritative TOML 結構的 validate 必 wired 進 RiskConfig::validate 才 fail-closed,runtime mirror struct 的 validate 是 test-only defense-in-depth。

## 2026-07-03 — Part 2 post-approval drift gate (IMPL-B, 未 commit) E2 對抗審 → RETURN E1（2 HIGH + 2 MED + 2 LOW）
- HIGH 全靠親手 temp-repo 構造抓到,單元測試/真實回放全綠也擋不住:(1) **binary rename R099 under docs → EXEMPT**(numstat rename 用 curly-brace `docs/{a=>b}` 與 name-status path 永不匹配,binary flag 不觸;bug 在復用的 v734 `_binary_paths_from_numstat`);(2) **gitlink(submodule)A/M under docs → EXEMPT**——實測 gitlink numstat 是 `1 0`/`1 1` 而非 `-`,spec 自己的「numstat 出現 -」偵測配方對 submodule 根本無效(spec recipe 錯,實作忠實繼承);修法=改用 `git diff --raw` mode bits(160000/120000 deny)可一併關 symlink-add。教訓:**「diff 內容歧義」類 guard 必須對 rename/gitlink/symlink 三種非常規 entry 親手構造 temp-repo 驗證,numstat "-" 只覆蓋 blob-binary 一種**。
- MED:fnmatch/startswith 大小寫敏感 → `docs/evil.TOML`/`docs/.ENV.x` 繞 basename hard-deny(修=casefold);tests-segment 豁免寬於 spec 列舉(newdir/tests/** 也 exempt,spec step2 字面 vs step4「新增未知頂層目錄」內部矛盾→escalate)。刻意放寬(dirty/HEAD≠origin 不阻斷)有測試釘住(mutation 反向驗證紅),方向正確未退。
- **Re-review 2026-07-03 → PASS to E4(APPROVE_WITH_NITS)**:E1 修全落地——`collect_mode_aware_diff_inputs`(--raw mode bits + --no-renames numstat)關 HIGH-1/HIGH-2/LOW-1;原 4 攻擊構造親重放全 ROTATED(binary-rename-R099/gitlink-A/gitlink-M/symlink+docs/evil.TOML);fail-closed 邊親證(quoted unicode symlink/非 repo 採集失敗/None/型別攻擊/parse-fail 全 blocker);mutation A/B/C 親跑各 1-2 紅(mode-deny 清空/unmatched-blocker 移除/四家族放寬);96 passed;v734 兩檔 `git diff` 空輸出親證 0 改動。3 NITs 不阻:SCRIPT_INDEX 條目未同步四家族收緊(doc 比 code 寬=deny 向)、MODULE_NOTE「E2 修復」歸屬措辭、main() mode-aware 用 symbolic origin/main 有極窄 TOCTOU(建議改傳 resolved sha)。

## 2026-07-03 — v734 guard 共用 collector 三假陰性修復（E1 IMPL，未 commit）E2 對抗審 → RETURN E1（1 MEDIUM：unmatched 安全網 wiring 零測試釘住）
- 代碼本身裁 SOUND：親手 temp-repo 重放三攻擊（binary rename/裸 gitlink/symlink under docs），舊碼 READY 假陰性（gitlink 僅 worktree_dirty 偶然擋，與 E1 自述一致）、新碼全 BLOCKED，diff.renames on/off 皆同；bonus：typechange T(file↔symlink) under docs 舊假陰性也被 raw mode 層順帶關閉。無誤攔：chmod 644→755 / docs text rename / docs 純文字修改皆 READY；unicode binary rename 三路引號一致、精準 blocker、安全網不誤觸。方向紅線 0 違反（全案例 NEW blockers ⊇ OLD）。
- mutation 4 跑 3 咬 1 逃（cp backup→mutate→test→restore，sha 逐次驗還原）：A(SAFE_FILE_MODES 加 160000/120000)→2 紅；C(去 --no-renames)→1 紅（靠安全網轉 binary_or_mode_path_unmatched 才紅）；D(去 raw mode 層)→2 紅；**B(刪 unmatched 安全網 wiring guard:364-373)→四 suite 96 全綠=逃逸**。複合 B+C 親證 binary rename 攻擊回 READY 全假陰性→安全網是 mutation C 唯一 backstop 卻零測試釘 wiring。seam test(test:282 直呼私函數+合成 git_errors)只釘兩半不釘 collect 內接線。修法=monkeypatch `_binary_paths_from_numstat` 注 phantom path 的 temp-repo e2e wiring 測試。
- 其餘全過：18+63+10+5=96 與 E1 報告逐項符；既有 14 test 純增無改（98+/0-）；drift gate 僅動 docstring 一句（untracked 無法 git-diff，靠 mtime 序 00:18/00:36/00:38/00:42+E1 memory NIT-fix 輪次分離+63 綠間接證）；collector 回傳只加鍵、drift gate blockers 全 deny 向傳導；0 硬編路徑；註釋中文優先。教訓：**「安全網」類 fail-closed 分支的 mutation 驗證必須刪 wiring 而非刪函數——直呼私函數+合成輸入的 seam test 對 wiring 刪除零偵測力，e2e 釘接線須 monkeypatch 上游採集製造三路不一致**。
- **Re-review 2026-07-03 → PASS to E4**：E1 修僅動 test 檔一檔（cumulative 98→121 純增 0 刪＝只加新測試，guard.py sha `0f3ef7c7` 不變、drift gate mtime 00:42 不變），新 `test_collect_escalates_unmatched_flagged_path_to_blocker`（monkeypatch `_binary_paths_from_numstat` 注 phantom path，temp-repo 真跑 collect，斷言 BLOCKED+`binary_or_mode_path_unmatched`，中文注釋記載逃逸緣由）。mutation B 親重放：唯新測試轉紅（1 failed/18 passed），還原 sha 核對一致；guard 19+drift gate 63=82 綠。四 mutation 至此全釘住（A:2 紅/B:1 紅/C:1 紅/D:2 紅）。

## 2026-07-03 — E4 盤點 pre-existing 測試紅三票（組A env/import ×4、組B stock_etf static ×2、組C 雜項 ×5 + TODO v739 row）E2 對抗審 → PASS to E4（0 finding 待修，3 INFO）
- 斷言削弱掃描全綠：in-scope diff 17 條 `-assert` 逐條 1:1 映射到重加等價（來源檔搬移/生產訊息同步），#6 安全測試舊 6 斷言全保留於 opt-in 分支+新增未 opt-in spoof 免疫 3 條=嚴格更強；commit 證據 65e784376/19fa94fc4/ae71575e8/9bf4fd62d/e259674fe/631f5ce3b/929593791 全親核（git log -p + 生產檔逐字）。mutation 4 probe 全咬：樹外 include_str!/concat! 變形/子模組白名單路徑→組B 測試紅；_proxy_headers_trusted 改無條件 True→#6 紅；全部 cp backup 還原後 status clean。
- 親跑三 suite 精準對數：tests/ 默認+importlib 各 1F(僅 event_consumer defer)/804P/2S；research 裸跑 1417P/3S/0F；cargo 5 test 真跑 176.93s 非 skip，fake-HOME+strip-PATH 環境 5 skipped reason 誠實；locator 四層本機由 layer3 `rustup which cargo` 解析（layer4 glob sorted[0] 多 toolchain 時可能撿舊版=INFO，fail-loud 於 build）。TODO v739 句對 d0eeafb41 逐項核：generator :379 emit policy 字段、gate :452 缺字段→blocker fail-closed。
- §5 race：5e 觸發——review 中 sibling push db80212f4（standing_envelope 兩檔 dirty→committed），comm 證 0 overlap 我 12 檔 scope、worktree 內容不變故測試證據仍有效；4 舊 stash 未碰。教訓：**「測試同步到生產現狀」型 review 的硬手段=removed-assert 全量 sorted 清單逐條找 re-add 對應+對「宣稱已審演進」的 commit 親跑 git show 驗內容，1:1 映射外的任何缺席=削弱候選**。

## 2026-07-03 — event_consumer 熱檔拆分 `337a0442d`（純機械搬移宣稱）E2 對抗審 → PASS to E4（0 finding 待修，2 偏差裁決可接受）
- 純移動獨立雙證法（不信 E1 腳本）：①行級 multiset 差集（base 4 檔 vs head 9 檔全行 Counter）——removed 僅 5 行（4 fn 簽名升 pub(super)+1 tracing use 拆分）、added 98 行全屬佈線/module-doc/biz() 複製/visibility，逐條與 spec §2.2/§4.2/§4.4 收支對帳平衡；②per-symbol brace-matching 抽取 13 個搬移 item body 全 byte-identical（含 handle_tick_event 16993B/handle_pending_registration 14213B）；4 個升 visibility fn token-identical（唯一差=close_dup 簽名 wrap 的參數列尾逗號，Rust 語法中性=宣稱偏差#1 可接受）。硬邊界原樣：OPEN_NO_RETRY `[u64;0]=[]`、CLOSE_RETRY_DELAY_MS `[100,400]`、`_ => Structural` fail-closed。
- 測試守恆算術：base/head 各 4228 名、正規化 sorted diff EXIT=0；raw diff 76 行全為 dispatch::tests→dispatch_retcode::tests 遷移（56=18 留守+38 遷入，loop 6=6）；4227P/0F/1I 前後嚴格相等。fmt 偏差#2 裁決成立：spec「搬移塊 fmt-clean」前提在 base 即不實（全 repo 680 既有 drift hunks），event_consumer 29=29 hunks、5 個拆分檔 hunk 遷移後內容逐字同+24 非拆分 hunk 不動=零新增 drift。
- mutation 2 probe 咬合後還原 clean：pad loop_tick>800→靜態測試紅帶檔名；刪一條 arm re-export→E0425 fail-loud。re-export 面精確收口親 grep：9 項各有留守消費者、OPEN_NO_RETRY 僅 test 消費故 cfg(test) 方向對、未 re-export 三項留守檔僅注釋提及。教訓：**行級 multiset 差集+per-symbol byte-compare 是「純搬移」宣稱的最快決定性組合（multiset 抓丟行/重行/內容改，symbol-compare 補殺同行換位），比肉眼過 color-moved 長 diff 可靠且可量化**。

## 2026-07-07 — WP2.1 training-run PIT manifest gate E2 review → RETURN E1（1 MED + 1 LOW）
- Gate ordering/dry-run/report field propagation largely sound; py_compile + focused 41p/1s + registry 49p + diff-check all green, and pooled fail-before-train verified ad hoc.
- RETURN because PIT sidecar and acceptance report write final paths directly (`write_bytes`/`json.dump`), violating PA no partial-write drift; add atomic temp+replace persistence and a permanent pooled-symbol fail-closed test. 991-line file accepted as review-attention, not blocker.

## 2026-07-07 — WP2.1 training-run PIT manifest gate E2 re-review → PASS to E4
- E1 closed both RETURN items: sidecar/report now use same-directory temp + `Path.replace()` with caught-failure cleanup/final preservation; `persist_required=True` raises while default remains fail-soft. Pooled `None`/`ALL` permanent test asserts `train == 0`.
- Verification: py_compile passed; focused WP2.1 pytest `46 passed, 1 skipped`; registry adjacency `49 passed`; manual atomic failure probe preserved old finals and left zero temp files. INFO only: `run_training_pipeline.py` now 1005 lines (>800 review-attention, <2000 hard cap).

## 2026-07-07 — WP6 reward_ledger ProofPacket bridge E2 review → RETURN E1
- Source-only boundary and focused tests were mostly sound (py_compile PASS; WP6/upstream 104 passed; adjacency 83 passed; static side-effect rg no matches; diff-check PASS), but RETURN on 2 MED contract holes.
- MED-1: `validate_reward_record()` only recomputes `record_hash`; forged upstream lineage hashes plus recomputed record hash still validate ready. MED-2: registry lineage requiredness is inferred from absent flags and silently defaults to `execution_reward_not_training_contract_bound`.
- Report: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-07-07--wp6_reward_ledger_proofpacket_bridge_review.md`.

## 2026-07-07 — WP6 reward_ledger ProofPacket bridge E2 re-review → RETURN E1
- E1 closed original MED-1/MED-2 probes: forged lineage+record_hash now rejects, missing registry contract defaults to `registry_lineage_missing`; source_artifacts are now source-backed and validation reruns upstream hashes/validators. Verification: py_compile PASS, WP6/upstream 109 passed, adjacency 83 passed, static side-effect rg no matches, diff-check PASS.
- New MED: optional registry mode can still override contradictory source claims. `registry_required=False` + allowed optional reason passes even when `effect_window.registry_required=True` and `acceptance_report_ref.contract_bound=True`; optional mode needs contradiction rejection before E4.
- Report: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-07-07--wp6_reward_ledger_proofpacket_bridge_rereview.md`.

## 2026-07-07 — WP6 reward_ledger ProofPacket bridge E2 re-review 2 → PASS to E4
- E1 rework2 closes the remaining optional-registry MED: build-time contradiction probe now rejects `registry_optional_source_contract_bound`, and post-build `source_artifacts` mutation with recomputed `record_hash` also rejects. Clean explicit execution-only optional fixture and registry-required happy path both pass.
- Verification: py_compile PASS; WP6/upstream `112 passed`; adjacency `83 passed`; original forged lineage and missing-registry probes remain fail-closed; static side-effect rg no matches; diff-check PASS.
- `reward_ledger.py` is 913 lines, classified as review-attention (>800, <2000) not blocker. Report: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-07-07--wp6_reward_ledger_proofpacket_bridge_rereview2.md`.

## 2026-07-07 — WP7 learning_effect_review stop-loop E2 review → RETURN E1
- Source-only boundary and focused tests are green (`py_compile` no output; WP7+upstream `117 passed`; diff-check no output), but RETURN on 3 MED fail-closed holes.
- MED-1: top-level `proof_packet_refs` / `mutation_envelope_refs` are only checked non-empty, and `reward_ledger_refs` need not exactly cover embedded records. Forging proof/mutation refs or dropping reward refs plus recomputing `review_hash` still returns `valid=True / promote_review_only`.
- MED-2: authority scan catches explicit order/live/mainnet/Cost Gate/model/symlink aliases but misses `trade_allowed`, `trading_enabled`, `enable_trading`, and `execution_authority_granted`, which still promote after hash recompute.
- MED-3: `loss_limits.breach="true"` plus missing/non-numeric consecutive-window or missing USDT cap fields can still produce `promote_review_only`; malformed loss controls must preempt profitable decisions.
- Report: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-07-07--wp7_learning_effect_review_stop_loop_review.md`. 教訓：review packet hash integrity 不只重算整包 hash，還要釘「top-level refs == embedded source lineage exact set」；否則 attacker 可同步改 refs+review_hash 讓 metadata 說謊而核心 source_artifacts 仍綠。

## 2026-07-07 — WP7 learning_effect_review stop-loop E2 re-review 2 → PASS to E4
- E1 rework2 closes the remaining authority string-grant gap: `trade_allowed` / trading aliases / execution authority aliases with `"allowed"`, `"allow"`, `"active"`, `"authorized"`, or `"approved"` plus recomputed `review_hash` now reject as `authority_boundary_violation`; explicit false strings `false`/`0`/`disabled`/`denied` remain non-violating.
- Rechecked prior MED-1 and MED-3: dropped/extra reward refs and forged proof/mutation refs stop at `stop_evidence`; `breach="true"`, missing USDT cap, missing/bad consecutive-window cap stop at `stop_loss_control`.
- Verification: py_compile PASS no output; WP7+upstream `134 passed in 3.40s`; diff-check PASS no output. Report: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-07-07--wp7_learning_effect_review_stop_loop_rereview2.md`.

## 2026-07-08 — win①/win②/G0.5 pre-commit E2 review → ① & ③ APPROVE, ② RETURN E1 (verify gap + Linux-gated)
- win① GUI honesty (`tab-stock-etf-auth-account.js`): APPROVE + Mac-commit-safe. object paths correct (`account=status.account_snapshot` L166; `snapshotUsable=account_snapshot_present===true && account.accepted===true` = FA nested path; normalizer+fallback carry the fields). No numeric renders unless present&&accepted; contract_violations[] alarm preserved. node --check PASS; 0 forbidden browser-writes; passes all 4 GUI static guards.
- win② Rust TOML wiring (`stock_etf.rs`+singleton §2.9): fail-closed CORRECT (missing/unparseable→Err→denied fallback `..default()` caps=0 + allow_*=false; never permissive; lane enabled=false/shadow_only=true; Bybit hard-boundary fields not in this struct=untouched; denied fallback byte-identical to old inline policy). Path idiom matches main_scanner_init.rs:98-100; put_risk! Option→null happy path; singleton registered. **RETURN reason MED**: win②'s "displays real caps" is UNVERIFIED at engine level — TOML→policy mapping covered by openclaw_types acceptance test (5/10 caps, accepted) but engine glue (loader file-resolution + OnceLock + policy_status_summary IPC emit) has NO test; the only engine test (foundation_status_fixtures.rs:199-248) asserts the DENIED fallback and passes only because cargo-test CWD lacks the file. Silent runtime fallback (wrong CWD/env) would quietly not deliver win②. Fix: add engine test set OPENCLAW_RISK_CONFIG_DIR→controlled dir w/ TOML asserting accepted=true+config_version=1+caps 1000/5000/10000+open 5/10+load_error=null. LOW: policy_status_summary now env+OnceLock-coupled → foundation_status_fixtures greenness implicitly CWD/env/ordering-dependent (currently benign). **Linux-gated**: no cargo on Mac → MUST pass `cargo check -p openclaw_engine && cargo test -p openclaw_engine stock_etf` before commit.
- G0.5 CI (`ci.yml` stock-etf-static-guards): APPROVE + Mac-commit-safe. Decisively verified: clean venv (pytest+pytest-asyncio only, no fastapi/psycopg2) ran exact CI cmd → 25 passed 0.53s (conftest psycopg2-lazy so collects clean; bare helper import resolves via no-__init__ prepend). PR-only + ubuntu 1x cost-compliant.
- §5 race PASS: `git log origin/main -- <4 files>` empty = 0 sibling overlap; origin ahead only by unrelated near-bounded-plan docs; touched nothing, no git writes. 教訓：把 env+filesystem+OnceLock 依賴引入原本 pure 的 status fixture 時，happy-path 必須有專屬 engine test 設 env→controlled dir 斷言真 caps，否則「唯一測到的 engine test 斷言 fallback」= 特性本身零覆蓋 + runtime 靜默降級無人知。

## 2026-07-09 — IBKR P1 fingerprint-only SecretSlotLoader E2 對抗審 → APPROVE / PASS to E4（0 blocker，全 LOW/NIT）
- 範圍：新檔 `rust/openclaw_engine/src/ibkr_secret_slot_loader.rs`（~560 行含測試）+ `lib.rs` +4 行（3 註解 + `pub mod ibkr_secret_slot_loader;`，插 health↔instrument_info）。未 commit（untracked）；Mac 無 cargo → read-level 人肉編譯闸。
- **明文零逃逸 PASS（結構性）**：唯一 content read = `read_account_id_zeroized` 的 `read_to_string`（line 88，account_id）；`ibkr_paper_slot_fingerprint` 純 stat-only（filename+mode+len，零讀）。struct 13 欄全 enum/bool/64-hex 無欄位可容明文；`secret_content_serialized`/`account_id_serialized` 恒常量 false；全 error `format!` 只帶 path+io::Error（無 content，E3-6 親證）；唯一 log = `tracing::warn!(error=%e)` 帶的 e 由構造無明文；`{:?}` 僅測試對 blocker enum。T7 serde 子串斷言真能抓漏。
- **live 不 fail-open PASS**：`eval_live_slot` 任何 entry→LivePresentDenied+false；NotFound→absent+true（僅在 ancestor+base read_dir 已成功後可達）；其他 I/O Err→denied；symlink live 由 `reject_symlink_slot` 先 Err（E3-3）；nonexistent-base 由 `ensure_ancestor_owner_only` 先 Err→denied fallback `live_secret_absent_or_empty=false`（E3-5）。ibkr/ 0o700 阻 non-owner 進 live/。找不到 fail-open 縫。
- **指紋單點+stat-only / 祖先校驗 / Err-vs-Ok(PA §7) / scope 全 PASS**：兩指紋單一 pub(crate) 純函數無算法複製；hex::encode lowercase→is_sha256_hex by construction；`ensure_ancestor_owner_only` 覆 external/+ibkr/（0o700+uid==euid，lstat）;僅 2 檔 0 forbidden wiring（0 matrix/validate_operation/envelope/flag/IPC/socket/DB/migration，types 未動）。
- **read-level 編譯 PASS**：imports 全 in-scope（sha2/hex/libc/tracing 全 dep 齊，PermissionsExt/MetadataExt 各使用點內 import）；13 欄逐一對齊無多缺；posture/blocker variant 名皆存在；`is_sha256_hex` 走 module 路徑非 root re-export（正確）；edition 2021→`set_var` 安全；`test_env_lock::guard()` 存在簽名對；#[cfg(unix)] 一致。
- **E1 三決策裁決**：(a) 檔級 `#![allow(dead_code)]` ACCEPT——param_extractor precedent 親證屬實（曾用檔級後窄化，E1 正處「首個 consumer 前」同位置且 TODO(P5) 承諾窄化）；(b) `ibkr_account_fingerprint_hash` 內 `normalized` String 未歸零 = LOW 可接受（account_id 是 paper 帳號非憑證 secret，威脅模型 LOW；但 ZeroizedString 保護被此 copy 部分抵銷）；(c) E3-6 chmod 000 → **root 跑會 unwrap_err-on-Ok panic** = LOW，建議加 skip-if-root 守衛，E4 須確認 cargo 非 root 跑。
- 另 LOW/INFO：手刻 `ZeroizedString::Drop` 的 `*b=0` 可能被 optimizer DSE 消除（zeroize crate 才有 volatile 保證）—PA Q4 已裁 no-dep+手動 Drop 接受；祖先信任鏈止於 external/（grandparent secrets/ 未檢，屬覆写③c 鎖定邊界）；測試覆蓋缺 readonly 寬權限/present-not-dir 分支（NIT）。
- §5 race：5a HEAD `a923893ef` 領先 origin/main `39376e113` 1 個無關 docs[skip ci] commit（ALR stub）；2h 窗 sibling 全 docs[skip ci]，觸 rust/ 者（c66338e8b P0 stock_etf 等）皆已在 origin 史且 0 檔 overlap ibkr loader。5b **髒樹含隔壁 GUI/auth session leftover**（auth*.py/login.html/test_ops1*/reference_gui_redesign_baseline_2026_07_09.md）— 非本任務 scope、與 review 檔 0 overlap，**未碰未 revert**（多 session 紀律）；本 review 只審 2 rust 檔。5c 無 unknown stash 動作；5d N/A（未 commit）。無 git 寫操作。
- 教訓：純 loader 的「stat-only 指紋 vs content read」邊界要 grep `read_to_string`/`fs::read` 窮舉證唯一 content read；手刻 zeroize 的 DSE-elision 與「hash 前 normalize 產未歸零 copy」是明文歸零最常見兩個半漏（低敏資產可接受但要點名）；chmod-000 negative test 必配 skip-if-root（root bypass file perm → unwrap_err panic 假紅）。

## 2026-07-09 — IBKR P2 external-surface gate producer E2 對抗審 → RETURN E1/CC（1 HIGH spec-lineage 待裁 + 1 MED latent；其餘全綠）
- 範圍：新 `ibkr_phase2_gate_producer.rs`（1347 行/22 test，非自報 26）+ lib.rs(+mod) + secret_slot_loader(2 fn→pub(crate) 純 visibility) + precontact.rs(1 行 re-verify)。types crate 4 檔 0 改（git status 證）。Mac 無 cargo→read-level 編譯闸。
- **HIGH（primary RETURN，須 CC/operator 裁）finding-2 lineage 未閉**：E1 mitigation（型別凍→不加欄，改鏡 adr/source_commit/reviewer_roles 進 raw hash；approval amd 07-08-01/expiry 僅 seal-time 閘不入 artifact）**不滿足 CC finding-2 字面**（sealed artifact 不記 07-08-01、無 approval-content-hash 無法 pin 是哪份 approval）。完閉需 types 加 `contact_authorization_amd`+`approval_lineage_hash`（=out-of-scope 單獨 review）。E2 不能自 waive CC finding→RETURN escalate。緩：production 恒 BLOCKED（`phase2_producer_outcome` 從不呼 seal，seal 僅 test 觸達）→ gap 全 latent 零 runtime 效果，建議 CC 出 waiver+follow-up ticket（wire seal 前必補欄）或現在擴 scope。
- **MED（latent）finding-1 覆寫**：`build_phase2_artifact_candidate:314-315` 無條件覆寫 topo.fp=secret.fp→triangulation 對任何 built artifact tautological；enforce（不等拒 seal）present+F1 測到（字面 finding-1 達標），但 P5 topology 變獨立源時此覆寫會遮真 cross-leg mismatch，必於 wiring 時移除。現 topology 恒 source_template→dormant。
- **5 偏差裁決**：①verify 拆分=ACCEPT（pre-seal 閘真在 write 前 seal:590，consumer `reverify_sealed_artifact_at` 強制 hash+path+validate 等價、triangulation 由 raw hash 兩 fp 欄 transitively 覆蓋）；②finding-2=見 HIGH；③T8 調整=CORRECT（producer 令 gate 5flag=policy_flags 恒同步→無 mismatch，T9 專測 desync）；④`OPENCLAW_BROKER_SETTINGS_DIR`=ACCEPT（真消費於 wired-when 路徑、鏡 OPENCLAW_RISK_CONFIG_DIR 範式+fallback settings/broker）；⑤redacted json!=ACCEPT（workspace serde_json 無 preserve_order→BTreeMap 決定序；raw hash 才主錨且 struct-derive 序穩定；任何漂移 fail-closed）。
- **對抗 8 軸全綠（實證）**：no-fake-success（seal 序 validate→verify→triangulation→path-bind→write 全先於任何 write；prod 恒 Blocked；success 路徑 T1 真觸達+斷 0o400 非 vacuous）；write-once（create_new tmp+hard_link atomic-fail-if-exists+絕無 rename+fsync file+chmod0o400+fsync 父+refuse-ephemeral@resolve+gov_dir owner pre-check；T2 byte-identical 無 tmp 殘）；hash 覆蓋全實質欄（serde derive 宣告序，僅 2 hash 欄排除+sealed 正規化，T5/verify_sealed_detects_ondisk_tamper 證竄任一欄破 hash；immutable_storage_path 既入 raw hash 又 anti-relocation 雙保）；approval 6 綁定全測（expired/replay/wrong-amd 06-29-01/future-dated/非owner-only/symlink；anti-replay=source_commit==BUILD_GIT_SHA+拒 unknown；clock 異常 fail-closed；producer 絕不自注 Operator）；compile read-level（14 symbol 全 openclaw_types re-export、TOML 5 section 對齊 bundle、production 0 unwrap/panic/log、Phase2SealError derive Debug/Eq 齊）；scope（0 DB migration/flag/Bybit/types 改，precontact 真 re-verify 非 file-exists）。
- **LOW/INFO**：chmod0o400 若在 hard_link 後失敗→final 殘 0o600（owner-writable）但 hash re-verify 仍擋竄改=硬化縫非洞；gov_dir 祖先校僅 2 層（gov+governance/，同 P1 範式）；test 22≠自報 26。
- §5 race：5a `git fetch` 後 origin/main 落後 HEAD 1 個本地未推 commit（9acea66a4 ALR，無關）；2h 窗 sibling 觸 rust 者（P1 3217e94b4/AMD fae556847）皆已在 origin 史且 0 檔 overlap P2 4 檔。5b 髒樹仍含隔壁 GUI/auth leftover（auth*.py/login.html/test_ops1*）+untracked gui-redesign memo=out-of-scope，**PM commit 須窄 `git commit --only` 本 4 rust 檔勿掃入**；未碰未 revert。5c/5d N/A；無 git 寫。
- 教訓：型別凍結下的 governance-lineage finding 若需新欄=scope-vs-spec 衝突，E2 不自 waive CC finding→escalate；「enforce equality by 覆寫再 check」使 triangulation tautological，字面達標但語義在獨立源接入時失效=latent 必點名；seal producer 的 no-fake-success 要證 success 路徑真可達（全綠 fixture+0o400 斷言）才排除 vacuous-block。

## 2026-07-09 — IBKR P2 T1(SEAL-LINEAGE-FIELDS)+T2(TRIANGULATION-CROSSCHECK) WAIVER-gated fix E2 re-review → PASS to E4（0 blocking；此即上輪 RETURN 之 HIGH finding-2 + MED finding-1 的閉合）
- 承上輪 RETURN：CC/operator 批 WAIVER 解凍 types crate 加 2 欄。T1=`contact_authorization_amd`+`approval_lineage_hash`（閉 HIGH finding-2）；T2=移除 `build_phase2_artifact_candidate` 無條件 topo.fp=secret.fp 覆寫→真 cross-leg equality（閉 MED finding-1）。7 檔工作樹未 commit，Mac 無 cargo→read-level 編譯闸（E4 Linux `cargo test -p openclaw_types -p openclaw_engine` 為真闸）。
- **T1 breaking-change 完整（無 #[serde(default)] 加必填欄）全對點**：grep 全 11 個 `IbkrPhase2GateArtifactV1 {` 構造+`..default()`——producer:360 顯式填兩欄；2 fixture(artifact:25/matrix:34)皆 `..default()`+顯設 approval_lineage_hash 64-hex(contact 靠 default 常量)；其餘 6 構造走 `..accepted_artifact_fixture()` spread；`ibkr_feature_flag_secret_auth.rs:80` 走 Default impl(已含兩欄)。反序化僅 seal re-verify(:405/:420 `serde_json::from_str`)→缺欄 fail→false=fail-closed(安全向；且 prod 恒不 seal 故無舊檔)。template TOML 非載入 struct(僅 toml::Value parse 測)。
- **compute_approval_lineage_hash 決定性 CONFIRMED**：`serde_json::json!` 顯式 key + `roles.sort()`(非裸 to_string)；canonical 覆蓋 Phase2SealApproval 全 6 欄(adr/amd/reviewer_roles/approved_source_commit/issued/expires)；determinism test 有 bite(roles 反序同 hash / issued+1 變 hash / is_sha256_hex)。serde_json Value 無論 preserve_order 與否 to_string 皆決定序。
- **T2 遮蔽真消除 + equality 真啟用**：覆寫刪；`account_fingerprint_triangulation_ok`(:392)+seal:631 攔截未削；`build_full_green` 改 `matched_topology`(否則全綠 seal test 假失敗)；f1 專測=破 fp+重算 hash→seal NotValid(mutation bite)；prod summary test 斷 triangulation_ok=false(placeholder topo≠denied secret=自然 BLOCKED 非回歸)。
- **3 偏差全 REASONABLE**：①t11→matched_topology=CORRECT+**完整**（唯一「intended failure 在 triangulation 下游(目錄權限 seal step5)」之測；t3/t4/t7/t8 用 valid_topology 但 intended failure 在 validate step1(secret/operator/policy)上游 triangulation→intent 保留、正確未改）；②MODULE_NOTE:17-18 改寫=CORRECT（「不改型別 crate」經 T1 授權後成陳述性錯）；③+1 test(determinism)+2 test-assert(t1 lineage 欄/summary triangulation-false)+acceptance array 11→13=正當覆蓋非遮測。
- **PM 3 鎖全遵守**：①types validate() 不加 triangulation blocker（triangulation 僅在 producer seal/outcome）；②contact default=常量(:72)→`default_gate_artifact_blocks_contact` blocker vec 只加 `ApprovalLineageHashInvalid` 不加 `ContactAuthorizationAmdMismatch`(精確對)；③approval_lineage 不入 IPC summary(:836-847 無此欄，leg_status 12 bool 無此欄)。decl==check 序（enum:172-193 vs validate:84-154），2 新變體位置對(ContactAuth 在 Amd 後、ApprovalLineage 在 RedactedSummary 後)。
- **常量單一真源**：私有 `PHASE2_CONTACT_AMD` 已刪，全改 `IBKR_PHASE2_CONTACT_AMD`（gate.rs:15 定義→lib.rs:78 re-export→producer/artifact 引）。scope：`phase2_producer_outcome` 仍永不呼 seal(:810-811)；0 Python/DB migration/Bybit/flag 改；3 TODO 改寫正確(finding-2 標閉合/finding-1 標 P5 殘餘)。
- **LOW/INFO(非 blocker)**：t1 `write_approval:971` 用字面 "AMD-2026-07-08-01" 而 `direct_approval:912` 用常量(該 write_approval 之 loaded approval inert、僅測 loader parse，build_full_green 走 direct_approval)；lineage 決定性註解「顯式固定 key 序」措辭略不精(真機制=serde_json Value 序穩定)；template TOML contact AMD 字面(TOML 無法引常量、值正確)。
- **兩 ticket 可 closed**：T1 完全閉合(HIGH finding-2)；T2 閉合(遮蔽消除+真 equality+f1 mutation 證)，P5 殘餘(topology 真獨立 account 源)=自然 phase 依賴非 waiver 缺口(prod 恒 BLOCKED 正確 fail-closed)。
- §5 race：5a `git fetch` 後 HEAD==origin/main `ae7ee9669` 無 sibling divergence；review 期間無新 push(5e N/A)。5b 髒樹仍含隔壁 GUI/auth session leftover(auth.py/auth_legacy_routes/auth_routes_common/main_legacy/login.html/test_batch_b/test_ops1_csp/test_ops1_csrf + memory churn + 2 untracked project memo)=out-of-scope 與 IBKR 7 檔 0 overlap，**PM commit 須窄 `git add` 僅 IBKR 7 檔勿掃入 auth workstream**；未碰未 revert(多 session 紀律)。5c 無 unknown stash 動作；5d N/A(未 commit)；無 git 寫操作。
- 教訓：新增必填欄(無 serde default)的 breaking-change 審查=grep 全構造點窮舉分「顯式填/spread 填/Default 填」三類+反序化路徑證缺欄 fail-closed；「seal 多階閘」下判「哪些 negative test 需 matched fixture」看 intended-failure 是否在 triangulation 下游(上游者 intent 天然保留、下游者不改則 assertion 過但測錯東西=t11 型陷阱)。

## 2026-07-10 — R3 修復包(WP-A/B/C 已交付部分)E2 對抗審 → RETURN E1(2 P1)
- 全綠面:F1 去重真 bite(真 NEAR ledger 5058 rows → n_eff=2、candidate=False、t/BH=None 親跑證);BH/t/sign-flip 全改吃 n_eff;PROFIT-1 證據鏈逐行親證(gates.rs 二次扣成本公式+19d active 面+哨兵[90] validation_passed 盲區+PG 71,207 親查)且「證實不修」偏差=charter 內部衝突(「證實則修」vs「Cost Gate 不降級」)取保守側,判合理但需 PM 追認;WP-B cap=5 釘死/spawn-fail 不耗名額/zero-leak 靜態隔離 14 test 綠;WP-C 定價 $2/$10 intro 至 2026-08-31 經官方源核實。
- **RETURN 2 P1**:(1) n_eff 門檻默認 5 vs QC 預註冊(同包落檔)E1=30 未對齊——n_eff∈[5,30) 仍可成候選,違 charter WP-A.1「低於預註冊門檻不得成候選」;(2) 主判成本用 q50 vs 預註冊 §6.1 凍結 E[abs(slip)]=mean_abs(需擴 slippage_quantile_artifact)+tail 用 conservative_v1 vs §6.2 CVaR90——同 charter 條目兩交付品公式矛盾,align-or-arbitrate。
- 教訓:同一修復包內「預註冊文檔」與「實作」由不同 agent 並行交付時,E2 必須把預註冊當 spec 對實作逐條 diff(門檻值/公式/去重鍵三處全不一致)——實作者「暫定值待定案」注釋不豁免,因為定案文件已在同一 tree 裡。

## 2026-07-10 — GUI 大改 P0.1 tokens 統一(working-tree 未提交)E2 對抗審 → PASS to E4(附 1 commit-hygiene 條件)
- 全綠面(實證):tokens.css sha256 與正本 byte-identical;compat 16 映射逐條=spec、零 hex、零循環;22/22 檔 `<html data-theme="dark">` 精確+tokens→compat→一切 CSS/JS 之前+charset 最前;全 static/ 零 `:root` 殘留;窮舉 comm(消費41 vs 定義集)僅 2 落空且 git show HEAD 證均 pre-existing(tab-risk:158 `--bg-card` 無 fallback 透明底、tab-phase4:50/75 `--bg-elevated` 有 hex fallback,P0.4 清);47 inline script 塊抽取 node --check 全綠親跑;新增行 0 style=/0 hex/0 硬編路徑;cards/*.html=fetch+innerHTML partial 繼承父 tokens 非漏網文檔。
- 關鍵 finding:login.html 同檔交疊兄弟 auth session 改動(LOGIN_ERROR_FALLBACKS/formatLoginError/?soft=1,attribution=auth_routes_common.py 同名 AUTH_* code)——非 E1a scope creep,但 P0.1 commit 必須 `git add -p` narrow-stage login.html 只取 token hunks;color-scheme 淺色 OS 四檔原生控件回歸=正本 tokens.css 缺 `:root[data-theme=dark]{color-scheme:dark}`,屬 PA 正本層 gap 非 E1a 可修(byte-identical 約束),路由 PA 修正本再 resync。
- 教訓:GUI token 統一審查=「消費/定義雙集合 comm + git show HEAD 判 pre-existing」比逐檔目測快且完備;同檔多 session 交疊時 attribution 用「兄弟 diff 內同名常數」定歸屬,審查範圍≠提交範圍。

## 2026-07-10 — R3 修復包(WP-A/B/C)E2 對抗審 → RETURN E1(1 P1 + 4 P2)
- **P1(實證重現)**:outcome_review v4 的 n_eff=distinct entry_ts_ms(毫秒精確)≠ QC 預註冊 §2 正本(分鐘量化+per-horizon+非重疊窗 greedy+複本一致性不得靜默平均);eligibility 缺 §3 E2/E3(days≥5/top-day≤50%)、缺 §4 V=0→DATA_INTEGRITY_SUSPECT。合成 probe:30 個秒級重發(毫秒各異全同值)→ 直接成 DEMO_PROBE 候選 p=0.0 bh_pass=True——F1 形態在近似複製通道存活。outcome_writer:399-401 event 自帶 price 時 entry_ts_ms=event_ts_ms 證通道真實。教訓:去重 fix 審查必須用「同語義家族的近似複製」(非 exact copy)構造攻擊輸入;threshold 值引預註冊但度量單位不同=單位錯配。
- mutation 三連:floor 30→5 紅(bite)、dedup 中和紅(bite)、**t 檢定 n 改 raw outcome_count 133 測試全綠存活**(test gap,P2)。PG 抽驗 E1 右偏 claim 逐位吻合(2529|17.709|2.992|34.113)。PROFIT-1 no-fix 偏差=charter 內部衝突(「證實則修」vs「Cost Gate 不降級」)的合理保守解,證據鏈抽驗全對,留 PM 追認;哨兵 [90] validation_passed 盲區屬實。
- WP-B cap=5/zero-leak/預設 OFF 全綠(installer 與 template 漂移 P2);WP-C sonnet-5 $2/$10 intro 至 08-31 經官方源證實,鍵 dormant(MODEL_IDS 仍 4-6)。sibling push(GUI tokens)review 中進 origin,file-scope 0 overlap 照 SOP 記錄。

## 2026-07-10 — GUI P0.2 批次 1(oc-utilities.css+殼層 inline 清理)E2 對抗審 → PASS to E4(APPROVE-WITH-NITS,0 blocker / 2 LOW / 3 INFO)
- 全綠(親證非採信):§A vs 規格 §4 自行 awk 抽取 diff=byte-identical;59/59 added-line class token 全解析到定義(自寫 sweep);22/22 三連順序+`/static/` 前綴+17 tab 檔 +1/-0;六檔 style=0;JS 軸殘留=0(converted 元素 id 全 static 跨檔 grep,唯一留置=modals:393 whiteSpace 無軸衝突);className-wipe 窮舉(console 21+trading 11+app-*.js+common*)全部 wipe 目標零 utility 直掛;added-line hex=0/style==0/硬編路徑=0;node --check 3 JS+5 檔 inline 全綠親跑;§5.1/5.2 收斂逐條對表全合;冗餘刪除 claim 對 common.js:999-1000 注入 CSS 逐字證實。
- LOW×2:(1) baseline `hidden` 唯一既存使用=app.js/app-gui.js(index confirm-modal,styles.css:73 `.confirm-modal.hidden` 同值非衝突,行為零變)——E1a 報告未列此 blast-radius 條目;(2) console spbtn 容器 `row` 引入原 inline 沒有的 align-items:center(兩等高按鈕視覺零差,詞彙表無裸 flex utility)。INFO:oc-input--num 留給所屬批次追加(合 §11)、login.html 兄弟 auth hunk 需 `git add -p` hunk 級 narrow-stage、jsdom 腳本未入 repo(以自行重推替代)。
- 教訓:utility 全局掛載審查的決定性手法=「HEAD baseline 全 static class-token sweep(含 className/classList/JS 模板)」——比逐檔目測完備,一次抓出規格宣稱碰撞=0 之外的唯一真使用面(hidden×3 檔)並逐一判同值/衝突。

## 2026-07-10 — R3 修復包 round-3 E2 re-review → RETURN E1(0 P1 / 3 P2 待修;統計核心已閉)
- round-2 P1 親證閉合:v5 分鐘量化+§2.2/2.3+§2.6 greedy+E2/E3+V=0+§5.1 family 全落地;原攻擊(30 同分鐘 distinct-ms 全同值)重放 → n_eff=1/COLLECT_MORE/p=None/0 候選;mutation B(毫秒鍵)19 紅、C(刪 E2/E3)2 紅、D(BH family 灌水)1 紅=全有 bite;1570 passed(唯一紅=decision_packet 牆鐘 time-bomb,fixture 2026-06-22+336h 於 07-06 到期,兩檔 git 未觸,親證 pre-existing)。
- **RETURN 3 P2**:(1) t 檢定 n mutation(effective_entry_count→outcome_count)**仍存活全套 1570 測試**(round-2 P2 未修)——charter WP-A.1 核心不變量零 regression 鎖;(2) prereg 正本落 `docs/research/`=README 樹外目錄+未入 document_index(強制規則 #1/#3);(3) ai_pricing sonnet-5 intro 到期 2026-09-01 僅 yaml 注釋 TODO,無 TODO.md/healthcheck 配對(到期後成本低估 33%=budget 反保守)。
- 非阻塞:charter 未交付項 A.4/A.6/A.7/B.4/C.2 不在樹(主 session 排程);PROFIT-1 no-fix 保守解仍待 PM 追認;installer 漂移=E1 已申報偏差(NIT);WP-B cap=5/zero-leak/22 test 綠、schema v5 純增欄 0 removed key、消費端 fail-closed 驗訖。
- 教訓:re-review 必重放上輪同一 mutation 清單——「修復輪只修點名最小集」時,未列入最小集的 P2 test-gap 默認不會被順帶修,要麼在 RETURN 清單顯式列,要麼追到閉合為止。

## 2026-07-10 — WP-A.4 counterfactual_rerun 未 commit 代碼對抗審 → ISSUES(P2×3+NIT×7,無 P0/P1;inline 回報不落檔)
- 預註冊逐條對齊全綠(去重/n_eff≥30/CR1 day-cluster/BH q=0.10 只撤不扶/雙軌成本排除 1.3SM/sign-flip/§8 判定式);mutation 親證 2 枚有 bite(CR1 去 G/(G−1) 修正紅、backfill 注 look-ahead 紅,還原 byte-identical);PG SELECT-only(set_session readonly+write-grep 0)、Linux staging 已刪親證、跨平台 grep 0、artifact sha `d09bf86c` 與 PM 報告一致且 zero_cluster_variance 0 觸發。
- P2:①caller 用 std==0 代理 V=0,cluster sums 相消(V=0,std>0)時 eligible cell p=None 會被判 VETO 而非 prereg §4 的 DATA_INTEGRITY(latent,本 run 0 觸發);②凍結 SQL 非逐字(SELECT 投影改 ts_ms、去 reason)未記 deviation_log(WHERE 同一+計數錨保母集身分);③觀測構建→eligibility 中段(雙源 merge/replica/E1-E4 旗標/family 組裝)0 測試,僅 5 面純函數有測。
- 教訓:v3 凍結重放的保真度證法=「BH rediagnosis 可達集分析」(候選已過 avg_net≥0∧positive≥60 ⇒ demote 只可能落 BLOCK_CONFIRMED,GROSS_EDGE 枚舉 BH-不變)+anchor counts 機械比對,比逐行 diff 舊版更快更硬;兩源 dedup 鍵慣例(ledger=信號分鐘 vs backfill=+1 分鐘)要親查 writer 源頭,錯位由非重疊窗吸收屬設計內但必須寫進 finding。

## 2026-07-10 — GUI P0.2 批次 2(monitoring/system/settings 211 inline style 清理)E2 對抗審 → RETURN E1a(1 HIGH)
- 全綠面(親證):4 檔 style= 全形態 0、global 1420→1210 對賬;42 新類名 baseline sweep 僅 qa-chip-sm=申報死引用補正本;added-line hex 僅 2 行紫 verbatim relocation;§A/§4 追加 byte-identical+spec-drift guard 2/2 親跑綠;node --check 3 檔+governance.js 綠;tag balance 0 殘;轉換元素 id 在共享 JS(common*/governance)0 寫點;全部 wipe 目標零 utility 直掛(alert chip 走 `.alert-ch-head .oc-chip` 後代選擇器,每個重寫串保留 oc-chip 親證)。
- **HIGH:tab-system:85 `.mode-btn--live`(0,1,0) 輸 :44/:45 `.mode-btn:hover/.active`(0,2,0)**——原 inline 無條件勝,現 live_reserved active/hover 態紫框變 accent,破「零觀感變更」relocation 契約;jsdom 只斷 class 共存斷不到 computed winner。§5.2 10px 規則vs表矛盾裁決=表值 12 勝(§4 gap-3 註記+批次 1 shipped 先例),已直接在 §5.2 加釐清註記(doc 級授權內,drift guard 重跑綠)。
- 教訓:verbatim relocation 審查必比「原 inline 的無條件優先級」vs「新選擇器在完整 cascade(含 :hover/.active 兄弟規則)的勝負」——值逐字對了仍可能在狀態態輸;class-coexistence 斷言是此盲點的假安心。
