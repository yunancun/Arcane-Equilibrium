# E2 Review — P1 V083 ipc_close_symbol entry_context_id fix + P2 demo freq params

- **Date**: 2026-05-11
- **E2**: Senior Backend Code Reviewer + Adversarial Auditor
- **Verdict**: **APPROVE-CONDITIONAL**（P1 ship-as-is + P2 ship-as-is；附 1 MEDIUM follow-up + 1 LOW housekeeping）
- **Commits**:
  - `d4867676` E1 IMPL: P1 V083 ipc_close_symbol entry_context_id fix (Option B)
  - `27e86f89` P2 fix: bb_reversion min_persistence_ms 180→120s + ma_crossover min_trend_snr 0.75→0.60 (demo only)
- **Sources**:
  - PA design: `docs/CCAgentWorkSpace/PA/workspace/reports/2026-05-11--p1_v083_ipc_close_fix_design.md`
  - E1 IMPL DONE: `docs/CCAgentWorkSpace/E1/workspace/reports/2026-05-11--p1_v083_ipc_close_impl_done.md`

---

## 1. Scope

| Commit | Files | LOC delta |
|---|---|---|
| `d4867676` (P1) | `rust/openclaw_engine/src/tick_pipeline/commands.rs` (+97 -44) | net +53 |
| | `rust/openclaw_engine/src/tick_pipeline/tests/resolve_close_entry_context_id.rs` (new) | +86 |
| | `rust/openclaw_engine/src/tick_pipeline/tests/mod.rs` (+1) | +1 |
| `27e86f89` (P2) | `settings/strategy_params_demo.toml` (+2 -1) | +1 |

E1 IMPL DONE 報告 + memory 文檔不在生產 review 範圍。

---

## 2. 8 條 §九 + OpenClaw 對抗 checklist

| Item | 結果 |
|---|---|
| 改動範圍與 PA 方案一致 | ✓ Option B 第一波止血嚴格落實，未動 V083 SQL / writer / paper_state / owner_attribution |
| 沒有 except:pass 或靜默吞異常 | ✓ Rust path，N/A |
| 日誌使用 %s 格式（非 f-string） | ✓ Rust path，N/A |
| 新 API 端點 `_require_operator_role()` | ✓ N/A，本 wave 無新 API endpoint |
| `except HTTPException: raise` 順序 | ✓ N/A |
| `detail=str(e)` 改 "Internal server error" | ✓ N/A |
| asyncio 路由 blocking threading.Lock | ✓ N/A |
| 私有屬性穿透 `._xxx` | ✓ N/A |
| 跨平台 grep `/home/ncyu` `/Users/[^/]+` | ✓ 0 hit |
| 雙語注釋（2026-05-05 後默認中文） | ✓ 注釋全中文（helper docstring + 5 callsite + test file 全中文） |
| Rust unsafe 零容忍 | ✓ 0 hit |
| Rust unwrap/expect 限不可恢復場景 | ✓ 新代碼 0 hit；test code `parse::<u64>().expect("must be parseable")` 是測試斷言合理 |
| Rust panic 不在交易路徑 | ✓ 0 hit |
| 跨語言 IPC schema 一致 | ✓ N/A，未動 IPC layer |
| Migration Guard A/B/C | ✓ N/A，無 SQL 改動 |
| healthcheck 配對 | ✓ N/A，無新被動等待 TODO |
| Singleton 登記 §九 表 | ✓ N/A，無新 singleton |
| 文件大小（800/2000 行） | ✓ commands.rs 1374 (<2000 hard cap, >800 warning 但 well-cohesion 模組 governance 接受) / test 86 / mod.rs 53 |
| Bybit API 改動先查字典手冊 | ✓ N/A，未動 Bybit REST/WS |

---

## 3. 對抗反問結果（adversarial drill）

**Q1**: 「你說『5 處 call site 全改』— 漏了什麼？」
- 跑 `grep -nE 'resolve_close_entry_context_id\(' rust/openclaw_engine/src/tick_pipeline/commands.rs`
- 結果：6 hit（1 def line 1040 + 5 callsite line 513/744/938/1121/1194）✓ 全綁
- 跑 `grep -nE 'get_entry_context_id' rust/openclaw_engine/src/tick_pipeline/commands.rs`
- 結果：3 hit — line 167（submit_external_order，**未改**，見 §4.1 MEDIUM）/ line 1038（helper docstring 引用）/ line 1041（helper impl）

**Q2**: 「你說『不變式守護』— entry path 真的沒被誤改？」
- Read line 187-218：entry path `is_close_fill_for_db == false` 分流仍走 `let fill_entry_ctx = String::new()` ✓
- helper 只在 `existing_entry_ctx` capture 處被叫，不直接寫 fills → 下游 close/entry 分流仍有效

**Q3**: 「你說『test 真覆蓋邏輯』— 沒 mock 掉 bug？」
- 跑 `cargo test --release -p openclaw_engine --lib resolve_close_entry_context_id`
- 4/4 PASS ✓；test 用真 `TickPipeline::with_balance(...)` + 真 `paper_state.apply_fill(...)` 建倉，不是 stub mock
- test name 與行為對齊：`test_resolve_real_id_when_present` 確實 set 真 id 再查；`test_resolve_synthetic_when_missing` 沒建倉；`test_resolve_synthetic_when_empty_string` 建倉但 set 空（驗 accessor empty→None filter）；`test_synthetic_pattern_well_formed` 嚴格 split 三段 + u64 cast
- `test_resolve_synthetic_when_empty_string` 設計細緻 — 模擬「set empty 是 no-op + position 自然 empty」雙重 fallback path，確實覆蓋 orphan adopt 場景

**Q4**: 「你說『規格一致』— PA 文件第幾行對應你哪行 code？」
- PA spec §3.1 helper 簽名 line 138-145 → commands.rs:1039-1045 完全對齊（`#[inline]` ✓ `&self` ✓ pattern `orphan_recovery_ctx:{symbol}:{ts_ms}` ✓）
- PA spec §3.2 不變式 3 條 → 全守
- PA spec §5.1 4 個 test → 全實作（test name 略改命名，行為等價）

**Q5**: 「同污染路徑 line 167 `submit_external_order` — E1 push back 對嗎？」
- 親 read line 158-218 + line 299-334（Fill TradingMsg dispatch）
- 結論：**E1 push back 對**。line 167 同 pattern `unwrap_or("")` 真實會走 line 320 `entry_context_id: fill_entry_ctx.clone()` 進 trading.fills，close case `is_close_fill_for_db == true` 時撞 V083
- 流量評估：external/IPC manual order 入口，非 hot path automatic close。在 22 min × 518 burst 場景中不會主導，但 latent bug 真實存在
- E1 嚴守 user prompt + PA spec scope = 合理 trade-off；但 PA 必須在第二波或同 sprint 補修
- **本 review APPROVE 但標 §4.1 MEDIUM follow-up**

**Q6**: 「Cargo test 2789 PASS — 是否真實跑了？」
- 跑 `cargo test --release -p openclaw_engine --lib` parallel
- 結果：`2789 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` ✓ E1 報告與 runtime 證據完全一致

**Q7**: 「P2 commit 真 demo only？」
- 跑 `grep -nE 'min_trend_snr|min_persistence_ms' settings/strategy_params_*.toml`
- live.toml line 31 `min_trend_snr = 0.75`（unchanged）/ line 16 `min_persistence_ms = 240000`（unchanged）
- paper.toml line 24 `min_trend_snr = 0.75`（unchanged）/ line 16 `min_persistence_ms = 240000`（unchanged）
- demo.toml line 22 `min_trend_snr = 0.60`（modify）/ line 30 `min_persistence_ms = 120000`（ADD 在 bb_reversion section）✓
- TOML parse 成功：`tomli/tomllib/toml` 三 lib 全 OK，inline `#` comment 不破 parser ✓

---

## 4. Findings

### 4.1 [MEDIUM] line 167 `submit_external_order` 同 pattern V083 latent bug 未動（已知 E1 push back）

**位置**：`rust/openclaw_engine/src/tick_pipeline/commands.rs:165-169`

**現狀**：
```rust
let existing_entry_ctx = self
    .paper_state
    .get_entry_context_id(symbol)
    .unwrap_or("")            // ← 同 V083 violation pattern
    .to_string();
```

**為什麼 MEDIUM 非 HIGH**：
- 流量低：external/IPC manual order 入口，非 hot path automatic close
- E1 IMPL DONE §6.1 已 explicit push back，不是隱蔽問題
- W-D MAG-083 P1-RCA-1 22 min × 518 burst 起源確認是自動 close path（line 938/1121/1194），不是 line 167
- 第一波止血不阻；不影響當前 P1 deploy

**為什麼 MEDIUM 非 LOW**：
- 真實 close case `is_close_fill_for_db == true` 時 line 320 直入 trading.fills.entry_context_id（與 line 320 `entry_context_id: fill_entry_ctx.clone()` 同路徑）
- V083 CHECK 仍會 reject → batch buffer 同樣會卡死
- future external close path（GUI IPC / manual API trigger）可能撞

**建議修法**（PA decide 同 sprint 修否）：
```rust
// commands.rs:158
let now_ms = openclaw_core::now_ms();
// commands.rs:165-169 replace
let existing_entry_ctx = self.resolve_close_entry_context_id(symbol, now_ms);
```
- 零行為變化（entry case `is_close_fill_for_db == false` 仍走 `String::new()`）
- LOC 改動 ~5 行（4 -> 1）
- 風險：低（同 helper，已 4 unit test PASS 證 path 安全）

**Verdict**：P1 第一波止血**不阻**，但 PA 應在 follow-up 同步處理（建議 next deploy 一起 patch，避免 future external close 再撞同 V083）。

---

### 4.2 [LOW] Helper docstring 引用舊 LOC 標號（PA spec stale）

**位置**：`rust/openclaw_engine/src/tick_pipeline/commands.rs:1028`

**現狀**：
```rust
/// 詳見 W-D MAG-083 P1-RCA-1（commands.rs:1108 / 945 / 1183 / 512 / 749 五處
/// close-path call site 全走本 helper）。
```

**問題**：
- PA spec §1.2 給的 LOC 標號是改動**前**的（1108 / 945 / 1183 / 512 / 749）
- 改動後實際 LOC 已 shift（513 / 744 / 938 / 1121 / 1194）
- docstring 將 mislead 未來 reader（grep 找 line 1108 找不到）

**建議修法**（E2 可直接補；屬 obvious housekeeping，§E2 角色允許小範圍修）：
```rust
/// 詳見 W-D MAG-083 P1-RCA-1（commands.rs 5 處 close-path call site 全走本 helper）。
```
或更新為 post-fix LOC（513 / 744 / 938 / 1121 / 1194）。建議用前者，免後續再次 LOC drift。

**為什麼 LOW**：純文檔，不影響執行邏輯，不影響 cargo test，不影響 deploy。

**Verdict**：**不阻 deploy**，作為 housekeeping clean-up 留 E1 或 E2 在 follow-up commit 一起處理（與 §4.1 MEDIUM 同 patch）。

---

## 5. PA spec 標號錯位（E1 §6.2 already raised）

E1 IMPL DONE §6.2 指出 PA spec §1.2 table 把「execute_position_close」標 LOC 512+749，實際 512 是 `apply_confirmed_fill` not `execute_position_close`。E1 按代碼語義（不按 spec 字面函數名）替換正確 5 處。

**E2 acknowledge**：E1 disambiguation 對。對抗 `grep -nE 'fn apply_confirmed_fill|fn execute_position_close'` 確認：
- line 488 `pub fn apply_confirmed_fill` (含 line 513 callsite)
- line 712 `fn execute_position_close` (含 line 744 callsite)

PA spec stale，E1 IMPL 正確。

---

## 6. P2 commit 27e86f89 細部 verify

| Check | 結果 |
|---|---|
| demo only（live + paper 0 改動） | ✓ live.toml/paper.toml `min_trend_snr/min_persistence_ms` 全留原值 |
| TOML inline `#` comment 不破 parser | ✓ tomli/tomllib 解析成功 |
| ma_crossover.min_trend_snr 0.75→0.60 (line 22) | ✓ |
| bb_reversion.min_persistence_ms = 120000 是 ADD (line 30) | ✓ 原 section line 24-29 無此 key |
| 注釋中文（CLAUDE.md §七 2026-05-05） | ✓ |
| QC verdict APPROVE 2/2 (A1+A2) | ✓ commit message 引用 QC report |

P2 commit 純 config 改動，無 runtime code path 修改，無新 PG schema。

---

## 7. Verdict

**APPROVE-CONDITIONAL** ship-as-is for current deploy window：
- P1 (d4867676): ✓ 止血 ready，5 close path callsite 全綁 helper，4 unit test PASS，2789/2789 regression PASS
- P2 (27e86f89): ✓ demo only confirmed，TOML 解析成功，scope 嚴格

**必修（同 sprint follow-up，不阻當前 deploy）**：
1. [MEDIUM] line 167 `submit_external_order` 同 pattern V083 latent bug — PA decide 是否同 sprint patch；建議 next deploy 一起修，避免 future external close path 再撞 V083
2. [LOW] commands.rs:1028 helper docstring LOC 標號 stale — 與 §4.1 同 patch

**E2 不直接修**：
- §4.1 是業務邏輯範圍（PA spec scope decision），E2 角色不代寫，退回 PA decide
- §4.2 可由 E2 直接補（純 docstring housekeeping）— 但 operator 集中 push 流程下，建議與 §4.1 fix 同 patch 由 E1 一起做

---

## 8. PASS to E4

✓ E4 regression scope：
1. `cargo test --release -p openclaw_engine --lib` 0 failed / 0 ignored（已 verify 2789/2789）
2. `cargo build --release -p openclaw_engine --bin openclaw-engine` clean
3. Linux PG dry-run（per PA spec §5.2）：在 Linux trade-core 確認 synthetic id 通過 V083 CHECK
4. P2 TOML 不需 cargo build（pure config，hot-reload at runtime）

E4 不需重跑 4 個新 unit test（E2 已 verify）。

---

## 9. E2 Sign-off

- Cargo test runtime evidence 已自跑 verify
- 5 處 call site 對抗 grep 全綁 helper
- 同污染路徑 line 167 已 acknowledge MEDIUM follow-up
- PA spec stale LOC 標號 already noted E1 §6.2 + 本 review §5
- 跨平台 grep 0 hit / 雙語注釋全中文 / 文件 size 1374<2000 / 無 unsafe / no panic
- TOML demo only scope 嚴格

PASS to E4 regression（with §4.1 MEDIUM follow-up scoped for next deploy）.

E2 REVIEW DONE: APPROVE-CONDITIONAL · report path: /Users/ncyu/Projects/TradeBot/srv/docs/CCAgentWorkSpace/E2/workspace/reports/2026-05-11--p1_v083_p2_freq_e2_review.md
