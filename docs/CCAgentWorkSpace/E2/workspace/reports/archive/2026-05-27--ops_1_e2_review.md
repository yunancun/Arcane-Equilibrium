# E2 PR Adversarial Review — P0-OPS-1 HTTPS / Secure cookie / CSRF / CSP

**日期**：2026-05-27
**角色**：E2
**範圍**：E1 IMPL 1242/22 LOC（Track A + B + C） — 限定 OPS-1 scope；worktree 內 OPS-2 / OPS-4 / live_authorization Rust 改動為其他 sub-agent 並行成果，**不在本 review 範圍**。
**Spec**：`docs/execution_plan/specs/2026-05-26--p0-ops-1-https-secure-cookie.md`
**E1 報告**：`docs/CCAgentWorkSpace/E1/workspace/reports/2026-05-27--ops_1_https_secure_cookie_impl.md`
**Multi-session race §5**：fetch 後 HEAD = origin/main；本 review 期間 0 sibling push 到 origin/main 衝突；status 內混雜 OPS-2/OPS-4 file 已在 task scope 中認知豁免（非 leak）。

---

## §1 Track ABC Findings 矩陣

| # | 嚴重性 | Track | 位置 | Finding |
|---|---|---|---|---|
| F-1 | **HIGH** | B | `static/risk-tab.js:1079` + `app.js:407` (`apiPost`) + `handoff_helper.js:820` (`fetchWithIdempotency`) + `app-paper.js:1325/1332/1397/1421/1451` | **CSRF 死路** — 7+ 個原始 `fetch()` POST 寫操作未過 `ocApi()` / `ocFetchWithCsrf`，enforcing 上線即 403。E1 IMPL §5.2 自陳「`tab-replay.html` 含一些自定 fetch...都走 ocApi 因此覆蓋」**經 grep 證偽**：`app-paper.js` `_fetchReplayJson` 是獨立 raw fetch，**不走 ocApi**。shadow mode 雖緩衝 14d，但 cutover 後直接斷 GUI |
| F-2 | **HIGH** | B | `auth_legacy_routes.py:118-126` (`/api/v1/auth/check`) | **既有 session csrf cookie 不會自動 seed** — 24h `oc_auth_token` 內存活的使用者，OPS-1 deploy 後沒有 `oc_csrf` cookie，enforcing 上線後寫操作全 403。`auth/check` 應在 cookie 缺失時 re-issue csrf；目前只回 `{authenticated:true}` |
| F-3 | **MED** | B | `main_legacy.py:313-323` 註釋 vs 實際 ASGI onion 行為 | **註釋誤導** — comment 寫「為什麼放在 SlowAPI 後：rate-limit 先擋住惡意洪流，再做 CSRF 比對節省 CPU」。實際 Starlette `add_middleware` LIFO/onion：CORS→SlowAPI→CSRF 註冊次序代表 inbound 處理順序 **CSRF → SlowAPI → CORS → route**。CSRF 先於 SlowAPI 跑，CPU 節省效果反向。功能 OK（OPTIONS 不在 `_WRITE_METHODS`，CORS preflight 不影響），但 comment 不修會誤導後續 reviewer |
| F-4 | **MED** | C | `main_legacy.py:384-403` `/api/v1/csp/report` | **未限頻 + 未限大小** — endpoint 無 slowapi `@limiter.limit()`，依賴 global 120/min（per-IP）。tailnet 內任一設備可灌 logger.info JSON 撐爆 journal。亦無 `request.json()` payload 大小上限。spec §5.3 沒明確指定限制但 production 標準應限頻 + 限 8KB body |
| F-5 | **MED** | B | `csrf_middleware.py:64` `/api/v1/auth/logout` 在 EXEMPT | **logout 豁免風險** — SameSite=Strict 確實阻擋跨站 POST，但 attacker 透過已被 XSS 注入的同源頁面可發動 logout 騷擾（DoS）。改 `/api/v1/auth/logout` 改成 require token（前端登出鏈本就有 cookie + token，不會卡）即可消除。spec §4.2 沒列為 exempt，IMPL 自加為 exempt 是 deviation |
| F-6 | **LOW** | A | `helper_scripts/install_caddy.sh:142-145` | macOS dry-run `tailscale cert "$CERT_HOST"` 不會 fail，但 `chown root:caddy` 在 macOS 沒 `caddy` user → silenced `2>/dev/null \|\| true`。dry-run path（OK）但 `--apply` path 在 Mac brew 安裝 caddy 時實際 user = `_caddy` 或 user 跑（brew services），chown root:caddy 失敗會留 cert root-only 不可讀。可選擇性偵測 `id caddy` exit code 改 message |
| F-7 | **LOW** | A | `helper_scripts/systemd/openclaw-tls-renew.service:23-33` | `ExecStart=/bin/bash -c '...source ...; host=$(...); ...'`：使用 `set -e` 在 `/bin/bash -c` 模式下行為差（一行內 `&&` chain），若 `tailscale cert` 後 chown 失敗整體繼續到 reload caddy = 部分失敗 silent。建議 `set -euo pipefail` prefix 或拆 helper script |
| F-8 | **LOW** | A | `Caddyfile.template:46` `{$OPENCLAW_API_BACKEND_PORT:8000}` | 用 Caddy env default syntax `:default` 而非 envsubst default。envsubst 不展開 `${VAR:8000}` 為 8000，會原文輸出 `:8000`（Caddy syntax 接得住），但 install_caddy.sh L57 已預設 `BACKEND_PORT=8000` 並透過 env 注入，**重複 default 機制**。功能 OK，刪一個更乾淨 |
| F-9 | **LOW** | A | `install_caddy.sh:175-178` | macOS 部署 launchd 只印指引、不實作 plist 落地。spec §1.2 強調 Apple Silicon Mac portability，但 install script 在 Mac 上 `--apply` **沒有實際把 service 起來**，只是手動指引。CLAUDE.md §六「新代碼必須隨時可部署 Mac」是 portability 要求；不是「立刻 prod-ready Mac」要求。可接受為 spec §7 已說「Linux 現址 + Apple Silicon Mac 都能 `tailscale cert`」，但建議 IMPL 報告或 SCRIPT_INDEX 加註「Mac plist 需手動」 |
| F-10 | **NIT** | C | `main_legacy.py:390-400` | csp_report 註釋寫「fail-closed 改 silent 200」——實際回 `204`，註釋與 code 不一致 |
| F-11 | **NIT** | B | `csrf_middleware.py:33-41` `MODULE_NOTE (English)` 區塊 | 現規範（feedback_chinese_only_comments 2026-05-05）默認中文，舊雙語塊修改時移除英文。本檔 modify 時應移除冗餘英文 MODULE_NOTE（保留中文）。E2 不直接修；提示 E1 |
| F-12 | **NIT** | A | `install_caddy.sh:117-119` | envsubst 不會校驗變數展開後是否含特殊字符（如 cert host 帶單引號）；目前 cert host 來自 `tailscale status --json` 安全，但建議加 `printf '%q'` 包裝防未來 user-provided host |

**HIGH = 2**, **MED = 3**, **LOW = 4**, **NIT = 3**。

---

## §2 PROXY-SPOOF-RISK fix verify（強制要求）

**Verify 結果 = PASS**。

- `auth_routes_common.py:60-72` `_proxy_headers_trusted()`：讀 `OPENCLAW_TRUST_PROXY_HEADERS` env；in `{"1","true","yes","on"}`（lower-case after strip）→ True
- `auth_routes_common.py:75-94` `_has_https_proxy_hint()`：**第一行** `if not _proxy_headers_trusted(): return False`；env 未設時 **完全不讀** `x-forwarded-proto` / `x-forwarded-ssl` / `forwarded`
- `auth_routes_common.py:222-225` `should_set_secure_cookie()` 流程：`OPENCLAW_COOKIE_SECURE=auto` 預設 → `request.url.scheme=="https"` OR `_has_https_proxy_hint(req)` → 二者均 False 時 cookie 不 Secure（fail-closed 至 scheme 真相）
- `test_ops1_csp_report_only.py:87-101` 顯式測：env unset + `x-forwarded-proto: https` 偽造 → `_has_https_proxy_hint` = False, `should_set_secure_cookie` = False ✓
- `test_ops1_csp_report_only.py:134-143` `_proxy_headers_trusted` edge values：`0/false/no/off/""/random` 全 False，`1/true/yes/on/TRUE/On` 全 True ✓
- Caller chain grep（E1 §3）：`auth_routes_common.py:75 _has_https_proxy_hint` 唯一 caller = `auth_routes_common.py:224 should_set_secure_cookie`；無 leak
- Regression：`test_batch_b_security_auth.py::test_cookie_secure_can_be_forced_and_proxy_trusted` L114-129 仍 PASS（10/10 passed）

**結論**：P1-OPS-1-PROXY-HEADER-SPOOF-RISK fail-closed semantics 落實。env unset 時偽造 proxy header 無效。

---

## §3 Test Coverage 邊界 Audit

| AC | Test | 覆蓋是否充分 |
|---|---|---|
| AC-5 POST without token → 403 | `test_post_without_csrf_cookie_or_header_returns_403` | ✓ |
| AC-5 mismatch token | `test_post_with_only_cookie_no_header_returns_403` + `test_post_with_mismatched_cookie_header_returns_403` | ✓ |
| AC-6 matching → pass | `test_post_with_matching_token_passes` | ✓ |
| GET regression | `test_get_request_bypasses_csrf` | △ **只測 GET**；缺 HEAD / OPTIONS 顯式驗證（CORS preflight 路徑） |
| Login exempt | `test_login_endpoint_exempt` | ✓ |
| CSP report exempt | `test_csp_report_endpoint_exempt` | ✓ |
| Shadow mode | `test_shadow_mode_lets_mismatch_through` | △ **只測 cookie+header 都缺**；缺「shadow + cookie 有 header 無 mismatch」+「shadow + cookie/header mismatch」分支 |
| AC-7 CSP report header | `test_csp_report_only_header_present_on_responses` | ✓ |
| AC-7 invalid JSON tolerated | `test_csp_report_endpoint_tolerates_invalid_json` | ✓ |
| AC-9 cross-platform | `test_ac9_no_hardcoded_user_paths_in_new_files` | ✓ |
| **F-1 raw fetch POST callsites** | **缺** | ✗ **未測** — 沒 test 對 risk-tab.js / app.js apiPost / handoff_helper.js / app-paper.js 寫操作驗證 CSRF header 是否送出。E4 regression 必跑 |
| **F-2 existing session csrf 缺失** | **缺** | ✗ **未測** — 沒 test 模擬「已有 `oc_auth_token` 但無 `oc_csrf` 的 user POST」場景，shadow vs enforcing 行為差異 |
| **F-5 logout exempt 是否合理** | **缺** | ✗ 沒 test 驗 `auth/logout` 在 enforcing 模式下 CSRF 行為是否符 spec §4.2 預期 |
| **constant-time compare correctness** | **缺** | △ `test_post_with_matching_token_passes` 驗 happy path；缺「token 長度不等」+「token 含 unicode/非 ASCII」+「empty string 雙端」邊界 |

**測試補強優先序**：F-1 / F-2 兩個邊界 E1 必補；其餘 LOW / NIT 視 E2 round 2 結果再決定。

---

## §4 退回 E1 修復清單

> E2 邊界規則：不直接改 business logic。以下退回 E1 修。LOW / NIT 級在退回單內以「建議」標示，E1 可酌情決定 round 2 是否一併處理。

**必修（HIGH 阻 merge）**：

1. **F-1 raw fetch POST CSRF 補洞**：
   - `static/risk-tab.js:1079` `POST /api/v1/ai_budget/config`：改走 `ocApi()` 或在 fetch options 加 `headers = ocCsrfHeaders('POST', headers)`
   - `static/app.js:407` `apiPost` helper：在 headers 處補 `ocCsrfHeaders('POST', headers)` 或重寫 wrapper 共用 `ocFetchWithCsrf`
   - `static/handoff_helper.js:820` `fetchWithIdempotency`：headers 區補 csrf token；注意 `Idempotency-Key` 已自訂，可同時帶
   - `static/app-paper.js:1325/1332/1397/1421/1451` `_fetchReplayJson` 及 inline：所有 `method:"POST"` 補 `ocCsrfHeaders` 或讓 `_fetchReplayJson` 內部自動補（每次 POST 走同一 helper 最簡）
   - 補完後跑 `node --check` 全部變動檔；新增整合 test「raw fetch POST 寫操作 CSRF header 自動附帶」覆蓋
   - 補 unit test：mock `document.cookie='oc_csrf=tok'`，呼叫 `apiPost('/x', body)` 後 spy `fetch` 第二參數 `.headers['X-CSRF-Token']` 必含 `tok`

2. **F-2 既有 session re-issue csrf**：
   - `auth_legacy_routes.py::auth_check`：cookie token 有效但 `oc_csrf` cookie 缺失時 → response 附 `set_csrf_cookie(resp, generate_csrf_token(), secure=should_set_secure_cookie(request))` 再回 `{authenticated:true}`
   - 補 test：fixture 設只有 `oc_auth_token` cookie → 呼 `GET /api/v1/auth/check` → response 應含 `Set-Cookie: oc_csrf=...`
   - 或者：deploy SOP 改為「先強制全部使用者 re-login」（不推薦，影響 UX）

**MED 修（不阻 merge 但 round 1 修）**：

3. **F-3 middleware ordering 註釋**：`main_legacy.py:313-323` 改寫 comment，反映實際 inbound order `CSRF → SlowAPI → CORS → route` 並說明為何此 order 安全（OPTIONS 不在 write methods，CORS preflight 不被攔截）

4. **F-4 `/api/v1/csp/report` 限頻 + 限 body**：
   - 加 `@limiter.limit("60/minute")` decorator（瀏覽器正常 violation 頻率 ≤幾次/分鐘）
   - `request.body()` size check `if len(body) > 8192: return JSONResponse(status_code=413)`
   - 或保守做：CSP violation 量大 → logger 改 ratelimited 形式 (per-IP cache + N/min)

5. **F-5 logout exempt 評估**：
   - **Option A**（推薦）：把 `/api/v1/auth/logout` 從 `_EXEMPT_PATHS` 移除；前端 logout 流程已是同源 + 帶 cookie，會帶 csrf header
   - **Option B**：保留豁免，但補 spec §4.2 明確列出 logout 例外的理由（DoS 風險可接受）
   - **Option C**（最差）：保留 + 不補理由 → 違反 spec

**LOW / NIT（建議；不阻 merge）**：

6. **F-6 install_caddy macOS chown**：偵測 `id caddy >/dev/null 2>&1` 後條件 chown
7. **F-7 systemd ExecStart pipefail**：`/bin/bash -c 'set -euo pipefail; source ...; ...'` 或拆 helper script `tls_renew.sh`
8. **F-8 Caddyfile default port 雙重 default**：移除 `:8000` 預設留 envsubst 那層
9. **F-9 macOS launchd plist**：補 plist template 落地 (`deploy/com.openclaw.caddy.plist`) + install script `--apply` Mac branch 實作；或在 SCRIPT_INDEX 明寫「macOS prod 部署 plist 需手動」
10. **F-10 csp_report 注釋與 code 不一致**：把 `silent 200` 改成 `silent 204`
11. **F-11 csrf_middleware 雙語 MODULE_NOTE**：移除英文版 MODULE_NOTE（保留中文），per 2026-05-05 規範
12. **F-12 envsubst special-char hardening**：cert host 經 `printf '%q'` 包裝

---

## §5 Verdict

**RETURN to E1** — 2 個 HIGH 阻 merge：

1. **F-1**：spec §6.1 AC-5/AC-6 + spec §7.2 風險 #2 mitigation（shadow mode）只能緩衝 14d；enforcing cutover 必斷至少 4 個 tab 寫操作（ai_budget / handoff / replay launch / earn promote 等）。E1 IMPL §5.2 已自承「E2 須 grep 確認沒有遺漏」並承認 risk，但**沒主動補洞**就交 E2，違反「E2 不寫業務碼」邊界。
2. **F-2**：現有 24h `oc_auth_token` 帶有 ~hundreds 個 session cookie 的 production state；enforcing 切換瞬間，已登入但未重新登入的 user 全斷 — 必須 server-side 補 seed csrf，不能假設 user 主動 re-login。

**Plus 3 MED 強烈建議 round 1 一併修**（F-3 / F-4 / F-5）以對齊 spec § 4.2 豁免清單 + spec §5.3 endpoint 強度。

**所有其他項目（PROXY-SPOOF env gate / cross-platform / except:pass / log %s / 私有屬性 / 24/24 既有 test PASS / 23 new test PASS）通過**。

**E1 round 2 預期工作量**：HIGH 2 個 ~3-4hr（含 test 補強）+ MED 3 個 ~1-2hr = round 2 總計 4-6hr。

---

## §6 E2 8 條 + OpenClaw §3 9 條 checklist 結果

| Item | 狀態 |
|---|---|
| 改動範圍 vs PA 方案一致 | ✓（worktree 內 OPS-2/4 認知豁免） |
| except:pass / 靜默吞異常 | ✓（無命中） |
| 日誌用 %s 不 f-string | ✓（csp_report logger.info / csrf_middleware warning 均 %s） |
| 寫入 endpoint 有 `_require_operator_role()` | N/A — OPS-1 沒新增寫入 endpoint（CSP report 是公開接收瀏覽器 violation） |
| `except HTTPException: raise` 順序 | N/A — csp_report 用 `except Exception:` only，沒 HTTPException 路徑 |
| `detail=str(e)` → `"Internal server error"` | ✓（csp_report 用 fail-silent + warning log，無 leak）|
| asyncio 中無 blocking threading.Lock | ✓ |
| 無私有屬性穿透 | ✓ |
| 跨平台 grep `/home/ncyu` / `/Users/[^/]+` | ✓（所有新檔 0 命中） |
| 注釋規範（中文優先） | △ F-11 `csrf_middleware.py` 仍含英文 MODULE_NOTE，建議清 |
| Rust unsafe / unwrap / panic | N/A — OPS-1 純 Python + bash |
| 跨語言 IPC schema | N/A |
| Migration Guard A/B/C | N/A — OPS-1 不開 SQL migration |
| Healthcheck 配對 | ✓ `install_caddy.sh:185` 跑 `curl -kI` + systemd `Restart=always`；spec §2.2 提 14d threshold |
| Singleton 登記 | ✓ CSRFMiddleware 是 stateless middleware；無新 mutable singleton |
| 文件大小 | ✓（csrf_middleware.py 228 行 / tls_cert.sh 117 行 / install_caddy.sh 193 行 — 全在 800 以下） |
| Bybit API 改動 | N/A — OPS-1 不碰 Bybit |
| Multi-session race §5 | ✓ fetch 後 HEAD = origin/main；無 sibling push 衝突 |

---

**E2 REVIEW DONE: RETURN to E1**

report path: `docs/CCAgentWorkSpace/E2/workspace/reports/2026-05-27--ops_1_e2_review.md`
