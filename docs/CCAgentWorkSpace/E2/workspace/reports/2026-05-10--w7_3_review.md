# E2 Review — W7-3 Emergency 1-Tick Defense（ma_crossover on_rejection duplicate_position sync）

**Date**: 2026-05-10
**Reviewer**: E2
**Commit**: `d8697c41`
**Scope**: `srv/rust/openclaw_engine/src/strategies/ma_crossover/strategy_impl.rs` (+48) · `tests.rs` (+152, 4 unit test)
**Verdict**: **APPROVE — pass to E4**

---

## 1. 重點審查 7 點對照

| # | PA #3 §8 重點 | E2 評估 | 結論 |
|---|---|---|---|
| 1 | duplicate_position 命中 `return` 是否誤跳 cooldown rollback | E1 設計 = 命中**僅跳** prev_position rollback；同時刻意保留 last_trade_ms（comment 與 PA §8 對齊：reject 後 cooldown gate 多擋一輪，hot loop 雙重防護）。fallback（contract drift）路徑仍走完整 RC-04 cooldown rollback。**設計對稱合理**，asymmetric 是有意（治標）。 | ✅ |
| 2 | reason 字串是否 byte-identical 契約對齊 | rejection_coding.rs:147-152 唯一 producer，格式 `"duplicate_position: {symbol} already {LONG|SHORT} {qty}"`，是 `pub(super)` enum 外部 caller 無法構造。E1 用 `contains()` 而非 `starts_with()` 容忍前綴 metric tag 注入；fallback 路徑 + warn log 對 future format drift 有監測。 | ✅ |
| 3 | TickContext signature 是否被動 | `git diff` 純改 `strategy_impl.rs` + `tests.rs`，0 byte 觸 TickContext / mod.rs / paper_state — PA dispatch 邊界守住。 | ✅ |
| 4 | regression PASS | `cargo test -p openclaw_engine --lib --release` Mac 端 = **2639 PASS / 0 fail / 0 ignored**；ma_crossover 子集 58/58。 | ✅ |
| 5 | typo / lint / dead code | `_reason` → `reason` 拿掉 underscore 是有意（現在實際讀取）；無新 warning（cargo check Mac 確認）；無 dead import。 | ✅ |
| 6 | 4 unit test 覆蓋足？ | already SHORT / already LONG / contract drift fallback / non-duplicate full rollback — 4 case 對齊 PA §6。**LOW gap**：fallback 路徑沒驗「contract drift 仍正確 rollback cooldown」，但 test #4 已間接覆蓋 RC-04 cooldown clear 邏輯（cost_gate reject + ts=0 sentinel）。multi-symbol concurrent 不適用（HashMap key 互不相干，無 shared state）。 | ✅ (LOW gap, 不阻攔) |
| 7 | CLAUDE.md §七 規範 | 注釋默認中文 ✅；無 `/Users/ncyu` / `/home/ncyu` 字面值（grep 0 命中代碼，pwd 路徑不算）；strategy_impl.rs 354 行 / tests.rs 822 行皆遠未達 800 警告線（tests.rs 為 #[cfg(test)] block）；無新 singleton；無 SQL migration。 | ✅ |

---

## 2. CLAUDE.md §九 8 條 checklist

| Item | 狀態 |
|---|---|
| 改動範圍與 PA #3 Option B 一致（不動 TickContext / 不改 router / 不做 5 策略） | ✅ |
| 沒 `except:pass` 或靜默吞異常（contract drift 走 tracing::warn + fallback） | ✅ |
| 日誌 `%s` 格式（Rust 用 tracing 結構化欄位 `symbol = %sym`，等價） | ✅ |
| 新 API endpoint 加 `_require_operator_role`（N/A，純 Rust strategy 內部） | N/A |
| `except HTTPException` 順序（N/A） | N/A |
| `detail=str(e)` 改 internal（N/A） | N/A |
| asyncio 路由無 blocking lock（N/A） | N/A |
| 無私有屬性穿透（直接寫 `self.positions` / `self.cooldown` 是 self own field） | ✅ |

## 3. OpenClaw 9 條 §3 checklist

| Item | 狀態 |
|---|---|
| 跨平台 grep（無硬編 user home） | ✅ |
| 雙語注釋 / 默認中文 | ✅（2026-05-05 governance change 後） |
| Rust unsafe / unwrap / expect | ✅（無新增） |
| panic 不在交易路徑 | ✅（無 panic / unwrap） |
| 跨語言 IPC schema 一致 | N/A（純 Rust 內部） |
| Migration Guard A/B/C | N/A（無 SQL） |
| healthcheck 配對（被動等待） | N/A（無新 TODO） |
| Singleton 登記 §九 表 | ✅（無新 singleton） |
| 文件大小 800/2000 | ✅（354 / 822） |
| Bybit API 改動 | N/A（純策略邏輯） |

## 4. 對抗反問結果

- **「sibling strategies bb_breakout / bb_reversion 不也撞同 hot loop？」** — 是，但 PA #3 §7 與 E1 §7 都明標「W6 baseline 沒看到只是 signal 沒對齊」，補丁範圍邊界在 ma_crossover only；治本走 W-AUDIT-8a Option A。E2 接受邊界。
- **「happy path early-return 跳 prev_position rollback，下一 tick prev_position 是否殘留 stale 狀態？」** — 看 line 180：on_tick 每 tick 必 overwrite `prev_position[sym]` snapshot，無 stale leak。
- **「fallback 路徑（contract drift）會否退回 hot loop？」** — 會，但這是 acceptable degradation：契約破裂時 fail-back 到舊 RC-04 行為（hot loop 是 pre-W7-3 baseline），不引入新風險，且 warn log 給 operator 修 reason format 訊號。
- **「Rust release test 是否真跑？」** — Mac 端 `cargo test --release -p openclaw_engine --lib` 2639 PASS / 0 fail / 0 ignored / finished in 0.56s，已驗。Linux runtime 端等 PM `restart_all --rebuild` 時生效。

## 5. Findings

| 嚴重性 | 位置 | 描述 | 動作 |
|---|---|---|---|
| LOW | tests.rs | fallback (contract drift) 路徑沒獨立驗 cooldown clear 行為（測 #4 間接覆蓋同 path） | **Accept**（不退）；W-AUDIT-8a Option A 治本時可順便補 |

無 CRITICAL / HIGH / MEDIUM finding。無 direct fix 需要（無 typo/lint）。

## 6. Regression 跑結果

```
cargo test -p openclaw_engine --lib --release ma_crossover  →  58 passed; 0 failed; 0 ignored
cargo test -p openclaw_engine --lib --release (full)         →  2639 passed; 0 failed; 0 ignored; 0.56s
```

## 7. 結論

**APPROVE — pass to E4**

W7-3 補丁設計對齊 PA Option B、契約對齊 rejection_coding.rs byte-identical 格式、保留 RC-04 cooldown rollback 副作用（fallback 路徑）、4 unit test 覆蓋核心 case、2639 lib regression 0 fail。1 個 LOW finding 是 nice-to-have 不阻攔，留 W-AUDIT-8a 治本時順帶。

E4 可接：
1. 跑 mock fill flow 確認下一 tick 進 exit 分支不再撞 gate 1.5
2. 驗 cargo build --release --bin openclaw-engine 在 Linux 端可 link（Mac 端僅 cargo check 過）
3. 走 `restart_all --rebuild --keep-auth` deploy SOP 後 watch INXUSDT ma_crossover risk_verdicts duplicate_position rate（應降 ≤1/min）

---

**E2 REVIEW DONE**: APPROVE · report path: `srv/docs/CCAgentWorkSpace/E2/workspace/reports/2026-05-10--w7_3_review.md`
