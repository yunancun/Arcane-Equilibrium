# E1 Memory — 工作記憶

> 本檔=長期教訓+近期記錄；超 300 行由 R4 巡檢標記、PM 派工壓實，舊條目原文遷 memory-archive.md（append-only）；agent 完成序列照常追加於檔尾。

## 長期教訓

- 自評不可信：任何「IMPL DONE / PASS」宣稱（含前手 agent、含自己）commit 前必 grep 證 fn 真存在 + 親跑 spec'd 測試；commit message 與 report 必對齊，Partial 就寫 Partial 並列未進 commit 的檔案清單。
- PG-coupled 改動（V### migration / schema / writer）必先 ssh trade-core 對 Linux PG empirical reflection（`\d` + pg_get_constraintdef）；spec 內 SQL 非 SSOT；migration 必雙 apply 驗冪等；PG 容器=trading_postgres、user trading_admin、db trading_ai。
- spec / dispatch packet 的路徑、fn 名、section 引用、base SHA、LOC 估算常 stale 或 typo：IMPL 前 find / grep / git cat-file 驗證，以代碼真實狀態為準；偏差必 push back 且在報告明標 deviation（prompt > spec）。
- 改 struct 欄位 / fn 簽名 / enum variant / return 語意必 grep 全 production caller + 全 test 構造點；`cargo build --lib` 過 ≠ `cargo test` 編譯過（manual struct literal、exhaustive match）；gate / cutover 類改動必跑 base-vs-HEAD 全套失敗清單 diff。
- production path 嚴格 fail-closed：不留 unwrap()、normalize / parse 得 0 或非法值回 None 不送原值、缺 secret / 解析失敗=disabled 非 error；LiveDemo=live 槽憑證+demo 端點，是合法 Live 模式（5-gate 按 Live 嚴格標準），任何守衛不可誤殺。
- 測試必須 bite：修 race / gate 後做 mutation A/B（臨時還原 buggy 版證 test 真 FAIL 再還原修法）；不 monkeypatch 掉被測對象；fixture 必鏡像真實上游 boundary 不變量（synthetic fixture 讓 silent bug 沉默通過）；對抗 probe 必含 production-empirical 真實字串，全 mock 自我引用=自驗空話。
- 「語法過+已提交 ≠ 已接線」：helper 加了但 emit site 不走、Python client 存在但 dispatch arm 沒接、gate 維度沒 thread 進 producer 都是 silent 死碼；接手他人 IMPL 必 trace 全鏈接線 + 親跑 full test。
- refactor 搬移模塊前先 grep tests 的 monkeypatch / patch 命名空間（命名解析縫是 load-bearing 約束）；stub `from PKG import SUB` 須 dual patch sys.modules + package attr，finally 反序還原防跨 test 汙染。
- multi-session dirty tree 紀律：不 revert 不認識的改動；stash / linter 事件後 git status + grep 驗自己 edits 還在；cross-wave 檔 `git checkout HEAD --` 還原；meta-doc 用 `git commit --only`；worktree 內寫檔絕對路徑必指 worktree root 非主 srv tree。
- Mac 只能驗純邏輯層（無 fastapi / pytest 受限、無 PG / engine）；advisory-lock、PG runtime 語意、真連線 smoke 必 ssh trade-core 實機；Mac engine not_running 是預期非故障。
- 跨平台路徑紅線：production 代碼與 doc 均不得出現 /home/ncyu、/Users/...（placeholder 也會被 grep 命中）；用 `Path(__file__).resolve().parents[N]`（N 必實測）或 CARGO_MANIFEST_DIR / srv_root() 推算。
- ssh 非互動環境陷阱：不讀 .bashrc → 無 OPENCLAW_DATABASE_URL；`set -a; . envfile` 不可靠且 env 檔密碼可能與運行容器 drift（docker inspect 為準）；API bind 在 Tailscale IPv4 非 loopback；cron 無真 shell parser（% 有特殊語意）。
- grep 驗證要正反兩面設計：裸字面 grep 會被註釋 / docstring 合法提及誤紅；剝字串的 code-only helper 會漏 SQL 字面；include_str! 自 grep 會抓到自己的解釋註釋。
- 註釋是 governance trail：誤導性註釋會合理化 race / 掩蓋 bug，修 bug 必同步修註釋；改 return / 降級語意必清同檔過期註釋；test vs source 矛盾先找 caller 生產意圖 + git -S 定位翻轉 commit，不預設改哪側。
- 數學 / 研究 gate 慣例：funding / 事件累積用 half-open interval 防重複計算；denominator non-positive 即時 FAIL 防 NaN；bootstrap seed per-caller 獨立保留；sample_insufficient 與 signal_failure 必分流避免誤 reject；跨寫者 hash 一致性=共用 EXACT 物件不重算。
- 檔案 >800 行即拆（metrics / IO 分層）；新測試先讀目標目錄既有 inline-test 拆分慣例再選；大規模 callsite 機械改動用 receiver-aware / struct-context 的腳本批改，改完逐點驗誤注入。
- 工具環境韌性：Mac bash 通道間歇靜默 → 輸出 redirect 到 /tmp/*.txt 再 Read 讀回；psql 2>/dev/null 會吞 SQL error 必交叉檢核；cargo 並行 build race 的偶發 filesystem 失敗先 retest 再追因。
- Rust 慣例：依賴晚建資源用 Arc<RwLock<Option<...>>> late-inject slot（對齊 cost_edge_advisor / h_state_cache / account_manager）；hot-path 多欄位 mut 借用用顯式 split-borrow method / free fn；持 secret 的 struct 禁 derive(Debug)，手寫 REDACTED Debug。

## 近期記錄

## 2026-06-08 L2 D3 redactor v3→v4：CRITICAL fast-path gate secret-leak + 2 LOW（待 re-E2/re-E3）
- **CRITICAL（re-E2 Finding 1，gate secret-leak）**：v3 fast-path gate `not _NEEDS_OFFSET_MAP_RE.search(text) AND is_normalized(NFKC)` 的手列 regex **不是** strip 謂詞 `_is_control_or_format`（strip 全 Cc/Cf + 零寬，只留 \t\n\r）的超集。brute-force 實測 OLD regex **漏 136 個 strip-set 字元**（U+00AD/U+061C/U+06DD/U+070F/U+180E/U+2066-2069 isolates/U+E0000 tag 區 U+E0001+U+E0020-E007F），這些 strip 掉但 NFKC-stable → 被它切斷 keyword 的 secret 走 fast-path 在 **raw 文本**（不 strip）偵測→keyword 仍斷→不命中→**verbatim 入庫洩漏**。**修法=E2 方向 A（gate-set==strip-set by construction）**：新 `_needs_offset_map(text)` = `'%' in text OR any(_is_control_or_format(c)) OR not is_normalized(NFKC)`，直接複用 strip 謂詞當 gate，drift 結構上不可能。brute-force 證新 gate 0 miss（全 223 strip-set 字元 route slow-path）。**教訓：fast-path 廉價快篩若用「手列 codepoint regex」鏡像另一個謂詞，必 drift；正解=直接呼叫該謂詞，禁雙源**。
- **LOW-1（re-E3 keyword 補全）**：`_KW_DISPATCH` keyworded 臂 + `_SENSITIVE_KEY_RE`（JSONB-key 臂）補 `auth_signing_key/hmac_key/hmac/signing_key/signing_secret/auth_key/裸 secret/private_key`。alternation 序「長/具體先於短前綴」（hmac_key 先 hmac、auth_signing_key 先 signing_key），裸 secret 置 g_api 末作 catch-all→短前綴不 shadow 長 keyword + 少回溯（ReDoS-safe）。hmac→g_bearer、signing 類→g_auth、裸 secret/private_key→g_api。
- **LOW-2（re-E3 DoS cap）**：`redact()` 入口加 256KB（`_MAX_REDACT_INPUT_CHARS`）size cap，截斷後消毒、附 `…[TRUNCATED:n chars]` marker（事後附不過偵測零誤遮）+ 冪等抑制（尾端已有 marker 不再截，`_TRUNCATION_MARKER_TAIL_RE`）。**理由=store-original per-char Python 迴圈在 degenerate 全-evasion super-linear（~n^1.7，800K 9.4s）**；cap 後 worst-case clamp ~23ms（800K/2M 皆→256K）。放在 redactor 入口=一處 bound 全 caller（ledger system_prompt/raw_response/final_summary + cost-tracker 自由文本 + JSONB leaf）。256KB 遠大於現實 L2 response 不影響 forensic。
- **改檔（3 source 區 + 2 doc）**：`l2_secret_redactor.py`（_needs_offset_map 取代 _NEEDS_OFFSET_MAP_RE / keyword 補全 / cap+marker / REDACTOR_VERSION v3→v4 / +logging）；`tests/test_l2_d3_ledger.py`（v3→v4、+TestRedactorFastPathGateCriticalV4 含 7-param Cf 回歸 + brute-force-strip 斷言 + byte-identical、+TestRedactorKeywordCompletenessV4 free-text+JSONB 兩形、+TestRedactorSizeCapV4 6 test，共 +33 test）；`singleton-registry.md` §2.6 行號（class :73→:99 / _WRITER :330→:364 / getter :333→:367 / engine-call :352→:655，def :323 巧合相符）；PA design §B.5.1 v4 addendum。**未動 l2_call_ledger_writer.py / layer2_cost_tracker.py / layer2_critic.py / migrations / 接線 / sha256（E2 已 PASS）**。
- **Mac pytest 真實**：test_l2_d3_ledger.py **78 passed + 4 xfailed**（4 naked-residual xfail-strict 全 held 無 XPASS=gate fix+keyword 補全沒誤抓裸 token）。6 in-scope 檔合計 **272 passed + 4 xfailed + 0 failed**。全庫（除 tests/replay 4 個 pre-existing import-path collection error，與我無關）**66 failed=100% pre-existing infra**（PG port 15432 refused + engine.sock 缺，CLAUDE §六；grep 證 0 失敗檔 reference 我任何 module）。ReDoS：cap 後 worst-case bound 實測、realistic 線性。

### residual PART 4 Phase 2 — MIT HIGH-1/HIGH-2 follow-up（2026-06-08, commit 67730b7b, 待 re-E2+MIT-confirm）
- **任務**：MIT Linux-empirical 審計揭露我 P2（2a5df09e+7d2cdcba）兩個 Mac unit test 抓不到的 pre-activation HIGH。同分支 `feature/residual-activation`（worktree `/private/tmp/wt-residual-act`）follow-up commit（**非 amend**）。行為仍三重 OFF。
- **HIGH-1（PG jsonb 丟負零破壞 hash cross-check）**：registry hash 進 PG 前對 in-memory report 算（`residual_hidden_oos_bridge._canonical_sha256`，TEXT）；source-contract 在 **jsonb 讀回後**重算（`candidate_evidence_source_contract:399`）。**PostgreSQL jsonb 丟 IEEE-754 符號位（-0.0 讀回變 0.0）**，但 int/float 不正規化、其他欄位不動（MIT 真 PG round-trip 驗）。-0.0 在 defer/weak-alpha cohort 真實可達：弱/共線 factor 的 `np.mean`/回歸係數 → `residual_mean_bps`/`beta_loadings[factor]`（`_to_bps` 的 `-0.0*10000=-0.0`）。→ `registry_hash != expected_hash` → INVALID。**修法=在 canonical report 唯一表示 `ResidualEdgeReport.to_dict()` chokepoint 加遞迴 `_normalize_zeros`（`x + 0.0 if x == 0.0 else x`），report 永不帶 -0.0 → 進 jsonb 前後 bytes 相同**。教訓：**hash 跨 in-memory↔PG jsonb 邊界時，序列化前必須正規化所有 IEEE-754 表示歧義（-0.0、NaN 已被 _json_safe 轉 None）；單元測試在 in-memory 層過、PG 才咬 → 必須在 chokepoint 抹平而非靠 caller**。
- **HIGH-2（net_side strategy-wide 而非 per-symbol → funding 反號放大 carry）**：`derive_net_side_from_fills`/`load_candidate_net_side` 只按 `name==strategy_name` 篩，net_side 跨全 symbol 聚合。MIT 實測 grid_trading 全域淨 short、但 per-symbol RAVEUSDT/PENGUUSDT 淨 long → `grid_trading::RAVEUSDT` 候選拿 -1 而真實曝險 +1 → funding sign 反號 → carry **放大**（=本功能要殺的 false-promote 向量）。被三重 OFF + single-config-defer 遮蔽。**修法=加 optional `symbol` 參數（None=全 symbol，既有 caller byte-identical；提供時同時篩 `fill.symbol==symbol`），`_FILLS_QUERY` 已回 `f.symbol` 故 Python 端篩不改 query shape；orchestrator 傳候選實際 symbol**。教訓：**候選身分是 `strategy::symbol`、factor（funding）也是該 symbol 的 → net_side 必須逐 (strategy, symbol)；strategy-wide 聚合在「策略整體一方向、個別 symbol 另一方向」時給出相反 sign**。
- **mutation bite 雙證**：bypass `_normalize_zeros` → HIGH-1 兩測紅；drop symbol filter → HIGH-2 三測紅（e2e `assert -1 == 1`）；還原全綠。**陷阱**：給 `load_candidate_net_side` 加 `symbol=` kwarg 後，3 個既有 monkeypatch fake 的簽名（`(conn, strategy, *, engine_mode, since)`）會因 orchestrator 新傳 `symbol=` 而 TypeError → 必須同步加 `symbol=None` 到 fake 簽名（改 signature 的副作用=既有 test fake 必同步 retrofit）。
- **驗收**：full ml_training+learning_engine **855 passed / 31 skipped**（850 baseline + 5 新，0 回歸）。0 hard-boundary token / 0 hardcoded user path（grep 自證）。staged 只我 5 檔（`git add` 列舉），pre-existing dirty `E2/memory.md` + 未追蹤 PA report 留外。

## 2026-06-08 L2 Advisory Mesh Phase 2 — Orchestrator + registry + LANE_DIRECTION linchpin（CC linchpin 階段，待 CC/E2/E3）
- **任務**：實作 PA P2 locked 設計（`2026-06-08--l2-p2-orchestrator-tech-design.md`）。branch `feature/l2-critic-lessons-tools`（P1 D3 已 commit @f1c3c1ca）。operator 拍板 registry=**TOML-only SSOT**（無 DB 表/V137，零 migration/零 Rust/IPC）。全 capability enabled=false，本 pass 實作但**不部署**。
- **新檔（5 source + 1 test + 1 TOML，全 control_api_v1/app/ + settings/）**：
  - `l2_capability_registry.py`(355)：**LANE_DIRECTION 型別表（:67，無 'live' key）+ effective_autonomy（STEP-1 `if LANE_DIRECTION[cap.lane]=="expand": return MANUAL` @:119，函數頂、非-overridable，CC 5/10 linchpin）**。typed model（pydantic extra="forbid"，enabled 預設 false）。loader 三 reject 分支（宣告 autonomy_level / can_auto_deploy_to_paper-as-posture / lane∉表）+ unknown-top-key + min_tier∉L1-5。
  - `l2_prompt_contract_registry.py`(170)：versioned 確定性 PromptContract registry（frozen）；種子 l2.manual_reasoning 沿用既有 l2_contract.v1/l2_schema.v1（single source from layer2_engine 常數，不複製字面）；`resolve_contract_versions` fallback 既有常數。
  - `l2_out_of_bound_guard.py`(167)：確定性 guard，verdict {pass,clamp,reject}；leverage/size clamp、負成本 reject、invented-axis reject、None/NaN reject；無 model。
  - `l2_conflict_adjudicator.py`(136)：**fixed precedence literal dict（PRECEDENCE contract>neutral>expand）**；adjudicate_vs_gate（gate reject 永勝）+ adjudicate_cross_capability（方向優先/更嚴/orthogonal/unresolved→escalate）；**stateless 純函數，無 model 呼叫（F.2/CC 6）**。
  - `l2_advisory_orchestrator.py`(447)：conductor singleton（module-level `_ORCHESTRATOR` + getter）。admission stage（dedup→debounce→coalesce→budget→tier/posture，§F.1）+ fail-safe SM（HEALTHY→RETRY→DEGRADE_OLLAMA→NO_ADVICE→TRIPPED→GLOBAL_CONSERVATIVE，§H）。**0 order/lease/promote_tier**（CODE-level tokenizer 自證）。
  - `tests/test_l2_p2_orchestrator.py`(678，**60 test 全綠**)。
  - `settings/l2_capability_registry.toml`：P2 skeleton（無 capability stanza，P3 才填；checked-in 預設可 boot）。
- **改檔（2 source + 1 doc）**：
  - `layer2_engine.py`(+18/-2)：**wiring delta（§A.2）= contract_ver/schema_ver 改由 contract registry 解析（lazy import 避 cycle，fallback 既有常數 fail-soft）**；manual-trigger 解析回既有 l2_contract.v1（零回歸，`TestEngineWiring` + 我 `TestEngineWiringDeltaReachable` 雙證）。
  - `layer2_routes.py`(+95，純加)：`_get_orchestrator()` accessor + 4 route。**E3-E1：write（/registry/reload、/orchestrator/fail-safe/reset）= operator-scope（reuse `require_scope_and_operator(actor,"ai_budget:write")`）；read（/orchestrator/status、/registry/capabilities）唯讀**。route 薄 parse→call→format。
  - `singleton-registry.md` §2.6.2（Orchestrator）+ §2.6.3（admission=orchestrator-internal state 無獨立 binding）+ §2.6.4（adjudicator/registry/guard=stateless 無 singleton row）。
- **CC carbon-layer 單一 construct 位置（供 CC grep）**：C1=l2_advisory_orchestrator.py CODE 0 promote_tier/acquire_lease/order；C2=orch+registry CODE 0 can_auto_deploy_to_paper；F.2=l2_conflict_adjudicator.py CODE 0 run_session；LANE_DIRECTION=l2_capability_registry.py:67（表）+:119（STEP-1 if）。
- **教訓（grep 測試 vs 註解）**：C1/C2/F.2 的 grep 測試**不能裸 `'promote_tier' in src`**——MODULE_NOTE/docstring 合法地寫「不呼 promote_tier」解釋不變式，會誤紅。正解=用 `tokenize.generate_tokens` 剝 COMMENT+STRING token 只留真碼，再驗禁字 0 引用（mutation bite 自證：comment-only 被剝、real-call 保留）。CC 概念上 grep 的是真 `if`/呼叫非散文。**這是 carbon-layer 不變式測試的通用陷阱：驗「code 無引用」要剝註解，否則 self-documenting 的安全註解反而讓測試紅**。
- **Mac pytest 真實**：P2 suite **60 passed**（LANE_DIRECTION linchpin 9 + registry loader 13 + carbon grep 6 + adjudication 7 + guard 7 + admission 11 + fail-safe 6 + contract 4 + wiring-reachable 1，含 mutation：STEP-1 移除/comment-vs-code bite）。既有 L2 全套 **298 passed+4 xfailed 零回歸**（test_l2_d3_ledger/layer2/critic/escalation/g3_08/tools）。broad layer2|l2|tier|governance **800 passed+1 skipped+4 xfailed**。全庫 collect（除 tests/replay 4 pre-existing `from program_code` import-path error，與我無關）4456 clean。
- **環境陷阱**：Mac 預設 `python3`=3.10.1（無 tomllib/pydantic）；**正解 venv=`/Users/ncyu/Projects/TradeBot/srv/venvs/mac_dev/bin/python`（3.12.13 + pydantic 2.13.3）**。tomllib 需 3.11+；路徑解析 reuse paper_trading_routes 範式（`OPENCLAW_BASE_DIR` env + `parents[5]` fallback，禁硬編 user path）。
- **scoped footprint**：只我 7 新 + 3 改。4 個 pre-modified 檔（aeg builder/tests、test_layer2_critic.py）是別 session WIP（initial git status 已 ` M`，0 reference 我 module，未碰）。
- **owed**：CC load-bearing audit（stress-test 5/6/10/15/16/18）+ E2 + E3（write-auth + fail-safe-under-fault）+ E4 Linux regression（無 migration dry-run owed，P2 零 migration）。

## 2026-06-08 L2 P2 orchestrator — E2 RETURN（1 HIGH+2 MED）+ E3-LOW-1 修補（待 re-E2/re-E3）
- **任務**：修 L2 P2 的 E2 RETURN + E3-LOW-1，嚴格照 pinned 不擴範圍。branch feature/l2-critic-lessons-tools（P2 疊 P1 f1c3c1ca，未 commit）。linchpin（LANE_DIRECTION/C1/C2/F.2/fail-safe iron rule）勿動。Mac venvs/mac_dev/bin/python（3.12）。改 2 source 檔 + 1 test 檔，**0 改 layer2_routes.py**（E3-LOW-1 源頭在 loader，route 只 echo）。
- **HIGH-1 per-cap 日上限 no-op 修法 = 真 per-cap accumulator（首選，非退路）**：舊 `if cap_daily>0 and remaining<=0` re-read **全域** remaining（check_daily_budget 是所有 cap 的 total，無 per-cap 拆分）→ 宣稱了不存在的保證。改：`_AdmissionState` 加 `cap_daily_spend: dict[(cap_id, utc_day), float]`（in-memory，restart re-arm 符 §L）；admission stage 4 比「該 cap 當日累計花費」vs cap_daily；新增 `record_capability_spend(cap_id, usd, now)` public mutator（P3 executor model 呼叫後累計，P2 dispatch dormant 故 accumulator 恆空、閘現不觸發、P3 接線即活）；`_cap_spend_today` 讀。**獨立於全域**：cap_daily=$0.50 而全域 remaining=$2.00 時該 cap 花滿 $0.50 仍被擋。UTC-day 歸桶（`_utc_day` 對齊 cost_tracker daily_spend date_key），跨日歸零。
- **MED-1 fail-safe escalation 與 ollama_available 解耦**：舊 `elif ollama_available: DEGRADE_OLLAMA` 在計數閾值「之前」→ ollama-up 持續失敗永卡 DEGRADE_OLLAMA、到不了 TRIPPED（違 §H systemic escalation）。改：先以連續失敗計數定升級階（≤max_retries→RETRY / <5→中間階 / <10→TRIPPED / ≥10→GLOBAL_CONSERVATIVE），`ollama_available` 只在中間階選 floor（DEGRADE_OLLAMA vs NO_ADVICE）。保 subtraction-only（每階只減 L2 能力，0 live-enabling write，CC iron rule grep 仍綠）。**保既有測試**：test_retry_then_degrade_then_no_advice / test_escalates 語義不變。
- **MED-2 admission window 無 lock → race 修 = 整段 _admit 納 self._lock**：舊 dedup/debounce read-modify-write 無鎖，P3 多執行緒同 dedup_key 兩 trigger 都讀 None 都 admit。修：Stage1 讀 + Stage2 debounce 讀寫 + Stage4 per-cap 讀 + admitted-mark 收進「同一」`with self._lock:` 臨界區（檢查→放行→標記原子化）。**關鍵陷阱**：`self._lock` 原是 `threading.Lock`（non-reentrant），但 admission 內呼 `_cap_spend_today`（也取本鎖）→ **自鎖 deadlock**。**必須改 `threading.RLock()`**（同執行緒重入安全，跨執行緒互斥不變）。`_admit` 是 sync 無 await（asyncio 邊界不在此），故 threading lock 跨 sync 呼叫（_check_budget / effective_autonomy）安全。
- **E2-LOW-1 read-path malformed-TOML fail-soft**：cold-cache 壞 TOML → `_registry_obj()` loader raise → GET /orchestrator/status 500。修：`_registry_obj()` catch `L2RegistryLoadError` → 退 last-good（`_registry_last_good`，reload 後新 TOML 壞時保留前一份）或空 registry（cold-cache 無 last-good）+ 設 `_registry_degraded` 旗標（status() 暴露 registry_degraded/registry_degraded_reason）。**仍 fail-closed**：降級時 enabled_capabilities 空，不誤放行 advisory。write-path（reload route :722）另行明確 reject，不走 fail-soft。
- **E3-LOW-1 path leak 源頭修 = loader basename 化**：`l2_capability_registry.py:309`（唯一嵌 `{p}` 的 error，其餘 error 嵌 cap_id/field/lane 無 host path）改 `{p.name}`。error 字串回給已認證 caller（route status/capabilities/reload），完整 resolved path 含 /home/ncyu，**繞過 main_legacy `_LEAK_PATTERN`**（只處理 HTTPException-str-detail，不處理 :696 200-body / :726 dict-detail，且 path 本身不 match `:\s+\w*Error|:\s+\d+` regex）。basename 足夠 operator 定位（檔名固定）不洩主機路徑，亦消 CLAUDE §六。route 已 reason-coded（l2_registry_load_rejected）+ 現 path-free 故不改 route。
- **P3 deferred（碼註解 + PA §備註標）**：E2-LOW-2（adjudicate unknown verdict→b_wins）+ E3-LOW-3（guard 只查 top-level nested 漏）= P3（真 gate verdict + appliers 落地時收）。本 pass 不修。
- **Mac pytest 精確**：test_l2_p2_orchestrator.py **74 passed**（60 原 + 14 新）。14 新：per-cap-independent-of-global / per-cap-utc-rollover / per-cap-record-noop / per-cap-no-budget-skip / admission-window-uses-lock(RLock proxy) / concurrent-same-dedup-only-one-admitted(16-thread barrier) / mutation-bite-per-cap-uses-accumulator / ollama-up-persistent-fail(50×→GLOBAL_CONSERVATIVE) / ollama-up-reaches-tripped-band / cold-cache-malformed-status-no-raise / failsoft-retains-last-good / failsoft-recovers / parse-error-basename-not-abspath / loader-source-basenames。broad L2|layer2|critic|escalation|tier|governance **884 passed+1 skipped+4 xfailed**；adjacent 7 L2 檔 **372 passed+4 xfailed** 零回歸。
- **測試陷阱**：`_thread.RLock.__enter__` 唯讀無法 monkeypatch（AttributeError attribute '__enter__' is read-only）→ 改用 `_CountingRLock` proxy（delegates 真 RLock + 計數 __enter__）替換 `o._lock`。per-cap 測試證無 deadlock（若 Lock non-reentrant 會在 locked _admit 內呼 _cap_spend_today 時掛死）。tests/replay 4 個 `from program_code` import-path error pre-existing 與我無關（--ignore=tests/replay）。
- **OWED to PM**：Linux E4 regression（全套 + flag scenarios）。本 pass Mac-only（FakeTracker/injected registry_loader/mock ledger，無真 PG/engine）。

### 2026-06-08 RESIDUAL-GAP-A-BASKET-FIX 教訓（real-data 選取 bug，合成測試漏掉）

- **「從 PIT-active universe 取字母序前 N」是 silent real-data bug，合成測試結構性漏掉**：`_load_multi_factor_inputs` 用 `sorted(set(active))[:max_basket_symbols]` 取 market basket。合成測試直接注 klines（`SYM0..9USDT` 全有資料），永不走真 DB 選取 → 永遠綠。但真 Linux PG 上 PIT-active ~616 symbol，字母序前 60 = `0GUSDT`/`1000000BABYDOGEUSDT`/`1000000CHEEMSUSDT`…（冷門幣，窗內 4h bar=0）→ market basket 每桶 0 成員 → `no_aligned_buckets` → btc/market/funding 多因子閘**真實資料上永不計算**（btc-only 正常）。**教訓：凡「從真實 DB 選取子集再算」的邏輯，合成測試注資料會繞過選取 seam；必加一條走真選取邏輯（不 monkeypatch 該 selector）的測試，否則選取 bug 永不被測到**。本次新增 `test_load_multi_factor_inputs_selects_data_bearing_not_alphabetical` 刻意**不** patch `load_liquid_basket_symbols`（與既有 `_patch_db` fixture 相反），走真 seam — 這正是當初漏掉 bug 的同一盲點。
- **修法：按窗內 bar 計數（流動性代理）選取，candidate 域夾在 PIT-active 集內保 survivorship**：新 `load_liquid_basket_symbols(conn, candidate_symbols, ...)` 一條 read-only `SELECT symbol, count(*) FROM market.klines WHERE timeframe=%s AND ts BETWEEN %s AND %s AND symbol = ANY(%s) GROUP BY symbol ORDER BY count(*) DESC LIMIT %s`。真實 PG `count(*) GROUP BY` **不回 0-bar symbol**（沒列就不入 group）→ 選到的必有資料。**survivorship 不變式保留**：candidate 仍是 `pit_active_symbols(lifecycles, s, e)`（lifecycle 權威，含已下市但當時在交易者），`symbol = ANY(active)` 把流動性排序**夾在 PIT-active 集內** → 「當時在交易、今已下市」者只要窗內有 bar 仍入選；「今日才上市」者因不在 active 集被排除。**不能直接換成「今日 universe」**（會破 MIT P1 驗的 PIT 性質）。
- **real-data 驗證是唯一能證此 fix 的證據（合成不行）**：ssh trade-core read-only conn（`set_session(readonly=True)` 硬 guard）+ no-op capturing register_fn（從 `body.manifest_jsonb["demo_residual_alpha_report"]` 抓 report，回 dry result，**零寫入**）。grid_trading::BTCUSDT 真跑：`basket_symbols_with_bars>0=60/60`（修前全 0）、top by liquidity `BTCUSDT/ETHUSDT/ZECUSDT/DOGEUSDT/SOLUSDT`、`market_buckets=113`（修前 0）、閘出真 report `verdict='fail'`（誠實，非 bug）`residual_mean_bps=12.44` `beta_loadings={btc:7.83,market:-1.67,funding:-63.38,_intercept:-122.36}` `net_side=-1`。**驗後 `replay.experiments` 近 10min +0 row、drar source='residual_stage0r_preflight' +0 row** = 確認零寫入。**部署機制**：Linux 在 main（非我 feature 分支），但 PART 4 commits 已部署 main（dep files SHA 全 SAME），只我 2 個改檔 DIFF → scp 我的改檔覆蓋（先備份 /tmp/_orig_*）→ 跑驗證 → restore 原檔 + 刪 temp（Linux git status 還原 clean）。
- **env 檔 ssh 非互動 shell `set -a; . file; set +a` 不可靠**：`POSTGRES_PASSWORD` 等 KeyError（值未進 os.environ）。改在驗證腳本內**直接 parse KEY=value**（永不 print 值）robust 於 ssh 非互動 sourcing 失敗。**env 檔只有 `POSTGRES_DB/USER/PASSWORD/PORT`，無 HOST** → host 預設 `127.0.0.1`（context-loading.md 文件化的本機 PG 連線）。macOS 無 `timeout` 指令（沿用既有教訓，ssh 直接跑別包 timeout）。
- **驗證結果以即時跑為準**：full ml_training+learning_engine **859 passed/31 skipped**（baseline 855/31 + 4 新，0 回歸）。mutation-verified：把選取還原成 alphabetical slice → 跑 orchestrator 測試**紅**（`0GUSDT`/`1000000BABYDOGEUSDT`/`1000000CHEEMSUSDT` 跑進 basket）→ restore 後綠。commit `2fca92fe`（`git commit --only` 隔離 4 檔，避開 worktree 既有非我 TODO.md v121 + E2 memory.md edit + 未追蹤 PA report），未 push，等 E2。

## 2026-06-09 L2 P2 orchestrator re-E2：MED-1（cap_daily_spend 無界）+ LOW-1（docstring overclaim）+ /cost/reset operator-scope fold-in（待 re-E2/re-E3）
- **任務**：嚴格照 pinned 修 re-E2 三項，不擴範圍。branch feature/l2-critic-lessons-tools（P2 疊 P1 f1c3c1ca，未 commit）。CC APPROVE/E3 PASS 的 linchpin（LANE_DIRECTION/C1/C2/F.2/fail-safe subtraction-only/RLock/per-cap-獨立性）勿動。Mac venvs/mac_dev/bin/python(3.12)。改 2 source（l2_advisory_orchestrator.py / layer2_routes.py）+ 2 test（test_l2_p2_orchestrator.py 新 14 test + test_layer2.py 修 2 collateral）。
- **MED-1 prune（latent 無界）**：上輪我加的 per-cap accumulator `cap_daily_spend: dict[(cap_id,utc_day),float]` 永不 prune→每日每 cap 一永久 key 線性無界（P3 接 executor 即活、無 healthcheck）。修=`_AdmissionState._prune_stale_spend(today)`（list-comp 收非-today key 後 del），在 `record_capability_spend` 內**先 prune 再累加今日**（持鎖內）→ dict 任何時刻只含「今日」key（≤cap 數）。**安全性論證**：閘 `_cap_spend_today` 只讀 ts 所屬 day_key，跨日歸零靠新 day_key 無紀錄；舊 day 桶從不再讀，留著純洩漏。**保留策略=只留今日**（碼無跨日比較需求；不留昨日）。
- **LOW-1 docstring（純註釋，無碼改）**：`_registry_obj` docstring 舊「降級時 enabled_capabilities 為空，不會誤放行」僅 cold-cache 成立。warm last-good 退回保留已驗證 enabled cap（dispatch 仍 admit，合理=last-good 是通過全 loader reject 的已驗證 config，subtraction-only 降級非放行壞 config 新 cap）。改成區分 cold-cache退空（真 fail-closed）vs warm退last-good（保留已驗證），去除「降級必空」overclaim，明標「advisory subtraction-only」。
- **/cost/reset + /cost/pricing operator-scope fold-in（P2 budget-integrity 命門）**：兩 handler 先前只 `Depends(current_actor)`→任何已認證 viewer 可 `reset_today_costs()` 歸零 DOC-08 $2/day counter＝**繞過 P2 admission storm-control 剛驗的 $2/day bound（cap-bypass）**。:666-667 註釋謊稱已 operator-scope。修=兩 body 在任何 state 變更**前**加 `base.require_scope_and_operator(actor, "ai_budget:write")`（鏡像 :731 /registry/reload + :757 fail-safe/reset 已驗模式），訂正 :676-680 註釋為實況（含校正後行號 :254/:373/:413/:731/:757）。pricing 同收（定價驅動 cost 估算/預算判斷，間接影響計帳）。
- **collateral（必修，非 pre-existing infra）**：`test_layer2.py::test_update_pricing_empty/valid` 用 bare `MagicMock()` actor（無 operator role）→ 加 gate 後正確 403 紅。這是「測試斷言了 pre-gate（不安全）行為」的必然 collateral，**非回歸**。修=改用 operator+scope actor pattern（`mock_actor.roles={"operator"}; scopes={"ai_budget:write"}; actor_id="test_op"`，鏡像同檔 :1166-1169 `/trigger` 既有測法）。`grep reset_today_costs tests/` 確認 0 其他 caller。
- **route auth-matrix test 法（infra-independent，asyncio.run 直呼 handler）**：用 `asyncio.run(LR.reset_today_costs(actor=viewer))` 直接呼 async handler——viewer 在 gate 即 raise HTTPException(403)（gate 在 `_get_cost_tracker()` 之前，不需 running app/PG/engine）；operator+scope 通過後 monkeypatch `_get_cost_tracker` 回 MagicMock 驗 `.called`。**陷阱**：handler 回 `_layer2_response` envelope，payload 在 `["data"]`（非頂層）→ 斷言要 `out["data"]["cleared"]` 非 `out["cleared"]`（初版 KeyError 紅，查 _layer2_response :121 修）。+ source-grep test（鏡像 test_static_high_risk_posts_use_scope_gates）：`inspect.getsource(fn)` 驗 require_scope_and_operator 字面在 `_get_cost_tracker` index 之前。
- **mutation bite 雙證**：①移除 `_prune_stale_spend` 呼叫 → 3 bounded test 紅（dict 跨 10 日長到 10 key 非 1）；②移除 reset handler 的 gate → viewer-reject + operator-missing-scope + source-grep 3 test 紅。還原皆綠。
- **Mac pytest 精確**：test_l2_p2_orchestrator.py **88 passed**（74 原 + 14 新：5 bounded + 3 failsoft-semantics + 6 auth-matrix）。7 L2/layer2 檔合計 **386 passed + 4 xfailed + 0 failed**（含 test_layer2.py 修 2 collateral 後綠）。batch security/auth **25 passed**（static auth-surface invariant 不破）。**全庫（--ignore=tests/replay）66 failed=100% pre-existing infra**（PG port 15432 refused + engine.sock 缺，CLAUDE §六；66 數與上輪同檔記錄一致；5 失敗檔 grep 0 reference 我任何 module，逐一證 Connection refused/Not connected）。tests/replay 4 collection error = pre-existing `from program_code` import-path（與我無關）。
- **scoped footprint**：只改 4 檔（2 source + 2 test）。0 migration / 0 Rust/IPC / 0 hard-boundary token 新增（grep :35/:72/:167 命中皆既有 MODULE_NOTE 邊界宣告 prose 非我區）。RLock/per-cap-獨立性/fail-safe subtraction-only 未退化。
- **教訓**：(1) latent 無界 dict 修 prune 要證「被清的 key 從不再被讀」才安全（這裡靠 _cap_spend_today 只讀 today + 跨日新 key 歸零）——盲清會破跨日語義。(2) 加 auth gate 必 grep 全 test 找「斷言 pre-gate 行為」的 collateral（bare MagicMock actor 是常見漏網），同步改成 operator-actor pattern；漏改=綠變紅誤判回歸。(3) route auth test 不需 TestClient：asyncio.run 直呼 handler + gate-before-state-access 設計 = viewer 在 gate 就 403，operator 過後 mock 依賴；但回應 envelope 包一層 ["data"] 要查清。(4) 誤導註釋（:666-667 謊稱已 scope）不只修碼還要訂正註釋為實況，否則下個 reviewer 被同謊誤導。
- **owed**：re-E2 對抗審 + re-E3（auth matrix 對抗 + fail-soft-under-fault）+ E4 Linux regression（0 migration，無 dry-run owed）。本 pass Mac-only。

---

## 2026-06-09 mlde evidence_filter AmbiguousColumn 修復（fix/mlde-ambiguous-cols）

- **任務**：修 learning-layer SQL 回歸 AmbiguousColumn（`mlde_demo_applier_evidence_filter.fetch_pending_sql_and_params`），只做欄位限定不改業務邏輯，並掃 5 個 sibling 檔同類風險。worktree `/tmp/wt-mlde` off origin/main 6c1b015f，commit `28e376c0`（未 push，PM 統一三端）。
- **根因**：2026-06-07 residual/hidden_oos registry join 上線（`residual_registry=on`）後，LEFT JOIN 的 e/drar/hos 帶同名欄（drar 有 engine_mode/source/strategy_name；hos 有 replay_experiment_id；e 有 manifest_hash）→ base 表裸欄位 `WHERE engine_mode=%s` 變 ambiguous → 每日 03:17 cron 壞 ~2 天。先前 06-05/06 manifest_hash 同類已被 *_select 別名修過，這次換 engine_mode。
- **修法（穩健全限定，非打地鼠）**：base 表加別名 `msr`；SELECT/WHERE/ORDER BY 所有 base 欄位全加 `msr.`；3 個 JOIN ON 的全名 `learning.mlde_shadow_recommendations.X` 改 `msr.X`（加別名後全名失效會報 invalid FROM-clause ref）；Block A/B evidence_filter 片段同樣限定；子查詢內欄位（replay.experiments scope）單表不加別名。
- **sibling 5 檔全 clean**：mlde_demo_applier / residual_hidden_oos_bridge / residual_stage0r_preflight / promotion_evidence / replay/experiment_registry 對這些表全是**單表 SELECT/UPDATE/INSERT 無 JOIN**，裸 base 欄位無歧義。判別法 = grep 該檔每個 FROM/JOIN 子句，確認無 multi-table join。唯一有 multi-table join + 裸 base 欄位的就是目標檔。
- **真 PG 驗證（operator 硬要求，關鍵）**：用測試的 `_ProbeCursor`+`PROBE_FULL_SCHEMA` 生成 registry-join 全啟用的真實 SQL → 替換 %s 為字面值 → 對 trade-core docker `trading_postgres`/`trading_ai` 跑 EXPLAIN。**對照實驗閉環**：修復前舊 SQL → `ERROR: column reference "engine_mode" is ambiguous`（精確重現 cron 失敗）；修復後 → EXPLAIN 成功 21 行 plan；legacy fallback（無 join，僅別名）→ 也 EXPLAIN 成功。教訓：**AmbiguousColumn 是 planner 階段錯，不是字串生成階段——既有 Case6 live PG smoke 只驗 caps+SQL token 含某字串，不 EXECUTE/EXPLAIN，所以抓不到此類 bug**。要真抓必須對 PG EXPLAIN 動態生成的 SQL。
- **測試**：既有斷言鎖死舊裸欄位字串（5 處 Block A `COALESCE(evidence_source_tier,...)` + 1 處 `encode(manifest_hash,...)` + 1 處 partial 全名）→ 更新為限定形式（測試 docstring 本就寫「不驗 SQL 完整內容，驗含正確 token」），並加回歸斷言 `FROM ... msr` + `msr.engine_mode` 等。ml_training 633 passed/31 skipped，replay registry 114 passed。Mac advisory，待 E4 Linux authoritative。

## 2026-06-09 — L2 P3a ml_advisory cascade executor（首個 L2 capability，承前 E1 partial）

- **任務**：完成 L2 Phase 3a（diagnose_leak + interpret_result 兩模式 cascade）。前一 E1 round socket 中斷留下 3 partial（TOML registry stanza / PromptContract registry / out-of-bound guard），我 verify coherent（皆 coherent，0 修）後建剩餘交付物。**P3a ONLY**：斷言無 alpha → 無 math gate → 不建 B1/beta_neutral_check（=P3b/QC-gated），不建 hypothesize 模式。
- **3 partial verify 法**：逐檔讀全文對 PA design + 鐵律。TOML（2 stanza enabled=false/lane=ml_backlog→neutral/min_tier L1）✓；PromptContract（確定性 versioned 模板 + Ollama 禁生成 + constraints 含 advisory-only/fact-inf-assm/「LLM NEVER validates alpha」+ M3 source_class 常數 ML_ADVISORY_LEAK_SOURCE_CLASSES/LEAKFREE）✓；guard（callable _GUARD_REGISTRY 非 P2 ref-echo + clause B M3 typing：name_pattern_check 宣稱 leak-free→reject + clause C regime_caveat + hypothesize fail-closed-reject 不在 per-mode 表）✓。**全 coherent，建於其上**。
- **executor 架構決策（關鍵）**：P3a cascade ≠ §G.2 math-gate cascade（那是 hypothesize/P3b）。P3a diagnose/interpret 流程 = **Ollama screen（M4）→ cloud-L2 單發 structured JSON（survivors only）→ 確定性 guard（M3 typing，=P3a 主 gate 因無 alpha math gate）→ advisory sink + D3**（PA design §D line 169 明指）。cloud step 用 `engine._provider_complete`（role="agent" base_tier=sonnet）單發，**非** `run_session` agentic loop（diagnose/interpret 是「對結構化 context 出一個 JSON」的確定性單發）。prompt = PromptContract.template（checked-in，禁 model 生成）。
- **sink 映射陷阱（PA design 未明指，我擇 schema-valid）**：`mlde_shadow_recommendations` 的 `source` CHECK ∈ {linucb,ml_shadow,dream_engine,opportunity_tracker}——**'ml_advisory' 不在集合，直接 INSERT 會違 CHECK**。`recommendation_type` CHECK ∈ {rank,veto,parameter_proposal,experiment_plan,regret_summary,dream_insight}。決策：`source='ml_shadow'`（schema-valid 最近語義）+ `recommendation_type='regret_summary'`（刻意不用 rank/veto 避免落交易推薦命名空間，診斷是事後分析摘要）+ `created_by='ml_advisory'`（free-text discriminator，無 CHECK）+ payload 帶 asserts_no_alpha=True/mode/l2_reply_id。硬寫 applied=false/requires_governance=true/decision_lease_id=NULL（**結構上不可能寫 applied row** = 鐵律 0 新執行權）。**教訓：feeds 既有 sink 前必讀該表 CHECK 約束全文，PA「feeds existing sink」不等於 schema 自動容納新 source/type**。已 flag PM 確認此映射。
- **cost 記帳真陷阱**：`Layer2CostTracker` **無 estimate_cost**（我初版誤用→永遠 cost=0）。`check_daily_budget` 讀 `daily_spend.<day>.total_usd`，**只有 `record_claude_cost(session,in,out,tier)` 更新此 counter**（`record_call` 只更新 ollama_calls 統計，不更新 daily）。若 ml_advisory cloud/screen 花費不經 record_claude_cost → admission $2/day 硬閘看不到 ml_advisory 花費 = storm 防護漏洞。修：`_record_call_cost` 建臨時 Layer2Session 呼 `tracker.record_claude_cost` 回真 cost + 更新 DOC-08 counter。**教訓：reuse 既有 cost 設施前必查「哪個 method 真更新 budget-gate 讀的 counter」，名字像記帳的（record_call）不一定更新該 counter**。
- **coarse_subject DoS（E3 P3-flag）兩道防護**：dedup_key=cap_id|spec|coarse_subject，高基數 subject 撐爆 last_served_ts/debounce_pending（無 evict→memory DoS，P3 接 executor 即活無 healthcheck 可察）。(1) `_derive_coarse_subject`：空→"default"，截斷 maxlen=48+:trunc 標記，折非白名單字元（防空白/控制字元造無限變體），upper-case 正規化降基數。(2) `_evict_admission_windows`：TTL=24h（過期 key 早超去重窗口永不再讀，清除不改去重判定）+ maxsize=4096 硬上限兜底（驅逐最舊）。在 `_admit` 臨界區內持鎖呼叫。**eviction 在 admit 開頭跑（add 前）故每 call 後最多 maxsize+1**——測試斷言 `<= maxsize+1` 非 `<= maxsize`（off-by-one 是設計正確，不隨輸入基數線性增長即達 DoS 防護目的）。
- **dispatch 活化（dormant→live）**：P2 的 sync `dispatch()` 只做 admission+routing「決策」，無 caller 真呼（dormant）。新增 async `dispatch_and_execute()`：先跑 sync dispatch() 取決策，**只在 admitted+routed_to=neutral_sink+cap_id.startswith('ml_advisory')** 才接 `run_ml_advisory_cascade`。sync 路徑零改→P2 88 測零回歸。fail-safe SM：cascade ok（sink_written / screen_rejected / guard_rejected = 確定性閘正確擋下，非故障）→ healthy；cloud_unavailable / sink_write_failed = 真故障→降級。
- **AdmissionDecision.reason vs DispatchResult.admission_reason 易混**：`_admit` 回 AdmissionDecision（欄 `.reason`）；`dispatch` 回 DispatchResult（欄 `.admission_reason`）。測 `_admit` 直接結果用 `.reason`。
- **測試（誠實）**：新 `test_l2_p3a_ml_advisory.py` **44 passed**（2 模式 / cascade screen+cloud survivor-only / M4 校準 disable / M3 typing reject / regime_caveat / sink 0-exec / LLM-never-validates-alpha grep / dispatch reachability 6 短路路徑 / coarse_subject derive+TTL+maxsize / cost-routes-record_claude_cost / per-cap spend）。含 4 mutation bite（cloud-only-on-survivors / low-recall-must-disable / unbounded-without-eviction / per-cap）。**L2 mesh+layer2 全 430 passed/4 xfailed（xfailed 既有無關）**。fake engine（_provider_complete 用 max_tokens 區分 screen/cloud）+ _CapturingConn 注入捕捉 sink INSERT（無真 DB/model）。
- **P2 test 必要更新（in-scope）**：`test_default_checked_in_toml_loads_empty_skeleton` 斷言 registry 空——但 P3a TOML partial 加 2 stanza（PM 已驗 coherent）。改 `test_default_checked_in_toml_loads_p3a_stanzas_all_disabled`：2 stanza registered 但全 enabled=false（fail-closed 保留）+ 無 hypothesize。**TOML 改是 P3a 一部分，非破壞**。
- **regression 隔離法（66 broader 失敗證偽）**：full tree 跑出 66 失敗（snapshot/product-family/runtime-bridge），全 `PG Connection refused` + `engine.sock not connected`（Mac sandbox 無 PG/engine）。證偽法：(a) grep 失敗檔 0 import 我的 L2 模塊；(b) `git stash` 我的 orchestrator edit 後跑失敗子集 → **baseline 同樣失敗**（3 failed 不變）→ 確證非我引入。**教訓：Mac full-tree 必有大量 env-dependent baseline 失敗，回歸隔離要 stash-diff 或 grep-import-overlap，不能看絕對失敗數**。
- **scoped footprint**：改 `l2_advisory_orchestrator.py`（+174/-4，coarse_subject evict + dispatch_and_execute + _resolve_engine）；新 `l2_ml_advisory_executor.py`（698 行）；新 `test_l2_p3a_ml_advisory.py`；更新 2 partial 所屬測試 1 處。**未碰** linchpin（LANE_DIRECTION/STEP-1/C1/C2/F.2/fail-safe SM 邏輯）、live-config、lease、Rust。executor 純函數無新 singleton（orchestrator/writer 已 P2 登記）。

## 2026-06-09 L2 P3a sink CRITICAL 修補（E2 CRITICAL + MIT S-2：mlde_shadow_recommendations → agent.lessons）
- 根因：原 sink direct INSERT `learning.mlde_shadow_recommendations`（source='ml_shadow'+recommendation_type='regret_summary'）**不安全**——正是 active `mlde_demo_applier.py:451` 掃描去 mutate demo RiskConfig 的 namespace（若 MIN_CONFIDENCE=0，P3a 中性診斷被抓去改配置=違 0 新執行權鐵律）；且 prod 必失敗（evidence_source_tier V040 NOT NULL 無 default + V051 paired-CHECK + V037 REVOKE PUBLIC INSERT）。
- 修法（operator 拍板）：sink 改 `agent.lessons`（V133，genuinely inert）。**reuse critic 既有 redactor-protected 寫路徑語意**（非 import critic helper——避免 cycle；直接複用 `l2_secret_redactor.redact()` + db_pool，與 `layer2_critic._persist_lessons_sync` 同模式）。content 過 redactor（redact-then-truncate[:4000]）；`context_id=l2_reply_id`（D3 鏈，P1 已映此）；`source='ml_advisory'`（與 critic lessons 'l2_session' 分離 namespace，避免污染 pg_trgm 檢索池）；symbol NOT NULL → 佔位 'ml_advisory'；lesson_type=mode；outcome_net_bps 恆 NULL；session_trigger 真實 thread（非硬編）。
- **inert 結構性驗證**：grep `FROM agent.lessons` 全庫 = 只 layer2_critic.persist 寫 / retrieve_lessons 唯讀 / layer2_engine 呼叫 persist；**0 applier/mutator 掃描**。對比 mlde_shadow_recommendations 被 applier 掃描去執行。故 0 新執行權在此 sink 是「結構性成立」非靠 applied=false 旗標。
- 教訓 `_code_only()` 剝 STRING token：grep 鐵律 helper 剝掉註解+字串，故 SQL 字面 'INSERT INTO agent.lessons'（三引號內）在 `_EXEC_CODE` 看不到——**正面查表名要查 raw source，負面（applier 表名不殘留）查 stripped code 一樣有效**。先前寫成正面查 _EXEC_CODE → 紅，改查 raw source。
- 保留勿動（已 E2+MIT 驗）：cost 記帳（record_claude_cost→DOC-08 $2/day）/ dispatch 活化 / cascade（Ollama M4→cloud→guard M3）/ M3 typing / M4 校準 / coarse_subject DoS（TTL+maxsize）/ D3 ledger+gate-seam。scoped footprint = 只 2 P3a 檔（executor + test，皆 untracked）。
- LOW-2（orchestrator re-resolve contract_ver/schema_ver）**未動**：dispatch() 用 contract_ref=cap.prompt_contract_ref（branch 1），dispatch_and_execute re-resolve 傳 None（branch 2/3）——兩者**可能分歧**（若 cap ref ≠ seed 映射），改它=改 D3 version 語意=PA/E2 決策非 E1 順手 cleanup。已 scope-out flag PM（minimal-footprint discipline）。
- Mac pytest：P3a 48 passed（44 + 4 net-new sink）；L2 全集（critic+P2+redactor+ledger）561 passed/4 xfailed/0 fail（排除 pre-existing-broken tests/replay：ModuleNotFoundError program_code，需 repo-root sys.path，非我引入）。venv=venvs/mac_dev/bin/python(3.12.13)。**OWED E4 Linux**：agent.lessons 真寫 + V133 schema 驗（同 critic 既有路徑=低風險）。未 commit（鏈 E1→E2→E3→E4）。

## 2026-06-14 — PROFIT-1 cost_gate 雙重扣成本 latent issue 預防性 healthcheck [90]（IMPL 待 E2）
- 任務：把 cost_gate 對 validated-positive runtime cell 的「雙重扣成本」誤拒（replay 裁 NO-FIX、currently dormant，但 parallel session 建 explore-gate 一旦寫 validation_passed=true 即轉 active）轉為可監測。不改 gate 邏輯本身。
- 架構（仿 [80] pg_dump 範式：standalone SSOT + wrapper delegate）：新 standalone `canary/healthchecks/check_cost_gate_double_deduct.py`（純 filesystem read-only，跨平台無 Linux guard 可 Mac 自測；讀 settings/edge_estimates.json + 三 risk_config_<env>.toml；三環境分別算 threshold_bps=fee_bps/win_rate_floor×safety_multiplier，fee_bps=2×(fee_rate+slippage)×1e4，與 gates.rs 一致；偵測 validation_passed==true AND runtime_bps>0 AND runtime_bps<threshold → WARN；fail-soft 缺料→INSUFFICIENT_SAMPLE）+ wrapper `passive_wait_healthcheck/checks_cost_gate_double_deduct.py::check_90_cost_gate_double_deduct`（WARN-by-default，OPENCLAW_COST_GATE_DOUBLE_DEDUCT_REQUIRED=1 升 FAIL）+ runner.py [90] 註冊（post-cursor filesystem）+ __init__ re-export + SCRIPT_INDEX。
- **關鍵偏差（已報告）**：PA 說 fee_rate/slippage「取 [slippage] 段」，但 [slippage] 實際只有 default_rate（slippage fallback），無 fee_rate（gates.rs 的 fee_rate 是 runtime Bybit per-symbol，非 TOML）。最小安全解=fee_rate 取同 TOML `[market_gate].max_taker_fee_bps`/1e4（per-env taker 上限，保守=threshold 偏大=寧早報），slippage 取 `[slippage].default_rate`。docstring 明標。
- 教訓：edge cell SSOT=settings/edge_estimates.json（檔非 PG 表）；runtime_bps key 優先、缺則 shrunk_bps fallback；win_rate_shrunk 優先、缺則 win_rate（與 edge_estimates.rs 反序列化對齊）。max check 號=89 → 我占 [90]。

## 2026-06-14 — AI-PRICING 殘項修（F-B Rust stale 真名 + F-D Python fail-OPEN→fail-closed，IMPL 待 E2）
- 任務一（pricing 真值）：AI-E 查證值已正確（opus-4-8 $5/$25 / sonnet-4-6 $3/$15 / haiku-4-5 $1/$5），唯 `layer2_types.py:PricingTable` 三 anthropic tier 的 last_verified_date 2026-04-16→2026-06-14（值不動）。YAML AI-E#1 haiku 後綴 TODO 非本任務範疇，留。
- 任務二 F-B：唯一 production 硬編 stale=`tasks.rs:270`（claude-sonnet-4-5 已退役/YAML active:false 會 404）→ 改讀 env `OPENCLAW_CLAUDE_TEACHER_MODEL`，預設真名 claude-sonnet-4-6（對齊本檔 :331 CRYPTOPANIC env 慣例）。`client.rs:98-99` doc 註釋同步真名。consumer_loop.rs ×8/client.rs:275 全在 `#[cfg(test)]` 內走 MockClient=inert 假陽性，NO-OP 不改（最小 scope）。
- 任務三 F-D：`layer2_cost_recording.py:104-106` 未知 tier 靜默退 sonnet（fail-OPEN，低估 opus 成本繞 DOC-08 $2/day 閘）→ 改 `raise ValueError`（fail-closed，根原則 6）。連動修鍵空間 bug：`_sync_to_rust_budget(model=...)` 原傳 tier 別名，但 Rust pricing.rs lookup 按真實 model-id→None→Rust fail-closed 漏記；改 `model=MODEL_IDS.get(model_tier, model_tier)`（anthropic 別名→真名，非 anthropic tier fallback 原值）。
- **關鍵發現**：record_claude_cost 的 model_tier 非僅 haiku/sonnet/opus——eff_tier 經 map_tier_to_provider 可為 deepseek/openai 真名（gpt-4o/o1…），但這些全是 PricingTable.models 鍵 → fail-closed raise 不誤拒合法呼叫；且全不在 MODEL_IDS → fallback 原值不破。
- 驗證：cargo build（lib+bin）綠、ai_budget::pricing 9 tests 綠；test_layer2.py 98 passed（含改寫的 unknown_tier_fails_closed）；mutation bite 雙證（還原 silent-sonnet→新測紅→restore byte-identical）。0 hardcoded path / 0 hard-boundary token。Mac-only，Rust runtime 行為待 E4 Linux。改 5 檔（tasks.rs/client.rs/layer2_cost_recording.py/layer2_types.py/test_layer2.py），YAML 無改（值已對）。

## 2026-06-14 — M4 fills_loader + closed_pnl DIRTY-FIX（E2 RETURN 3 LOW，待 re-E2/E4）
- E2 RETURN 3 LOW latent（0 CRIT/HIGH/MED；core fix sound、mutation 有 bite）。dirty 多-session 樹 HEAD=origin/main 48cdbfe2；只改我觸碰的 4 檔（2 source + 2 test），其餘 dirty .py 為隔壁 session WIP 不碰。
- **LOW-1（fills_loader）**：entry_fill_found=TRUE 不保證 net label 非 NULL（entry e.fee 為 NULL 時 entry_fee=NULL → label NULL 但旗標 TRUE）。修=LATERAL 加 `e.fee IS NOT NULL` 謂詞 → entry_fill_found=TRUE ⟹ entry_fee 必非 NULL（精確化 discriminator）。
- **LOW-2（fills_loader）**：close 行 realized_pnl=NULL 時 `COALESCE(NULL,0)-fee-entry_fee`=純費用小負值被當真 net label。修=CASE 加 `AND f.realized_pnl IS NOT NULL` + 移除 realized_pnl 的 COALESCE → NULL 時 emit NULL label。docstring/MODULE_NOTE 訂正：caller 必 dropna 於 net label 欄（權威信號）非 entry_fill_found 旗標。
- **LOW-3（closed_pnl）**：fail-closed 不對稱——Bybit closedPnl 真缺時 `_closed_pnl_snake_row` 捏造 learning_pnl=0.0 標 authoritative='bybit_closed_pnl'，與 PG-fallback 路徑（learning_pnl=None + bybit_unavailable_fail_closed）矛盾。修=缺漏時 authoritative/learning_pnl=None + source='bybit_pnl_missing_fail_closed'；`_attach` override 改用原始 row["closedPnl"] 判存在（非展示欄 enriched["closed_pnl"]，後者缺漏已補 0.0 會誤判）。closed_pnl/bybit_closed_pnl 展示欄保留 0.0 不動（僅 UI）。
- mutation bite 全證紅→還原綠：①移 e.fee IS NOT NULL 紅 ②restore COALESCE(realized_pnl) 紅 ③restore 捏造 0.0 + enriched["closed_pnl"] override 紅。測試：m4 116（+2）/ closed_pnl pagination 43（+3）/ route 18 全綠 0 regression（mac_dev py3.12）。0 hardcoded path / %% escape 不變（5）/ 0 新英文大段注釋。
- **E4 Linux-owed（合理 defer）**：(a) E2 既 flag 的 LATERAL EXPLAIN + fan-out 行數核（1000PEPE close case）；(b) 本輪 `e.fee IS NOT NULL` / `f.realized_pnl IS NOT NULL` 真 PG 行為（fee-NULL/rpnl-NULL 盛行率 + label 確 NULL）。Mac 只能驗 SQL 文字 + Python row 行為，不證 planner 語意。
- 驗證：Mac venvs/mac_dev/bin/python(3.12)；standalone 16 + wrapper 7 全綠；真 config CLI 跑出 demo=112.67/live=104.00/paper=147.33bps threshold + verdict=PASS（證 dormant 正確）；mutation bite=翻 `<`→`>` 6 test 紅還原綠。0 hardcoded user path。

## 2026-06-14 — AI-E P2：ai_pricing 雙副本不一致 fix（F-A YAML + F-C 選項2 對齊，IMPL 待 E2）
- 任務（scoped 嚴守 operator named 兩檔）：補 settings/ai_pricing.yaml 現行真名 + 消除與 layer2_types.py PricingTable drift。F-B（Rust 硬編 stale 真名 tasks.rs/consumer_loop.rs/client.rs）、F-D（layer2_cost_recording.py fail-OPEN→sonnet）**不在 named 兩檔→未碰，flag PM**。
- F-A：ai_pricing.yaml anthropic 節 +claude-opus-4-8($5/$25)/claude-sonnet-4-6($3/$15)/claude-haiku-4-5($1/$5) active；claude-sonnet-4-5+claude-opus-4-6→active:false（retired，價不動，符 pricing.rs Test5 慣例）；claude-haiku-4-5-20251001 保留 active（Python MODEL_IDS 實送此 dated id，防 budget gate fail-closed 誤拒）。標頭→2026-06-14。active_count=8(>=5 sanity OK)。
- F-C 選項2（最小對齊，非選項1 SSOT 合一—架構決策留 PM）：layer2_types.py MODEL_IDS opus `claude-opus-4-7`→`claude-opus-4-8`（唯一 drift；price 5/25 已對、sonnet-4-6 已對）；經 MODEL_IDS[MODEL_OPUS] 傳遞自動修正 PricingTable opus.model_id。collateral：pricing.rs Test8 spot-check 由退役 sonnet-4-5(is_some)改現行 sonnet-4-6(is_some)+加 4-5 is_none 斷言。
- 第三 stale 副本 flag PM（未碰，超 named scope）：provider_pricing_catalog.py:64 `claude-opus-4-7`（official_pricing_manifest opus）——若採 F-C 選項1 SSOT 應一併消除，本 pass 留待 PM 決架構。
- 驗證 Mac：Rust `cargo test ai_budget::pricing` 9/9（含真 YAML load_default active>=5）；Python test_layer2.py 98/98（is_stale 測試未動 last_verified_date 故仍綠）；cost/budget/provider 194 passed、2 failed=pre-existing infra（test_product_family_business_settings engine.sock/program_code import，0 touch 我模塊）。0 hard-boundary token / 0 hardcoded user path。待 E2。

## 2026-06-14 — PERF-1 Phase A：step_4_5_dispatch 5m 指標 bar-close gated 快取（IMPL 待 E2）
- 任務：PA fix_spec「SHIP PHASE A ONLY」。step_4_5_dispatch.rs:222 每 tick 呼 `compute_indicators_for_timeframe(sym,"5m")` 重算整套 5m 指標，但 5m 指標只在新 5m 收盤 OR ewma_lambda 熱重載時變 → 同 bar 內每 tick 結果相同＝熱路徑浪費。Phase B（1m path / hurst hysteresis dwell）DEFER 另票。
- 改 6 檔（全我獨佔，0 碰 parallel WIP handlers_config.rs/schema_contract_test.rs）：(1) klines.rs：KlineBuffer `last_open_time_ms()`（讀 bars.back，**非 len()**——緩衝滿 cap=500 後 pop_front，len 凍結但 open_time_ms 仍遞增）+ KlineManager `last_closed_open_time_ms(sym,tf)`。(2) mod.rs：TickPipeline 加 `perf1_indicators_5m_cache: HashMap<String,IndicatorSnapshot>` + `perf1_indicators_5m_epoch: HashMap<String,(u64,f64)>`（f64 不可雜湊故 key 整組存 value 用 `==` 比，非當 HashMap key）。(3) pipeline_ctor.rs：ctor 兩空 map + remove_symbol 清兩 map（防同名 symbol 重入繼承過期快照）。(4) on_tick_helpers.rs：`cached_or_recompute_indicators_5m(&mut self,sym)`——epoch=(5m 最後收盤 open_time_ms, 5m ewma_lambda)；epoch 不變回 cache.clone()（bit-identical），變則重算刷 cache+epoch；**只快取 Some**（重算 None 不動 cache 傳 None，永不 cache None→暖機後不服務過期 Some）。(5) step_4_5_dispatch.rs:222 換呼新 helper（唯一改動行）。(6) tests/mod.rs 註冊。

## 2026-06-14 — BB seam LOW：shadow_decision_builder client-side 3-bucket pre-round 移除（IMPL 待 E2）
- 任務：消 shadow_decision_builder.py:266-278 客戶端硬編碼價格檔位 3-bucket qty pre-round（price>10000→5dp/>100→3dp/else→1dp），其在 price<=100 且 sub-0.1 qtyStep 小 notional 角落把合法 qty round 到 0 → reason=qty_rounds_to_zero 靜默丟單（只進 200-cap in-memory list 無 log），污染 paper attribution。paper-only。
- 修法（最小，BB 建議）：移除整段 pre-round（精度本就屬 Rust 權威 floor+min_qty rescue，in-code 註釋早已聲明）。移除後 `round` 不再改 qty，原第二道 `qty<=0` 守衛（reason=qty_rounds_to_zero）變不可達死碼且 reason 已過期 → 摺進上方既有 `qty<=0`（insufficient_balance）守衛，並補 `logger.warning` 使 fail-closed 丟單可見（對齊 Rust 側 log-WARNING）。單檔 1 處改，0 碰硬邊界/路徑。

## 2026-06-14 — seam 降級 LOW 三守衛（全 Rust，防呆+註釋，0 業務邏輯改；IMPL 待 E2）
- 任務：鎖死三個隱性 seam（E3/CC/QC 降級 LOW）。一次 session 做完避 cargo race。
- (1) submit_external_order（commands.rs:163，single sink；IPC handle_submit_paper_order + lifecycle.rs:187 皆委派此）加最前 early-reject `if self.effective_engine_mode()=="live" → Err("external_submit_blocked_on_live_pipeline")`。把「route 到 live 也只是模擬故無害」隱性不變量鎖成結構性不可能（未來 process() 下游接真 dispatch 即升 CRITICAL）。**關鍵**：engine_mode "live" 唯一=Live+Mainnet（真錢）；LiveDemo/Demo/Testnet 折疊 live_demo/live_testnet 仍合法模擬端點不被誤殺（mode_state.rs effective_engine_mode 為準，非 PipelineKind::Live）。docstring 補「all-pipeline simulation only, never real OMS」。
- (2) method_registry.rs:4-7 docstring 移除「can grow method-by-method」成長暗示，明標 descriptive metadata only / NOT authorization-enforcement surface / real auth = connection HMAC(SEC-08)+Python 5-gate。
- (3) strategy.rs:38 加中文註釋：registry 驗證刻意 defer 到 handle_update_strategy_params→find_strategy_mut（strategy_params.rs:117/131/145 回 Err"strategy not found" fail-closed，已驗），免後人誤把 as_str() 當授權 gate。
- 自測：cargo build 綠（僅 3 個 pre-existing dead-code warning，與我無關）。新增 fail-closed 測試 `test_f_submit_order_rejected_on_live_pipeline`（with_kind Live + set_endpoint_env Mainnet → engine=="live"，價/ATR/auth 齊備仍須 reject，倉位+fills 零變動）。mutation bite：`if false &&` 短路守衛 → 測試紅（assert result.is_err 失敗）→ 還原綠。submit_order 5 passed / ipc_server 116 / method_registry 3 / strategy_params 16 全綠。0 hardcoded path / 0 碰硬邊界 token（system_mode 命中皆 pre-existing 遠端碼未動）。改 4 檔（commands.rs / method_registry.rs / strategy.rs / submit_order_tests.rs）。
- 自測：Mac venvs/mac_dev/bin/python(3.12)；corner case（balance=10/fraction=0.02/price=5→qty=0.04）舊 round(_,1)→0.0 丟單，今 0.04 un-zeroed 抵 Rust submit；qty<=0（zero balance）仍 hold+不 submit+記 WARNING。既有 test_executor_shadow_to_live_e2e + test_executor_decision_parity 13 passed/2 skipped 零回歸。grep 證 qty_rounds_to_zero 僅存於解釋註釋無 live caller。
- **scope fence（最 load-bearing）**：PERF-1 只 gate「指標重算」**不 gate 策略分派**。bb_breakout::on_tick（唯一 indicators_5m 消費者 mod.rs:469）仍每 tick 跑，讀 cache 的 Donchian upper/lower 後對 live ctx.price 比（mod.rs:705-725）→ intra-bar breach 不漏＝無 backtest/live divergence。新測實證：同一份 cache 5m 快照下 price=110>=upper→Open / price=109<upper→Donchian Hard 硬拒（len 不同）＝breach 隨 live price 逐 tick 評估。
- 新測 perf1_indicators_5m_cache.rs 6 test 全綠（intra-bar bit-identical / bb_breakout per-tick breach / 新 5m 收盤推 epoch 重算 / lambda 熱重載重算 / never-cache-None / remove_symbol 清 cache）。**mutation bite 雙證**：drop lambda from epoch→lambda test 紅；cache default on None→never-cache test 紅；還原綠。
- 驗收：openclaw_engine lib 3823 passed/0 failed、openclaw_core lib 426/0、bb_breakout 92/0、klines 22/0。bench hot_path_baseline avg 27.383→27.265us（持平；bench 未 seed >=30 根 5m bar 故走 None early-return，benefit 在真 runtime 暖機後才顯）。0 hard-boundary token 新增（system_mode/decision_lease grep 命中皆既有未碰碼）、0 hardcoded path。未 commit 等 E2。

## 2026-06-14 — PROFIT-1 [90] E2 RETURN 修補（HIGH-1 per-cell win_rate + 4 doc/test）（待 re-E2）
- **HIGH-1（核心誤報）根因+修法**：原 standalone 用 **固定 win_rate_floor** 算 per-env 標量 threshold，忽略 cell.win_rate；gates.rs（L42-45/215-218/326-329）是 `wr=clamp(cell.win_rate,floor,1.0); threshold=fee_bps/wr×safety`＝**per-cell**。因 clamp>=floor → 標量 threshold 恆 >= 真門檻（demo win_rate=0.55 時 112.67 vs 真 61.45，1.8x）→ 把 gate 會放行的 validated-positive cell 也標誤拒＝false positive，違哨兵唯一目的。**取 Option 1（精準對齊，非退路 conservative-upper-bound）**：`compute_threshold_bps(risk_cfg)→float` 改 `compute_gate_params(risk_cfg)→GateParams`（存 fee_bps/floor/safety），新 `GateParams.cell_threshold_bps(win_rate)` 逐 cell clamp；`scan_double_deduct_cells(edge, gate)` 改傳 GateParams、新 `_cell_win_rate(cell)`（win_rate_shrunk>win_rate>0.5 fallback + clamp[0,1]，對齊 edge_estimates.rs L163-168）；hit dict 多帶 win_rate。
- **MED-1（不實 claim 訂正）**：standalone docstring/runner.py L382,L1569/SCRIPT_INDEX L6,L584 的「公式與 gates.rs 一致」改為「per-cell 門檻依 cell.win_rate clamp，與 gates.rs 一致」（現在 claim 為真）。
- **MED-2（偏差全揭）**：MODULE_NOTE 偏差段從只列 fee_rate 一項，補列全 4 項簡化+各自方向：fee_rate（取 max_taker_fee_bps 上限=over）/win_rate（已對齊非簡化）/n_trades+fresh+from_runtime_field（謂詞省略 gate 前置=寬鬆=over）；明標全方向 over-report=fail-safe。**選揭露不選 scan 納入**（最小 footprint，保守方向不變）。
- **LOW-1（test 回歸鎖）**：test docstring 公式訂正；split compute_gate_params/cell_threshold 測試；新增 `test_scan_per_cell_win_rate_weighting_one_hits_one_passes`（同 runtime_bps=60、win_rate 0.3 命中 vs 0.6 放行）鎖 HIGH-1 + win_rate_shrunk 優先 + 缺欄 0.5 fallback 三條。
- **LOW-2（報告訂正）**：上輪報告「[80] sibling 47 passed」不實，實際 test_check_pg_dump_freshness.py=**5 passed**（親跑證）。
- 驗證：standalone **21 passed**（16+5）+ wrapper 7 passed；mutation（cell_threshold 改回固定 floor）→ 3 test 紅（per-cell weighting + win_rate_shrunk + cell_threshold-above-floor）還原綠；真 config CLI 三 env PASS（dormant 正確，note 改報 fee_bps+floor-threshold 上界）；0 hardcoded path；無 stale floor-formula overclaim。scope=4 檔（standalone+test+wrapper docstring+runner 註解）+SCRIPT_INDEX 2 處，未碰接線/__init__/gate 邏輯。
- 教訓：哨兵宣稱「與 gate 對齊」必對 gate **逐分支**證偽——不只主公式（win_rate weighting），還有進該分支前的全部前置謂詞（n_trades short-circuit / fresh / from_runtime_field）；忽略任一都讓謂詞與 gate 行為分歧。over-report 方向安全但量級失準（~2x）即構成 alert 噪音＝失準非精準。

## 2026-06-14 — P2-MIT [9] model_registry_freshness shadow-only 盲區修復（IMPL 待 E2）
- 根因：`checks_ipc_edge.py:check_model_registry_freshness` 只查 `canary_status='production'`；所有 model 仍 shadow（從未晉升）時 production_slots=0 → 舊碼一律 PASS「expected in Phase 1a/2」→ 過期 shadow model 永遠綠＝監測假象。
- 最小修（surgical，僅改 `slots==0` 分支，production 路徑零改）：slot=0 時新增一條唯讀 fallback 查詢 `MAX(created_at) WHERE canary_status='shadow'`（shadow 的 promoted_at 恆 NULL，created_at 是唯一齡期信號）。三態：無 shadow→PASS empty；newest shadow ≤30d→PASS「shadow but fresh; dormant as designed」（區分 shadow-但新鮮非誤報）；>30d→WARN「shadow pool stale」（surface 非靜默 PASS）。**shadow 永不升 FAIL**（shadow 本就不晉升，缺晉升非故障，只有未刷新才是問題）。fail-soft：shadow 查詢丟例外退回原 PASS empty。
- 測試：新 `tests/helper_scripts/test_model_registry_freshness_shadow.py` 9 passed（三態 + boundary 29.5d=PASS + fail-soft + 表缺 FAIL + production slot 不發 shadow 查詢零回歸 + mutation bite）。mutation A/B 親證：暫換回 blanket PASS → 6 紅（3 production/table/boundary 不觸 shadow 探測仍綠）→ 還原全綠。0 hard-boundary token / 0 hardcoded path。未 commit 等 E2。

## 2026-06-14 — live config write gate 對齊（risk_routes update_per_engine_global_config，IMPL 待 E2）
- 照 PA fix_spec：`risk_routes.py` update_per_engine_global_config 在 `_require_risk_write`+engine-validity 之後、取 IPC 之前插 `if engine=="live":` guard，lazy `from . import live_preflight` 後呼 `all_five_live_gates_ok(actor, require_authz=True)`，失敗 raise 409 `{error:live_gate_failed, gate_failed:[...]}`（鏡 post_live_session_start 形態）。**複用唯一權威 primitive 非 copy inline gate**（零新授權邏輯）。demo/paper 分支不動（Demo 放寬/Live 收緊）。
- Rust defense-in-depth（audit-only，**非硬擋**）：`handlers_config.rs` handle_patch_config 成功臂在 info! 後加 `if config_name.starts_with("risk/live") { warn!(...) }`，讓 V014 audit row 有引擎側對應信號。**不硬拒 risk/live**：Rust 無 secret-slot/authz/global-mode context 無法判五門，硬擋會破合法 g2 canary 直連 socket 路徑（spec 風險 #6 明禁 E1 over-impl）。

## 2026-06-14 — P1-PERF-3 get_ollama_status E2 RETURN 修補（3 findings，待 re-E2）
- **HIGH**：殘留同步阻塞——line 517 `client.is_available()` 是 SYNC 版，cache-miss/過期時跑 `urllib.urlopen(timeout=1)` 凍 event loop（GUI ~60s 輪詢每次觸發）。PA PERF-3 scope 是「async route 內同步阻塞」整類非僅 line 531。修=`await client.is_available_async()`（兩 client 皆有，內部 `asyncio.to_thread` offload，ollama_client:173 / local_llm_factory:158）。上輪報告「is_available 是 60s-cached cheap check 出 scope」對 cache-miss path 是錯的。
- **MED**：fail-soft 非「byte-for-byte preserved」——httpx `.get()` 不對 4xx/5xx raise；上游 503+合法 JSON body（過載 proxy/LM Studio）時 `resp.json()` 成功、models 落空、except 永不進、model_list_error 永不設 → 誤報「0 模型」。修=兩臂 `await http.get()` 後加 `resp.raise_for_status()`（與本目錄 layer2_tools.py:545/1070 既有範式一致）。model_list_error 0 GUI consumer（tab-ai.html:1270 只讀 model_count）故 user-visible 影響小，但上輪「fail-soft byte-for-byte」宣稱是假的。
- **MED/LOW**：0 committed regression test。新增 `TestGetOllamaStatusPerf`（4 test，append test_layer2.py）用真 `httpx.MockTransport`（patch httpx.AsyncClient 注 transport，驅動真實 raise_for_status/json）：async-availability-not-sync（sync is_available 設 side_effect=raise 證未調用）/ LM-Studio-503-JSON-fail-soft / Ollama-500-fail-soft / unavailable-skips-fetch。**mutation bite 雙證**：還原 sync is_available→test1 紅；移除 raise_for_status→fail-soft 2 test 紅；還原全綠。
- **驗證**：full test_layer2.py **98 passed**（94 baseline + 4 新，0 回歸）。safety greps clean（route body 無 urlopen/sync-blocking 殘留=只在註釋；0 hard-boundary token；0 hardcoded user path，測試用 127.0.0.1 loopback 字面非 user path）。HEAD==origin/main 133eaccf。改 2 檔（layer2_routes.py route body + test_layer2.py 尾部新 class）。**教訓：E2 RETURN 修 perf 類缺陷要看 PA scope 的「缺陷類」非單行；httpx 取代 urllib 的 fail-soft 不等價（urllib HTTPError raise vs httpx 不 raise），必補 raise_for_status 才回復原語意**。

## 2026-06-14 — P1-PERF rust [profile.release] LTO+codegen-units（IMPL 待 E2）
- 任務：rust/Cargo.toml:61 [profile.release] 只有 strip="symbols"，缺 LTO/codegen-units。加 `lto="thin"`+`codegen-units=1`（純 build flag，僅這兩 key，不動其他 profile）。
- 量化（Mac，cargo 1.95，hot_path_baseline 是 harness=false plain-main bench 印 avg/p50/p99/max us，無 criterion 故無 `--save-baseline`，改記印出數值）：**PRE(strip-only) 3 run**：avg 26.0-26.5 / p50 33.0-34.0 / p99 42.5-43.5us。**POST(thin+cgu1) 3 run**：avg 25.0-25.1 / p50 31.9-32.4 / p99 41.1-41.9us。改善 avg −4.2% / p50 −4.1% / p99 −3.3%（穩定跨 run，非噪音）。
- **build time delta（clean build to clean build 才公平，incremental cache=46s 不可比）**：PRE clean 90.08s → POST clean ~170s（+89%）。但 **no-op incremental rebuild=instant（cached）**＝LTO 成本只在真重編譯付，dev 用 dev profile、release 僅 deploy 偶爾跑。
- **fat-LTO 探測（evidence-based 選 thin 非假設）**：臨時切 fat clean-build+bench：avg 24.7-25.3 / p50 31.8-32.5us＝與 thin 在噪音內無別（fat max-us 變異更大），fat build 已知更慢且 runtime 無額外收益 → 確認 PA primary 推薦 thin 正確，revert 回 thin。
- 判定 **FIXED 非 NO-OP**：~4% 熱路徑 latency 降是真實穩定收益，一次性 +80s clean-build 對 deploy-only 場景可接受（trading engine latency-sensitive），incremental 0 成本。diff=純 2 key + 中文 rationale 註釋（為什麼 thin/bit-identical 程式邏輯不變）。0 程式碼改、0 其他 profile 動。未 commit 等 E2。

## 2026-06-14 — ADPE explore-eligible 保活（修活化缺口，IMPL 待 E2）
- 缺口：`adaptive_demo_profit_engine/runner.py` build_desired_active 只 winner→active 其餘→dormant；rich-signal 下 all-flat → 全策略停用 → 無單 → demo explore-gate（gates.rs 在 order 放行）永不觸發 → 學習器饑餓。修：enable_explore_sink=True 時 desired = winners ∪ explore-eligible（任一 (strategy,regime) arm 的 `allocator.explore_budget_remaining(arm_id)>0` 即保 active）。**有界**：remaining==0（耗盡，達 explore_budget=30）不保活；explore 關→純 winner 行為不變。讀真實 explore_budget_remaining（非寫死）。
- 改 2 檔（runner.py + test_adaptive_demo_profit_runner.py，+4 test）。synthetic 三態驗證：under-sampled+explore→active(remaining=27)／exhausted→NOT active(remaining=0)／explore-off→NOT active。mutation A/B 證測試咬（還原純 winner→2 explore-positive 測試紅，restore 全綠 27 passed）。0 hard-boundary token / 0 hardcoded path。未碰既有/live/5-gate/all_regimes_flat 語義。
- direct-socket bypass（g2_03_bind_helper 直連 IPC + HMAC）**primary fix 不覆蓋**（socket trust tier > operator API token），spec 已 acknowledge，留 operator 決策；warn! 使其在 engine log+V014 可見但不擋。
- 新測 `tests/test_risk_routes_live_config_gate.py` 8 passed（live 五門未過→409+IPC 不呼 / 全過→放行 / require_authz=True / demo+paper 不諮詢 live 門 / 非 operator→403 非 409 順序 / 真實五門鏈 global_mode≠live_reserved→409 / 全綠→放行）。mutation bite：移除 guard → 3 live 測紅（live config patch 直穿成功 IPC），還原全綠。adjacent 6 檔(live_gate_fallback/paper_live_gate/batch_d/risk_view_client/live_authz_signing/executor_e2e)118 passed 零回歸；executor precedent 21 passed；Rust cargo build --lib 綠 + ipc config 16 tests 綠（含 engine=live routing）。0 hardcoded path。**未 commit**（鏈 E1→E2→E4→QA→PM）。

## 2026-06-09 — L2 P3b ml_advisory.hypothesize（alpha-bearing）+ B1 beta_neutral_check + altcap + leak producers（IMPL，待 E2/QC/MIT）

嚴格執行 PA P3b 設計（`2026-06-09--l2-p3b-implementation-design.md`）+ QC B1 spec + MIT spec。6 構件全 build，46 新測試綠，142 regression 綠。**未 commit**（鏈 E1→E2→QC→MIT→E4→QA→PM）。

**① beta_neutral_check（新 `program_code/learning_engine/beta_neutral_check.py`，614 行，pure-math 0 DB）**：wrap `residual_alpha_gate._fit_factor_beta`（不 fork lstsq）+ 加 coefficient SE（σ²_resid·diag((X'X)⁻¹)）+ DW<1.5→HAC（Newey-West Bartlett kernel 手搓無 statsmodels，L=floor(4·(n/100)^(2/9)））。QC 4 numbers：雙因子強制(altcap=None→DEFER)/90d window/|β_btc/alt/down|≥0.15→fail/β_upper=|β|+1.96·SE≥0.20→fail/down-leg≥180d span+≥30 bars else DEFER/Q1 n_trades_oos<50→DEFER。down-mask klines-direct prior-only(30d-dd>8% OR 7d<-5%，絕對 scalar 非 percentile，不綁 V127)。strictest-wins(fail>DEFER>pass)。**抓到並修自己 2 個真 bug**：(a) `_span_days` 用 list[0]/[-1] 但 residual gate `_sort_key` 按字串 repr 排（整數 ts 字典序 "99">"100"）→ down-leg span 假性 < 180d → 改取真 min/max（ordinal/數值）；(b) pooled_ts 字典序排 → DW/HAC 在亂序殘差上算錯 → 加 `_chrono_key`（datetime→toordinal / 數值→float）chronological 排序。**教訓：reuse 別人的 `_sort_key` 前先確認它的排序語意**（ISO datetime 對、整數錯）。

**② altcap_basket（新 `program_code/research/altcap_basket.py`，318 行，建 research package + tests）**：equal-weight ex-BTC CORE25=24（`cohorts.CORE25_PINNED` 減 BTCUSDT）daily-rebal on-the-fly 無 V137。**★ PIT walk-forward（MIT M3 熱點）**：bar t 成員 = `alive_from`≤t≤`alive_to`（FND-2 builder Step F effective lifetime；只取 included=True 行），新上市 alive_from 進籃、下市 alive_to 後離籃、**無 zombie forward-fill**。純函數核心 0 DB（synthetic 測），`load_fnd2_universe_rows` 才碰 FND-2 唯讀 data_loader。producer 不可建→空 returns→B1 DEFER。

**③ shift1_compliance（新 `program_code/ml_training/shift1_compliance.py`，薄 adapter）**：reuse `helper_scripts/m4/feature_engineering_validator.py`（is_leaky_sql/is_leakfree_sql/is_leaky_pandas/validate_shift1_pattern，**無新算法**）。source_class="shift1_compliance"。leak_free 只當每 feature pass 且無 defer（fail-closed：任一 DEFER→leak_free=False；leak_suspected→fail；insufficient_sample→DEFER never auto-pass thin data）。**驗證：clean-pass 須穩定 feature（window=30,n=200）；隨機噪音 window=10 會誤報 leak_suspected——這是既有 validator 語意非我 bug**。

## 2026-06-14 — ADPE reward_source↔allocator vocab 端到端一致（修 E2 RETURN：vacuous 真路徑）
- **根因**：view `mlde_edge_training_rows.linucb_arm_id = regime_norm||'__'||strategy_name_norm`（V031:326，regime_norm ∈ {trending,mean_reverting,random_walk} = view 詞彙）；但 allocator/runner arm-space 用 VALID_REGIMES（{bull,bear,high-vol,chop,range,insufficient_context}）。舊 reward_source 把 view 的 linucb_arm_id **原樣**當 ArmReward.arm_id（嵌 view 詞彙 regime），卻把 regime 欄映射到 allocator 詞彙 → arm_id 內部自相矛盾。下游 runner._discover_candidate_arms 對 arm_id parse 出 'trending' 等不在 VALID_REGIMES → `if regime in by_regime` False → **靜默丟棄 → 真路徑永遠 flat**。E2 抓的「手造 allocator-vocab arm_id，真路徑永不產出」就是舊測試直接寫 `"range__grid_trading"`（allocator 詞彙）當 view row，繞過真分歧。
- **修法**：reward_source 產出 arm_id 一律 `make_arm_id(alloc_regime, strategy)` 重建（與 runner/demo_maker_arm **同一構造函數 SSOT**，非兩處手寫）；strategy 由 view arm_id 後段 parse（fallback strategy_name 欄）。故 ArmReward.arm_id 的 regime 段 == ArmReward.regime == runner allocate() candidate 詞彙，逐字一致。
- **非 vacuous e2e 測試**：用真 view schema fake row（view 詞彙 linucb_arm_id `mean_reverting__grid_trading`）→ fetch_demo_arm_rewards → run_cycle 全鏈 → 斷言 arm 真拿權重成 active winner（非丟棄、非 flat）。**mutation bite 證**：臨時還原成 raw view arm_id → e2e 測試紅（desired_active={} candidate_decisions=[]，精確重現 flat bug）→ restore 綠。改 2 檔（reward_source.py + test_adaptive_demo_profit_runner.py，含舊 2 vacuous 測試改真 view 詞彙）。ADPE 59 passed / 相關 ml_training 101 passed 0 回歸。0 hardcoded path / 0 hard-boundary token。**教訓：凡「真 DB 列 → 跨模組詞彙轉換」的 arm/key，測試必用真 schema 詞彙的 row 走完整路徑斷言下游真接住，手造目標詞彙 key 會結構性繞過分歧（與 RESIDUAL-GAP-A-BASKET 同盲點：合成資料繞過真 seam）**。待 E2 複審。

**④ is_oos_gap（新 `program_code/ml_training/is_oos_gap.py`，函數 `check_oos_gap`）**：distinct 模塊/函數名 dodge `sample_weight_sensitivity.py:329` namesake（那是 RMSE gap-ratio overfit detector），但保留 M3 source_class 字串 "is_oos_gap"（MIT §1.2.3 option a，PA concur）。4 檢查（temporal separation / embargo gap / purge / no-shuffle 含 interleaved）。leak_free 只當四皆過。

**⑤ hypothesize 模式（改 4 檔）**：TOML 第三 stanza（雙閘：enabled=false + min_tier=L3 + tier_capability_flag=can_generate_hypotheses；lane=ml_backlog→neutral）+ PromptContract `ml_advisory.hypothesize.v1`（mechanism+falsification+signal_axes_used+beta_neutralization_plan，LLM NO alpha 斷言）+ guard clause E empty-mechanism（curve-fit reject）+ executor `_run_hypothesize_cascade`。**cascade-order reconcile（PA-flagged E1 decision）= §G.2 非 P3a cloud-first**：Ollama screen → **Ollama generate（cheap 結構化）** → guard → **novelty dedupe（executor DB read retrieve_lessons lesson_type=dead_mode，guard 0-DB 不變量 load-bearing 故不在 guard）** → **確定性 math gate（STEP0 Q1→STEP1 DSR(min_obs=50)→STEP2 PBO honest-DEFER 不捏造 peer→STEP3 B1→STEP4 leak，strictest-wins）** → **cloud interpret survivors only（math gate pass 才花）** → sink。math gate 唯一 alpha validator，**0 LLM-invocation**（AST grep 驗）。promotion routing：pass→backlog/DEFER→backlog 標 non-promotable/fail→logged-and-dropped。**0 新 live authority**。

**⑥ M4 path align**：benchmark schema 寫進 loader 既讀的 `..._screen_calibration.json`（PA §F.1）；loader 擴讀 MIT 擴充欄 recall_floor(override) + per_class_recall.bad_reject_rate(degenerate pass-everything→DISABLED，MIT §2.4 step4)。未建真 artifact（M4 correctly DISABLED until MIT seed bad-set）。

**關鍵洞**：PBO single-config 必 honest-DEFER（承 2026-06-08 Gap-A：捏造 peer=theater，genuine peer owed A-full Rust replay P4+）→ **P3b 單配置候選 overall 至多 DEFER**（誠實非吐 alpha）；要 math gate pass 須 genuine CPCV `cpcv_oos_returns_per_split`（≥2 cand,total≥320,PBO<0.5）。

**硬邊界驗**：LANE_DIRECTION STEP-1 expand→MANUAL 未動 / can_modify_live_config :671 literal False 未動 / 0 order/lease/promote import（raw-token grep）/ 無新 singleton（純函數+frozen 常數）/ 無硬編路徑（OPENCLAW_BASE_DIR+parents[5]）/ Chinese-first 注釋（0 大段英文 block）。

**Mac pytest（venvs/mac_dev/bin/python 3.12.13）**：46 新（B1 15 + altcap 7 + leak 14 + hypothesize 10）+ 142 regression（P3a 54 + P2 88，含我改的 3 個 P3a-snapshot 測試：unknown-mode 改用真未知 mode、alpha-gate-import 改驗「LLM-invocation 函數無 alpha gate」+「math gate 函數無 LLM-invocation」、TOML 3 stanza）全綠。learning_engine gate（residual/dsr/pbo 39）+ critic 全綠。**executor 1265 行（>800 review-attention，<2000 cap）**。**控制面全集 66 fail 全 pre-existing**（engine.sock/DB/auth 環境依賴，無一在我改的檔；tests/replay 4 collection error 亦 pre-existing ModuleNotFoundError program_code）。

**OWED-runtime（非 build 阻擋）**：V127 `--write-db` population / seed agent.lessons 5-10 dead-modes(M4 bad-set+novelty) / down-bars Linux 確認(full-span 309/last-90d 23) / altcap Linux smoke(24 sym×window 真 market.klines) / E4 Linux regression。未 commit。

## 2026-06-10 — L2 P3b beta_neutral_check int-bar-index 契約 fail-loud 修補（E4 real-smoke 抓到的 silent-drop）
**根因（教訓：reuse 別人的 ±inf fit-window 會把 temporal key 靜默吞掉）**：`beta_neutral_check._parse_series` reuse `residual_alpha_gate._parse_candidate_returns`，後者用 `_WideFitWindow` ±inf 邊界 → `_contains` 內 `float('-inf') <= ts <= float('inf')`，**date/datetime/str key 與 float 比較 raise TypeError 被 `except TypeError: return False` 吞** → row 全靜默丟 → 空 series → 偽 universal DEFER（reason 顯示 `window_below_90d`，實際 n_bars=0）。**只 int key 存活**（int vs float 可比）→ unit test 全用 `range(N)` int key 故先前 22 綠假象，真 market.klines date-key 給 0 down-bar。**教訓：fail-safe(DEFER) ≠ fail-loud；靜默 DEFER 會在接真資料時無聲壞掉整個 hypothesize 能力**。
**修補（最小 scoped，只動 beta_neutral_check 入口，不動 residual_alpha_gate）**：①入口 `_parse_series` 之前加 `_first_non_int_bar_index_key(cand,btc,altcap,mask)`：reuse residual gate `_extract_candidate_timestamp` 抽原始 key（Mapping→keys / Sequence→row→ts，與真解析同語意避 drift），`_is_int_bar_index` = `isinstance(int) and not isinstance(bool)`（**bool 是 int 子類必排除**）；任一非 int key → 顯式 `DEFER` + reason `temporal_keys_unsupported_need_int_bar_index` + `logger.warning`（帶型別/值，接線錯誤 log 即診斷）。②MODULE_NOTE + docstring 鎖 int-bar-index 契約（math_gate_inputs key MUST 共享 int bar index；date→int re-index 是 producer/conductor wiring 責任；違反顯式 DEFER 非靜默）。
**caller seam**：`l2_ml_advisory_executor._run_b1_stage:1120` 直接 map `res.verdict`（無新 verdict literal），新 reason 落 D3 ledger 為 `b1_temporal_keys_unsupported_need_int_bar_index`（取代誤導的 `b1_window_below_90d`）；executor `gate_inputs` 由上游 context assembly 供（今 P3b 未真 wiring，int-key 合成或 absent，故行為中性）。
**mutation bite 自證**：把入口 detector 中性化（`non_int_key=None`）→ date-key 退回 `window_below_90d` 靜默 DEFER、n_bars=0 → 6/7 契約測試紅（第 7 條 int-key-unaffected 正確保持綠，證 detector 不誤傷 int caller）；還原後 22 綠。
**Mac pytest（venvs/mac_dev/bin/python 3.12.13）**：beta_neutral_check 22（15 既有 + **7 新**：date/datetime/str/mixed/Sequence-row 顯式 DEFER + warning 斷言 + int-key-unaffected pass）+ residual_alpha_gate 14 + **full learning_engine 224** + L2 P3a executor 54 全綠，0 回歸。footprint=2 檔（beta_neutral_check.py + test，皆 P3b untracked），residual_alpha_gate **未動**，other-session aeg_candidate_metrics WIP **未碰**。**owed-before-wiring（文件化非本 fix）**：完整 date→int re-index（producer/conductor wiring 階段）/ 6 ex-BTC symbol(ATOM/ETC/FIL/ICP/INJ/UNI) market.klines 1d 覆蓋 gap（資料 owed）/ V127 population / agent.lessons seed。direction=neutral / Mac-only / 待 QA / 不自審 / 未 commit。

## 2026-06-10 — OPS-2 secret-split Phase 2 cutover（fix/ops2-phase2-cutover @ a3d27729，未 push）

- **任務**：D+14 soak 達成（PM 驗 engine.log+api.log fallback WARN=0 含三次重啟）後移除 Phase 1 legacy fallback。worktree `/tmp/wt-ops2-cutover` off main 28e376c0（主 checkout 是他 session WIP 不可碰）。9 檔 +365/−306，commit `a3d27729`，operator-gated 不 push 不 merge。
- **範圍依 runbook §13.3 為權威**（spec §4.1.1 Phase-1 列的 verify_in_memory/compute_signature 參數 `ipc_secret` rename 當年沒做、§13.3 也沒列 → 不做只 flag）。改：Rust read_live_auth_signing_key 純讀新 env + 刪 IpcSecretMissing 變體/Display/auth_error_kind arm + main.rs 第二 panic block（helper `enforce_live_auth_signing_key_or_panic`，緊跟 FIX-10 在 watcher spawn 前 per spec §9.5）+ watcher tests 單 set；Python `_read_live_auth_signing_key` 純讀 + reason rename `live_auth_signing_key_missing` + live_preflight/executor_routes 陳舊 Phase-1 文案。**runbook §13.3 沒列 live_preflight.py**（P1-01 後加的 caller，晚於 runbook）——grep `_read_live_auth_signing_key` 全 caller 才抓到。
- **panic 測試寫法**：runbook §13.4 指名 test `live_auth_signing_key_missing_panics_when_live`，panic 在 main.rs async_main 內聯不可測 → 抽 helper fn + bin 內 `#[cfg(test)] mod`。**用 catch_unwind 不用 #[should_panic]**：panic 後測試函數不再往下執行、無法還原 process-wide env，catch_unwind 讓 env restore 確定性發生（持 bin `test_env_lock::guard()` 下）。payload downcast &str/String 取訊息斷言含 env 名。
- **collateral 兩處（必修非回歸，承 2026-06-09 教訓「加 gate 必 grep 全 test 找斷言 pre-gate 行為者」）**：①`test_live_authorization_signing.py` fixture 只 setenv OPENCLAW_IPC_SECRET（吃 Phase 1 fallback）→ 7 測紅 → fixture 換 OPENCLAW_LIVE_AUTH_SIGNING_KEY + 負向測試 rename；②`test_executor_shadow_toggle_api.py` live-flip 鏈 6 處 env 注入用 IPC 當 verify key → all-gates-green + expired 2 測紅（其餘 4 個因 schema gate 先 trip 而僥倖綠＝誤導）→ 6 處全換。**找法 = grep tests 目錄 import 我改的 module，不是只跑指名測試檔**。
- **cargo test fail-fast 陷阱**：`cargo test -p openclaw_engine | grep ...` 背景跑完 exit 0 是 grep 的 exit code（無 pipefail）且 default fail-fast 在 stress_integration 紅後跳過剩餘 binaries（lib 3788 + bin 72 根本沒跑）——**全套件數字必須 `--no-fail-fast` + 數 `^test result` 行**。最終 4154 passed / 1 failed = `stress_tick_latency_benchmark` 1038μs vs 1000μs debug 閾值，**stash 基線同樣紅** = 既有 perf flake 非我回歸（stash-diff 法再次生效）。
- **Python 62 passed**（5 檔：9 secret-split 新版 + 16 signing + 7 recheck + 10 batchB + 20 toggle）。新 fail-loud 負向含「sign raise 前不可留部分寫入授權檔」+「fallback 機制移除斷言（hasattr 反證）」+ verify 端到端（先簽後刪 key → unverifiable + 新 reason）。
- **部署前 operator gate（已寫進 commit body）**：runbook §13.2 外部 Grafana/journald alert rule 先加 `live_auth_signing_key_missing` + `AuthError::LiveAuthSigningKeyMissing`（repo grep 不到外部規則）；§13.5 rollback 依賴 restart_all seed 邏輯故 seed 保留不動。runbook 本身 PA 域未動；TODO.md PM 域未動。

## 2026-06-10 — OPS-2 Phase 2 cutover E2 退回三項修復（cf1b9320，新 commit 不 amend）
- **任務**：修 E2 RETURN（1 HIGH/1 MED/1 LOW，全機械、E2 已精確定位），同 branch `fix/ops2-phase2-cutover` 疊 `a3d27729` 上新 commit `cf1b9320`（保留審查軌跡），不 push 不 merge。3 檔 +11/−7。
- **HIGH**：`test_strategist_promote_api.py:574` 漏掃 collateral——`test_live_apply_all_gates_green_succeeds` 注入 `OPENCLAW_IPC_SECRET` 當 verify key（Phase-1 fallback 行為）→ 換 `OPENCLAW_LIVE_AUTH_SIGNING_KEY`（grep 證該檔唯一注入點）。**根因承上輪教訓不足**：上輪 grep 找 collateral 用「tests 目錄 import 我改的 module」，但 promote_api 不 import live_trust_routes——它走 HTTP route → live_preflight 鏈間接吃 fallback。**正確找法=grep tests/ 的 `OPENCLAW_IPC_SECRET` 字串（env-key 字面），非 import 圖**；E2 全套 base-vs-HEAD 失敗清單 diff 是最終兜底。
- **MED**：`live_authorization.rs` `compute_signature`/`verify_in_memory` 參數 `ipc_secret` → `live_auth_signing_key`（:285/:287/:316/:334 四處，spec §4.1.1 Phase 2 欄指名；上輪我 flag 為 naming debt，E2 裁決 in-scope）。Rust 位置參數故唯一外部 caller `live_auth_watcher_tests.rs:179`（傳 `TEST_SECRET` 位置實參）零改動零影響。檔內殘餘 `ipc_secret` 字串 6 處全為 IPC-transport 域概念引用（comment/負向測試名），非參數殘留，勿動。
- **LOW**：`fresh_start.sh:81` 陳舊註釋「file 缺 → Phase 1 fallback」改 Phase-2 語義（缺 key → live 拒 spawn log kind `live_auth_signing_key_missing` / Python sign raise；operator 走 runbook §13.5 seed 或 §5.2.2 urandom）。comment-only 中文；`bash -n` 過。
- **驗證（E2 升級 SOP，全照做）**：Rust full `--no-fail-fast` = 43 targets **4155 passed / 0 failed**（`stress_tick_latency_benchmark` 本輪綠——再證它是非確定性 flake，上輪紅這輪綠，數字 4154+1=4155 自洽）；targeted lib live_authorization 24/24 + ops2_phase2_cutover 3/3 + watcher 12/12（E2 校正後正確計數）。Python 全套 `pytest tests/ --ignore=tests/replay`：**base(28e376c0 臨時 worktree) 66 failed vs HEAD 66 failed，FAILED 清單 sort+diff = 空**（Finding-1 新紅消失，0 新增）。base 跑法=`git worktree add --detach /tmp/wt-ops2-base 28e376c0` 同 venv 跑後 `git worktree remove` 清理。
- **教訓**：(1) 全套 base-vs-HEAD 失敗清單 diff 從此是 cutover/gate 類改動的必跑項——點名檔 + import-graph grep 都會漏「字串級 env-key 注入」collateral。(2) E2 retro 數字校正（watcher 15→12、signing 16→13）提醒：報 per-target 計數要從 `test result:` 行抄，不憑記憶。(3) flake 測試兩輪紅綠交替=確證 flake，報告寫「本輪 N/0」並註明 flake 歷史，不寫死預期紅數。

## 2026-06-10 — INVENTORY 全盤校準應用 + 更名 AE_INVENTORY_CONSOLIDATED.md（docs-only，待 E2/PM commit）
- **任務**：把 4 份核實 agent 修正清單（/tmp/inv_fixes_p1..p4.md，71 條）應用到 OPENCLAW_INVENTORY_CONSOLIDATED.md（3793 行歷史快照），git mv 更名 AE_INVENTORY_CONSOLIDATED.md，更新 5 個活引用。**71/71 applied、0 skipped**，新 3922 行。docs/archive 與 CCAgentWorkSpace 歷史引用不動；docs/README.md 更名說明已有人寫好不動。
- **批量文檔修正的可靠做法 = 先全量 LOCATE 驗證再一次應用**：71 條逐條手動 Edit 慢且有轉錄 typo 風險。先跑 Python 腳本對全部 LOCATE 做 substring 計數（OK/MULTI/MISS 三態 + 行號），看完所有異常後再寫「直接 parse 修正清單檔」的應用器（content 不經手抄）+ 每條 assert 唯一命中（等價 Edit 工具語義）+ 失敗即跳過記錄。insert-after 行級插入要按「下一行是否空行」決定補不補尾部空行，否則 list 中間插注記會黏行。
- **LOCATE 摘錄誤差的兩種真實形態**：(1) **MISS=兩處原文混寫**——p1 FIX-9/11 的 LOCATE 把頭部短形 `- **估計工作量**：4–6 人日` 與 Part 4 長形 `**估計工作量**：4–6 人日（含 schema 設計…）` 拼成不存在的混合體（頭部的 `- ` 前綴 + Part 4 的括注）。判別法：grep 特徵詞（估計工作量）列出全部 10 處，對照同清單相鄰 FIX-7/8/10/12 全命中頭部行 → 確定意圖是頭部，以實際原文為準。(2) **MULTI=附錄重複**——統合報告類文檔的「統合導讀」與「附錄原文」常含同文段（FIX-4/5/14 頭部與附錄 A 各一份）；附錄按 FIX-20 口徑保留原文 → override 取第一處。盲套 LOCATE 會把注記打進附錄。
- **歷史快照校準鐵則（本次任務口徑）**：注記式（`> **2026-06-10 校準**：…`）不改寫快照原文；運行面名稱（openclaw_engine / OPENCLAW_* / crate 名 / repo 名）一律不改，只改文檔標題與敘事總稱；附錄只加一條聲明。引用更新範圍要區分「活引用」（AGENTS.md / .codex 2 檔 / context-loading / inventory_summary）與「歷史快照引用」（docs/archive、R4 審計報告）後者不改。
- **驗證閉環**：61 個 insert-after 注記數 = grep "2026-06-10 校準" 計數 61（10 個 replace 不帶該標記，數字自洽）；appendix >L3100 僅 FIX-20 聲明一條（證 MULTI 沒打錯位置）；殘留引用 grep 僅剩 docs/README.md 歸檔說明行；git status 恰 6 檔（RM + 5 引用），多 session dirty worktree 其他檔零觸碰。

## 2026-06-08 — Residual PART 4 Phase 2 re-E2 修補：HIGH-1 payload write + LOW-3 idempotency（待 re-E2）
- **E2 退回**（針對 commit 2a5df09e）：HIGH-1（the whole point of P2 經驗證 empirically broken）+ LOW-3 + MED-2(file size note)。worktree `/private/tmp/wt-residual-act` 分支 `feature/residual-activation`。**follow-up commit（不 amend）**。改 2 檔（orchestrator + 其 test），未碰 bridge/producer_db/promotion_evidence/migration（surgical）。full suite 848→850（+2 新），0 回歸。
- **HIGH-1 命門（defer-by-absence 換名）**：orchestrator stamp lineage 但**從沒寫 `payload.demo_residual_alpha_report`**。下游 source contract（candidate_evidence_source_contract.build_live_candidate_evidence_from_source）**第一道**就讀 `payload.demo_residual_alpha_report`（production fetch `mlde_demo_applier_evidence_filter.fetch_pending_sql_and_params` 的 SELECT **根本沒有 top-level demo_residual_alpha_report 欄**，report 只能從 payload 取）→ 缺則死在 `residual_alpha:not_dict`（=defer-by-absence relabeled）。連 pass 候選都卡第一道（durable gate :407 不可達）。**修**：stamp UPDATE 加 `payload = jsonb_set(COALESCE(payload,'{}'::jsonb), '{demo_residual_alpha_report}', %s::jsonb)`，pass+defer 皆寫；report 用 capturing wrapper 抓到的 EXACT 物件（hash byte-identical），序列化 `json.dumps(report, sort_keys=True)`（與 drar report_jsonb 同 convention；jsonb 結構儲存→round-trip dict 後 `_canonical_sha256` 與 manifest hash 一致）。**覆寫**任何先前 attach_residual_reports btc-only report → payload report==manifest multi-factor report → registry-hash 跨檢一致（順帶關 E2 LOW-4）。
- **validator 順序陷阱（assertion 要誠實）**：`validate_demo_residual_alpha_report` 先檢 `passes`（line 64）再檢 `verdict`（line 67）。真實單配置 defer report `passes=False/verdict=defer_data` → reason 是 **`passes_not_true`**（非 E2 名的 `verdict_not_pass:defer_data`，那要 `passes=True` 才會到）。兩者皆 content/math reason（report 在場+math 判定），key 是 ≠ `not_dict`（absence）。e2e 測 assert 真實 `residual_alpha:passes_not_true` 並對照 payload-無-report → `not_dict`，證 math 是 deciding factor。**親驗三 case**（None→not_dict / passes=False→passes_not_true / passes=True+verdict≠pass→verdict_not_pass）再寫測。
- **e2e 測前輪假象（E2 抓的 false confidence）**：舊測在 `source_row` **top-level** 手放 `demo_residual_alpha_report`（orchestrator 從沒寫的欄）→ 遮蔽真 bug。**修**：重建 `source_row` 為 **production shape**（對齊 fetch_pending SELECT 欄：lineage 欄 + `payload` 帶 orchestrator 實際寫的 report，**無** top-level 欄）。新增 pass-candidate 測證 PASS report 過第一道（reason 不再 `residual_alpha:*`，推進到下游 registry gate）。**綁定 bite**：e2e 也 assert stamp SQL 含 `jsonb_set`+`demo_residual_alpha_report`，否則只讀 params 給假象。mutation 驗（拔 jsonb_set）→ six-step + 2 e2e 三測齊紅。
- **LOW-3 crash-retry 重複 experiment**：register 在 bridge get_pg_conn_fn xact，stamp 在 orchestrator conn；register commit 後 process 在 stamp 前崩 → 重跑（仍 `replay_experiment_id IS NULL`）→ 再 register 新 uuid → 重複 row。**修（cheap）**：capturing wrapper 設 `body.idempotency_key = f"residual_stage0r:{family_id}:{split_hash}"[:128]`（split_hash 從 `body.manifest_jsonb.hidden_oos_state.split_hash` 取，窗確定→key 穩定）。register_experiment 用 (actor,key) in-memory cache + PG advisory lock + H-2 hash guard。**誠實殘留**：in-memory cache 重啟即失（registry module note line 110/142）+ cache-miss race 下 registry 明確接受跨-process 重複 INSERT（H-1 取捨）→ 完全消除需 durable idempotency 欄（V049 無，OUT OF Gap A scope）。此 key 把窗從「任意 retry」縮到「僅 crash-then-restart」=strict-better，註解誠實標殘留。
- **LOW-4 attach_residual_reports 不重處理 stamped rec（驗了）**：`attach_residual_reports`（residual_alpha_cycle.py:317）在 mlde_shadow_advisor `_maybe_attach_residual_evidence`（generation 時、persist 前）對 **in-memory recs** mutate `rec.payload`（btc-only，required_factors=("btc",)）；不讀/不寫 DB stamped rec。orchestrator 後續 UPDATE DB payload jsonb_set 覆寫 → 最終 multi-factor。無 race（不同 op）。
- **`report is None` 保守路徑收緊（minor 行為改進）**：前輪缺 report 仍 stamp（無 payload 的 lineage 對下游無意義，且擋 WHERE-NULL 重選）。改為 skip（`register_ok_but_report_uncaptured`），experiment 已 commit 下次重選（idempotency_key 縮重複窗）。
- **教訓**：(1) 「gate 接上了」≠「gate 收到真資料」——要 trace 到 production fetch 的**實際 SELECT 欄**（top-level vs payload 是 not_dict-vs-真 verdict 的分水嶺）。(2) e2e 測的 source_row 必對齊 production fetch shape，手放欄=假 confidence（E2 抓的就是這個）。(3) validator 內部檢查順序決定 reason 字串，寫 assertion 前先親驗實際 reason（passes 先於 verdict）。(4) idempotency 機制要讀清楚 durable-vs-volatile（in-memory cache 重啟失效），cheap fix 縮窗但別 overclaim 完全消除。(5) e2e 讀 write-params 要綁回實際 SQL（assert jsonb_set in sql）才有 bite。

## 2026-06-10 — half_life 測試 scipy importorskip 守衛（fix/half-life-test-scipy-skip @ dc5c60d7，待 E2）
- 2 fit-path 測試加 `pytest.importorskip("scipy")` + requirements-ml.txt 顯式宣告 scipy>=1.10.0；斷言零改動。行為矩陣雙側 Mac 實證：有 scipy 7 passed / 無 scipy 5 passed 2 skipped。worktree /tmp/hl_scipy_fix 保留待 E2/E4。
- 教訓：模擬「依賴不存在」要用 `raise ModuleNotFoundError` shadow 而非裸 `ImportError`——後者觸發 pytest importorskip deprecation warning（module-exists-but-broken 路徑），與真實 absent-module 行為不同。

## 2026-06-10 — L2 P2p incident_sentinel IMPL（feat/l2-p2p-impl，待 E2）
- 四件套照 PA 設計落地：`canary/incident_sentinel.py`（6 軸 alert-only 哨兵，800 行）+ test（56 passed，隔離鐵則 0 真 DSN/0 真外發）+ cron wrapper/installer + SCRIPT_INDEX/watchdog docstring 1 行。watchdog alert 22 passed 零回歸；Mac dry-run drill 過（A1 stale 樣本 + db_unreachable lazy 路徑）。
- 教訓再驗：never-remediate 結構斷言裸 grep "watchdog_state.json" 被 MODULE_NOTE 合法邊界宣告誤紅 → 斷言收窄到「模塊 docstring 之後的代碼區 0 引用」（partition at `from __future__`）；alert body 措辭避開 `restart_all` 字面防 E2 grep 誤紅。
- 設計 ~400 行估算 vs 實際 800 行（Chinese-first 注釋覆蓋 + 6 軸先天廣度）；壓行三輪保住 ≤800 review-attention 門檻，rationale 未刪。

## 2026-06-10 P2p incident_sentinel E2 RETURN 2 MEDIUM 修復（commit 1e2b094d, feat/l2-p2p-impl 已 push）
- **MED-2 dedup key 截秒吞事件**：A2 alert_key 用 `int(max_alertable_ts)` 截整數秒而游標 float 全精度 → 同一整數秒 burst 跨輪次輪同 key 被 should_emit 靜默吞且游標已推過。修=key 全精度 ts（新批 ts 必嚴格 > 游標 ≥ 前批 max → 必為新 key，dedup 語義守恆）。**教訓：dedup 指紋與游標的精度必須一致，指紋降精度=在精度縫隙內吞事件**。
- **MED-1 正面結構斷言 no-bite**：`"X" in 全檔SRC` 正面斷言被模塊 docstring 合法提及滿足（負面斷言查全文有效、正面必收窄）——修=`inspect.getsource(_connect_readonly)` + 雙 partition('"""') 剝函數 docstring，命中只能來自功能行。mutation 三證（刪 read-only/statement_timeout kwarg、還原 int() 截秒）全紅，還原全綠；57+22 passed。

## 2026-06-10 P2p E4 RED HIGH 修復：cron wrapper Tailscale IPv4 解析 A3 API base（commit bd324886, feat/l2-p2p-impl 已 push）
- sentinel.py A3 默認 loopback:8000 在 trade-core 不可達（uvicorn bind Tailscale IPv4）→ 假 CRITICAL 常駐。修=wrapper 內 env 已設尊重/未設 `tailscale ip -4` 組 URL/失敗不設留 py 默認；mirror m11 inline **不 source lib/api_bind_host.sh**（server bind 視角、0.0.0.0 走 ERROR exit 2 不合 client fail-soft，且現無任何 cron wrapper source 它）。
- 教訓：bind-host 政策（Tailscale IPv4 優先、禁 0.0.0.0）下，任何 client 端 cron wrapper 不能信 py 默認 loopback，必帶同款解析；contained 驗證法=`OPENCLAW_PYTHON_BIN` 指向 echo-stub 全程跑真 wrapper 而不觸真 sentinel（防誤發告警）。
- set -euo pipefail 下 `VAR=$(cmd 2>/dev/null | head -1 || true)`：redirection 先於 command lookup，command-not-found 的 stderr 也被抑制，`|| true` 吞 pipefail 的 127——三案例（已設透傳/無 CLI 不設不炸/真解析 100.x URL）+ 58 passed 實證。

## 2026-06-10 L2 P4 E1-C（V137 + refund reconciler + healthcheck [82]-[86]，branch feat/l2-p4-e1c，待 E2）
- 兩段 commit+push：段1 `3e2ef6a3`（V137 461 行，第一代草稿 N-1/N-2/N-5 逐驗合格留用 + 我補 prh_falsification_chk 三值邏輯收緊：key 缺席 NULL 放行 + 陣列 `?` 元素匹配兩殘洞，前置存在性謂詞 + jsonb_typeof='object' 封死）；段2 `f90f1a26`（reconciler 581 行 + registry +8 行 + healthcheck 五軸 + cron OPTIONAL job + SCRIPT_INDEX；11 檔 +1545/−3）。Linux dry-run：apply×2 冪等 0 ERROR + N-1/N-2 CHECK 拒插原文 + role-absent NOTICE 三實證（p4v137_dryrun 已 drop）。
- 教訓：PG `CHECK (jsonb->'k' ? 'x')` 對 key 缺席整式 NULL → 放行，且 `?` 對 jsonb 陣列按元素匹配——jsonb 結構 CHECK 必前置 `jsonb ? 'k' AND jsonb_typeof(...)='object'`（N-1 三值邏輯原則的 jsonb 版）。
- 教訓：生產函數新增 `cur.rowcount` 讀面（registry double-seal warning）→ 既有 test fake cursor 無此 attr 必紅（6 個 bridge 測試）；正解=retrofit fake 鏡像 psycopg2 真實邊界（`rowcount: int = 1`），不是在生產碼 getattr 防禦遷就 fake。
- 教訓：drar（V131）無 symbol 欄、ResidualEdgeReport 無 strategy/symbol field——per-cell stage0r verdict 唯一可查處 = `mlde_shadow_recommendations`（strategy_name+symbol+replay_experiment_id 蓋章+payload report verdict）；dead-mode lesson 鑄造必須 source='dead_mode_seed'（incident sentinel A5 LESSONS_SOURCE_WHITELIST 硬列，自創 source 即觸 A5 告警）。
- deviation×3 已標：stage0r verdict 缺=pending 非 failed（防未跑 preflight 被誤鑄 dead-mode）；load_round_trips +optional symbol（HIGH-2 同檔先例，attribution 紀律 > 字面 reuse）；stage0r preflight cron 已存在（PART 4 Gap A）= 該交付項驗證後 NO-OP。owed-E4：dead-mode retrieve_lessons 真查 + 真 PG E2E dry-run。

## 2026-06-11 L2 P4 E1-B（control_api online-FDR 接線）兩段 commit 已 push（feat/l2-p4-e1b ac6d5291+5eaba216，待 E2）
- **段1**=l2_alpha_wealth_store（append-only INSERT/SELECT；FIX-1.1 supersedes-head + FIX-1.2 窗單調在 store 層；N-4 確定性 debit_id；partial-unique ON CONFLICT 冪等）+ l2_fdr_routes（bind-demo operator-scope 第一行 + GET wealth）+ main.py +2 行掛載（最小必要偏差）。**段2**=executor STAGE 3.55 precheck（FIX-3.1 value-invariant）→3.58 sealed→3.6 pre-reg→3.7 wealth→4 threshold（Option B）→4.5 debit（MIT #3：overall∈{pass,fail} OR dsr∈{pass,fail}）+ FIX-1.3 dead-mode mint；adapter N_eff seam（共享 ordinal int key 跨 variant 對齊，逐 variant 獨立 reindex 會錯位）+ FIX-2.1b 區間算術（bar_end=window_end+1d ≤ oos_start，==off-by-one 與非午夜 straddle 雙 golden）；guard clause F；contract v2 三點同步（registry/TOML/_MODE_CONTRACT_REF）；orchestrator tier_provider fail-closed L1。
- **慘痛教訓：mutation 測試的還原一律用 sed/python 反向替換或先 commit 再 git checkout——對「未 commit 的工作檔」跑 `git checkout --` 會整檔回 HEAD 把所有未提交改動蒸發**（本次執行器 543 行新碼被一鍵清空，靠 context 內 Edit 全文逐一重放救回；兩段 commit 紀律=活性證明+災難保險的雙重意義實證）。
- 測試：新 41+66 + P3b 10 更新（v2 形+FDR 語義）；l2/layer2 全家族 648 passed+4 xfailed 0 fail；3 組 mutation 全 bite（debit 條件 dsr 臂/區間算術→點比較/precheck 停用）。A 線 import 點（alpha_wealth_controller/n_eff_cluster）以 _FakeAwc+monkeypatch mock，merge 後 E4 全鏈驗真模組。executor 1274→1817 行（>800 review 線、<2000 cap），E2 須評 sibling-extract 時機。
## 2026-06-10 P5-SM soak 第三棒收尾（feat/p5sm-soak-observability @ 9eba5a40，待 E2）
- Wave1 驗證+V137 Linux dry-run 親跑（雙 apply 全 no-op/CHECK 負向拒/ROLLBACK 後零痕跡，max version 恆 136）；Wave2 沿用 0 重寫，抓到 1 真測試洞：[82] flag-OFF 觀測支路 mutation 存活——原測試數據被 OFF→ON transition 支路遮蔽，修=OFF 觀測改由非 flag_change 事件攜帶且無補償 transition，只有 belt-and-suspenders 支路能擋。**教訓：多支路同向 FAIL 的 gate，每支路的 bite 測試必須構造「只有該支路能擋」的數據，否則 mutation 被鄰路遮蔽**。
- mutation 驗證紀律：長 && 鏈跑多個 mutation 會被前一輪殘留汙染（F 輪紅錯測試名）——**一 mutation 一 bash call，restore 後綠再下一個**；八個 mutation（canary single-flight/backoff + [82] 六支路）standalone 全紅後綠。
- S5(b) smoke 的 profile 陷阱：GovernanceProfile Validation/Exploration 在 Rust `acquire_lease` 直接 `LeaseId::Bypass`（不 engage SM、bypass lease release=LeaseNotFound）→「demo profile」字面照做=驗不到 mutating arm；正解=Production profile + 環境層 demo 圍欄（OPENCLAW_ALLOW_MAINNET guard），且 engine_mode 值 Live/LiveDemo 同寫 'live_demo' 不可作環境斷言軸。

## 2026-06-11 L2 P4 E1-C E2 RETURN 修復輪（merge `809bf568` + fix `e752e960`，feat/l2-p4-e1c 已 push，待 re-E2）
- **C-CRIT-1 改號**：P5-SM merge main 佔走 V137+[82] → merge origin/main（2 衝突並存解：SCRIPT_INDEX 最後更新行 / runner append 區）+ 全面平移 V137→V138、[82]-[86]→[83]-[87]、`_v137_deployed`→`_fdr_tables_deployed`（撞號免疫命名含 skip 字串/test fixture）。commit 拆分=merge commit 承載全部機械改號（樹單一 V138 + runner import-coherent，detached worktree 實證 A 點綠 104 passed）、fix commit 只含三語義修復——供 E2 隔離審。
- **三語義修復**：C-LOW-1 `prh_falsification_chk` 最前置 `jsonb_typeof(spec_jsonb)='object'`（封頂層 array/scalar 三值放行，E2 攻擊路徑 `'["falsification_test"]'` Linux 實證修復前可入庫、修復後 CHECK 拒）；C-LOW-2 double-seal warning 補 rowcount=0+caplog bite 測試（mutation 刪 warning 塊→紅）；PM 裁決 stage0r verdict 非 pass（**真實字彙={pass,fail,defer_data}** 非派工原文「pass/defer 兩種」，按「僅 pass 進真值表」最小安全解落地）→ `stage0r_not_pass` 計數+skip-pending，failed 僅經 net<0 可達（mutation 移 guard→defer_data/fail 兩測紅）。
- **教訓**：改號輪動手前必親自 re-fetch 重驗號碼仍 free（散文式「免開 V138」提及 ≠ 佔用，要區分）；merge+改號+語義修復的 commit 拆分要保「每 commit import-coherent + migration 命名空間無雙號」，必要時用 exact-string 反向/正向替換把單檔 delta 暫時搬出 staging（對 renamed file 比 hunk-staging 可靠）。

## 2026-06-11 L2 P4 E1-C stage0r verdict 三向映射微修（feat/l2-p4-e1c @ 9f12a1d4 已 push，待 E2）
- PM 裁決三向：'pass'→green=True、'fail'（gate 結論性統計否定）→green=False 進 M1 真值表（NOT-green 臂自此真實可達，QC FIX-1.3 被證偽→鑄 dead-mode）；'defer_data'/缺席/字彙外值=非結論性 skip-pending（上輪裁決不變）。summary 計數更名 stage0r_not_pass→stage0r_deferred（fail 現進真值表，舊名失真；cron wrapper 整包透傳、唯一鍵級消費者=測試檔）。mutation bite：fail 臂改回 pending → 僅 fail 案例測試紅（1f/17p），還原 69 全綠（reconciler 18 + bridge/preflight 51）。
- 待 E2 注意：`tests/helper_scripts/test_ml_training_maintenance_cron_static.py::test_f08_runner_pins_the_five_audit_jobs` 紅 = merge-seam pre-existing（main 側 f08 pin `VALID_JOBS==CORE+AUDIT` vs branch OPTIONAL_JOBS；da2aba119 經 merge 809bf568 入分支；detached baseline e752e960 同紅實證）——merge main 前須收此 pin，但在本微修「0 其他改動」範圍外，已 flag PM/E2。

## 2026-06-11 BB-REVERSION regime observability（feat/bb-regime-observability @ 52727d82，待 E2）
- persist_intent +hurst 參數 + details 兩鍵（hurst_label/hurst_value，缺失/NaN→null fail-soft）；dispatch 4 透傳點同 tick snapshot；3 檔 +217/−1；lib base-vs-HEAD 3787→3791/0 fail；mutation 雙 bite（site→None / 鍵移除）。
- 教訓：base main clippy 整鏈不可用 = openclaw_core price_tracker.rs:132 `deprecated(since="2026-04-22")` 非 semver 觸 clippy 1.95 deny-by-default `deprecated_semver` 在 dep 編譯即斷——驗自己檔法=臨時本地改 since 跑完還原（不入 commit，flag follow-up）；crate 級 fmt drift 大面積 pre-existing，只修自己 diff 行嚴禁全檔 fmt。
- 教訓：簽名加參後編譯器只強制 arity，「call site 被改傳 None」的縫用 include_str! 結構測試 count==N 鎖（writer 自審先例）；stress_tick_latency_benchmark debug 閾值這台 Mac base 即紅（1053μs），熱路徑 perf 斷言只看 base-vs-HEAD 同機 delta（本次 0.1-4.4μs 噪音帶）。

## 2026-06-11 subagent 四態契約生效
- 收尾契約上線：最終回覆第一行 `STATUS: DONE|DONE_WITH_CONCERNS|NEEDS_CONTEXT|BLOCKED` + 一行理由；完整報告落檔 workspace/reports/，回覆只給 ≤500 字摘要+報告路徑+關鍵結論。卡住/缺輸入就回 BLOCKED/NEEDS_CONTEXT，絕不沉默交出不確定的工作。

## 2026-06-11 25 skill description 觸發條件式改寫（待 E2/PM commit）
- 全 25 個 `.claude/skills/*/SKILL.md` description 改 WHEN-only（刪內容清單、保 owner 路由+互補壓縮），3,525→2,469 字（−30%），PyYAML 25/25 safe_load PASS；只動 frontmatter description 行（git diff 25 檔 ±1 行），未 commit。報告：`workspace/reports/2026-06-11--skill_descriptions_trigger_rewrite.md`。
- 教訓：description 改寫必逐檔對照正文「何時觸發」段保真（寫漏=漏觸發、寫寬=誤觸發）；YAML plain scalar 安全靠全形標點（`：`代`: `），`V###` 的 `#` 前無空白非註釋；「≤120 全形字」遇 32 字長 skill 名交叉引用時 raw 口徑會超標，按 ASCII=0.5 全形等效口徑驗收並雙口徑披露。

## 2026-06-11 Claude Code hooks 接線（settings.json + rtk shim + session-start，待 E2/PM commit）
- 3 新（`.claude/settings.json` / `hooks/rtk-rewrite.sh` vendored rtk v3 @6785a6c7 / `hooks/session-start.sh`）+ 4 改（.gitignore 白名單 +3 / CLAUDE.md §八 +11 / .codex 兩檔鏡像 hint）；協議 6 臂 fake-rtk 實測（ask 臂無 permissionDecision=不繞權限模型）、rtk/jq 缺失+malformed 全 exit 0 無輸出、注入內容與 PM INJECT 塊 byte-identical。報告：`workspace/reports/2026-06-11--claude_hooks_settings_wiring.md`。
- 教訓：①SessionStart hook 場景 heredoc 在 bash 5.3+ 有實錄掛死（superpowers#571），穩定路線=printf 逐行 | `jq -Rs`，並用 `rtrimstr("\n")` 修 printf 尾換行才能逐字 round-trip；②vendor 上游 shim 時「README 自述 silent-fail 但實作噴 stderr」這類意圖/實作不一致,以任務 fail-open 要求裁決並在檔頭逐條列舉偏差;③hook settings 的 `!` gitignore 白名單生效驗法=`git check-ignore -v` 看命中規則是否反向 + `git status` 呈 `??` 雙證。

## 2026-06-11 rtk pytest error 計數缺陷修復（外部 repo patch + PR #2399，待 E2/E4）
- pin=develop HEAD `6785a6c7`（rtk 預設 branch 是 develop 非 master，PR 必 target develop）；根因四疊加：parse_summary_line 無 error 解析 + 全綠提前 return 丟已收集 ERROR + summary 捕捉條件（===形/-q 裸形兩處）不含 error → collection error 全零誤報 No tests collected + `=== ERRORS ===` 區塊從未進狀態機（真實使用 rtk 注入 `-rxX`，short summary 不列 ERROR 行，細節唯一來源就是 ERRORS 區塊=load-bearing）。
- 交付：`srv/tools/rtk/`（patch+README，apply --check 證乾淨）+ patched binary `/tmp/rtk-work/target/release/rtk` + 上游 PR https://github.com/rtk-ai/rtk/pull/2399（CLA 待 operator 簽）。測試：4 新 unit（mutation bite 證 4 紅/還原綠）+ workspace 2154/0（base 2150/0）+ 4 真實場景 A/B vs base binary（exit code 全透傳一致，全綠路徑 byte-identical）。
- 教訓：①外部貢獻先讀 CONTRIBUTING/PR template——target branch、Conventional Commits、pre-commit 三關（fmt --check/clippy/test）、CLA 都是 merge 硬條件；②修「計數器漏類別」bug 時全綠 fast-path 加 `failures.is_empty()` 兜底，防 summary 解析漏抓時細節被丟（fail-closed）；③裸 `gh repo fork <repo> --remote` 在 clone 內會誤打 help，正形=clone 內 `gh repo fork --remote --remote-name fork`。

## 2026-06-11 rtk pytest patch E2 RETURN 修復輪（squash `32561a07` 已 force-push PR #2399，待 re-E2）
- E2 HIGH：初版加寬 `-q` 裸 summary substring 啟發式的 error 臂後，失敗測試 Captured stdout 的 `retrying after error in connection pool` 被第一命中佔位 → 真 footer 被擠掉 → RED run 變 `No tests collected`。修=整行 summary 文法錨定（`is_bare_summary_line`：每逗號段 `<count> <category>` 白名單 + 尾綴 `in <float>s` + 可選 `(h:mm:ss)`）+ **first-match→last-match**（真 footer 永遠最後，stdout 逐字內層 summary 也壓不住）；順手實證 pre-existing passed 餌在 base 是**假全綠** `Pytest: 3 passed`（exit=1 零細節），寫進 PR 當錨定全啟發式的賣點。workspace 2157/0、mutation A/B 雙紅證、六場景×四 binary A/B 表入報告。
- 教訓：①「掃描器把內容行誤認結構行」類 bug，修法=全行文法錨定+last-match-wins 雙保險——單靠文法錨，逐字餌（測試自己驅動 pytest 印內層 summary）仍可第一命中佔位；②「段內不捕捉」方案要先 trace 真行到達時的狀態機位置（`-q` RED run 真 summary 到達時正在 Failures 段內，段內禁捕=連真行一起丟）；③錨 pytest summary 尾綴必須容 `(h:mm:ss)`（>60s run），純 `in X.XXs$` 會誤殺；④外部 PR 上游未 review 前 squash+force-with-lease 最乾淨，單 0001 patch 對裝機/E4 也最簡。

## 2026-06-11 analyze_token_usage.py 適配（superpowers MIT pin 6fd45076，待 E2/PM commit）
- 新 `helper_scripts/analyze_token_usage.py`（stdlib-only 唯讀，主會話/subagent 分桶 token 統計+按 agentType 聚合）；源腳本兩個會計假設被本機真實 transcript probe 推翻：①同 message.id 流式多行各自帶 usage，逐行累加超計 ~3.3x（且 agent 檔 output_tokens 逐行遞增）→ 正解=按 message.id 去重取最後一行；②主會話 `toolUseResult.usage` 實測=subagent 末次 API call 非累計（且被殺 BG agent 無 toolUseResult）→ 正解=直接解析 `subagents/agent-*.jsonl`（18 檔 vs 13 toolUseResult 實證）。實測最近 2 session 跑通（580/1192 calls 分桶自洽）；SCRIPT_INDEX + AI-E memory 已登記。
- 教訓：借用外部 transcript 解析腳本前必先對本機真實 jsonl 做欄位/語意 probe（dedup 必要性、欄位是累計還是末次、誰是權威來源），不能信源腳本的格式假設；transcript 目錄默認值用 `Path(__file__).parents[N]` 動態編碼推導，同值滿足任務字面又不踩 user-path 硬編紅線。

## 2026-06-11 mnemopi dev-memory 召回索引試點落地（Mac，待 E2/PM commit）
- 交付：`@oh-my-pi/pi-mnemopi@15.11.2` `--ignore-scripts` 全域裝 + bun 1.3.14（Homebrew，**包硬依賴 `bun:sqlite`+shebang bun，Node 26 不可替代**）+ seed 腳本 `helper_scripts/mnemopi_seed_from_memory.py`（200 條=101 topic+99 index bullet，重跑=整 bank 重建冪等）+ `<project-root>/.mcp.json`（repo 外不進 git）+ 協議檔 `docs/references/2026-06-11--mnemopi_pilot_protocol.md`。外連審計：全包恰 3 個 fetch 點（embedApi/callRemoteLlm/ExtractionClient），`MNEMOPI_NO_EMBEDDINGS=1` 短路 + 空 LLM_BASE_URL + 測試-only 構造=全滅；stats `vectors:0` 實證。
- 教訓：①seed 索引型 MEMORY.md 不能整檔一條（巨型文檔 FTS 命中一切污染 top-k），逐 bullet 才對齊召回判準；②mnemopi CLI `store`/`recall` 無 `--bank` 旗標、無 `--version` 子命令——bank 寫入要走 `mcp --bank` stdio JSON-RPC（單長駐子進程也快一個量級），版本驗證用 `npm ls -g`；③`--ignore-scripts` 跳過的是 onnxruntime-node postinstall 二進制下載，FTS-only 在 darwin 對它是死碼（win32-only import+embeddings 短路），store/recall 實測完好；④零外連要 gate+鑰匙雙拿：env gate 之外把 OPENROUTER/OPENAI key 從子進程 env 剝除，未來版本 gate 漂移也連不出去。

## 2026-06-14 Adaptive Demo Profit Engine 核心 allocator（regime_bandit_allocator.py，新檔 only，未接引擎，待 E2）
- 交付：`program_code/ml_training/regime_bandit_allocator.py`(542)+`tests/test_regime_bandit_allocator.py`(325，22 passed)。regime 條件化 Thompson 配置器：ingest_arm_outcome→共軛更新→allocate(regime,候選)→權重 dict（含 flat-arm）+ fractional-Kelly 上限係數 + explore 預算/dormant 清除。bandit 數學 100% reuse thompson_sampling（NIGPosterior/empirical_bayes/update_posterior/sample_nig/exploitation_floor），本模組只做編排不發明數學。純 Python+numpy、RNG/ts 全注入（無 wall-clock）、零 DB IO（PG/cron/consumer 接線=下一輪 operator-gated）。
- 誠實鐵則三條全 code 化並 mutation 驗：①全負 EV→allocate 回 `{FLAT_ARM_ID:1.0}`（學會歸零，flat 後驗 μ=-cost_floor 當硬基準，真 arm 要贏過 flat+過 P(μ>0)≥0.55 歸零閘才被配）；②demo 無排隊 maker artifact(`maker_no_queue_demo_artifact`)→transferable_only 軌不吸收、all_fills 軌標 saw_artifact（雙軌隔離，promotion 只信 transferable）；③只接呼叫端餵入 realized PnL，不合成正權重。synthetic 自測：noisy +arm→集中該 arm；ALL-neg→flat-only；+arm-via-artifact→transferable 軌 flat。
- E2 對抗 grep 自證：新包 0 hits `outcome_net_bps`（已知 100% NULL bug 欄，絕不當 reward）/0 functional hits exec-authority(mainnet/place_order/decision_lease 只 disclaimer prose)/0 hardcoded user-path；MODULE_NOTE+「尚未接入引擎」marker 在。dirty WIP（TODO.md/8 memories/fills_loader.py）零碰，git status 只我 2 untracked。reuse thompson/linucb 測試 33 passed 零回歸，full ml_training 750 collected 不破。
- 教訓：①flat/cash stand-down arm 是「naive Thompson 在全負環境不歸零」的唯一解（flat-arm + P(μ>0) 硬閘 + explore 上限三件缺一不可，否則把 demo 帳戶配到死）——這管線買的是「誠實歸零的閘」非 alpha 機器；②demo-artifact 雙向偏誤（零排隊樂觀+只 price-cross 悲觀）淨效應不可量化→只標 transferable=False 不修正，任何 demo 正 edge 必 mainnet 復證；③reuse 別人數學前確認語意（sample_nig 已測→複用估 P(μ>0) 不引第二套數學漂移）。

## 2026-06-14 ADPE 閉環 runner + demo-maker arm（新檔 only，接核心 allocator，待 E2）
- 交付（全新檔，0 改既有/髒檔）：`program_code/ml_training/adaptive_demo_profit_engine/`（`__init__.py`/`reward_source.py`/`ipc_lever.py`/`demo_maker_arm.py`/`runner.py`）+ `settings/adaptive_demo_profit.toml` + 2 test（`test_adaptive_demo_maker_arm.py` 11 + `test_adaptive_demo_profit_runner.py` 17，含核心 allocator 28 共 56 passed）。閉環=reward_source 讀 mlde_edge_training_rows(demo-scope/post-fee/attribution-gated)→ingest 進 regime_bandit_allocator→逐 regime allocate→贏家映 active/負 EV 映 dormant→ipc_lever 冪等 diff 落 demo 引擎。複用既有 `sync_ipc_call('set_strategy_active')`(0 新 IPC)+`RustSnapshotReader`(讀 active 態)+linucb engine_mode_scope/SQL pattern。
- 誠實鐵則 + demo 沙盒硬鎖（mutation 三證皆 bite）：①runner `_assert_demo` engine_mode!='demo' raise（mutation→2 test 紅）；②reward SQL `engine_mode=ANY(engine_mode_scope('demo'))`+`attribution_chain_ok`+`net_bps_after_fee IS NOT NULL`，0 hits decision_outcomes/outcome_net_bps/'live'（mutation 改 live→test 紅）；③demo-maker arm 硬編 `maker_no_queue_demo_artifact` tier、簽名不暴露 tier override→all_fills 軌標 saw_artifact、transferable_only 軌 n_trials=0 不吸收、promotion 軌上 80bps artifact 仍歸 flat=1.0 無 promotion power（mutation 改 taker_real→4 test 紅）。kill switch=snapshot(read active)+restore(冪等還原)，dry-run 默認不發 IPC。
- 教訓：①ipc_lever fail-safe=現態未知(snapshot 缺)+想關→skipped_unknown 不主動關（寧可不動不誤關 operator 想開的）；想開→發（開是安全方向）；②mlde view 的 regime enum(trending/mean_reverting/random_walk)≠allocator VALID_REGIMES(bull/bear/high-vol/chop/range/insufficient_context)，兩套 enum 無共用 SSOT→reward_source 做 best-effort 映射(trending→high-vol/mean_reverting→range/random_walk→chop)，映射不到退 insufficient_context 不 cherry-pick；③mutation 還原後 test 偶紅=stale `__pycache__`（mutation 跑留下的 .pyc），清 pycache+`-p no:cacheprovider` 復綠，源實際正確。

## 2026-06-14 ADPE reward_source 查詢效能修（signals lateral 砍除，3975x，免-migration，待 E2）
- 照 MIT RCA（2026-06-14--adpe-reward-query-perf-rca，Linux PG 親驗）改 `reward_source.py` 的 `_REWARD_SQL`：根因=view `mlde_edge_training_rows` 的 `intent_base` CTE 含 signal_id-only `LEFT JOIN LATERAL trading.signals`，signals 是壓縮 hypertable（segmentby=symbol 不含 signal_id、PK=(signal_id,ts)），裸 signal_id 無 ts 約束→ChunkAppend 不能 prune→歷史壓縮 chunk per-outer-row bulk-decompress 全掃；30d demo（144,526 rows）實測 3827s（64 分）。修法=砍 signals lateral、直查 base 表 `trading.intents JOIN learning.decision_features`、保留走 PK 的 `decision_context_snapshots` lateral 取 regime（962ms/~3975x，MIT per-(strategy,regime) 分組 count 逐 arm diff=0=行為等價）。needs_migration=false。
- vocab 端到端不破（命門）：SQL 輸出仍 5 欄同序 (linucb_arm_id, strategy_name, regime, net_bps_after_fee, ts_secs)，regime/strategy 仍 view 詞彙（regime_norm 邏輯逐字鏡像 V031:274-282、linucb_arm_id 形狀鏡像 V031:326），下游 `map_view_regime_to_alloc_regime`+`make_arm_id` 重建路徑零改。誠實 caveat：attribution 由「signals 再讀比對 signal_context_id=context_id」放寬為「結構性」(signal_id/context_id 非空+df.label 存在)，MIT 30d demo 實證對 label-present row 濾 0 行（144,526==144,526），語義略放寬但行為等價；嚴格保留只能 schema 級（V###+Guard+double-apply）且無 demo 收益→不採。
- MIT BLOCKER#2（64 分查竟跑完=timeout 未生效）：那是 operator 手動 psql 跑 view，非走本 code path。本 code 的 `_set_local_statement_timeout` 用 `SET LOCAL statement_timeout` 套在 psycopg2 autocommit=False 的 with-block 交易內=機制正確；修法後 30d 962ms 遠在 5000ms 限內，timeout 為兜底非熱路徑。docstring 補 fail-soft 說明。
- 改 2 檔：`reward_source.py`（_REWARD_SQL 重寫+MODULE_NOTE/docstring 改源說明，0 改 vocab 函數/engine_mode 硬鎖/make_arm_id 重建）+ `test_adaptive_demo_profit_runner.py`（attribution 斷言改結構性 token、+1 regression-lock test 鎖死 signals/view 不得殘留+base 表查詢+3-param 簽名）。Mac venv 32 passed（runner 23+maker 9），0 hard-boundary token / 0 hardcoded path。0 改 V###/Rust/IPC/5-gate/髒檔。**OWED E4 Linux**：對 trade-core PG 跑 7d/30d 雙窗 EXPLAIN ANALYZE 復證 962ms+per-arm diff=0（Mac 無真 PG，本輪 SQL 是 MIT Linux 實證 SQL 的逐字轉錄）。

## 2026-06-14 Track1 demo explore-gate（Rust E1-A，PA 設計 §3.2/§3.4，待 E2）
- **任務**：建 explore-gate Rust 部分。只改 demo 路徑：`edge_estimates.rs` CellEstimate +2 欄 + parse、`gates.rs` demo 函數 `cost_gate_moderate_with_slippage` branch A(深負低樣本) + branch B(穩健負) 各插 `if cell.explore_eligible && cell.explore_remaining>0 { return None; }`（None=探索放行）。**live 函數 `cost_gate_live_with_slippage` 一個字不碰**=demo↔live 唯一隔離點。
- **fail-closed**：parse 用 `unwrap_or(false/0)`，JSON 缺欄→不探索→維持現行 block（absence=no-explore，舊 JSON byte-identical）。explore 翻 reject 只翻 cost gate(Gate3) 此最後一道，物理上不繞 Guardian(Gate2)/Kelly/P1 cap(Gate2.6)/准入(Gate2.7)（在前已執行）=結構保證非 emergent。
- **隔離證據**（E2 重點）：python 函數邊界切片證 explore token 9 個全在 demo 函數、live 函數 body **0 個**；`git diff -U0` 兩 hunk 皆 `-X,0`（純加 0 刪）且不觸 line 255-320 live 區。型別=`explore_remaining: u64`（設計建議 u32，但 serde_json `as_u64()` 對齊，擇 u64 免 cast）。
- **struct 構造點陷阱**：CellEstimate 無 derive(Default)，唯一 manual 構造在 `scanner/opportunity.rs:386`（test）→ 必補 2 欄否則 `cargo test` 不編譯（`cargo check --lib` 過≠test 過）。既有 demo gate 測試走 `load_from_str` JSON 路徑非 struct literal→加 optional 欄零回歸。
- **驗證**：cargo check --tests 過（僅 pre-existing warning）；+7 新測試（branch A/B 放行 + remaining=0 仍 block + not-eligible 仍 block + 缺欄 fail-closed + live 忽略 explore 仍 block）；mutation bite：branch B 改 `if false &&`→該測試 FAIL→還原綠。**full lib 3813 passed/0 failed**。改 4 檔 +172/-0；0 hardcoded path；4 檔啟動前皆不在髒 set。kill-switch=`OPENCLAW_EDGE_RELOAD=0`（既有）。**OWED**：E2 審 + E4 scratch-JSON 端到端 + E1-B Python sink（另派）。

## 2026-06-14 Track1 demo explore-gate（Python E1-B sink + runner 接線，PA 設計 §3.3/§4，待 E2）
- **任務**：建 explore-gate Python 部分（E1-A Rust 已另派）。新 `adaptive_demo_profit_engine/explore_quota_sink.py`（純函數 `build_explore_overlay` + IO 層 `merge_into_edge_estimates`）+ `runner.py` 最小接線。footprint=1 新 + 1 改（runner +101/-2），啟動前皆不在髒 set，commit-by-PM。
- **explore_eligible 真實從 allocator n_trials 衍生（鐵則，嚴禁寫死全 true）**：`build_explore_overlay` 對每 `strategy::symbol` cell 查 `allocator.explore_budget_remaining(make_arm_id(current_regime, strategy))`=`max(0, explore_budget - n_trials)`；`explore_eligible = remaining > 0`。under-sampled→True、sample-sufficient（remaining=0）→False。synthetic 三證：2 trials→remaining 3 eligible True、8 trials(budget 5)→remaining 0 eligible False、cold-start 0 trials→eligible True。
- **additive merge 不踩 JS writer**：`merge_into_edge_estimates` 讀現有 `edge_estimates.json`→只對「已存在 cell」就地塞 `explore_eligible`/`explore_remaining` 2 欄→原子 temp+os.replace（鏡像 james_stein `_write_json_snapshot`）。既有欄(shrunk_bps/n/win_rate)/`_meta`/未提及 cell 全原樣保留；overlay 提到但無對應現有 cell→不憑空建（n_missing 計數，additive 守恆）。
- **dry_run 默認 true，真寫需 apply=True**：dry_run 或 not apply→只回 plan dict 不寫檔；runner `_run_explore_sink` 傳 `apply=not dry_run`（與 lever 同一 dry_run 紀律，--apply 才落檔）。fail-closed：缺檔/解析失敗/頂層非 object→wrote=False+reason 不部分寫不建檔（Rust 缺欄自動回退 block，absence=no-explore）。
- **regime collapse / symbol fan-out（§3.3）**：`current_regime` 在 Python 端 collapse（gate 無 regime 上下文）；同 strategy 所有 symbol cell 共享同一 (regime,strategy) arm 的 explore 狀態（allocator arm 無 symbol 維，per-symbol explore=未來 arm-space 擴展非 Track1）。
- **偏差（最小安全解，已標 PA/E2）**：設計 §3.3 原指 runner 從 `research.aeg_regime_labels` 讀「當前 regime」。runner 無既有 regime DB 讀取 helper，新建會擴 scope+增 leak-prone DB 面（§5.7 風險）。改為 `current_regime` 設可注入參數（config `explore_current_regime` / CLI `--explore-regime`），預設 `insufficient_context`（fail-closed，是 VALID_REGIMES 之一，allocator 對該 arm 自有 n_trials；不亂塞具體 regime 造 selection bias）。candidate_cells 取現有 edge_estimates.json cell（gate 已關心格子）非新 DB symbol-universe 查詢=零新 DB 面。enable_explore_sink 預設 False（opt-in，不改既有 cycle 行為）。
- **驗證**：AST+import OK；synthetic 全綠（overlay 真實衍生 NOT 全 true / additive 保留既有欄+_meta / dry_run 不寫 / missing cell 不建 / 缺檔 fail-closed / opt-out 不碰檔 / --apply 真寫）；run_cycle 端到端（fresh runner→2 trials→remaining 3、dry-run 不寫、enable=False 無 audit）；既有 ADPE suite **32 passed 零回歸**；CLI `--explore-sink` dry-run rc=0 且未碰真 settings/edge_estimates.json；0 hard-boundary token / 0 hardcoded path / Chinese-first 註釋。**OWED**：E2 審（additive 不互踩 + 真實衍生 + dry_run）+ E4 Linux 整合（與 Rust E1-A scratch-JSON 端到端）+ 補 unit test 入 test_adaptive_demo_profit_runner.py（本輪 synthetic 為命令列即跑驗證，正式 pytest 用例 E2 後可補）。

## 2026-06-14 — L2 get_ollama_status P1 perf：同步 urlopen → async httpx（IMPL 待 E2）
- 根因：`layer2_routes.py` get_ollama_status（async route）內用同步阻塞 `urllib.request.urlopen(url, timeout=5)` 取模型清單（LM Studio /models 與 Ollama /api/tags 兩臂），GUI 每次刷新調用此端點時凍住整個 event loop ~至 5s。
- 修法（最小+local style）：兩臂改 `async with httpx.AsyncClient(timeout=5.0) as http: resp = await http.get(url)` + `resp.json()`，timeout 與 fail-soft `except Exception`（→ models=[] + model_list_error）完全保留。httpx 為本目錄既有依賴（requirements.txt 宣告，layer2_tools.py:529/strategy_ai_routes.py 已用 AsyncClient 範式）；`is_available()`(L517) TTL-cache 屬 PA 範圍外不動。
- 驗證：AST OK；自測三案全綠（reachable→models 正確解析 / unreachable→fail-soft 降級語意不變 / **non-blocking 證：status 跑 0.5s 慢回應期間並行 ticker 推進 40/40 tick=event loop 未凍**）；test_layer2.py + test_layer2_tools.py **133 passed 零回歸**；0 hardcoded path / 0 hard-boundary token / 無 GUI JS 改動（node 無關）。OWED：E2 審 + E4 Linux 回歸。

## 2026-06-14 — P1-SCHEMA-1 schema-consumer contract test（最小一次性，IMPL 待 E2）
- 根因：engine 全 runtime-checked sqlx（99 call site、0 `query!` 巨集、無 `.sqlx` cache），migration 把熱表 column rename/drop 對 Rust 編譯+Python 全隱形，首見=runtime first-touch；quality_writer 類 INSERT fail 只 warn! 靜默丟。既有 migrations_test.rs 查 information_schema.columns 0 次 + 5 test 全 gate 在 OPENCLAW_TEST_PG（CI 從不設）=green-but-empty。
- 交付 3 檔：① 新 `rust/openclaw_engine/tests/schema_contract_test.rs`（沿用 maybe_pool/srv_root 模式 + OPENCLAW_TEST_PG gate；run_if_enabled 跑真 V001-V139；對 6 熱表開 tx→INSERT/SELECT→ROLLBACK；data_quality_events/fills/exit_features/decision_outcomes/model_registry/klines，覆蓋與真實 writer/consumer 同形 query 含 ON CONFLICT/engine_mode/type/nullable）② ci.yml 新 job `schema-contract`（**if pull_request only**=零 macOS 10x；timescale/timescaledb:**2.26.1-pg16** pin=prod 對齊；前置 `CREATE EXTENSION timescaledb`=避 V006 false-RED；cargo test --test-threads=1 + audit_migrations.py 反向檢）③ audit_migrations.py +1 cwd-relative candidate（可攜 CI checkout dir，向後相容）。
- **不做（PA 明列 future P2）**：不改 99 call site 為 `query!` 宏 + `cargo sqlx prepare --check`（需 build-time live PG / .sqlx cache，改動面太大）。
- 小決策：image tag 從 PA 建議的 prod-aligned 2.26.1-pg16 落地（2.16.1 不存在；Docker Hub 驗 2.26.1-pg16 存在）；decision_outcomes/outcome_pnl 取 decision_outcomes（outcome_pnl 無 INSERT call site）。
- 驗證：Mac cargo build --tests 過（僅 pre-existing warning）；schema_contract_test + migrations_test 各自 6/5 passed（OPENCLAW_TEST_PG 未設→SKIP 路徑保 cargo test green）；audit_migrations.py py_compile + cwd candidate 解析 OK；ci.yml YAML 合法。**OWED E1/E4 CI 首跑（Mac 無活 PG 看不到）**：V001-V139 全樹真 apply 綠 + timescaledb_information view 欄位 + 6 case 真 INSERT/SELECT。本 test 不覆蓋 V037 REVOKE PUBLIC INSERT permission 面（CI 單 superuser）。未 commit（鏈 E1→E2→E4）。

## 2026-06-14 — P1-SCHEMA-1 E2 RETURN 修補（V005 CRITICAL 修；MEDIUM 修；新發現 V023 全樹 BLOCKER 升 PA）（待 re-E2）
- **E2 CRITICAL（V005 account_snapshots_legacy）已修+真 PG 實證**：V005 PART 4 有 5 個無條件 `CREATE OR REPLACE VIEW public.X AS ... FROM public.X_legacy`（account_snapshots/system_health/paper_pnl_snapshots/risk_events/learning_events），但 X_legacy 只在 PART 3 `IF EXISTS public.X THEN RENAME` 條件式生；virgin ephemeral PG 缺 brownfield 歷史→PART 3 跳過→VIEW 在 ordinal 5 hard-fail。修=test helper `seed_legacy_precondition()` 在 run_if_enabled 前建 5 個 `public.X_legacy` 最小 stub（欄位逐欄鏡像 VIEW SELECT 投影）。實證（ssh trade-core timescale 容器 ephemeral DB + Mac→tunnel 跑真 cargo test）：修前 ordinal 5 炸 account_snapshots_legacy；修後**過 V005~V022（6.44s）**，失敗點移到 ordinal 23=V005 確修。0 改 migration 內容。
- **MEDIUM（audit no-op gate）已修=relabel informational-only**：audit_migrations.py main() 恆 `return 0`；其 parser 是 naive forward regex 無 drop/rename 感知，對 create-then-drop 表（learning.decision_features/observability.scorer_predictions 等，grep 證真存在）會報 false-positive missing→若改硬 gate 會誤紅 CI。故取 E2 第二選項：ci.yml step 改名「(python, informational-only)」+ 註解去掉「反向檢抓 drift」gate 框架 + main() 加註解說明刻意 return 0。真 drift gate=cargo schema_contract_test。
- **新發現 BLOCKER（升 PA/PM，非 test 層可解）= V004/V005/V023 model_registry brownfield 三方矛盾**：V004:135 建舊 shape model_registry（含 is_active）；V005:168 建 `ON model_registry(is_active)` 索引（依賴舊欄）；V023:67 schema_guard A 要求新欄（strategy/canary_status/verdict…）否則 RAISE。**沒有單一靜態 shape 同時滿足 V005 與 V023**（schema 真實演化、V005 假設舊 shape 為現況）。試過 pre-seed V023-shape→V005 is_active 索引炸；pre-seed union(舊+新欄)→**反觸發 run_if_enabled 的 canary（canary_v023_applied 只查 model_registry 是否存在）→V001-V023 被 seed-skip 不執行→schema 不建全→V024 guard 連鎖炸**。sqlx migrator 一次套全樹無法在 V004↔V023 間插 drop-stub 步。**唯一真解=migration-hygiene（PA 範疇）**：V023 guard 改 repair-not-raise / V005 索引加 column-exists 守衛 / V004 stub 對齊；E1 不可改已套用 migration 內容（=P0 sqlx hash-drift 事故類）。在 PA 修前，virgin 全樹仍在 ordinal 23 fail，6 probe 跑不到=「全樹綠」claim 仍 BLOCKED。test 檔 MODULE_NOTE 已標此 BLOCKER。
- 教訓：①run_if_enabled 的 legacy-seed canary=「model_registry 是否存在」，故任何 pre-seed model_registry 都會反觸發 V001-V023 seed-skip——「為過某 guard 而 seed 一張表」可能踩到 runner 自己的 brownfield 偵測邏輯，必先讀 runner 的 canary/seed 路徑。②真 PG 全樹 apply 必經 Mac→ssh tunnel 對 timescale 容器 ephemeral DB（template0 繞 collation-version 髒），raw-psql 順序 apply 與 sqlx-migrator apply **行為不同**（後者走 ensure_legacy_seeded 會 skip），證 bug 必用真 cargo test 路徑非 psql 手跑。

## 2026-06-14 — PERF-1 E2 RETURN LOW-1 修補（dead import）（待 re-E2→E4）
- E2 LOW-1：perf1_indicators_5m_cache.rs:23 `use crate::strategies::{Strategy, StrategyAction, StrategyParams};` 的 `StrategyParams` 未用（唯一新增 unused_imports warning）；`Strategy`(trait→.on_tick/.update_params)+`StrategyAction`(matches!) 真用。修=去 `, StrategyParams` 一行 surgical edit。Mac cargo build -p openclaw_engine --tests 過：本檔 0 warning（剩 3 lib + 2 test warning 全 pre-existing 別檔 db_writer/m3_emitter/live_auth_watcher_tests）；perf1 6/6 passed。檔仍 untracked（鏈 E1→E2→E4），落主工作樹待 commit。

## 2026-06-14 — P2-DIRTY m4 fills + closed_pnl 聚合修（surgical 增補既有 14-20h stalled WIP，待 E2）
- m4 `fills_loader.py` FILLS_QUERY_SQL：把 fan-out 裸 `LEFT JOIN trading.fills entry_fill ON context_id=entry_context_id` 換成 single-representative `LEFT JOIN LATERAL (... e.entry_context_id IS NULL AND 排除 risk_close:/orphan_close:/adopted_close:/shadow_fill:/unattributed: 前綴 ORDER BY e.ts ASC LIMIT 1) ON TRUE`（對齊 canonical edge_label_backfill.py:188-198）。兩缺陷修：(1)context_id 非唯一→行倍增；(2)缺 entry_context_id IS NULL→close 行被當 entry、close fee 雙重扣。entry-missing 時 net label CASE WHEN entry_fill_found ELSE NULL（不 COALESCE 成 0 捏造 gross）+ emit entry_fill_found discriminator。`%%` LIKE 轉義正確（caller stage1_production_runner.fetch_rows 走 cur.execute(sql, params) psycopg 路徑）。fills 仍 latent（runner:340 generate_stage1_candidates 不吃 fills），build-it-dirty 預先修。
- closed_pnl_pagination.py：`_fetch_pg_closed_pnl_fallback` fail-closed learning_pnl=None/source='bybit_unavailable_fail_closed' 補測試（_FakeConn 注入 db_pool）+ pg_unavailable fail-loud test；`_bybit_gross_pnl`/`_bybit_fee_total` helper 測試。docstring 中文化：_safe_float（去 bilingual dup 留中文）/_bybit_gross_pnl/_bybit_fee_total/_fetch_pg_closed_pnl_fallback（fail-closed 理由）。
- 自測：m4 全套 114 passed（新 4 行為 test：fanout/entry-missing-NULL/close-shaped-exclude/post-fee）；test_closed_pnl_pagination 40 passed（新 6）；route regression test_bybit/live_closed_pnl 18 passed。mutation bite：fan-out 字串重現→black-list 斷言紅，clean→綠。4 檔落主工作樹未 commit（鏈 E1→E2→E4→QA→PM）。

## 2026-06-14 — DYNAMIC-RISK-SIG-1 + KELLY-SIG-1 sizing 保守修（survival-first，IMPL 待 E2）
- 缺陷1 dynamic_risk_sizer.rs：per-trade SR 無顯著性 gate。修=新增 sig_gate_enabled/sig_z(1.645)/sig_min_trades(50) key；UP 路徑改用下置信界 LCB=SR−z·SE（SE=sqrt((1+0.5·SR²)/(n−1)) Lo 2002）+ 硬 floor max(min_trades,sig_min_trades)，LCB<sharpe_high 或筆數不足→不加倉。DOWN 刻意不 gate（降倉永遠 survival-safe）。compute_sharpe 數學不動（避免行為不確定性），改文件化 per-trade 語義 + 新增 last_sharpe_annualized 純遙測欄（不參與決策）。validate 追加 sig_z>=0 finite / sig_min_trades>=min_trades。
- 缺陷2 kelly_sizer.rs + config/risk_config.rs(KellyTierConfig)：point-estimate win_rate/R 無 shrinkage。修=新增 uncertainty_shrinkage_enabled/kelly_sig_z(1.645)/r_haircut(0.0) key（KellyTierConfig + KellyConfig 雙struct + from_risk_config:136 映射）；win_rate 改用 Wilson 下界 W_lcb（clamp [0,W] 只縮不放）入 Kelly 公式；R 套 shrink-toward-1 r_shrink=1−r_haircut（default 0=不動 R，operator 旋鈕）。FIX-27(kelly<=0 拒)/max_fraction cap/vol adjust 全保留 → W_lcb 翻負即拒單。
- 行為保證：兩開關 default=true 但嚴格只增保守（UP 門檻只升、DOWN 不變、Kelly W 只縮、R default 不動）；set false 僅回退舊 runtime 行為不增風險。新 key 全 #[serde(default)] 保守 default，三環境 TOML 缺 key 自動取嚴。
- 偏差（已報告）：live `min_trades=100` → sig_min_trades default 50 < 100 會 validate FAIL，故 live TOML 顯式設 sig_min_trades=100（與 min_trades 對齊，最小安全解）。新增 sentinel test_sig_keys_all_three_env_tomls_validate 鎖死此約束（任何人改 live min_trades 漏改 sig_min_trades 即紅）。
- 自測：cargo build --lib 過；dynamic_risk_sizer 23 passed（+9 含 gate A/B mutation-bite：gate OFF fire vs ON block 同樣本 / LCB clears / UP floor / DOWN ungated / 遙測）；kelly 31 passed（+7 含 shrinkage-only-reduces / higher-z-more-conservative / r_haircut / FIX-27 疊加拒單 / Wilson 性質）；risk_config_tests +4；三環境 TOML validate sentinel 過。全 lib 3845 passed/0 failed/1 ignored（pre-existing）。clippy 0 warning。0 hardcoded user path。改 7 檔（3 source + 3 TOML + 2 test 檔，dynamic_risk_sizer/kelly_sizer 自帶 inline test）落主工作樹未 commit（鏈 E1→E2→E4→QA→PM）。未碰任何硬邊界（max_retries/live_execution_allowed/execution_authority/system_mode）。

## 2026-06-14 — Deribit DVOL/vol-surface PIT 採集器（Track2 新軸，artifact-only，IMPL 待 E2）
- 任務：建 Track2 隱含波動率另類數據軸。新建 `helper_scripts/research/deribit_vol_axis/`（`__init__`/`collector`/`artifact`/`cli`）+ 測試 `tests/test_deribit_vol_axis.py`。read-only / offline / 不碰執行路徑 / 不碰 5-gate / 不建 PG 表（避 V### migration）/ PIT append-only 禁回填。完整 mirror polymarket_axis 模式（ThrottledJsonClient 禁 redirect、append-only run dir 重名 raise、manifest+sha256、duckdb parquet 原子寫 fail-soft）。
- **關鍵設計決策（真 API probe 驗證）**：surface 用 `/public/get_book_summary_by_currency?kind=option` **單請求**回全 944 option（每筆含 mark_iv+underlying_price），不走逐 instrument ticker loop——單請求 = 同一時刻全 surface PIT 一致；逐 ticker 既慢又跨時刻拼接破 PIT。DVOL 走 `/public/get_volatility_index_data`（result.data=[[ts_ms,o,h,l,c]]，年化 vol%）。instrument_name `CCY-DDMMMYY-STRIKE-{C|P}` 正則解析出到期/strike/型別→採集端構造 term-structure（各到期 nearest-ATM mark_iv）+ skew（±10% moneyness wing IV proxy，skew_proxy_bps=(otm_put_iv-otm_call_iv)×100）。book_summary 不帶逐筆 greeks/delta，故 wing 用 nearest-moneyside-strike proxy 非真 25Δ，標 band/strike 供研究端精算。
- **真 smoke（live public API）**：BTC manual run = dvol_rows 25 / surface_rows 944 / term 12 expiry / skew 12 / 0 errors / 2 HTTP req；DVOL≈40-41%、近月 ATM IV 40.20%、6-15 skew +141bps（put wing 57.37>call 55.96 = 下跌保護需求）。append-only guard 真實觸發 FileExistsError。Deribit JSON-RPC error body（HTTP 200 帶 error 物件）已在 client 偵測升 CollectorHTTPError。
- 治理對照：net 計算禁 maker rebate（grep 0）；零 hard-boundary token；零 /home|/Users 硬編路徑（parents[3]→srv 實測）；零生產 import；host allowlist 只 www.deribit.com（/api/v2/private/* 結構性排除，code-token test 證 private/authorization/order_id 不在真碼）。測試 37 passed+1 skip（opt-in live smoke）；polymarket 48 零回歸。無新 singleton（純函數+無狀態，比 polymarket 少 state 環節）。SCRIPT_INDEX 已補。cron wrapper/installer **未建**（PA 任務未要求，scope-out；如需 daily 自動化 follow-up 另派）。

## 2026-06-14 — AI-PRICING-FB-FD E2 RETURN 修補（5 finding；待 re-E2）
- HIGH-1（report-only）：訂正 report 改動清單為真實 8 處（前報漏報 ai_pricing.yaml 重寫 + pricing.rs:276 spot-check 改名+retired assert，且把 layer2_types MODEL_IDS[opus] 真名 4-7→4-8 誤寫成「值不動」）。教訓：report 改動清單必對 `git diff --name-only` 逐檔核，不靠記憶。
- MED-1（vacuous fix 補 test bite）：`MODEL_IDS.get(model_tier, model_tier)` 鍵空間修先前零 test 覆蓋（E2 還原成 model_tier 仍 98 綠）。補 2 test：anthropic tier patch `_sync_to_rust_budget` 斷言 model==MODEL_IDS[sonnet]（真名，非裸 'sonnet'）；非-anthropic（deepseek-v4-flash，PricingTable 鍵但不在 MODEL_IDS）斷言原值 fallback。mutation 親證：還原 buggy 版→anthropic test 紅（'sonnet'!='claude-sonnet-4-6'），deepseek test 維持綠（fallback 不變=正確不變量）→restore byte-identical。
- LOW-1+LOW-2（消三方 drift）：`provider_pricing_catalog.py:64` opus model_id 4-7→4-8（official_pricing_manifest 經 refresh_pricing_if_needed→update_pricing 寫回 runtime，stale 真名會 undo E1 對齊）。修後 MODEL_IDS=yaml=manifest 三方一致；無 test 斷言此 model_id 故無 collateral。
- 自測：test_layer2 100 passed（98+2）；+test_provider_keys_store 共 112 passed；py_compile OK。改 2 檔（test_layer2.py +2 fn / provider_pricing_catalog.py opus id）落主工作樹。HEAD==origin/main=48cdbfe2（0/0 無 race）。INFO（tasks.rs `let model=as_str()` 簡化 / anthropic-only last_verified bump 不改 is_stale）= E2 標明不需修，未動（守最小 scope）。未碰硬邊界。

## 2026-06-14 — hftbacktest fill-realism harness + D2 re-validation（artifact-only 離線，待 E2）
- 建 `helper_scripts/research/hftbacktest_fill_realism/`（__init__/data_fetch/converter/harness/bridge_d2/artifact/cli）+ `settings/hftbacktest_fill_realism.toml` + test（35 passed，含真 njit smoke）。mirror deribit_vol_axis 模式。offline/read-only：不碰執行路徑/5-gate/order_manager/IPC/restart_all；artifact-only 不建 PG 表；PIT append-only 重名 raise。
- **DECISIVE BLOCKER 鐵則執行點**：`MAKER_REBATE_BPS=0.0` hardcode + `assert_no_rebate()` 在 net 計算入口硬擋任何非 0 rebate（mutation 親證：no-op 守衛→6 test 紅→restore 綠）。**未成交 maker 單計入 fill_rate 分母**（D2 non-harvestable 假說核心）。
- **三個關鍵 IMPL 偏差（皆最小安全解，已報告）**：(1) hftbacktest 2.4.4 的 ProbQueueModel 在 2.x 不是頂層類而是 `BacktestAsset.log_prob_queue_model()` builder method——對齊真實 2.x API。(2) 用官方 `hftbacktest.data.utils.tardis.convert`（vendor-supported，正確處理 exch_ts/local_ts 雙時戳 + trades 在 depth 前的 queue 雙扣防護）而非自製 CSV→8-field converter。(3) PA design 走 Rust crate 或 Python 2.x——選 Python 2.x（同 Rust 引擎核心，離線研究膠水非生產路徑，§2.2 授權的降級）。
- **兩個真 bug（首版抓出修掉，否則 verdict 不可信）**：①`hbt.elapse(duration)` 吃「相對時長」非「絕對時戳」——首版傳絕對 epoch-ns → entry_ts 永達不到 event_ts → 全 leak + fill_rate=0 假 NON-HARVESTABLE。修=先 elapse(1) bootstrap，再 elapse(event_ts−current_timestamp)。②peg 用 cascade 的 stale liquidation last_price 定錨 → 掛單落 BBO 外 ~50bps 永不成交（peg artifact 假 0-fill）。修=peg 改相對「事件當時 live BBO」的 offset（harness 內用真 BBO 算），baseline offset=0 貼 BBO。教訓：**寫 verdict 前必查每個「致命低」結果是真信號還是 API 誤用/參數 artifact——0-fill 太乾淨要懷疑**。
- **D2 真實裁決（Mac，hftbacktest 2.4.4，BBO-anchored 修正後可信）**：BTCUSDT 2024-01-01 n=34/fill_rate=0.88/conv≈0.40bps/adverse≈0.32bps/net_maker≈−3.92bps；ETHUSDT 2024-01-01 n=7/fill_rate=1.0/conv=0/adverse≈0.89bps/net_maker≈−4.89bps → 皆 **NON-HARVESTABLE**。機制：maker 接得到 cascade 對手盤（fill 不致命），但成交後 convergence 被逆選擇吃光（gross≈0），扣 2bps/leg maker fee（rebate=0）後 net<0。誠實邊界：small sample（n=34/7 單日單 symbol）只能裁 NON-HARVESTABLE（不利方向，小樣本穩健）；HARVESTABLE 需大樣本（free tier 不足，gate 強制 INSUFFICIENT-SAMPLE）。
- 治理對照：net 禁 rebate（grep+mutation 雙證）；零 hard-boundary/生產 import token（code-token test 剝註釋字串後驗）；零 /home|/Users 硬編（OPENCLAW_DATA_DIR + parents[3]）；host allowlist 只 datasets.tardis.dev + 禁 redirect。無新 singleton（純函數）。SCRIPT_INDEX 已補。owed：E4 Linux 用真 `market.liquidations`（--liq-source pg）跑驗 + 多 symbol/月跨樣本確認 NON-HARVESTABLE robust。
