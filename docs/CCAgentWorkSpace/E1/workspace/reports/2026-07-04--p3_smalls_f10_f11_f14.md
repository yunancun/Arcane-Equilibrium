# E1 — P3 代碼小項批 F10+F11+F14 實作報告 · 2026-07-04

- 任務:冷審計 R2 修復 wave · P3 批(slug=p3-smalls)。出處=E4 補審報告
  `docs/CCAgentWorkSpace/E4/workspace/reports/2026-07-04--e4_test_matrix_blindspot_audit.md`(F10/F11/F14)。
- worktree:`fix/p3-smalls-0704` @ origin/main `2be58c191`(隔離 worktree,主樹零觸碰)。
- commits:
  - `db07df83b` fix(F10) proof_exclusion engine_mode 正規化對齊 Rust trim+lower
  - `1791e433c` feat(F14) probe_outcome fill 對賬誠實性標記+消費側分開計數
  - `25353197e` refactor(F11) 刪除零 caller 四段版 order_link_id 校驗器

## 任務摘要

三個 LOW 級數據誠實性/dead-code 修復,全部按 spec 最小面積落地,零 runtime 操作、
零 SQL、live fail-closed 5 gates 與 9 安全不變量未觸碰、Cost Gate 主判準未動。

## 修改清單

| 檔 | 變更 |
|---|---|
| `helper_scripts/research/cost_gate_learning_lane/proof_exclusion.py` | F10:新 `_normalized_engine_mode`(strip+lower);order_link_id mode-tag 查表前正規化;proof key engine_mode 未 trim-stable 拒絕(鏡像 Rust :273)、trim-stable 大小寫變體正規化後判 demo-only |
| `helper_scripts/research/cost_gate_learning_lane/outcome_writer.py` | F14 writer:`fill_reconciliation` 欄位(filled/admitted_only/indeterminate)+ 對賬 helpers;`build_probe_outcome_records` 傳 `reconcile_fills=True`;blocked-signal 不附標記 |
| `helper_scripts/research/cost_gate_learning_lane/bounded_probe_result_review.py` | F14 消費側:`probe_result_summary.fill_reconciliation_counts` 分開計數(legacy 無標記行計 `unmarked`)+ markdown 一行 |
| `rust/openclaw_engine/src/bounded_probe_active_order.rs` | F11:刪 `is_bybit_safe_order_link_id_for_engine_mode`(:494-516),留中文墓碑注釋 |
| `helper_scripts/research/tests/test_cost_gate_proof_exclusion_engine_mode.py` | 新測 5 條(F10 對稱語義,先紅後綠 2 紅 3 pin) |
| `helper_scripts/research/tests/test_cost_gate_probe_outcome_fill_reconciliation.py` | 新測 8 條(F14 writer 5 態 + blocked 無標記 + 消費分計 + 空 ledger,先紅後綠 7 紅 1 pin) |

## 關鍵 diff(語義)

**F10** — 不對稱點:Rust `order_link_engine_mode_tag`/`learning_probe_admission_is_demo_only`
= `trim().to_ascii_lowercase()`;Python `proof_exclusion.py:188` 為 exact-match dict、`:233`
exact set。修法=鏡像對齊 authority(改 Python 讀側,不動 Rust 生產碼、不動任何 SQL):

```python
def _normalized_engine_mode(value): return _raw_str(value).strip().lower()
mode_tag = {"demo": "dm", "live_demo": "ld"}.get(_normalized_engine_mode(engine_mode))
# proof key 層:engine_mode_raw.strip() != engine_mode_raw → False(鏡像 Rust trim-stable fail-closed)
```

memory `project_engine_mode_tag_live_demo` 的 ML filter `IN ('live','live_demo')` 屬另一
子系統(fills SQL),本修零觸碰。接受集純 ASCII,`str.lower()` 與 `to_ascii_lowercase` 等價。

**F11** — 判斷依據(選「刪除」非「接線」):
1. 零 caller 實證:全庫 grep(全檔型)唯一非 docs 出現=定義本身;無測試引用、無 re-export。
2. 非更嚴:四段版驗舊格式 `oc_<mode>_<ts>_<十進位 seq>`;生產格式五段
   `oc_<mode>_<ts>_<base36 seq>_<FNV lineage hash>`,由
   `is_candidate_bound_bounded_probe_order_link_id`(2 生產 call site)驗證,後者額外綁
   lineage hash、canonical base36 round-trip、MAX_SEQ 上界。四段版對一切生產 id 必回
   false(`parts.len()==5 != 4`)——接到生產路徑只會全拒,是誤導性 dead-code。
3. 歷史:2026-06-25 report 稱保留給「non-active-probe callers」,至今未出現。

**F14** — 誠實性標記語義(不改 promotion 判準,QC P1-2 不越界):
- `filled`:outcome 生成當下 ledger 內存在可綁定(context/signal/order_link id,頂層/event/
  lineage 三段掃描)且攜帶執行證據(`fill_id`/`exec_id`/`execution_id`)的行。
- `admitted_only`:可對賬但無 fill 證據。`indeterminate`:admission 行無任何 lineage id
  (合成 attempt_id 不算,fill 行不會攜帶它)。
- `order_id` 不算 fill 證據(只證明曾下單)。
- 消費側只加 `fill_reconciliation_counts` 觀測欄位;completed 計數、review status、
  promotion 判準零變更(測試釘判準不變性)。

## 治理對照

- 硬邊界:max_retries/live_execution_allowed/execution_authority/system_mode 未觸碰。
- 跨平台:零硬編碼路徑(grep 過 diff,無 /home/ncyu、/Users/ncyu)。
- 注釋:全部中文、講 why(bilingual-comment-style)。
- 檔案大小:proof_exclusion 323 行、outcome_writer 466 行、result_review 793 行
  (result_review 原 759 已在 800 注意線附近,本次 +34 未破 800)。
- Simplicity:無新 singleton、無新依賴、無投機抽象;blocked-signal 不加標記(語義不適用,
  避免 scope 擴張)。

## 測試證據(誠實聲明)

- 先紅後綠:新 13 條先跑 9 failed(修復目標)/ 4 passed(行為 pin),實作後全綠。
- Python:`py_compile` 三檔綠;focused 31 passed;直接消費者 5 檔 122 passed;
  **research 全套 1430 passed / 0 failed / 3 skipped**(基線 1417+13 新)。Mac python3.10
  + pytest 9.0.3(repo 目標 3.12,語法 `from __future__ import annotations` 相容;Linux
  回歸屬 E4)。
- Rust:`cargo check -p openclaw_engine` 0 warning;`cargo test -p openclaw_engine --lib`
  **4261 passed / 0 failed / 1 ignored**(Mac aarch64 dev profile)。
- GUI/node --check:N/A(零 .js 改動)。跨語言 1e-4 浮點:N/A(本批無跨語言數值)。

## 不確定之處

1. F14 標記是 build-time 快照:outcome 行 append-only + idempotent skip,之後才出現的
   fill 證據不會回填已寫行(與 ledger append-only 設計一致;真 fill-backed writer 是
   C8/F8 另票)。
2. `unmarked` 第四計數鍵是小決策:spec 給三值,legacy 無標記行若併入 indeterminate 會
   混淆「writer 明確判定無法對賬」與「標記機制上線前舊行」,分開更誠實。
3. E1 memory.md 追加與主樹報告落檔被本任務「主樹零寫入」約束擋住 → 報告以本檔(worktree
   內)代替,memory 追加待 PM merge 後補(1 行:F10/F11/F14 三修 + 判斷依據指針到本檔)。

## Operator/PM 下一步

- E2 審查本 branch(`fix/p3-smalls-0704`,3+1 commits)→ E4 Linux 回歸(research suite +
  rust workspace)→ PM 統一 commit/push。
- F14 消費側只動了 result_review;`bounded_probe_execution_realism_review` 的
  fill-backed 判定(`_is_fill_backed` 認 order_id)與新標記語義有輕微出入,屬既有行為,
  未動(scope 外,可在 QC P1-2 一併裁)。

E1 IMPLEMENTATION DONE: 待 E2 審查(report path: docs/CCAgentWorkSpace/E1/workspace/reports/2026-07-04--p3_smalls_f10_f11_f14.md @ fix/p3-smalls-0704)
