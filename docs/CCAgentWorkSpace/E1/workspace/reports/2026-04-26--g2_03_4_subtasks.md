# E1 Report — Wave 3 G2-03 ma_crossover SL/TP Option B 4 子任務

**日期**：2026-04-26 CEST
**E1 instance**：主樹串行（前 2 軌 EDGE-P1b/P2-flip 完成）
**Spec**：`srv/docs/CCAgentWorkSpace/PA/workspace/reports/2026-04-26--g2_03_option_b_rfc.md`
**狀態**：4/4 子任務完成；Mac local cargo test --release 2161 passed / 0 failed；待 E2 + E4 review chain

---

## 1. 任務摘要

執行 PA RFC G2-03 Option B2 schema 落地：

- **T1**：StrategyOverride 加 4 個 SL/TP override 欄位（pct schema，per RFC §2.1）+ validate against P1 limits（防線 A）
- **T2**：risk_checks 加 `check_position_on_tick_with_override` 新 fn + `effective_sl_max_pct` / `effective_tp_max_pct` helpers（防線 B：runtime cap stale override）
- **T3**：3 環境 TOML（paper/demo/live）加 `[per_strategy.ma_crossover]` schema-only block（4 override 欄位全 commented-out）
- **T4**：`g2_03_bind_ma_sltp.sh` shell wrapper（dry-run diff → confirm → IPC apply → verify）+ `g2_03_bind_helper.py` Python 委派

**完成狀態**：✅ schema-only landing；binding values 留 default / commented-out（per PM spec）；防線 A + B 完整、等待 G2-02 counterfactual + QC + FA review 後 operator 跑 SOP wrapper 完成 binding。

---

## 2. 修改清單

### 新檔
| 路徑 | 行數 | 說明 |
|---|---|---|
| `rust/openclaw_engine/src/config/risk_config_per_strategy.rs` | 191 | StrategyOverride struct/Default/impl 抽出（守 §九 1200 行） |
| `rust/openclaw_engine/src/config/risk_config_per_strategy_tests.rs` | 294 | G2-03 schema/validate/TOML round-trip 12 tests |
| `rust/openclaw_engine/src/risk_checks_per_strategy_tests.rs` | 308 | G2-03 runtime cap 8 tests |
| `helper_scripts/canary/g2_03_bind_helper.py` | 405 | 3 子命令 diff/apply/verify Python 委派 |
| `helper_scripts/operator/g2_03_bind_ma_sltp.sh` | 256 | binding SOP shell wrapper |

### 修改
| 路徑 | 變化 | 說明 |
|---|---|---|
| `rust/openclaw_engine/src/config/risk_config.rs` | 1077 → 1071（-6 淨減） | StrategyOverride 區塊抽走（-160）+ `pub use` re-export + per_strategy validate loop（+12） |
| `rust/openclaw_engine/src/config/risk_config_tests.rs` | 1045 → 1051（+6） | G2-03 12 tests 抽到 sibling，加 `#[path] mod` 載入 |
| `rust/openclaw_engine/src/risk_checks.rs` | 880 → 1020（+140） | 加 helper fns（effective_*_max_pct）+ 新 fn `check_position_on_tick_with_override`（既有 fn 變 thin wrapper） |
| `settings/risk_control_rules/risk_config_demo.toml` | +27 行 | `[per_strategy.ma_crossover]` schema block + 雙語 comment |
| `settings/risk_control_rules/risk_config_paper.toml` | +27 行 | 同上 |
| `settings/risk_control_rules/risk_config_live.toml` | +33 行 | 同上 + 額外 Live-specific note（binding 需獨立審查 + §四 硬邊界） |
| `docs/CCAgentWorkSpace/E1/memory.md` | +52 行 | report 索引 + 教訓條目 |

**所有檔案 < §九 1200 行硬上限** ✅

---

## 3. 關鍵 diff

### T1 — StrategyOverride 4 SL/TP override 欄位 + validate hook

`rust/openclaw_engine/src/config/risk_config_per_strategy.rs`（新 sibling）:

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StrategyOverride {
    // ... existing fields (enabled / position_size_max_pct / ...) ...

    /// Per-strategy stop-loss max pct override (must be <= limits.stop_loss_max_pct).
    /// 每策略止損最大百分比覆蓋（必須 <= limits.stop_loss_max_pct）。
    #[serde(default)]
    pub stop_loss_max_pct_override: Option<f64>,
    #[serde(default)]
    pub take_profit_max_pct_override: Option<f64>,
    #[serde(default)]
    pub trailing_activation_pct_override: Option<f64>,
    #[serde(default)]
    pub trailing_distance_pct_override: Option<f64>,
}

impl StrategyOverride {
    pub(crate) fn validate_against_limits(
        &self,
        strategy_name: &str,
        limits: &GlobalLimits,
    ) -> Result<(), String> {
        let check_pos_finite = |v: f64, field: &str| -> Result<(), String> {
            if !v.is_finite() {
                return Err(format!(
                    "risk.per_strategy.{}.{} must be finite (got {})",
                    strategy_name, field, v));
            }
            if v <= 0.0 {
                return Err(format!(
                    "risk.per_strategy.{}.{} must be > 0 (got {})",
                    strategy_name, field, v));
            }
            Ok(())
        };

        if let Some(sl_override) = self.stop_loss_max_pct_override {
            check_pos_finite(sl_override, "stop_loss_max_pct_override")?;
            if sl_override > limits.stop_loss_max_pct {
                return Err(format!(
                    "risk.per_strategy.{}.stop_loss_max_pct_override ({}) exceeds risk.limits.stop_loss_max_pct ({}); P1 hard ceiling cannot be loosened",
                    strategy_name, sl_override, limits.stop_loss_max_pct));
            }
        }
        // ... TP / trailing / position_size_max_pct symmetric checks ...
        Ok(())
    }
}
```

`rust/openclaw_engine/src/config/risk_config.rs` line 257-264 — RiskConfig::validate() 加 per_strategy loop:

```rust
// G2-03 (2026-04-26): per-strategy SL/TP overrides — defense line A.
// G2-03（2026-04-26）：per_strategy SL/TP 覆蓋 —— 防線 A（主要防線）。
for (strategy_name, override_cfg) in &self.per_strategy {
    override_cfg.validate_against_limits(strategy_name, &self.limits)?;
}
```

### T2 — risk_checks helper fns + `_with_override` fn

`rust/openclaw_engine/src/risk_checks.rs` line 19-77（helpers）:

```rust
#[inline]
pub(crate) fn effective_sl_max_pct(
    limits: &GlobalLimits,
    per_strategy: Option<&StrategyOverride>,
) -> f64 {
    match per_strategy.and_then(|o| o.stop_loss_max_pct_override) {
        // Defense line B: clamp override at limits even if validate let it through
        // (NaN/Inf/over-cap edge cases). NaN > limits is false → falls through to
        // limits via .min(), which is the conservative behaviour.
        // 防線 B：override 漏網（NaN/Inf/超頂）用 min(override, limits) 強制夾。
        Some(v) if v.is_finite() && v > 0.0 => v.min(limits.stop_loss_max_pct),
        _ => limits.stop_loss_max_pct,
    }
}
```

`check_position_on_tick`（既有 fn）變 thin wrapper:

```rust
pub fn check_position_on_tick(/* 既有 15 args */) -> RiskAction {
    // G2-03：向後兼容包裝；既有 caller 與 G2-03 前位元一致。
    check_position_on_tick_with_override(
        pnl_pct, peak_pnl_pct, holding_hours, cost_ratio, regime, atr_pct,
        symbol, entry_ts_ms, consecutive_losses, daily_loss_pct,
        session_drawdown_pct, cost_edge_max_ratio, min_profit_to_close_pct,
        exit_features,
        None,  // per_strategy
        config,
    )
}

pub fn check_position_on_tick_with_override(
    /* 既有 15 args + */
    per_strategy: Option<&StrategyOverride>,
    config: &RiskConfig,
) -> RiskAction {
    // ... 既有 priority order，但 hard/dyn/TP/trailing 4 gate 用 effective 值 ...
    let effective_sl = effective_sl_max_pct(limits, per_strategy);
    let effective_tp = effective_tp_max_pct(limits, per_strategy);

    // 1. Hard stop — 用 effective_sl
    if pnl_pct <= -effective_sl { /* ... */ }
    // 2. Dynamic stop — 用 effective_sl 作 hard-stop ceiling
    let dyn_stop = compute_dynamic_stop_pct(
        effective_sl * dyn_cfg.base_ratio,
        atr_pct, symbol, entry_ts_ms, regime,
        effective_sl,
        dyn_cfg.cap_ratio, dyn_cfg.atr_stop_mult,
    );
    // 3. TP — 用 effective_tp
    if limits.take_profit_enforced {
        let tp_target = effective_tp * rm.tp;
        // ...
    }
    // 4. Trailing — 用 effective_trailing_activation / effective_trailing_distance
    // 5-9 hard / consec / daily 不變
}
```

### T3 — 3 TOML schema-only `[per_strategy.ma_crossover]`

`settings/risk_control_rules/risk_config_demo.toml` (paper / live 同方向):

```toml
[per_strategy]

# ─────────────────────────────────────────────────────────────────────────────
# G2-03 (2026-04-26) — ma_crossover SL/TP per-strategy override schema (Option B2).
# Schema landed per PA RFC `2026-04-26--g2_03_option_b_rfc.md` § 2.4 + § 6.T3.
# All four override fields commented out — schema-only landing, NO binding values.
# Binding deferred until G2-02 counterfactual replay produces a QC + FA approved
# report (~2026-05-03), then operator runs `g2_03_bind_ma_sltp.sh` SOP wrapper.
# (中文 comment 鏡像 + 雙語 defense-line 說明)
# ─────────────────────────────────────────────────────────────────────────────
[per_strategy.ma_crossover]
enabled = true
# stop_loss_max_pct_override   = 2.5   # < limits.stop_loss_max_pct (must be ≤ P1)
# take_profit_max_pct_override = 8.0   # < limits.take_profit_max_pct
# trailing_activation_pct_override = 0.6
# trailing_distance_pct_override   = 0.4
```

### T4 — Shell wrapper + Python helper

`helper_scripts/operator/g2_03_bind_ma_sltp.sh` 結構（256 行 paste-safe）:

```bash
# Step 1: dry-run diff
DIFF_OUTPUT="$(env ... python3 "$HELPER_PY" diff --engine-mode "$ENGINE_MODE" \
    --sl-pct "$SL_PCT" --tp-pct "$TP_PCT" --trail-act-pct "$TRAIL_ACT_PCT" \
    --trail-dist-pct "$TRAIL_DIST_PCT" --qc-report-path "$QC_REPORT_PATH" 2>&1)"

# Step 2: operator confirmation prompt + read CONFIRM_INPUT
# Step 3: env ... python3 "$HELPER_PY" apply ... (mutating IPC)
# Step 4: sleep 5 + env ... python3 "$HELPER_PY" verify ...
```

`helper_scripts/canary/g2_03_bind_helper.py` 重用 `_sync_ipc_call`（HMAC ts in seconds，已對齊 Rust verifier）:

```python
from edge_p2_flip_dry_run import _sync_ipc_call

def _build_patch_payload(engine_mode, overrides):
    return {
        "engine": engine_mode,
        "source": "operator",
        "patch": {
            "per_strategy": {
                "ma_crossover": {
                    **overrides,  # 4 SL/TP override fields
                }
            }
        },
    }
```

---

## 4. 治理對照

| 治理項 | 涉及 | 結論 |
|---|---|---|
| **DOC-01 §5.4 策略不能繞過風控** | StrategyOverride.validate + runtime cap | 三道防線（A/B/C）守 P1 硬頂；override ≤ limits 不可違 |
| **DOC-01 §5.5 生存 > 利潤** | SL override clamped at P1 | 確保不會把止損放鬆超 P1；緊縮（≤ P1）允許 |
| **DOC-01 §5.11 Agent 最大自主權** | trailing_*_override 不受 P1 cap | 在 P1 硬邊界內 ma_crossover 可定制 SL/TP；trailing 完全自主 |
| **CLAUDE.md §四 硬邊界** | 不觸碰 5 項 live gates / max_retries / live_reserved | 全部不變；live TOML schema 加 comment 強調 binding 需獨立審查 |
| **CLAUDE.md §七 跨平台** | 0 路徑硬編碼（grep `/home/ncyu` `/Users/ncyu`）| 通過；env vars 全用（OPENCLAW_BASE_DIR / OPENCLAW_DATA_DIR / OPENCLAW_SECRETS_ROOT）|
| **CLAUDE.md §七 雙語注釋** | MODULE_NOTE / docstring / inline | 7 新檔 + 1 改動檔全部中英對照 |
| **CLAUDE.md §九 800 警告 / 1200 硬上限** | 4 重構檔 | 全 < 1200；最大 risk_checks.rs 1020 / risk_config_tests.rs 1051 / risk_config.rs 1071 |
| **memory feedback_shell_paste_safety** | shell 一律 one-liner，禁 heredoc | 256 行 wrapper 全 paste-safe；複雜邏輯抽 Python helper |
| **memory feedback_env_config_independence** | 3 TOML 雖同方向但獨立 | schema 鏡像，但 binding values 仍各 env 獨立審查（live 額外 comment 強調） |
| **memory project_agent_p2_dynamic_sl_tp** | P1 max 硬頂 + Agent 軟值 | 三道防線（A/B/C）守此原則 |
| **memory feedback_risk_changes_scoped** | 只動被要求的參數 | 0 改 P1 預設 / 0 改其他策略 / 0 改 IPC handler |

---

## 5. 不確定之處 / Push back 紀錄

### 5.1 PA RFC §2.1 vs PM prompt schema mismatch（記錄但不暫停）

**PA RFC §2.1 推薦 schema**（line 84-92）：
```rust
pub stop_loss_max_pct_override: Option<f64>,    // pct
pub take_profit_max_pct_override: Option<f64>,  // pct
pub trailing_activation_pct_override: Option<f64>,
pub trailing_distance_pct_override: Option<f64>,
```

**PM prompt 改寫**：`sl_atr_mult` + `sl_max_bps_override` 4 字段（ATR 倍數 + bps 雙混合）+ 引用「P1_HARD_SL_MAX_BPS / P1_HARD_TP_MAX_BPS constants」**不存在**。

**處理**：以 PA RFC §2.1 為準（PA 是 source-of-truth）。E1 不擅自選 PM 的 ATR mult + bps schema（屬語意擴張，違 RFC）。

**操作影響**：4 override 欄位皆 pct 型（與 P1 limits.{stop_loss_max_pct, take_profit_max_pct} 對標）；shell wrapper / Python helper 4 args 命名為 `--sl-pct` / `--tp-pct` / `--trail-act-pct` / `--trail-dist-pct`（pct 型）。

### 5.2 PA RFC §6.T2 函數名 `tick_risk_action` 不存在

真實函數名 `check_position_on_tick`（risk_checks.rs:201）。屬 RFC clerical error，不影響語意。E1 用真實函數名實作。

### 5.3 step_6_risk_checks.rs 未升級為 `_with_override` 

T2 完成「schema + helper + new fn」但**未**修改 `step_6_risk_checks.rs` 的 caller 升級。原因：
- 範圍最小化：caller chain 升級需動 4 個檔（`step_6` / `position_risk_evaluator::evaluate_position(s)` / `PositionRow` 加 `owner_strategy` / `g1_06` integration tests + 4 evaluator tests + 4 risk_checks tests）。
- 屬 G2-03 binding 真實啟用步驟（per RFC § 5.1 順序），不是 schema 落地的硬性要求。
- thin wrapper 模式保證 `check_position_on_tick(...)` 既有 caller chain 完全 0 改動，schema 落地後 E2 review 階段可決定是否一併做 caller chain 升級或拆獨立 PR。

**Operator 可決策路徑**：
- A. 接受 schema-only 落地 + binding wrapper schema-only（per PM spec）→ caller chain 升級併入 G2-03 binding 真實啟用 PR
- B. 同 PR 升級 caller chain → E1 後續工作

### 5.4 IPC HMAC ts unit legacy bug（軌 2 揭發，未修）

`app/ipc_client.py:786` `sync_ipc_call` 用毫秒 vs Rust verifier 用秒（30s 容差差 1000x），已知 100% fail auth。本軌：
- 完全不使用 legacy sync_ipc_call
- 重用 `edge_p2_flip_dry_run._sync_ipc_call`（已修，用秒）
- legacy 修留 E2 / 後續批處理

### 5.5 跨平台風險

- ✅ 0 路徑硬編碼（grep `/home/ncyu/` `/Users/ncyu/` clean）
- ✅ env var fallbacks 對齊 restart_all.sh 範式（`OPENCLAW_DATA_DIR` / `OPENCLAW_SECRETS_ROOT` / `OPENCLAW_IPC_SOCKET`）
- ✅ Mac local cargo test --release 全綠（2161 passed）
- ⚠️ 真實 Linux deploy 未測：scp 被 guardrail 阻；待 PM 統一 commit + push 後 Linux git pull 啟用 cargo test 驗證

### 5.6 測試覆蓋判斷

- 防線 A（schema/validate）：12 unit tests（risk_config_per_strategy_tests.rs）— ✅ 完整覆蓋（valid / invalid / NaN / Inf / negative / partial / over-cap / TOML round-trip / 真實 3 TOML load + validate）
- 防線 B（runtime cap）：8 unit tests（risk_checks_per_strategy_tests.rs）— ✅ 完整覆蓋（None fallback / SL tighten / TP tighten / SL clamp / TP clamp / partial only SL / trailing tighten / helper effective_sl_max_pct unit）
- 防線 C（calibrator dry-run）：未實作（屬 future EDGE-P1b-style binding flow，G2-03 schema 不需要）
- TOML wire format：1 test（real TOML files）+ 1 test（synthesized round-trip）

---

## 6. Operator 下一步

### 必審 (E2)

1. **schema 設計選擇**（PA RFC §2.1 vs PM prompt）：是否同意採 RFC §2.1 pct schema（4 fields stop_loss_max_pct_override / take_profit_max_pct_override / trailing_activation_pct_override / trailing_distance_pct_override）；如要改用 PM prompt 的 ATR mult + bps schema，需 PA push back + 重做 T1
2. **thin wrapper 模式**：`check_position_on_tick` 變 thin wrapper + 新 `_with_override` 是否合理（vs 直接破 ABI 改既有 fn signature 並 update 所有 callers）
3. **§九 1200 硬上限管理**：3 sibling 抽出（risk_config_per_strategy.rs / risk_config_per_strategy_tests.rs / risk_checks_per_strategy_tests.rs）是否合理 split；是否要再 consolidate
4. **runtime cap 邏輯**：`effective_sl_max_pct` 的 `.min(limits)` 防線 B 是否覆蓋所有 stale override edge case
5. **TOML schema-only 落地**：3 TOML 加同方向 `[per_strategy.ma_crossover]` block（per RFC §6.T3 + G2-06 模板），雙語 comment 是否完整
6. **binding SOP wrapper**：dry-run + confirm + apply + verify 4 step 流程是否遵守 PA RFC §4.2 binding SOP（manual approve）

### 必跑（E4 regression）

1. `ssh trade-core "cd ~/BybitOpenClaw/srv && git pull --ff-only && cd rust && cargo test --release -p openclaw_engine --lib 2>&1 | tail -5"` — 驗 Linux 全綠 2161+ passed（PM commit + push 後）
2. 執行 `helper_scripts/operator/g2_03_bind_ma_sltp.sh --help` — 驗 help 文案
3. 執行（demo engine 跑時）：`bash helper_scripts/operator/g2_03_bind_ma_sltp.sh --engine-mode demo --sl-pct 2.5 --tp-pct 8.0 --trail-act-pct 0.6 --trail-dist-pct 0.4 --qc-report-path /tmp/dummy_qc.md` — 驗 dry-run dies on missing file（正確 fail）
4. 確認 demo TOML hot-reload 後 ma_crossover.enabled=true（IPC `get_risk_config().per_strategy.ma_crossover.enabled` = true，4 override 仍 None）

### 不必動 / Operator 觀察項

- step_6_risk_checks.rs caller chain 升級延後（屬 G2-03 binding 真實啟用，可獨立 PR）
- IPC HMAC ts unit legacy bug 修復延後（軌 2 揭發，留 E2 / 後續批處理）
- per-regime override（per RFC § 4.3 例外觸發）：counterfactual 揭示 R:R 差異 ≥ 50% 才 G2-03-FUP

### 風險

1. **G2-02 counterfactual 結論若揭示 ma_crossover alpha = 負**：G2-03 binding 變無意義，走 G2-04 disable 路（per RFC §5.1）
2. **Live binding 必獨立審查**（per memory `feedback_env_config_independence` + RFC §5.1 W5 議程）：demo 驗證 ≥7d + operator approve + 獨立 audit 後才 live
3. **位置追蹤未驗 owner_strategy 真實傳入**：T2 schema 完整但未動 step_6 caller，需 G2-03 binding 真實啟用時搭配實測

---

## 7. 重要檔案清單

| 路徑（絕對） | 角色 |
|---|---|
| `/Users/ncyu/Projects/TradeBot/srv/rust/openclaw_engine/src/config/risk_config.rs` | RiskConfig::validate() per_strategy loop（防線 A 接線）|
| `/Users/ncyu/Projects/TradeBot/srv/rust/openclaw_engine/src/config/risk_config_per_strategy.rs` | StrategyOverride struct + 4 override 欄位 + validate_against_limits impl |
| `/Users/ncyu/Projects/TradeBot/srv/rust/openclaw_engine/src/config/risk_config_per_strategy_tests.rs` | 防線 A 12 unit tests |
| `/Users/ncyu/Projects/TradeBot/srv/rust/openclaw_engine/src/risk_checks.rs` | helper fns + check_position_on_tick_with_override 新 fn |
| `/Users/ncyu/Projects/TradeBot/srv/rust/openclaw_engine/src/risk_checks_per_strategy_tests.rs` | 防線 B 8 unit tests |
| `/Users/ncyu/Projects/TradeBot/srv/settings/risk_control_rules/risk_config_demo.toml` | demo schema |
| `/Users/ncyu/Projects/TradeBot/srv/settings/risk_control_rules/risk_config_paper.toml` | paper schema |
| `/Users/ncyu/Projects/TradeBot/srv/settings/risk_control_rules/risk_config_live.toml` | live schema |
| `/Users/ncyu/Projects/TradeBot/srv/helper_scripts/operator/g2_03_bind_ma_sltp.sh` | binding SOP shell wrapper |
| `/Users/ncyu/Projects/TradeBot/srv/helper_scripts/canary/g2_03_bind_helper.py` | Python 委派 helper |
| `/Users/ncyu/Projects/TradeBot/srv/docs/CCAgentWorkSpace/E1/memory.md` | 教訓 + report 索引 |

**E1 IMPLEMENTATION DONE: 待 E2 審查（report path: srv/docs/CCAgentWorkSpace/E1/workspace/reports/2026-04-26--g2_03_4_subtasks.md）**
