# E1 LG5-W3-FUP-2 Fix 1 — Cron-ize edge_label_backfill.py + healthcheck [43]

**Date**: 2026-05-02
**Owner**: E1
**Spec source**: PA dispatch LG5-W3-FUP-2 Fix 1
**Branch**: main (uncommitted, awaiting E2 review)
**Repo**: `/Users/ncyu/Projects/TradeBot/srv/`

---

## 1. 任務摘要

MIT FUP-2 diagnosis 揭露 `[42b]` healthcheck FAIL root cause = `edge_label_backfill.py` 沒 cron / 純 on-demand。Fix 1 = 寫 cron wrapper（每 30 分對 demo + live_demo 各跑一次）+ 新 healthcheck `[43]` 監控 cron 活性。**0 byte change to backfill business logic**。**operator 手動 deploy crontab**（不在本 ticket 內）。

## 2. Deliverables

| 檔 | 操作 | LOC |
|---|---|---|
| `helper_scripts/cron/edge_label_backfill_cron.sh` (new dir + new file) | NEW | 134 |
| `helper_scripts/db/passive_wait_healthcheck/checks_governance.py` | EDIT | 372 → 519 (+147 含 docstring) |
| `helper_scripts/db/passive_wait_healthcheck/__init__.py` | EDIT | 176 → 181 (+5) |
| `helper_scripts/db/passive_wait_healthcheck/runner.py` | EDIT | 659 → 691 (+32) |
| `helper_scripts/db/test_lg5_healthchecks.py` | EDIT | 249 → 356 (+107，6 new tests) |
| `docs/healthchecks/2026-05-02--lg5_health_checks.md` | EDIT | 244 → 353 (+109) |

LOC 全部 < 800 警告線；hard cap 1500 寬鬆滿足。

## 3. 關鍵 design 決策

- **CLI flags 已存在**：`edge_label_backfill.py` argparse 已有 `--engine-mode` + `--batch-limit`，無需修改 backfill module（business logic 0 change）
- **engine_mode 寫入面對齊**：cron wrapper 用 demo + live_demo 兩 pass；`[43]` SQL 也用 `IN ('demo','live_demo')`；對齊 `[42b]` producer (`mlde_demo_applier`) 同樣 set
- **Age 在 PG 內算**：`extract(epoch from now() - max(label_filled_at))` 避 Python/DB 時鐘 skew
- **Threshold 工程提案**：PASS<2h（30min × 4 headroom）、WARN<6h、FAIL>=6h；MIT 沒明文 SLA，可後續觀察調整
- **Two-tier 哨兵**：`[43]`（30min cron 活性）+ `[42b]`（24h attribution ratio）；`[43]` 漏掉 → `[42b]` 24h 後 emergency lease_revoke 兜底
- **Fail-loud**：wrapper 任一 pass 非零即 break + exit 1，cron mailer 立即 page；不 mask 訊號

## 4. Tests

- 19/19 PASS（test_lg5_healthchecks.py：13 prior + 6 new for `[43]`）
- 106/106 PASS（helper_scripts/db/ baseline regression）
- 15/15 PASS（program_code/ml_training/tests/test_edge_label_backfill.py — backfill module 未動）

## 5. 驗證 checklist

- [x] `bash -n helper_scripts/cron/edge_label_backfill_cron.sh` clean
- [x] `python3 -m py_compile` × 4 modules clean
- [x] `python3 -m pytest helper_scripts/db/ -q` PASS（含 [43] 6 new tests）
- [x] full pytest baseline preserved (lg5 19/19, helper_scripts/db 106/106, backfill module 15/15)
- [x] `git diff --check` 0
- [x] cross-platform `grep -E '/home/ncyu|/Users/[^/]+'` 0 hit（wrapper 用 env var；docstring 用 `$HOME` 變量字面）
- [x] 雙語注釋（MODULE_NOTE + docstring + inline + SQL 注釋全覆蓋）
- [x] `python3 -m helper_scripts.db.passive_wait_healthcheck --help` 顯示 `[43] label_backfill_freshness` slot

## 6. 治理對照

- ✅ CLAUDE.md §七 跨平台兼容 / 雙語注釋 / 被動等待 TODO 必附 healthcheck（cron schedule 30min ↔ `[43]` PG max age verdict）
- ✅ CLAUDE.md §九 1500 LOC hard cap（最大 691 / 519，遠低於警告線）
- ✅ PA dispatch 限定範圍：0 backfill business-logic / 0 SQL migration / 0 governance hub change / 0 strategy params / 不 commit / 不 deploy

## 7. 下一步

E2 review → E4 SSH Linux regression（建議真跑一次 wrapper + verify `[43]` PASS）→ QA → PM 統一 commit + push（與 Fix 2 batch 一起）→ operator 手動加 crontab（不在 PM commit 內）。

詳細 diff / rationale / Operator 指令見 `.claude_reports/20260502_205913_lg5_w3_fup_2_fix_1.md`。
