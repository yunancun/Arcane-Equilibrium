# REF-20 Wave 5 Batch 5A-B — P3a-Q2 + P3a-Q6 + RGM-Q1 IMPL

**日期：** 2026-05-03
**Owner：** E1 sub-agent
**派發：** PM REF-20 Wave 5 Batch 5A-B（Mac dev IMPL bundle）
**狀態：** READY-FOR-E2-REVIEW + READY-FOR-E4-REGRESSION + READY-FOR-MIT-REVIEW

## 1. 任務摘要

PM 派發 Wave 5 三 task 順序 IMPL（Q2 → Q6 → RGM-Q1），全部完成 + 40 pytest case PASS。

| Task ID | Spec | Files | Tests |
|---|---|---|---|
| **R20-P3a-Q2** | V3 §8.1 OOS embargo `max(7d, 2 × half_life_days)` | V041 SQL + `embargo_validator.py` | 6+8 = 14 PASS |
| **R20-P3a-Q6** | V3 §8.1 freshness ≤72h + sample power ≥200 | `calibration_gate.py` | 11 PASS |
| **R20-RGM-Q1** | V3 §8.4 #1 first 500 fills warmup per cell | `regime_controller.py` | 15 PASS |

**Total**: 4 module + 4 test + 1 SQL + 1 SQL test + 1 package init = **10 file**, **40 / 40 pytest PASS**, **0 cross-platform path violation**, **0 trading.* mutation**, **all bilingual MODULE_NOTE**.

## 2. 修改清單（10 file 全絕對路徑）

### TASK 1 / R20-P3a-Q2（4 file）
- `/Users/ncyu/Projects/TradeBot/srv/sql/migrations/V041__replay_oos_embargo_enforcement.sql`（249 LOC，Guard A + Guard B + chk_embargo_days CHECK + bootstrap stub for replay.experiments）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/exchange_connectors/bybit_connector/control_api_v1/replay/embargo_validator.py`（260 LOC，3 public API + EmbargoCheckResult）
- `/Users/ncyu/Projects/TradeBot/srv/tests/migrations/test_v041_oos_embargo.py`（226 LOC，6 PASS 含 cross-language alignment 12-case sweep）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/__init__.py`（new package）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_embargo_validator.py`（205 LOC，8 PASS）

### TASK 2 / R20-P3a-Q6（2 file）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/exchange_connectors/bybit_connector/control_api_v1/replay/calibration_gate.py`（407 LOC，CalibrationGate class + 4 dataclass + 4 literal）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_calibration_gate.py`（259 LOC，11 PASS）

### TASK 3 / R20-RGM-Q1（2 file）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/learning_engine/regime_controller.py`（358 LOC，RegimeController base + 2 dataclass + 2 literal forward-compat）
- `/Users/ncyu/Projects/TradeBot/srv/program_code/learning_engine/tests/test_regime_controller.py`（288 LOC，15 PASS）

### Ledger（1 file 編輯）
- `/Users/ncyu/Projects/TradeBot/srv/sql/migrations/REF-20_RESERVATION.md`（V041 reserved → land；§6 ledger v1.3 → v1.4）

## 3. 關鍵 diff（精煉摘要）

### V041 chk_embargo_days CHECK（核心不變量）
```sql
ALTER TABLE replay.experiments
    ADD CONSTRAINT chk_embargo_days
    CHECK (
        embargo_days IS NULL
        OR half_life_days IS NULL
        OR embargo_days >= GREATEST(7, CEIL(2.0 * half_life_days)::INTEGER)
    );
```

### embargo_validator.py（Python sibling）
```python
def compute_min_embargo_days(half_life_days: Optional[float]) -> int:
    if half_life_days is None:
        half_life_days = 14.0  # V3 §8.1 conservative fallback
    multiplied = HALF_LIFE_MULTIPLIER * float(half_life_days)
    ceiled = int(math.ceil(multiplied))
    return max(MIN_EMBARGO_DAYS_FLOOR, ceiled)
```

### CalibrationGate.gate_handoff（複合 verdict）
```python
if freshness.status == "ok" and power.status == "ok":
    verdict = "handoff_ok"
elif freshness.status == "stale" and power.status == "ok":
    verdict = "stale_calibration"
elif freshness.status == "ok" and power.status == "insufficient":
    verdict = "insufficient_power"
else:
    verdict = "both_fail"
    reason_zh = f"{freshness.reason_zh}；{power.reason_zh}"
    reason_en = f"{freshness.reason_en}; {power.reason_en}"
```

### RegimeController.check_warmup（500 fills gate）
```python
if fills_count >= self._warmup_threshold:
    ready = True
    remaining = 0
    status = "ready"
else:
    ready = False
    remaining = self._warmup_threshold - fills_count
    status = "warming_up"
```

## 4. 治理對照（V3 §12 acceptance binding）

| # | Acceptance | 本 task 落地點 | 驗證 |
|---|---|---|---|
| **#15** | execution_calibration_freshness | `CalibrationGate.check_freshness` ≤72h | test_check_freshness_boundary_72h_exact_passes + test_gate_handoff_rejects_stale_calibration |
| **#16** | execution_calibration_power | `CalibrationGate.check_sample_power` n≥200 | test_check_sample_power_boundary_200_exact_passes + test_gate_handoff_rejects_insufficient_power |
| **#18** | replay_regime_shift_gate | `RegimeController.check_warmup` 500 fills warmup | test_check_warmup_ready_at_500_fills + test_check_warmup_still_warming_at_499_fills（boundary）|
| V3 §8.1 OOS embargo | DB CHECK + Python validator 兩層守 | V041 chk_embargo_days + `embargo_validator.validate_embargo` | test_v041_check_aligns_with_python_validator（12-case cross-language alignment sweep）|

### V3 §11 P3a Exit
- ✅ `stale <=72h` 閾值（CalibrationGate hardcode FRESHNESS_MAX_HOURS=72.0）
- ✅ `n >= 200 per strategy-window` 閾值（SAMPLE_POWER_MIN_N=200）
- ✅ OOS embargo `max(7d, 2 * half_life_days)` 三層強制（V041 CHECK + Python validator + cross-lang alignment test）

### V3 §8.4 #1（warmup 500 fills）
- ✅ `WARMUP_FILLS_THRESHOLD=500` 為 V3 §8.4 #1 spec hardcode
- ✅ ctor override 限 hermetic test；test_default_constructor_uses_v3_spec_threshold + test_warmup_threshold_constant_matches_v3_spec 雙驗證

### CLAUDE.md §七 強制檢查
- ✅ Guard A/B 在 V041 SQL（fresh CREATE 用 IF NOT EXISTS + Guard A 寬鬆驗 experiment_id；ADD COLUMN IF NOT EXISTS + Guard B 驗 type）
- ✅ Bilingual MODULE_NOTE 在所有 4 module（EN/中 雙塊 + 公開 API 雙語 docstring）
- ✅ Cross-platform path：grep `/home/ncyu` / `/Users/[^/]+` 0 hit on 4 module + V041 SQL（test 模組 `_THIS_FILE.resolve()` 動態解析路徑無硬編碼）
- ✅ `max_retries=0` / `live_execution_allowed` / `execution_authority` 不觸碰
- ✅ 0 SQL `INSERT INTO trading.*`；0 `live_*` mutate
- ✅ 文件大小：全 4 module ≤ 407 LOC（< 800 警告線）；V041 SQL 249 LOC

### 跨語言一致性（1e-4 contract）
- V041 SQL `GREATEST(7, CEIL(2.0 × half_life_days)::INTEGER)` ≡ Python `max(7, math.ceil(2.0 * h))`
- 12-case sweep 驗證（test_v041_check_aligns_with_python_validator + test_python_validator_aligns_with_v041_check_expression）
  - boundary cases: 3.0 / 3.5 / 5.0 / 5.6 (fractional ceil) / 7.5 / 14.0 / 100.0 全對齊
  - NULL semantics: SQL NULL → CHECK pass；Python None → 14d fallback → 28d min（兩語意都 forward-compat 留 fixture row）

## 5. pytest 全 PASS 列表（40 case）

```
tests/migrations/test_v041_oos_embargo.py                                                                                  6 PASS
program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_embargo_validator.py                     8 PASS
program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_calibration_gate.py                     11 PASS
program_code/learning_engine/tests/test_regime_controller.py                                                              15 PASS
─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
TOTAL                                                                                                                     40 PASS
```

執行：`python3 -m pytest tests/migrations/test_v041_oos_embargo.py program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_embargo_validator.py program_code/exchange_connectors/bybit_connector/control_api_v1/replay/tests/test_calibration_gate.py program_code/learning_engine/tests/test_regime_controller.py -v`

執行時間：< 0.05s（全 unit test，0 PG / HTTP server / async I/O 依賴）。

## 6. 不確定之處（向 PM push back）

1. **`replay.experiments` fixture 部署順序未確認**：V041 採雙路徑 bootstrap stub + ADD COLUMN IF NOT EXISTS 容忍兩順序：(a) V041 先 land + fixture 後 land 補完整 schema；(b) fixture 先 land 完整 schema + V041 ADD COLUMN no-op + 加 CHECK。Mac dev pytest 不驗 fixture；Linux operator deploy 兩順序均 idempotent。**請 PM 確認部署協議**。
2. **embargo_validator / calibration_gate / regime_controller 未 wire 進 replay_routes**：本 task 範圍純 module IMPL + unit test。後續 sub-task wire 進 manifest POST pre-write hook（embargo_validator）+ generate_handoff_verdict endpoint（calibration_gate + regime_controller）；後者尚未存在 endpoint，需 P4/P6 wave 補。建議 PM 派 R20-P3a-Q2-WIRING + R20-P3a-Q6-WIRING + R20-RGM-Q1-WIRING 三 sub-task 完成接線。
3. **RegimeController 為 Q2/Q3/Q4 base**：本 commit 交付 RGM-Q1 warmup gate 並設 CompositeCellStatusLiteral 故意窄；Q2/Q3/Q4 sub-task IMPL 時 widen literal + 加 dataclass 欄。建議 sibling sub-task 派發時提示 maintain ABI 穩定性（不 break Q1 forward-compat hook：extra_payload Dict / get_cell_status 介面）。
4. **`learning_engine/` package 已 sibling-occupied**：本 task IMPL 時 sibling agent 已 land half_life_estimator + quantile_bootstrap；regime_controller 加進 package 不衝突；__init__.py 不需修。
5. **CalibrationGate timezone-aware 強制**：對 naive datetime 直接 raise ValueError（避 DST/region drift 沉默對齊錯誤）。若有 caller 傳 naive，需 caller 端 patch（非本 module 容忍範圍）。

## 7. Operator 下一步

1. **PM**：確認 V041 vs replay.experiments fixture 部署協議；確認 ledger v1.4 寫法；考慮派 3 wiring sub-task。
2. **E2 review**：對 V041 雙路徑 fixture-tolerant 設計接受 + chk_embargo_days CEIL 跨語言對齊 12 case 全 PASS + 雙語 MODULE_NOTE 完整 + LOC < 800。
3. **E4 regression**：Linux trade-core 跑 `psql -f V041__replay_oos_embargo_enforcement.sql` × 2 驗 idempotent；Mac dev 已預驗 pytest 40/40 PASS。建議跑全 sibling Wave 5 P3a-Q1 (half_life_estimator) + P3a-Q3 (quantile_bootstrap) test 集確認無 regression。
4. **MIT review**：CalibrationGate 與 P3a-Q1 half_life_estimator 接口（HalfLifeResult.half_life_days 餵 manifest → embargo_validator + calibration_gate）；確認 Q2/Q3/Q4 RegimeController 擴展時 ABI 穩定性。
5. **PM commit message draft**：
   ```
   feat(replay): P3a-Q2+Q6 + RGM-Q1 — embargo + freshness gate + warmup (Wave 5 Batch 5A)
   ```

---

**完成序列**：
- 雙語 MODULE_NOTE / Guard A+B / cross-platform / LOC<800：✅
- pytest 40/40 PASS（任務要求 16）：✅
- E2/E4/MIT review-ready：✅
- ledger v1.4 update：✅
- E1 memory.md 追加：✅
