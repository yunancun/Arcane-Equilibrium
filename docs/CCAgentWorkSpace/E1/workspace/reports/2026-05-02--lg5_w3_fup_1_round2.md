# E1 LG5-W3-FUP-1 ROUND 2 — Fix HIGH-1 + MED-1 + 4 NIT

**Date**: 2026-05-02
**Owner**: E1
**Spec source**: PA dispatch LG5-W3-FUP-1 ROUND 2 (after E2 RETURN with 1 HIGH + 1 MED + 4 NIT)
**Branch**: main (uncommitted, awaiting E2 round-3 review)
**Repo**: `/Users/ncyu/Projects/TradeBot/srv/`

---

## 1. 任務摘要

E2 round 1 RETURN 6 findings：HIGH-1 是 fundamental design bug —— round 1 的 `is_authorized()` hard-skip 反而會破壞 IMPL-2 audit emission 路徑、讓 `[42]` healthcheck 永遠 FAIL。Round 2 全部 6 finding 修復；11/11 + 59/59 PASS；LOC 716 < 1500；§九 4 singleton 已登記。

## 2. 6 Findings 修法

- **HIGH-1**：刪 `_run_cycle()` unauthorized hard-skip block；改信任 IMPL-2 R6 evaluator emit `reject_hard_veto` audit row；新增 `_total_rejected_hard_veto` verdict-derived metric；MODULE_NOTE / NOTE 雙語注釋強調「不要把 hard-skip 加回去」；healthchecks doc 改寫 unauthorized observability 為 `total_rejected_hard_veto` + `governance_audit_log` SQL grep
- **MED-1**：CLAUDE.md §九 表加 2 行 entry（4 singleton：`_consumer`/`_consumer_lock`、`_LEADER_LOCK_FD`/`_LEADER_LOCK_PATH`）鏡 `EDGE-SCHEDULER-LEADER-1` 格式
- **NIT-2**：`test_disabled_env_returns_none` 加 `assert "lg5-review-consumer" not in [t.name for t in threading.enumerate()]`
- **NIT-3**：新 `TestUnauthorizedStillCallsImpl2` 3 tests（unauthorized hub 仍 call IMPL-2、mixed reject reasons 子集統計、`is_authorized()` raise wrapper 不在乎）
- **NIT-4**：`_total_errors += 1` 從 except block 移到 cycle 末尾 `with self._lock:` 區內 `+= len(errors)`
- **NIT-1**：用 `wc -l` 真值報告

## 3. LOC delta

| 檔 | Round 1 | Round 2 | Δ |
|---|---|---|---|
| scheduler.py | 677 | 716 | +39 |
| test.py | 337 | 454 | +117 |
| healthchecks.md | (untouched) | +13 | +13 |
| CLAUDE.md §九 | (untouched) | +2 | +2 |

716 < 1500 hard limit ✓

## 4. Tests

- 11/11 PASS（new test file）
- 59/59 PASS（baseline regression: mlde_demo_applier + lg5_review_live_candidate）
- 70/70 combined ✓

## 5. 驗證 checklist

- [x] py_compile 0 error
- [x] new test 11 PASS
- [x] baseline 59/0 preserved
- [x] LOC < 1500
- [x] CLAUDE.md §九 4 singleton grep 4 hit
- [x] git diff --check 0
- [x] cross-platform grep 0 hardcoded path
- [x] 雙語注釋

## 6. 下一步

E2 round 3 review → E4 regression（SSH Linux 跑 11 tests）→ QA → PM 統一 commit + push + `restart_all.sh --rebuild --keep-auth`。

詳細 diff / rationale / Operator 指令見 `.claude_reports/20260502_204500_lg5_w3_fup_1_round2.md`。
