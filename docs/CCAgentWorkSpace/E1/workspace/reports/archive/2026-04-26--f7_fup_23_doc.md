# E1 F7-FUP-23-DOC — docstring 修正（E2 第二輪 RETURN doc-only fix）

日期：2026-04-26 後段（commit 跨 00:42 隔日 04-27）
Worktree：`worktree-e1-f7-isolated`
Branch：`e1-f7-healthchecks-isolated`
Commit：`e437a87`（前序 `bdde091` HEAD）

## 修改

| path | 動作 |
|---|---|
| `helper_scripts/db/passive_wait_healthcheck/checks_engine.py` | 修 2 處 docstring 末段表名（英 543-545 / 中 571-572）：`learning.execution_orphans` → `trading.fills` 配 `strategy_name LIKE 'unattributed:%'` 標記（無獨立 orphan table） |

## 驗證

- Mac smoke：39/39 tests OK（doc-only 不影響）
- Diff scope：1 檔 +4 / -3，無誤觸他檔
- Push：`bdde091..e437a87` → `origin/e1-f7-healthchecks-isolated`

## E2 第二輪 RETURN 對應

E2 grep `sql/` + `program_code/` 0 hit `learning.execution_orphans` → 表不存在；F4 真實落地通道是 `trading.fills` 用 `strategy_name LIKE 'unattributed:%'`，沒有獨立 orphan table。本次只修 docstring 末段引用錯，docstring 上半段（`unattributed_fill_observer` emitter + 機制描述）已正確不動。

## 報告

`.claude_reports/20260427_004243_e1_f7_fup_23_doc.md` 6 節必備（任務摘要 / 修改清單 / 關鍵 diff / 治理對照 / 不確定之處 / Operator 下一步）。

## 下一步

ssh trade-core verify import → PM 直接 merge（task brief：純 doc 改動 E2 已標 acceptable for self-fix，不必 re-E2）→ batch merge sign-off。
