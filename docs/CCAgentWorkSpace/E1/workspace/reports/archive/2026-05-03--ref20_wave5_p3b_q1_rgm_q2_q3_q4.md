# REF-20 Wave 5 Batch 5B-D — P3b-Q1 + RGM-Q2/Q3/Q4 IMPL

**日期：** 2026-05-03
**Owner：** E1 sub-agent
**派發：** PM REF-20 Wave 5 Batch 5B-D（Mac dev IMPL bundle）
**狀態：** READY-FOR-E2-REVIEW + READY-FOR-E4-REGRESSION + READY-FOR-MIT-REVIEW

## 1. 任務摘要

PM 派發 Wave 5 Batch 5B-D 4 task 順序 IMPL（Q1 → Q2 → Q3 → Q4），全部完成 + 57 pytest case PASS（含 RGM-Q1 既有 15 regression）。

| Task ID | Spec | Files | Tests |
|---|---|---|---|
| **R20-P3b-Q1** | V3 §8.1 cell n>=30 + §8.2 block bootstrap CI | `cell_calibrator.py` | 20 PASS |
| **R20-RGM-Q2** | V3 §8.4 #2 CUSUM ±3σ break → freeze handoff | `regime_controller.py` extend | 6 PASS（CUSUM 子集） |
| **R20-RGM-Q3** | V3 §8.4 #3 Kupiec POF n>=250 cell-independent | `regime_controller.py` extend | 6 PASS（Kupiec 子集） |
| **R20-RGM-Q4** | V3 §8.4 #4 PSR(0)<0.95 across 3×250 → refit + PM alert | `regime_controller.py` extend | 7 PASS（PSR 子集） |
| Q2/Q3/Q4 共用 | constructor + module constants | 同上 | 3 PASS |

**Total**: 5 file（2 module 新建 + 1 module extend + 1 internal helper extract + 2 test）, **57 / 57 pytest PASS**, **0 cross-platform path violation**, **0 trading.* mutation**, **all bilingual MODULE_NOTE**.

## 2. 修改清單（5 file 全絕對路徑）

### TASK 1 / R20-P3b-Q1（2 file 新建）
- `/Users/ncyu/projects/TradeBot/srv/program_code/learning_engine/cell_calibrator.py`（659 LOC，CellCalibration + CellCalibrator + CellGateLiteral；3 public method calibrate_cell / gate / incremental_update + 1 helper get_cell；委派 P3a-Q3 QuantileBootstrap）
- `/Users/ncyu/projects/TradeBot/srv/program_code/learning_engine/tests/test_cell_calibrator.py`（457 LOC，20 PASS — 4 必測 + 16 extras）

### TASK 2-4 / R20-RGM-Q2/Q3/Q4（3 file，1 extend + 1 internal extract + 1 test）
- `/Users/ncyu/projects/TradeBot/srv/program_code/learning_engine/regime_controller.py`（1062 LOC — RGM-Q1 base 擴展 3 method + 3 dataclass + widen Literal 從 2 → 6 狀態）
- `/Users/ncyu/projects/TradeBot/srv/program_code/learning_engine/_regime_math.py`（199 LOC 新建 — internal helper module；底線前綴示意 internal-only）
- `/Users/ncyu/projects/TradeBot/srv/program_code/learning_engine/tests/test_regime_controller_q2_q3_q4.py`（559 LOC，22 PASS — 12 必測 + 10 extras）

## 3. 關鍵 diff（精煉摘要）

### CellCalibrator gate 三維 verdict
```python
def gate(self, cell: CellCalibration) -> CellGateLiteral:
    if cell.n < cell.n_threshold:
        return "insufficient_n"
    if not _is_finite_ci(cell.ci_low, cell.ci_high):
        return "bootstrap_unstable"
    half_width = _ci_half_width(cell.ci_low, cell.ci_high)
    if half_width > self._unstable_half_width_bps:
        return "bootstrap_unstable"
    return "ready"
```

### CellCalibrator.incremental_update edge-trigger rebootstrap
```python
new_n_since_last = len(state.fill_buffer) - state.last_bootstrap_n
force_rebootstrap = (
    state.last_calibration is None
    or new_n_since_last >= self._rebootstrap_threshold
    or (
        state.last_calibration.n < self._n_threshold
        and len(state.fill_buffer) >= self._n_threshold
    )
)
```

### RegimeController.check_cusum (RGM-Q2)
```python
max_abs_z, mean, std = _cusum_statistic(arr)
break_detected = max_abs_z > self._cusum_sigma_threshold
state_after = "break" if break_detected else "active"
```

### RegimeController.check_kupiec_pof (RGM-Q3) sufficient_sample gate
```python
sufficient = n >= self._kupiec_min_n
if not sufficient:
    # V3 §8.4 #3 「cell n<250 skipped」
    return KupiecResult(..., sufficient_sample=False, reject_h0=False, p_value=NaN, ...)
```

### RegimeController.check_psr_3windows (RGM-Q4) tail-window slicing
```python
required = self._psr_num_windows * self._psr_window_size  # 750
tail_start = n_total - required
for w in range(self._psr_num_windows):
    start = tail_start + w * self._psr_window_size
    end = start + self._psr_window_size
    psr_val = _psr_zero(arr[start:end])
    window_psrs.append(psr_val)
all_below = all(p < self._psr_threshold for p in window_psrs)
refit_trigger = all_below
```

## 4. 治理對照（V3 §12 acceptance binding）

| # | Acceptance | 本 task 落地點 | 驗證 |
|---|---|---|---|
| **#16** | execution_calibration_power | `CellCalibrator.gate` cell n>=30 | test_calibrate_cell_n_below_threshold_returns_insufficient + test_calibrate_cell_n_at_threshold_returns_ready（boundary 29 / 30 雙驗）|
| **#18** | replay_regime_shift_gate | `RegimeController` Q1+Q2+Q3+Q4 sequential state machine | test_check_warmup_ready_at_500_fills (Q1) + test_cusum_detects_break_on_drift_shift (Q2) + test_kupiec_reject_h0_when_violations_far_above_expected (Q3) + test_psr_3windows_refit_when_all_below_threshold (Q4) |
| V3 §8.1 cell sample | DEFAULT_N_THRESHOLD = 30 | `cell_calibrator.py` constant | test_default_n_threshold_matches_v3_spec |
| V3 §8.2 block bootstrap | delegate to QuantileBootstrap (sibling 5A-A) | `cell_calibrator._compute_calibration` | test_calibrate_cell_n_at_threshold_returns_ready (CI finite + bounded) |
| V3 §8.4 #2 CUSUM | CUSUM_SIGMA_THRESHOLD = 3.0 | `regime_controller.py` | test_cusum_no_break_on_iid_gaussian / test_cusum_detects_break_on_drift_shift |
| V3 §8.4 #3 Kupiec | KUPIEC_MIN_N = 250；reject if p<0.05 | `regime_controller.py` | test_kupiec_n_below_250_skipped / test_kupiec_reject_h0... |
| V3 §8.4 #4 PSR(0) | PSR_THRESHOLD=0.95 / 3×250 windows | `regime_controller.py` | test_psr_3windows_refit_when_all_below_threshold + test_psr_3windows_no_refit_when_one_window_above_threshold |

### CLAUDE.md §七 強制檢查
- ✅ Bilingual MODULE_NOTE 在所有 3 module（EN/中 雙塊 + 公開 API 雙語 docstring）
- ✅ Cross-platform path：grep `/home/ncyu` / `/Users/[name]/` 0 hit
- ✅ `max_retries=0` / `live_execution_allowed` / `execution_authority` / `system_mode` 不觸碰
- ✅ 0 SQL `INSERT INTO trading.*`；0 `live_*` mutate
- ✅ 文件大小：cell_calibrator 659 / _regime_math 199 / regime_controller 1062（< 1200 硬上限；> 800 警告線 — flagged ambiguity）

### 跨語言一致性（1e-4 contract）
- 純 Python math（numpy + scipy）；無 Rust sibling；測試在 1e-4 容差內可重現（pytest 用固定 seed 多次運行得相同結果）
- 22 RGM-Q2/Q3/Q4 test 全 deterministic（np.random.default_rng(seed)）

## 5. pytest 全 PASS 列表（57 case 本 task 範圍 + 0 sibling regression）

```
program_code/learning_engine/tests/test_cell_calibrator.py                      20 PASS
program_code/learning_engine/tests/test_regime_controller.py (RGM-Q1)           15 PASS (regression OK)
program_code/learning_engine/tests/test_regime_controller_q2_q3_q4.py           22 PASS
─────────────────────────────────────────────────────────────────────────────────────────
TOTAL                                                                           57 PASS
```

執行：
```bash
python3 -m pytest \
    program_code/learning_engine/tests/test_cell_calibrator.py \
    program_code/learning_engine/tests/test_regime_controller.py \
    program_code/learning_engine/tests/test_regime_controller_q2_q3_q4.py -v
```

執行時間：< 0.50s（全 unit test，0 PG / HTTP / async I/O 依賴）。

全套 learning_engine test 集（含 sibling Wave 5 5A-A/5A-B + 本 task）：**103 / 103 PASS**。

## 6. 不確定之處（向 PM push back）

1. **regime_controller.py 1062 LOC > 800 警告線**：task description 要求「extend 同 file」（修改檔案 = 同上 = `regime_controller.py`），4 個 sequential gate（warmup + CUSUM + Kupiec + PSR）合在一起到 1062 LOC。已抽 199 LOC math helper 到 `_regime_math.py` 把 controller 從 1225 拉回 1062（< 1200 硬上限）。建議 PM 派 wiring sub-task 時考慮拆 controller subclass 或新增 `regime_state_machine.py` 為複合層（current controller 留作低階 gate primitives）。**不阻塞 5B-D 接受**。

2. **PSR pm_alert_callback 介面 vs DB write 落地**：本 task 用 callable hook 不直接寫 `learning.governance_audit_log`（純 math module 不該 PG write）。後續 wiring sub-task 應派發：caller 在 `generate_handoff_verdict` 內 instantiate `RegimeController(pm_alert_callback=lambda c, p: pg.execute(...))`。
   - **既有 V035 schema 限制**：`event_type` 列舉 5 種（`review_live_candidate / lease_grant / lease_auto_revoke / bulk_re_evaluation / audit_write_failed`）— 寫 PSR refit alert 需通過 `payload` JSONB column（schema 已支援 forward-compat），`event_type` 可用 `bulk_re_evaluation` 或新增 `replay_psr_refit`（後者需新 migration）。
   - **請 PM 在 wiring sub-task 決定**：PSR refit alert event_type 命名 + 是否需新 V### migration。

3. **cell_calibrator MAX_FILL_BUFFER=5000 cap**：187 cell × 5000 ≈ 1M row 記憶體 footprint。V3 §11 P3b KPI「30d S0 累積」每 cell 平均 fill 數未量化；如真實生產數據 > 5000/cell，buffer 截斷可能丟早期 fill 影響 long-window CI。建議 PM 跟 FUP-2 attribution writer deploy 後實測再調。

4. **187 cell incremental update 跑頻率**：本 task spec「187 cells incremental update」未在 V3 §8.2 / §11 P3b 直接量化（187 推測為 5 strategy × 25 symbol × ~1.5 effective side ≈ 187）。caller 若每 hour 跑 187 cell 各自 incremental 全集，每 hour 187 × bootstrap_iter (1000) × 平均 cell n (~100) ≈ 187M numpy ops — 約 10s CPU。建議 PM 確認 caller scheduler 跑頻率（建議 24h batch 而非 hourly）。

5. **rebootstrap_threshold 預設 30 vs P3a-Q3 1000-iter cost**：每次 rebootstrap = 1000 iter Politis-Romano (~0.05s/cell)。187 cell × 30 fill threshold ⇒ 大批量更新前 187 × 0.05 = 9s。建議 caller 對非急用 cell 設較高閾值（如 100），或先按 cell `is_low_confidence` flag 排序（先處理 n>=30 cell）。

6. **CompositeCellStatusLiteral widen forward-compat**：本 commit widen 6 狀態（`warming_up / ready / break / refit_pending / reactive / kupiec_fail`）；既有 5A-B RGM-Q1 caller（如 sibling 派的 P3b-Q2 hierarchical_bayes test）若用窄 Literal type-check 可能 break。`check_warmup` + `get_cell_status` 行為不變（仍只用 Q1 mapping），但靜態 type lint 可能 widen → narrow 不接受。建議 E2 review 用 mypy 跑 sibling 模組驗 type safety。

## 7. Operator 下一步

1. **PM**：
   - 確認 1062 LOC > 800 警告線在 task spec「extend 同 file」要求下可接受；
   - 確認 PSR refit alert 寫 governance_audit_log 的 event_type 命名（用既有 `bulk_re_evaluation` / `payload` 還是新 migration 加 `replay_psr_refit`）；
   - 派 4 wiring sub-task（R20-P3b-Q1-WIRING / R20-RGM-Q2-WIRING / R20-RGM-Q3-WIRING / R20-RGM-Q4-WIRING）接 `replay_routes::generate_handoff_verdict`。

2. **E2 review**：
   - 1062 LOC > 800 警告線是否接受；`_regime_math.py` 底線前綴示意「internal」是否合套件慣例；
   - cell_calibrator MAX_FILL_BUFFER=5000 是否合 187 cell × 30d S0 累積規模（V3 §11 P3b KPI）；
   - mypy 驗 CompositeCellStatusLiteral widen 對 sibling caller forward-compat。

3. **E4 regression**：Linux trade-core 跑全套 `python3 -m pytest program_code/learning_engine/tests/` 全綠（103/103）+ sibling Wave 5 5A-A/5A-B 不退化。

4. **MIT review**：
   - CellCalibrator + RegimeController 與 ML pipeline downstream（`generate_handoff_verdict` / `shrinkage_router`）接口；
   - CompositeCellStatusLiteral widen 對既有 RGM-Q1 caller forward-compat 是否成立。

5. **PM commit message draft**：
   ```
   feat(replay): P3b-Q1 + RGM-Q2/Q3/Q4 — cell calibration + CUSUM + Kupiec + PSR (Wave 5 Batch 5B)
   ```

---

**完成序列**：
- 雙語 MODULE_NOTE / cross-platform / LOC<1200 硬上限：✅
- pytest 57/57 PASS（任務要求 16；含 15 RGM-Q1 regression）：✅
- E2/E4/MIT review-ready：✅
- E1 memory.md 追加：✅
