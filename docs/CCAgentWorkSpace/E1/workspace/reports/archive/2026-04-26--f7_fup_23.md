# E1 F7-FUP-23 — [23] orders ⊇ fills SQL exclude F4 unattributed audit row

日期：2026-04-26 後段（commit 跨 00:25 隔日）
Worktree：`worktree-e1-f7-isolated`
Branch：`e1-f7-healthchecks-isolated`

## 修改

| path | 動作 |
|---|---|
| `helper_scripts/db/passive_wait_healthcheck/checks_engine.py` | [23] SQL WHERE 加 `AND f.strategy_name NOT LIKE 'unattributed:%'` + docstring 補 cross-cut rationale 中英對照 + inline NOTE |
| `helper_scripts/db/test_f7_new_healthchecks.py` | 新增 `test_sql_excludes_f4_unattributed_audit_rows` contract test + module docstring 計數 38→39（[23] 5→6） |

## 驗證

- Mac smoke：`python3 -m unittest helper_scripts.db.test_f7_new_healthchecks -v` 39/39 OK 0.014s
- Diff scope：2 檔 +70 / -4，無誤觸他檔

## Cross-cut

F4 unattributed audit row（commit 53973ef，`strategy_name LIKE 'unattributed:%'`，
context_id=`unattrib-...`）audit-by-design 無對應 `trading.orders` row。
LEFT JOIN 計入 missing → 6+ row false-positive FAIL 覆蓋真 alarm。
WHERE 加 `NOT LIKE` 排除，audit row 本身仍由 F4 專屬通道紀錄落差。

## 報告

`.claude_reports/20260427_002549_e1_f7_fup_23.md` 6 節必備（任務摘要 / 修改清單
/ 關鍵 diff / 治理對照 / 不確定之處 / Operator 下一步）。

## 不確定

1. F4 audit row 寫到「`learning.execution_orphans`」是 task brief 邏輯推斷；E2 順帶 grep 驗證。
2. SQL contract test 用 substring `assertIn` — 未來 WHERE 重排 / 寫法改可能誤紅；
   接受 trade-off 換高可讀。

## 下一步

E2 審查（focus：cross-cut 涵蓋面 + docstring 表名是否真實 + contract test 強度）→
E4 PG runtime 跑 SQL 確認 F4 audit row 不再觸 FAIL → PM 統一 sign-off batch merge。
