# ALR P2-1 Scanner Read Adapter

P2-1 is complete and source-accepted. It adapts the existing Rust scanner
snapshot row into a hash-bound, evidence-only ALR cycle and rejects malformed,
duplicate, inconsistent, and late-cycle inputs safely. It grants no exchange,
trading, proof, serving, or promotion authority.

The next active action is not deployment: it is a fresh exact-scope
`PM -> E3 -> BB -> PM` review for P2-2. Only after that gate may the isolated
append-only `learning.alr_*` migration/repository work proceed. No migration was
created or applied, no service started, and no scanner cycle was consumed here.
