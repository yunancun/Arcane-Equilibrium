# 2026-06-24 Demo / Learning / Autonomy Audit Context

Generated by PM at 2026-06-24T00:58:34Z.

## Operator question

Operator asks PM to dispatch subagents for a comprehensive current-state audit.
Known concern: Demo has not placed orders for a long time. Unknowns:

- Is the backend learning engine continuously learning?
- Are strategy agents continuously drafting trade/order signals?
- What is the current development/runtime situation?
- Judge against the core product requirement: a long-term, sustainable,
  autonomous-learning and autonomous-evolving system that can produce profit,
  autonomously tune trading parameters, and keep risk parameters controllable.

## PM source context already loaded

- Repo root: `/Users/ncyu/Projects/TradeBot/srv`
- Current local branch after fetch: `main...origin/main`
- Current local HEAD: `c88deea7 Restore flash dip working orders into pending cap [skip ci]`
- `AGENTS.md` binds this session to PM-first dispatch under `srv/`.
- `CLAUDE.md` hard boundaries: Rust `openclaw_engine` is trading/risk/config
  authority; Python/control/learning must not directly mutate live state; AI
  output must become Decision Lease / governance-reviewed evidence before any
  trading effect.
- `README.md` stable state: Demo is Stage 1 micro-canary target; Paper is
  archive/diagnostic, not an active promotion lane.
- `TODO.md` v452 states latest mainline is Cost Gate / demo-learning evidence:
  ranked false-negative candidates exist, but global Cost Gate lowering,
  probe authority, order authority, and promotion proof remain false.

## Audit rules for all roles

- This is an audit/investigation only.
- Do not place orders, cancel orders, change leverage, change risk config,
  enable writers, install crons, restart services, run deploy, run Linux cargo,
  write PG, or mutate `trade-core`.
- Linux `trade-core` probes are read-only only: `ps`, `systemctl --user status`,
  `crontab -l`, `ls`, `cat`, `tail`, `find`, `stat`, existing healthcheck
  scripts, and `psql SELECT`.
- Separate fact, inference, and assumption.
- Return final status using `STATUS: DONE|DONE_WITH_CONCERNS|NEEDS_CONTEXT|BLOCKED`.

## Required questions to answer

1. Demo order reality:
   - Are there recent intents/orders/fills in demo/live_demo?
   - If orders are absent or stale, is the blocker Cost Gate, missing authority,
     source/runtime drift, cron/writer absence, strategy starvation, or
     execution/touchability?
2. Learning reality:
   - Are demo-learning / Cost Gate learning crons installed and fresh?
   - Are ledger, blocked outcomes, reviews, worklist, scorecard, and health
     artifacts refreshing from runtime rather than one-off source smokes?
   - Is PG accumulating decision/risk/reject evidence?
3. Strategy-agent reality:
   - Are strategy agents producing candidate signals/proposals?
   - Are those signals becoming risk verdicts / rejected evidence / intents?
   - Are any candidates approved to order authority?
4. Autonomy/product gap:
   - What capabilities are already present?
   - What is still artifact-only, operator-gated, or blocked?
   - What must be true before PM can call the system long-term sustainable,
     autonomously learning/evolving, profit-producing, and risk-controllable?

## Expected report shape

- Verdict first.
- Facts with timestamped evidence.
- Inferences clearly labeled.
- Blocking gaps ranked P0/P1/P2.
- Concrete next actions that respect current hard boundaries.
