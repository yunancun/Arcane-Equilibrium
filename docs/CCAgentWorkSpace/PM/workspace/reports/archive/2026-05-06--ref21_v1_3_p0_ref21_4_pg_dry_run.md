# REF-21 V1.3 P0-REF21-4 PG Dry-Run Closure

**Date:** 2026-05-06
**Owner:** PM
**Status:** P0-REF21-4 CLOSED; R2/R3 still BLOCKED by P0-REF21-5/6/7

## Decision

Started V1.3 execution with the earliest hard blocker: V057-V060 Linux PG
dry-run. Before running it, I found a real spec-to-migration drift: the active
V1.3/TODO contract required Guard A/B/C, but the committed V057-V060 migrations
only had Guard A. Treating that as a blocker was correct because a plain psql
success would not satisfy the governance gate.

## Landed

- Added runtime Guard B to V057-V060:
  - V057 verifies replay tier enum values plus signed approval constraints.
  - V058 verifies freeze-log constraints plus symbol-universe PK/type contract.
  - V059 verifies retention/hash constraints plus edge snapshot primary key.
  - V060 verifies route, actor, request-count, and manifest-hash constraints.
- Added runtime Guard C to V057-V060:
  - verifies PUBLIC write grants are revoked,
  - verifies each migration's hot-path indexes exist.
- Extended static migration tests so future edits cannot drop Guard B/C or the
  public-write/index checks silently.

## Linux PG Dry-Run

Host: `trade-core`

Method: streamed the updated migration SQL into `psql` under one transaction:

- pre-check target objects,
- `BEGIN`,
- apply V057, V058, V059, V060,
- verify target objects exist inside the transaction,
- `ROLLBACK`,
- verify target objects are absent after rollback.

Result:

- `pre_existing`: V057/V058a/V058b/V059/V060 = `false/false/false/false/false`
- all Guard A/B/C notices emitted for V057, V058, V059, V060
- `inside_tx`: V057/V058a/V058b/V059/V060 = `true/true/true/true/true`
- `after_rollback`: V057/V058a/V058b/V059/V060 = `false/false/false/false/false`

No production DB object was persisted by the dry-run.

## Verification

- `python3 -m pytest tests/migrations/test_v057_v060_ref21_replay_governance.py -q`
  -> 5 passed.
- Linux PG transaction dry-run -> passed with rollback proof.

## Remaining

- P0-REF21-5: implement non-stub `replay.calculate_promotion_metrics` SECURITY
  DEFINER body aligned with `dsr_gate.py`, `pbo_gate.py`, and
  `quantile_bootstrap.py`.
- P0-REF21-6: implement true `POST /api/v1/replay/full-chain/run`.
- P0-REF21-7: enforce replay dedicated Bybit public client and 50 req/s rate/IP
  isolation.
