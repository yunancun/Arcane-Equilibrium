# PM Authorization - GLOBAL_QUALIFIED_AUTONOMOUS_LEARNING_SHADOW_V1 WP1 Runtime

Date: 2026-07-10
Exact target: `5ae414521ca76e34529c97348ce4363efdd3dec6`
State: `AUTHORIZED_ONE_ATTEMPT_ISOLATED_PG_THEN_ALR_SERVICE_ONLY_SOAK`

Disposition: `NOT_EXECUTED_STALE_HEAD`. The first guard failed before
PostgreSQL startup because the shared main worktree advanced to a GUI-only
commit. No authorized action ran and R1 cannot be retried.

PM accepts the fresh E3 and BB verdicts without expansion. Execute exactly one
refined sequence from the same target:

1. fresh Unix-socket-only disposable Mac PostgreSQL, existing V030/V151-V156
   and role contract only, fixture reset per canonical harness;
2. explicit equivalent-DEFER and DB-clock heartbeat/skew probes, with exact
   rows/edges/no-authority assertions;
3. full cleanup plus separate process/listener/socket/directory residue audit;
4. target-pinned Linux fetch and ff-only merge after fresh clean/head/migration
   checks;
5. atomic exact-pin replacement and restart of only
   `openclaw-alr-shadow.service`;
6. 420-second polls and quantified before/after acceptance from E3.

Pre-restart derived cache must remain zero. On any mismatch, stop before the
next phase. On post-apply failure, stop ALR, restore/hash-check the old unit,
daemon-reload, leave ALR inactive, preserve append-only evidence, and do not
retry or reset source under this packet.

The retracted QA probe is inadmissible. Engine/API/watchdog/scanner writer,
production migrations/roles/credentials, exchange/API/MCP, order/fill/probe,
Guardian/RiskConfig, Decision Lease, global Cost Gate, serving, promotion,
latest pointer, parameter apply, proof/profit claim, and protected-evidence
deletion remain prohibited.
