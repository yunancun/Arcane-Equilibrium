# PM R2 Authorization - GLOBAL_QUALIFIED_AUTONOMOUS_LEARNING_SHADOW_V1 WP1

Date: 2026-07-10
Exact immutable target: `7d1c247947f0fb6c139f8a0583c5e6ed6ae62c70`
State: `AUTHORIZED_R2_ONE_ATTEMPT_EXACT_SHADOW_ONLY`

Disposition: `ATTEMPT_FAILED_ROLLBACK_COMPLETE`. The probe stopped before
fixture ingestion because the detached worktree had no virtualenv. The
independent residue guard found and then removed the disposable PG process and
directory. R2 cannot be retried.

PM accepts fresh E3 R2 and BB R2 without expansion. Execute the R2 request
exactly: protected-path remote comparison, detached target worktree,
Unix-socket-only disposable PG with all explicit probes and cleanup, exact
Linux merge, ALR-unit-only repin/restart, and 420-second quantified soak.

Any protected drift, temp/worktree residue, derived cache, PG/harness failure,
privilege drift, engine/API/watchdog identity change, restart loop, retention
delta, authority mismatch, starvation, hot-loop, or missing durable metric
stops progression. After post-sync failure, restore the prior unit for audit
and leave ALR inactive. No retry is authorized.

All production migration/role/credential changes, moving branch-tip merge,
engine/API/watchdog/scanner signal, exchange/order/fill/probe, Guardian,
Decision Lease, global Cost Gate, serving, promotion, latest pointer,
parameter apply, proof/profit claim, and protected-evidence deletion remain
prohibited.
