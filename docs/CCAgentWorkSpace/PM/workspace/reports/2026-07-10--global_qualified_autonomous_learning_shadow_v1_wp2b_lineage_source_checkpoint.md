# GLOBAL_QUALIFIED_AUTONOMOUS_LEARNING_SHADOW_V1 — WP2-B Lineage Source Checkpoint

Date: 2026-07-10
Owner: PM
State: `PARTIAL_SOURCE_ACCEPTED_PROPAGATION_ACTIVE`

## Accepted checkpoints

- Candidate-board Module extraction: `13d2b980cecd4b9f83de669c19220c820afe4e89`.
- Prospective event/evaluation lineage: `1afdf423104ce8303d90f9e86b0896039948a692`.
- Mac and `origin/main` matched the prospective-lineage checkpoint at acceptance.
- Last accepted Linux checkout and `openclaw-alr-shadow.service` pin remains the
  historical WP1 target `7d1c247947f0fb6c139f8a0583c5e6ed6ae62c70`.

## Source effect

The candidate board now owns identity, construction, and hashing behind a
narrow evaluator Interface without changing four golden hashes. Rust captures
`candidate_event_context_v1` only for an organic Demo/live-demo exchange Cost
Gate reject and does so before `strategy.on_rejection` can mutate parameters or
confidence. The flat context includes full build SHA, strategy config, scanner,
market, risk, accepted Demo equity, and portfolio snapshot lineage. Missing or
invalid inputs produce durable `CAPTURE_BLOCKED`; they are not backfilled.

The optional context is serialized through the existing learning ledger and is
omitted for old rows. Python now has a pure `candidate_evaluation_context_v1`
builder/validator for complete event hashes, D-1 regime lineage, all fixed
regime buckets, exact D-7..D-1 resource evidence, decimal portfolio context,
contiguous proof stages, hidden-OOS state, and a bound evaluation hash. One
shared JSON fixture pins Rust/Python canonical Unicode and float bytes.

## Gates

- Candidate-board characterization: `199 passed`; four golden hashes unchanged.
- Python dedicated/fixture contract: `66 passed`.
- Independent Python E4: `220` unique tests across contract, board/methodology,
  and learning-lane policy.
- Formal Rust E2: `16 passed`, P0/P1/P2 `0/0/0`.
- Formal Rust E4 after one format-only retry: `35 passed`, `cargo check --lib`
  PASS, all new ranges rustfmt-clean, P0/P1/P2 `0/0/0`.
- QA: `PASS_SOURCE_CHECKPOINT_TO_PM`, P0/P1/P2 `0/0/0`.

The initial E4 format-only P2 was fixed only in the three new ranges. Existing
format debt in the documented large hot-path files was not rewritten. The
pre-existing 2,093-line writer is now listed in the exception registry; its
production logic was unchanged and its only source diff was a test fixture.

## Boundaries and next dispatch

This is source acceptance, not deployment or runtime proof. No Linux, service,
PostgreSQL, Bybit, order, probe, Decision Lease, Guardian, RiskConfig, global
Cost Gate, training, serving, promotion, or retention action occurred.

WP2-B remains active. B2.2 must propagate only validated prospective context
through Python ledgers/outcomes, keep historical PG and pipeline-snapshot
materialization explicitly unqualified without fabrication, and project a
validated cold evaluation context into the existing candidate-board seam.
After that, the product still needs a restart-safe event-driven primary handoff;
cron remains reconciliation fallback only.
