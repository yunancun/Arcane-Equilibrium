# 玄衡 TODO - 活躍派發佇列

**版本** v862 | **校正日期** 2026-07-29 | **W0 intake source 基線** `83dc0ec3017e541e156e557c2e2d16f1704682a3` | **W1-W5 launch audit 基線** `815eff545980d9a51fbbb084eaf46c5ab4a67f15`
**來源／runtime 指針**：S0=`PROGRAM_ADOPTED`；S1=`S1_CLOSED`；S2 七個窄義 source seams 已 landed。2026-07-29 Linux 唯讀複核：`openclaw-learning.service` 與 `arcane-equilibrium-aiml-engine-scanner.service` 均 `not-found/inactive`，兩個 canonical AIML roots 均不存在；source sync 不等於 deploy。
**W0 驗證結論**：`SOURCE_SYNCED_WITH_REVIEW_DEBT`，不是完整 PASS。PR #160 已 exact-head merge，Mac／GitHub／Linux source 均為 `815eff545980d9a51fbbb084eaf46c5ab4a67f15`，但 merge 後 review 留下 2 個 P1＋1 個 P2：S2.0 lazy membership mutation 尚無 Adapter、S2E.4 未逐項承接全部 S2E.1 residual、CodeQL #95 缺 durable evidence pin。三項分別綁入 `S2E-LW4`／`S2E-LW5`；舊 packet 的 out-of-band `GRANT/REVOKE` 指示不得執行。
**當前唯一 AI/ML 派發**：`S2_EFFECT_EXECUTION_READINESS_ACTIVE`，九個 effect-readiness 拆分包已完成 **5/9**：`S2E.0`、`S2E.1`、`S2E.2a`、`S2E.2b-1`、`S2E.3`。唯一 ACTIVE 入口是 **`S2E.2b-2`**；後續只按 `S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5` 前進，不重派已完成包。W0 source/ledger projection 為 `admitted_production_effect_receipt_count=0/6`、九項 authority artifacts 均 false；這是 W0 scope 的 machine projection，不是全局 runtime 不存在性證明。S2 未關閉，舊 `S2-operator-action-packet-v1.md` 保持 `DRAFT_NOT_EXECUTABLE`；fresh Operator authorization 不是唯一 blocker，S1 disposable authority 不可沿用。
<details>
<summary>已關閉的 S2.4/WP4/W5 歷史投影（非 ACTIVE 派發）</summary>

**S2.4/WP4 即時校正（2026-07-28）**：W0–W4 已 merge；W5 本地 active worktree 為 `/Users/ncyu/Projects/TradeBot/.codex-worktrees/aiml-s2-wp4-w0`，branch 為 `agent/aiml-s2-wp4-w5`，已提交十個 head——`3412281ac`（§10.5 acceptance sweep + W5 wave projection + W0→W5 chain）、`b78185b00`（§9.2 dependency-refresh gate＝**唯一不存在的 §10.1 owned path**）、`b5d546e3a`（第二輪對抗收口）、`a757c006f`（五腿 exact-generation 第三輪收口）、`fcc44eca7`（W5 發射器 + §10.1.2 owned-path 修正案 + ledger 投影）、`756a59ef7`（八件 W5 receipts 落盤）——前六個**已 merge 為 GitHub main `427bd0dd7`（同 tree hash）並同步 Linux**；其上 **PR #145**（open，required CI 於 head 全綠）另有四個**已提交**的 head——`c2a7263ce`（第四輪 remediation）、`25d501366`（八件 receipts 在 round-4 head 重新發射）、`aaee7f1a2`（第五輪 remediation，**已提交**）、`456bd0c20`（八件 receipts 在 round-5 head `aaee7f1a2` 重新發射並落盤；該世代已被後續 re-emission 取代）。現行綁定：W5-RECEIPT-BINDING: source_head=f30ede36134f1464cef4bc025f808b931aebe180 carrier_commit=3df9d2f451ceb7ab66117de6e33fd882d59a9531 artifacts=8 round=9 status=COMMITTED。（round-9＝PR#153 Codex 修復後於本輪最終 head 重發；round-8（bound `cc6a8b9…`）、round-7（bound `5be4721…`）與更早各代依既定規則整組被取代。）五輪對抗審核，前四輪每一輪都在**前一輪的修正**裡找到缺陷（詳見 PROGRESS `S2-WP4-W5` row）；第四輪是發射器／修正案／ledger 的**首次** review，3 P1 全屬同一形狀——「source 正確但刪掉整棵樹仍綠的守衛」或「被宣稱而非被量測的斷言」，其中 P1-3 是第三輪自己寫進 exported ABI 的一句**可量測為假**的能力宣稱（漏數第五處 `aiml_gate_receipt_wave_w2.py`；PM 裁決改碼不改句）。acceptance headline 經 E4 在 `756a59ef7` 獨立重評為 **36 proven / 3 partial / 0 unproven**（取代同一位 E4 上一個 head 的 35/4/0；數字與本 wave 最初自評相同但**推理相反**，採用的是 E4 的推理）：#24 因鑑別性測試已 mutation-proven 而升為 PROVEN；三條 PARTIAL＝#26／#28a／#29，理由不是缺測試而是 obligation ledger 顯示它們**在 WP4 內根本無法關閉**，其中 **#29 對 S2.4 是終局 PARTIAL**（repo 內不存在 S2.5 source／fixture）。**標籤更正**：舊文把「S2.5 lifecycle-source fold」記為「#28a」，它其實是 §10.5 **#29** 的第二子句。**`w5-emit` 發射器已建並已 merge**；**bound head 一律讀 artifact 自帶的 `source_head`，不是攜帶它們的那個 commit**，現行世代見上方 marker（歷代 bound to `fcc44eca7`→`c2a7263ce`→`aaee7f1a2`→`0faa6499d`→`5be4721…`→現行，各整組**取代**而非編輯；ledger digest 現值 `sha256:90f92618…ed208e`（S2E.0 四列翻轉後），`persisted_dir` repo-relative、`remaining_owned_obligation_count=28`）。第五輪 remediation 改到 `tests/structure/test_agent_governance_s2_4_install_w5.py`（W5 owned path），PM 已按規則在承載它的 head `aaee7f1a2` 再發射一次並提交為 `456bd0c20`——髒 owned scope 上曾實測 `W5_EMIT_REFUSED / w5_wave_exit` 且零檔案寫出,committed-blob 契約正常運作；round-6（PM 工單收口）不觸任何 W5 owned path（新增 discriminator test 為非 owned path＋TODO/PROGRESS 投影），依規則不欠新發射。第五輪（在 `25d501366`）判本 wave 已收斂：1 P1 + 2 P2 全落在**帳本與測試守衛**而非閘邏輯——P1 是本行與 PROGRESS 對 receipts 的兩句假話（把 committed at 當成 bound to、把已完成的 round-4 再發射寫成尚欠），P2-1 是 §10.1.2 (b) 圍欄可被同節內的誘餌圍欄遮蔽，P2-2 是第四輪自己新增的標題唯一性守衛沒有任何測試。第六輪（PM audit 工單，在 `456bd0c20`）複現 1 P1＝TODO/PROGRESS active-state 仍描述 `c2a7263ce` 世代＋PM-owner 投影過期（舊文「PM 裁決，四項」漏 3 條 PM row 且誤把 owner=`W6` 的 `DEPENDENCY_REFRESH_REPRODUCER_NODE_IS_DECLARATIVE` 計入 PM），由 2026-07-28 commit 收口：投影校正至 round-5 世代、新增 discriminator regression `tests/structure/test_aiml_w5_receipt_binding_projection.py`（八件 artifact 唯一 `source_head` × TODO/PROGRESS `W5-RECEIPT-BINDING` 投影 × 活 ABI PM-owned 集合，三方機械等值，舊 `c2a7263ce` 投影或「round-5 未提交」敘述復活即紅）、PM-owned 六項裁決完成（0 source-closure blocker；S2.4-AMEND-1/AMEND-2 兩修正案 gate `S2.4@EFFECT_DONE`；全文見 PROGRESS「W5 PM-owned obligations adjudication」節）。round-6 收口經五路獨立複驗（E2/E4/CC/OPS/QA exact-generation @`029d2adfc`，0 P0/0 P1；攜帶 P2＝本 projection commit 帶閉的測試硬化與措辭修正）、required classified CI 於該 head 全綠、Codex P1 thread 已回覆並 resolve 後，PR #145 以 `--match-head-commit` exact-head merge 為 **`e1b14b7d5`**（merge tree 與 `029d2adfc` 逐位元組同一），三端（Mac/GitHub/Linux trade-core）已 ff-only 同步至同一 head。**PM 據此於 2026-07-28 宣告 `S2.4@SOURCE_READY` 成立**（六條退出條件全 MET，逐條證據見下方 gate 列）；宣告只授「該 head 的 source 結構」——effect 面前置＝S2.4-AMEND-1/AMEND-2（gate `S2.4@EFFECT_DONE`）。**綠色 W0→W5 鏈只證明「該 head 上的 source 結構自洽」**：evidence digest 是形狀性的（鏈在 `sha256:111…1` 上一樣導出 PASS），它永不證明測試跑過、也永不證明有人審過；任何測試數字必須引用實測 suite run。不得從 W4 PASS、W5 本地測試、CI 或 source-only receipt 推論 runtime/effect；2026-07-27 01:44 CEST Linux 唯讀觀察為 learning service `inactive`／`not-found`。
</details>

**入口**：當前 S2 獨立審核 `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-28--aiml_s2_source_and_effect_readiness_audit.md`；正式方案 `docs/execution_plan/2026-07-19--ai_ml_long_lived_repair_and_landing_plan.md`；交付協議 `docs/agents/ai-ml-landing-delivery-protocol.md`；長效進度帳本 `docs/execution_plan/ai_ml_landing/PROGRESS.md`；版本日誌 `docs/CLAUDE_CHANGELOG.md`；TODO 規範 `docs/agents/todo-maintenance.md`；v738 歸檔 `docs/archive/2026-07-04--todo_v738_pre_slim_archive.md`。

---

## AI/ML 一分鐘派發看板

本節只負責回答四件事：**現在做哪個 Sprint、包含哪些 Session、哪些可並行、何時停止**。S0 已關閉，S1=`S1_CLOSED`；S2 七個窄義 source seams 均 landed，但 effect-execution readiness 尚未完成。九個拆分包已完成 **5/9**：`S2E.0`、`S2E.1`、`S2E.2a`、`S2E.2b-1`、`S2E.3`；**現在唯一派發 `S2E.2b-2`**，其後 `S2E.2b-3 → S2E.4 → S2E.5`。不得重開 WP1-WP5 或五個已完成包，不得執行 `DRAFT_NOT_EXECUTABLE` packet，不得把 source/CI 當 runtime。28 條 obligations 必須從 live ABI 枚舉並逐條關閉或裁決。詳細 scope 以本 TODO 的 S2E 表與 `PROGRESS.md` ledger v16 為準；兩者衝突時停止，由 PM 綁定 current generation。

### 命令解析

| Operator 命令 | Agent 必須執行的範圍 | 停止點 |
|---|---|---|
| `開始並完成S2E-LW1-LW5` | 精確觸發下方 launch waves `S2E-LW1 → S2E-LW2 → S2E-LW3 → S2E-LW4 → S2E-LW5`。這不是已關閉的歷史 `WP1-WP5`，也不是每個 Session 內部的 Route／Build／Review／Fix／Verify `W1-W5`。從 current generation 最早未完成 launch wave 開始，在同一 finite task 內連續完成其餘 waves、全鏈對抗複驗、publication、terminal projection 與 source 三端同步。 | 正常終態只能是 current-head `BLOCKED_OPERATOR_ACTION_PACKET_READY`，同時 `task_issued_authority_count=0/9`、`admitted_production_effect_receipt_count=0/6`、`production_runtime_effect_performed_by_task=false`、沒有 fresh `PLATFORM_OR_EXTERNAL_ATTESTED` observation 時 `runtime_state=UNVERIFIED_NOT_OBSERVED`、`packet_execution_receipt_absent_within_task_scope=true`、`S2_CLOSED=false`；或 `OPERATOR_STOP_NOW`／有 current machine evidence 且無其他 READY source work 的 hard blocker。單一 commit、checkpoint、PR、CI、wave 或 review 完成不得停止。 |
| `開始並完成S2E` | 從 current projection 的最早未完成包繼續；本版精確入口為 `S2E.2b-2`，依序完成 `S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5`，包含 review/fix/publication 與 disposable rehearsal；不重派五個已完成包，不執行 production effect。 | 只在 S2E.5 關閉並重發 machine-checkable Operator packet、`OPERATOR_STOP_NOW`，或無可自行解除且有新證據的 hard blocker 停止。 |
| `開始並完成S<n>`（空格可有可無），例如 `開始並完成S1` | 精確觸發 `AIML-LONG-LIVED-LANDING-V2` 的完整 Sprint n；不是優先級 Pn、不是 IBKR 的 W*-Sn、也不是單一 Session。先核實並完成 hard predecessor，再連續完成目標 Sprint 的全部 Session。 | 只可在目標 Sprint exit、`OPERATOR_STOP_NOW` 或不可自行解除的外部／authority hard blocker 停止；不得因單一 commit、測試、報告或 Session 完成而提前退出。 |
| `推進 S<n>`，例如 `推進 S1` | `S1` 代表完整 Sprint 1，不是 S1.1。先完成尚未關閉的 hard predecessor，再依本看板執行 S1 全部 Session。 | S1 exit 或不可自行解除的 hard blocker；不得自行進 S2。 |
| `推進 S<exact-id>`，例如 `推進 S1.3`、`推進 S2.2A` | 只處理該 exact Session；suffix 不得截斷或猜測。 | 該 Session closure；若 dependency 未滿足，只回報 exact blocker，不代做其他 Session。 |
| `繼續推進` | 從當前 Sprint 最早未關閉 Session 繼續，完成當前 Sprint。 | 當前 Sprint exit 或 hard blocker。 |
| `持續推進至完成 AI/ML` | 依 S0 → S8 順序逐 Sprint 推進。 | trading/no-candidate terminal、`OPERATOR_STOP_NOW` 或外部 hard blocker。 |

這是一個**有界派發契約，不是無限 loop**。完成單一 Session、寫出報告、測試通過或 CI 綠燈都不等於 Sprint 完成。

### `開始並完成S1` 強制展開

這個 exact phrase 的解析結果固定如下；舊 session 摘要、其他 TODO row 或同名子階段不得覆寫它。

| 欄位 | 強制值 |
|---|---|
| Program／目標 | `AIML-LONG-LIVED-LANDING-V2`／完整關閉 Sprint `S1` |
| 優先級範圍 | 只推進 `P0-AIML-LONG-LIVED-RUNTIME-REPAIR` 的 S1 部分；S1 完成不代表整個 P0 關閉。 |
| 明確排除 | `P1-AIML-END-TO-END-LANDING`、全部 P2/P3、其他全局 P0/P1、IBKR、交易候選、Demo/order/profit lane，除非它們是 S1 exact effect receipt 的只讀證據來源。 |
| 必讀上下文順序 | `AGENTS.md` → `.codex/agents/PM.md` → `docs/agents/context-loading.md` → 本看板與 §1 AIML rows → `docs/agents/ai-ml-landing-delivery-protocol.md` §§2-6 → 正式方案 → `docs/execution_plan/ai_ml_landing/PROGRESS.md` current rows／attempts／receipts。 |
| Intake | PM 先核對 current head、branch、dirty scope、S0 receipts 與 active attempt；以 `agent_governance.py route/context` 編譯每個 Session 的實際 DAG。舊報告只能作線索，不能代替 current source/runtime/effect evidence。 |
| Hard predecessor | 已滿足：`S0.1`/`S0.2`/`S0.3` 全部關閉；Linux trusted-host receipt `sha256:1a124bcaebb741a69c97e37a828e5b85c9b6499cdf053e8ef62451448878f93b` 已簽發 `PROGRAM_ADOPTED`，producer-signed finalization attestation 與 execution bundle 可由 repo trust root 獨立重驗。 |
| Exact Sessions | 必須完成 `S1.1`、`S1.2`、`S1.3`、`S1.4`、`S1.5`、`S1.6`；每個 Session 都獨立執行 W0-W9 並取得自己的 current-generation closure。 |
| 固定排程 | `S1.1 ∥ S1.2 → S1.3 ∥ S1.4 → JOIN → S1.5 → S1.6`；`∥` 只有在 W0 證明 path/effect manifest 互斥時成立，否則依序執行。 |
| 工程原則 | 依 `Observe → 最高影響缺口 → 可運行 vertical slice → source/runtime 驗證 → durable state`；不得以一次性 wrapper、cron、Codex/session 常駐或 source-only PASS 代替長效 runtime 能力。 |
| S1 exit | 同時具備 current、可驗證的 `effect_seams_ready_receipt_v1`／`EFFECT_SEAMS_READY` 與 `learning_runtime_choice_receipt_v1`，且 S1.1-S1.6 無未解 P0/P1 finding、無 ghost/duplicate attempt、必要 disposable apply/rollback/postcheck 已通過。 |
| 結束行為 | 寫入各 Session closure 與 S1 summary，列出 source/tests/CI/effect/receipts/blockers；停止在 S1，不執行 S2，只回報下一個 READY pool。 |

任何 Agent 若把 `開始並完成S1` 解讀為「處理所有 P1」、「只完成 S1.1」、「執行 IBKR W*-S1」或「產出一輪報告後停止」，均屬派發錯誤，必須在 W0 fail closed。

### 當前起點

| 項目 | 當前值 |
|---|---|
| Program | `AIML-LONG-LIVED-LANDING-V2` |
| 當前 Sprint | `S2` runtime 修復；狀態 `S2_EFFECT_EXECUTION_READINESS_ACTIVE`。七個 narrow source seams 已 landed，但 effect routing/host execution/durability/runtime attestation 尚未 ready；production effect 0/6，S2 未關閉。 |
| 已關閉 Session | `S0.1`–`S0.3` 與 `S1.1`–`S1.6` 全部關閉；S1 exact-head、Linux effects、operator SSHSIG、對抗審核、CI 與 PR #115 merge 證據齊全。 |
| 最早未關閉 source | **`S2E.2b-2` ACTIVE，且是唯一當前入口。** 九包已完成 5/9：`S2E.0`、`S2E.1`、`S2E.2a`、`S2E.2b-1`、`S2E.3`；剩餘順序固定為 `S2E.2b-2 → S2E.2b-3 → S2E.4 → S2E.5`。`S2E.2b-2` 建真 S2.5 host runner，對齊 S2E.3 雙 lock 簽名並關閉下方列出的 recovery／observer／verifier／size-adjudication 邊界。**誠實邊界**：production effect 0/6；傳遞性阻塞後九步全部不可 closure PASS（S2.0 為全鏈根）；無任何真 effect receipt，runtime 仍 inactive，九項 authority 仍全 false，S2 未關閉。 |
| 當前 gate | **`S2.4@SOURCE_READY = MET`（PM 宣告 2026-07-28，於 exact-head merge `e1b14b7d5`＋三端 sync 之後；宣告只授 source 結構，九項 authority 仍全 false；effect 前置＝S2.4-AMEND-1/2）**（六條退出條件全 MET，逐條證據）：①**MET（committed-head 實測）**——在 current committed head 直接重跑 focused/adjacent/security/integration 並通過。已 merge 的 `756a59ef7` committed clone 實測 `6148/46`（structure 3601/6 + ML 2547/40）；第四輪 remediation `c2a7263ce` 實測 `6167/46`（structure 3620/6 + ML 2547/40）；第五輪 remediation `aaee7f1a2` 實測 `6169/46`（structure 3622/6 + ML 2547/40，+2 測試、零回歸、零新 skip）；round-6 head `029d2adfc` 實測 structure `3628/6` + ML `2547/40`（6175/46，+6 discriminator 測試、零回歸、零新 skip）——PM 先量於 delta==round-6 內容的工作樹，隨後 **E2 於 committed clone @`029d2adfc`、E4 於 clean committed worktree @`029d2adfc` 各自獨立重跑得到逐字相同的尾行**（committed-head 證據已補齊，CC round-6 P2 措辭問題就此消解）；merge `e1b14b7d5` tree 與 `029d2adfc` 逐位元組同一，數字直接適用；receipt 驗證：七件 receipt 於 bound head `aaee7f1a2` checkout `validate_aiml_artifact` 全 PASS＋`artifact_self_digest` 全 MATCH（W2 的 exported-ABI 重導出為 tree-dependent——carrier commit 把 receipts 寫進樹即改變量測面，故驗證一律在 bound head 重放，此為既有契約非缺陷），derivation record 七個交叉 self_digest 全 MATCH、`obligation_ledger_digest` 等於活 ABI 重算值、count=28；`git diff --check` clean；`agent_governance.py validate`＝`{"status":"PASS","roles":20}`；`engine-scanner-split`／`application-closure`＝PASS、`reasons=[]`。②**ADJUDICATED（PM 裁決完成 2026-07-28，六項，由活 ABI 導出）**——PM-owned rows 實為 6 條（活 ABI owner counts：W6B=15／PM=6／W6=4／W5=1／S2.5A=1／S1.3=1；舊文「四項」漏 3 條 PM row，且誤把 owner=`W6` 的 `DEPENDENCY_REFRESH_REPRODUCER_NODE_IS_DECLARATIVE` 計入 PM）：<!-- W5-PM-ADJ-IDS-BEGIN -->`DEPENDENCY_REFRESH_RECEIPT_BINDING_ABSENT`、`SOURCE_IDENTITY_FRESHNESS_HAS_NO_PRODUCTION_CALL_SITE`、`EMITTED_EVIDENCE_DIGESTS_ARE_UNAUTHENTICATED`、`EXPECTED_TOPOLOGY_IS_CALLER_SUPPLIED_AND_UNSIGNED`、`EVIDENCE_CLASS_TO_SCHEMA_MAP_IS_MANY_TO_ONE`、`OWNED_PATH_PROJECTION_RULER_IS_NOT_UNIFORM`<!-- W5-PM-ADJ-IDS-END -->。裁決：**0 條 source-closure blocker**；4 條另行排程（`S2.4-AMEND-1`＝refresh 進 APPLY 的 ingress＋terminal receipt 的 §3 refresh-digest 欄位＋§9.1 profile 穿線，收 `DEPENDENCY_REFRESH_RECEIPT_BINDING_ABSENT`／`SOURCE_IDENTITY_FRESHNESS_HAS_NO_PRODUCTION_CALL_SITE`／`EVIDENCE_CLASS_TO_SCHEMA_MAP_IS_MANY_TO_ONE`；`S2.4-AMEND-2`＝plan-derived `expected_topology`，收 `EXPECTED_TOPOLOGY_IS_CALLER_SUPPLIED_AND_UNSIGNED`；兩修正案 gate `S2.4@EFFECT_DONE`、入 operator packet 前置清單，不 gate `SOURCE_READY`）；2 條 accepted carry-forward（`EMITTED_EVIDENCE_DIGESTS_ARE_UNAUTHENTICATED`＝source-lane 結構性誠實邊界，關閉歸 W6 trusted-host lane；`OWNED_PATH_PROJECTION_RULER_IS_NOT_UNIFORM`＝ruler 統一排 W6 intake）。逐條理由＝PROGRESS「W5 PM-owned obligations adjudication」節；投影由 `tests/structure/test_aiml_w5_receipt_binding_projection.py` 與活 ABI set-equal 釘住。原列第五項 §10.1.2 owned-path 修正案已於 2026-07-27 置入並上測試（見下），不再是 blocker。③**投影已完成、規則持續生效**——義務一律從 current `_W5_EXPORTED_ABI["remaining_owned_obligations"]` 枚舉（現為 **28 條**）：PROGRESS.md 新增「S2.4 (WP4) unclosed owned obligations」整表由 ABI 生成並有測試在 id 不一致時變紅，`w5-emit` 的 derivation record 亦逐列序列化同一份清單；禁止沿用固定 12/15/13 項清單。④**MET（round-6 五路複驗 @`029d2adfc`）**——第四輪（兩名 reviewer 在已 merge 的 `756a59ef7`）回報 3 P1 + 6 P2，remediation＝`c2a7263ce`；第五輪（在 `25d501366`）回報 1 P1 + 2 P2 並判本 wave 已收斂，remediation＝`aaee7f1a2`；第六輪 PM audit（在 `456bd0c20`）複現 1 P1＝active-state 投影過期，remediation＝`029d2adfc`，隨後 **E2／E4-verifier／CC／OPS／QA 五路獨立 exact-generation 複驗於 `029d2adfc` 全 PASS、0 P0/0 P1**（E2 突變矩陣 18/18 該紅全紅＋committed-clone 全 suite 複測；E4 committed-head 全 suite＋3/3 突變紅＋bound-head receipt 重放；CC 憲章面零權限擴張；OPS 投影/CLI 全複現；QA 兩份工單逐點 SATISFIED）；攜帶 P2（E2×2 測試硬化、CC×1 措辭）已由本 projection commit 帶閉。§11.2 predicate 4 MET。⑤**MET**——required classified CI 於 PR #145 head `029d2adfc` 全綠（GitHub checks；heavy jobs 由 classifier 正確 skip），Codex P1 thread 已回覆並 resolve，merge 以 `--match-head-commit 029d2adfc` 綁定執行＝`e1b14b7d5`。⑥**MET**——`w5-emit` 發射器已建並已 merge；宣告時世代＝round-5（bound to `aaee7f1a2`，carrier `456bd0c20`）；其後依既定規則歷經 round-6（WP5 tranche 1 帳本收口，bound to `0faa6499d`）、round-7（tranche 2 中央註冊四檔）、round-8（S2E.0 帳本四列翻轉）與 round-9（PR#153 Codex 修復後最終 head，現行綁定見 marker）再發射——bound head 一律讀 artifact 欄位，不是攜帶它們的 commit；ledger digest 現值 `sha256:90f92618…ed208e`（歷代 `sha256:4548d526…e5af`→`sha256:fe3558a3…ca97`→`sha256:57696d69…53ea`→現值，各整組取代），`persisted_dir` repo-relative，`remaining_owned_obligation_count=28`。`756a59ef7` 為止的 PR exact-head merge 與三端 sync 已完成（GitHub main `427bd0dd7`，同 tree hash）；`c2a7263ce`／`25d501366`／`aaee7f1a2`／`456bd0c20`＋round-6 commit 由 PR #145 一併 merge。round-6 不觸 W5 owned path，依規則不欠新發射。即使六條全成立也只代表 source，九項 authority、runtime、PG effect、service start 仍全 false／未授予。 |
| §10.1.2 修正案（已收口 2026-07-27） | **前兩輪 W5 拒絕置入，兩次都是對的，兩個錯都在 PM 草案側；第三輪草案的每一條具名事實逐條複驗為真後已置入。** 拒絕一：(c) 段稱三葉「imports none of `os`, `socket`, `subprocess`, `psycopg2`」，但 `agent_governance_s2_4_credential.py:35` 確有 `import subprocess as _subprocess`。拒絕二：改稿稱「all four S2.4 effect adapters」，但沒有這樣一組四——Registry 的 18 個 effect adapter 中恰 4 個是 `AUTHORITY_LOCKED_PRODUCTION_CAPABLE`，其中 `pg_observer_bootstrap_adapter_v1` 屬 S2.0，S2.4 只有三個；v2 matrix 則是七條 per-row adapter 身分。定稿把兩個真數字都寫進去並具名排除 S2.0 那一個。置入版本 = `docs/execution_plan/ai_ml_landing/design/S2.4-install-source-seams.md` §10.1.2（1225-1338 行，深度同 §10.1.1）。本輪逐條複驗結果：§10.1 逐字 60 條（0 重複）、W3∪W4∪W5 投影減去該 60 條 = **恰好 45 條且與 (b) 段逐條全等**（含 `agent_governance_s2_4_w5_emit.py`、`test_agent_governance_s2_4_install_w5_emit.py`、`aiml_gate_receipt_w5_obligations.py` 三條）、三葉零網路 client 且零 `run`/`Popen`/`check_output`/`check_call`/`os.system`/`os.popen`、`_credential.py:35` import + 唯一用途在 `:575` 建 `CalledProcessError` 實例餵紅字化探針、`_probe.py` `driver is None`（`:954`）確為 `EXTERNAL_VERIFICATION_PENDING` 且零變更、`observe_pg_topology` 根本沒有 driver 參數、`CREDENTIAL_INSTALL` typed pending 的 `driver is None` guard 在 `agent_governance_s2_4_apply.py:739`（return 在 `:740`）且該葉只經 `apply_s2_4_credential_install` 這一列進入。(d) 條的執行面已落地：`test_agent_governance_s2_4_install_w5.py` 三支測試**同時解析**設計文件的 §10.1 與 §10.1.2 兩個圍欄（不留硬編副本），斷言硬超集／窄反向／排序無重複；窄反向而非全等的理由寫在測試註解裡（刪檔改名是 §10.1.1 條件 1 允許的重構，全等會逼 worker 去編設計文件，而 §10.4 恰好禁止）。 |
| 工程並行上限 | 最多 2 個 path/effect manifest 互斥的 writer Session；不互斥則串行。 |
| 最終同步 | 本 TODO 校正完成後依 `.codex/SYNC.md` 做一次 **source-only** Mac／GitHub／Linux ff-only exact-head sync；先執行 canonical `/Users/ncyu/Projects/TradeBot/srv` main clean guard，保留全部 unrelated `memory/` dirty/untracked 檔，禁止 stash/reset/clean/absorb。guard 失敗即回傳 `STOP_MAC_MAIN_SYNC` 且不得宣稱三端同步完成。不得把未 merge 的 W5 tree 複製到 Linux；W5 合併時須再做其 own exact-head sync。任何 sync 都不等於 deploy/rebuild/restart/runtime attestation；整個 AIML program 的 terminal final sync 仍只在 S8.4 執行。 |

### `S2E-LW1-LW5` 有界 launch map

這五個 ID 是本次剩餘 S2E source 工作的**唯一 launch-wave 別名**，不得縮寫成
`W1-W5`。固定依賴為 `LW1 → LW2 → LW3 → LW4 → LW5`；shared kernel／route／closure
writer 全部串行，只有凍結 exact generation 後的 read-only reviewer 可並行。

**2026-07-30 校準 checkpoint（不是 LW1 exit）**：launch-gate source 已補上
purpose-specific、candidate/source/wave/review/expected-entry 綁定的 trusted-signer
consumption bootstrap authority，使 state＋anchor＋lock 全刪／全改名後，無 authority
或 cross-candidate authority 均 fail closed，而 exact signed entry 可 crash-retry；
governed pytest provider 改由 reviewed Git head 的 exact lock＋8 支 hash-pinned wheels
離線建立，不再信任 ambient user-site，closure schema 強制記錄 provider，source manifest
遞迴納入 repo-local Python dependencies 與 pytest 會執行的 tracked package-parent
`__init__.py`；210-test W0 在 300 秒留下 2 項未完成，故預設 bounded timeout
依 exact-head machine evidence 校正為 600 秒。
這只關閉 launch tooling 的 `PA-S2E-LW1-P2-006`、`F-LW1-04`、
`F-LW1-04-schema` 與 `LAUNCH-04` review debt；**尚未發行 W0 genesis 或 LW1 wave
receipt，尚未取得 `S2E_2B_2A_SECURITY_RECOVERY_READY`，不得解鎖 LW2**。
`S2E.2b-2` 保持 ACTIVE，production effect=0，九項 authority 仍全 false。

`S2E-LW1` 必須先建立並審核共用 closed `s2e_launch_genesis_receipt_v1`／
`s2e_launch_wave_receipt_v1` schema、generator、validator，再由該 tooling 對 W0 exact
head 發行 genesis；只有 genesis 可用 `predecessor=null`，LW1-LW5 各 receipt 必須精確
引用前一 receipt digest。genesis 必須分別綁 W0 `baseline_head/tree` 與 tooling
`schema_carrier_head/tree`，並證明 schema carrier 是 baseline descendant；不得把兩者混成
同一 self-referential head。每個 wave 都只能由 code-owned validator 在 current task-branch
source head 重算 acceptance predicates 後發行 `TASK_BRANCH_CHECKPOINT_READY` receipt。
所有 source predicates／evidence 必須從指定 commit 的 Git blobs 重放，owned paths clean，
並記錄 bound head/tree。receipt payload 不得自引用自己的 carrier commit；payload digest
只能由 post-commit external `receipt_carrier_attestation_v1` 或下一代 receipt 綁定 carrier
head/tree 及 ancestry。carrier attestation 必須綁 schema/tooling head、payload digest、
carrier head/tree、signer/role、trusted time/freshness、immutable readback 與 signature，
transition gate 重新驗證且拒 caller-supplied attestation；禁止用 dirty worktree bytes 冒充
所綁 head。

receipt 的 test／mutation／review evidence 只接受 governed capture/workflow receipt：
provider schema、exact command/argv、exit code、stdout/stderr digest、bound source head、
actor/role、trusted time window、signature 與 immutable readback 全部可重驗；手填、複製或
僅形狀正確的 digest 一律拒絕。receipt 必須記錄 `side_effect_class=SOURCE_ONLY` 或
`DISPOSABLE_TEST`、`production_effect_count=0`；每個 disposable effect 另綁 typed
intent/result/postcheck/rollback receipt，不得用 `effect=NONE` 抹掉真實 test effect。
manual TODO/status flip、caller-supplied predicate、stale receipt、缺 predecessor 不得解鎖
下一 wave。修復 finding 後須在新 head 重發受影響 receipt。task-branch receipt 只容許同一
task 內 Advance，**不等於** canonical `SOURCE_LANDED`。LW5 candidate freeze 前必須在同一
reviewed candidate head 重算 LW1-LW5 全部 acceptance predicates，發行 consolidated
cross-wave receipt；只驗舊 receipt digest、未重放 live predicate 不得進 publication。

| Launch wave | 對應 package／狀態 | 必做工程與機械 exit |
|---|---|---|
| `S2E-LW1` | `S2E.2b-2A`／**ACTIVE**；非 package 終態 | 修 private-penetration guard：逐檔正向 import/capability allowlist 同時覆蓋 `Import`、`ImportFrom`、alias、star import 與 dynamic execution surface，`runpy`／`imp`／`code`／`pdb`／`timeit`／`concurrent.futures` 不得靠除名逃逸。建立 closed `s2_5_recovery_intent_v1` 家族，綁 unresolved state、state-root identity、**由不同 owner/capability 控制且位於可替換 state root 之外**的 trusted anchor、exact journal set/head、replay-ledger head、generation/previous-root、pre-state、允許動作、consume-once signed TTL authorization、rollback 與獨立 postcheck；anchor 必須使用 append-only/WORM，或使用同時綁獨立 key identity、外部 monotonic counter/append-only head、trusted freshness window、latest-generation immutable readback 的 trusted-host SSHSIG，單一簽章不能防 rollback；同一 writer 可 coherent rewrite 或舊合法 anchor 可重放時只能得 `UNVERIFIED`。manifest 在雙 lock 下按 write→fsync→atomic replace→directory fsync 更新，missing/extra/unlink/rename/duplicate/fork/corrupt manifest、整 root replacement、stale/cross-root replay、自由文字 `resolve()`、手工刪檔或重啟清閂全部 fail closed。每步 actor/verifier 身分必由 kernel/admission/capture 導出，不可由 caller 提供，並交叉綁 step、head、host、node、permission、process/uid/cgroup、receipt/postcheck；改名冒充、同一受限身分、caller nonce、跨 step replay 不得取得 `COMPLETED_EXACT`。`aiml_gate_receipt_validator.py` 2004 行只可實際拆分，或在 `docs/references/2000_line_exception_registry.md` 正式登記 path／owner／理由／拆分 trigger／review evidence 並由結構測試執法。exit=`S2E_2B_2A_SECURITY_RECOVERY_READY`，且須由 current-head `s2e_launch_wave_receipt_v1` transition gate 確認；**不得**翻轉 `S2E.2b-2`。 |
| `S2E-LW2` | `S2E.2b-2B`／WAITING_LW1；唯一可關閉 `S2E.2b-2` | 消費 LW1 exact interface digest，交付 production host runner 及**獨立、code-owned、不可由 caller 選 unit/path 的 disposable systemd target profile**；manager/root/unit/state-root identity 全固定，`production`／`unknown` target 在 driver 構造前 fail closed。接真 `TypedSystemdDriver` injection、unit／pid-cgroup／mount／network／PG identity 五維獨立 observer、S2.4 install-lock probe＋S2.5 lifecycle-lock hold、closed kernel session、CLI 與 authorized startup recovery。於 disposable target 真跑 start→observe→fault→recovery→rollback，逐 fault window 零 residue、可重放；每項 effect 都必須是 `DISPOSABLE_TEST` typed intent/result/postcheck/rollback chain，缺真 disposable target 不得以 fake runner/source simulation 關包。task-branch exit=`S2E_2B_2B_HOST_RUNNER_CHECKPOINT_READY`；只有 final reviewed head exact-merge 且 publication receipt 驗證後才投影 `S2E.2b-2=SOURCE_LANDED`。 |
| `S2E-LW3` | `S2E.2b-3`／WAITING_LW2 | 串行交付 `HOST_IDENTITY_INSTALL`、`PG_ROLE_ACL_MIGRATION`、`CREDENTIAL_INSTALL`、`LEARNING_RUNTIME`、`ENGINE_SCANNER` 五支 host row driver；逐項以最窄 typed capability 修正 `daemon-reload`、`systemd-creds` FD/binary stdout、`useradd` variable argv、T2 `--user`＋env 等 kernel hard boundary，每項先 fail-first mutation，再由獨立 CC/E3 裁決，不得一次放寬全域 argv/env。`AggregateInstallDriver` 必須按 code-owned fixed order 執行五 row，對每個 fault window逆序補償並由獨立 T1/T2 重觀測；所有 host mutation 都綁 `DISPOSABLE_TEST` typed effect chain，在 LW5 packet-ready 前 **T2 不得 SKIP**。task-branch exit=`S2E_2B_3_ROW_DRIVERS_CHECKPOINT_READY`。 |
| `S2E-LW4` | `S2E.4`／WAITING_LW3 | 建 approved S2.2B remote-readonly observer、**獨立 S2 runtime-capture evidence kind**、domain-separated signer/finalizer、typed closure、trusted-host runtime-attestor SSHSIG producer/verifier 與 CLI/route/closure；不得放寬既有 test-only `command_capture_v2` 冒充 runtime。證據 DAG 必須無環：raw capture payload → capture digest/signature → postcheck/receipt → final bundle signature；底層 capture 覆蓋 exact argv/stdout/stderr/result、host/head/step、actor/verifier identity 與時間窗，但**不得**反向引用上層 receipt/postcheck digest，最終 bundle 才交叉綁全部 digest，並 exact-consume、拒缺／多／重複項。必須逐項關閉或以 machine evidence 拒絕 W0/PR#160 與 S2E.1 的全部 residual：S2.4 probe/prepare 中央語義 branches；gap-list 從 live gate 導出；runtime capture/signed-bundle kind 可表示；`verifier_capture_digest` 納入 final bundle signature；trusted `evaluation_timestamp` 單調性；S2.5 postcheck verifier↔receipt verifier；S2.4 四步與 S2.2B 的 applier≠verifier source proof；NEW-P2-F 機械等式；CC D-1..D-5 與 E2 RES-1..RES-5 不得靜默 carry。S2.0/S2.1 必須新增**只在 platform-attested evidence 成立時**可表示的 production success path，使從 live `S2_EFFECT_STEPS` 導出的九步皆存在非空但 authority-locked 的 closure path。lazy membership 必須納入新或重審後的 typed Adapter：固定 `NOSUPERUSER` login、`GRANT` 嚴格位於 adapter steps 7/8、對稱 `REVOKE`＋獨立 postcheck，caller 不得任選 `connect_as`；舊 packet 的帶外 mutation 指示作廢。permit 與 attestor 至少須使用不同 public-key identity／namespace，source 不得僅靠宣告聲稱 custody 已分離；production closure 另強制 `key_custody_attestation_v1`，只接受 platform-attested provisioning/ACL/HSM identity，未有該證據時 custody=`UNVERIFIED_PENDING_OPERATOR` 且不得解鎖 effect。attestor 主動採集 observation，拒 caller-authored observation。S2.2B SSHSIG 必綁 attestor identity/namespace/key fingerprint/trusted time/expiry、head/host、S2.5B final digest、V151-V160 exact bytes/digests、learning-runtime digest 與 verifier capture/identity。2231 行 effect-binding test 遵同一 split/registry＋結構測試規則。task-branch exit=`S2E_4_RUNTIME_CLOSURE_CHECKPOINT_READY`。 |
| `S2E-LW5` | `S2E.5`／WAITING_LW4 | 從 live `S2_EFFECT_STEPS` 導出並在 non-production disposable target 排練 **6 個業務階段／9 個 routed effect steps**，不可只測六個 headline；rehearsal 必須與 production live registry 在 `(step_id, adapter_id, executable entrypoint, intent/result/rollback schema, capability projection)` 集合完全相等，只允許 target profile 由 production 換成 disposable，平行 mock harness 不算。正向路徑、逐 step fault/rollback/postcheck、caller/self-claimed capture/nonce/attestor/schema/identity substitution 全部有 mutation proof；每步綁 `DISPOSABLE_TEST` typed effect chain。另發 `DISPOSABLE_DAG_REHEARSAL_PASS` typed receipt，必須 `production_effect=false` 且永不換算任何 `EFFECT_DONE`。obligations 的 ID／數量由 current live ABI 動態導出（W0 基線為 28，禁止硬編）；每項有固定 `obligation_class` 與 `closure_policy` enum，`SECURITY/RUNTIME/ROLLBACK/ATTESTOR/DURABILITY` 永遠 `PACKET_BLOCKING`，不得重分類或文字豁免；其餘逐條 CLOSED 或具 machine evidence 的 PM adjudication。建立 closed `s2_operator_action_packet_v1` JSON schema＋generator＋validator，以 live registry 導出九步順序、source head、授權形制、rollback/postcheck 與前置；Markdown 只能由 JSON render，不是 authority。generator 必須機械斷言 `task_issued_authority_count=0/9`、`admitted_production_effect_receipt_count=0/6`、`production_runtime_effect_performed_by_task=false`、沒有 fresh `PLATFORM_OR_EXTERNAL_ATTESTED` observation 時 `runtime_state=UNVERIFIED_NOT_OBSERVED`、`packet_execution_receipt_absent_within_task_scope=true`，且禁止輸出 `S2_CLOSED`／`EFFECT_DONE`／runtime-ready／deploy-ready；只有 authenticated S2 signed bundle 才能產生 `PLATFORM_OR_EXTERNAL_ATTESTED`。CodeQL pin：alert `95`、rule `py/clear-text-storage-sensitive-data`、high、`helper_scripts/maintenance_scripts/agent_governance_terminal_receipt_sink.py:510`、W0 head `815eff545980d9a51fbbb084eaf46c5ab4a67f15` 時為 open、analysis key=`dynamic/github-code-scanning/codeql:analyze`、W0 CodeQL CLI=`2.26.1`；evidence package 必綁 alert/rule/ref/full 40-char commit/location、CodeQL bundle/CLI/query-pack versions、analysis/workflow run IDs、source→guard→sink trace、negative regression 與 GitHub API response digest。final head 必須真修復並由 exact-head CodeQL 證明；只有 machine-proven `false_positive` 可在 fresh explicit authority 下，附同一證據與 exact dismissal reason/comment 取得 GitHub dismissal receipt，`accepted_risk`／`won't_fix` 仍為 packet blocker。先達 task-branch `S2E_LW5_IMPLEMENTATION_READY`，再 push candidate PR 取得 reviewed-head CodeQL／required-CI evidence；修復會改 head 時重跑。exact-head merge 與三端同步後，governed finalizer 發行 packet／publication receipt；只有該 receipt 綁定 `reviewed_head`、`reviewed_tree`、`merge_head`、`merge_tree`、ancestry/tree-equivalence、三端 heads 與 CodeQL evidence 後，才可進 terminal projection；尚未完成 projection 時不得宣稱整體 exit。 |

**執行／publication 契約**：這是一個 finite multi-wave task，不是 `/loop`。預設使用一個
non-main linked worktree、一個 feature branch、一張 writer lease；每個 LW 可拆成多個 bounded
clean local checkpoints，但 checkpoint/role fragment/測試 PASS 不是停點。LW5 達
`S2E_LW5_IMPLEMENTATION_READY` 後 push candidate PR，凍結 `reviewed_head`，讓 classifier
決定 required CI 並取得該 head 的 CodeQL evidence；禁止手動觸發非必要 heavy CI。final
cross-wave 對抗鏈至少包含 PA integration、E2 mutation/security、E4 regression、CC/E3
authority、OPS source/runtime boundary、QA acceptance；P0/P1 必修，影響安全／closure 的
P2 亦必修，修復後只針對 finding IDs 做 delta recheck。任何修復若改 head，必須重建受影響
的 wave receipts／ABI ledger／CodeQL／test evidence，再更新 reviewed head。

final reviewed head 只能 exact-head merge；post-merge verifier 必須證明 `reviewed_head` 是
`merge_head` 的 ancestry 且 `reviewed_tree == merge_tree`，再依 `.codex/SYNC.md` 完成
Mac／GitHub／Linux source heads 等值，最後才由不寫 repository 的 governed finalizer 發行
`s2e_publication_receipt_v1`／Operator packet。允許的 durable sink adapter 只有
`EXTERNAL_WORM_V1`，或具獨立 signer、monotonic anchor 與 immutable readback 的
`TRUSTED_HOST_SSHSIG_APPEND_ONLY_V1`；本地 chmod-only sink 不合格。sink receipt 必須綁
adapter/schema version、authority ref、retention mode、object/version ID、readback digest、
key identity/signature 與 failure state。finalizer 以 `(merge_head, merge_tree, packet_digest)`
作 idempotency key；重試只可讀回同一 receipt，conflicting payload fail closed，並以測試覆蓋
sync 後／sink publish 前 crash recovery。沒有合格 sink 時停在
`BLOCKED_FINALIZATION_SINK_UNAVAILABLE`，不得偽造 terminal packet。

publication receipt 成立後，唯一可選工作改為 `POST_PUBLICATION_PROJECTION_ONLY`：
先釋放 implementation lease，以 merged main 作 fresh baseline 重新 route/admit，建立新的
projection-only branch／linked worktree／writer lease。該 PR 只能把 TODO／PROGRESS 的
S2E packages 投影為 `SOURCE_LANDED_PENDING_TERMINAL_RECEIPT`，overall status 為
`PENDING_TERMINAL_RECEIPT`，並綁 implementation `reviewed_head/merge_head`、publication
receipt digest 與 packet digest；不得預先寫 `BLOCKED_OPERATOR_ACTION_PACKET_READY`，
不得改 production code 或重跑 LW1-LW5。
projection candidate 經 classifier-required checks、exact-head merge、tree/ancestry 驗證與
Mac/GitHub/Linux source sync 後，finalizer 以同一 sink 追加
`s2e_terminal_projection_receipt_v1` 與 terminal packet wrapper；wrapper 必須機械綁定
publication receipt digest、projection reviewed/merge head/tree ancestry-equivalence、
Mac/GitHub/Linux final heads，以及 `terminal_projection_head/tree → implementation
executable-manifest digest → implementation merge head/tree → Operator packet digest`；
不得把 pre-projection packet 直接冒充 current-head packet。sink 不可達、receipt fork、
同一 idempotency key 多份 payload、任一 final head 不等值都 fail closed。selector 必須先驗
sink receipts 再讀 TODO：valid publication receipt＋缺 projection receipt 時只派 projection；
valid projection receipt＋wrapper 時才組合出 `BLOCKED_OPERATOR_ACTION_PACKET_READY` 並
不得再派 S2E。須有 regression 機械證明 crash-window `PENDING_TERMINAL_RECEIPT` 與 terminal
狀態均不會回選 LW1。正常 exit 只在 terminal projection receipt 與 wrapper 均成立後。

若 guard 或可審查性要求分 PR，PM 必須記錄 machine reason；每個 split boundary 皆須依序
完成 post-push SHA、current-head review、classifier required CI、exact-head merge、
Mac/Linux ff-only sync、舊 lease release，再以 merged head 作 fresh baseline 重新
route/admit、建新 worktree/branch/lease。中間 boundary 必須由獨立
`s2e_split_publication_receipt_v1` 綁 predecessor wave/carrier receipt、reviewed/merge
head-tree equivalence、三端 sync、`production_effect_count=0` 與 next-wave ID；不得誤用只在
LW5 才產生的 terminal publication receipt。code-owned continuation gate 驗證該 receipt 後
回傳 `ADVANCE`，才可進下一 wave。任一 clean-main／Linux guard 失敗即依 `.codex/SYNC.md`
停止，不得跳過同步、沿用 stale lease 或宣稱下一 wave 已正式 landed。

### S2E 當前 ACTIVE 派發

此表是 S2 的唯一可選 queue。`SOURCE_READY` 的 WP1-WP5 與
`DRAFT_NOT_EXECUTABLE` Operator packet 均不得被 selector 當成下一工作。

| ID | Lane／狀態 | 依賴 | 精確工作 | 驗收／下一步 |
|---|---|---|---|---|
| `S2E.0` | **SOURCE_LANDED 2026-07-28** | WP1-WP5 narrow source seams landed | S2.4-AMEND-1（§9.2 refresh ingress 進 APPLY＋terminal receipt `dependency_refresh_digests`＋§9.1 profile discriminators）與 S2.4-AMEND-2（plan-derived `expected_topology`＋五基線鍵 fail-closed＋cluster/HBA 交叉綁定＋delta 窗＋precheck hoist）已落地：最終 head `f30ede361`＋round-9 receipts（carrier `3df9d2f45`）。 | 已收口：forged/stale refresh、caller topology substitution、profile mismatch mutations 全拒（E2 突變 7/7 kill）；E2/E4/CC/E3 全 PASS；obligations 16/23/25/26 翻轉。無當前派發。 |
| `S2E.1` | **SOURCE_LANDED 2026-07-29** | S2E.0（已收口） | 為 canonical §1.2 effect DAG `S2.0→S2.4→S2.5A→S2.1→S2.5B→S2.2B` 建九條 per-phase claim-gated EFFECT route（selector `s2_effect_adapter_selection_v1`＋per-step exact claim inventory＋class 互鎖＋強制 `authority` 面使 constitutional_gate 恆入 DAG）、S2.2B identity-only registry adapter、closure binding 模組委派四道既有 per-step 硬門。head `d6f7b585d`，全量 6490/46/0。 | 已收口；傳遞性阻塞後九步全部不可 closure PASS（S2.0 為全鏈根）。遺留責任不再泛稱 carry-forward：runtime `command_capture_v2`／signed-bundle kind／`scope=runtime` typed closure schema、apply actor↔verifier identity cross-binding、trusted-attestor SSHSIG producer/verifier 歸 `S2E.4` 實作；`S2E.5` 必須以全鏈 rehearsal 證明 distinct applier/verifier 並拒絕 caller/self-claimed capture、nonce、attestor 或 schema substitution。 |
| `S2E.2a` | **SOURCE_LANDED 2026-07-29** | S2E.1（已收口） | 已建 trusted-host kernel／read-only observer 與真實 S2.0/S2.1 runners；S2.0/S2.1 在 throwaway PostgreSQL 完成 apply→independent postcheck→compensate→byte-for-byte restoration。S2.4 只達 source simulation，S2.5 無真 systemd disposable target，兩者未在此包冒充完成。 | 已收口（PR #156 merge `5486a6183`）；pure-addition、四個 adapter modules byte-identical。production 無 authority，零 production effect。 |
| `S2E.3` | **SOURCE_LANDED 2026-07-29** | S2E.1（已收口） | 修 S2.5 F2/F3/F4/note-1：reconcile 納入 lifecycle hold、install-lock probe 與 lifecycle-lock hold 分離、release failure typed、journal self-digest/hash-chain。 | 已收口（PR #155 merge `94a9f514b`）；競態、crash、release failure、tail truncation/history rewrite mutations 全被殺。無當前派發。 |
| `S2E.2b-1` | **SOURCE_LANDED 2026-07-29** | S2E.2a ＋ S2E.3（皆已收口） | 已落地 S2.4 recovery core：POSIX file/lock drivers、受 consumed-plan ledger 約束的 startup 逆序補償器、journal-routed driver injection；**零新增 host capability**，五支 row driver 與 T2 kernel amendments 留在 `S2E.2b-3`，S2.5 runner 留在 `S2E.2b-2`。 | 已收口（PR #158 merge `521c813c5`）；15 個 fault windows 只落 `COMPLETED_EXACT`／`RECOVERY_REQUIRED`。CLI 的兩個 S2.4 mode 仍無 aggregate host driver，未宣稱 disposable full rehearsal。 |
| `S2E.2b-2` | **ACTIVE / P0 — 唯一當前入口** | S2E.2b-1（已收口） | 精確拆成 `S2E-LW1` security/recovery 非終態與 `S2E-LW2` host-runner task-branch checkpoint；完整 scope／exit 以上方 launch map 為準。LW1 不得冒充 package closure，LW2 只能解鎖同 task 的 LW3；只有 final reviewed head exact-merge＋publication receipt 才可正式翻轉。 | `S2E_2B_2A_SECURITY_RECOVERY_READY` 只解鎖 LW2；`S2E_2B_2B_HOST_RUNNER_CHECKPOINT_READY` 只解鎖 LW3；post-merge publication receipt 才投影本 package `SOURCE_LANDED`。production authority 缺失時仍 fail closed。 |
| `S2E.2b-3` | WAITING_S2E_2b_2 / P0 | S2E.2b-2 | 由 `S2E-LW3` 串行完成五支 row driver、逐項 kernel hard-boundary amendment、aggregate fixed-order integration、逐 fault-window 反序補償與 T1/T2 independent re-observation。 | 五支 driver 必須在真 disposable target 可執行；每項 kernel amendment 獨立審查；packet-ready 前 T2 **不得 SKIP**。 |
| `S2E.4` | WAITING_S2E_2b_3 / P0 | S2E.2b-3 | 由 `S2E-LW4` 關閉 W0/PR#160 與 S2E.1 全部 residual；建立 S2 專屬 runtime-capture evidence kind、domain-separated signer/finalizer 與 schema-valid closure，**不得**只把 test-only `command_capture_v2` 的 `scope` 放寬。補齊 S2.0/S2.1 platform-attested success representation、lazy membership typed Adapter、S2.2B remote observer/SSHSIG、kernel-derived actor/verifier identities、不同 attestor public-key identity，以及 effect-time `key_custody_attestation_v1` gate 與 2231-line size 裁決。 | 從 live `S2_EFFECT_STEPS` 導出的九步均有 authority-locked、platform-attested 才可達的 production closure path；未有 platform custody evidence 時誠實保持 `UNVERIFIED_PENDING_OPERATOR`；caller JSON/self-digest-only/test→runtime relabel、同 OS identity、wrong key/namespace、stale/future、跨 step/host replay、任一 V151-V160 mismatch 全拒。 |
| `S2E.5` | WAITING_S2E_4 / P0 | S2E.4 | 由 `S2E-LW5` 在 non-production disposable target rehearsal 6 個業務階段／9 個 live routed steps；obligations ID/count 從 current ABI 動態導出；生成 `DISPOSABLE_DAG_REHEARSAL_PASS` 非 production receipt，以及 closed `s2_operator_action_packet_v1` JSON schema/generator/validator＋衍生 Markdown。CodeQL #95 以 exact reviewed-head evidence package 修復或正式裁決。 | task branch 先到 `S2E_LW5_IMPLEMENTATION_READY`；零未解 source P0/P1，安全／runtime／rollback／attestor blocker 不得文字豁免。implementation exact-head merge/sync、durable publication receipt、projection-only PR merge/sync 與 terminal projection receipt 全成立後，才可轉 `BLOCKED_OPERATOR_ACTION_PACKET_READY`。 |

**停止規則**：完成單一 commit/PR/test/wave 不得停止。只在 terminal projection receipt
驗證後的 S2E.5 exit、
`OPERATOR_STOP_NOW`，或同一不可自行解除 blocker 具 current machine evidence 且
不存在其他 READY source work 時停止。S2E 全程不得 production PG/deploy/restart/
credential/broker/order/live；CI 只跑 classifier 判定 required 的 workflow。

### Sprint／並行／JOIN 總表

符號：`→` 表示 hard dependency；`∥` 表示可同時 READY，但仍受「最多 2 writer 且 manifest 互斥」限制；`JOIN(...)` 表示列出的 Session 全部達到其 receipt 後才可前進。

| Sprint | Session 排程 | 可並行安排 | Sprint exit |
|---|---|---|---|
| `S0` 治理採納 | `S0.1`（已發布）→ `S0.2 → S0.3` | 無；metadata/governance 串行。 | `PROGRAM_ADOPTED` |
| `S1` effect seam／runtime 選型 | `S1.1 ∥ S1.2 → S1.3 ∥ S1.4 → JOIN → S1.5 → S1.6`（全部完成） | 已完成。 | **`S1_CLOSED`**；authenticated receipts、exact-head CI、PR #115 merge 全部成立。 |
| `S2` runtime 修復 | WP1-WP5 narrow source seams 已 landed；九個 S2E 拆分包完成 5/9，唯一入口 `S2E.2b-2`，剩餘 `S2E.2b-2→S2E.2b-3→S2E.4→S2E.5`；全部完成後才可進 production effect DAG | 已完成的 S2E.2a/S2E.3 並行窗不可重開；剩餘 source packages 串行，production apply-chain 永遠串行且 fresh-authority gated。 | S2E packet-ready 後仍須真 running/final attestation + `ingestion_compatibility_receipt_v1` 才 `S2_CLOSED` |
| `S3` controller／Scanner／retention | `S3.1A@SOURCE_READY → S3.1B`；之後 `S3.2 ∥ S3.3`；`S3.2 → S3.2A`；`JOIN(S3.2A,S3.3) → S3.4` | S3.2/S3.3 可並行；S3.2A 解鎖後補位。 | `FOUNDATION_READY`（72h／兩 cycle） |
| `S4` scope／portfolio／label／PIT | `S4.1 → S4.2 → S4.3 → S4.4` | 同一 `landing_scope_id` 串行，避免 cohort／label／PIT 漂移。 | `pit_dataset_receipt_v1` 或合法 no-candidate branch |
| `S5` fit／OOS／registry | `S5.1 → S5.2 → S5.3` | 串行；OOS 未通過不得進 registry/shadow。 | registry/shadow receipt 或合法 no-candidate branch |
| `S6` Rust consumer／Demo integration | `S6.1 → S6.2 → S6.3 → fresh authority → S6.4` | 串行；S6.4 是獨立 effect gate。 | required mechanical Demo receipt；economic receipt 可選 |
| `S7` G2／DR／soak／candidate | Trading：`S7.1 → S7.2 → S7.3 → S7.4`；No-candidate：`S7.2 → S7.3 → S7.NC` | 只執行已固定 branch；不得同時跑 trading/no-candidate terminal lane。 | `AIML_LANDING_CANDIDATE` 或 no-candidate platform candidate |
| `S8` 全局收口 | `S8.1 → S8.2 → S8.3 → S8.4`；Trading：`S8.5T → S8.6`；No-candidate：`S8.5NC → S8.NC` | S8.1-S8.4 串行；final branch 二選一。 | trading terminal 或 platform-only no-candidate terminal |

### 每個 Session 的固定 Wave

Agent 接到 Sprint 命令後，對每個 READY Session 依序執行下列 Wave；不得把多個 Session 合併成一個大 closure。

| Wave | 工作 | 完成條件 |
|---|---|---|
| `W0 Intake` | 讀本看板與 `PROGRESS.md` exact row；核對 dependency、head、dirty state、scope、effect、CI classifier；建立 attempt/owner。 | exact Session 可安全開始，或留下 typed blocker。 |
| `W1 Route` | PM 依 row 的 role route 編譯實際 agent DAG 與 context。 | owner、builder、reviewer、effect actor、postcheck actor 清楚且互不冒充。 |
| `W2 Build` | builder 只修改該 Session owned paths；effect/observation Session 不建立空 branch。 | bounded implementation/effect/observation 完成。 |
| `W3 Review` | source 先 E2、再 E4；再按事實加入 QC/MIT/AI-E、CC/E3/OPS、BB、QA。 | 無未解 P0/P1；dissent 被保留。 |
| `W4 Fix` | builder 修正 findings，受影響 reviewer 只複驗新 generation。 | current generation 被接受。 |
| `W5 Verify` | focused tests、必要 adjacent regression、receipt/schema check、`git diff --check`。 | 驗證可重現。 |
| `W6 Publish` | 只有 source Session 才 commit/PR/merge；CI 只在 classifier 判定必要時跑。 | reviewed head 與 merge head 一致，或 non-source typed N/A。 |
| `W7 Effect` | 有 effect 才做 preflight → authority → Adapter → rollback/postcheck；source PASS 不得代替 runtime PASS。 | typed effect receipt 或誠實 blocker。 |
| `W8 Close` | QA 驗收，PM 寫 Session closure／receipt。 | 該 Session terminal predicate 成立。 |
| `W9 Advance` | 更新進度投影，釋放 slot，立即選同 Sprint 下一個 READY Session。 | Sprint exit、下一 Session 已啟動，或 hard blocker 已精確落帳。 |

### 防漂移與停止規則

1. 同時最多 2 個 writer Session；只有 owned paths 與 effect manifests 都互斥才可並行。reviewer 可在同一 Session 內並行，不額外算 writer Session。
2. PM 每次只從 `PROGRESS.md` 選 dependency 已滿足的 READY row；低優先但已 READY 的工作不得越過本 Sprint 的 JOIN。
3. 一個 Session 關閉後必須自動補位，不能因「本輪有產出」提前結束 Sprint。
4. 只有三種自然停止：目標 Sprint exit；無其他 READY 工作且遇到不可自行解除的外部／authority blocker；`OPERATOR_STOP_NOW`。
5. 禁止為了續作而 stash/reset/clean、吸收不明 dirty files、降低 Cost Gate、擴大 runtime/order authority，或把 source／CI 說成 runtime／profit proof。
6. 最終回報固定列出：完成 Session、仍未完成 Session、並行 slot 使用、tests/CI/effect、blocker、下一個 exact Session。不得只回報 commit 或報告路徑。

## §0 影響派發的當前事實

| 事實 | 內容 | 派發影響 |
|---|---|---|
| AI/ML 完成覆蓋再審核（2026-07-20） | `CURRENT_V1_TODO_COMPLETION_NOT_SUFFICIENT`。V1 缺少可實作的 PG 唯讀身分 Adapter、可接受的部署 effect seam、經證明的長效 runtime 選型、實體化 PIT／label revision／parity 保證、Scanner 丟失帳、僅合格資料可訓練、真實 Rust consumer、有因果約束的 bounded Demo policy 整合、第二代學習、DR 還原與 causal-time 落地 manifest。 | 先發布 V2 規劃文件並接受 serving-authority ADR/AMD；治理 bootstrap 完成後才可簽發 `PROGRAM_ADOPTED`。僅有 source／tests／migrations／shadow 不得關閉落地。交易落地必須綁定一個 platform scope、明確 decision-cell 覆蓋與 Shadow-to-Demo promotion edges；盈利與 live activation 另行證明。 |
| 穩定 loop 來源 | `docs/agents/profit-first-autonomy-loop.md` 定義既有 profit-first 自主 loop。 | Agent 從 loop 規範讀取穩定行為，從本 TODO 讀取當前任務；不得把當前候選細節寫回穩定 loop 規範。 |
| AI/ML 當機裁決（2026-07-19 16:33-17:05 CEST） | `NOT_LANDED_RUNTIME_BROKEN`：engine／Scanner snapshot 自 2026-07-18 起過期；watchdog 為 `circuit_broken`；ALR 因 whole-head 不一致而 `NRestarts=23579` 且持續增加；controller／workers 不存在；舊 ML cron 缺 LightGBM，當時執行阻塞超過 13 小時；process argv 曾暴露 PG credential（已遮蔽）；learning lane 佔 28 GB；沒有當前 model consumer 或盈利證明。 | 不再派發 packet／validator／repin loop。從 LR0 開始執行 §1 `P0-AIML-LONG-LIVED-RUNTIME-REPAIR`；在 `FOUNDATION_READY` 前禁止聲明 ML runtime 完成。 |
| AI/ML source 現實 | 已存在 Scanner→PG snapshot/notify、ALR adapters／repositories／contracts、V151-V160，以及 proof／reward／retention／trusted-fit／verifier source。已停止分支有可挽救候選提交 `0abbc1cd3`；其 8 個未追蹤 venv prototype 曾三次未通過 E2，不得採用。 | 只能在重新審核後重用 source。保留該分支；僅在 LR1 接受精確 manifest 後 cherry-pick／重作其範圍，並棄用失敗的 relocatable-venv 設計。 |
| AI/ML migration 真相 | Source 具有 V151-V160；同日已停止的 runtime-refactor 任務曾以有界唯讀觀察報告全部已套用。本任務未修改 PG；在任何 writer／restart／deploy／migration 前，必須先完成 LR0A 唯讀身分 Adapter，再由 S2.0 建立 production observer，並透過 S2.1/S2.2B 重新證實 production shape／rows，並於 ML0 對修復後 cohort 再刷新。 | 宣稱 V159/V160 不存在的舊 row 已過期並被取代。migration 存在仍不能證明 fit、registry、serving 或 learning。 |
| 合格 ALR 歷史 source 狀態 | WP1-WP4 contracts、Scanner/PG adapters、V151-V160 與隔離 verifier 可作為 source 基礎重用。當前 source 檢查未找到 production trusted-fit runner／V160 consumer；`alr_local_runner.py` 只是單步診斷，不是 controller。 | §1 兩個 AIML row 已取代歷史 WP source row 的派發權。不得再建立 design-only packet 或延續舊 WP4 chain；應把通過審核的 contract 整合進 LR-1／LR0A-LR0C／LR0-LR6 與 ML0-ML10。 |
| Operator Demo 政策 | Operator 已授予廣義 Demo 開發／部署／profit-loop 方向，但每個 effect 仍須具備當前、明確範圍、機器可讀的 authority 與 loss control。 | 不再重問廣義方向，也不得把它解讀為當前 order／probe／runtime effect 授權。每個精確 Demo effect window 都要重新實體化有期限的 envelope。 |
| Runtime 授權現實 | 最後一份對齊 <code>ma_crossover&#124;NEARUSDT&#124;Buy</code> 的 runtime 授權 sha `05fe07f5...` 已於 `2026-07-09T00:12:30.886090+00:00` 過期，且該候選證據其後被判定無效。 | `EXPIRED_UNCONSUMABLE_HISTORICAL`。它不提供當前 loss-control、Decision Lease、broker、order、probe 或 ML6 authority。未來 Demo 整合需要新候選、新 scope、新授權及同一時間窗 E3/BB gate。 |
| v738 refresh attempt | `bfbbd343` v6 reached READY + E3/BB approval, then final source drift check failed before any runtime action; terminal state `ROTATED`. | Do not consume v738 approval/READY packets. Restart from current fetched `origin/main` and exact reviewed scope. |
| Source/build/runtime drift | 2026-07-07 PM->E3->BB Demo-only env restoration cleared engine env to `OPENCLAW_BOUNDED_PROBE_ADAPTER_ENABLED=1` and materialized standing auth. First direct SSH helper restart briefly inherited default `/tmp/openclaw` paths, then PM corrected with engine-only restart using approved home data/socket paths; final engine PID `544751` verified. | This is no longer a standing-envelope materialization blocker. Treat the helper env inheritance gap as deploy hygiene for future restart scopes; it does not grant rebuild/API restart/order authority. |
| Cron / data-path state | D2 crontab restoration is effectively complete: 34 active commands, `OPENCLAW_DATA_DIR=/home/ncyu/BybitOpenClaw/var/openclaw`. One active `alpha_discovery_throughput_cron.sh` still pins `OPENCLAW_EXPECTED_SOURCE_HEAD=c0c49deeb`. | Cron massacre recovery is no longer a P0 active blocker. The stale pin is hygiene debt to reconcile at the next cron/deploy pass; it does not grant runtime mutation. |
| Migration 狀態 | 先前只觀察到 V150 的結果已成歷史並被取代。Source 包含 V151-V160；同日第二份唯讀觀察報告它們已套用，但 production 權威狀態仍為 `RUNTIME_UNVERIFIED_PENDING_LR0_3_REATTEST`。 | 本 row 不得對 production apply 或 double-apply migration。LR0A/S2.0-S2.2B chain 必須先透過批准的唯讀 Adapter 與 observer 綁定 checksum、migration ledger、schema/ACL shape、runtime identity 與 TTL；任何後續 migration effect 都需要獨立審核 gate。 |
| trade-core OOM 風暴 (2026-07-15 取證+量測) | kernel 16+ OOM kill;**單進程失控實測 79-85GB anon-rss(kernel victim,非 E5 估 20-25GB)**;引擎/watchdog adj=200 來源=user manager `DefaultOOMScoreAdjust=200`(16:35 task_memcg=watchdog cgroup);probe_ledger 6.2G/117 段、polymarket_axis_runs **21GB/1708 檔**(磁碟炸彈,E5 未覆蓋)。引擎「crash」皆記憶體下游,無 panic。證據報告 §四。 | flock+OOM-victim 已 source-landed(見 §1 row);**權限硬約束**:user 無特權降引擎 adj(需 root)、cron 無 DBUS 故 MemoryMax 不可用。任何 runtime-touching 先讀報告。 |
| Profitability reality | Current candidate has 34,574 strict ETH Buy ledger rows but zero candidate-matched order/fill/fee/slippage proof. Maker-first mature-perp pivot was signed NO-GO at current Bybit VIP0 fee tier. | There are candidate paths and learning rows, but no profit proof. Do not route effort into mature-perp passive spread capture unless new fee-tier/event evidence changes the premise. |
| GUI risk cap + equity gate | GUI `P1 Risk/Trade=10.0%` means Rust fraction `0.1`, not fixed `10 USDT`; `Max Single Position=25%` is percent-derived budget; `max_order_notional_usdt=0.0` remains fail-closed until resolved by reviewed cap lineage. | Local `10 USDT` is never global risk authority. Any stale artifact without GUI/Rust RiskConfig + accepted equity lineage must be refreshed before admission. |
| Demo book / loss state | Latest known cleanup showed Bybit Demo open orders=0 and nonzero positions=0; cleanup/risk-close/unattributed fills are proof-excluded. This was not revalidated in the v739 TODO hygiene pass. | Treat book-clean as stale before proof. Revalidate inside any future E3/BB-reviewed runtime/order-capable window. |
| Current candidate evidence | **R3 F1 verdict (2026-07-10)**: the NEAR candidate's statistical evidence is invalidated. <code>ma_crossover&#124;NEARUSDT&#124;Buy</code> "avg net 64.983bps / outcome_count=5058" collapses under preregistered dedup (`docs/research/2026-07-10--counterfactual_rerun_preregistration.md`) to 2 distinct `entry_ts_ms` -> n_eff=1, single UTC day; rerun rejudges it `SAMPLE_INSUFFICIENT_AFTER_DEDUP` + `EXECUTION_REALISM_SUSPECT` (counterfactual +130.5 vs realized −12.0bps). WP-A.4 counterfactual rerun (verdict artifact sha `d09bf86c...`) over the frozen 71,207-row population + 33 GROSS cells: family m=7 all VETO (`BLOCK_CONFIRMED_UNDER_EXPECTED_COST`, cluster p 0.98-1.0, BH 0 pass), 0 flipped cells, gate net-contribution −33,608 bps·n => false-kill hypothesis hammered; the Cost Gate is a net stop-loss. Old NEAR packet/proposal/order-capable shas (`02c7eec6...`/`74535a9c...`/`305774b2...`) are evidence-dead. Report: `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-10--r3_fix_package_report.md`. | **NEAR dispatch FROZEN**: do not consume any NEAR-based READY packet. Candidate board after rerun = zero eligible candidates; per prereg §3.4 do not claim "no edge proven" either — only lawful action is accumulating distinct-entry samples under the dedup lane (`49049f84d`). The WP-A.6 order-capable checklist distinct-entry `n_eff` precondition is landed in `docs/agents/profit-first-fast-demo-promotion-loop.md`; reopening still requires fresh evidence passing it plus full current lineage. |
| Current bounded-probe materialization prep | Operator authorization packet sha `0e075af5...` for <code>ma_crossover&#124;NEARUSDT&#124;Buy</code> is `BOUNDED_DEMO_PROBE_AUTHORIZED` using standing Demo authorization as confirmation source, with max orders `2` and expiry `2026-07-09T00:12:30.886090+00:00`. Plan inclusion review sha `5e08595c...` was `PLAN_INCLUSION_PREVIEW_READY_NO_ADMISSION`; inactive adapter gate was `ADAPTER_DISABLED`, hypothetical adapter-enabled decision was `ADMIT_DEMO_LEARNING_PROBE`. After E3 sha `bb03bcc9...` and BB sha `b730fdf6...` approval, PM materialized the reviewed plan preview into canonical runtime soak plan sha `a296365e...`; materialization record sha `a71b9fc4...` states `CANONICAL_SOAK_PLAN_MATERIALIZED_NO_ORDER_AUTHORITY`. | Plan materialization branch is complete. It grants no order/probe/private endpoint/Decision Lease/adapter enablement authority. **Superseded by the R3 F1 freeze (see Current candidate evidence row)**: do not proceed to E3/BB review while the NEAR candidate is frozen. |

## §1 活躍派發佇列

**當前 blocker 覆寫**：GUI cap 語義仍綁定 GUI/Rust RiskConfig；connector Demo mode ready 不等於 promotion proof；當前 standing/loss-control READY 只清除開啟下一 scope 的前置條件。核心禁令不變：不得下單、不得降低 Cost Gate、不得 promotion proof、不得從 `_latest` promotion、不得 append ledger、不得 service restart/rebuild/env mutation，也不得把已釋放 lease／no-order admission／materialized plan 重用為 runtime order authority。任何 order-capable 動作仍須在同一 invocation window 內具備獨立 PM→E3→BB packet、active Decision Lease、fresh BBO／instrument／order shape、Guardian/Rust authority，以及 auditability／reconstructability。

| ID | 優先級 | 狀態 | Owner 路徑 | 驗收條件 | 最新證據 | 下一步 |
|---|---:|---|---|---|---|---|
| `P0-AIML-LONG-LIVED-RUNTIME-REPAIR` | 0 | **S2_EFFECT_EXECUTION_READINESS_ACTIVE** | PM 綁 S2E work package；builder → E2/E4，security/effect semantics 由 E3/CC/OPS，QA/PM close | 先完成九個 S2E 拆分包，使六段 effect DAG 真正可路由、可在 disposable target 重放、可驗證與可 rollback；其後取得 fresh authority 串行完成 production effects，Linux runtime installed/active 且 S2.2B receipt valid 才關閉 S2。 | S0/S1 已關閉；WP1-WP5 的七個窄義 source seams 已 landed。S2E 已完成 5/9：`S2E.0`、`S2E.1`、`S2E.2a`、`S2E.2b-1`、`S2E.3`；production effect 0/6，S2 未關閉；Linux unit/install roots 均不存在。 | **現在唯一派 `S2E.2b-2`**；其後精確順序 `S2E.2b-3 → S2E.4 → S2E.5`。不重開任何已完成包、不等 authority、不做 production effect。 |
| `P1-AIML-END-TO-END-LANDING` | 1 | BLOCKED_BY_FOUNDATION_READY | PM 為 blocked owner；ML 語義經 QC/MIT/AI-E；builder → E2 → E4；effect 由 CC/E3/OPS；BB 只處理另行授權的 Bybit Demo | 對一個精確 `landing_scope_id` 完成 ML0-ML10；它必須綁定 `platform_scope=(venue,instrument_class,strategy_family)`、`policy_surface_id`、明確覆蓋的 `decision_cell=(symbol,side,horizon,regime)` 值與 evidence-environment promotion edges。ML1/LR4 保存完整 pre-filter universe 與誠實 propensity；ML2/ML2A 建立 bitemporal labels 與 actual-loaded-row PIT matrix；ML3/ML4 證明 qualified reproducible fit、holdout consumption accounting 與不可變 gate precedence；ML5/ML5A 使用 Rust `IntentProcessor + EdgePredictorStore` registry-authorized consumer；ML6 分別證明必要的 mechanical Demo policy integration 與可選 economic causality；ML7 完成由自然 controller 觸發的第二代評估，其中 `NO_CHANGE`／`REJECT_NO_PROMOTION` 是合法結果；ML8 證明跨 failure-domain DR 與 7 天無人值守；ML9 只可產生 `AIML_LANDING_CANDIDATE`；ML10/S8.6 經 exact-head sync 與最新完整 runtime attestation 後，才可簽發 `AIML_MODULE_LANDED_FOR_TRADING`。若沒有 decision cell 合格，S7.NC 只能產生 platform candidate，S8.NC 只能簽發 `aiml_platform_no_candidate_receipt_v1`；candidate Session 應標為 `NOT_APPLICABLE_NO_CANDIDATE` 而非 `DONE`，P1 交易 row 與 ML5-ML10 仍未完成。 | V151-V160 與 contracts/tests 可重用，但 V1 缺少實體化 PIT/bitemporal data、真實 Rust consumption、causal policy integration、第二代閉環、DR 與 terminal identity proof。post-draft review 亦發現 scope 過粗、同窗 TTL 不可能與過早簽發 landing；V2 已改為明確 cell coverage 與 causal validity graph。 | 等待 `FOUNDATION_READY`。`PROGRAM_ADOPTED` 後可先做 ML5 以前的 source-only 工作；ML5/ML6 另須綁定已接受的 serving-authority receipt。弱資料只能返回 `COLLECT_MORE`、`ROTATE`、`NO_EDGE` 或 `NO_ELIGIBLE_CANDIDATE`；這是正確運作，不能偽造 trading landing。Demo economics、live activation 與 activation 後 live profit 分開證明。 |
| `P0-STANDING-DEMO-LOSS-CONTROL-ENVELOPE-REFRESH-CURRENT-HEAD` | 0 | EXPIRED_UNCONSUMABLE_HISTORICAL | PM → E3 → BB → PM | 僅保存歷史 envelope refresh；不得提供當前 AI/ML Demo authority。 | 最後一份 candidate-aligned authorization 與相關 readiness packet 已於 2026 年 7 月過期，NEAR 候選證據亦已失效。報告：`docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-07--runtime_loss_control_authorization_ready.md`。 | 不得派發或消費此 row。未來 ML6 effect 必須從新候選、當前 head/runtime/risk state 與獨立的同窗 PM → E3 → BB authorization 開始。 |
| `P0-PROFIT-FIRST-DYNAMIC-CANDIDATE-ALIGNED-GATE-REFRESH` | 0 | FROZEN_F1_EVIDENCE_INVALIDATED | PM -> E3 -> PM -> BB -> PM | Produce machine-readable candidate-aligned no-authority standing envelope, false-negative review, bounded-probe preflight, touchability, placement, authority readiness, and operator-auth readiness artifacts for the latest dynamically selected candidate, without granting probe/order authority. | Source fix `725fddc3...` prevents stale cap-feasible selection from overriding fresh false-negative candidate selection. Dispatch precheck refreshed exact no-authority runtime `_latest` chain binding for <code>ma_crossover&#124;NEARUSDT&#124;Buy</code>: operator review `80579cec...`, bounded preflight `bdd8988f...`, touchability `29ccfd57...`, placement `4e2b0a39...`, authority readiness `baa38ff5...`, operator-auth readiness `63f537fd...` with `decision=defer`. E3 report verdict `APPROVE_FOR_PM_BB_REPAIR_REVIEW_REQUEST`; BB report `docs/CCAgentWorkSpace/BB/workspace/reports/2026-07-08--profit_first_dynamic_candidate_no_authority_chain_repaired_bb_review.md` verdict `APPROVE_FOR_PM_FINAL_WINDOW_PREP_REQUEST`. **2026-07-10 R3 F1 verdict invalidates the NEAR candidate evidence (n_eff=1).** | FROZEN by R3 F1 verdict: do not open Bybit/Decision Lease/order/probe and do not consume this READY state. Unfreeze only when the dedup lane (`49049f84d`) yields a candidate passing preregistered n_eff/day gates and the already-landed WP-A.6 checklist precondition. See `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-10--r3_fix_package_report.md`. |
| `P0-CURRENT-CANDIDATE-ACTUAL-ADMISSION-EXECUTION-ENVELOPE-REVIEW` | 0 | WAITING_FRESH_SCOPE | PM -> E3 -> BB -> PM | Consume actual-admission active-window evidence only after GUI cap lineage, auth freshness, lease, Guardian/Rust authority, fresh BBO, exact order shape, audit, and reconstructability all pass. | Prior runtime/loss-control readiness through `2026-07-08T01:53:48.341325+00:00` is expired historical evidence and cannot be consumed. Latest no-order final admission sha `5d26cf03...` remains historical READY_NO_ORDER; canonical plan sha `30056993...`; final-window lease was released. | Open fresh same-window E3/BB scope; released lease/no-order admission/materialized plan are not order authority. |
| `P0-CURRENT-CANDIDATE-FRESH-INVOCATION-WINDOW-LEASE-BBO-ORDER-SHAPE-GATE` | 0 | DONE_WITH_CONCERNS_NOORDER_SUBSCOPE | PM -> E3 -> BB -> PM | In one auditable fresh invocation window, validate standing/bounded auth freshness, active lease, fresh BBO/instrument, exact order shape, Guardian/Rust authority, GUI cap lineage, book-clean, auditability, and reconstructability. | E3/BB approved exact no-order Phase 0/A/B request sha `89eb2f59...` at checkpoint `08f7e957...`; PM consumed only that no-order subscope. Runtime manifest `/home/ncyu/BybitOpenClaw/var/openclaw/profit_first_dynamic_candidate_same_window_final_gate_20260708T175744Z_08f7e957_noorder/renewed_active_bbo_execution_manifest.json` sha `17a3a426...` shows Phase A/B public GET counts `3/3`, active `TRADE_ENTRY` lease `lease:4142221203d4` acquire/release true, post-governance lease/live `0/0`, and no private/order/probe/DB/runtime/Cost Gate/live/proof authority. | Closed only for approved no-order Phase 0/A/B. Do not treat the released lease or no-order BBO window as order-shape/order authority. Next order-capable step requires a separate exact scope, fresh same-window rechecks, E3/BB review, and operator authorization. |
| `P0-CURRENT-CANDIDATE-NOORDER-REFRESH-CURRENT-HEAD-E3-BB-REQUEST` | 0 | FROZEN_F1_EVIDENCE_INVALIDATED | PM -> E3 -> BB -> PM | Generate fresh current-head no-order refresh request from accepted Demo equity + current source/runtime evidence. E3/BB approval is required before quote/materialization/`_latest`/lease/private endpoint/PG/service/env/risk mutation/Cost Gate/live. | Current-head request `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-08--profit_first_near_buy_order_capable_fresh_window_refresh_request.json` sha `e9e6c0fd...` and manifest emitted at source/runtime `c66338e8...`, Linux clean, candidate <code>ma_crossover&#124;NEARUSDT&#124;Buy</code>, standing auth sha `05fe07f5...`, canonical soak plan sha `a296365e...`, active-order contract sha `cf2c9ff2...`, strict scan sha `ca4bf9cb...`, and blocked order-capable packet sha `305774b2...`. | FROZEN by R3 F1 verdict (2026-07-10): NEAR evidence is invalidated (n_eff=1, single day); do not dispatch E3/BB and do not consume request sha `e9e6c0fd...`. Unfreeze condition and rerun verdict live in `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-10--r3_fix_package_report.md`. |
| `P0-CURRENT-CANDIDATE-ORDER-CAPABLE-DEMO-INVOKE-FRESH-WINDOW-RUN-GATE` | 0 | FROZEN_F1_EVIDENCE_INVALIDATED | PM -> E3 -> BB -> PM | Fresh invocation window must recheck source/runtime heads, packet/input hashes, auth freshness, no-authority inputs, active lease, fresh BBO/instrument/order shape, Guardian/Rust authority, audit/reconstructability, and proof exclusion before any runtime/order-capable action. | Canonical soak plan is now NEAR-aligned sha `a296365e...`. Active-order contract sha `cf2c9ff2...` is `ACTIVE_ORDER_WIRING_CONTRACT_READY_FOR_E3_BB_REVIEW`; strict scan sha `ca4bf9cb...` has candidate rows `0` and no actual order/fill evidence. Current order-capable packet sha `305774b2...` is blocked only by `renewed_active_bbo_manifest_stale_for_review_packet`; stale manifest sha `17a3a426...` is tied to checkpoint `08f7e957...` and cannot be reused at `c66338e8...`. | FROZEN by R3 F1 verdict (2026-07-10): counterfactual rerun rejudged NEAR `SAMPLE_INSUFFICIENT_AFTER_DEDUP` + `EXECUTION_REALISM_SUSPECT`; candidate board = zero eligible candidates. Do not enable adapter/writer, acquire Decision Lease, call Bybit, or place/cancel/modify orders. Unfreeze only via dedup-lane evidence (`49049f84d`) passing preregistered gates plus WP-A.6 checklist precondition. |
| `P0-EXECUTION-EVIDENCE-LOOP` | 0 | WAITING_ORDER_CAPABLE_SCOPE_AUTHORIZATION | PM -> PA -> E1 -> E2 -> E4 -> E3/BB -> PM | Once standing envelope + bounded auth are runtime-admissible, run only bounded/loss-controlled Demo execution and record candidate-matched fills/fees/slippage/controls/reconstruction. | Read-only touchability audit has aggregate `FILL_FLOW_PRESENT`, but current NEAR Buy candidate has zero candidate-matched orders/fills; first-attempt bootstrap is not proof. No-order Phase 0/A/B succeeded, but no candidate-matched order/fill/proof evidence exists. | Outcome evidence cannot start from no-order evidence. Wait for separate order-capable authorization and loss-controlled Demo execution scope. |
| `P0-PROFIT-OUTCOME-REVIEW` | 0 | WAITING | PM -> QC/MIT/BB -> PM | Review candidate-matched fills, actual fees/slippage, controls, execution realism, proof exclusions, regime/OOS labels, and repeat path. | No authorized candidate-matched bounded Demo outcomes yet. | Wait for bounded Demo outcomes. |
| `P1-FEE-TIER-PRIVATE-READ-RUNTIME-INVOKE` | 1 | DEFERRED | PM -> E3 -> BB -> PM | One-shot exact-symbol fee-rate read only inside standing Demo evidence envelope or separate E3/BB-reviewed read scope; sanitized artifact only; no PG write, no runtime fee-cache replacement, not proof by itself. | v585 envelope was READY_NO_READ; actual private fee read has not run. | Reopen only when execution envelope needs actual fee-tier evidence and the read is inside approved loss controls. |
| `P1-L2-ADVISORY-MESH-E2E-1` | 1 | DONE_WITH_CONCERNS_FENCE_SINK_FOLLOWUP | PM -> operator -> E3 -> E1/E4 | Close L2 tail only with one explicitly approved true `diagnose_leak` invocation that performs a real model call, writes a real `agent.l2_calls` row, records evidence, and restores capability disabled. | Historical root ledger archived at `docs/archive/2026-06-05--l2_advisory_mesh_todo.md`. First 2026-07-10 one-shot (R3 WP-C.2) was SDK-blocked with fail-closed restore verified (evidence `docs/CCAgentWorkSpace/E1/workspace/reports/2026-07-10--l2_e2e1_oneshot_blocked_evidence.md`; error-path rows only, cost $0.00). **2026-07-10 rerun after operator-approved unlock path A (anthropic SDK into control API venv) achieved the first true model call**: `agent.l2_calls` row `l2r:724ac38bc4fc`, model `anthropic:sonnet`, cost_usd $0.0149, latency 17,801ms, raw_response 3,401 chars of substantive diagnosis; daily cost counter 0.0 -> 0.0149. Restore verified stronger than before: registry TOML sha `a48b0a85...` byte-identical, 12/12 per-worker orchestrator in-memory probes `enabled=[]`, runtime tree clean, no service/engine restart. Evidence: `docs/CCAgentWorkSpace/E1/workspace/reports/2026-07-10--l2_e2e1_oneshot_rerun_success.md` + runtime dir `l2_e2e1_oneshot_20260710_rerun/`. | Acceptance (real model call / real `agent.l2_calls` row / evidence / restore disabled) is **PARTIAL pending lessons-sink verification** — the model-call / ledger-row / evidence / restore-disabled sub-goals are met, but the `agent.lessons` sink half is unverified (the one-shot's valid fenced-JSON reply parsed to None so the sink was never written), and no live reply has yet exercised the sink write; **L2 remains fully disabled**. Remaining gap = follow-up ticket, not this row: executor does not strip markdown fences before `json.loads`, so the valid fenced-JSON reply parsed to None, the `agent.lessons` sink was not written, and stage was mislabeled `cloud_unavailable_or_unparsable` (plus stage-string semantic overload and NULL token columns). Fence-parsing sink fix is new code via full PA -> E1 -> E2 -> E4 chain before any future L2 enablement. |
| `P2-GLOBAL-QUALIFIED-AUTONOMOUS-LEARNING-SHADOW-V1` | 2 | SUPERSEDED_BY_AIML_LONG_LIVED_LANDING_V2 | PM | 保留已接受的歷史 source contracts 與 authority boundaries，但不再延續過時的 WP4 source-only 派發序列。 | V151-V160 與 strict verifier source 已存在；2026-07-19 runtime audit 證明 schema/contracts 並未形成 active trainer、controller、serving consumer 或 profit path。 | 不得派發此 row。把可重用 contract 納入上方兩個 active AIML program row；當機完成度由 LR-1／LR0A-LR0C／LR0-LR6 與 ML0-ML10 管理。 |
| `P1-GATE-B-AUTO-CAPTURE-NEXT-5-LISTINGS` | 1 | ACTIVATED_AWAITING_FIRST_LISTING | PM -> E3 -> PM | AMD-2026-07-10-01 (Active): operator authorized (2026-07-10, relayed via R3 main session) auto Gate-B capture for the next 5 new listings. cap=5 persisted counter, auto-stop + audit line at cap, R-0 zero-leak boundary unchanged (capture artifacts never enter the trading path; zero order / zero DB write / zero Cost Gate contact), default OFF (`OPENCLAW_GATE_B_AUTO_CAPTURE=1` enables), spawn failure does not consume quota. | Code landed `b7359b2cd` (`gate_b_auto_capture.py` + `gate_b_watch.py` wiring + crontab template two-line update + 14 new tests; canary lane 533 passed). Governance rows landed `10dbfb10b` (AMD + SPECIFICATION_REGISTER + document_index). 2026-07-12 conductor 親證 Linux 活化:crontab 2 行帶 `OPENCLAW_GATE_B_AUTO_CAPTURE=1`;cap 消耗 0/5;state 檔尚無 `auto_capture` key(未觸發屬預期 IDLE)。 | 被動等首個上市事件(named external event: Bybit 新上市);巡檢 = audit jsonl(auto-capture 觸發/cap 消耗行)。Cap renewal/raise needs a new AMD. |
| `P2-AI-PRICING-SONNET5-INTRO-EXPIRY` | 2 | SCHEDULED_REVIEW_2026-09-01 | PM -> E1 | On 2026-09-01 update `settings/ai_pricing.yaml` `claude-sonnet-5` from intro $2/$10 to standard $3/$15 per MTok (intro window ends 2026-08-31 per official pricing page; recorded in the yaml comment). Leaving the intro price past expiry underestimates L2 cost ~33%, which is anti-conservative for the daily budget gate. | Key landed `473706171` with dated TODO comment in the yaml; R3 report §一 WP-C.1. | Dated review 2026-09-01: single-point yaml edit + E2 glance. No other action; healthcheck = this review date. |
| `P2-RUNTIME-SOURCE-BUILD-PIN-DRIFT-HYGIENE-2026-07-07` | 2 | DEFERRED | PM -> E3 if deploy/runtime-touching -> PM | Keep runtime source/build/API boot/crontab pin facts reconstructable after source-only syncs. Acceptance is a read-only packet showing checkout head, engine build_sha, control API boot repo_head, cron expected-head pins, service bind address, and whether no-op vs deploy is justified. | 2026-07-07 probe: checkout `5d1622994`, engine build_sha `54d5fbf99`, control API repo_head `70d114316`, crontab 34 active commands with one stale `c0c49deeb` pin. | Re-run after the requested three-side sync or before any runtime-touching work. Do not restart/rebuild solely from this row without explicit PM/operator deploy scope. |
| `P0-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W0` | 0 | DONE_POLICY_ACCEPTED | PM -> PA -> TW -> PM | AMD-2026-07-11-01 is Accepted. It supersedes only conflicting old IBKR capability-development restrictions; it does not activate IBKR. | Stable policy/docs updated; default remains inactive; Bybit remains the only currently active live venue. | Dispatch W1. |
| `P0-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W1` | 0 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> CC/E3 -> PM | P2 sealed-artifact production consume is build-generation-bound and fail-closed: exact euid `0400` regular file; data-root→governance→ibkr_phase2 `lstat`/`O_NOFOLLOW`/`fstatat`/`openat` inode-bound traversal; exact `0700` owner-only dirs; anti-relocation/hash/type validation; FIFO-safe nonblocking final open. | E1 implementation plus P1 remediations; E2/E3 final PASS; E4: Rust `32 passed`, phase2 static `26 passed`, type acceptance `45 passed`, rustfmt/diff PASS. Default remains inactive; no broker/session/contact/order/runtime action. | W2 active: PA designs controlled Rust production seal/supersession caller without enabling contact. |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W2` | 1 | DONE_SOURCE_SECURED_HARDENED | PM -> PA -> E1 -> E2 -> E4 -> E3 -> PM | Add controlled production seal/supersession caller; remove permanent production blocker without creating contact. | R1 四腿(PA 補簽設計檔/E2 APPROVE_WITH_NOTES/E3 PASS_WITH_NOTES/E4 雙腿綠)+ **R2 加固 PR#28 `19985f312`(2026-07-15)**:E2-F1 HIGH staggered-expiry brick 修復(expiry 只約束 active leaf;consume 側過期即 false;supersede/revoke 不再被過期祖先 brick)+ /run 族 denylist + 相對路徑拒絕 + readdir errno fail-closed + anti-placeholder + 8 新測試(102/0);R2 雙審 E2 APPROVE_WITH_NOTES + E3 PASS_WITH_NOTES;CI `rust-ibkr-tests` 綠(ubuntu 腿);E2-F2 = CC 裁 `DRIFT-CONFIRMED/NOT-BLOCKER`,AMD-07-08-01 **澄清 #3** 入典(綁定目標 07-11-01+審計重算配方+EA3 歸檔義務,Operator acknowledgement only)。 | 收口。production seal arming 屬 EA3(操作前置含批准檔位元組歸檔;澄清 #3 Operator **acknowledged 2026-07-16**);殘餘 LOW/NOTE 入 R3+ 順手清單(Revoke 豁免壞 inputs/compile_error 守衛/expiry 上界/測試加硬)。下一步:loop R3 = W3(TWS session manager + fake-TWS harness)。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W-CI` | 1 | DONE_LANDED_FIRST_GREEN | PM -> E1 -> E2 -> E4 -> PM | IBKR Rust 測試進 CI(37 個 `openclaw_types` acceptance 檔 + 引擎 in-file/IPC 測試)+ `helper_scripts/ci/ibkr_g4_symbol_audit.sh` 接入 `ci.yml`;symbol 斷言效期綁 W3 build-posture 重裁(`IBKR_TODO.md` §5 W-CI)。 | PR#21 merge `48872c4f`(2026-07-15):job `rust-ibkr-tests` 首綠 **7m35s**(timeout 35m 充裕);五 scope=types 74+287(37 檔)/engine `--lib ibkr` 94/`--lib stock_etf` 33(含 31 IPC 契約)/seal bin 2/audit PASS;質量鏈=E1(申報 name-filter 空轉偏離)-> E2 REJECT(F1 IPC 缺)-> 修復 -> E2 APPROVE(31+2 溯源閉合、零誤吞)。 | 收口。殘項(R8 審計定界,→ W5-S0/R9 必修):①classifier 缺 `helper_scripts/ci/ibkr_*.sh` **三**腳本(g4/fake-absence/driver-absence);②`rust-ibkr-tests` job 未被 workflow static test 釘住(可整 job 刪除而靜默過);③W4 lockstep/parity pytest 不在 hosted CI。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W3` | 1 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> IB/E3 -> PM | Expand handshake/`reqCurrentTime` into a recoverable TWS transport/session manager. | **DONE 2026-07-16(loop R3-R6,4 片)**:S1 wire+IbErrorClass(PR#32 `ad14d5c1`)· S2 六態 FSM+INV-1 permit stub(PR#33 `9c39d310`)· S3 pacing governor 單一出口+有界排隊(PR#35 `6c8c66f8`)· S4 fake-TWS dev-crate+端到端 driver(本波 PR)。CC 裁 build-posture B′;IB U1-U6 官方現勘入碼;3 機器守衛入 CI;211 engine 測試+9 fake;E2 APPROVE+E3 PASS(INV-1 production 端到端無放行路徑=NO 二元確認)+E4 雙腿。 | W3 收口。**移交**:F1 historical ordering→W6;send_framed 真消費者+queued-heartbeat→W4;reactivation 重置 governor+permit audit-scope 擴 driver→W8(見 IBKR_TODO §W8)。下一=W4(connection-health IPC + normalizer lockstep)。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W4` | 1 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> IB -> PM | Complete P4 connection-health IPC/route and Python normalizer lockstep. | **DONE 2026-07-16(loop R7,W4-1 lockstep 單 PR)**:`IbkrConnectionHealthReportV1` 契約 + Rust emitter(構 ephemeral manager 撞 permit → `Disconnected(EnvelopeRequired)` 回 `external_verification_pending`,零 socket,session/pacing 首個 production caller)+ `stock_etf.get_connection_health` IPC + FastAPI GET route + Python normalizer 三層 lockstep(hard-safety 恆 flag/lineage-absent all-false/lineage-present W5+ 不可達)+ fail-closed 四道 + 新 driver-absence audit 入 CI。五腿:E2 APPROVE+E3 PASS(授權面無繞過:populated 真值無 lineage 漏過=NO)+IB PASS(語義忠實)+E4 全綠(211 engine/201 Python)+QA ACCEPT(端到端 operator 讀誠實 PENDING)。 | W4 收口。**W5 blocking**:E3-F1 Layer 3 窮舉補齊 + E3-F2/E2-F3 parity superset 斷言(seal lineage 前必修否則 fail-closed 退化)。GUI defer W9。pre-existing handler-cap 債已解(PR#42 `275c76c59`:`stock_etf.rs` 826→779、結構守衛接 CI 並統一現行 2000 行門檻、classifier 擴 6 prefix;task_2dbb7f53 closed)。下一=W5(account/positions/executions + session attestation)。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W5` | 1 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> IB/E3 -> PM | Implement account, positions, open orders, executions, commissions, and session attestation. Slice plan = IBKR_TODO §5 W5(S0-S4)。 | **W5 全包收口(2026-07-16~17 loop R9-R13,PR#48/#49/#57/#60/#61)**:CI 守衛鏈+Layer3 窮舉+parity superset/四 typed row contracts+配對守衛/account·positions 消化/exec·commission·orderStatus·openOrder-minimal 消化/attestation producer(禁聲明自填=型別層落實)。收口四腿:E2 APPROVE_WITH_NOTES+E3 PASS(INV-1 HOLDS)+QA ACCEPT_WITH_NOTES+CC PASS_WITH_NOTES(16/16);engine ibkr 280/fake 12/types 453。帳本=PROGRESS R9-R13。 | **re-scope 注記:三腿指紋三角測量第三腿(auth-matrix 接 loader 真值+attested 真消費)顯式移 W6 必修同票**(E2-F1/QA-NOTE-1/CC 背書)。**W6-S0(R17)+S1(R18)+S3a(R19 PR#76:mktData 紅線=E2-F1 注入修+regulatorySnapshot FUNDS-LOCKED+quote/provenance 契約+OUT builders,E2 APPROVE/E3/IB PASS)已收口**;**S3b(R20 PR#77:IN decode+entitlement FSM+lines semaphore,E2 APPROVE/E3 FUNDS-LOCKED HOLD/IB byte-exact)已收口→W6-S3 mktData lane 完整**;**W6(S0-S4)全收口 DONE_SOURCE_SECURED(R22 PR#79:provenance calendar 真綁定+IB-B′ 移除+E2-N1 守衛;E2/E3/IB 過門;PM scope reconciliation=必修同票六項 R17 已閉/ApiPending W7/EA 項 EA3-gated)**;**W7 設計輪已落(R23 PR#80:PA 設計文檔+IB 現勘;INV-ORDER transport-gating=OrderFrame newtype+OrderEffectPermit 恆拒;切片 S0 地基→S1 狀態機→S2 cash→S3 三向對賬 P0→S4 option B HMAC;order 10.x band re-pin BLOCK-ORDER-BAND-1/2/3=EA3 前 blocking;ApiPending→transient 態=前解)**;下一=R24 W7-S0(transport-gating 恆拒 stub 地基,CC 建 authority seam 基線);**D8 已裁(operator 2026-07-17)=KEEP 176**(不降 157;原誤記 D5,R29 校準改編,見 IBKR_TODO §9 D8)→W5-S2/S3+W6-S1/S3 pinned decoder(≤157)EA3 前必對 10.x band(sv158-176)重現勘+重 pin(EA3 前唯一可完成路徑=license-gated 官方 10.x 源;EA 窗捕捉=EA3 後顯式授權校準步,blocking);ServeDisconnectCause 三無 cause 路徑+世代負測試(R17 E2-F1/F5)仍 W6 IPC 投影前;**ApiPending 須 W7 前解**;10.x band 補證=operator;EA 前置=TWS UTC 模式+exec_time grammar attestation。下一=R14 W8A∥R15 W9A。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W6` | 1 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> IB -> PM | Implement contract details, market-data snapshot/subscription, calendar, and entitlement states(含 W3 移交 F1 historical ordering、entitlement 三態)。 | **W6 全收口(2026-07-17 loop R17-R22,PR#69/#70/#76/#77/#78/#79)**:S0 必修同票硬化六項(三角測量第三腿=W5 re-scope carve 閉合)+S1 contract details lane(message-version==8 硬 pin/裁決 (b) head-prefix)+S3a mktData 紅線+契約(E2-F1 出站注入修/**regulatorySnapshot 源碼封死 FUNDS-LOCKED**/quote+provenance 契約)+S3b engine digest+S2 trading calendar(DST gap/fold 臂鎖死)+S4 收口(provenance calendar_hash 真綁定+兩 IPC loose ends)。delayed-only v1 成立;逐輪 E2/E3/IB 腿全過,INV-1 HOLDS。帳本=PROGRESS R17-R22。**(本行狀態遷移於 R29 校準補記——R22 收口輪 S6③ 欠帳)** | 收口。blocking 移交:pinned decoder ≤157 band 受 **D8(KEEP 176)** 約束=EA3 前 10.x band(sv158-176)重現勘+重 pin(IBKR_TODO §9 D8/BLOCK-ORDER-BAND 家族)。下一=W7(R23 起已開)。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W8A` | 1 | DONE_SOURCE_SECURED | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/IB -> PM | readonly-scope `ibkr_activation_envelope_v1` 最小切片(W8 全包吸收本切片,共用驗證代碼路徑禁漂移)。 | **收口(2026-07-17 R14,PR#63)**:契約 25-blocker+驗證器 nonce 原子消費+order verb 結構性拒+permit 四聯 audit 擴 driver 面(**W3 移交 E3-F1 首階段 CLOSED**);四腿 E2 APPROVE+CC 16/16+E3 INV-1 HOLDS+IB PASS;types 475/engine ibkr 292。帳本 PROGRESS R14。 | **W8 吸收票 blocking**:durable nonce 消費紀錄/lineage·指紋現值比對/attestation fingerprint 派生正本/activation-record authenticity 申報;W8 第二階段=mint 正則收緊+socket 掃描全域化+cfg(test) 斷言。**R16 已收口(07-17,PR#66):G4 entry Gate 5 消費 envelope 驗證器,IB-NOTE-1 CLOSED——EA1-EA3=零工程等待,剩 operator 動作+runbook(RB-1~RB-5 見 IBKR_TODO §6)**;singleton registry §2.10 已登記。下一=W6(IPC 投影)。 |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W9A` | 1 | DONE_SOURCE_SECURED | PM -> E1 -> E2 -> E3/OPS/IB -> PM | IB Gateway 安裝腳本+systemd unit(default disabled/masked)+headless 預備;安裝執行=operator 批准窗(OPS 按既備 preflight/postcheck 鏈簽收)。 | **收口(2026-07-17 R15,PR#62)**:TOFU pin+三元組漂移偵測(官方無 checksum=CONFIRMED-ABSENT)/兩層 unit 佈局(B1 trade-core dummy-unit 實證)/RM-1 兩段式 manifest/dormant postcheck 工具;E2 REJECT→APPROVE_WITH_NOTES+OPS FAIL→PASS_WITH_NOTES(source-ready 達標)+E3/IB PASS。帳本 PROGRESS R15。 | 安裝執行待 operator 開窗(EA2 前置);EA2 屆時=timer America/New_York 定點+WatchdogSec wrapper+sentinel 掛接。**D2 觸發:三包齊已通知 operator 開 EA1/EA2**。 |
| `P0-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W7` | 0 | IN_PROGRESS_S3_DONE | PM -> PA -> E1 -> E2 -> E4 -> IB/E3/QA -> PM | Implement paper/live preview/place/cancel/replace, fills, partial/reject/reconnect, and restart reconciliation using fake TWS. Slice plan = IBKR_TODO §5 W7(S0-S4)。 | W3-W6 全收口;W7 設計輪 R23 PR#80(INV-ORDER transport-gating 不變量)。 | **W7-S3 收口(2026-07-18 loop R27,PR#85)**:三向對賏引擎(P0 核心幻影倉防線)=`ibkr_order_reconciliation.rs`(broker 真值×intent journal×本地態,idempotency_key join,P0-A reduce-only fail-closed/P0-B unknown-terminal 持久凍結/P0-C execId 去重,**持久凍結 store sticky-until-adjudication**+終態量對賏 i128);engine ibkr 511。**全 P0 panel(E2/E4/IB/CC)揪 2 真 fail-closed 缺陷並閉合**:CC F1=E2 MED-2 凍結非持久(→OrderReconciler 持久 store)+CC F2 reduce-only 非持久(→升 ManualReview fill=None)+E2 MED-1 終態量分歧漏判(→i128 定點對賏);複審 CC PASS(F1/F2 CLOSED)+E2 APPROVE(MED-1/2 CLOSED)。下一=R28 W7-S4(**option B HMAC+check_effect_contact 放行臂+IPC+fake-TWS 全矩陣+收口**,P0 effect+authority,EA5 paper 窗 blocking 前置;+E3+CC)。**S4 blocking acceptance carry**:CC NOTE-1 symbol-binding 保真(order-emit 查 is_symbol_frozen 綁真 symbol)+E4 NOTE-2 order-emit 查持久 frozen store 當閘+IB EA-CAL-1 orderRef=idempotency_key wire+Execution.orderRef 捕捉(與 reconcile 同活化)+EA-CAL-2 permId fallback tier+IB EA-NOTE-3 ledger 禁加總 exec shares。**W7-S2 收口(2026-07-17 loop R26,PR#83)**:cash 約束引擎(deterministic pre-submit gate)=`ibkr_cash_account_constraints.rs` 純函數 `evaluate` 窮舉 7 gate(settled-funds T+1/GFV/no-short 硬邊界/RTH/order-type 白名單/fractional/LULD),i128 定點金錢,`CashAccountRules` 注入(官方數值全 illustrative 待 IB/EA 现勘);engine ibkr 482。初審 E2/IB现勘 PASS(5 gate CONFIRMED 附 SEC 15c6-1(a) 出典)→rework 修 3 項(MKT buffer 補唯一 fail-open/GFV 語義收緊 unsettled_funded 修誤殺/LULD venue-flag 權威+本地 approximate-only)→複審 E2 APPROVE+IB PASS(2 DIVERGENT CLOSED)。下一=R27 W7-S3(**三向對賬引擎,P0 核心**:broker 真值×intent journal×本地態,無序 join tolerant,差異 fail-closed,reduce-only;+CC fail-closed 審)。**S3 carry**:E2-LOW-2 tranche 重複計數整合不變量+task_03abee1a bundle;**EA 校準 7 項**(§3.3)含 MKT buffer 真機制/GFV 硬拒 vs strike 姿態/LULD tier=EA-gated。**W7-S1 收口(2026-07-17 loop R25,PR#82)**:訂單狀態機 driver+intent journal(不送出)=14-態 runtime driver+唯一 mutator `apply_lifecycle_event`(reduce-only fail-closed)+journal hash chain+nextValidId+idempotency_key join+ApiPending transient 分流(解 W5-S3 carry)+restart recovery+**NOTE-1 (a) sealed WireBytes**(型別層阻斷 order bytes 退化餵 driver::send_framed)+place/cancel encoder(NO send)+encode ceiling guard;engine ibkr 443。初審四腿→**IB 揪 2 CONFIRMED DIVERGENT wire 缺陷**→rework(placeOrder 全欄序重導升 **CONFIRMED-vs-9.81.1 ≤157**/whatIf tail decode fail-close PreviewDecodeBlockedPendingFullSequence)→複審 E2 APPROVE+IB PASS(DIVERGENT-1/2 CLOSED,byte 證據 encoder==官方 wire)。下一=R26 W7-S2(cash 約束引擎:T+1/GFV/no-short/RTH/LMT-MKT-DAY 白名單)。**新 EA3 前 blocking:BLOCK-ORDER-BAND-4(placeOrder 158-176 sv-gated re-pin)/BAND-5(whatIf 全序列解碼 blocked-until);BAND-3 修訂(定位缺陷)**;非阻擋 S3 carry=task_03abee1a(G5d 完整性/golden 再生器/E4-GAP-3·4·5/exec_data 拆檔等)。**W7-S0 收口(2026-07-17 loop R24,PR#81)**:transport-gating 恆拒 stub 地基=`OrderFrame` newtype+非 Clone `OrderEffectPermit`(mint `#[cfg(test)]`,production 零構造)+單一 `send_order_framed`+`EffectEnvelopeRequiredStub` 恆拒+`order_effect_capability_gate` machine-check+4 structure guard;engine ibkr 414/final-binary DCE 零 seam。四腿:E2 APPROVE_WITH_NOTES+E3 PASS(INV-ORDER+INV-1 HOLDS)+IB PASS+CC PASS A 級 16/16。**S1 blocking discipline(E2-NOTE-1/E3-NOTE-1)**:S0 承重=「無 encoder+0 caller+DCE」非型別屏障→S1 encoder 落地必強制 order bytes 唯一經 gated send_order_framed(新 G5 守衛禁直達 send_framed,或委 S4 runtime gate);sealed WireBytes newtype+binary DCE 守衛=S1 評估。下一=R25 W7-S1(狀態機 driver+intent journal+place/cancel encoders+encode ceiling guard+openOrder 尾 decode+ApiPending transient 態)。**order 10.x band re-pin BLOCK-ORDER-BAND-1/2/3=EA3 前 blocking(D8 KEEP-176;原誤記 D5 與策略 universe 撞號,R29 校準改編,見 IBKR_TODO §9 D8)**。**(R29 校準注:R28 W7-S4a option-B HMAC 核心 in-flight——遠端分支 `agent/ibkr-w7-s4a-option-b-hmac` head `36e829cb1`+**PR#88 OPEN(WIP)**,R28 記帳已在支線但質量鏈未走完;接棒按 loop §6 驗屍(cargo test 實跑)→續審 E2/E3/CC/IB→合入,禁憑半成品自報)** |
| `P0-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W8` | 0 | QUEUED | PM -> PA -> E1 -> E2 -> E4 -> CC/E3/IB -> PM | Wire Rust risk/authority, activation envelope, kill switch, and audit persistence. | W7; no envelope/session means inactive. | Dispatch after W7. |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W9` | 1 | QUEUED | PM -> PA -> E1 -> E1a -> E2 -> E4 -> OPS/IB -> PM | Complete DB/evidence, FastAPI, GUI, service config, observability, and rollback. | W5-W8; GUI is never authority. | Dispatch after prerequisites. |
| `P1-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W10` | 1 | QUEUED | PM -> E4 -> QA -> OPS -> IB -> PM | Run Mac tests, fake-TWS E2E, Linux inactive deploy, QA, and three-way sync without IBKR contact. | W1-W9. | Dispatch after implementation. |
| `P0-IBKR-STOCK-ETF-FULL-LIVE-CAPABILITY-W11` | 0 | QUEUED | PM -> CC/E3/IB/OPS/QA -> PM | Deep global gap rescan, adversarial audit, and closure. Only external session/credential/activation may remain pending. | W1-W10. | Dispatch after W10. |
| `P2-AUDIT-REMEDIATION-6-7-Q4-V157-DEPLOY` | 2 | DONE_WITH_REATTESTATION_REQUIRED | PM → ML0 OPS 唯讀 → PM | 在啟用真實 registry writer 前，保留 V157 lineage 行為並重新證實 production relation/function/row shape。 | Source 包含 V151-V160。同日 2026-07-19 已停止的 runtime-refactor 任務以有界唯讀觀察報告 V151-V160 已套用；這取代 2026-07-11 `prod PG at V150` 的陳述，但不證明 rows 或 serving。 | 本 row 不得 apply migration。透過 ML0 批准的唯讀 PG 身分重新證實 V151-V160 與 lineage columns，確認 shape 後再 close/archive。 |
| `P2-AUDIT-SEAM-REPROBES-2026-07-11` | 2 | QUEUED_INVESTIGATE_FIRST | PM -> per-owner -> PM | Read-only investigate the 8 cross-axis seam re-probes before any consolidation fix; carve a fix only when a probe confirms and target files are clean (not blind-fix). | ultracode audit `wf_27620bc0-5a4` coverage debt: (1) fee-constant 5.5bps SSOT fragmentation [BB]; (2) IBKR port 4001 attestation-not-inspection [IB]; (3) CV-leakage -> deprecated IID t-test false-GO amplifier [QC]; (4) composite live-write reachability worst-case env [E3]; (5) dormancy compile-guard absent [CC]; (6) 4-head integrity reconciliation [OPS]; (7) agent-session token spend uncapped [AI-E]; (8) cron actual-exec head vs stale pin [OPS]. | Dispatch as read-only probes per owner. |
| `P2-AUDIT-GUI-9-10-REDO-POST-P0.4` | 2 | QUEUED_AFTER_GUI_REDESIGN | PM -> PA -> E1a -> E2 -> E4 -> A3 | Re-implement audit items 9/10 (defense-in-depth UX): live risk-config save typed-confirm + old->new value diff; Danger-Zone reset/resume [PAPER]-scoped labeling. | 2026-07-11 audit MED, security REFUTED (server route enforces the full 5-gate; UX-only, no real-funds exposure). Original worktree impl was uncommitted and lost when scratch cleaned; main `risk-tab.js` still has the single-click structure; parallel P0.4 GUI redesign actively reworks `risk-tab.js`. | Redo after P0.4 GUI redesign settles to avoid collision; not urgent. |
| `P3-AUDIT-AE-INVENTORY-ARCHIVE` | 3 | DONE | PM -> E5 -> E1 -> PM | Archive `AE_INVENTORY_CONSOLIDATED.md` (~422KB root blob) to docs/archive/ only if no live reader. | 2026-07-12 E1 執行:git mv → `docs/archive/2026-04-25--ae_inventory_consolidated.md`(archive 命名=內容快照日期);live 引用已解:`initiative_index.md` repoint+`document_index.md` 注記式追加+`path_redirects.md` Executed Redirects (2026-07-12) 留 grep 召回鏈;移入檔頂加歸檔注記(內部 `.claude_reports/`/`srv/` 相對連結=歷史死鏈);`ae_runtime_rename_migration_guide` grep-based path-agnostic 免改;其餘命中皆凍結歷史檔(correct-as-of-date 不改寫)。 | PM merge 時同步 memory `project_2026_07_11_ultracode_audit_remediation.md` Item 14 解除注記+ledger 隊列 F 收尾。 |
| `P0-TRADE-CORE-OOM-STORM-CRON-MEMORY` | 0 | SOURCE_MITIGATIONS_LANDED_AWAITING_OPERATOR_DEPLOY_AND_ROOT | PM(E5 done) -> operator -> OPS/E1 | kernel OOM kill 歸零、watchdog/引擎不再連坐。E5 RCA 已定位:單進程 20-25GB=全量物化 probe_ledger(5.3x 膨脹)+純 Python B=1000 sign-flip;**全機放大器=cron shell stale-lock 超時清鎖照跑新進程不殺舊進程(alpha_discovery_throughput_cron.sh:36-44 / cost_gate_learning_lane_cron.sh:152,367-372),每 15 分鐘淨疊 20-25GB**;另 runtime_runner 每 */15 inline 重做 hourly review(status.py:649-762)+ polymarket replay_history glob-parse 全目錄。緩解均 runtime mutation=operator-gated。 | 2026-07-15 取證+E5 RCA 全文:報告 §四(`docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-15--watchdog_audit_dsn_rca_runbook.md`);16 次 kernel OOM kill/watchdog 連坐 5 次/16:35:48 引擎被直接殺(oom_score_adj=200)。量級歸因缺口=3 個零風險 runtime 量測(報告 §四)。相鄰:PR#19/20 已對同族 cron 的「日誌」面做 size-cap(logrotate),RAM 面即本 row。 | **兩個零特權 source 修都已 landed**:①疊加機 flock(CRON-STALE-LOCK-FLOCK-1,PR#27)②OOM-victim 自標(CRON-OOM-VICTIM-1,本輪;hog 自標 adj=800>>引擎 200,OOM 優先殺 hog)——皆 E2 PASS 0 P0/P1 + E4 trade-core 密閉全綠(flock 7-case、victim 5-case 兩跑;消費者 smoke adj=800 probe PID==flock holder;引擎/watchdog 實測 adj=200<800 前提滿足)。**operator 待辦**:①**部署窗**:Linux srv pull(flock+hog;換鎖窗先 `pgrep -f alpha/cost_gate` 防新舊鎖並行一輪)+跨長跑週期驗 python 實例≤1 且被標 python adj=800;②**需 root(唯一從根本移引擎出 OOM 射程)**:降引擎/watchdog adj——改 user manager `DefaultOOMScoreAdjust` 或引擎轉 system unit 設負 `OOMScoreAdjust`;③**MemoryMax 本輪不可安全定值**(量測:單進程失控 85GB vs 正常峰值未知——lane 一直被殺從沒正常跑完,盲設<正常=誤殺 livelock;且 cron 無 DBUS systemd-run --user 不可用)→ 真解=P2 streaming 代碼修。**follow-up**:鎖家族 flock+victim 遷移(SCRIPT_INDEX 清單:full-pattern 餘 12+部分模式 20)、static 鎖 mark-before-spawn 防 refactor、polymarket_axis 21GB retention、E5 P2 streaming/投影(reject_materializer/outcome_review 別全量物化,砍 95%)。**07-16 深夜~07-17 追加**:learning-lane 有界投影/streaming 修復波已上 main(p0a-p0d:PR#43/#44/#46/#50/#52/#54/#56;PR#52 body 明文以 candidate-scoped 投影+streaming JSONL 取代全量物化)——P2 方向 source 已大步落地;`agent/p0-oom-*` 4 支線(ledger/polymarket streaming、data-plane、systemd scope)in-flight 未併;07-17 三端同步後 runtime checkout 已含已併修,cron 下輪 fire 即用新碼,OOM row 狀態重評歸下輪。 |
| `P1-WATCHDOG-AUDIT-DSN-RUNTIME-APPLY` | 1 | WAITING_OPERATOR_WINDOW | PM(E2/E3/E4 done) -> operator -> PM | runtime 手管 user unit(`~/.config/systemd/user/openclaw-watchdog.service`)補 `OPENCLAW_DATABASE_URL_FILE` env 行 + daemon-reload + E3 post-check(`LoadState=loaded`+Environment 含該行);其後下次 watchdog 重生事件 `watchdog.log` 無 `no DSN buildable` 且 `audit_events` 出現非 backstop 直寫行。source 側已併(unit 模板+resolve_dsn 第 4 步備援+測試)。 | RCA+runbook:`docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-15--watchdog_audit_dsn_rca_runbook.md`(§五 Step 1-4;E3 條件 GO;審計帳未丟——bridge 14:52:01 已補 audit_events id 1877/1878)。chronic 自 06-20;07-15 24 條 skip 全由 bridge 兜住。 | operator 依 runbook §五執行 Step 1(+可選 Step 3 srv pull);Step 2 主動 restart 有引擎連坐代價(KillMode=control-group),非必需。durable-unit 收口(system unit 化/installer var 路徑)併此 row 追蹤。 |
| `P1-PROFIT-MOVE23-RESEARCH-IMPL` | 1 | PHASE0_DONE_DEADLINE_DEFUSED | PM -> QC -> E1 -> E2 -> E4 -> MIT/OPS -> PM | Implement the two R3 profit-diagnosis GO_WITH_CONDITIONS lines as $0 read-only research (no order authority, no engine lane): #2 bb_reversion R1 measurement instrument; #3 daily-XS research pipeline. Both prereg v1.1 must be formally frozen (rules+harness sha256) before any outcome; #2 L1 episode slice pinned before the 2026-07-19 retention cliff. | **2026-07-11 Phase 0 done — 07-19 deadline DEFUSED.** #2 L1/trades episode slice pinned immutable: trade-core `~/BybitOpenClaw/artifacts/move2_episode_pin` (0444) + Mac `artifacts/move2_episode_pin`; 31 merged windows / 136,772 l1_events rows / 0 empty windows, manifest sha256 `0a4dff9c...`; G8 unpinnable (last close_maker_attempt 06-19 < l1_min 06-20 — L1 aged out, pre-existing loss). Exporter `program_code/research/microstructure/episode_slice_pin.py` (E1) + 12 offline tests; E2 verdict RUN_WITH_FIXES (no P0, read-only unbypassable, no write statement exists), 3 P1s handled at invocation; host readonly proven (`ReadOnlySqlTransaction` rejects writes under superuser). Both prereg v1.1 frozen-READY: [#2](docs/CCAgentWorkSpace/QC/workspace/reports/2026-07-11--move2_prereg_v1_1.md) (FIX-1..5 + RT-9/11/12), [#3](docs/CCAgentWorkSpace/QC/workspace/reports/2026-07-11--move3_prereg_v1_1.md) (FIX-1..7 + blocking §⑥ H_TREND boundary vs 06-02 NO-GO-TREND). Runbook `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-11--move2_slice_pin_runbook.md`. Origin: memory `project_2026_07_09_profit_diagnosis_roi_map`. | **Phase 1**: build #2 R1 ITT four-state harness (fill_sim sibling) -> formal prereg freeze (piece-2 harness_sha256) -> run R1 (expected INSUFFICIENT->KILL, M12 value either way). **Phase 2**: #3 Stage B 3x `--apply` (top-100 1d / funding 2yr / 06-27 gap; operator-authorized this session) + `helper_scripts/research/xs_daily_lane/` Stage A harness + forward-shadow cron. Hard stops: $0 read-only, no order/lease/Cost-Gate/engine-lane, no maker-nogo reopening, H_TREND boundary blocks #3 M-family outcomes. |

## §2 已關閉且不得重複派發的標記

| ID | 狀態 | 最新報告／規格 | 不得重複規則 |
|---|---|---|---|
| `P1-MAKER-FIRST-MATURE-PERP-PIVOT` | NO_GO | `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-06--maker_first_microstructure_feasibility_verdict.md` | Do not reopen mature-USDT-perp maker-first/passive spread capture as a P0/P1 engineering profit lever at current Bybit VIP0 fee tier. Reopen only on changed fee/rebate tier, institution/MM program approval, or pre-registered new event/listing evidence. New-listing screens and M12 adaptive router are separate future PM dispatches, not current order/proof authority. |
| `P2-OFFICIAL-MCP-EXCHANGE-TOOL-REVIEW` | DONE_WITH_CONCERNS | `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-05--official_mcp_exchange_tool_review.md` | Official Bybit MCP and IBKR AI/MCP surfaces may inform taxonomy/UX/capability matrices only. No MCP install, credential, private read, order path, runtime truth, proof lane, or Cost Gate use without future ADR/AMD plus E3/BB-reviewed exact scope. |
| `P2-LEGACY-TODO-RESIDUAL-AUDIT-2026-07-05` | DONE | `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-05--legacy_todo_mag083_phase5_resolution.md`; `docs/CCAgentWorkSpace/PM/workspace/reports/2026-07-05--legacy_todo_remaining_work_audit.md` | `docs/architecture/multi_agent_rework_2026-05-05/AgentTodo.md` and `docs/references/2026-04-10--signal_diamond_db_todo.md` are historical ledgers, not active queues. Future shared-compute fan-out or agent-stage work must be opened from root TODO/ADR/PM dispatch. |
| 【family】`P0-LEARN-*` x8 | DONE_WITH_CONCERNS | commits `ed8c3595`/`f1d1a26c`/`1a8cedb3`/`ed54bf93`/`300ee0af`/`7cfec46e`/`6b93cf2a`/`f2a827c2`; reports `docs/CCAgentWorkSpace/PM/workspace/reports/2026-06-29--learning_*.md` | Reopen only when serving/adjudicator/proof-evidence schema, row-backed fill identity, proof-exclusion, OOS/repeat/control requirements, or promotion review contract changes. Model loading/serving, PG/registry write, bounded Demo execution, Cost Gate lowering, live, and proof promotion still need separate gates. |
| 【family】`P1-RUNTIME-HEALTH-HYGIENE-*` x34 | DONE_WITH_CONCERNS | Full text in `docs/archive/2026-07-04--todo_v738_pre_slim_archive.md` §2 | Reopen only on candidate, GUI RiskConfig, equity, Guardian, standing envelope, or order-shape evidence changes. These markers grant no order/live/Cost Gate/proof authority. |
| 【family】`P0-GUI-RISK-CAP-RESOLVER-*` x4 | DONE_WITH_CONCERNS/ROTATED | Full text in archive snapshot; drift-reconcile manifest sha `97aea4ed...` | Cap/risk parameters must resolve through GUI-backed Rust RiskConfig + accepted equity. Stale artifacts without matching candidate/equity/RiskConfig/Guardian/envelope lineage must refresh. |
| 【family】`P0-STANDING-DEMO-*` x6 + `P0-ALIGNED-ETH-*` x2 | DONE_WITH_CONCERNS | Full text in archive snapshot | `_latest` is a consumption boundary; emitted auth object is review evidence only. Reopen on schema/validator/env wiring/candidate scope/loss-control policy changes. |
| `P1-PROFIT-FIRST-LOOP-REBASE` | DONE_WITH_CONCERNS | `docs/agents/profit-first-autonomy-loop.md` | Do not encode current task details back into stable loop. Reopen only on operator loop-principle change or governance conflict. |
| `P1-TODO-MAINTENANCE-SOURCE-POINTER-DRIFT-CORRECTION` | DONE_WITH_CONCERNS | `docs/CCAgentWorkSpace/PM/workspace/reports/2026-06-26--todo_source_pointer_drift_correction.md` | Reopen when TODO active state again conflicts with source/runtime evidence or `todo-maintenance.md`. |
| `P0-PROFIT-EVIDENCE-QUALITY` | DONE_WITH_CONCERNS | `.../2026-06-26--demo_residual_cleanup_refresh_action_clean_exchange.md` | Reopen only on new exchange inventory/fill attribution/proof-quality evidence. Unattributed/cleanup fills never count as proof. |
| `P0-PROFIT-CANDIDATE-SELECTION` | DONE_WITH_CONCERNS | `.../2026-06-26--profit_candidate_selection_avax_review_packet.md` | Reopen on new evidence that changes candidate feasibility/ranking or when the loop rotates candidate. Maker-first mature-perp NO-GO is a separate marker above. |
| `P1-LEARNING-LOOP-CLOSURE` | SUPERSEDED_BY_AIML_LONG_LIVED_LANDING_V2 | `.../2026-06-24--learning_ssot_decision_packet.md` | `probe_ledger.jsonl` 只屬舊 cost-gate 證據，不是 V2 AIML queue、dataset、fit、registry、serving、policy integration 或 landing state 的 SSOT；這些狀態由上方 typed PG/receipt chain 管理。 |
| `P1-AUTONOMOUS-PARAMETER-PROPOSAL` | DONE_WITH_CONCERNS | `.../2026-06-24--autonomous_parameter_proposal_contract.md` | Learning output is inactive review packet or envelope-permitted parameter update; never direct live mutation. |
| `P0-CURRENT-CANDIDATE-*` closed/no-order family | DONE_WITH_CONCERNS/BLOCKED_BY_LOSS_CONTROL | Full text in `docs/archive/2026-07-04--todo_v738_pre_slim_archive.md` §2 | Historical no-order/admission/lease/BBO packets are not order authority. Future order-capable work needs fresh same-window evidence and fresh E3/BB review. |

## §3 硬門與損失控制

| 門檻 | 規則 |
|---|---|
| 盈利優先 | 追求扣除 fee/slippage 後的真實風險調整淨 PnL。流程 gate 用於防止失控虧損與假證據，不得取代盈利這個最佳化目標。 |
| Standing Demo 授權 | Operator 已給予推進 Demo loop 的廣義方向；Agent 必須把它轉為結構化、runtime 可讀的 loss-control envelope，不得反覆重問或只產生 defer audit。 |
| 損失控制 | 自主交易動作必須位於 max loss、max notional、max attempts、max concurrency、kill-switch 與 reconstructability 上限內。擴大上限必須經過審核的 envelope 變更。 |
| Runtime/order 路徑 | Order-capable 動作仍必須經 Rust authority 與 Decision Lease；禁止未經審計的直接 order path。 |
| Cost Gate | 不得降低全域 Cost Gate。使用 candidate-scoped bounded Demo 證據與 outcome review，判斷某路徑是否值得擴量或變更。 |
| 證明 | 盈利證明必須具備 candidate-matched fills、實際 fee/slippage、matched controls、execution realism、proof-exclusion pass 與 repeat/OOS 路徑。 |
| Live/mainnet | 仍不在本方案範圍。Demo 證據只能保留與 live 相關的 lineage，不能證明 live activation 或 live profit；兩者分別需要獨立 gate，以及 activation 後的真實 live fills。 |

## §4 交接命令

```bash
git -C /Users/ncyu/Projects/TradeBot/srv status --short --branch
git -C /Users/ncyu/Projects/TradeBot/srv rev-parse HEAD origin/main
ssh trade-core 'cd /home/ncyu/BybitOpenClaw/srv && git rev-parse HEAD origin/main && git status --short --branch'
cat /Users/ncyu/Projects/TradeBot/srv/TODO.md
ssh trade-core 'sha256sum ~/BybitOpenClaw/var/openclaw/cost_gate_learning_lane/bounded_demo_probe_soak_plan.json ~/BybitOpenClaw/var/openclaw/cost_gate_learning_lane/standing_demo_operator_authorization.json'
ssh trade-core 'systemctl --user is-active openclaw-trading-api openclaw-watchdog; pgrep -af openclaw-engine'
ssh trade-core 'tail -n 8 ~/BybitOpenClaw/var/openclaw/boot_history.jsonl'
ssh trade-core 'crontab -l | awk '\''NF && substr($1,1,1)!="#" {count++} END {print count}'\''; crontab -l | grep OPENCLAW_EXPECTED_SOURCE_HEAD= || true'
# 刻意省略直接 psql；請使用未來經批准的 LR0A
# local-socket/read-only-identity Adapter，以及其 hash/TTL/platform-bound receipt。
```

**維護契約**：V2 的執行排程與狀態投影由 `docs/agents/ai-ml-landing-delivery-protocol.md` 與 `docs/execution_plan/ai_ml_landing/PROGRESS.md` 管理；它們不授予 normative/effect authority。權限仍只來自 accepted ADR/AMD、明確 Operator 決定與治理 Adapter。`docs/agents/profit-first-autonomy-loop.md` 與舊 WP4 queue 只作歷史輸入，不再是 active AI/ML 派發。根 TODO 只保留兩個 active umbrella：P0 長效修復與 P1 端到端落地。
**自檢**：S0=`PROGRAM_ADOPTED`，S1=`S1_CLOSED`；S2 七個 narrow source seams 已 landed，但這只證 source contracts。九個 S2E 拆分包完成 5/9；current production effect=0/6，Linux learning unit/install roots 均 absent，S2 未關閉。唯一 ACTIVE row=`S2E.2b-2`；WP1-WP5、五個已完成 S2E 包與舊 Operator packet 不可重派/執行。S2E.2b-2→S2E.5 完成前不得宣稱 packet-ready；完成後仍須 fresh authority 與真實 runtime receipts。整個 AIML terminal final sync 仍在 S8.4，S8.5T/S8.5NC 證實 runtime，S8.6/S8.NC 才寫唯一 terminal WORM receipt。
