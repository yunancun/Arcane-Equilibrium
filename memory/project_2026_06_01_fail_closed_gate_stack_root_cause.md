---
name: project_2026_06_01_fail_closed_gate_stack_root_cause
description: 2026-06-01 代碼審計找到 6 個 fail-closed gate（程式層真實但多為次要/潛伏）；但 Phase-1 Linux runtime 查驗【推翻】了「gate 棘輪是主因」的結論——cost_gate 零誤殺正確阻擋真負 edge，真問題回到「為何已實現 edge 普遍為負」(a 無alpha/b exit-drag/c 成本)，待 QC 樞紐診斷
metadata: 
  node_type: memory
  type: project
  originSessionId: 07386332-55ba-43c0-a468-02f0f16dd863
---

> ⚠️ **2026-06-01 PHASE-1 RUNTIME 修正（最重要，凌駕下方代碼審計推論）**：本檔下半部「gate 棘輪是主因/成本牆是假象」的結論被 Linux runtime 只讀查驗**大幅推翻**。實測：(1) cost_gate 14d 拒絕 90.5%，但被拒 estimate **100% 為真負**(avg −13bps，範圍 −20.6~−5.4)、**0 筆正-edge 誤殺**（主會話親驗 SQL）→ cost_gate 是「正確阻擋真負 edge」非「錯殺好交易」；(2) H0 freshness gate 81M+41M checks **0 blocked** → root #2/#6b H0 部分**證偽**；(3) 驗證歸零(root#1) live JSON 實測殺傷≈0（只 4 cell n≥30 全真負、被歸零正 cell 全低樣本走探索）→ **潛伏非主兇**；(4) edge_estimates 205 cell `validation_passed=0`、只 2 cell shrunk>0、`runtime_bps>0`=0；(5) 各策略 demo 14d：grid 105(+0.108)/ma 72(**−0.437 主虧**)/bb_reversion 12/bb_breakout **2(OI gate 確認)**。**真問題回到：為何幾乎每個 cell 已實現 edge 都負？三選一：(a) 入場信號無 alpha（收斂上一輪 textbook+純OHLCV結構性貧乏）/(b) 入場有 alpha 但被 exit policy 摧毀（cut-winners/let-losers 非對稱 + regime_multipliers H2 對 random_walk 用錯 1.0 倍率）/(c) 成本牆。** operator 拍板「QC 先做樞紐診斷（分解 entry-alpha vs exit-drag vs cost）再設計」。MIT 警告：別放鬆 cost_gate（放鬆只放出已知虧損單）。**教訓：主會話代碼審計階段把 gate stack 過度歸因為主因，是 runtime 查驗擋下的排序錯誤——這正是 operator 堅持「先真實查驗再修」的價值。**

**Phase-1.5 樞紐診斷結果（PM 親跑 PG，beta-controlled，待 QC 正式化驗證）**：用 market.klines 算入場信號 forward return（dir-adj、exit-policy 無關，demo 21d）+ 無條件 beta 基準分解，把 (a)/(b)/(c) 拆開：
- **grid_trading（主力 250 單）= (b) 入場有真 alpha 被 exit 丟掉**：dir-adj fwd_24h +76bps = ~+36 short-beta(88%做空×跌市) + **~+40bps 真 timing alpha**（beta 之外）；空頭擇時 +66（好）、多頭 30 單 −153（爛）。但已實現≈0（+0.108 USD）→ exit policy（grid_close/phys_lock 分鐘級平倉 + trailing 鎖 0.1~0.7 + dynamic stop 吃 −3~−4）在分鐘級截斷、丟掉 4-24h 的 forward return。**可修的程式/設計根因**。caveat：單一 21d 跌市窗，regime-bet 風險待 QC walk-forward 驗。
- **ma_crossover（主虧 72 單）= (a) 信號爛**：1h Buy 後價跌(−22)、Sell 後價漲(+52) = 追高殺低 MA 滯後 whipsaw；修 exit 救不了，需重做/停用。
- **bb_reversion**：+40bps@4h 疑有 alpha 但僅 15 單，待擴樣本。
- 基準：grid 幣 21d 無條件 24h drift −47.5bps（跌市）。
- **⚠️ 穩健性自我修正（第 4 次修正）**：分週看 grid 空頭擇時 dir-adj fwd_24h 完全不穩——wk0 +82/wk1 −128/wk2 +177/wk3 −232，聚合 +76 幾乎全來自 wk2（169 單大跌週做空），跌週賺漲週賠（wk3 漲週 −232）。單幣也離散（INJ −232/DOT +306）。**結論：grid「+40bps alpha」不穩健 = 跌市 short-beta + 高方差噪音，非可利用擇時。「grid 有真 alpha 被 exit 丟掉(b)」不成立。** 57 天 bursty 樣本不足以確立或排除策略 alpha（坐實上一輪「資料深度」限制）。**收斂結論：主因是 alpha 不足/資料不足，非 gate bug；ma 信號真爛(1h whipsaw 72單機制性)較穩；grid 無穩健 alpha；bb_breakout OI gate/qty_zero/regime 接線/root#1 是真 bug 但非「無 edge」主因，修了恢復乾淨測量不產生 alpha。** 待 QC walk-forward/PBO 獨立驗證收斂。
- **QC 獨立驗證 + PM 補查已收斂定案（最終）**：C1 survivorship 證偽（250 單全有 24h future bar、0 丟、median_24h +129>mean +76 廣泛非離群）→ grid 正 forward return 真實但 = beta；扣 4h short-beta 後 grid 純 alpha 僅 ~+5-6bps@4h < 成本 15bps；regime 分桶 random_walk(46)−15.6/mean_reverting(13)+70.6 但 191/250 context_id join 失敗 inconclusive；ma median 4h −11(真負)；bb_reversion median +34/n=15(唯一乾淨正但樣本不足)。QC 機制裁決：grid 走自有 cross 平倉(signal.rs:332-373)+22bps step floor(grid_helpers.rs:169) = mean-revert 收割器持 momentum 信號的 horizon 錯配，「修 exit」修不到(trailing 3.5%≫22bps)；grid Buy −153 因 demo btc_lead_lag 失效(cross_asset/mod.rs:39-43)跌市裸奔接刀；ma KAMA×SMA20+ADX≥25+240s persistence 在 1m 嚴重滯後追高殺低=信號本身負期望停用。**最終結論：現有 57 天樣本無任何策略有穩健跨 regime 扛成本 alpha；cost_gate 正確；4 真 bug(bb_breakout OI/qty_zero/regime 接線/root#1)是 hygiene 非 alpha 來源；修復分 Track A(PA 修 bounded bug 恢復乾淨測量)+ Track B(QC 策略研究：新 alpha 源/驗 bb_reversion OOS/等資料回填)。別放鬆 cost_gate、別把 grid 改長持有(regime-bet 陷阱)。** 與 [[project_phase5_promotion_edge_crisis]] + [[project_2026_05_31_v58_alpha_pivot]] 收斂。
- **「查死窄線索」OOS 時間分割定案（operator 要求，最終）**：regime 分桶(時間最近 dc snapshot join，比 context_id 可靠)+demo+live_demo 28d+median：beta≈0 的非趨勢 regime（mean_reverting 4h drift −5/random_walk +0.5）裡——**grid KILLED**：非趨勢 grid median fwd_4h 兩半 +22.5/+6.4 但 **mean 兩半 −13.6/−30.5（肥左尾，少數巨虧 Buy 側接刀主導）→ 實際必虧**，median 正是假象；trending regime grid −4/−38（該 gate 掉）。**ma KILLED**（信號爛）。**唯 bb_reversion@mean_reverting 存活**：mean+median 兩 OOS 半都正(+20.9/+78.3)且>成本 15bps、thesis 自洽，但 n=27(half2 僅 7)太薄。**最終：唯一可追 alpha 線索=regime-gate bb_reversion 到 mean_reverting(它唯一有效 regime，現在沒 gate 在別處賠掉)+擴樣本+OOS/deflated-Sharpe 驗證；grid/ma 死；4 bounded bug 是 hygiene。** 下一步 operator 計劃：PA/QC/CC 設計(Track A 4 bounded bug + Track B bb_reversion regime-gate&validate)→PM 簽收→不過派 subagent 核查。
- **PA/QC/CC 設計 + PM 簽收完成（2026-06-01）**：三方收斂。CC=B Conditional APPROVE（0 BLOCKER/0 硬邊界/0 安全不變量削弱，6 條 E1 hard-guard，P0 兩條：#2 qty 首選 skip-not-reject 不得突破 P1 cap、#4 只改 demo cost-gate、cost_gate_live byte 不動+測試鎖死）。QC=grid REJECT(停 active 留 dormant)、bb_reversion CONDITIONAL accumulate-only(promotion 門檻 ≥40 RT/≥8 cluster/DSR PSR≥0.95/硬 4h time-stop/taker 成本；bb_reversion 用 MARKET 單)。PA=技術方案 file:line+runtime 全驗，A-1 OI 解耦(WS 確有餵 OI、根因是 25-cohort<40+雙 fail-closed)/A-2 skip-not-reject/A-3a regime_1h writer+A-3b vocab arm(BLOCKED 待 QC 定乘數,不擅設風控數值)/A-4 移除 Python 歸零讓 Rust demo/live 雙路徑接管(當前零傷害 n=5<min_n)/B regime 硬 gate(leak-free 驗,accumulate-only)。**PM 簽收=CONDITIONAL PASS**：linchpin 親驗解除(regime_5m=decision_context_producer.rs:160=indicators.hurst.regime=gate 讀的同一 label,無 train/serve skew,OOS 有效)；A-1/A-2/A-3a/A-4/B 准入 E1 Wave1(最大並行 4)；A-3b held 待 QC 定乘數；Track B accumulate-only 非 promotion 需 passive-wait healthcheck+review date；高風險 A-1/A-4/B 強制 A3+E2+MIT(leak)核驗。**誠實預期：此 batch 是 hygiene+追一條薄線索，本身不產生 alpha；修後 bb_breakout 恢復交易(可能也 alpha 貧乏待測)、BTC 噪音 reject 清除、regime 出場校正、bb_reversion 在唯一可行 regime 累積樣本待數週後重驗；系統極可能仍無穩健 alpha。** 未授權 E1 實作，待 operator 拍板。
- **E1 Wave1 實作 + regime 設計複查（2026-06-01，operator 追問 regime 接線觸發的修正）**：E1-1 完成且 green（A-1 OI 解耦：trait `requires_oi_panel()` default-false + bb_breakout override + Option<f64>，flag=true fail-closed 保留；A-2 qty skip-not-reject 移除兩污染源不碰 P1 cap；cargo test 3694 passed/0 failed，無 commit）。E1-4(B bb_reversion regime 硬 gate) 實作中。**operator 追問「regime 為何零消費者/是否接線 bug」→ 追到 2 個重要修正**：(1) **regime_1h = 死 schema 欄位**（V003 建欄但 Rust DecisionContext struct 無此欄、producer 不寫、全庫零讀取；higher-TF 角色其實由 ma_crossover 內部 higher_tf_sma EMA 擔當；策略入場直接讀活指標 ind.hurst.regime 不讀 DB regime）→ A-3a 確認延後（cosmetic 無 consumer）。(2) **A-3b regime_multipliers vocab mismatch 是誤判，撤銷（DROP）**：`derive_regime`(pipeline_helpers.rs:599) 是翻譯層，餵 regime_multipliers 前把 Hurst vocab(mean_reverting/random_walk)→ADX vocab(trending/ranging)，故到達的永遠是合法 key、regime-aware 出場倍率正常運作（mean_reverting→ranging tp0.7 快速止盈恰合 bb_reversion 短 horizon）；最後 grep 確認策略/exit_features 無任何路徑把原始 Hurst regime 傳進 multiplier/stop fn。**教訓：孤立看 regime_multipliers 會誤判，必須追 derive_regime 翻譯層——PA/QC/CC/PM 都曾部分採信此誤判，operator 追問擋下。** 實作批次淨縮為 A-1✓/A-2✓/B(進行中)/A-4(待) = 3 真 hygiene + 1 alpha 線索；兩個 regime「修復」(A-3a/A-3b) 嚴查下蒸發。
- **alpha batch 全核驗通過，待 operator deploy（2026-06-01）**：4 fix 全實作 green（A-1 bb_breakout OI 解耦 trait requires_oi_panel default-false / A-2 qty_zero skip-not-reject 不碰 P1 / B bb_reversion regime 硬 gate accumulate-only / A-4 移除 james_stein:319-321 歸零靠 Rust demo-live 雙路徑）。核驗鏈：**E2 對抗 4/4 APPROVE（CC 6 hard-guard 全達）→ MIT B leak-free PASS（regime_5m=1m misnomer，與 gate 讀的 ind.hurst.regime 位元級同源，我 OOS 用對 label，QC 風險A 徹底解除）→ E4 regression PASS（Rust lib 3702/0、+10 新測試無退化；workspace 1 fail = stash-baseline 證明 pre-existing Mac-debug latency artifact 非本批；跨語言 1e-4 零風險）**。未 commit/deploy（HEAD 2e809b96）。A-2 skip e2e Mac 不可行（MODULE_NOTE 禁 scaffold）→ 改 Linux-replay 規格給 QA post-deploy。**4 deploy-gated 運行時驗證交 QA**：A-4 下輪 cron runtime_bps 不歸零+成熟 cell demo 放行/live reject；A-2 skip 真路徑 counter++/0 reject row；B 只在 mean_reverting fire+累積；A-1 bb_breakout cohort 外幣恢復交易。**誠實重申：此 batch=hygiene+啟動 bb_reversion 累積，本身不產生 alpha，系統極可能仍無穩健 alpha。** MIT 標註：bb_reversion 本最薄，gate 後累積到 ≥40RT ETA 長，可考慮 demo 放寬加速（需 Linux 量化 mean_reverting regime 佔比）。部署=commit+push+Linux restart_all --rebuild（operator-gated）。V126 schema 清理排在此部署之後。
- **已部署且驗證（2026-06-01）**：operator 授權部署全 4 fix。commit `324001c3`（僅 12 code 檔，未吸平行 session 的 meta-doc；2 個 benign 文檔 commit b3f8a02c/b54d2d2f 隨行）push origin main。Linux ff-only pull 至 324001c3，跑 `build_then_restart_atomic.sh`（flock build window 防 watchdog/multi-session race）：cargo release build 40s（pre-SHA 30adb40c→post f1851df）→ restart_all --engine-only --keep-auth → **內建驗 /proc/1655039/exe SHA == post-build SHA f1851df（運行引擎=新 binary）**。部署後獨立驗：新 PID 1655039 alive、舊 968350 歿、engine.log 持續寫、無 ERROR/panic/halt、watchdog 穩定、demo+live alive 未 paused。**DEPLOY-ATOMIC-VERIFIED。** 注意 binary 名是 `openclaw-engine`(連字號)非 underscore（grep 易錯）。post-deploy 待辦：QA 驗 4 個 deploy-gated（A-1 bb_breakout cohort 外幣恢復交易/A-2 skip counter++/A-4 下輪~3h cron runtime_bps 不歸零/B bb_reversion 只 mean_reverting+累積）；Track-B 量化 mean_reverting 佔比估 ≥40RT ETA（可能 demo 放寬加速）；V126 schema 清理現已 unblock。**誠實重申：此批是 hygiene+啟動累積，不產生 alpha；系統極可能仍無穩健 alpha，真 alpha 缺口是後續策略/特徵研究。**
- **alpha 來源研究路線圖（QC，2026-06-01，operator 轉攻真問題）**：接上 AEG 主線排序。成本牆 SSOT：BTC/ETH taker ~15bps/top-alt 20/mid-alt 35/maker(100%fill) 6-8。資料現況(V125 packet Linux 反射)：klines 1.74M/143sym/**57天**、funding 25sym/180d、OI 160k/25sym、long-short 13.7k/25sym。排序：**①bb_reversion@mean_reverting 擴樣本——已部署(QC 排第一但讀 stale 未-commit 報告，實際已 deploy)，現為 30d 累積 watch，需 n≥200 同-regime 跑 PSR(0)≥0.95+block-bootstrap CI；風險:confluence 同向使增量 fills 主靠硬否決+None fail-closed,累積恐慢,30d 後 n<100 須延長/放寬非-regime 條件**。②**多日 trend/TSMOM——唯一「逃逸機制已驗(abs-move 824bps/成本占 3-4%)、解 gate 後數天出新-edge 定論」的最高 ROI 可行線**，卡 operator scope:(i)開 V125 儲存實作 scope(E2/E4/MIT review+dry-run 後)(ii)開歷史 kline backfill writer+endpoint 任務=AEG-S1 下一步。③listing fade(可達性驗 alpha 未驗,adverse+6.6bps,collector 卡,~Q4)。④oi_delta ensemble(訊號 leak-free 但 1m standalone 1.9-2.9bps≪成本,需先有承載策略)。⑤funding carry(break-even 160%APR>2024bull 119%,regime-dormant,demo 無 spot lending,ADR-0046 未 Accepted)。REJECT:cross-asset pairs(不協整)+cash-carry(demo 結構不可行)。全表 0 bull-only/0 survivorship;56-57d 窗結論一律標樣本不足不可晉升。**operator 待決:是否推進候選 2(解 V125+backfill scope)= 最快新-edge 路徑。**
- **operator 拍板(2026-06-01)：推進候選 2(多日 trend)——派 E2/E4/MIT review 既有 V125 儲存 migration-design(in-scope 安全步,不需新 gate)。且指示「做完後逐個自主完成 3-5,不再每步問」。**自主推進候選 3→4→5,各推到 gate/極限就停呈報(非假裝完成 gated 工作)。誠實 gate-reality：#4(oi_delta ensemble)結構性依賴候選 1/2 先產 validated 承載策略,天然排後;#3(listing)Gate-B probe scope 觸資料攝入需 operator approve + alpha 定論~Q4(等 n≥30 真上市窗);#5(funding)revive 需 operator 把 ADR-0046 Proposed→Accepted + 新 AMD + 接受 demo 無 spot-lending 的 directional regime-bet 定位。候選 1 bb_reversion 已部署=30d watch。
- **V125 儲存設計 review 三方完成(2026-06-01)= APPROVED-WITH-CONCERNS(0 BLOCKER)**：E4(dry-run 計劃 PASS,11 first-apply+4 second-apply no-op 斷言,5 硬 gate)+E2(對抗審 5 CONCERN,最重 C-3 data-col 缺 NOT NULL/parse-reject→既有 parser 0.0-default 致 silent fake-zero PIT 污染、C-5 compress_segmentby 未定+多 run 同(sym,ts)重複計數)+MIT(實證 packet §1 高度準確、3 must-fix)。設計地基健全可進 E1,待 operator 開 impl scope。**MIT 揪出候選 2 戰略現實(V125 單獨解不了)：無原生日線 timeframe(只 1m-4h)→需回填 interval=D(~2 req/sym 快)或 4h→1d resample;klines 多數 sym<30 天(avg 14.8/僅 5 sym≥55)須大回填;PIT universe 僅 25 天血緣(onset 2026-05-07)→早於此回測 survivorship-contaminated(緩解:多日 TSMOM 用固定流動 universe 可降此風險)。** 上一輪「~125 req/數分鐘」只對日線成立(1m 深歷史 ~260 req/sym 差 130×)。候選 2 operator-gate:(i)開 V125 SQL impl scope (ii)開日線 backfill writer+endpoint scope=最快路徑。**E1 收斂契約:C-3 data-col NOT NULL+C-5 segmentby+accepted-run filter+L2 sqlx-path double-apply 冪等+L3 klines retention exactly-1-job+L1 避 compressed-twin。** 接著 operator 指示「逐個完成 3-5」用 Workflow 編排候選 3/4/5 研究設計。
- **候選 3-5 研究設計完成（2026-06-02，Workflow 3 QC parallel；synthesis agent 中 result-capture bug 只回顯 prompt，PM 從 transcript 親抽 3 輸出手動整合）**：**#3 listing fade = PROCEED-WITH-CONCERNS to operator scope-gate**——成本牆正確側(單move 500-1500bps/成本<5%)，但 3 阻塞(0 真上市窗bars/+6.6bps adverse markout toxic/無出場模型)皆研究設計可治需 ~Q4 forward 樣本。根因 scanner_config.toml:39 min_turnover 50M + ticker-driven 訂閱致訂閱集 disjoint 上市集。交付：Gate-B isolated artifact-only probe(獨立進程/public WS/不接 production/只落 artifact，capture_lag≤5min 硬驗收，雙軌 kline.1+publicTrade 捕 sub-minute pump) + 出場模型(關鍵洞察:maker-fill 88% 不是 edge 是 toxic reachability，entry edge 不存在、edge 全在 exit timing；進場要求 trade-flow imbalance<0 才下被動空 + 三閘出場) + alpha-test 協議(n≥30 真捕捉)。operator-gate:probe 觸資料攝入需 approve。**#4 oi_delta ensemble = REVISE 設計就緒但執行 blocked-on-dependency**——融合特徵需候選 1 或 2 先 validated 承載策略才有融合對象，無 operator 決策可立即解(downstream)；交付 IC/IR 融合協議(IC>0.05留/ρ≥0.7去冗餘/IR≥20%提升/Bonferroni+DSR) + 多日 horizon 變體(oi_delta_1h vs 4h-1d IC，共用候選2 backfill 依賴)。**#5 funding carry = CONDITIONAL gate-blocked at ADR-0046 Accept**——**break-even 重設計關鍵數學:現 amortized 22/10000/1.5=14.67bps/8h≈160% APR，其中 12.5bps funding variability 佔 57% 應從固定成本移到收益σ→N=9(3day hold) break-even 降至 ~12% APR(落入 2024 bull BTC 119% 內可fire)**；但 demo 無 spot lending → 收 funding short = 單腿 directional bet 非中性 carry(PnL=carry+(−price_return)，bull premium 時價漲吞 carry=V2 死因經濟本質)，回測須並列 carry-only vs net-of-price-leg(只報前者=bull-only AEG 不可晉升)；operator-gate:ADR-0046 Proposed→Accepted + 新 AMD + V117 + 5-gate/Stage0R，需 ~月低turnover 樣本。**operator 決策優先序:①候選2 開 V125 SQL+日線 backfill scope(最快) ②候選3 approve Gate-B isolated probe scope(低風險啟動捕捉,~Q4 alpha) ③候選5 ADR-0046 Accept 決策(regime-dependent,月級,demo-limited) ④候選4 無需決策,候選1/2 validated 後自動解鎖。**
- **修復方向（待 QC→PA→CC→PM 簽收）**：grid 改 exit/holding-horizon 捕獲 alpha（不放鬆 cost_gate）+ 評估禁 grid Buy 側；ma 停用/重做；3 個 bounded bug（bb_breakout OI gate / qty_zero BTCUSDT 精度無 min-notional floor / regime_1h 空+regime_multipliers key mismatch）。operator 流程：QC 樞紐診斷→PA/QC/CC 設計→PM 簽收，不過則派 subagent 核查根因。

---

（以下為 2026-06-01 代碼審計階段的原始推論，**已被上方 runtime 修正部分推翻，保留供追溯**）

2026-06-01 operator 要求嚴格代碼審計「6 週無 net-positive edge 的程序層根因」，明確要求**不被上一輪 [[project_2026_05_31_v58_alpha_pivot]] / [[project_2026_05_31_funding_short_structural_doa]] 的「成本牆+資料深度」結論誤導**。獨立審計（主會話親驗 + 9 個並行對抗 sub-agent）代碼層發現：6 個 fail-closed gate 結構（代碼鏈屬實，但 runtime 證明多為次要/潛伏，見上方修正）。

**元根因**：系統有 ≥6 個獨立進場/sizing/選樣 gate，每個都用「系統自己劣化的已實現統計」或「不可能達到的驗證門檻」判斷要不要交易，命中即歸零/阻擋，**全無 exploration floor、無 decay/恢復出口**。56 天冷啟動下一切「不確定」→ 全部預設拒絕 → 只漏網最差的 cold-start/exploration 單 → 負 edge → 回饋收緊 gate。會擋掉**已知有 alpha 的好策略**（非「正確拒絕爛信號」），故是因非症。上一輪量的是漏網劣質單的已實現 edge → 因果倒置成「信號太弱」。

**6 個機制（全代碼級驗證，file:line）**：
1. **PRIMARY 端到端親驗**：未過驗證的正 edge 被歸零。`james_stein_estimator.py:319`（`if not validation_passed and runtime_bps>0: =0`）→ `:540` 寫進 edge_estimates.json `runtime_bps` → `edge_estimates.rs:149`（Rust 優先讀 runtime_bps 進 shrunk_bps）→ `intent_processor/gates.rs:177`（`0.0>0.0`=false 跳過「demo 放行未驗證正 edge」臂）→ `:216` 當負阻擋。validation 需 ≥120 天（`edge_estimate_validation.py` wf_train90+test30×2windows），系統 56 天 → `validation_passed` 恆 false → 所有 n≥30 成熟正 edge 全擋。gates.rs:178-181 註釋自警「fail-close unvalidated-positive 會重現 Phase 5 99.98% reject 死循環」，Python 歸零正好從另一路徑重建此死循環。
2. **bb_breakout OI 硬 gate**：`enable_oi_signal=false`(demo 預設) 下信號不用 OI 卻被 OI panel 雙重硬 gate（`step_4_5_dispatch.rs:378` + `bb_breakout/mod.rs:481`）；missing/stale/幣不在硬編 25-cohort(<universe 40)/NaN → 0 單。
3. **regime**：(H2 親驗) `risk/regime.rs:23-48` regime_multipliers 表只有 trending/volatile/ranging/squeeze，Hurst 產出的 `mean_reverting`/`random_walk` 落預設 1.0/1.0/1.0；(H1) `risk_config_regime.rs:88` hurst Phase-B `enabled=false` → 原生寬帶 0.60/0.40 → 大量歸 random_walk → ma_crossover 要求 Persistent(H>0.6) 飢餓。
4. **funding break-even 鎖死**：`funding_harvest/mod.rs:154` Gate B 真實要 fr>135% APR（`fr·10000−37/3>0`）vs 宣稱 5% APR，超 BTC funding 物理上限 → active 後永不入場。funding_arb 同型 124% APR（dormant，G-2 n=13/0勝/−36.76bps 疑即此）。
5. **Kelly 自我歸零**：`ml/kelly_sizer.rs:225` + `router.rs:437`，cell ≥50 筆後用劣化 realized stats 算 Kelly≤0 → qty=0 拒單，FIX-27 移除 1% floor → 負期完全停單無法翻身。
6. **scanner 永久排除 + H0 freshness**：`scanner/scorer.rs:800` exploration_only/market_gate 全中→symbol 踢出 universe→0 訂閱→0 fill，無 decay 永久排除；`h0_gate.rs:384` `max_data_age_ms=1000` → last tick ≥1 秒舊即硬 block 整 tick、零審計列 → 長尾低流動幣常態被擋。

**已證偽（非病因）**：手續費重複計數（realized_pnl 是毛利 `fill_engine.rs:300`，Bybit closedPnl 全庫零消費僅 GUI 上色，親驗）／信號方向寫反（4 textbook 策略方向全對）／回測成本高估（backtest_engine.py 是 stub，textbook −11~−42bps 是真實 live-demo 成交非回測）／edge 單位符號錯（全鏈一致 bps）。

**待 Linux 只讀確認以排序殺傷面**（Mac edge_estimates.json 是 2026-04-20 stale 單格、grand_mean −45.7，不算數）：當前 edge_estimates.json cell 分佈(runtime_bps=0/n≥30 數)／h0_gate.stats.blocked_* 佔比／reject-code 直方圖／regime 字串分佈／Kelly qty=0 拒單率。

**修復方向**：非散修，是架構級——引入一致的 demo 探索地板 + decay/恢復貫穿所有 gate，讓系統能 bootstrap；逐一修 6 點。觸及進場/sizing/執行 gating → 高風險需 CC 合規審查 + PA 整體設計。**關鍵實驗**：修 gate 後讓策略用真實信號跑 demo，才能第一次真正測到策略真實 edge（被 gate 卡 6 週，至今不知策略到底有無 alpha）。operator 2026-06-01 選擇「先擴大審計再動手」，審計已完成，待決定是否派 PA。

全文裁決見本次會話；證據 file:line 如上，全部 read-only。


---

## [index-archive 2026-06-10] 原 MEMORY.md 索引條目全文(壓縮索引前歸檔,內容為當時點狀態)

- [6 週無 edge 根因調查 (2026-06-01) — Phase-1 runtime 推翻 gate-棘輪論](project_2026_06_01_fail_closed_gate_stack_root_cause.md) — 代碼審計找 6 個 fail-closed gate，但 **Linux runtime 查驗推翻「gate 棘輪是主因」**：cost_gate 14d 拒 90.5% 但被拒 estimate 100% 真負(avg −13bps)、0 誤殺正 edge（主會話親驗 SQL）→ 正確阻擋非錯殺；H0 81M+41M checks 0 blocked（root#2/#6b 部分證偽）；驗證歸零 root#1 殺傷≈0（潛伏）；真問題回到「為何已實現 edge 普遍負」= (a)無入場alpha/(b)exit-policy 摧毀真alpha(cut-winners/let-losers + regime_multipliers H2)/(c)成本，operator 拍板 QC 先做樞紐診斷再設計；bounded 真 bug：bb_breakout OI gate(14d僅2單)/qty_zero BTCUSDT 精度(13.9萬噪音 reject)/regime_1h 100%空+key mismatch；證偽手續費重複計數/信號反向/回測高估；**教訓：代碼審計易過度歸因 gate，runtime 查驗擋下排序錯**
