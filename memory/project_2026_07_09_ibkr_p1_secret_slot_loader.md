---
name: project_2026_07_09_ibkr_p1_secret_slot_loader
description: IBKR stock_etf 只读接入 P1 SecretSlotLoader + P2 gate producer 已 land（皆 dormant scaffold）；含 OPENCLAW_SECRETS_DIR base-path 陷阱、stat-only 裁决、cargo-check 盲区教训、P2 approval-model 裁决 + 2 gating tickets
metadata: 
  node_type: memory
  type: project
  originSessionId: 4243f6d9-4e38-49af-8604-412777c6d559
---

IBKR `stock_etf_cash` 只读接入（腿②后端）P1 SecretSlotLoader 2026-07-09 landed `3217e94b4`，Mac/origin/Linux 三端同步。链：PA 设计 → E3 安全(0 CRIT/0 HIGH/3 MED fail-closed 硬化) → CC 合规(Conditional-Approve-B) → E1 impl → E2 APPROVE(0 blocker) → E4 **Linux cargo PASS**(ibkr_secret_slot 18/0, stock_etf 回归 33/0)。engine **未重启**(dormant scaffold，0 production caller，直到 P5 接 attestation 才 wire)。承 [[project_2026_06_14_cold_audit]] 治理弧。

**最有价值的陷阱 — base-path 语义**：runtime `OPENCLAW_SECRETS_DIR` **未设定(unset)**；bybit 既有码(`read_secret_file`/`authorization_path`/`restart_all.sh`)把该 env 名语义定为 **`secret_files/bybit` 根**(非 secrets root)。故 IBKR loader **绝不 overload** 它，改用**独立 locator** `OPENCLAW_SECRETS_ROOT`(否则 `$HOME`/`$USERPROFILE` fallback) → `/home/ncyu/BybitOpenClaw/secrets/external/ibkr/{readonly,paper}/`；`live/` **永不创建**。AMD-2026-07-08-01 字面写 `$OPENCLAW_SECRETS_DIR/external/ibkr` 被 CC 裁为**非实质澄清(materialization)**→ secrets-root，落在 AMD「Post-Acceptance Clarification」节 + SPECIFICATION_REGISTER；不变量全保持，Operator 仅 ack 无需重批。

**stat-only 指纹裁决**(E3+CC 推翻 PA 的 content-digest 建议)：`secret_slot_fingerprint` = sha256(paper 槽描述符 = filename+mode+len，**不读凭证内容**)；`account_fingerprint_hash` = sha256(trim+upper(paper/account_id))，account_id 是**唯一**被读内容的文件。理由：最小明文读取面 + 贴 AMD「stat」字面 + triangulation 不需凭证 tamper 检测。两指纹为**单一 pub(crate) 纯函数**(P5 复用避免算法漂移)。**paper 槽=身份**：Phase-2 只读 session 是 `BrokerEnvironment::Paper` on 纸面网关 4002(`IbkrApiSessionTopologyV1`)，contract `validate()` 要求 paper PresentHashed / readonly 可选，故指纹取 paper(与 P5 从 `reqAccountSummary` 重算对齐)。

**过程教训 — cargo check 盲区**：E4 Linux `cargo test` 抓到一个 `cargo check` + Mac read-review(E1/E2)都漏掉的编译错——`ZeroizedString`(account_id 明文 RAII 归零 wrapper)**刻意不 derive Debug**(防明文经 Debug 泄漏)，但测试对 `Result<ZeroizedString,_>` 调 `.unwrap_err()`(要求 Ok:Debug) → lib-test 编不过 → 连带阻断全部 stock_etf 测试。根因：`cargo check` **不编 `#[cfg(test)]`**；必须 `cargo test`(或 `cargo check --tests`)。修法 `.err().expect(...)`。**Mac 无 cargo，Rust 验证必走 Linux 且必跑 cargo test 非只 check。**

**多 session 脏树**：本次共享工作树含隔壁 GUI/auth session 未提交 leftover(`auth*.py`/`main_legacy.py`/`login.html`/`test_*`)。纪律：验证走**隔离 git worktree**(从 origin/main 拉，copy 2 文件)；提交必 `git add <具体2文件> && git commit --only`(绝不 `add -A`)。local main 常被并发 session 推进(一次操作间 HEAD 从 fc33dc39→0ae469acb)，push 前 re-fetch + `merge-base --is-ancestor` 确认 ff、绝不 force。

**剩余(Operator-gated，未自动执行)**：真实 readonly/paper 槽创建 = **E3→BB→Operator**(build-checklist：`account_id` 文件=IBKR paper 帐号 token、dir 0o700/file 0o600、父目录 owner-only、无 symlink、`live/` 永不建)。**下一开发阶段 = P2** `phase2_ibkr_external_surface_gate_v1` producer(现 `settings/broker/ibkr_external_surface_gate.toml` status=BLOCKED)→ 密封 immutable PASS。**G4 首次外部接触**(健康/server-time 读=外部接触，不豁免)需 **Operator 一次性批准 + BB + E3**。不变量全程保持：只读/零真钱/live·tiny-live 结构性拒绝/无 order-write/无 fake-success/Rust-only/live absent/env-var 凭证 fallback 禁/Bybit 不变/无 DB migration。

---

## P2 external-surface gate producer 已 land（2026-07-09 `b89c7b2d8`，operator 批准继续 P2 后）

engine 新 `ibkr_phase2_gate_producer.rs`（1359L）：组装 `IbkrPhase2GateArtifactV1`(policies TOML+allowlist+gate+P1 secret-slot contract+topology) → validate + **verify 重算 hash**(因 validate 只验 shape) + triangulation 三绿 → write-once seal immutable PASS(`create_new`/`hard_link` **绝不 rename** + fsync + 0o400 + refuse-ephemeral)。链：PA→E3(0C/1H/3M)→CC(Conditional-Approve-B+waiver)→E1→E2 APPROVE→E4 Linux PASS(ibkr_phase2 22/0, 全 lib 4381/0 +22 零回归)。**producer 是 fail-closed scaffold：production `phase2_producer_outcome` 永不调 seal**(真槽 absent+无 approval);seal 仅 test 可达。engine 未重启。

**P2 approval-model 裁决(CC)**：G4-unlock 的 seal 用 **owner-only approval 文件 + 6 绑定(option A),非 HMAC(option B)**。理由=read-only/zero-money governance-evidence sign-off,与 live-money `authorization.json`(§四#5)不同轴;同-uid ncyu 威胁模型下 HMAC 密码学增益≈0(key 同 uid 可读)→强推 (B) 是过度 gate。6 绑定:owner-only 0o600+0o700 祖先+symlink-reject / `source_commit==BUILD_GIT_SHA`(anti-replay) / `adr==ADR-0048 ∧ amd==AMD-2026-07-08-01`(contact-授权 AMD,注意 artifact.amd 字段仍 shape-AMD `06-29-01` 常量,两轴) / expiry+freshness(clock 异常不 fail-open) / approval lineage 入 sealed hash / **producer 绝不自注 Operator**。**升级 trigger**:若延用到 order-write/tiny-live/任何 capital exposure → **必升 (B) 签名路径**。immutable 落 `<OPENCLAW_DATA_DIR>/governance/ibkr_phase2/`(持久 owner-only,refuse `/tmp`)。落 AMD「Post-Acceptance Clarification #2」。

**2 张 WAIVER-gated follow-up ticket — 皆已 CLOSED 2026-07-09 `58d0e9749`**(operator 批准提前做;PA→E1→E2 APPROVE→E4 Linux PASS: openclaw_types 403/0+ibkr_phase2 23/0+full lib 4382/0;CC 确认 waiver 提前满足):(T1) `IBKR-P2-SEAL-LINEAGE-FIELDS`(finding-2)DONE=`IbkrPhase2GateArtifactV1` 加 `contact_authorization_amd`(自记 07-08-01)+`approval_lineage_hash`(sha256 of 授权 approval canonical,json!显式序+roles.sort 决定性),两栏入 raw+redacted hash 覆盖域,validate enforce;新常量 `IBKR_PHASE2_CONTACT_AMD` 单一真源。(T2) `IBKR-P2-TRIANGULATION-CROSSCHECK`(finding-1)DONE=移除 `build_phase2_artifact_candidate` 无条件覆写 topo fp,改真 cross-leg equality(seal enforce,f1 mutation test 证);pre-P5 topo=placeholder→production BLOCKED(正确)。**P5 forward-carry(CC,非阻)**:P5 接真独立 topology account-fp 源时须加「两**真**源 triangulation 测试」(secret ∧ session-attestation ∧ sealed-artifact 同一 non-live account fp,per `FeatureFlagSecretAuthMatrixV1`)→ 列入 P5 acceptance。

**教训补**:(1) `validate()` 只验 hash **shape**(is_sha256_hex)非内容→tamper-evidence 必须 producer 自带 verify 重算;`"a".repeat(64)` 都能过 validate。(2) 类型冻结(不改 types crate)约束下,lineage 完整闭合需单独 ticket,不在 producer PR 内硬塞栏位。(3) 角色 defer:BB(P2 非 exchange-facing,E3-waived)、QA(never-seals scaffold 无 e2e 面)、final CC/FA producer 复审 均 defer 到未来 seal-wiring gate(CC 已在 waiver 中审真码)。

---

## B1 只读 TWS 连接器 已 land（2026-07-09 `aedca2291`，operator 拒 live 槽/拒空-API 后选「先建缺件」路径,再拍「只先建 B1」）

**operator 原指令(建槽 API 留空 + 建 live 档 + 直接做 2-4)被 PM push back 挡下**:①**live 槽拒建**(operator 自定不变量 AMD-07-08-01 inv#6 + loader 任何 live entry→LivePresentDenied 会破 readonly/paper 路径 + 改 live 需新 ADR);②空-API 无功能(loader 需真 account_id;且**无 credential-write 后端**、**无连接器**、trade-core **无 IB Gateway**);③G4 首接触物理不可能(缺连接器/gateway/creds)。operator 接受,改选建缺件。

**缺件 A(GUI 凭证写路径)= CC 裁 substantive,BLOCKED pending operator ACK**:新 secret-write 信任面(Operator out-of-band → application-mediated),非 clarification。已起草 **AMD-2026-07-09-01(DRAFT,`9d97e3998`)** 含 10 硬 guard + GET-only→受限 mutating IPC 例外 + P1.5 gate row,**待 operator 接受 + 显式 ACK**。手动 custody(operator/E3 放 account_id 档)是免-amendment 替代,operator 选了走 amendment。

**缺件 B(P3 连接器)= CC build-now 授权**:operator 选「只先建 B1」。**B1 = connect handshake + reqCurrentTime(G4 最小首接触:server-time/health),零新契约**。链 CC→FA(分期:B1 first,B2/B3 需 S3/S5 新 row 契约在 G4 后 P5/P6)→PA→E3(0C/0H/3MED)→E1→E2 APPROVE(post-fix)→E4 Linux PASS(ibkr_readonly_tws_client **22/0**,回归 33/23/18,full lib **4404/0**)。engine `openclaw_engine/src/ibkr_readonly_tws_client.rs`+g4 bin+symbol-audit sh。**惰性 build-now/run=G4**:唯一 `TcpStream::connect` 在 `#[cfg(feature="ibkr_g4_contact")]`,**default build 0 socket 符号**(+ 0-production-caller **DCE** = 真保证,E4 实证符号=0);L2 runtime gate(env APPLY==1→sealed re-verify→G4 approval 文件 PM+Operator 6-binding 镜像 P2)→L3 struct host/port(127.0.0.1:4002,4001/7496 硬拒)。

**关键工程事实**:(1)**engine crate 非 net-free**(net-free 只绑 openclaw_types;engine 已用 tokio::net)→ 手写 TWS wire client 落 engine 合规。(2)**凭证不经 wire**:IB Gateway out-of-band(UI/IBC)认证 paper 账号,socket 只发 client_id → B1 码零 creds;**真 G4 run 依赖 trade-core 有 running+logged-in IB Gateway paper(4002)**=真 G4 operational blocker。(3)**MED-1**:loopback 4002 非 uid-gated(不同 engine 的 0o600+HMAC unix socket)→ 系统「只读」load-bearing 控制在 **Gateway "Read-Only API" checkbox**,非 B1 码;G4 setup 须勾 read-only API + loopback bind + trusted-IP 127.0.0.1。(4)**managedAccounts(msgId 15)**握手必推(含 DU 账号)→ **drop**(never bind/log/serialize);**ERR_MSG(id 4)** info code≥2100(2104/2106/2158…)握手必现于 currentTime 前 → **容忍 drain**,fatal<2100→`GatewayError{code}`(numeric-only)。(5)**wire 硬化**:u32 BE、MAX_FRAME_LEN before-alloc、typed CodecError 零 panic、msgId allowlist、read budget+timeout、fuzz never-panic。(6)**L3 symbol-audit 教训**:release `strip="symbols"` 使 nm 审计 vacuous(fail-open)→ 改审 unstripped debug + fail-closed on inconclusive(exit 5)+ `grep -m5||true` 避 SIGPIPE(exit 141→1)。

**INFO**:production `phase2_immutable_pass_artifact_present()` 恒 false(真槽 absent+pre-P5 placeholder triangulation mismatch)→ G4 run 结构性封死 until seal-wiring。**下一步全 Operator-gated**:A amendment ACK / 手动 custody 放 paper account_id / 起 IB Gateway paper(read-only API) / seal-wiring(需 A? 不,seal 用手动 custody 即可)+approval+G4 一次性批准。TODO IBKR row B1-landed 更新 deferred(别 session v765 rewrite 占 TODO.md 未提交,避免扫入其 uncommitted)。

## 引擎 rebuild+restart 部署 已完成(2026-07-09 23:59,operator 授权 A+B;先前「engine 未重启」已作废)

operator 令「A+B」→ PM push-back 后 E3 deploy 提案 + BB gate + QA e2e,operator 明确接受**跨-session ALR P2-8 notifier 激活**后执行。**`restart_all.sh --engine-only --rebuild --require-clean-build-window`**(显式 export home paths 避 restart_all.sh:45 的 `/tmp/openclaw` default 陷阱)成功:旧 PID 1561777(07-05 binary)→ **新 PID 1983100**(build `26401fbb`,binary mtime 23:59,home paths 正确)。E3 CONDITIONAL-GO 5 前置全满足:①operator ACK ALR notifier;②BB demo book clean(pos/orders=0)+ **probe-on-boot SAFE**(bounded-probe admission 在 adapter 检查前先撞 plan-stale 26.6h>24h + auth-expired 两 fail-closed 闸,15.4h 活引擎 0 probe 实证);③07-05 binary 备份 `var/openclaw/binary_backups/openclaw-engine.pre-ibkr-deploy`(rollback);④delta 2787042d0..HEAD **0 rust 改动**(engine==E3 审过);⑤home-path env。post-restart 验证:fills:0(boot 无单)、Bybit demo WS running=True/disconnect=0、**ALR authority 5 旗标全 false**(notifier 激活零授权,research-only)、API 仍 :8000(--engine-only 未动,mixed-version new-engine+07-05-API 可接受,IPC additive-compat)。

**QA post-restart e2e PASS + 诚实校正(重要,勿过度声称)**:16 端点仍全 honest fail-closed;**真正反映到 GUI 只读面的只有 P0(+P2 一栏)**:①**P0 完整 wired**——policy-status caps 现 = **1000/5000/10000/5/10**(读 `risk_config_stock_etf_paper.toml`)、config_version=1、risk_config_load_error=null、risk_policy.accepted=true(旧 07-05 为硬编 0/default);②**P2** 仅 wire `precontact.rs:58` 的 `phase2_immutable_pass_artifact_present()`(磁盘 sealed re-verify→false,never-seals);③**P1 loader 未 wire 进任何 status handler**(`grep ibkr_secret_slot_loader ipc_server/`=空,仅 P2 producer 内部引用)——authorization/account 显示的 `secret_slot_contract`/`session_attestation` 是**静态 default contract 非活体 loader 输出**(值诚实 fingerprint_present=false 且与「槽缺席」一致,但 provenance=静态);④**B1** socket compile-excluded(`reqCurrentTime`/`g4_operator_triggered_first_contact` 符号=0)+ connector 全静态 dormant。→ **P1/B1 是 compiled-dormant-not-GUI-wired(设计如此,P1→P5 attestation / B1→P4 IPC 才 wire),非缺陷**;PM 不应对外声称「P1/B1 已反映到 GUI」。INFO(LOW):authz `live_secret_absent_or_empty=false` 是 default 未初值(若 P1 wire 应为 true),语境 moot(整体 blocked)。教训:**部署 dormant scaffold ≠ 该 scaffold 已 GUI-wired**;区分 compiled-in vs status-handler-wired。

