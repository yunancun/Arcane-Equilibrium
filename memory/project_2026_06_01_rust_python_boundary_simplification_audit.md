---
name: project_2026_06_01_rust_python_boundary_simplification_audit
description: 全程序通盤審計 — Rust/Python 邊界乾淨 0 誤置 + 精簡空間=少數熱點非膨脹；附 actionables
metadata: 
  node_type: memory
  type: project
  originSessionId: 32c26ae5-1871-403c-899e-a88e33e2f8de
---

2026-06-01 operator 要求通盤分析全程序兩問題：(1) Rust 化後有無把該 Rust 的誤寫成 Python (2) 精簡優化空間。主會話派 PA(權威邊界)/FA(跨語言重複死碼)/E5(精簡膨脹) 三獨立 agent 並行，結論互相印證。

**Q1 邊界：0 真正誤置（HIGH conf）。** 名字嚇人的 Python 檔(`h0_gate.py`/`risk_governor_state_machine.py`/`bybit_rest_client.py`/`guardian_agent.py`/`executor_agent.py`…) 全部 0 個 Rust 熱路徑 caller → 結構上不可能是交易真相層。歸三類：(A) 合法 Python 設計(GovernanceHub 協調/5-Agent 冷路徑/唯讀投影/parse→IPC→format)、(C) 遷移後死碼(`risk_manager.py` 52行shim、`h0_gate.py` gating 半部、Python `bybit_rest_client.py` 下單方法；Rust `h0_gate.rs` 註釋自證「移植自 Python 832行」)、(B) 誤置=**無**。**方法論**：PA 指出只靠檔名+docstring 會誤報 6-8 個(因 Python 檔註釋真寫 "hot path/<1ms SLA")，靠 call-path grep 推翻 → 命中 [[project_2026_06_01_fail_closed_gate_stack_root_cause]] 的「代碼審計易過度歸因」教訓。

**Q1 place_order — 2026-06-01 PA+CC 設計階段更正前述「0 caller」結論(FALSE NEGATIVE)**：`bybit_rest_client.py:801` Python `place_order()` 實有 **2 個生產 caller**=`helper_scripts/clean_restart_flatten.py:137/:178`(reduce-only demo flatten + dust sweep)，經 `clean_restart.sh:271` + `fresh_start.sh:252` 兩個生產生命週期腳本調用。先前 FA/PA grep 只掃 `program_code/` 漏掉 `helper_scripts/` 的 caller。關鍵：此路徑**設計用於引擎停止時**用 REST 平倉(IPC 此時不可用)，Rust 無替代。mainnet 已硬禁(`return 7`)→demo-only，**無 hard-boundary 違規但是真實能力**，直接刪會破壞兩腳本。CC BLOCKER-1：刪除式 diff 必 REJECT。operator 原指示「移除」基於 0-caller 假前提 → 已 push back，建議 **B-1 保留+加固**(client 層加 mainnet fail-closed 斷言)。**教訓:再次命中「grep 範圍不全 → 假死碼」,刪碼前 helper_scripts/ + 動態 caller 必掃**。

**Q1 策略性債**：SM-01/02/04(Auth/Lease/RiskGov) **雙源**(Python `risk_governor_state_machine.py` ↔ Rust `sm/risk_gov.rs` 等)。非誤置(契約允許控制平面 Python + 引擎 Rust 鏡像)，但是手動同步 silent drift 隱患 → 待 operator 決定：接受/加跨語言契約測試/長期單源收斂。

**Q2 精簡：精瘦+少數熱點，非膨脹(E5，FA 印證)。** 全庫只 1 檔破 2000 硬上限(`strategy_ai_routes.py` 2552)，Rust 0 超標。投機性死碼疑點(pipeline slots/v5.8 凍結 stub/dream/cognitive/opportunity) 查證全 load-bearing 有 caller。Rust 大檔(`intent_processor/mod.rs` 1968)內聚不該硬拆(borrow-checker 耦合)。兩個 225K LOC 對半=合理切分非膨脹。
- 真值得做(投報×風險排序)：①`governance_routes.py`(1978，**第一個 route 在 960 行**)抽 autonomy service=低風險同修檔案大小+route 紀律 ②`strategy_ai_routes.py` 抽 closed-PnL 分頁+demo-snapshot ③**16 個統計函數在 w_audit_8b/8c 複製貼上**(formula drift 風險)收斂 ④ PG-connect helper(≥8份)+`_safe_float`(13份)收斂 ⑤`replay_full_chain_routes.py`(1931行/2 route) 下推 `replay/`。根因：`helper_scripts/lib/` 無 Python 共用庫。
- 純清理：README+program_code/README 仍列已刪的 `program_code/{governance,risk_control,trade_executor}/`(文檔漂移)；`backups/trading_ai_pre_phase0a_20260404_180411.dump` 未 gitignore 應 `git rm`+gitignore；~1400 行 Python 可回收(h0_gate gating + reconciliation_engine/hub.reconcile() 僅測試調)**但刪前需 ssh trade-core 確認 0-caller**(Mac 靜態查不到 cron/動態 import)。

agent workspace memory 已各自更新(PA/FA/E5)。全部不擋 live gate=純維護投資,適合 design-freeze 窗口。


---

## [index-archive 2026-06-10] 原 MEMORY.md 索引條目全文(壓縮索引前歸檔,內容為當時點狀態)

- [Rust/Python 邊界 + 精簡通盤審計 (2026-06-01)](project_2026_06_01_rust_python_boundary_simplification_audit.md) — PA/FA/E5 三獨立 agent 印證：(1) 邊界**0 真正誤置**，名字嚇人的 Python 檔(h0_gate/risk_governor_sm/bybit_rest_client) 全 0 Rust 熱路徑 caller=結構不可能是交易真相層；殘留是「該刪死碼」非「該改寫漏網邏輯」；call-path grep 推翻檔名+docstring 誤報 6-8 個(命中過度歸因教訓) (2) **非膨脹**=精瘦+少數熱點，全庫僅 1 檔破 2000(strategy_ai_routes 2552)，投機死碼疑點(slots/凍結stub/dream)全 load-bearing；真精簡=route 紀律(governance_routes 第一 route 在 960 行)+共用 helper 收斂(16 統計函數複製/8 PG-connect/13 _safe_float)+文檔漂移修(README 列已刪 program_code/{governance,risk_control,trade_executor})；**隱患**：Python place_order()→/v5/order/create；**2026-06-01 PA+CC 更正「0 caller」為 FALSE NEGATIVE**——實有 2 生產 caller(clean_restart_flatten.py via clean_restart.sh/fresh_start.sh 引擎停止時 REST 平倉,Rust 無替代,mainnet 硬禁=demo-only),直接刪破壞 2 腳本,CC BLOCKER,改 B-1 保留+加固；**SM 雙源更正**：非對稱,Rust 已是 runtime 權威(governance_hub IPC 路由+event_consumer 寫 lease_transitions),且 drift 已存在(Rust 有 Reconciler/NotificationFailsafeTimeout/7d-cooling,Python 無)→ PA 設計 4a 跨語言 contract-test(先 RED 暴露 drift)→4b spec-codegen→4c 選擇性 Python projection collapse；**operator 決策(2026-06-01)**：place_order=**B-1 保留+加固**(client 層加 mainnet fail-closed 斷言,不刪);SM=**先做 4a contract test**看實際 drift 再定 end-state(4b 留 Python 投影 vs Option2 Rust 唯一)。執行序 P0 contract-test(linchpin,gate end-state 決策)→P1 共用庫+stats→P2 governance/strategy_ai 拆分→P3 place_order 加固→P4 replay→P5 codegen(等 WIP 落地)。WIP 與所有目標檔 disjoint;openclaw_core(SM 所在)不含 WIP 故 cargo test -p openclaw_core 可在 main 直跑;commit --only 隔離;刪碼(D)需 ssh(本 session 不通)gated。**進度(2026-06-01)**:Phase0(b3f8a02c+b54d2d2f docs/dump)+P0(7cefadec SM contract test,E1+E2 獨立重抄三表 0-mismatch PASS)+P1(4e111d52 共用庫 −314 LOC,E1+E2 byte-identical PASS)全 committed 未 push。**4a 關鍵結果**:Rust↔Python SM drift 小且單向(Rust 領先:Reconciler/NotificationFailsafeTimeout/7d-failsafe/active_lock_profit;Python 嚴格落後無衝突),**0 個分歧安全判定**→Option2(Rust 唯一權威刪 Python SM)比初估更可行(Python SM 近乎冗餘)但 P5 仍 gated(engine-down 治理場景分析+ssh+WIP+reconcile 窗口)。**P1 surfaced _n_eff floor bug**(over-PASS bias,已修 ceil canonical,dormant 對現役 caller 0 影響)。E4-Linux regression owed(ssh down)。**已完成並提交 6 階段(8 commits)**:Phase0(b3f8a02c/b54d2d2f)+P0(7cefadec)+P1(4e111d52)+P3(0a16b772 place_order endpoint-keyed mainnet guard,E2+BB PASS)+P2a(0dd055dd governance_routes 1978→1346 抽 autonomy service,E2 AST byte-clean)+P2b(401d7d69 strategy_ai_routes 2552→1541,E2 抓到並修 CRITICAL:跨模塊 caller live_session_account_routes get_live_closed_pnl 漏 3 mandatory kwarg 會 500,修為注入 strategy_ai_routes 同 singleton 行為保留)。**剩**:P4 replay push-down(最高風險,serial-after-P2,ready)/ P5 SM codegen(gated:ssh+WIP+end-state 決策)/ D 死碼(blocked:ssh 0-caller 驗)。所有 E4-Linux regression + P5/D + place_order 真網驗 全 owed on ssh(本 session 不通)。SM end-state 仍待 operator 定(4a 數據已交,rec=Option2)。**2026-06-01 三端同步完成**:Mac=origin=Linux trade-core=401d7d69(push ff 6 commits;Linux git pull --ff-only,未 restart=未 deploy,uvicorn 無 --reload)。**ssh trade-core 其實一直通**——先前整 session 誤判「ssh down」是因 macOS 無 `timeout` 指令,`||` fallback 誤觸;Linux srv=/home/ncyu/BybitOpenClaw/srv。**E4-Linux regression P0-P3 已補**:P0 Rust contract test PASS(authoritative,cargo 3.23s)+92 py sync + P1 31 golden 全 PASS。**新發現(pre-existing infra 洞)**:Linux control_api venv **無 pytest-asyncio**→全專案 @pytest.mark.asyncio 測試在 authoritative Linux box 靜默 skip(現存 sibling test_bybit_closed_pnl_route 11/12 skip 為證);我的 async 測試在 Mac 過、Linux skip——非我引入,但建議裝 pytest-asyncio 補 coverage 洞。P5/D 現已 unblock(ssh 通);Rust SM WIP 已落 324001c3。**2026-06-02 收尾**:async-infra(9afb811a pytest-asyncio==1.3.0+strict,+43 async test 由 skip→run)+P4(344025f9 replay 1931→953 byte-identical)committed,三端再同步=344025f9(10 commits 全程 E1→E2→BB/E4 鏈,push 含他 session learning fix 7c382999/f18676e2 已在 origin)。**D operator 拍板關閉=保留現狀**:驗證證 h0_gate.py(dormant paper H0,paper OFF→H0_GATE=None)+reconciliation_engine(operator 手動對賬 governance route :1102 + 未 spawn 的 ReconciliationWorker,Linux audit 0 invocation)是 dormant 保留能力非死碼,移除=砍能力違「不影響功能」——再次命中「代碼審計過度歸因死碼，runtime+接線查驗推翻」。**SM end-state operator 拍板=Option 2**(Rust 唯一權威、刪 Python SM transition 邏輯、Python 降唯讀投影+留 live-auth 閘+GovernanceHub 控制面)→P5 design-first(PA migration+CC invariant guard→operator sign-off→impl),parity gate=已 commit 的 4a contract test。P0-P4+async-infra+D 全完成,P5 為新階段。**P5 step-i Rust 已交付**(a99bfa1d):治理 SM IPC surface(7 PipelineCommand round-trip+dispatch:3 lease method 契約 byte-match Python client+4 read 投影;fail-closed;additive/dormant flag-OFF;openclaw_core 0 改),E2 PASS+CC PASS A 級(0 hard boundary,single-write-entry 保留)+Mac 3734+16 test+**Linux cargo regression exit 0**+三端同步。CONDITIONAL(step-iii cutover gate 非本 commit blocker):R1=cutover 前 Linux 驗 Rust 寫 auth/risk transition audit 行;C2=kill-switch Python 端強制。**next=E1b**(Python hub IPC 路由+shadow-compute+divergence comparator+4a 擴充)→step-ii→step-iii cutover(operator sign-off gated)→soak 24-48h。**E1b 亦完成(2026-06-02,e6aa5e37)**:Python 權威路由+divergence comparator。E2 初審退回 1 HIGH(comparator 近盲:acquire shadow 在 Step-2 後跑→Rust-grant/Python-deny 被預過濾;release/get echo→always-match)+MED(singleton 未註冊)+LOW;**fix dispatch 連 2 次 socket 中斷留 broken partial**(release over-fire 1 失敗測試);operator 選「先試 A 再 B」,第三次 dispatch 成功:A1 獨立 auth-axis 比對器(is_authorized_via_ipc→E1a governance.is_authorized read,acquire Step-2 之前純觀測解近盲)+A3 release no-opinion 排除。re-E2 PASS(mutation 驗有 bite)+re-CC PASS A 級。committed+三端同步+E4 Linux 133 passed。**step-i 實作鏈全完成,剩 Linux --rebuild+flag-on 24-48h soak(0 divergence)= step-i gate(operator-timed runtime 部署)**→step-ii→step-iii cutover(task #10 Rust auth/risk audit emit + operator sign-off gated)。**教訓:socket 中斷後 agent 自標 task completed 不可信,須查 on-disk 實況(本次抓到 release over-fire 失敗測試+#5 未解);#5 設計微妙者把設計寫死給 E1 只實作,降搖擺**
