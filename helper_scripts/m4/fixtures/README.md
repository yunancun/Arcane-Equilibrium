# M4 Stage 1 fixtures

Reserved 目錄。Sprint 2 末 W2-D MIT 接 cron wire-up 後在此放：

- `ohlcv_known_meanrevert.parquet` — W1-B spec §5.3 cross-language fixture（Python / Rust / SQL 對齊 1e-4 驗）
- `funding_flip_events.parquet` — funding flip event-window 對齊 fixture
- `liquidation_cascade.parquet` — cascade detection fixture
