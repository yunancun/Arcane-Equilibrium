[pytest]
# strict 模式：只驅動帶 @pytest.mark.asyncio 標記的測試，不改變同步測試行為。
# 顯式設定以保證 Mac 與 Linux 回歸機行為一致（不依賴 pytest-asyncio 預設值，
# 預設值未來版本可能改變）。Mac 原本即在 mode=strict（插件預設），此處為 no-op 固化。
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
