# environment_files

此目录仅用于保存环境变量模板、去敏样例或说明文件。
This directory is only for environment variable templates, redacted examples, or notes.

真实本地环境文件不进入 Git。
Real local environment files must not be committed to Git.

真实文件位置：
Real files location:

- ~/BybitOpenClaw/secrets/environment_files/
